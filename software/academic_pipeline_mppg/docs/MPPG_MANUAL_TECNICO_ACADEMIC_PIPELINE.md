---
document_id: MPPG-ACADEMIC-PIPELINE-MANUAL-001
canonical_filename: MPPG_MANUAL_TECNICO_ACADEMIC_PIPELINE.md
project: Software MPPG / Academic Pipeline
title: Manual técnico e operacional completo do Academic Pipeline
status: vigente
effective_from: 2026-08-07
last_revised: 2026-08-07
language: pt-BR
audience:
  - usuários do Academic Pipeline
  - estudantes e pesquisadores
  - mantenedores do Software MPPG
  - revisores de código
  - assistentes de IA
scope: funcionalidades operacionais, usabilidade, configuração, interfaces, fluxos, saídas, diagnóstico, PRISMA, renderização, módulos canônicos e governança documental
revision_control: git
canonical_source_root: software/academic_pipeline_mppg
baseline_branch: master
baseline_commit: 019464a6aefd38acbe346fa1a0d286f68e9e34df
baseline_commit_subject: "docs(mppg): add canonical Academic Pipeline technical manual"
refactor_program_state: closed
refactor_program_progress_percent: 100
legacy_runtime_state: retired
manual_synchronization_policy: mandatory_after_functional_change
mandatory_next_step: none
---

# Manual técnico e operacional completo do Academic Pipeline — Software MPPG

## 1. Função deste manual

Este documento é a referência técnica e de usabilidade do **Academic Pipeline MPPG** no baseline canônico indicado no cabeçalho.

Seu objetivo é responder, em um único lugar, a cinco perguntas:

1. **o que o programa faz;**
2. **como o usuário executa cada função;**
3. **quais entradas, configurações e arquivos participam de cada fluxo;**
4. **quais artefatos são produzidos;**
5. **como o software está organizado internamente para manutenção e evolução.**

A estrutura documental foi inspirada no modelo `UAC_DIRETRIZ_PERMANENTE_NOMENCLATURA_CANONICA_IA.md`, especialmente em sua ideia de fonte canônica estável, baseline explícito, seções operacionais, governança e sincronização documental. O conteúdo funcional deste manual, contudo, deriva do **Academic Pipeline real**, não do software UAC.

Este manual adota um nome canônico estável:

```text
MPPG_MANUAL_TECNICO_ACADEMIC_PIPELINE.md
```

A evolução futura deve ocorrer no Git. Não devem ser criadas autoridades paralelas como `..._v2.md`, `..._final.md` ou arquivos datados para representar a versão vigente.

---

## 2. Baseline canônico documentado

```yaml
repository:
  branch: master
  commit: 9e24ad6db001d56d7334bf3ab97c97a05cce579a
  canonical_software_root: software/academic_pipeline_mppg

package:
  name: academic-pipeline-mppg
  package_version: 0.1.0
  python: ">=3.11"
  console_entrypoint: academic-pipeline
  console_target: academic_pipeline.cli:main
  module_entrypoint: python -m academic_pipeline

refactor:
  state: closed
  progress_percent: 100
  legacy_productive_runtime: absent
```

O commit-base deste manual já contém a publicação dos artefatos canônicos do projeto Atestmed e é o HEAD de `master` utilizado para a presente documentação.

---

## 3. Visão geral: o que o Academic Pipeline faz

O Academic Pipeline é um pacote Python modular para **geração, validação, rastreabilidade e renderização de documentos acadêmicos**. Ele combina:

- configuração declarativa em TOML;
- documentos locais como corpus;
- orientações e prompts;
- bibliografia e DOI;
- geração estruturada por IA quando habilitada;
- modelo documental canônico em JSON;
- validações antes e depois da geração;
- renderização ORG/LaTeX/PDF;
- renderização DOCX;
- perfis e layouts institucionais;
- relatórios de qualidade e conformidade;
- mapas mentais;
- traduções;
- resumos e palavras-chave de papers;
- fluxos de revisão estruturada PRISMA;
- busca externa, triagem e curadoria de referências;
- interfaces CLI, TUI e GUI;
- utilitários de projeto, diagnóstico e recompilação sem IA.

O programa pode ser usado tanto para **gerar um documento novo** quanto para **reutilizar um `document.json` existente**, **recompilar um ORG**, **validar configurações/artefatos**, **gerenciar pesquisa PRISMA** ou **preparar um artigo final a partir de referências congeladas**.

---

## 4. Arquitetura operacional

O caminho público principal é:

```text
academic-pipeline
        │
        └── academic_pipeline.cli:main
                │
                └── academic_pipeline.runtime:run
                        ├── rotas informativas
                        ├── doctor
                        ├── check-config
                        ├── list-profiles
                        ├── institution-compliance
                        ├── DOI manifest
                        └── default_runtime
                                ├── configuração
                                ├── corpus
                                ├── bibliografia
                                ├── PRISMA
                                ├── document_model
                                ├── validação
                                ├── mapa mental
                                ├── ORG/PDF
                                ├── DOCX
                                ├── traduções
                                ├── prompt lock
                                ├── conformidade
                                └── qualidade/manifests
```

A mesma autoridade é alcançada por:

```bash
academic-pipeline ...
```

ou:

```bash
python -m academic_pipeline ...
```

O runtime canônico seleciona deliberadamente a rota apropriada. Não há fallback produtivo para o antigo monólito RC.

---

## 5. Requisitos e dependências Python

O pacote exige **Python 3.11 ou superior**.

Dependências declaradas:

```text
openai >= 1.0.0
pydantic >= 2.0
python-dotenv >= 1.0
pypdf >= 4.0
python-docx >= 1.1
openpyxl >= 3.1
```

Alguns fluxos também dependem de ferramentas externas disponíveis no ambiente, especialmente quando há compilação LaTeX/PDF, renderização de mapa mental ou interfaces opcionais.

### 5.1. Instalação do pacote em ambiente de desenvolvimento

A partir de `software/academic_pipeline_mppg`:

```bash
python -m pip install -e .
```

Após a instalação:

```bash
academic-pipeline --help
```

e:

```bash
python -m academic_pipeline --help
```

devem apontar para a mesma superfície pública.

### 5.2. Recursos incluídos na distribuição

O `pyproject.toml` inclui explicitamente como dados de pacote:

- exemplos TOML;
- assets FGV;
- templates DOCX e ORG;
- estilos LaTeX;
- prompts institucionais;
- validators TOML;
- prompts globais, de documento, pesquisa e PRISMA;
- templates CSL;
- recursos misc necessários ao runtime.

---

## 6. Quick start

### 6.1. Ver a ajuda

```bash
academic-pipeline --help
```

### 6.2. Diagnosticar o ambiente

```bash
academic-pipeline --doctor
```

### 6.3. Listar presets TOML

```bash
academic-pipeline --list-toml-profiles
```

### 6.4. Listar perfis institucionais

```bash
academic-pipeline --list-institutions
```

### 6.5. Explicar o perfil FGV

```bash
academic-pipeline --explain-profile fgv
```

### 6.6. Validar um TOML antes da execução

```bash
academic-pipeline --check-config --config caminho/projeto.toml
```

### 6.7. Executar um projeto configurado

```bash
academic-pipeline --config caminho/projeto.toml
```

### 6.8. Abrir a Central Operacional FGV

```bash
academic-pipeline --tui
```

### 6.9. Abrir o wizard de TOML

```bash
academic-pipeline --init-toml
```

ou com preset:

```bash
academic-pipeline --init-toml --toml-profile atividade_local_fgv
```

---

## 7. Formas de interação

### 7.1. CLI

É a superfície pública mais completa e previsível. Use quando:

- automatizar fluxos;
- executar por shell;
- validar em scripts;
- trabalhar com TOML já existente;
- depurar uma etapa isolada.

### 7.2. TUI — Central Operacional FGV

A TUI usa `prompt_toolkit` e oferece navegação guiada, seleção de projetos, confirmação antes de operações custosas, campos de caminho com conclusão e logs operacionais.

Fluxo visual declarado:

```text
escolher categoria
→ configurar
→ conferir
→ validar
→ gerar
→ revisar
```

A TUI mantém estado em:

```text
app_bundle/.academic_pipeline_tui_state.json
```

e logs em:

```text
app_bundle/output/tui_logs/
```

Ela separa explicitamente:

- **TOML global da pipeline**, usado nos fluxos gerais;
- **estado/TOML do artigo PRISMA**, usado no fluxo robusto de artigo final.

### 7.3. Perfis guiados da TUI

A Central Operacional expõe estes perfis:

1. `atividade_local_fgv` — atividade acadêmica local;
2. `resumo_artigos_local_fgv` — resumo analítico de artigos;
3. `paper_local_fgv` — paper a partir de corpus local;
4. `paper_prisma_fgv` — paper + relatório PRISMA;
5. `dissertacao_local_fgv` — dissertação local;
6. `dissertacao_prisma_fgv` — dissertação + PRISMA;
7. `relatorio_prisma_fgv` — relatório PRISMA autônomo;
8. `relatorio_prisma_busca_orientada_fgv` — busca externa, deduplicação e triagem humana;
9. `somente_renderizar_fgv` — renderização de `document.json` existente, sem nova geração textual.

### 7.4. GUI

A opção:

```bash
academic-pipeline --gui
```

abre a interface gráfica FGV disponível no bundle. Ela é especialmente orientada ao fluxo de atividades acadêmicas e projetos com configuração TOML.

### 7.5. Wizard TOML

O wizard:

```bash
academic-pipeline --init-toml
```

gera a configuração do projeto por perguntas e presets. A opção `--toml-profile` permite iniciar diretamente em um perfil.

---

## 8. Catálogo completo da superfície CLI

A tabela a seguir consolida a superfície pública encontrada no runtime/parser canônico, incluindo a opção dinâmica `--list-profiles` e os wrappers PRISMA tratados pelo runtime.

| Opção | Área | Finalidade |
| --- | --- | --- |
| --config <arquivo.toml> | Fluxo principal | Seleciona o TOML do projeto/documento a executar. |
| --tui | Interface | Abre a Central Operacional FGV em terminal, baseada em prompt_toolkit. |
| --gui | Interface | Abre a interface gráfica FGV para atividades acadêmicas. |
| --init-toml | Configuração | Abre o gerador interativo completo de TOML. |
| --toml-profile <perfil> | Configuração | Define o preset inicial do gerador TOML. |
| --no-clear | Interface | Evita limpeza convencional da tela durante o wizard/TUI. |
| --list-toml-profiles | Descoberta | Lista presets disponíveis do gerador TOML. |
| --list-profiles | Descoberta | Lista os perfis TOML disponibilizados pelo runtime canônico. |
| --list-institutions | Institucional | Lista perfis institucionais instalados. |
| --list-layouts | Institucional | Lista layouts disponíveis para o perfil institucional selecionado. |
| --explain-profile [perfil] | Institucional | Explica perfil institucional; sem valor explícito usa FGV. |
| --show-prompts | Prompts | Mostra prompts e diretivas ativos para o TOML informado. |
| --write-prompt-lock | Prompts | Gera prompt_lock.json e prompt_lock.md e encerra. |
| --check-institution-compliance | Validação | Valida artefatos existentes contra o perfil institucional. |
| --doctor | Diagnóstico | Diagnostica ambiente, ferramentas, recursos institucionais e estilo bibliográfico. |
| --check-config | Validação | Valida preventivamente o TOML e encerra. |
| --recompile | Renderização | Recompila um arquivo ORG existente sem nova chamada à IA. |
| --org <arquivo.org> | Renderização | Arquivo ORG usado por recompile, quality-report ou conformidade. |
| --academic-writing <arquivo.el> | Renderização | Override do academic-writing.el em recompilação. |
| --latex-extra-path <caminho> | Renderização | Override de caminho extra de LaTeX em recompilação. |
| --pdf-engine <motor> | Renderização | Override do motor PDF em recompilação. |
| --no-clean | Renderização | Preserva arquivos auxiliares de compilação no modo recompile. |
| --somente-renderizar | Reuso | Usa document.json existente e somente renderiza saídas. |
| --somente-mapa-mental | Mapa mental | Usa document.json existente e executa apenas a etapa de mapa mental. |
| --reusar-mapa-mental | Mapa mental | Reaproveita mapa já existente quando disponível, sem nova geração. |
| --forcar-regeneracao-mapa-mental | Mapa mental | Remove artefatos conhecidos do mapa mental e força recriação. |
| --document-json <arquivo.json> | Reuso | Caminho do document.json existente. |
| --prisma-importar-triagem <csv> | PRISMA | Importa triagem humana e consolida matriz/relatório PRISMA. |
| --init-project <nome> | Projeto | Cria a estrutura de projeto em app_bundle/projetos/<nome>. |
| --project-type <tipo> | Projeto | Tipo: paper, atividade, prisma, atividade_prisma ou paper_prisma. |
| --institution <perfil> | Projeto | Perfil institucional utilizado na criação do projeto; padrão fgv. |
| --base-dir <caminho> | Projeto | Raiz alternativa do academic_pipeline/app_bundle para init-project. |
| --overwrite-project | Projeto | Permite sobrescrever somente arquivos considerados seguros pelo inicializador. |
| --make-doi-manifest | DOI | Gera doi_manifest.csv de uma pasta ou ZIP de documentos. |
| --input-zip <arquivo.zip> | DOI | ZIP de documentos usado para criação do manifesto DOI. |
| --input-dir <diretório> | DOI | Diretório de documentos usado para criação do manifesto DOI. |
| --output <arquivo> | Saída | Arquivo de saída em comandos que aceitam destino explícito. |
| --output-dir <diretório> | Caminhos | Override de document_output_dir. |
| --work-dir <diretório> | Caminhos | Override de work_dir para extrações temporárias. |
| --cache-dir <diretório> | Caminhos | Override de cache_dir para fulltext_cache. |
| --research-output-dir <diretório> | Caminhos | Override do diretório de saída de pesquisa/PRISMA. |
| --output-prefix <prefixo> | Caminhos | Override do prefixo canônico dos artefatos do documento. |
| --layout <layout> | Documento | Override de documento.layout. |
| --tipo-conteudo <tipo> | Documento | Override de documento.tipo_conteudo. |
| --genero-academico <gênero> | Documento | Override de documento.genero_academico. |
| --no-output-subdir | Caminhos | Evita criação do subdiretório document_prefix sob o output. |
| --inspect-bib <arquivo.bib> | Bibliografia | Inspeciona BibTeX e gera relatório Markdown/JSON. |
| --quality-report | Qualidade | Gera quality_report.md a partir de document.json, opcionalmente ORG. |
| --bib <arquivo.bib> | Bibliografia | BibTeX opcional para qualidade ou conformidade. |
| --docx <arquivo.docx> | Conformidade | DOCX opcional para validação institucional. |
| --pdf <arquivo.pdf> | Conformidade | PDF opcional para validação institucional. |
| --prisma-curadoria-menu | PRISMA | Abre submenu de curadoria de referências. |
| --prisma-curadoria-ia | PRISMA | Executa curadoria assistida por IA e gera XLSX/CSV. |
| --prisma-curadoria-sem-ia | PRISMA | Executa curadoria por heurística local, sem chamada à IA. |
| --prisma-curadoria-reexportar-xlsx | PRISMA | Reexporta XLSX revisado para triagem_humana.csv. |
| --prisma-curadoria-importar | PRISMA | Importa triagem_humana.csv e produz PRISMA final. |
| --prisma-curadoria-fluxo-completo | PRISMA | Executa curadoria e depois a importação final. |
| --prisma-curadoria-prompt <yaml> | PRISMA | Prompt estruturado específico da curadoria. |
| --prisma-curadoria-input <arquivo> | PRISMA | Entrada XLSX/CSV específica da curadoria. |
| --prisma-curadoria-out-dir <diretório> | PRISMA | Diretório explícito de saída da curadoria/relatório. |
| --prisma-curadoria-max-incluir <n> | PRISMA | Limite máximo de referências incluídas. |
| --prisma-curadoria-top-n-candidatos <n> | PRISMA | Número de candidatos avaliados/enviados à curadoria IA. |
| --prisma-curadoria-limiar-minimo <n> | PRISMA | Limiar mínimo de inclusão da curadoria. |
| --prisma-exportar-bib | PRISMA/artigo | Wrapper canônico para exportação BibTeX do conjunto PRISMA. |
| --prisma-congelar-artigo | PRISMA/artigo | Wrapper para congelar insumos selecionados do artigo PRISMA. |
| --prisma-gerar-toml-artigo | PRISMA/artigo | Wrapper para gerar o TOML final do artigo a partir do estado PRISMA. |
| --prisma-gerar-artigo-final | PRISMA/artigo | Wrapper para gerar o artigo final com os insumos PRISMA congelados. |
| -h / --help | Ajuda | Exibe ajuda do entrypoint oficial academic-pipeline. |

### 8.1. Regra de precedência

Alguns comandos são rotas especializadas e deliberadamente não podem ser misturados livremente com outros comandos:

- `--doctor`;
- `--check-config`;
- `--list-profiles`;
- `--check-institution-compliance`;
- `--make-doi-manifest`.

O runtime contém verificações explícitas de combinação e retorna erro de uso quando uma rota especializada é misturada com opções incompatíveis.

---

## 9. Configuração TOML

O TOML é o contrato declarativo central do fluxo principal.

O pipeline carrega o arquivo, registra internamente seu caminho e diretório, aplica o perfil institucional e depois aceita overrides de CLI.

Seções observadas no runtime e módulos:

```text
[projeto]
[documento]
[paths]
[openai]
[documentos_locais]
[orientacoes]
[bibliografia]
[prompts]
[pipeline]
[latex]
[docx]
[mapa_mental]
[idiomas_saida]
[resumo_paper] / configurações equivalentes de resumos
[pesquisa]
[busca_prisma]
[atividade]
```

A presença exata de algumas seções depende do perfil/preset.

### 9.1. Paths principais

O runtime trabalha com:

```text
document_output_dir
document_prefix
work_dir
cache_dir
research_output_dir
create_document_subdir
```

A CLI pode sobrescrever os caminhos principais com:

```text
--output-dir
--work-dir
--cache-dir
--research-output-dir
--output-prefix
--no-output-subdir
```

### 9.2. Overrides do documento

```text
--layout
--tipo-conteudo
--genero-academico
```

permitem alterar aspectos do documento sem editar permanentemente o TOML.

### 9.3. Modelo OpenAI

O fluxo que exige geração textual cria o cliente OpenAI a partir da configuração/ambiente e seleciona o modelo definido no projeto. Modos de renderização/recompilação podem evitar nova chamada à IA.

---

## 10. Inicialização de projetos

Use:

```bash
academic-pipeline --init-project meu_projeto --project-type paper
```

Tipos aceitos:

```text
paper
atividade
prisma
atividade_prisma
paper_prisma
```

O inicializador utiliza templates de configuração do bundle e cria uma árvore de projeto em `app_bundle/projetos/<nome>`, com arquivos auxiliares e placeholders adequados ao tipo.

Exemplo institucional:

```bash
academic-pipeline   --init-project trabalho_final   --project-type atividade   --institution fgv
```

A opção `--overwrite-project` não é uma autorização para apagar arbitrariamente conteúdo; ela libera apenas sobrescritas consideradas seguras pelo inicializador.

---

## 11. Fluxo principal de geração de documento

O fluxo padrão por TOML pode ser resumido assim:

```text
1. carregar .env e TOML
2. aplicar perfil institucional
3. aplicar overrides da CLI
4. resolver outputs/work/cache
5. validar preventivamente a configuração
6. descobrir documentos locais
7. copiar documentos para fulltext_cache
8. carregar orientações
9. gerar/validar bibliografia
10. executar PRISMA, quando habilitado
11. criar AcademicDocument/document.json com IA
12. sanear vazamentos técnicos e bibkeys crus
13. validar document_model
14. gerar resumos/palavras-chave de paper, quando habilitados
15. gerar/reutilizar mapa mental, quando habilitado
16. salvar document.json
17. renderizar ORG
18. compilar PDF, conforme configuração
19. renderizar DOCX, conforme configuração
20. gerar traduções adicionais, quando configuradas
21. registrar prompt_lock
22. validar conformidade institucional
23. gerar relatório de qualidade
24. gerar run report e manifesto de outputs
```

### 11.1. Modo somente pesquisa

Quando a configuração desativa a produção do documento acadêmico e mantém a pesquisa, o programa pode gerar bibliografia/PRISMA e relatórios sem criar `document.json`, ORG, PDF ou DOCX final.

---

## 12. Corpus de documentos locais

O módulo `corpus_manager.py` é responsável por:

- localizar documentos locais;
- tratar entrada por diretório/ZIP conforme a configuração;
- extrair texto de formatos suportados;
- capturar avisos de PDFs estruturalmente irregulares;
- limitar tamanho textual quando necessário;
- produzir metadados de origem;
- copiar documentos para `fulltext_cache`;
- carregar documentos de orientação.

O fluxo principal usa:

```text
discover_local_documents
copy_documents_to_fulltext_cache
collect_orientation_docs
```

O cache é um artefato operacional, não a autoridade bibliográfica original.

---

## 13. Bibliografia e DOI

### 13.1. Construção de bibliografia

`bibliography_manager.py`:

- identifica DOI em texto/metadados;
- consulta provedores quando configurados;
- normaliza DOI;
- produz metadados bibliográficos;
- renderiza entradas BibTeX;
- lê e separa entradas BibTeX;
- identifica entradas por DOI ou metadados;
- calcula qualidade das entradas;
- deduplica versões concorrentes;
- tenta associar documentos do corpus às respectivas chaves BibTeX.

Funções técnicas importantes incluem:

```text
metadata_provider_statuses
extract_doi_from_text
bibtex_escape
render_bib_entry
split_bib_entries
bib_entry_key
extract_field
entry_identity
entry_quality
deduplicate_entries
build_bibliography
```

### 13.2. Manifesto DOI

Para gerar um CSV de apoio:

```bash
academic-pipeline   --make-doi-manifest   --input-dir ./fontes   --output ./doi_manifest.csv
```

ou:

```bash
academic-pipeline   --make-doi-manifest   --input-zip ./fontes.zip   --output ./doi_manifest.csv
```

A rota valida sua própria superfície de opções para evitar combinações ambíguas.

### 13.3. Inspeção de BibTeX

```bash
academic-pipeline --inspect-bib referencias.bib
```

gera relatórios de inspeção em Markdown/JSON.

---

## 14. Modelo documental canônico

O documento interno é modelado por Pydantic em `document_model.py`.

A autoridade intermediária principal é:

```text
<prefixo>.document.json
```

Ela representa a estrutura semântica usada pelos renderizadores.

O modelo contempla, entre outros elementos:

- metadados;
- seções;
- blocos;
- texto e conteúdo inline;
- citações;
- listas;
- tabelas;
- figuras;
- bibliografia;
- diagnósticos.

Essa separação permite gerar o conteúdo uma vez e depois renderizá-lo em diferentes formatos.

---

## 15. Geração estruturada por IA

`document_builder.py` transforma corpus, orientações, configuração, bibliografia e prompts em `AcademicDocument`.

O fluxo:

- seleciona diretivas/prompt bundle;
- monta contexto controlado;
- chama o modelo configurado;
- exige saída estruturada;
- pode trabalhar em etapas/checkpoints em perfis que habilitam geração seccional;
- valida a estrutura antes de seguir;
- não considera o texto bruto da IA como produto final: o produto passa por modelo, saneamento e validação.

O modo completo requer credencial OpenAI disponível no ambiente quando houver etapa de IA.

---

## 16. Validação do documento

`document_validator.py` valida:

- coerência estrutural do `AcademicDocument`;
- citações contra chaves bibliográficas disponíveis;
- conteúdo ORG;
- vazamentos ou estruturas técnicas indevidas;
- contratos necessários para renderização.

O runtime chama validação antes da publicação dos artefatos finais e interrompe o fluxo em erros relevantes.

---

## 17. Modo `--somente-renderizar`

Quando o conteúdo já foi gerado:

```bash
academic-pipeline   --config projeto.toml   --somente-renderizar   --document-json output/meu_documento.document.json
```

O programa:

1. carrega o `document.json`;
2. resolve a bibliografia associada;
3. saneia eventuais marcas técnicas/bibkeys crus;
4. reutiliza resumos existentes quando disponíveis;
5. opcionalmente reutiliza ou regenera mapa mental;
6. renderiza novamente as saídas sem regenerar o texto acadêmico principal.

É o modo recomendado para ajustes de layout/compilação quando o conteúdo semântico já está aprovado.

---

## 18. Recompilação ORG sem IA

Use:

```bash
academic-pipeline   --recompile   --config projeto.toml   --org output/meu_documento.org
```

Overrides disponíveis:

```text
--academic-writing
--latex-extra-path
--pdf-engine
--no-clean
```

Esse modo recompila um ORG já existente, sem solicitar nova geração textual à IA.

---

## 19. ORG, LaTeX e PDF

`render_org_latex.py` é um dos principais renderizadores.

Capacidades identificadas:

- títulos/seções normalizados;
- renderização de blocos textuais;
- citações em LaTeX;
- listas;
- quotes;
- figuras;
- quebras de página;
- tabelas ORG pequenas;
- tabelas largas convertidas para LaTeX responsivo `longtblr`;
- landscape para tabelas largas;
- configuração de bibliografia/biblatex;
- estilos ABNT, APA, author-year, IEEE/numeric conforme configuração;
- elementos e layouts institucionais;
- suporte a resumos multilíngues.

Funções relevantes incluem:

```text
clean_heading_title
org_heading
render_table_block_org
normalize_biblatex_style
enforce_abnt_biblatex_options
bibliography_style_from_cfg
biblatex_options_for_style
strip_org_cite_export_lines
render_bibliography_preamble
render_block_org
render_org_latex
```

A compilação final é coordenada por `latex_compile.py`.

---

## 20. DOCX

`render_docx.py` converte o `AcademicDocument` para DOCX.

O fluxo pode usar templates e recursos institucionais distribuídos no pacote. A validação posterior do DOCX é registrada nos relatórios do pipeline.

Quando o perfil institucional exige um modelo específico, a configuração do perfil/layout é incorporada antes da renderização.

---

## 21. Perfis institucionais e FGV

O sistema possui três camadas:

```text
institution_profiles.py
institution_layouts.py
institution_compliance.py
```

### 21.1. Perfis

O perfil institucional pode fornecer:

- defaults;
- paths de assets;
- templates;
- estilos;
- prompts;
- validators;
- layouts disponíveis.

Comandos:

```bash
academic-pipeline --list-institutions
academic-pipeline --explain-profile fgv
```

### 21.2. Layouts

```bash
academic-pipeline --list-layouts --config projeto.toml
```

O layout é resolvido a partir da configuração/perfil, podendo ser sobrescrito por:

```bash
--layout <nome>
```

### 21.3. Conformidade

Exemplo:

```bash
academic-pipeline   --check-institution-compliance   --config projeto.toml   --org output/documento.org   --bib output/documento.bib   --docx output/documento.docx   --pdf output/documento.pdf
```

A validação gera estrutura de relatório e pode produzir versões Markdown/JSON.

---

## 22. Prompts e diretivas

`prompt_manager.py` resolve prompts a partir de:

- configuração do projeto;
- diretório do TOML;
- perfil institucional;
- recursos `profile://`;
- categorias globais;
- documento;
- paper/atividade/dissertação;
- pesquisa/PRISMA.

O gerenciador:

- resolve paths;
- carrega texto;
- remove diretivas inadequadas de exposição de raciocínio interno em prompt geral;
- compõe bundles;
- calcula SHA-256 do material carregado;
- permite relatório dos prompts ativos.

### 22.1. Ver prompts ativos

```bash
academic-pipeline --show-prompts --config projeto.toml
```

### 22.2. Prompt lock

```bash
academic-pipeline --write-prompt-lock --config projeto.toml
```

produz:

```text
<prefixo>.prompt_lock.json
<prefixo>.prompt_lock.md
```

O prompt lock registra rastreabilidade, versão do pipeline, configuração, perfil e prompts utilizados.

---

## 23. Relatório de qualidade

Use:

```bash
academic-pipeline   --quality-report   --document-json output/documento.document.json   --org output/documento.org   --bib output/documento.bib
```

O módulo `quality_report.py`:

- conta palavras;
- mede distribuição por seção;
- coleta citações;
- examina ORG;
- procura termos técnicos indevidos;
- produz alertas;
- registra status de qualidade.

A geração completa também pode produzir o relatório automaticamente.

---

## 24. Mapas mentais

A configuração `[mapa_mental]` controla essa capacidade.

O módulo permite:

- verificar se a geração está ativa;
- construir prompt específico;
- sanear PlantUML;
- determinar paths de `.puml` e imagem;
- renderizar mapa existente;
- gerar novo mapa por IA;
- recolorir/normalizar;
- anexar figura ao `document_model`;
- reutilizar mapa;
- remover artefatos conhecidos antes de regeneração forçada.

### 24.1. Somente mapa mental

```bash
academic-pipeline   --config projeto.toml   --somente-mapa-mental   --document-json output/documento.document.json
```

### 24.2. Reutilizar

```bash
academic-pipeline   --config projeto.toml   --somente-renderizar   --document-json output/documento.document.json   --reusar-mapa-mental
```

### 24.3. Forçar regeneração

```bash
academic-pipeline   --config projeto.toml   --somente-renderizar   --document-json output/documento.document.json   --forcar-regeneracao-mapa-mental
```

A regeneração forçada pode exigir novamente o cliente OpenAI e o renderizador PlantUML.

---

## 25. Traduções

`document_translation.py` traduz apenas conteúdo substantivo.

O módulo:

- identifica strings translatáveis;
- protege paths/campos técnicos;
- evita traduzir URLs, IDs, chaves e metadados estruturais;
- divide conteúdo em lotes com limite configurável;
- chama o modelo de tradução;
- reinsere o texto traduzido na mesma estrutura;
- permite renderizações adicionais por idioma.

Configuração relevante:

```text
[idiomas_saida]
```

O limite de lote é normalizado pelo runtime para evitar requisições excessivamente pequenas ou grandes.

---

## 26. Resumos e palavras-chave multilíngues

`paper_abstracts.py` é responsável por:

- determinar idiomas de resumo;
- diferenciar idiomas do paper principal de cópias traduzidas;
- extrair conteúdo substantivo do documento;
- proteger bibliografia e metadados técnicos;
- gerar resumo e palavras-chave;
- salvar sidecar:

```text
<prefixo>.resumos_paper.json
```

O renderizador ORG/PDF e o DOCX podem incorporar os resumos conforme a configuração.

---

## 27. PRISMA — visão geral

O subsistema PRISMA cobre várias etapas distintas:

```text
busca
→ deduplicação
→ triagem/curadoria
→ revisão humana
→ consolidação
→ relatório
→ bibliografia selecionada
→ full text
→ congelamento de insumos
→ artigo final
```

Módulos centrais:

```text
prisma_busca_externa.py
prisma_curadoria_ia_referencias.py
prisma_pipeline.py
prisma_builder.py
prisma_model.py
prisma_validator.py
render_prisma_org.py
render_prisma_docx.py
render_prisma_xlsx.py
render_prisma_flow.py
prisma_exportar_bib.py
prisma_baixar_fulltext_artigos.py
prisma_fulltext_garantido.py
prisma_congelar_artigo.py
```

---

## 28. PRISMA — busca externa

`prisma_busca_externa.py` oferece infraestrutura para:

- catálogos/provedores de busca;
- estratégias configuráveis;
- normalização de registros;
- retries;
- rate limiting;
- deduplicação;
- geração de material para triagem;
- integração com pesquisa configurada no TOML.

A busca externa é executada apenas quando habilitada/configurada.

---

## 29. PRISMA — triagem humana

A opção:

```bash
academic-pipeline   --config projeto_prisma.toml   --prisma-importar-triagem caminho/triagem_humana.csv
```

importa a decisão humana e consolida os artefatos PRISMA.

Esse fluxo é especialmente associado ao perfil de busca orientada, em que a revisão humana é uma etapa explícita.

---

## 30. PRISMA — curadoria assistida por IA ou heurística

### 30.1. Curadoria IA

```bash
academic-pipeline   --config projeto.toml   --prisma-curadoria-ia
```

### 30.2. Sem IA

```bash
academic-pipeline   --config projeto.toml   --prisma-curadoria-sem-ia
```

### 30.3. Parâmetros

```text
--prisma-curadoria-input
--prisma-curadoria-out-dir
--prisma-curadoria-prompt
--prisma-curadoria-max-incluir
--prisma-curadoria-top-n-candidatos
--prisma-curadoria-limiar-minimo
```

### 30.4. Revisão XLSX

Após revisão humana:

```bash
academic-pipeline   --config projeto.toml   --prisma-curadoria-reexportar-xlsx   --prisma-curadoria-input caminho/revisado.xlsx
```

### 30.5. Importação final

```bash
academic-pipeline   --config projeto.toml   --prisma-curadoria-importar
```

### 30.6. Fluxo completo

```bash
academic-pipeline   --config projeto.toml   --prisma-curadoria-fluxo-completo
```

Esse comando encadeia curadoria e importação final conforme a lógica do runtime.

---

## 31. PRISMA — artigo final

O runtime reconhece quatro wrappers de alto nível:

```text
--prisma-exportar-bib
--prisma-congelar-artigo
--prisma-gerar-toml-artigo
--prisma-gerar-artigo-final
```

Eles formam a ponte entre um relatório PRISMA aprovado e o produto acadêmico final.

### 31.1. Exportar bibliografia incluída

Produz BibTeX das referências incluídas.

### 31.2. Congelar artigo

`prisma_congelar_artigo.py` reúne os insumos aprovados, registra SHA-256/metadados e prepara uma área estável para geração do artigo.

### 31.3. Gerar TOML do artigo

O gerador do artigo extrai tema, recorte, objetivo, pergunta, hipótese, tese e estrutura a partir dos dados da pesquisa/configuração e cria o TOML final baseado em template.

Ele exige os artefatos PRISMA necessários, incluindo a bibliografia e arquivos de pesquisa definidos pelo fluxo.

### 31.4. Gerar artigo final

Executa a geração final sobre os insumos congelados, reduzindo o risco de a pesquisa mudar entre a aprovação da triagem e a redação final.

---

## 32. Fluxo de estado do Artigo PRISMA

Existe uma CLI auxiliar:

```text
artigo_prisma_workflow.py
```

Ações:

```text
status
validate
mark-reviewed
```

Exemplos:

```bash
python -m app_bundle.scripts.pipeline.artigo_prisma_workflow   status   --art-dir caminho/artigo
```

```bash
python -m app_bundle.scripts.pipeline.artigo_prisma_workflow   validate   --art-dir caminho/artigo
```

A confirmação de revisão humana exige `--xlsx`.

---

## 33. Full text PRISMA

Os módulos:

```text
prisma_baixar_fulltext_artigos.py
prisma_fulltext_garantido.py
```

tratam obtenção/organização e garantia de full text no fluxo robusto. A finalidade é assegurar que a redação final opere sobre as evidências efetivamente selecionadas, com rastreabilidade dos arquivos disponíveis e ausentes.

---

## 34. Doctor

```bash
academic-pipeline --doctor
```

Executa diagnóstico do ambiente e pode verificar, conforme o contexto:

- configuração;
- paths;
- ferramentas;
- recursos institucionais;
- bibliografia;
- capacidades de pesquisa;
- diretórios de saída;
- condições necessárias às etapas posteriores.

`--doctor` é uma rota própria; o runtime rejeita combinações inadequadas com outros comandos de alto nível.

---

## 35. Check-config

```bash
academic-pipeline --check-config --config projeto.toml
```

É a validação preventiva do TOML. O fluxo carrega o perfil institucional, aplica overrides válidos e verifica erros/warnings antes da geração custosa.

A geração principal também executa uma validação preventiva e bloqueia quando existem erros de configuração.

---

## 36. Listagens e descoberta

### 36.1. Presets do wizard

```bash
academic-pipeline --list-toml-profiles
```

### 36.2. Perfis TOML do runtime

```bash
academic-pipeline --list-profiles
```

### 36.3. Instituições

```bash
academic-pipeline --list-institutions
```

### 36.4. Layouts

```bash
academic-pipeline --list-layouts --config projeto.toml
```

### 36.5. Explicação do perfil

```bash
academic-pipeline --explain-profile fgv
```

---

## 37. Artefatos produzidos

Dependendo do projeto/configuração, podem ser produzidos:

```text
<prefixo>.document.json
<prefixo>.org
<prefixo>.pdf
<prefixo>.docx
<prefixo>.bib
<prefixo>.prompt_lock.json
<prefixo>.prompt_lock.md
<prefixo>.quality_report.json
<prefixo>.quality_report.md
<prefixo>.compliance_report.json
<prefixo>.compliance_report.md
<prefixo>.run_report.json
<prefixo>.outputs.txt
<prefixo>.resumos_paper.json
```

Além destes, fluxos PRISMA podem gerar:

- matrizes CSV/XLSX;
- triagem humana;
- relatório PRISMA ORG/PDF/DOCX;
- diagrama de fluxo;
- bibliografia de referências incluídas;
- arquivos de full text;
- dados congelados do artigo;
- TOML específico do artigo final.

Nem todo fluxo gera todos os artefatos.

---

## 38. Rastreabilidade

O programa registra múltiplas camadas de rastreabilidade:

### 38.1. `document.json`

Autoridade intermediária estruturada do conteúdo.

### 38.2. `prompt_lock`

Registra quais prompts/diretivas foram usados.

### 38.3. `run_report`

Registra execução, configuração, modelo, outputs e warnings.

### 38.4. `outputs.txt`

Manifesto legível dos caminhos produzidos.

### 38.5. Relatórios de qualidade e conformidade

Separam avaliação textual de avaliação institucional.

### 38.6. SHA-256 em fluxos de congelamento

O artigo PRISMA utiliza hashes para estabilizar e identificar os insumos aprovados.

---

## 39. Tratamento de bibliografia no renderizador

O renderizador normaliza estilos BibLaTeX e possui suporte explícito a aliases de:

```text
ABNT
APA
authoryear
IEEE/numeric
Vancouver → numeric
```

Para ABNT, o renderizador impõe opções coerentes de `biblatex`, incluindo backend Biber e ordenação apropriada, salvo override explícito.

O sistema também remove diretivas `#+CITE_EXPORT` herdadas quando conflitam com a estratégia atual de citações LaTeX.

---

## 40. Tabelas e elementos gráficos

Tabelas pequenas permanecem preferencialmente em sintaxe ORG.

Tabelas largas ou com células extensas podem ser convertidas para LaTeX `longtblr` e, em casos mais largos, `landscape`, para reduzir cortes laterais no PDF.

Figuras, inclusive mapa mental, são registradas no `document_model` e renderizadas como parte do documento.

---

## 41. Caminhos e raiz do projeto

`academic_pipeline.repository_paths` fornece resolução segura da raiz.

Variável reconhecida:

```text
ACADEMIC_PIPELINE_PROJECT_ROOT
```

Quando definida, deve apontar para uma raiz válida contendo:

```text
pyproject.toml
academic_pipeline/
app_bundle/
```

`repository_resource()` impede que um recurso resolvido escape da raiz do projeto.

---

## 42. Inventário técnico integral auditado por AST

Esta edição substitui o catálogo resumido anterior por um inventário **função por função**.

### 42.1. Baseline efetivamente lido pelo reconstrutor

```yaml
git_head: 019464a6aefd38acbe346fa1a0d286f68e9e34df
git_subject: "docs(mppg): add canonical Academic Pipeline technical manual"
modules_parsed: 75
classes: 66
symbols_total: 1506
module_functions: 1248
methods: 195
nested_functions_callbacks: 63
async_symbols: 0
public_by_name: 898
internal_or_dunder: 608
documentation_cards: 1506
documentation_coverage_percent: 100
unique_qualified_names: 1502
duplicate_qualified_names: 4
definitions_in_duplicate_groups: 8
shadowed_definition_candidates: 4
static_final_binding_candidates: 1502
```

**Gate de cobertura das definições-fonte:**

```text
definições AST encontradas: 1506
fichas individuais produzidas: 1506
cobertura documental das definições-fonte: 1506/1506 = 100%
nomes qualificados únicos: 1502
nomes qualificados redefinidos: 4
```

O gate fecha somente porque a quantidade de fichas é calculada diretamente do mesmo inventário AST que identifica os símbolos; não existe lista manual paralela.

### 42.2. Escopo de fontes operacionais incluído

Foram incluídos:

```text
academic_pipeline/**/*.py
app_bundle/scripts/pipeline/**/*.py
scripts Python não-versionados na raiz de software/academic_pipeline_mppg
```

Foram excluídos da cobertura operacional viva:

- `.orig`;
- backups;
- testes e ferramentas de refatoração;
- arquivos cujo próprio nome declara versão/revisão (`_vN`, `_rcN`, `_revN`);
- caches;
- outros artefatos históricos fora das raízes operacionais.

Esses arquivos excluídos são listados explicitamente na seção 42.4.

> **Importante:** presença de uma definição em uma fonte operacional não prova, por si só, que essa definição seja alcançável em runtime. Este manual garante cobertura documental das definições-fonte. Alcance dinâmico, dispatch por strings, registries, redefinições e código morto exigem auditoria própria e não são presumidos neste gate.

### 42.3. Matriz de módulos e cobertura

| Módulo | Categoria | LOC | Classes | Funções/métodos/nested | SHA-256 |
| --- | --- | ---: | ---: | ---: | --- |
| `academic_pipeline` | core runtime | 18 | 0 | 1 | `4d08f2e264da8da506351ceaf4a5efd17fa4211bae7b44da458086618137413c` |
| `academic_pipeline.__main__` | core runtime | 9 | 0 | 0 | `31840fb9a79716886a21e2026f9255e4df5bdf897531cecf63399692ada047f4` |
| `academic_pipeline.check_config_runtime` | core runtime | 164 | 2 | 5 | `4459286e415f3f2fd240c0bfc15c538b1a5e03146ac323500951aaf51895b56b` |
| `academic_pipeline.cli` | core runtime | 13 | 0 | 1 | `570375a810136f385074638b226db9db24f16100a5ff47b4005affd9a8824231` |
| `academic_pipeline.cli_parser` | core runtime | 133 | 0 | 2 | `f6fd1b98c489e1adf5d8ab61419cab6d78db348b93958ff6d93199df0e5cfbb8` |
| `academic_pipeline.command_dispatch` | core runtime | 325 | 1 | 20 | `9255c4b924fd61b7120b8c5e02684d338f6788de42ae7c352b049a488a308afe` |
| `academic_pipeline.default_runtime` | core runtime | 1332 | 0 | 51 | `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0` |
| `academic_pipeline.doctor_runtime` | core runtime | 211 | 2 | 8 | `2944cb78c2b1959acaf2f0f4192b09964e3cf721885aafac7c88164c1b287ec9` |
| `academic_pipeline.document_orchestration` | core runtime | 437 | 1 | 26 | `62a8eeb2caa7a0170c83618112e432eec1512fc0e7bd6502f857b93be7bb8ae2` |
| `academic_pipeline.doi_manifest_runtime` | core runtime | 170 | 1 | 8 | `9d4dc1a4a9cee30a69effc31424ccc3ead1cf041bf3fbd09a679659e34fe015b` |
| `academic_pipeline.institution_compliance_runtime` | core runtime | 155 | 2 | 6 | `41b323120b03a64a317ef6288df3cecbdbc042532d7b739c0485ee04b3e94117` |
| `academic_pipeline.list_profiles_runtime` | core runtime | 38 | 1 | 2 | `9a6bbbd980c0111067af17613b97a30b4bc9c333852cc30279c33999653e333d` |
| `academic_pipeline.prisma_generic_orchestration` | core runtime | 790 | 1 | 102 | `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4` |
| `academic_pipeline.repository_paths` | core runtime | 74 | 1 | 4 | `f762fec50168d8a56ea53e1d58a35bfe293d4175f2ae42b9c15b26c04ee3278f` |
| `academic_pipeline.runtime` | core runtime | 645 | 3 | 26 | `3e2192ea4522be375e0a2ea2fb22a0654fc4107ec581712372ccae3068dfb8f9` |
| `app_bundle.scripts.pipeline` | pipeline operacional | 0 | 0 | 0 | `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` |
| `app_bundle.scripts.pipeline.academic_pipeline_gui` | pipeline operacional | 1185 | 4 | 72 | `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01` |
| `app_bundle.scripts.pipeline.academic_pipeline_toml_generator` | pipeline operacional | 95 | 0 | 3 | `97470090c33aa8ff641e6117d4c0094ec5823c09e9cabd387c71b90c29cf15dc` |
| `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo` | pipeline operacional | 5024 | 3 | 160 | `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff` |
| `app_bundle.scripts.pipeline.academic_pipeline_tui` | pipeline operacional | 1456 | 1 | 62 | `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325` |
| `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets` | pipeline operacional | 814 | 2 | 37 | `b66fc9fb26f3359c6efb6960684426a0c7a571c6ea58162fa75a65ab1569389f` |
| `app_bundle.scripts.pipeline.article_workflow` | pipeline operacional | 5 | 0 | 0 | `110f39cd27f9c21baa7687a3ee9f601ab15a4fe7d20f574d73988cb3e9decf52` |
| `app_bundle.scripts.pipeline.article_workflow.state` | pipeline operacional | 181 | 2 | 15 | `f681a4157b3fa2b7a1cca07dd7b49de015e559cb38a422e8e4aab6ba7a71a6cd` |
| `app_bundle.scripts.pipeline.article_workflow.validators` | pipeline operacional | 305 | 2 | 30 | `f2eb8379ca042a32eddabfe972a74fe5df9850d646ab04734b0339bf68211a83` |
| `app_bundle.scripts.pipeline.artigo_prisma_workflow` | pipeline operacional | 51 | 0 | 2 | `62578a918b0064c7812c2425e90d8652d16d1682379fdf1e1fda0f0a5e49baee` |
| `app_bundle.scripts.pipeline.bibliography_manager` | pipeline operacional | 632 | 2 | 31 | `ccf7efa67c51a306a53f604b4bc98e3f43d9434a01f09147138a9c19f7236d16` |
| `app_bundle.scripts.pipeline.citation_renderer` | pipeline operacional | 68 | 0 | 4 | `e543ed12787cbaf014b18d112c287d0848da20d17815caa24679959efeea4229` |
| `app_bundle.scripts.pipeline.clean_bundle` | pipeline operacional | 288 | 1 | 11 | `b30cc9df1eb311edf90a16279c9f33fed3beddfeffed79199212d688db87d301` |
| `app_bundle.scripts.pipeline.corpus_manager` | pipeline operacional | 279 | 2 | 11 | `b240573df7e930283e26d91a8808eff81280b3da854faf75bc5ce33d12679712` |
| `app_bundle.scripts.pipeline.diagnostics` | pipeline operacional | 526 | 0 | 23 | `347f7137c96e87af21470088a621cd14fc9b66a5b7389bc273a39a69d48fc173` |
| `app_bundle.scripts.pipeline.document_builder` | pipeline operacional | 715 | 1 | 26 | `4342a2454736eb84a52335af282120bbd31d58630f6b0b487a3beb2ffcf83c0d` |
| `app_bundle.scripts.pipeline.document_model` | pipeline operacional | 192 | 12 | 5 | `3fdb6646ee253b131be83db8e02f33b71e838db8b0cf323fc63fda530d6db6ae` |
| `app_bundle.scripts.pipeline.document_translation` | pipeline operacional | 270 | 1 | 13 | `daa8dbd13cd2c0c410b680d8632b159fb988522ca3ee90be9b7f3c819ba0810c` |
| `app_bundle.scripts.pipeline.document_validator` | pipeline operacional | 500 | 0 | 16 | `e8fa0bc7f5f22a8adc48dc1946ae28791788d8ad114c394a0f91598453c0a2b6` |
| `app_bundle.scripts.pipeline.gerar_artigo_final_unificado` | pipeline operacional | 775 | 0 | 45 | `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9` |
| `app_bundle.scripts.pipeline.gerar_artigo_longo_fulltext_secional` | pipeline operacional | 439 | 0 | 14 | `3af681f2cfdcb1158ea0cce858d6c2fd76d488b408fdb19b5200b64026f8c4dc` |
| `app_bundle.scripts.pipeline.institution_compliance` | pipeline operacional | 396 | 1 | 19 | `5117477811204e8ab59642e7bfe63c361995b089cc7985f8aa747932109fae4a` |
| `app_bundle.scripts.pipeline.institution_explainer` | pipeline operacional | 83 | 0 | 2 | `989eea81a522d2f88beb5471b4dc085af9b447c07f3bc8b5ba5e17bb182df2bf` |
| `app_bundle.scripts.pipeline.institution_layouts` | pipeline operacional | 149 | 1 | 5 | `5103320fca74fe14814431370e06a20f8d3436bf98ada5ce47613a6b39343cb6` |
| `app_bundle.scripts.pipeline.institution_profiles` | pipeline operacional | 211 | 0 | 10 | `e6f37ebbaa90beb1dee3d44f55b1455d29f0fbf0447fdca0b3156b133c00928f` |
| `app_bundle.scripts.pipeline.latex_compile` | pipeline operacional | 78 | 0 | 2 | `ad4dd565018e387f79b76f3e635550b99ffc29969092a69af61ec7db3e06f42d` |
| `app_bundle.scripts.pipeline.mindmap_manager` | pipeline operacional | 360 | 1 | 15 | `8e60d620392d0d3ffc68330dc454a7d9991bef8fff2211b5e8f8e2e2753ccad9` |
| `app_bundle.scripts.pipeline.paper_abstracts` | pipeline operacional | 428 | 1 | 22 | `e8e0688ef30b7b1be1f424667f9ec676e232f1b262ebe2faae3fd0d4efe02c0e` |
| `app_bundle.scripts.pipeline.pipeline_orchestrator` | pipeline operacional | 27 | 0 | 1 | `b2cece847fdac97db991c8667c97c53d52146f57875e19ddcb93f995781613bd` |
| `app_bundle.scripts.pipeline.preparar_artigo_longo_fulltext` | pipeline operacional | 389 | 0 | 19 | `6e846516c6c057aa2958fe53cc8c055ca185177d9a2aee450fa99329c86c6f11` |
| `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos` | pipeline operacional | 528 | 0 | 30 | `9a4b0a12218a3c728bcc57182549973d066035d1a0324522a8d9be5d6758bc62` |
| `app_bundle.scripts.pipeline.prisma_builder` | pipeline operacional | 437 | 0 | 15 | `a4ad333b1b98465a27bddc03a81648e052b7016e85b009759da5874031ce336c` |
| `app_bundle.scripts.pipeline.prisma_busca_externa` | pipeline operacional | 2336 | 1 | 101 | `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec` |
| `app_bundle.scripts.pipeline.prisma_congelar_artigo` | pipeline operacional | 204 | 0 | 18 | `b17f589816d0fa9aabc471546bc1ae79a30ecb505fd5ee57d771136263d49526` |
| `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias` | pipeline operacional | 997 | 0 | 36 | `73f3e311e2c2d6ab169c77e63d632206aa2a22c74dd8a7f1973887aab01d3006` |
| `app_bundle.scripts.pipeline.prisma_diagrama_fluxo` | pipeline operacional | 592 | 0 | 24 | `4eea6a684358943bc75f20322f813921f542f4d534bf3dc23316f01f90138a11` |
| `app_bundle.scripts.pipeline.prisma_exportar_bib` | pipeline operacional | 126 | 0 | 13 | `cb74100755f65effd874fc02e32eaf373f73dc8daa1ad103078594a56eeb7c81` |
| `app_bundle.scripts.pipeline.prisma_fulltext_garantido` | pipeline operacional | 1181 | 1 | 55 | `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95` |
| `app_bundle.scripts.pipeline.prisma_model` | pipeline operacional | 137 | 8 | 4 | `9261e27fcfbac2c3f4d3b383ae45f6ff1c0e6ed9483da5c6fc57124a335c8327` |
| `app_bundle.scripts.pipeline.prisma_pipeline` | pipeline operacional | 148 | 0 | 4 | `1da4b859211b698664d1471560886030cd3e601b0634f63f8f6391ed03925640` |
| `app_bundle.scripts.pipeline.prisma_validator` | pipeline operacional | 49 | 0 | 2 | `ec79d03fb146270266b496a4d2a42212c48a0be11c51a10fcd38c54659b26800` |
| `app_bundle.scripts.pipeline.project_tools` | pipeline operacional | 344 | 1 | 11 | `0c990206d6325d952d49575210bc44f58f103760732eb5002ce62a58739151e2` |
| `app_bundle.scripts.pipeline.prompt_lock` | pipeline operacional | 80 | 0 | 4 | `c2e0a21f4daeb7e880c43772b454131971ccac857501aa7f0f915f3cc00ba5e3` |
| `app_bundle.scripts.pipeline.prompt_manager` | pipeline operacional | 293 | 2 | 15 | `c739b72f2fb83607c8e968d200cd4d3ee53618360f17c2296ce2c84c418163ad` |
| `app_bundle.scripts.pipeline.quality_report` | pipeline operacional | 196 | 0 | 10 | `ba57aa48ec5099d5c83c893354f4d2855fb3d153109d086f03b04841a2a705f6` |
| `app_bundle.scripts.pipeline.render_docx` | pipeline operacional | 403 | 0 | 14 | `118720fdbedbad31e089de3018802c70ec7c4f0d608655a3abb80bd58fd207cb` |
| `app_bundle.scripts.pipeline.render_docx_canonico` | pipeline operacional | 1313 | 1 | 56 | `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3` |
| `app_bundle.scripts.pipeline.render_org_latex` | pipeline operacional | 1018 | 0 | 49 | `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47` |
| `app_bundle.scripts.pipeline.render_prisma_docx` | pipeline operacional | 157 | 0 | 5 | `f691deb04e730266ba7a6d3e6a964ab2b267aec6705b3a4bf0edc73b16b4fdbb` |
| `app_bundle.scripts.pipeline.render_prisma_flow` | pipeline operacional | 66 | 0 | 3 | `c3795f7ef2098c15098e05853fa541f56ada20cc1d74302e57c598115dbb4963` |
| `app_bundle.scripts.pipeline.render_prisma_org` | pipeline operacional | 171 | 0 | 3 | `91c756bfc15dab47df8e070320f170d6748b031f7e5b99f4f13ba99254e3992e` |
| `app_bundle.scripts.pipeline.render_prisma_xlsx` | pipeline operacional | 99 | 0 | 4 | `73fcefcc0a1b32e6f8834dbd56e396b64ea91fb64e1a26411b6425ee30771805` |
| `app_bundle.scripts.pipeline.utils` | pipeline operacional | 73 | 0 | 10 | `2acb618894448925c6b54b4bc038cc183a56142a9ca5dadb3fe4fcc167554727` |
| `app_bundle.scripts.pipeline.validar_artigo_longo_fulltext` | pipeline operacional | 99 | 0 | 4 | `f1f2e8b94a8826e1251bd169f8946b611a367fc36fbe998ad73d565f2a7d86c1` |
| `atualizar_academic_pipeline_bundle` | script auxiliar da raiz | 474 | 0 | 14 | `a0c0e4ccb2217552514faf5a5b612bf3c3a5c5daafc5d60c61c7313852d76cfb` |
| `configurar_pretriagem_ia_prisma` | script auxiliar da raiz | 124 | 0 | 4 | `82d629145538c05120256e07f7567c3de0bb0244822ab5433d1c41d565c9bdc9` |
| `diagnosticar_fontes_prisma` | script auxiliar da raiz | 245 | 0 | 7 | `fcad537963f75f26341502c0b42f5c65cac9b58c3dd73e61b6e31b5bf44f0910` |
| `gerar_artigo_final_unificado` | script auxiliar da raiz | 775 | 0 | 45 | `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9` |
| `gerar_docx_canonico` | script auxiliar da raiz | 7 | 0 | 0 | `f781044cf985eb61eec521b9c41ebc920296e64718076393a9c3d6b4b790b019` |
| `gerar_log_diagnostico_artigo` | script auxiliar da raiz | 271 | 0 | 9 | `17a83fda712aa3ab63957358282f70aa425d53b587dca8bae4defd36c5f4d757` |

### 42.4. Arquivos históricos/excluídos do escopo vivo

| Arquivo excluído | Motivo |
| --- | --- |
| `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_v0_3_1.py` | nome de arquivo explicitamente versionado/histórico |
| `aplicar_docx_canonico_v10.py` | script raiz versionado/histórico |
| `aplicar_docx_canonico_v11.py` | script raiz versionado/histórico |
| `aplicar_docx_canonico_v12.py` | script raiz versionado/histórico |
| `aplicar_docx_canonico_v13.py` | script raiz versionado/histórico |
| `aplicar_docx_capa_disciplina_v14.py` | script raiz versionado/histórico |
| `aplicar_docx_only_gerador_unificado_v9.py` | script raiz versionado/histórico |
| `configurar_pretriagem_ia_prisma_v16.py` | script raiz versionado/histórico |
| `executar_artigo_longo_fulltext_v1_13.py` | script raiz versionado/histórico |
| `executar_artigo_longo_fulltext_v1_14.py` | script raiz versionado/histórico |
| `gerar_log_diagnostico_artigo_v1_18.py` | script raiz versionado/histórico |


### 42.5. Adjudicação das redefinições no mesmo namespace

A auditoria encontrou **4 nomes qualificados com mais de uma definição textual**.
Isso explica por que existem **1506 definições/fichas**, mas apenas
**1502 identificadores `módulo:qualname` únicos**.

- `academic_pipeline.default_runtime:load_config` — definições em L260–266, L1241–1243. Definição textual final: `L1241`; anterior(es) `L260` classificada(s) como potencialmente sombreada(s).
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_wiz_disable_references` — definições em L4376–4405, L5000–5010. Definição textual final: `L5000`; anterior(es) `L4376` classificada(s) como potencialmente sombreada(s).
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_outputs_and_options` — definições em L3361–3511, L4911–4956. Definição textual final: `L4911`; anterior(es) `L3361` classificada(s) como potencialmente sombreada(s).
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:render_toml` — definições em L3642–4054, L4963–4972. Definição textual final: `L4963`; anterior(es) `L3642` classificada(s) como potencialmente sombreada(s).

Critério adotado:

```text
definições-fonte documentadas: 1506/1506
nomes qualificados únicos: 1502
redefinições excedentes: 4
definições participantes de grupos duplicados: 8
candidatos a binding textual final: 1502
```

Esta classificação é **estática**. Para definições sequenciais no mesmo namespace,
a definição posterior normalmente substitui a anterior quando o módulo termina de
ser importado. O manual não converte essa regra em prova universal de alcance:
condicionais, execução durante importação, registries e referências dinâmicas
precisam ser auditados separadamente quando forem relevantes.


---

## 43. Dicionário integral de classes

Cada classe encontrada no mesmo escopo é descrita abaixo. Os métodos aparecem novamente na seção 44, com ficha individual completa.

### Classe `CheckConfigRuntimeError`

**Módulo:** `academic_pipeline.check_config_runtime`
**Arquivo:** `academic_pipeline/check_config_runtime.py`
**Linhas:** 25–26
**Bases:** `RuntimeError`
**Decoradores:** nenhum

**Finalidade:** Classe `CheckConfigRuntimeError`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `CheckConfigRuntimeContext`

**Módulo:** `academic_pipeline.check_config_runtime`
**Arquivo:** `academic_pipeline/check_config_runtime.py`
**Linhas:** 30–47
**Bases:** nenhuma explícita
**Decoradores:** `dataclass(frozen=True, slots=True)`

**Finalidade:** Classe `CheckConfigRuntimeContext`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `DispatchResult`

**Módulo:** `academic_pipeline.command_dispatch`
**Arquivo:** `academic_pipeline/command_dispatch.py`
**Linhas:** 9–11
**Bases:** nenhuma explícita
**Decoradores:** `dataclass(frozen=True, slots=True)`

**Finalidade:** Classe `DispatchResult`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `DoctorRuntimeError`

**Módulo:** `academic_pipeline.doctor_runtime`
**Arquivo:** `academic_pipeline/doctor_runtime.py`
**Linhas:** 35–36
**Bases:** `RuntimeError`
**Decoradores:** nenhum

**Finalidade:** Classe `DoctorRuntimeError`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `DoctorRuntimeContext`

**Módulo:** `academic_pipeline.doctor_runtime`
**Arquivo:** `academic_pipeline/doctor_runtime.py`
**Linhas:** 40–57
**Bases:** nenhuma explícita
**Decoradores:** `dataclass(frozen=True, slots=True)`

**Finalidade:** Classe `DoctorRuntimeContext`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `DocumentStageResult`

**Módulo:** `academic_pipeline.document_orchestration`
**Arquivo:** `academic_pipeline/document_orchestration.py`
**Linhas:** 6–9
**Bases:** nenhuma explícita
**Decoradores:** `dataclass(frozen=True, slots=True)`

**Finalidade:** Classe `DocumentStageResult`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `DoiManifestRuntimeError`

**Módulo:** `academic_pipeline.doi_manifest_runtime`
**Arquivo:** `academic_pipeline/doi_manifest_runtime.py`
**Linhas:** 64–65
**Bases:** `RuntimeError`
**Decoradores:** nenhum

**Finalidade:** Classe `DoiManifestRuntimeError`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `InstitutionComplianceRuntimeError`

**Módulo:** `academic_pipeline.institution_compliance_runtime`
**Arquivo:** `academic_pipeline/institution_compliance_runtime.py`
**Linhas:** 26–27
**Bases:** `RuntimeError`
**Decoradores:** nenhum

**Finalidade:** Classe `InstitutionComplianceRuntimeError`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `InstitutionComplianceRuntimeContext`

**Módulo:** `academic_pipeline.institution_compliance_runtime`
**Arquivo:** `academic_pipeline/institution_compliance_runtime.py`
**Linhas:** 31–43
**Bases:** nenhuma explícita
**Decoradores:** `dataclass(frozen=True, slots=True)`

**Finalidade:** Classe `InstitutionComplianceRuntimeContext`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `Preset`

**Módulo:** `academic_pipeline.list_profiles_runtime`
**Arquivo:** `academic_pipeline/list_profiles_runtime.py`
**Linhas:** 9–19
**Bases:** nenhuma explícita
**Decoradores:** `dataclass(frozen=True)`

**Finalidade:** Classe `Preset`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `PrismaStageResult`

**Módulo:** `academic_pipeline.prisma_generic_orchestration`
**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 8–11
**Bases:** nenhuma explícita
**Decoradores:** `dataclass(frozen=True, slots=True)`

**Finalidade:** Classe `PrismaStageResult`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `RepositoryRootError`

**Módulo:** `academic_pipeline.repository_paths`
**Arquivo:** `academic_pipeline/repository_paths.py`
**Linhas:** 10–11
**Bases:** `RuntimeError`
**Decoradores:** nenhum

**Finalidade:** Classe `RepositoryRootError`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `RuntimeRoute`

**Módulo:** `academic_pipeline.runtime`
**Arquivo:** `academic_pipeline/runtime.py`
**Linhas:** 150–163
**Bases:** `str`, `Enum`
**Decoradores:** nenhum

**Finalidade:** Rotas deliberadas do entrypoint durante a transição.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `RuntimeContext`

**Módulo:** `academic_pipeline.runtime`
**Arquivo:** `academic_pipeline/runtime.py`
**Linhas:** 167–189
**Bases:** nenhuma explícita
**Decoradores:** `dataclass(frozen=True, slots=True)`

**Finalidade:** Dependências explícitas necessárias à primeira onda nativa.

**Métodos documentados individualmente:** 1

- `RuntimeContext.as_dispatch_mapping`


### Classe `NativeRuntimeError`

**Módulo:** `academic_pipeline.runtime`
**Arquivo:** `academic_pipeline/runtime.py`
**Linhas:** 192–193
**Bases:** `RuntimeError`
**Decoradores:** nenhum

**Finalidade:** Indica quebra do contrato da primeira onda nativa.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `Step`

**Módulo:** `app_bundle.scripts.pipeline.academic_pipeline_gui`
**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 82–85
**Bases:** nenhuma explícita
**Decoradores:** `dataclass(frozen=True)`

**Finalidade:** Classe `Step`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `AutocompletePathEntry`

**Módulo:** `app_bundle.scripts.pipeline.academic_pipeline_gui`
**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 164–272
**Bases:** `ttk.Entry`
**Decoradores:** nenhum

**Finalidade:** Entrada de caminho com autocomplete, Tab e lista suspensa.

**Métodos documentados individualmente:** 10

- `AutocompletePathEntry.__init__`
- `AutocompletePathEntry._on_key_release`
- `AutocompletePathEntry._on_focus_out`
- `AutocompletePathEntry._on_tab`
- `AutocompletePathEntry._on_down`
- `AutocompletePathEntry._on_escape`
- `AutocompletePathEntry.show_suggestions`
- `AutocompletePathEntry.hide_suggestions`
- `AutocompletePathEntry._choose_selected`
- `AutocompletePathEntry._choose`


### Classe `ScrollableFrame`

**Módulo:** `app_bundle.scripts.pipeline.academic_pipeline_gui`
**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 275–310
**Bases:** `ttk.Frame`
**Decoradores:** nenhum

**Finalidade:** Frame rolável, usado para formulários longos sem poluir a janela.

**Métodos documentados individualmente:** 5

- `ScrollableFrame.__init__`
- `ScrollableFrame._sync_scroll_region`
- `ScrollableFrame._sync_width`
- `ScrollableFrame._on_mousewheel`
- `ScrollableFrame.to_top`


### Classe `AcademicPipelineGUI`

**Módulo:** `app_bundle.scripts.pipeline.academic_pipeline_gui`
**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 313–1160
**Bases:** nenhuma explícita
**Decoradores:** nenhum

**Finalidade:** Wizard gráfico focado no perfil de atividade local FGV.

**Métodos documentados individualmente:** 49

- `AcademicPipelineGUI.__init__`
- `AcademicPipelineGUI._build_variables`
- `AcademicPipelineGUI._preset`
- `AcademicPipelineGUI.project_slug`
- `AcademicPipelineGUI.project_dir`
- `AcademicPipelineGUI.config_path`
- `AcademicPipelineGUI.output_dir`
- `AcademicPipelineGUI._activity_data`
- `AcademicPipelineGUI._write_toml`
- `AcademicPipelineGUI._configure_style`
- `AcademicPipelineGUI._build_layout`
- `AcademicPipelineGUI._build_sidebar`
- `AcademicPipelineGUI._build_header`
- `AcademicPipelineGUI._build_progress`
- `AcademicPipelineGUI._build_footer`
- `AcademicPipelineGUI._build_step_pages`
- `AcademicPipelineGUI._card`
- `AcademicPipelineGUI._field`
- `AcademicPipelineGUI._text_field`
- `AcademicPipelineGUI._choose_file`
- `AcademicPipelineGUI._choose_dir`
- `AcademicPipelineGUI._filetypes_for`
- `AcademicPipelineGUI._page_project`
- `AcademicPipelineGUI._page_activity`
- `AcademicPipelineGUI._refresh_data_mode`
- `AcademicPipelineGUI._page_inputs`
- `AcademicPipelineGUI._refresh_corpus_mode`
- `AcademicPipelineGUI._refresh_orientation_mode`
- `AcademicPipelineGUI._capture_orientation_inline`
- `AcademicPipelineGUI._capture_manual_instruction`
- `AcademicPipelineGUI._capture_multiline_fields`
- `AcademicPipelineGUI._page_outputs`
- `AcademicPipelineGUI._page_review`
- `AcademicPipelineGUI._step_index`
- `AcademicPipelineGUI._render_step`
- `AcademicPipelineGUI.previous_step`
- `AcademicPipelineGUI.next_step`
- `AcademicPipelineGUI._validate_through`
- `AcademicPipelineGUI._validate_step`
- `AcademicPipelineGUI._summary_text`
- `AcademicPipelineGUI._refresh_review`
- `AcademicPipelineGUI.save_config`
- `AcademicPipelineGUI.validate_config`
- `AcademicPipelineGUI.generate_activity`
- `AcademicPipelineGUI._run_command`
- `AcademicPipelineGUI._poll_worker_queue`
- `AcademicPipelineGUI._set_busy`
- `AcademicPipelineGUI._append_log`
- `AcademicPipelineGUI._open_path`


### Classe `Preset`

**Módulo:** `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo`
**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 1684–1694
**Bases:** nenhuma explícita
**Decoradores:** `dataclass(frozen=True)`

**Finalidade:** Classe `Preset`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `_WizLocalFlowState`

**Módulo:** `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo`
**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 4421–4447
**Bases:** nenhuma explícita
**Decoradores:** nenhum

**Finalidade:** Classe `_WizLocalFlowState`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 2

- `_WizLocalFlowState.__init__`
- `_WizLocalFlowState.local`


### Classe `_WizInputController`

**Módulo:** `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo`
**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 4450–4748
**Bases:** nenhuma explícita
**Decoradores:** nenhum

**Finalidade:** Classe `_WizInputController`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 8

- `_WizInputController.__init__`
- `_WizInputController.ask`
- `_WizInputController._ask_supported_file`
- `_WizInputController._ask_text_ready_file_and_optional_reuse`
- `_WizInputController._ensure_reference_policy`
- `_WizInputController._print_skip_notice`
- `_WizInputController._ask_and_prepare_corpus`
- `_WizInputController._prepare_known_corpus`


### Classe `AcademicPipelineTUI`

**Módulo:** `app_bundle.scripts.pipeline.academic_pipeline_tui`
**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 61–1433
**Bases:** nenhuma explícita
**Decoradores:** nenhum

**Finalidade:** Coordena o fluxo configurar → conferir → validar → gerar → revisar.

**Métodos documentados individualmente:** 59

- `AcademicPipelineTUI.__init__`
- `AcademicPipelineTUI._load_state`
- `AcademicPipelineTUI._save_state`
- `AcademicPipelineTUI.active_config`
- `AcademicPipelineTUI.set_active_config`
- `AcademicPipelineTUI.load_toml`
- `AcademicPipelineTUI.section`
- `AcademicPipelineTUI.resolve_config_path`
- `AcademicPipelineTUI.is_project_config`
- `AcademicPipelineTUI.is_activity_config`
- `AcademicPipelineTUI.list_project_configs`
- `AcademicPipelineTUI.list_activity_configs`
- `AcademicPipelineTUI.cfg_output_dir`
- `AcademicPipelineTUI.cfg_prefix`
- `AcademicPipelineTUI.input_rows`
- `AcademicPipelineTUI._global_project_context_text`
- `AcademicPipelineTUI._article_context_text`
- `AcademicPipelineTUI.status_text`
- `AcademicPipelineTUI.require_config`
- `AcademicPipelineTUI._write_log`
- `AcademicPipelineTUI.run_command`
- `AcademicPipelineTUI.choose_existing_config`
- `AcademicPipelineTUI.choose_document_profile`
- `AcademicPipelineTUI.choose_project_directory`
- `AcademicPipelineTUI.run_wizard`
- `AcademicPipelineTUI.menu_project`
- `AcademicPipelineTUI.show_inputs`
- `AcademicPipelineTUI.validate_config`
- `AcademicPipelineTUI.show_prompts`
- `AcademicPipelineTUI.write_prompt_lock`
- `AcademicPipelineTUI.generate_full`
- `AcademicPipelineTUI.import_prisma_triage`
- `AcademicPipelineTUI.rerender_existing`
- `AcademicPipelineTUI.recompile_org`
- `AcademicPipelineTUI.cfg_research_output_dir`
- `AcademicPipelineTUI._run_python_file`
- `AcademicPipelineTUI._input_int`
- `AcademicPipelineTUI._article_workflow`
- `AcademicPipelineTUI._article_workflow_status_text`
- `AcademicPipelineTUI._article_workflow_mark_ok`
- `AcademicPipelineTUI._article_workflow_can_run_or_message`
- `AcademicPipelineTUI.mark_prisma_xlsx_reviewed`
- `AcademicPipelineTUI.create_or_edit_briefing`
- `AcademicPipelineTUI.run_prisma_preliminary_search`
- `AcademicPipelineTUI._curadoria_prompt_default`
- `AcademicPipelineTUI.run_prisma_ai_cut`
- `AcademicPipelineTUI.open_prisma_review_xlsx`
- `AcademicPipelineTUI.generate_prisma_final_from_review`
- `AcademicPipelineTUI._ask_article_final_settings`
- `AcademicPipelineTUI.generate_article_final_unified`
- `AcademicPipelineTUI.open_article_output_folder`
- `AcademicPipelineTUI.menu_prisma_article_flow`
- `AcademicPipelineTUI.open_path`
- `AcademicPipelineTUI.list_output_files`
- `AcademicPipelineTUI.menu_outputs`
- `AcademicPipelineTUI.run_configured_flow`
- `AcademicPipelineTUI.run_new_document_flow`
- `AcademicPipelineTUI.run_guided_flow`
- `AcademicPipelineTUI.main_menu`


### Classe `TUIUnavailable`

**Módulo:** `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets`
**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui_widgets.py`
**Linhas:** 22–23
**Bases:** `RuntimeError`
**Decoradores:** nenhum

**Finalidade:** Indica que a dependência opcional da TUI ainda não foi instalada.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `_direct_click_radio.DirectClickRadioList`

**Módulo:** `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets`
**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui_widgets.py`
**Linhas:** 135–150
**Bases:** `base`
**Decoradores:** nenhum

**Finalidade:** Classe `DirectClickRadioList`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 1

- `_direct_click_radio.DirectClickRadioList._get_text_fragments`


### Classe `StageRecord`

**Módulo:** `app_bundle.scripts.pipeline.article_workflow.state`
**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/state.py`
**Linhas:** 40–66
**Bases:** nenhuma explícita
**Decoradores:** `dataclass`

**Finalidade:** Classe `StageRecord`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 2

- `StageRecord.to_dict`
- `StageRecord.from_dict`


### Classe `WorkflowState`

**Módulo:** `app_bundle.scripts.pipeline.article_workflow.state`
**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/state.py`
**Linhas:** 69–181
**Bases:** nenhuma explícita
**Decoradores:** nenhum

**Finalidade:** Leitura/gravação do ``artigo_state.json``.

**Métodos documentados individualmente:** 13

- `WorkflowState.__init__`
- `WorkflowState._now`
- `WorkflowState._default`
- `WorkflowState.load`
- `WorkflowState._normalize`
- `WorkflowState._apply_dependency_blocks`
- `WorkflowState.save`
- `WorkflowState.record`
- `WorkflowState.status`
- `WorkflowState.is_ok`
- `WorkflowState.can_run`
- `WorkflowState.mark`
- `WorkflowState.next_pending_stage`


### Classe `StageValidation`

**Módulo:** `app_bundle.scripts.pipeline.article_workflow.validators`
**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/validators.py`
**Linhas:** 28–33
**Bases:** nenhuma explícita
**Decoradores:** `dataclass`

**Finalidade:** Classe `StageValidation`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `ArticleWorkflow`

**Módulo:** `app_bundle.scripts.pipeline.article_workflow.validators`
**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/validators.py`
**Linhas:** 36–305
**Bases:** nenhuma explícita
**Decoradores:** nenhum

**Finalidade:** Controla estado, inferência por arquivos e contratos de saída.

**Métodos documentados individualmente:** 30

- `ArticleWorkflow.__init__`
- `ArticleWorkflow._store_metadata`
- `ArticleWorkflow._guess_article_cfg`
- `ArticleWorkflow.output_dir`
- `ArticleWorkflow.dados_prisma_dir`
- `ArticleWorkflow.prefix`
- `ArticleWorkflow.org_path`
- `ArticleWorkflow.bib_path`
- `ArticleWorkflow.pdf_path`
- `ArticleWorkflow._read_toml`
- `ArticleWorkflow._section`
- `ArticleWorkflow._resolve_config_path`
- `ArticleWorkflow.research_output_dir`
- `ArticleWorkflow._glob_any`
- `ArticleWorkflow.find_prisma_artifacts`
- `ArticleWorkflow.validate_briefing`
- `ArticleWorkflow.validate_toml_prisma`
- `ArticleWorkflow.validate_prisma_preliminar`
- `ArticleWorkflow.validate_xlsx_cut`
- `ArticleWorkflow.validate_revisao_humana`
- `ArticleWorkflow.validate_prisma_final`
- `ArticleWorkflow.validate_fulltext`
- `ArticleWorkflow.validate_artigo_org`
- `ArticleWorkflow.validate_pdf_final`
- `ArticleWorkflow.validations`
- `ArticleWorkflow.refresh_from_files`
- `ArticleWorkflow.mark_human_review`
- `ArticleWorkflow.mark_stage_ok`
- `ArticleWorkflow.can_run`
- `ArticleWorkflow.format_status`


### Classe `BibMetadata`

**Módulo:** `app_bundle.scripts.pipeline.bibliography_manager`
**Arquivo:** `app_bundle/scripts/pipeline/bibliography_manager.py`
**Linhas:** 128–144
**Bases:** `BaseModel`
**Decoradores:** nenhum

**Finalidade:** Classe `BibMetadata`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `BibBuildResult`

**Módulo:** `app_bundle.scripts.pipeline.bibliography_manager`
**Arquivo:** `app_bundle/scripts/pipeline/bibliography_manager.py`
**Linhas:** 148–153
**Bases:** nenhuma explícita
**Decoradores:** `dataclass`

**Finalidade:** Classe `BibBuildResult`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `CleanAction`

**Módulo:** `app_bundle.scripts.pipeline.clean_bundle`
**Arquivo:** `app_bundle/scripts/pipeline/clean_bundle.py`
**Linhas:** 21–26
**Bases:** nenhuma explícita
**Decoradores:** `dataclass`

**Finalidade:** Classe `CleanAction`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `SourceDoc`

**Módulo:** `app_bundle.scripts.pipeline.corpus_manager`
**Arquivo:** `app_bundle/scripts/pipeline/corpus_manager.py`
**Linhas:** 38–44
**Bases:** nenhuma explícita
**Decoradores:** `dataclass`

**Finalidade:** Classe `SourceDoc`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `_CaptureLogHandler`

**Módulo:** `app_bundle.scripts.pipeline.corpus_manager`
**Arquivo:** `app_bundle/scripts/pipeline/corpus_manager.py`
**Linhas:** 65–76
**Bases:** `logging.Handler`
**Decoradores:** nenhum

**Finalidade:** Classe `_CaptureLogHandler`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 2

- `_CaptureLogHandler.__init__`
- `_CaptureLogHandler.emit`


### Classe `SectionBundle`

**Módulo:** `app_bundle.scripts.pipeline.document_builder`
**Arquivo:** `app_bundle/scripts/pipeline/document_builder.py`
**Linhas:** 230–233
**Bases:** `StrictBaseModel`
**Decoradores:** nenhum

**Finalidade:** Pacote estruturado usado na geração incremental por etapas.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `StrictBaseModel`

**Módulo:** `app_bundle.scripts.pipeline.document_model`
**Arquivo:** `app_bundle/scripts/pipeline/document_model.py`
**Linhas:** 15–22
**Bases:** `BaseModel`
**Decoradores:** nenhum

**Finalidade:** Base estrita compatível com OpenAI Structured Outputs.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `Citation`

**Módulo:** `app_bundle.scripts.pipeline.document_model`
**Arquivo:** `app_bundle/scripts/pipeline/document_model.py`
**Linhas:** 40–55
**Bases:** `StrictBaseModel`
**Decoradores:** nenhum

**Finalidade:** Classe `Citation`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 1

- `Citation.validate_keys`


### Classe `TextSpan`

**Módulo:** `app_bundle.scripts.pipeline.document_model`
**Arquivo:** `app_bundle/scripts/pipeline/document_model.py`
**Linhas:** 58–62
**Bases:** `StrictBaseModel`
**Decoradores:** nenhum

**Finalidade:** Classe `TextSpan`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `TableData`

**Módulo:** `app_bundle.scripts.pipeline.document_model`
**Arquivo:** `app_bundle/scripts/pipeline/document_model.py`
**Linhas:** 68–71
**Bases:** `StrictBaseModel`
**Decoradores:** nenhum

**Finalidade:** Classe `TableData`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `Block`

**Módulo:** `app_bundle.scripts.pipeline.document_model`
**Arquivo:** `app_bundle/scripts/pipeline/document_model.py`
**Linhas:** 74–94
**Bases:** `StrictBaseModel`
**Decoradores:** nenhum

**Finalidade:** Classe `Block`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 1

- `Block.normalize_block`


### Classe `Section`

**Módulo:** `app_bundle.scripts.pipeline.document_model`
**Arquivo:** `app_bundle/scripts/pipeline/document_model.py`
**Linhas:** 97–108
**Bases:** `StrictBaseModel`
**Decoradores:** nenhum

**Finalidade:** Classe `Section`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 1

- `Section.normalize_section`


### Classe `DocumentMetadata`

**Módulo:** `app_bundle.scripts.pipeline.document_model`
**Arquivo:** `app_bundle/scripts/pipeline/document_model.py`
**Linhas:** 111–128
**Bases:** `StrictBaseModel`
**Decoradores:** nenhum

**Finalidade:** Classe `DocumentMetadata`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `AbstractBlock`

**Módulo:** `app_bundle.scripts.pipeline.document_model`
**Arquivo:** `app_bundle/scripts/pipeline/document_model.py`
**Linhas:** 131–134
**Bases:** `StrictBaseModel`
**Decoradores:** nenhum

**Finalidade:** Classe `AbstractBlock`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `FigureSpec`

**Módulo:** `app_bundle.scripts.pipeline.document_model`
**Arquivo:** `app_bundle/scripts/pipeline/document_model.py`
**Linhas:** 137–143
**Bases:** `StrictBaseModel`
**Decoradores:** nenhum

**Finalidade:** Classe `FigureSpec`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `BibliographyInfo`

**Módulo:** `app_bundle.scripts.pipeline.document_model`
**Arquivo:** `app_bundle/scripts/pipeline/document_model.py`
**Linhas:** 146–149
**Bases:** `StrictBaseModel`
**Decoradores:** nenhum

**Finalidade:** Classe `BibliographyInfo`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `DiagnosticsInfo`

**Módulo:** `app_bundle.scripts.pipeline.document_model`
**Arquivo:** `app_bundle/scripts/pipeline/document_model.py`
**Linhas:** 152–164
**Bases:** `StrictBaseModel`
**Decoradores:** nenhum

**Finalidade:** Diagnósticos serializados após a geração.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `AcademicDocument`

**Módulo:** `app_bundle.scripts.pipeline.document_model`
**Arquivo:** `app_bundle/scripts/pipeline/document_model.py`
**Linhas:** 167–179
**Bases:** `StrictBaseModel`
**Decoradores:** nenhum

**Finalidade:** Classe `AcademicDocument`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 1

- `AcademicDocument.validate_document`


### Classe `TranslationError`

**Módulo:** `app_bundle.scripts.pipeline.document_translation`
**Arquivo:** `app_bundle/scripts/pipeline/document_translation.py`
**Linhas:** 19–20
**Bases:** `RuntimeError`
**Decoradores:** nenhum

**Finalidade:** Indica falha na tradução automática de uma versão adicional.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `ComplianceItem`

**Módulo:** `app_bundle.scripts.pipeline.institution_compliance`
**Arquivo:** `app_bundle/scripts/pipeline/institution_compliance.py`
**Linhas:** 42–46
**Bases:** nenhuma explícita
**Decoradores:** `dataclass`

**Finalidade:** Classe `ComplianceItem`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `LayoutSpec`

**Módulo:** `app_bundle.scripts.pipeline.institution_layouts`
**Arquivo:** `app_bundle/scripts/pipeline/institution_layouts.py`
**Linhas:** 33–42
**Bases:** nenhuma explícita
**Decoradores:** `dataclass(frozen=True)`

**Finalidade:** Classe `LayoutSpec`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `MindmapOutput`

**Módulo:** `app_bundle.scripts.pipeline.mindmap_manager`
**Arquivo:** `app_bundle/scripts/pipeline/mindmap_manager.py`
**Linhas:** 31–32
**Bases:** `BaseModel`
**Decoradores:** nenhum

**Finalidade:** Classe `MindmapOutput`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `PaperAbstractError`

**Módulo:** `app_bundle.scripts.pipeline.paper_abstracts`
**Arquivo:** `app_bundle/scripts/pipeline/paper_abstracts.py`
**Linhas:** 29–30
**Bases:** `RuntimeError`
**Decoradores:** nenhum

**Finalidade:** Indica falha na geração, leitura ou inserção de resumos do paper.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `ProviderUnavailableError`

**Módulo:** `app_bundle.scripts.pipeline.prisma_busca_externa`
**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 360–365
**Bases:** `RuntimeError`
**Decoradores:** nenhum

**Finalidade:** Sinaliza que uma fonte deve ser interrompida sem repetir todos os blocos.

**Métodos documentados individualmente:** 1

- `ProviderUnavailableError.__init__`


### Classe `Candidate`

**Módulo:** `app_bundle.scripts.pipeline.prisma_fulltext_garantido`
**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 264–277
**Bases:** nenhuma explícita
**Decoradores:** `dataclass`

**Finalidade:** Classe `Candidate`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `PrismaMetadata`

**Módulo:** `app_bundle.scripts.pipeline.prisma_model`
**Arquivo:** `app_bundle/scripts/pipeline/prisma_model.py`
**Linhas:** 17–30
**Bases:** `BaseModel`
**Decoradores:** nenhum

**Finalidade:** Classe `PrismaMetadata`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `QueryRecord`

**Módulo:** `app_bundle.scripts.pipeline.prisma_model`
**Arquivo:** `app_bundle/scripts/pipeline/prisma_model.py`
**Linhas:** 33–38
**Bases:** `BaseModel`
**Decoradores:** nenhum

**Finalidade:** Classe `QueryRecord`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `SearchStrategy`

**Módulo:** `app_bundle.scripts.pipeline.prisma_model`
**Arquivo:** `app_bundle/scripts/pipeline/prisma_model.py`
**Linhas:** 41–46
**Bases:** `BaseModel`
**Decoradores:** nenhum

**Finalidade:** Classe `SearchStrategy`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `ScreeningCriteria`

**Módulo:** `app_bundle.scripts.pipeline.prisma_model`
**Arquivo:** `app_bundle/scripts/pipeline/prisma_model.py`
**Linhas:** 49–51
**Bases:** `BaseModel`
**Decoradores:** nenhum

**Finalidade:** Classe `ScreeningCriteria`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `PrismaFlow`

**Módulo:** `app_bundle.scripts.pipeline.prisma_model`
**Arquivo:** `app_bundle/scripts/pipeline/prisma_model.py`
**Linhas:** 54–74
**Bases:** `BaseModel`
**Decoradores:** nenhum

**Finalidade:** Classe `PrismaFlow`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 1

- `PrismaFlow.normalize_counts`


### Classe `StudyRecord`

**Módulo:** `app_bundle.scripts.pipeline.prisma_model`
**Arquivo:** `app_bundle/scripts/pipeline/prisma_model.py`
**Linhas:** 77–105
**Bases:** `BaseModel`
**Decoradores:** nenhum

**Finalidade:** Classe `StudyRecord`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 2

- `StudyRecord.normalize_key`
- `StudyRecord.normalize_doi`


### Classe `Diagnostics`

**Módulo:** `app_bundle.scripts.pipeline.prisma_model`
**Arquivo:** `app_bundle/scripts/pipeline/prisma_model.py`
**Linhas:** 108–112
**Bases:** `BaseModel`
**Decoradores:** nenhum

**Finalidade:** Classe `Diagnostics`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `PrismaReport`

**Módulo:** `app_bundle.scripts.pipeline.prisma_model`
**Arquivo:** `app_bundle/scripts/pipeline/prisma_model.py`
**Linhas:** 115–137
**Bases:** `BaseModel`
**Decoradores:** nenhum

**Finalidade:** Classe `PrismaReport`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 1

- `PrismaReport.normalize_report`


### Classe `InitProjectResult`

**Módulo:** `app_bundle.scripts.pipeline.project_tools`
**Arquivo:** `app_bundle/scripts/pipeline/project_tools.py`
**Linhas:** 50–56
**Bases:** nenhuma explícita
**Decoradores:** `dataclass`

**Finalidade:** Classe `InitProjectResult`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `PromptSource`

**Módulo:** `app_bundle.scripts.pipeline.prompt_manager`
**Arquivo:** `app_bundle/scripts/pipeline/prompt_manager.py`
**Linhas:** 31–37
**Bases:** nenhuma explícita
**Decoradores:** `dataclass`

**Finalidade:** Classe `PromptSource`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


### Classe `PromptBundle`

**Módulo:** `app_bundle.scripts.pipeline.prompt_manager`
**Arquivo:** `app_bundle/scripts/pipeline/prompt_manager.py`
**Linhas:** 41–51
**Bases:** nenhuma explícita
**Decoradores:** `dataclass`

**Finalidade:** Classe `PromptBundle`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 1

- `PromptBundle.report`


### Classe `ArticlePaths`

**Módulo:** `app_bundle.scripts.pipeline.render_docx_canonico`
**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 35–47
**Bases:** nenhuma explícita
**Decoradores:** `dataclass`

**Finalidade:** Classe `ArticlePaths`; o código não fornece docstring descritiva.

**Métodos documentados individualmente:** 0

Nenhum identificado pela análise estática.


---

## 44. Dicionário integral de funções, métodos e callbacks

### 44.1. Como ler as fichas

Cada ficha contém:

- assinatura;
- arquivo e intervalo de linhas;
- tipo e visibilidade;
- finalidade baseada em docstring ou, quando ausente, inferência conservadora por AST;
- parâmetros;
- retorno;
- efeitos externos observáveis;
- chamadas realizadas;
- chamadores estáticos quando a resolução por nome é inequívoca;
- exceções explícitas;
- opções CLI mencionadas;
- artefatos literais reconhecidos;
- complexidade estrutural;
- hash integral do arquivo-fonte.

Uma função sem docstring **não recebe uma descrição inventada de regra de negócio**. Nesses casos o manual declara a ausência de docstring e limita-se a inferir responsabilidade a partir de nome, assinatura e corpo.

### `academic_pipeline:main`

**Assinatura:**
```python
def main(argv: Sequence[str] | None = None) -> int
```

**Arquivo:** `academic_pipeline/__init__.py`
**Linhas:** 14–18
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Executa o entrypoint oficial, delegando ao runtime compatível.

**Docstring resumida:** Executa o entrypoint oficial, delegando ao runtime compatível.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `Sequence[str] \| None` | `None` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `cli_main(argv)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `cli_main`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4d08f2e264da8da506351ceaf4a5efd17fa4211bae7b44da458086618137413c`
**LOC do arquivo-fonte:** 18


### `academic_pipeline.check_config_runtime:default_check_config_runtime_context`

**Assinatura:**
```python
def default_check_config_runtime_context() -> CheckConfigRuntimeContext
```

**Arquivo:** `academic_pipeline/check_config_runtime.py`
**Linhas:** 50–66
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `default_check_config_runtime_context` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `CheckConfigRuntimeContext`. Retornos observados estaticamente incluem: `CheckConfigRuntimeContext(load_config=base.load_config, apply_cli_path_overrides=base.apply_cli_path_overrides, output_paths=base.output_paths, research_output_paths=base.research_output_paths, external_search_enabled=base.external_search_enabled, check_config=check_config, print_check_config_report=print_check_config_report, write_json=base.write_json)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `CheckConfigRuntimeContext`
- `default_doctor_runtime_context`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.check_config_runtime:run_check_config_command`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4459286e415f3f2fd240c0bfc15c538b1a5e03146ac323500951aaf51895b56b`
**LOC do arquivo-fonte:** 164


### `academic_pipeline.check_config_runtime:_normalize_argv`

**Assinatura:**
```python
def _normalize_argv(argv: Sequence[str] | None) -> tuple[str, ...]
```

**Arquivo:** `academic_pipeline/check_config_runtime.py`
**Linhas:** 69–73
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Normaliza a responsabilidade representada por `argv` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `Sequence[str] \| None` | `obrigatório/ausente` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |

#### Retorno

Anotação declarada: `tuple[str, ...]`. Retornos observados estaticamente incluem: `tuple((str(item) for item in source))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `str`
- `tuple`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4459286e415f3f2fd240c0bfc15c538b1a5e03146ac323500951aaf51895b56b`
**LOC do arquivo-fonte:** 164


### `academic_pipeline.check_config_runtime:_build_parser`

**Assinatura:**
```python
def _build_parser() -> argparse.ArgumentParser
```

**Arquivo:** `academic_pipeline/check_config_runtime.py`
**Linhas:** 76–85
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Constrói a responsabilidade representada por `parser` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `argparse.ArgumentParser`. Retornos observados estaticamente incluem: `parser`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `CheckConfigRuntimeError`
- `cli_parser.build_parser`
- `isinstance`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `CheckConfigRuntimeError('build_parser não retornou ArgumentParser')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4459286e415f3f2fd240c0bfc15c538b1a5e03146ac323500951aaf51895b56b`
**LOC do arquivo-fonte:** 164


### `academic_pipeline.check_config_runtime:_prepare_config`

**Assinatura:**
```python
def _prepare_config(args: argparse.Namespace, context: CheckConfigRuntimeContext) -> dict[str, Any]
```

**Arquivo:** `academic_pipeline/check_config_runtime.py`
**Linhas:** 88–110
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prepare_config` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `argparse.Namespace` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `context` | positional-or-keyword | `CheckConfigRuntimeContext` | `obrigatório/ausente` | Contexto explícito de dependências utilizado para desacoplar a rotina de imports/globais. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `{}`; `prepared`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `CheckConfigRuntimeError`
- `Path`
- `Path(args.config).expanduser`
- `Path(args.config).expanduser().resolve`
- `context.apply_cli_path_overrides`
- `context.load_config`
- `isinstance`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `CheckConfigRuntimeError('load_config não retornou dict')`
- `CheckConfigRuntimeError('apply_cli_path_overrides não retornou dict')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4459286e415f3f2fd240c0bfc15c538b1a5e03146ac323500951aaf51895b56b`
**LOC do arquivo-fonte:** 164


### `academic_pipeline.check_config_runtime:run_check_config_command`

**Assinatura:**
```python
def run_check_config_command(argv: Sequence[str] | None = None, *, context: CheckConfigRuntimeContext | None = None) -> int
```

**Arquivo:** `academic_pipeline/check_config_runtime.py`
**Linhas:** 113–155
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `check_config_command` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `Sequence[str] \| None` | `None` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |
| `context` | keyword-only | `CheckConfigRuntimeContext \| None` | `None` | Contexto explícito de dependências utilizado para desacoplar a rotina de imports/globais. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `int(result.value)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `CheckConfigRuntimeError`
- `MappingProxyType`
- `_build_parser`
- `_build_parser().parse_args`
- `_normalize_argv`
- `_prepare_config`
- `bool`
- `default_check_config_runtime_context`
- `dispatch_stage_017`
- `int`
- `list`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.runtime:_run_native_check_config`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `CheckConfigRuntimeError('run_check_config_command exige --check-config')`
- `CheckConfigRuntimeError('dispatch_stage_017 não tratou --check-config')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4459286e415f3f2fd240c0bfc15c538b1a5e03146ac323500951aaf51895b56b`
**LOC do arquivo-fonte:** 164


### `academic_pipeline.cli:main`

**Assinatura:**
```python
def main(argv: Sequence[str] | None = None) -> int
```

**Arquivo:** `academic_pipeline/cli.py`
**Linhas:** 10–13
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Executa o runtime oficial por rotas canônicas nativas.

**Docstring resumida:** Executa o runtime oficial por rotas canônicas nativas.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `Sequence[str] \| None` | `None` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `run(argv)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `run`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `570375a810136f385074638b226db9db24f16100a5ff47b4005affd9a8824231`
**LOC do arquivo-fonte:** 13


### `academic_pipeline.cli_parser:build_parser`

**Assinatura:**
```python
def build_parser(*, pipeline_version: str) -> argparse.ArgumentParser
```

**Arquivo:** `academic_pipeline/cli_parser.py`
**Linhas:** 7–122
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Constrói a responsabilidade representada por `parser` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `pipeline_version` | keyword-only | `str` | `obrigatório/ausente` | Parâmetro `pipeline_version` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `argparse.ArgumentParser`. Retornos observados estaticamente incluem: `parser`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `argparse.ArgumentParser`
- `parser.add_argument`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.check_config_runtime:_build_parser`
- `academic_pipeline.cli_parser:parse_args`
- `academic_pipeline.doctor_runtime:_build_parser`
- `academic_pipeline.doi_manifest_runtime:_build_parser`
- `academic_pipeline.institution_compliance_runtime:_build_parser`
- `academic_pipeline.runtime:_build_parser`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `--academic-writing`
- `--base-dir`
- `--bib`
- `--cache-dir`
- `--check-config`
- `--check-institution-compliance`
- `--config`
- `--doctor`
- `--document-json`
- `--docx`
- `--explain-profile`
- `--forcar-regeneracao-mapa-mental`
- `--genero-academico`
- `--gui`
- `--init-project`
- `--init-toml`
- `--input-dir`
- `--input-zip`
- `--inspect-bib`
- `--institution`
- `--latex-extra-path`
- `--layout`
- `--list-institutions`
- `--list-layouts`
- `--list-toml-profiles`
- `--make-doi-manifest`
- `--no-clean`
- `--no-clear`
- `--no-output-subdir`
- `--org`
- `--output`
- `--output-dir`
- `--output-prefix`
- `--overwrite-project`
- `--pdf`
- `--pdf-engine`
- `--prisma-curadoria-fluxo-completo`
- `--prisma-curadoria-ia`
- `--prisma-curadoria-importar`
- `--prisma-curadoria-input`
- `--prisma-curadoria-limiar-minimo`
- `--prisma-curadoria-max-incluir`
- `--prisma-curadoria-menu`
- `--prisma-curadoria-out-dir`
- `--prisma-curadoria-prompt`
- `--prisma-curadoria-reexportar-xlsx`
- `--prisma-curadoria-sem-ia`
- `--prisma-curadoria-top-n-candidatos`
- `--prisma-importar-triagem`
- `--project-type`
- `--quality-report`
- `--recompile`
- `--research-output-dir`
- `--reusar-mapa-mental`
- `--show-prompts`
- `--somente-mapa-mental`
- `--somente-renderizar`
- `--tipo-conteudo`
- `--toml-profile`
- `--tui`
- `--work-dir`
- `--write-prompt-lock`

#### Artefatos/caminhos literais reconhecidos no corpo

- `Gera prompt_lock.json/md para o TOML e encerra`
- `Recompila um .org existente sem chamar IA`
- `Arquivo .org para --recompile`
- `Usa document.json existente e só renderiza saídas`
- `Usa document.json existente e gera/renderiza apenas o mapa mental`
- `Caminho de document.json existente`
- `Cria app_bundle/projetos/<nome> com TOML, ZIPs placeholder e doi_manifest.csv`
- `Gera doi_manifest.csv a partir de --input-zip ou --input-dir`
- `Inspeciona arquivo .bib e gera relatório .md/.json`
- `Gera quality_report.md a partir de --document-json e opcionalmente --org`
- `Arquivo .bib opcional para --quality-report ou --check-institution-compliance`
- `Arquivo .docx opcional para --check-institution-compliance`
- `Arquivo .pdf opcional para --check-institution-compliance`
- `Reexporta o XLSX de curadoria revisado para triagem_humana.csv.`
- `Importa triagem_humana.csv e executa a geração final do PRISMA.`

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f6fd1b98c489e1adf5d8ab61419cab6d78db348b93958ff6d93199df0e5cfbb8`
**LOC do arquivo-fonte:** 133


### `academic_pipeline.cli_parser:parse_args`

**Assinatura:**
```python
def parse_args(argv: Sequence[str] | None = None, *, pipeline_version: str) -> argparse.Namespace
```

**Arquivo:** `academic_pipeline/cli_parser.py`
**Linhas:** 125–130
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Interpreta a responsabilidade representada por `args` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `Sequence[str] \| None` | `None` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |
| `pipeline_version` | keyword-only | `str` | `obrigatório/ausente` | Parâmetro `pipeline_version` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `argparse.Namespace`. Retornos observados estaticamente incluem: `build_parser(pipeline_version=pipeline_version).parse_args(argv)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `build_parser`
- `build_parser(pipeline_version=pipeline_version).parse_args`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f6fd1b98c489e1adf5d8ab61419cab6d78db348b93958ff6d93199df0e5cfbb8`
**LOC do arquivo-fonte:** 133


### `academic_pipeline.command_dispatch:_not_handled`

**Assinatura:**
```python
def _not_handled() -> DispatchResult
```

**Arquivo:** `academic_pipeline/command_dispatch.py`
**Linhas:** 14–15
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_not_handled` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `DispatchResult`. Retornos observados estaticamente incluem: `DispatchResult(False, None)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `DispatchResult`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.command_dispatch:dispatch_stage_001`
- `academic_pipeline.command_dispatch:dispatch_stage_002`
- `academic_pipeline.command_dispatch:dispatch_stage_003`
- `academic_pipeline.command_dispatch:dispatch_stage_004`
- `academic_pipeline.command_dispatch:dispatch_stage_005`
- `academic_pipeline.command_dispatch:dispatch_stage_006`
- `academic_pipeline.command_dispatch:dispatch_stage_007`
- `academic_pipeline.command_dispatch:dispatch_stage_008`
- `academic_pipeline.command_dispatch:dispatch_stage_009`
- `academic_pipeline.command_dispatch:dispatch_stage_010`
- `academic_pipeline.command_dispatch:dispatch_stage_011`
- `academic_pipeline.command_dispatch:dispatch_stage_012`
- `academic_pipeline.command_dispatch:dispatch_stage_013`
- `academic_pipeline.command_dispatch:dispatch_stage_014`
- `academic_pipeline.command_dispatch:dispatch_stage_015`
- `academic_pipeline.command_dispatch:dispatch_stage_016`
- `academic_pipeline.command_dispatch:dispatch_stage_017`
- `academic_pipeline.command_dispatch:dispatch_stage_018`
- `academic_pipeline.command_dispatch:dispatch_stage_019`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9255c4b924fd61b7120b8c5e02684d338f6788de42ae7c352b049a488a308afe`
**LOC do arquivo-fonte:** 325


### `academic_pipeline.command_dispatch:dispatch_stage_001`

**Assinatura:**
```python
def dispatch_stage_001(args: Any, runtime: Mapping[str, Any]) -> DispatchResult
```

**Arquivo:** `academic_pipeline/command_dispatch.py`
**Linhas:** 18–29
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Encaminha a responsabilidade representada por `stage_001` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `Any` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `runtime` | positional-or-keyword | `Mapping[str, Any]` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `DispatchResult`. Retornos observados estaticamente incluem: `DispatchResult(True, run_gui())`; `_not_handled()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `DispatchResult`
- `_not_handled`
- `run_gui`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9255c4b924fd61b7120b8c5e02684d338f6788de42ae7c352b049a488a308afe`
**LOC do arquivo-fonte:** 325


### `academic_pipeline.command_dispatch:dispatch_stage_002`

**Assinatura:**
```python
def dispatch_stage_002(args: Any, runtime: Mapping[str, Any]) -> DispatchResult
```

**Arquivo:** `academic_pipeline/command_dispatch.py`
**Linhas:** 32–43
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Encaminha a responsabilidade representada por `stage_002` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `Any` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `runtime` | positional-or-keyword | `Mapping[str, Any]` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `DispatchResult`. Retornos observados estaticamente incluem: `DispatchResult(True, run_tui(no_clear=bool(args.no_clear)))`; `_not_handled()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `DispatchResult`
- `_not_handled`
- `bool`
- `run_tui`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9255c4b924fd61b7120b8c5e02684d338f6788de42ae7c352b049a488a308afe`
**LOC do arquivo-fonte:** 325


### `academic_pipeline.command_dispatch:dispatch_stage_003`

**Assinatura:**
```python
def dispatch_stage_003(args: Any, runtime: Mapping[str, Any]) -> DispatchResult
```

**Arquivo:** `academic_pipeline/command_dispatch.py`
**Linhas:** 46–58
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Encaminha a responsabilidade representada por `stage_003` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `Any` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `runtime` | positional-or-keyword | `Mapping[str, Any]` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `DispatchResult`. Retornos observados estaticamente incluem: `DispatchResult(True, 0)`; `_not_handled()`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `DispatchResult`
- `_not_handled`
- `print_profiles`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9255c4b924fd61b7120b8c5e02684d338f6788de42ae7c352b049a488a308afe`
**LOC do arquivo-fonte:** 325


### `academic_pipeline.command_dispatch:dispatch_stage_004`

**Assinatura:**
```python
def dispatch_stage_004(args: Any, runtime: Mapping[str, Any]) -> DispatchResult
```

**Arquivo:** `academic_pipeline/command_dispatch.py`
**Linhas:** 61–73
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Encaminha a responsabilidade representada por `stage_004` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `Any` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `runtime` | positional-or-keyword | `Mapping[str, Any]` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `DispatchResult`. Retornos observados estaticamente incluem: `DispatchResult(True, 0)`; `_not_handled()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `DispatchResult`
- `_not_handled`
- `bool`
- `generate_interactive`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9255c4b924fd61b7120b8c5e02684d338f6788de42ae7c352b049a488a308afe`
**LOC do arquivo-fonte:** 325


### `academic_pipeline.command_dispatch:dispatch_stage_005`

**Assinatura:**
```python
def dispatch_stage_005(args: Any, runtime: Mapping[str, Any]) -> DispatchResult
```

**Arquivo:** `academic_pipeline/command_dispatch.py`
**Linhas:** 76–84
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Encaminha a responsabilidade representada por `stage_005` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `Any` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `runtime` | positional-or-keyword | `Mapping[str, Any]` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `DispatchResult`. Retornos observados estaticamente incluem: `DispatchResult(True, 0)`; `_not_handled()`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `DispatchResult`
- `_not_handled`
- `print`
- `runtime['describe_institution_profiles']`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9255c4b924fd61b7120b8c5e02684d338f6788de42ae7c352b049a488a308afe`
**LOC do arquivo-fonte:** 325


### `academic_pipeline.command_dispatch:dispatch_stage_006`

**Assinatura:**
```python
def dispatch_stage_006(args: Any, runtime: Mapping[str, Any]) -> DispatchResult
```

**Arquivo:** `academic_pipeline/command_dispatch.py`
**Linhas:** 87–108
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Encaminha a responsabilidade representada por `stage_006` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `Any` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `runtime` | positional-or-keyword | `Mapping[str, Any]` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `DispatchResult`. Retornos observados estaticamente incluem: `DispatchResult(True, 0)`; `_not_handled()`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `DispatchResult`
- `RuntimeError`
- `_not_handled`
- `layouts.items`
- `print`
- `runtime['Path']`
- `runtime['Path'](args.config).expanduser`
- `runtime['Path'](args.config).expanduser().resolve`
- `runtime['available_layouts']`
- `runtime['load_config']`
- `runtime['resolve_layout_spec']`
- `spec.get`
- `str`
- `str(spec.get('description') or spec.get('descricao') or '').strip`
- `str(spec.get('genero_academico') or '').strip`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('--list-layouts exige --config caminho.toml')`

#### Opções CLI mencionadas diretamente no corpo

- `--list-layouts exige --config caminho.toml`

#### Artefatos/caminhos literais reconhecidos no corpo

- `--list-layouts exige --config caminho.toml`

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9255c4b924fd61b7120b8c5e02684d338f6788de42ae7c352b049a488a308afe`
**LOC do arquivo-fonte:** 325


### `academic_pipeline.command_dispatch:dispatch_stage_007`

**Assinatura:**
```python
def dispatch_stage_007(args: Any, runtime: Mapping[str, Any]) -> DispatchResult
```

**Arquivo:** `academic_pipeline/command_dispatch.py`
**Linhas:** 111–119
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Encaminha a responsabilidade representada por `stage_007` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `Any` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `runtime` | positional-or-keyword | `Mapping[str, Any]` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `DispatchResult`. Retornos observados estaticamente incluem: `DispatchResult(True, 0)`; `_not_handled()`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `DispatchResult`
- `_not_handled`
- `print`
- `runtime['explain_profile']`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9255c4b924fd61b7120b8c5e02684d338f6788de42ae7c352b049a488a308afe`
**LOC do arquivo-fonte:** 325


### `academic_pipeline.command_dispatch:dispatch_stage_008`

**Assinatura:**
```python
def dispatch_stage_008(args: Any, runtime: Mapping[str, Any]) -> DispatchResult
```

**Arquivo:** `academic_pipeline/command_dispatch.py`
**Linhas:** 122–133
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Encaminha a responsabilidade representada por `stage_008` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `Any` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `runtime` | positional-or-keyword | `Mapping[str, Any]` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `DispatchResult`. Retornos observados estaticamente incluem: `DispatchResult(True, 0)`; `_not_handled()`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `DispatchResult`
- `RuntimeError`
- `_not_handled`
- `print`
- `runtime['Path']`
- `runtime['Path'](args.config).expanduser`
- `runtime['Path'](args.config).expanduser().resolve`
- `runtime['json'].dumps`
- `runtime['load_config']`
- `runtime['prompt_report_for_cfg']`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('--show-prompts exige --config caminho.toml')`

#### Opções CLI mencionadas diretamente no corpo

- `--show-prompts exige --config caminho.toml`

#### Artefatos/caminhos literais reconhecidos no corpo

- `--show-prompts exige --config caminho.toml`

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9255c4b924fd61b7120b8c5e02684d338f6788de42ae7c352b049a488a308afe`
**LOC do arquivo-fonte:** 325


### `academic_pipeline.command_dispatch:dispatch_stage_009`

**Assinatura:**
```python
def dispatch_stage_009(args: Any, runtime: Mapping[str, Any]) -> DispatchResult
```

**Arquivo:** `academic_pipeline/command_dispatch.py`
**Linhas:** 136–152
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Encaminha a responsabilidade representada por `stage_009` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `Any` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `runtime` | positional-or-keyword | `Mapping[str, Any]` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `DispatchResult`. Retornos observados estaticamente incluem: `DispatchResult(True, 0)`; `_not_handled()`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `DispatchResult`
- `_not_handled`
- `bool`
- `print`
- `runtime['Path']`
- `runtime['Path'](args.base_dir).expanduser`
- `runtime['Path'](args.base_dir).expanduser().resolve`
- `runtime['init_project']`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9255c4b924fd61b7120b8c5e02684d338f6788de42ae7c352b049a488a308afe`
**LOC do arquivo-fonte:** 325


### `academic_pipeline.command_dispatch:dispatch_stage_010`

**Assinatura:**
```python
def dispatch_stage_010(args: Any, runtime: Mapping[str, Any]) -> DispatchResult
```

**Arquivo:** `academic_pipeline/command_dispatch.py`
**Linhas:** 155–177
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Encaminha a responsabilidade representada por `stage_010` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `Any` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `runtime` | positional-or-keyword | `Mapping[str, Any]` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `DispatchResult`. Retornos observados estaticamente incluem: `DispatchResult(True, 0)`; `_not_handled()`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `DispatchResult`
- `RuntimeError`
- `_not_handled`
- `print`
- `runtime['Path']`
- `runtime['Path'](args.input_dir).expanduser`
- `runtime['Path'](args.input_dir).expanduser().resolve`
- `runtime['Path'](args.input_zip).expanduser`
- `runtime['Path'](args.input_zip).expanduser().resolve`
- `runtime['Path'](args.output).expanduser`
- `runtime['Path'](args.output).expanduser().resolve`
- `runtime['make_doi_manifest']`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('Use --make-doi-manifest com --input-zip ou --input-dir.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `doi_manifest.csv`

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9255c4b924fd61b7120b8c5e02684d338f6788de42ae7c352b049a488a308afe`
**LOC do arquivo-fonte:** 325


### `academic_pipeline.command_dispatch:dispatch_stage_011`

**Assinatura:**
```python
def dispatch_stage_011(args: Any, runtime: Mapping[str, Any]) -> DispatchResult
```

**Arquivo:** `academic_pipeline/command_dispatch.py`
**Linhas:** 180–192
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Encaminha a responsabilidade representada por `stage_011` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `Any` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `runtime` | positional-or-keyword | `Mapping[str, Any]` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `DispatchResult`. Retornos observados estaticamente incluem: `DispatchResult(True, 0 if report.get('ok') else 1)`; `_not_handled()`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `DispatchResult`
- `_not_handled`
- `bib.with_name`
- `print`
- `report.get`
- `runtime['Path']`
- `runtime['Path'](args.inspect_bib).expanduser`
- `runtime['Path'](args.inspect_bib).expanduser().resolve`
- `runtime['inspect_bib']`
- `runtime['render_bib_inspection_markdown']`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.md e `
- `.json`

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9255c4b924fd61b7120b8c5e02684d338f6788de42ae7c352b049a488a308afe`
**LOC do arquivo-fonte:** 325


### `academic_pipeline.command_dispatch:dispatch_stage_012`

**Assinatura:**
```python
def dispatch_stage_012(args: Any, runtime: Mapping[str, Any]) -> DispatchResult
```

**Arquivo:** `academic_pipeline/command_dispatch.py`
**Linhas:** 195–202
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Encaminha a responsabilidade representada por `stage_012` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `Any` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `runtime` | positional-or-keyword | `Mapping[str, Any]` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `DispatchResult`. Retornos observados estaticamente incluem: `_not_handled()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `RuntimeError`
- `_not_handled`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('Use apenas um entre --somente-renderizar e --somente-mapa-mental.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9255c4b924fd61b7120b8c5e02684d338f6788de42ae7c352b049a488a308afe`
**LOC do arquivo-fonte:** 325


### `academic_pipeline.command_dispatch:dispatch_stage_013`

**Assinatura:**
```python
def dispatch_stage_013(args: Any, runtime: Mapping[str, Any]) -> DispatchResult
```

**Arquivo:** `academic_pipeline/command_dispatch.py`
**Linhas:** 205–212
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Encaminha a responsabilidade representada por `stage_013` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `Any` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `runtime` | positional-or-keyword | `Mapping[str, Any]` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `DispatchResult`. Retornos observados estaticamente incluem: `_not_handled()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `RuntimeError`
- `_not_handled`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('Use apenas um entre --reusar-mapa-mental e --forcar-regeneracao-mapa-mental.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9255c4b924fd61b7120b8c5e02684d338f6788de42ae7c352b049a488a308afe`
**LOC do arquivo-fonte:** 325


### `academic_pipeline.command_dispatch:dispatch_stage_014`

**Assinatura:**
```python
def dispatch_stage_014(args: Any, runtime: Mapping[str, Any]) -> DispatchResult
```

**Arquivo:** `academic_pipeline/command_dispatch.py`
**Linhas:** 215–231
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Encaminha a responsabilidade representada por `stage_014` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `Any` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `runtime` | positional-or-keyword | `Mapping[str, Any]` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `DispatchResult`. Retornos observados estaticamente incluem: `DispatchResult(True, 0)`; `_not_handled()`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `DispatchResult`
- `RuntimeError`
- `_not_handled`
- `print`
- `runtime['external_search_enabled']`
- `runtime['output_paths']`
- `runtime['research_output_paths']`
- `runtime['write_prompt_lock']`
- `runtime['write_prompt_lock_markdown']`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('--write-prompt-lock exige --config caminho.toml')`

#### Opções CLI mencionadas diretamente no corpo

- `--write-prompt-lock exige --config caminho.toml`

#### Artefatos/caminhos literais reconhecidos no corpo

- `--write-prompt-lock exige --config caminho.toml`
- `.prompt_lock.json`
- `.prompt_lock.md`

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9255c4b924fd61b7120b8c5e02684d338f6788de42ae7c352b049a488a308afe`
**LOC do arquivo-fonte:** 325


### `academic_pipeline.command_dispatch:dispatch_stage_015`

**Assinatura:**
```python
def dispatch_stage_015(args: Any, runtime: Mapping[str, Any]) -> DispatchResult
```

**Arquivo:** `academic_pipeline/command_dispatch.py`
**Linhas:** 234–252
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Encaminha a responsabilidade representada por `stage_015` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `Any` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `runtime` | positional-or-keyword | `Mapping[str, Any]` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `DispatchResult`. Retornos observados estaticamente incluem: `DispatchResult(True, 0 if report.get('ok') else 2)`; `_not_handled()`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `DispatchResult`
- `RuntimeError`
- `_not_handled`
- `print`
- `report.get`
- `runtime['Path']`
- `runtime['Path'](args.bib).expanduser`
- `runtime['Path'](args.bib).expanduser().resolve`
- `runtime['Path'](args.docx).expanduser`
- `runtime['Path'](args.docx).expanduser().resolve`
- `runtime['Path'](args.org).expanduser`
- `runtime['Path'](args.org).expanduser().resolve`
- `runtime['Path'](args.pdf).expanduser`
- `runtime['Path'](args.pdf).expanduser().resolve`
- `runtime['output_paths']`
- `runtime['render_compliance_markdown']`
- `runtime['run_institution_compliance']`
- `runtime['write_compliance_reports']`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.institution_compliance_runtime:run_institution_compliance_command`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('--check-institution-compliance exige --config caminho.toml')`

#### Opções CLI mencionadas diretamente no corpo

- `--check-institution-compliance exige --config caminho.toml`

#### Artefatos/caminhos literais reconhecidos no corpo

- `--check-institution-compliance exige --config caminho.toml`
- `.org`
- `.bib`
- `.docx`
- `.pdf`

#### Complexidade estrutural observada

```yaml
branches: 7
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9255c4b924fd61b7120b8c5e02684d338f6788de42ae7c352b049a488a308afe`
**LOC do arquivo-fonte:** 325


### `academic_pipeline.command_dispatch:dispatch_stage_016`

**Assinatura:**
```python
def dispatch_stage_016(args: Any, runtime: Mapping[str, Any]) -> DispatchResult
```

**Arquivo:** `academic_pipeline/command_dispatch.py`
**Linhas:** 255–267
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Encaminha a responsabilidade representada por `stage_016` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `Any` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `runtime` | positional-or-keyword | `Mapping[str, Any]` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `DispatchResult`. Retornos observados estaticamente incluem: `DispatchResult(True, 0 if report.get('ok') else 2)`; `_not_handled()`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `DispatchResult`
- `_not_handled`
- `report.get`
- `runtime['external_search_enabled']`
- `runtime['output_paths']`
- `runtime['print_doctor_report']`
- `runtime['research_output_paths']`
- `runtime['run_doctor']`
- `runtime['write_json']`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.doctor_report.json`

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9255c4b924fd61b7120b8c5e02684d338f6788de42ae7c352b049a488a308afe`
**LOC do arquivo-fonte:** 325


### `academic_pipeline.command_dispatch:dispatch_stage_017`

**Assinatura:**
```python
def dispatch_stage_017(args: Any, runtime: Mapping[str, Any]) -> DispatchResult
```

**Arquivo:** `academic_pipeline/command_dispatch.py`
**Linhas:** 270–283
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Encaminha a responsabilidade representada por `stage_017` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `Any` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `runtime` | positional-or-keyword | `Mapping[str, Any]` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `DispatchResult`. Retornos observados estaticamente incluem: `DispatchResult(True, 0 if report.get('ok') else 2)`; `_not_handled()`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `DispatchResult`
- `RuntimeError`
- `_not_handled`
- `report.get`
- `runtime['check_config']`
- `runtime['external_search_enabled']`
- `runtime['output_paths']`
- `runtime['print_check_config_report']`
- `runtime['research_output_paths']`
- `runtime['write_json']`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.check_config_runtime:run_check_config_command`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('--check-config exige --config caminho.toml')`

#### Opções CLI mencionadas diretamente no corpo

- `--check-config exige --config caminho.toml`

#### Artefatos/caminhos literais reconhecidos no corpo

- `--check-config exige --config caminho.toml`
- `.check_config_report.json`

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9255c4b924fd61b7120b8c5e02684d338f6788de42ae7c352b049a488a308afe`
**LOC do arquivo-fonte:** 325


### `academic_pipeline.command_dispatch:dispatch_stage_018`

**Assinatura:**
```python
def dispatch_stage_018(args: Any, runtime: Mapping[str, Any]) -> DispatchResult
```

**Arquivo:** `academic_pipeline/command_dispatch.py`
**Linhas:** 286–293
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Encaminha a responsabilidade representada por `stage_018` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `Any` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `runtime` | positional-or-keyword | `Mapping[str, Any]` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `DispatchResult`. Retornos observados estaticamente incluem: `DispatchResult(True, runtime['run_recompile'](args, runtime['cfg']))`; `_not_handled()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `DispatchResult`
- `_not_handled`
- `runtime['run_recompile']`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9255c4b924fd61b7120b8c5e02684d338f6788de42ae7c352b049a488a308afe`
**LOC do arquivo-fonte:** 325


### `academic_pipeline.command_dispatch:dispatch_stage_019`

**Assinatura:**
```python
def dispatch_stage_019(args: Any, runtime: Mapping[str, Any]) -> DispatchResult
```

**Arquivo:** `academic_pipeline/command_dispatch.py`
**Linhas:** 296–322
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Encaminha a responsabilidade representada por `stage_019` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `Any` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `runtime` | positional-or-keyword | `Mapping[str, Any]` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `DispatchResult`. Retornos observados estaticamente incluem: `DispatchResult(True, 0)`; `_not_handled()`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `DispatchResult`
- `RuntimeError`
- `_not_handled`
- `artifacts.get`
- `isinstance`
- `prisma_outputs.setdefault`
- `runtime['Path']`
- `runtime['cfg'].get`
- `runtime['external_search_enabled']`
- `runtime['import_manual_prisma_triage']`
- `runtime['make_run_report']`
- `runtime['print_outputs']`
- `runtime['render_external_prisma_outputs']`
- `runtime['research_output_paths']`
- `runtime['stage']`
- `runtime['write_json']`
- `runtime['write_outputs_manifest']`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('--prisma-importar-triagem exige um TOML do perfil relatorio_prisma_busca_orientada_fgv.')`

#### Opções CLI mencionadas diretamente no corpo

- `--prisma-importar-triagem exige um TOML do perfil relatorio_prisma_busca_orientada_fgv.`

#### Artefatos/caminhos literais reconhecidos no corpo

- `.run_report.json`
- `.outputs.txt`

#### Complexidade estrutural observada

```yaml
branches: 9
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9255c4b924fd61b7120b8c5e02684d338f6788de42ae7c352b049a488a308afe`
**LOC do arquivo-fonte:** 325


### `academic_pipeline.default_runtime:stage`

**Assinatura:**
```python
def stage(message: str) -> None
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 249–252
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Mostra etapas de execução em tempo real.

**Docstring resumida:** Mostra etapas de execução em tempo real.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `message` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `message` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `_ap003e_impl_stage_1({**globals(), **locals()}, message)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_ap003e_impl_stage_1`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.default_runtime:_ap003f_pipeline_core`
- `academic_pipeline.prisma_generic_orchestration:_ap003e_body_render_external_prisma_outputs_1`
- `academic_pipeline.prisma_generic_orchestration:run_prisma_stage_003`
- `academic_pipeline.prisma_generic_orchestration:run_prisma_stage_004`
- `academic_pipeline.prisma_generic_orchestration:run_prisma_stage_007`
- `academic_pipeline.prisma_generic_orchestration:run_prisma_stage_008`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:_json_or_none`

**Assinatura:**
```python
def _json_or_none(value: str | None) -> Any
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 255–257
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_json_or_none` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `str \| None` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Any`. Retornos observados estaticamente incluem: `_ap003e_impl__json_or_none_1({**globals(), **locals()}, value)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_ap003e_impl__json_or_none_1`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:run_prisma_stage_006`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:load_config @ L260`

**Assinatura:**
```python
def load_config(path: Path) -> dict[str, Any]
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 260–266
**Tipo:** function
**Visibilidade:** pública por nome
**Status de binding estático:** definição anterior no mesmo namespace; é candidata a ser sombreada pela redefinição posterior em `L1241` sob execução sequencial normal do módulo.
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Carrega a responsabilidade representada por `config` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `cfg`.

#### Efeitos colaterais e fronteiras externas

- Abre arquivo para leitura ou inspeção.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `apply_institution_profile`
- `path.open`
- `path.resolve`
- `str`
- `tomllib.load`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:make_client`

**Assinatura:**
```python
def make_client(model_override: str | None = None) -> tuple[Any, str]
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 269–271
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Cria a responsabilidade representada por `client` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `model_override` | positional-or-keyword | `str \| None` | `None` | Parâmetro `model_override` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `tuple[Any, str]`. Retornos observados estaticamente incluem: `_ap003e_impl_make_client_1({**globals(), **locals()}, model_override)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_ap003e_impl_make_client_1`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.default_runtime:_ap003f_pipeline_core`
- `academic_pipeline.prisma_generic_orchestration:run_prisma_stage_004`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:_section`

**Assinatura:**
```python
def _section(cfg: dict[str, Any], name: str) -> dict[str, Any]
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 274–276
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_section` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `name` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `name` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `_ap003e_impl__section_1({**globals(), **locals()}, cfg, name)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_ap003e_impl__section_1`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:output_paths`

**Assinatura:**
```python
def output_paths(cfg: dict[str, Any]) -> tuple[Path, str]
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 279–282
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Resolve a saída final do documento pela seção [paths].

**Docstring resumida:** Resolve a saída final do documento pela seção [paths].

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `tuple[Path, str]`. Retornos observados estaticamente incluem: `_impl_output_paths({**globals(), **locals()}, cfg)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_impl_output_paths`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.doctor_runtime:run_doctor_command`
- `academic_pipeline.prisma_generic_orchestration:run_prisma_stage_002`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:research_output_paths`

**Assinatura:**
```python
def research_output_paths(cfg: dict[str, Any]) -> tuple[Path, str]
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 285–294
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Resolve a saída canônica da busca e consolidação PRISMA.

**Docstring resumida:** Resolve a saída canônica da busca e consolidação PRISMA.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `tuple[Path, str]`. Retornos observados estaticamente incluem: `_ap003e_impl_research_output_paths_1({**globals(), **locals()}, cfg)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_ap003e_impl_research_output_paths_1`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.doctor_runtime:run_doctor_command`
- `academic_pipeline.prisma_generic_orchestration:run_prisma_stage_002`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:work_cache_paths`

**Assinatura:**
```python
def work_cache_paths(cfg: dict[str, Any], prefix: str) -> tuple[Path, Path]
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 297–307
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Resolve diretórios operacionais pela seção [paths].

**Docstring resumida:** Resolve diretórios operacionais pela seção [paths].

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `prefix` | positional-or-keyword | `str` | `obrigatório/ausente` | Prefixo canônico usado para compor nomes de artefatos. |

#### Retorno

Anotação declarada: `tuple[Path, Path]`. Retornos observados estaticamente incluem: `(work_dir, cache_dir)`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(str(cfg.get('__config_dir__') or Path.cwd())).resolve`
- `Path.cwd`
- `_section`
- `bool`
- `cache_dir.mkdir`
- `cfg.get`
- `paths.get`
- `resolve_path`
- `str`
- `work_dir.mkdir`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.default_runtime:_ap003f_pipeline_core`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:apply_cli_path_overrides`

**Assinatura:**
```python
def apply_cli_path_overrides(cfg: dict[str, Any], args: argparse.Namespace) -> dict[str, Any]
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 310–317
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Aplica overrides de caminhos informados na linha de comando.

**Docstring resumida:** Aplica overrides de caminhos informados na linha de comando.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `args` | positional-or-keyword | `argparse.Namespace` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `_impl_apply_cli_path_overrides({**globals(), **locals()}, cfg, args)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_impl_apply_cli_path_overrides`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.check_config_runtime:_prepare_config`
- `academic_pipeline.default_runtime:_ap003f_pipeline_core`
- `academic_pipeline.doctor_runtime:run_doctor_command`
- `academic_pipeline.institution_compliance_runtime:_prepare_config`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:load_existing_document_json`

**Assinatura:**
```python
def load_existing_document_json(path: Path) -> AcademicDocument
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 320–322
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Carrega a responsabilidade representada por `existing_document_json` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `AcademicDocument`. Retornos observados estaticamente incluem: `_impl_load_existing_document_json({**globals(), **locals()}, path)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_impl_load_existing_document_json`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.default_runtime:_ap003f_pipeline_core`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:resolve_bib_for_existing_document`

**Assinatura:**
```python
def resolve_bib_for_existing_document(document: AcademicDocument, document_json_path: Path, out_dir: Path, prefix: str) -> tuple[Path, list[str]]
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 325–328
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Resolve o .bib em modo --somente-renderizar sem exigir que ele já esteja no output_dir.

**Docstring resumida:** Resolve o .bib em modo --somente-renderizar sem exigir que ele já esteja no output_dir.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `document` | positional-or-keyword | `AcademicDocument` | `obrigatório/ausente` | Parâmetro `document` recebido pela função; não há descrição individual explícita no código. |
| `document_json_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `out_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `out_dir` recebido pela função; não há descrição individual explícita no código. |
| `prefix` | positional-or-keyword | `str` | `obrigatório/ausente` | Prefixo canônico usado para compor nomes de artefatos. |

#### Retorno

Anotação declarada: `tuple[Path, list[str]]`. Retornos observados estaticamente incluem: `_impl_resolve_bib_for_existing_document({**globals(), **locals()}, document, document_json_path, out_dir, prefix)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_impl_resolve_bib_for_existing_document`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.default_runtime:_ap003f_pipeline_core`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `Resolve o .bib em modo --somente-renderizar sem exigir que ele já esteja no output_dir.`

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:_openai_model_from_cfg`

**Assinatura:**
```python
def _openai_model_from_cfg(cfg: dict[str, Any]) -> str
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 331–332
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_openai_model_from_cfg` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `str((cfg.get('openai', {}) if isinstance(cfg.get('openai'), dict) else {}).get('model') or os.getenv('OPENAI_MODEL') or DEFAULT_MODEL)`.

#### Efeitos colaterais e fronteiras externas

- Lê configuração/credenciais do ambiente do processo.
- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `(cfg.get('openai', {}) if isinstance(cfg.get('openai'), dict) else {}).get`
- `cfg.get`
- `isinstance`
- `os.getenv`
- `str`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.default_runtime:_ap003f_pipeline_core`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:_load_optional_config`

**Assinatura:**
```python
def _load_optional_config(path: str | None) -> dict[str, Any] | None
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 335–341
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Carrega a responsabilidade representada por `optional_config` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `str \| None` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `dict[str, Any] | None`. Retornos observados estaticamente incluem: `None`; `load_config(p)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `FileNotFoundError`
- `Path`
- `Path(path).expanduser`
- `Path(path).expanduser().resolve`
- `load_config`
- `p.exists`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.default_runtime:_ap003f_pipeline_core`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `FileNotFoundError(f'Config não encontrada: {p}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:_resolve_latex_paths_for_recompile`

**Assinatura:**
```python
def _resolve_latex_paths_for_recompile(args: argparse.Namespace, cfg: dict[str, Any] | None) -> tuple[Path | None, Path | None, str]
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 344–346
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Resolve a responsabilidade representada por `latex_paths_for_recompile` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `argparse.Namespace` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `cfg` | positional-or-keyword | `dict[str, Any] \| None` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `tuple[Path | None, Path | None, str]`. Retornos observados estaticamente incluem: `_impl_resolve_latex_paths_for_recompile({**globals(), **locals()}, args, cfg)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_impl_resolve_latex_paths_for_recompile`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:run_recompile`

**Assinatura:**
```python
def run_recompile(args: argparse.Namespace, cfg: dict[str, Any] | None) -> int
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 349–351
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `recompile` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `argparse.Namespace` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `cfg` | positional-or-keyword | `dict[str, Any] \| None` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `_impl_run_recompile({**globals(), **locals()}, args, cfg)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_impl_run_recompile`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:render_external_prisma_outputs`

**Assinatura:**
```python
def render_external_prisma_outputs(cfg: dict[str, Any], out_dir: Path, prefix: str, prisma_payload: dict[str, Any], *, phase: str) -> tuple[Path | None, Path | None]
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 355–363
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Renderiza relatório PRISMA externo em ORG e, quando solicitado, em PDF.

**Docstring resumida:** Renderiza relatório PRISMA externo em ORG e, quando solicitado, em PDF.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `out_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `out_dir` recebido pela função; não há descrição individual explícita no código. |
| `prefix` | positional-or-keyword | `str` | `obrigatório/ausente` | Prefixo canônico usado para compor nomes de artefatos. |
| `prisma_payload` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Payload estruturado recebido ou produzido pela operação. |
| `phase` | keyword-only | `str` | `obrigatório/ausente` | Parâmetro `phase` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `tuple[Path | None, Path | None]`. Retornos observados estaticamente incluem: `_ap003e_impl_render_external_prisma_outputs_1({**globals(), **locals()}, cfg, out_dir, prefix, prisma_payload, phase=phase)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_ap003e_impl_render_external_prisma_outputs_1`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:run_prisma_stage_004`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:render_additional_language_versions`

**Assinatura:**
```python
def render_additional_language_versions(*, client: Any, model: str, cfg: dict[str, Any], document: AcademicDocument, bib_path: Path, bib_keys: list[str], out_dir: Path, prefix: str, doc_cfg: dict[str, Any], latex_cfg: dict[str, Any], config_dir: Path, abstract_bundle: dict[str, Any] | None = None) -> tuple[dict[str, Any], list[str]]
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 366–374
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Traduz e renderiza versões adicionais a partir do document.json canônico.

**Docstring resumida:** Traduz e renderiza versões adicionais a partir do document.json canônico.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `client` | keyword-only | `Any` | `obrigatório/ausente` | Cliente de serviço externo/IA utilizado pela rotina; pode ser opcional conforme o fluxo. |
| `model` | keyword-only | `str` | `obrigatório/ausente` | Identificador do modelo utilizado pela etapa de IA. |
| `cfg` | keyword-only | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `document` | keyword-only | `AcademicDocument` | `obrigatório/ausente` | Parâmetro `document` recebido pela função; não há descrição individual explícita no código. |
| `bib_path` | keyword-only | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `bib_keys` | keyword-only | `list[str]` | `obrigatório/ausente` | Parâmetro `bib_keys` recebido pela função; não há descrição individual explícita no código. |
| `out_dir` | keyword-only | `Path` | `obrigatório/ausente` | Parâmetro `out_dir` recebido pela função; não há descrição individual explícita no código. |
| `prefix` | keyword-only | `str` | `obrigatório/ausente` | Prefixo canônico usado para compor nomes de artefatos. |
| `doc_cfg` | keyword-only | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `latex_cfg` | keyword-only | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `config_dir` | keyword-only | `Path` | `obrigatório/ausente` | Parâmetro `config_dir` recebido pela função; não há descrição individual explícita no código. |
| `abstract_bundle` | keyword-only | `dict[str, Any] \| None` | `None` | Parâmetro `abstract_bundle` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `tuple[dict[str, Any], list[str]]`. Retornos observados estaticamente incluem: `_impl_render_additional_language_versions({**globals(), **locals()}, client=client, model=model, cfg=cfg, document=document, bib_path=bib_path, bib_keys=bib_keys, out_dir=out_dir, prefix=prefix, doc_cfg=doc_cfg, latex_cfg=latex_cfg, config_dir=config_dir, abstract_bundle=abstract_bundle)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_impl_render_additional_language_versions`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:_prisma_curadoria_default_config`

**Assinatura:**
```python
def _prisma_curadoria_default_config() -> str
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 379–381
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_curadoria_default_config` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `_ap003e_impl__prisma_curadoria_default_config_1({**globals(), **locals()})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_ap003e_impl__prisma_curadoria_default_config_1`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_config_from_args_1`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:_prisma_curadoria_default_out_dir`

**Assinatura:**
```python
def _prisma_curadoria_default_out_dir() -> str
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 384–386
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_curadoria_default_out_dir` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `_ap003e_impl__prisma_curadoria_default_out_dir_1({**globals(), **locals()})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_ap003e_impl__prisma_curadoria_default_out_dir_1`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_out_from_args_1`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:_prisma_curadoria_default_prompt`

**Assinatura:**
```python
def _prisma_curadoria_default_prompt() -> str
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 389–391
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_curadoria_default_prompt` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `_ap003e_impl__prisma_curadoria_default_prompt_1({**globals(), **locals()})`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `_ap003e_impl__prisma_curadoria_default_prompt_1`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_prompt_from_args_1`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:_prisma_curadoria_script_path`

**Assinatura:**
```python
def _prisma_curadoria_script_path() -> str
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 394–396
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_curadoria_script_path` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `_ap003e_impl__prisma_curadoria_script_path_1({**globals(), **locals()})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_ap003e_impl__prisma_curadoria_script_path_1`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_build_cmd_1`
- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_mostrar_caminhos_1`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:_prisma_curadoria_arg`

**Assinatura:**
```python
def _prisma_curadoria_arg(args, name: str, default = None)
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 399–401
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_curadoria_arg` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `name` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `name` recebido pela função; não há descrição individual explícita no código. |
| `default` | positional-or-keyword | `não anotado` | `None` | Parâmetro `default` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_ap003e_impl__prisma_curadoria_arg_1({**globals(), **locals()}, args, name, default)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_ap003e_impl__prisma_curadoria_arg_1`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_build_cmd_1`
- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_config_from_args_1`
- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_dispatch_1`
- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_fluxo_completo_1`
- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_input_from_args_1`
- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_out_from_args_1`
- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_prompt_from_args_1`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:_prisma_curadoria_config_from_args`

**Assinatura:**
```python
def _prisma_curadoria_config_from_args(args) -> str
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 404–406
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_curadoria_config_from_args` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `_ap003e_impl__prisma_curadoria_config_from_args_1({**globals(), **locals()}, args)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_ap003e_impl__prisma_curadoria_config_from_args_1`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_build_cmd_1`
- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_importar_no_pipeline_1`
- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_mostrar_caminhos_1`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:_prisma_curadoria_out_from_args`

**Assinatura:**
```python
def _prisma_curadoria_out_from_args(args) -> str
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 409–411
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_curadoria_out_from_args` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `_ap003e_impl__prisma_curadoria_out_from_args_1({**globals(), **locals()}, args)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_ap003e_impl__prisma_curadoria_out_from_args_1`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_build_cmd_1`
- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_importar_no_pipeline_1`
- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_input_from_args_1`
- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_mostrar_caminhos_1`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:_prisma_curadoria_prompt_from_args`

**Assinatura:**
```python
def _prisma_curadoria_prompt_from_args(args) -> str
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 414–416
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_curadoria_prompt_from_args` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `_ap003e_impl__prisma_curadoria_prompt_from_args_1({**globals(), **locals()}, args)`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `_ap003e_impl__prisma_curadoria_prompt_from_args_1`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_build_cmd_1`
- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_mostrar_caminhos_1`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:_prisma_curadoria_input_from_args`

**Assinatura:**
```python
def _prisma_curadoria_input_from_args(args, *, default_xlsx: bool = False) -> str
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 419–421
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_curadoria_input_from_args` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `default_xlsx` | keyword-only | `bool` | `False` | Parâmetro `default_xlsx` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `_ap003e_impl__prisma_curadoria_input_from_args_1({**globals(), **locals()}, args, default_xlsx=default_xlsx)`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `_ap003e_impl__prisma_curadoria_input_from_args_1`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_build_cmd_1`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:_prisma_curadoria_run_command`

**Assinatura:**
```python
def _prisma_curadoria_run_command(cmd: list[str]) -> int
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 424–426
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_curadoria_run_command` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cmd` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `cmd` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `_ap003e_impl__prisma_curadoria_run_command_1({**globals(), **locals()}, cmd)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_ap003e_impl__prisma_curadoria_run_command_1`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_importar_no_pipeline_1`
- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_reexportar_xlsx_1`
- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_run_ia_1`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:_prisma_curadoria_build_cmd`

**Assinatura:**
```python
def _prisma_curadoria_build_cmd(args, *, usar_ia: bool, reexportar_xlsx: bool = False) -> list[str]
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 429–431
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_curadoria_build_cmd` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `usar_ia` | keyword-only | `bool` | `obrigatório/ausente` | Parâmetro `usar_ia` recebido pela função; não há descrição individual explícita no código. |
| `reexportar_xlsx` | keyword-only | `bool` | `False` | Parâmetro `reexportar_xlsx` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `_ap003e_impl__prisma_curadoria_build_cmd_1({**globals(), **locals()}, args, usar_ia=usar_ia, reexportar_xlsx=reexportar_xlsx)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_ap003e_impl__prisma_curadoria_build_cmd_1`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_reexportar_xlsx_1`
- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_run_ia_1`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:_prisma_curadoria_run_ia`

**Assinatura:**
```python
def _prisma_curadoria_run_ia(args, *, usar_ia: bool = True) -> int
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 434–436
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_curadoria_run_ia` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `usar_ia` | keyword-only | `bool` | `True` | Parâmetro `usar_ia` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `_ap003e_impl__prisma_curadoria_run_ia_1({**globals(), **locals()}, args, usar_ia=usar_ia)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_ap003e_impl__prisma_curadoria_run_ia_1`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_dispatch_1`
- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_fluxo_completo_1`
- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_menu_1`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:_prisma_curadoria_reexportar_xlsx`

**Assinatura:**
```python
def _prisma_curadoria_reexportar_xlsx(args) -> int
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 439–441
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_curadoria_reexportar_xlsx` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `_ap003e_impl__prisma_curadoria_reexportar_xlsx_1({**globals(), **locals()}, args)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_ap003e_impl__prisma_curadoria_reexportar_xlsx_1`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_dispatch_1`
- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_menu_1`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:_prisma_curadoria_pipeline_supports_flag`

**Assinatura:**
```python
def _prisma_curadoria_pipeline_supports_flag(flag: str) -> bool
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 444–446
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_curadoria_pipeline_supports_flag` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `flag` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `flag` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `_ap003e_impl__prisma_curadoria_pipeline_supports_flag_1({**globals(), **locals()}, flag)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_ap003e_impl__prisma_curadoria_pipeline_supports_flag_1`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_importar_no_pipeline_1`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:_prisma_curadoria_importar_no_pipeline`

**Assinatura:**
```python
def _prisma_curadoria_importar_no_pipeline(args) -> int
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 449–451
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_curadoria_importar_no_pipeline` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `_ap003e_impl__prisma_curadoria_importar_no_pipeline_1({**globals(), **locals()}, args)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_ap003e_impl__prisma_curadoria_importar_no_pipeline_1`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_dispatch_1`
- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_fluxo_completo_1`
- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_menu_1`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:_prisma_curadoria_fluxo_completo`

**Assinatura:**
```python
def _prisma_curadoria_fluxo_completo(args) -> int
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 454–456
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_curadoria_fluxo_completo` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `_ap003e_impl__prisma_curadoria_fluxo_completo_1({**globals(), **locals()}, args)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_ap003e_impl__prisma_curadoria_fluxo_completo_1`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_dispatch_1`
- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_menu_1`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:_prisma_curadoria_mostrar_caminhos`

**Assinatura:**
```python
def _prisma_curadoria_mostrar_caminhos(args) -> None
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 459–461
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_curadoria_mostrar_caminhos` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `_ap003e_impl__prisma_curadoria_mostrar_caminhos_1({**globals(), **locals()}, args)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_ap003e_impl__prisma_curadoria_mostrar_caminhos_1`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_menu_1`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:_prisma_curadoria_menu`

**Assinatura:**
```python
def _prisma_curadoria_menu(args) -> int
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 464–466
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_curadoria_menu` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `_ap003e_impl__prisma_curadoria_menu_1({**globals(), **locals()}, args)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_ap003e_impl__prisma_curadoria_menu_1`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_dispatch_1`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:_prisma_curadoria_dispatch`

**Assinatura:**
```python
def _prisma_curadoria_dispatch(args) -> int
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 469–471
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_curadoria_dispatch` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `_ap003e_impl__prisma_curadoria_dispatch_1({**globals(), **locals()}, args)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_ap003e_impl__prisma_curadoria_dispatch_1`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:run_prisma_stage_001`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:_ap003f_pipeline_core`

**Assinatura:**
```python
def _ap003f_pipeline_core() -> int
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 475–1220
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_ap003f_pipeline_core` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `_ap003e_result_001.value`; `_ap003c_result_001.value`; `_ap003c_result_002.value`; `_ap003c_result_003.value`; `_ap003c_result_004.value`; `_ap003c_result_005.value`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.
- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `', '.join`
- `'\n- '.join`
- `(cfg.get('documentos_locais', {}) if isinstance(cfg.get('documentos_locais'), dict) else {}).get`
- `FileNotFoundError`
- `Path`
- `Path(args.document_json).expanduser`
- `Path(args.document_json).expanduser().resolve`
- `Path(str(cfg.get('__config_dir__'))).resolve`
- `RuntimeError`
- `_ap003c_dispatch_001`
- `_ap003c_dispatch_002`
- `_ap003c_dispatch_003`
- `_ap003c_dispatch_004`
- `_ap003c_dispatch_005`
- `_ap003c_dispatch_006`
- `_ap003c_dispatch_007`
- `_ap003c_dispatch_008`
- `_ap003c_dispatch_009`
- `_ap003c_dispatch_010`
- `_ap003c_dispatch_011`
- `_ap003c_dispatch_012`
- `_ap003c_dispatch_013`
- `_ap003c_dispatch_014`
- `_ap003c_dispatch_015`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_ap003e_body_main_2`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('Informe --config, ou use --doctor sem config.')`
- `RuntimeError('Configuração inválida:\n- ' + '\n- '.join(precheck['errors']))`
- `FileNotFoundError(f'document.json não encontrado para --somente-renderizar: {document_json_path}')`
- `RuntimeError('[mapa_mental] não está ativo no TOML. Ative gerar=true/ativo=true para usar --forcar-regeneracao-mapa-mental.')`
- `RuntimeError('Falha ao gerar o resumo acadêmico do paper: ' + str(exc))`

#### Opções CLI mencionadas diretamente no corpo

- `--reusar-mapa-mental informado, mas nenhum mapa existente foi encontrado; renderização seguirá com figuras já registradas no document.json.`

#### Artefatos/caminhos literais reconhecidos no corpo

- `.document.json`
- `document.json não encontrado para --somente-renderizar: `
- `Carregando document.json existente`
- `Salvando document.json atualizado com novo mapa mental`
- `--reusar-mapa-mental informado, mas nenhum mapa existente foi encontrado; renderização seguirá com figuras já registradas no document.json.`
- `.prompt_lock.json`
- `.prompt_lock.md`
- `.run_report.json`
- `.outputs.txt`
- `Gerando document.json canônico com IA em etapas`
- `Gerando document.json canônico com IA`
- `Salvando document.json`
- `.org`
- `.quality_report.md`

#### Complexidade estrutural observada

```yaml
branches: 132
loops: 1
comprehensions: 3
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:_refs_disabled`

**Assinatura:**
```python
def _refs_disabled(cfg: dict[str, Any] | None) -> bool
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 1229–1231
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_refs_disabled` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any] \| None` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `_impl_refs_disabled({**globals(), **locals()}, cfg)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_impl_refs_disabled`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:_refs_apply_runtime_policy`

**Assinatura:**
```python
def _refs_apply_runtime_policy(cfg: dict[str, Any]) -> dict[str, Any]
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 1234–1236
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_refs_apply_runtime_policy` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `_impl_refs_apply_runtime_policy({**globals(), **locals()}, cfg)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_impl_refs_apply_runtime_policy`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:load_config @ L1241`

**Assinatura:**
```python
def load_config(path: Path) -> dict[str, Any]
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 1241–1243
**Tipo:** function
**Visibilidade:** pública por nome
**Status de binding estático:** última definição textual desse nome qualificado; é candidata ao binding final após importação sequencial. Definições anteriores: `L260`.
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Carrega a responsabilidade representada por `config` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `_impl_load_config({**globals(), **locals()}, path)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_impl_load_config`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:build_bibliography`

**Assinatura:**
```python
def build_bibliography(cfg: dict[str, Any], docs: Any, out_dir: Path, prefix: str, client: Any, model: str) -> Any
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 1249–1251
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Constrói a responsabilidade representada por `bibliography` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `docs` | positional-or-keyword | `Any` | `obrigatório/ausente` | Coleção de documentos/entradas processados pela rotina. |
| `out_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `out_dir` recebido pela função; não há descrição individual explícita no código. |
| `prefix` | positional-or-keyword | `str` | `obrigatório/ausente` | Prefixo canônico usado para compor nomes de artefatos. |
| `client` | positional-or-keyword | `Any` | `obrigatório/ausente` | Cliente de serviço externo/IA utilizado pela rotina; pode ser opcional conforme o fluxo. |
| `model` | positional-or-keyword | `str` | `obrigatório/ausente` | Identificador do modelo utilizado pela etapa de IA. |

#### Retorno

Anotação declarada: `Any`. Retornos observados estaticamente incluem: `_impl_build_bibliography({**globals(), **locals()}, cfg, docs, out_dir, prefix, client, model)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_impl_build_bibliography`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:_refs_clear_document_bibliography`

**Assinatura:**
```python
def _refs_clear_document_bibliography(document: Any) -> Any
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 1254–1256
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_refs_clear_document_bibliography` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `document` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `document` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Any`. Retornos observados estaticamente incluem: `_impl_refs_clear_document_bibliography({**globals(), **locals()}, document)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_impl_refs_clear_document_bibliography`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:_refs_strip_org`

**Assinatura:**
```python
def _refs_strip_org(text: str) -> str
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 1259–1261
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_refs_strip_org` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `_ap003d_impl__refs_strip_org({**globals(), **locals()}, text)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_ap003d_impl__refs_strip_org`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:render_org_latex`

**Assinatura:**
```python
def render_org_latex(document: Any, org_path: Path, bib_filename: str, *, cfg: dict[str, Any], bib_keys: list[str] | None = None) -> str
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 1267–1269
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Renderiza a responsabilidade representada por `org_latex` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `document` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `document` recebido pela função; não há descrição individual explícita no código. |
| `org_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `bib_filename` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `bib_filename` recebido pela função; não há descrição individual explícita no código. |
| `cfg` | keyword-only | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `bib_keys` | keyword-only | `list[str] \| None` | `None` | Parâmetro `bib_keys` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `_impl_render_org_latex({**globals(), **locals()}, document, org_path, bib_filename, cfg=cfg, bib_keys=bib_keys)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_impl_render_org_latex`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:_prisma_artigo_generico_get_arg`

**Assinatura:**
```python
def _prisma_artigo_generico_get_arg(argv, name)
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 1282–1284
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_artigo_generico_get_arg` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |
| `name` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `name` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_ap003e_impl__prisma_artigo_generico_get_arg_1({**globals(), **locals()}, argv, name)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_ap003e_impl__prisma_artigo_generico_get_arg_1`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_artigo_generico_out_dir_1`
- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_artigo_generico_run_export_1`
- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_artigo_generico_run_freeze_1`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:_prisma_artigo_generico_strip`

**Assinatura:**
```python
def _prisma_artigo_generico_strip(argv)
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 1286–1288
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_artigo_generico_strip` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_ap003e_impl__prisma_artigo_generico_strip_1({**globals(), **locals()}, argv)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_ap003e_impl__prisma_artigo_generico_strip_1`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_ap003e_body_main_2`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:_prisma_artigo_generico_out_dir`

**Assinatura:**
```python
def _prisma_artigo_generico_out_dir(argv)
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 1290–1292
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_artigo_generico_out_dir` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_ap003e_impl__prisma_artigo_generico_out_dir_1({**globals(), **locals()}, argv)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_ap003e_impl__prisma_artigo_generico_out_dir_1`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_artigo_generico_run_export_1`
- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_artigo_generico_run_freeze_1`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:_prisma_artigo_generico_run_export`

**Assinatura:**
```python
def _prisma_artigo_generico_run_export(argv, silent = False)
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 1294–1296
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_artigo_generico_run_export` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |
| `silent` | positional-or-keyword | `não anotado` | `False` | Parâmetro `silent` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_ap003e_impl__prisma_artigo_generico_run_export_1({**globals(), **locals()}, argv, silent)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_ap003e_impl__prisma_artigo_generico_run_export_1`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_ap003e_body_main_2`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:_prisma_artigo_generico_run_freeze`

**Assinatura:**
```python
def _prisma_artigo_generico_run_freeze(argv, silent = False)
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 1298–1300
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_artigo_generico_run_freeze` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |
| `silent` | positional-or-keyword | `não anotado` | `False` | Parâmetro `silent` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_ap003e_impl__prisma_artigo_generico_run_freeze_1({**globals(), **locals()}, argv, silent)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_ap003e_impl__prisma_artigo_generico_run_freeze_1`
- `globals`
- `locals`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_ap003e_body_main_2`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:_normalize_exit_code`

**Assinatura:**
```python
def _normalize_exit_code(value: Any) -> int
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 1305–1312
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Normaliza a responsabilidade representada por `exit_code` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `0`; `int(value)`; `value`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `RuntimeError`
- `int`
- `isinstance`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.default_runtime:run_default`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError(f'Fluxo padrão retornou código inválido: {value!r}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.default_runtime:run_default`

**Assinatura:**
```python
def run_default(argv: Sequence[str] | None = None) -> int
```

**Arquivo:** `academic_pipeline/default_runtime.py`
**Linhas:** 1315–1331
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Executa o fluxo padrão por superfícies canônicas, sem adaptador legado.

**Docstring resumida:** Executa o fluxo padrão por superfícies canônicas, sem adaptador legado.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `Sequence[str] \| None` | `None` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `_normalize_exit_code(value)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_normalize_exit_code`
- `globals`
- `list`
- `locals`
- `run_prisma_generic_entrypoint`
- `str`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.runtime:_run_native_default`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `76c061bf5fa3ef613da87d6bafb1d7bb118b8611c7244b2037fff590f1ff44c0`
**LOC do arquivo-fonte:** 1332


### `academic_pipeline.doctor_runtime:_section`

**Assinatura:**
```python
def _section(cfg: dict[str, Any], name: str) -> dict[str, Any]
```

**Arquivo:** `academic_pipeline/doctor_runtime.py`
**Linhas:** 60–65
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_section` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `name` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `name` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `value if isinstance(value, dict) else {}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `cfg.get`
- `isinstance`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `2944cb78c2b1959acaf2f0f4192b09964e3cf721885aafac7c88164c1b287ec9`
**LOC do arquivo-fonte:** 211


### `academic_pipeline.doctor_runtime:_apply_cli_path_overrides`

**Assinatura:**
```python
def _apply_cli_path_overrides(cfg: dict[str, Any], args: argparse.Namespace) -> dict[str, Any]
```

**Arquivo:** `academic_pipeline/doctor_runtime.py`
**Linhas:** 77–90
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Aplica a responsabilidade representada por `cli_path_overrides` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `args` | positional-or-keyword | `argparse.Namespace` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `result`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `DoctorRuntimeError`
- `apply_cli_path_overrides_impl`
- `isinstance`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `DoctorRuntimeError('apply_cli_path_overrides_impl não retornou dict')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `2944cb78c2b1959acaf2f0f4192b09964e3cf721885aafac7c88164c1b287ec9`
**LOC do arquivo-fonte:** 211


### `academic_pipeline.doctor_runtime:_output_paths`

**Assinatura:**
```python
def _output_paths(cfg: dict[str, Any]) -> tuple[Path, str]
```

**Arquivo:** `academic_pipeline/doctor_runtime.py`
**Linhas:** 93–106
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_output_paths` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `tuple[Path, str]`. Retornos observados estaticamente incluem: `result`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `DoctorRuntimeError`
- `isinstance`
- `len`
- `output_paths_impl`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `DoctorRuntimeError('output_paths_impl retornou contrato inválido')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `2944cb78c2b1959acaf2f0f4192b09964e3cf721885aafac7c88164c1b287ec9`
**LOC do arquivo-fonte:** 211


### `academic_pipeline.doctor_runtime:_research_output_paths`

**Assinatura:**
```python
def _research_output_paths(cfg: dict[str, Any]) -> tuple[Path, str]
```

**Arquivo:** `academic_pipeline/doctor_runtime.py`
**Linhas:** 109–126
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_research_output_paths` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `tuple[Path, str]`. Retornos observados estaticamente incluem: `result`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `DoctorRuntimeError`
- `isinstance`
- `len`
- `research_output_paths_impl_001`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `DoctorRuntimeError('research_output_paths_impl_001 retornou contrato inválido')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `2944cb78c2b1959acaf2f0f4192b09964e3cf721885aafac7c88164c1b287ec9`
**LOC do arquivo-fonte:** 211


### `academic_pipeline.doctor_runtime:default_doctor_runtime_context`

**Assinatura:**
```python
def default_doctor_runtime_context() -> DoctorRuntimeContext
```

**Arquivo:** `academic_pipeline/doctor_runtime.py`
**Linhas:** 129–140
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `default_doctor_runtime_context` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `DoctorRuntimeContext`. Retornos observados estaticamente incluem: `DoctorRuntimeContext(load_config=base.load_config, apply_cli_path_overrides=_apply_cli_path_overrides, output_paths=_output_paths, research_output_paths=_research_output_paths, external_search_enabled=external_search_enabled, run_doctor=run_doctor, print_doctor_report=print_doctor_report, write_json=write_json)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `DoctorRuntimeContext`
- `default_runtime_context`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.check_config_runtime:default_check_config_runtime_context`
- `academic_pipeline.doctor_runtime:run_doctor_command`
- `academic_pipeline.institution_compliance_runtime:default_institution_compliance_runtime_context`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `2944cb78c2b1959acaf2f0f4192b09964e3cf721885aafac7c88164c1b287ec9`
**LOC do arquivo-fonte:** 211


### `academic_pipeline.doctor_runtime:_normalize_argv`

**Assinatura:**
```python
def _normalize_argv(argv: Sequence[str] | None) -> tuple[str, ...]
```

**Arquivo:** `academic_pipeline/doctor_runtime.py`
**Linhas:** 143–147
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Normaliza a responsabilidade representada por `argv` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `Sequence[str] \| None` | `obrigatório/ausente` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |

#### Retorno

Anotação declarada: `tuple[str, ...]`. Retornos observados estaticamente incluem: `tuple((str(item) for item in source))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `str`
- `tuple`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `2944cb78c2b1959acaf2f0f4192b09964e3cf721885aafac7c88164c1b287ec9`
**LOC do arquivo-fonte:** 211


### `academic_pipeline.doctor_runtime:_build_parser`

**Assinatura:**
```python
def _build_parser() -> argparse.ArgumentParser
```

**Arquivo:** `academic_pipeline/doctor_runtime.py`
**Linhas:** 150–159
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Constrói a responsabilidade representada por `parser` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `argparse.ArgumentParser`. Retornos observados estaticamente incluem: `parser`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `DoctorRuntimeError`
- `cli_parser.build_parser`
- `isinstance`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `DoctorRuntimeError('build_parser não retornou ArgumentParser')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `2944cb78c2b1959acaf2f0f4192b09964e3cf721885aafac7c88164c1b287ec9`
**LOC do arquivo-fonte:** 211


### `academic_pipeline.doctor_runtime:run_doctor_command`

**Assinatura:**
```python
def run_doctor_command(argv: Sequence[str] | None = None, *, context: DoctorRuntimeContext | None = None) -> int
```

**Arquivo:** `academic_pipeline/doctor_runtime.py`
**Linhas:** 162–202
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `doctor_command` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `Sequence[str] \| None` | `None` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |
| `context` | keyword-only | `DoctorRuntimeContext \| None` | `None` | Contexto explícito de dependências utilizado para desacoplar a rotina de imports/globais. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `0 if bool(report.get('ok')) else 2`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `DoctorRuntimeError`
- `Path`
- `Path(args.config).expanduser`
- `Path(args.config).expanduser().resolve`
- `_build_parser`
- `_build_parser().parse_args`
- `_normalize_argv`
- `active.apply_cli_path_overrides`
- `active.external_search_enabled`
- `active.load_config`
- `active.output_paths`
- `active.print_doctor_report`
- `active.research_output_paths`
- `active.run_doctor`
- `active.write_json`
- `bool`
- `default_doctor_runtime_context`
- `dict`
- `isinstance`
- `list`
- `report.get`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.runtime:_run_native_doctor`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `DoctorRuntimeError('run_doctor_command exige a opção --doctor')`
- `DoctorRuntimeError('load_config não retornou dict')`
- `DoctorRuntimeError('run_doctor não retornou Mapping')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.doctor_report.json`

#### Complexidade estrutural observada

```yaml
branches: 7
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `2944cb78c2b1959acaf2f0f4192b09964e3cf721885aafac7c88164c1b287ec9`
**LOC do arquivo-fonte:** 211


### `academic_pipeline.document_orchestration:load_config_impl`

**Assinatura:**
```python
def load_config_impl(runtime, path)
```

**Arquivo:** `academic_pipeline/document_orchestration.py`
**Linhas:** 11–12
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Carrega a responsabilidade representada por `config_impl` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `path` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `runtime['_refs_apply_runtime_policy'](runtime['_refs_original_load_config'](path))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `runtime['_refs_apply_runtime_policy']`
- `runtime['_refs_original_load_config']`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `62a8eeb2caa7a0170c83618112e432eec1512fc0e7bd6502f857b93be7bb8ae2`
**LOC do arquivo-fonte:** 437


### `academic_pipeline.document_orchestration:output_paths_impl`

**Assinatura:**
```python
def output_paths_impl(runtime, cfg)
```

**Arquivo:** `academic_pipeline/document_orchestration.py`
**Linhas:** 14–23
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Resolve a saída final do documento pela seção [paths].

**Docstring resumida:** Resolve a saída final do documento pela seção [paths].

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `cfg` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `(out_dir, prefix)`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `bool`
- `cfg.get`
- `out_dir.mkdir`
- `paths.get`
- `projeto.get`
- `runtime['Path']`
- `runtime['Path'](str(cfg.get('__config_dir__') or runtime['Path'].cwd())).resolve`
- `runtime['Path'].cwd`
- `runtime['_section']`
- `runtime['resolve_path']`
- `str`
- `str(paths.get('document_prefix') or projeto.get('nome') or 'documento').strip`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.doctor_runtime:_output_paths`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `62a8eeb2caa7a0170c83618112e432eec1512fc0e7bd6502f857b93be7bb8ae2`
**LOC do arquivo-fonte:** 437


### `academic_pipeline.document_orchestration:apply_cli_path_overrides_impl`

**Assinatura:**
```python
def apply_cli_path_overrides_impl(runtime, cfg, args)
```

**Arquivo:** `academic_pipeline/document_orchestration.py`
**Linhas:** 25–57
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Aplica overrides de caminhos informados na linha de comando.

**Docstring resumida:** Aplica overrides de caminhos informados na linha de comando.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `cfg` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `cfg`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `cfg.setdefault`
- `getattr`
- `isinstance`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.doctor_runtime:_apply_cli_path_overrides`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 11
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `62a8eeb2caa7a0170c83618112e432eec1512fc0e7bd6502f857b93be7bb8ae2`
**LOC do arquivo-fonte:** 437


### `academic_pipeline.document_orchestration:load_existing_document_json_impl`

**Assinatura:**
```python
def load_existing_document_json_impl(runtime, path)
```

**Arquivo:** `academic_pipeline/document_orchestration.py`
**Linhas:** 59–60
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Carrega a responsabilidade representada por `existing_document_json_impl` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `path` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `runtime['AcademicDocument'].model_validate_json(path.read_text(encoding='utf-8'))`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `path.read_text`
- `runtime['AcademicDocument'].model_validate_json`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `62a8eeb2caa7a0170c83618112e432eec1512fc0e7bd6502f857b93be7bb8ae2`
**LOC do arquivo-fonte:** 437


### `academic_pipeline.document_orchestration:resolve_bib_for_existing_document_impl`

**Assinatura:**
```python
def resolve_bib_for_existing_document_impl(runtime, document, document_json_path, out_dir, prefix)
```

**Arquivo:** `academic_pipeline/document_orchestration.py`
**Linhas:** 62–85
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Resolve o .bib em modo --somente-renderizar sem exigir que ele já esteja no output_dir.

**Docstring resumida:** Resolve o .bib em modo --somente-renderizar sem exigir que ele já esteja no output_dir.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `document` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `document` recebido pela função; não há descrição individual explícita no código. |
| `document_json_path` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `out_dir` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `out_dir` recebido pela função; não há descrição individual explícita no código. |
| `prefix` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Prefixo canônico usado para compor nomes de artefatos. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `(found, keys)`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `c.exists`
- `c.resolve`
- `candidates.append`
- `candidates.extend`
- `document_json_path.with_name`
- `document_json_path.with_suffix`
- `found.exists`
- `found.read_text`
- `found.resolve`
- `list`
- `m.group`
- `m.group(1).strip`
- `next`
- `out_dir.mkdir`
- `p.is_absolute`
- `re.finditer`
- `runtime['Path']`
- `runtime['Path'](raw).expanduser`
- `shutil.copy2`
- `str`
- `str(document.bibliography.bib_path or f'{prefix}.bib').strip`
- `target.resolve`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `Resolve o .bib em modo --somente-renderizar sem exigir que ele já esteja no output_dir.`
- `.bib`

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `62a8eeb2caa7a0170c83618112e432eec1512fc0e7bd6502f857b93be7bb8ae2`
**LOC do arquivo-fonte:** 437


### `academic_pipeline.document_orchestration:_resolve_latex_paths_for_recompile_impl`

**Assinatura:**
```python
def _resolve_latex_paths_for_recompile_impl(runtime, args, cfg)
```

**Arquivo:** `academic_pipeline/document_orchestration.py`
**Linhas:** 87–93
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Resolve a responsabilidade representada por `latex_paths_for_recompile_impl` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `cfg` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `(academic_writing, latex_extra, pdf_engine)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `(cfg or {}).get`
- `isinstance`
- `latex_cfg.get`
- `runtime['Path']`
- `runtime['Path'](str((cfg or {}).get('__config_dir__') or runtime['Path'].cwd())).resolve`
- `runtime['Path'].cwd`
- `runtime['resolve_path']`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `62a8eeb2caa7a0170c83618112e432eec1512fc0e7bd6502f857b93be7bb8ae2`
**LOC do arquivo-fonte:** 437


### `academic_pipeline.document_orchestration:run_recompile_impl`

**Assinatura:**
```python
def run_recompile_impl(runtime, args, cfg)
```

**Arquivo:** `academic_pipeline/document_orchestration.py`
**Linhas:** 95–112
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `recompile_impl` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `cfg` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `0`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `(cfg or {}).get`
- `FileNotFoundError`
- `RuntimeError`
- `cfg.get`
- `org_path.exists`
- `runtime['Path']`
- `runtime['Path'](args.org).expanduser`
- `runtime['Path'](args.org).expanduser().resolve`
- `runtime['Path'].cwd`
- `runtime['_resolve_latex_paths_for_recompile']`
- `runtime['clean_aux_files']`
- `runtime['make_run_report']`
- `runtime['print_outputs']`
- `runtime['run_compile_sequence']`
- `runtime['stage']`
- `runtime['write_json']`
- `runtime['write_outputs_manifest']`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('Use --recompile --org caminho/arquivo.org')`
- `FileNotFoundError(f'ORG não encontrado: {org_path}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `Use --recompile --org caminho/arquivo.org`
- `.outputs.txt`
- `.run_report.json`

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `62a8eeb2caa7a0170c83618112e432eec1512fc0e7bd6502f857b93be7bb8ae2`
**LOC do arquivo-fonte:** 437


### `academic_pipeline.document_orchestration:render_additional_language_versions_impl`

**Assinatura:**
```python
def render_additional_language_versions_impl(runtime, *, client, model, cfg, document, bib_path, bib_keys, out_dir, prefix, doc_cfg, latex_cfg, config_dir, abstract_bundle)
```

**Arquivo:** `academic_pipeline/document_orchestration.py`
**Linhas:** 114–171
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Traduz e renderiza versões adicionais a partir do document.json canônico.

**Docstring resumida:** Traduz e renderiza versões adicionais a partir do document.json canônico.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `client` | keyword-only | `não anotado` | `obrigatório/ausente` | Cliente de serviço externo/IA utilizado pela rotina; pode ser opcional conforme o fluxo. |
| `model` | keyword-only | `não anotado` | `obrigatório/ausente` | Identificador do modelo utilizado pela etapa de IA. |
| `cfg` | keyword-only | `não anotado` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `document` | keyword-only | `não anotado` | `obrigatório/ausente` | Parâmetro `document` recebido pela função; não há descrição individual explícita no código. |
| `bib_path` | keyword-only | `não anotado` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `bib_keys` | keyword-only | `não anotado` | `obrigatório/ausente` | Parâmetro `bib_keys` recebido pela função; não há descrição individual explícita no código. |
| `out_dir` | keyword-only | `não anotado` | `obrigatório/ausente` | Parâmetro `out_dir` recebido pela função; não há descrição individual explícita no código. |
| `prefix` | keyword-only | `não anotado` | `obrigatório/ausente` | Prefixo canônico usado para compor nomes de artefatos. |
| `doc_cfg` | keyword-only | `não anotado` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `latex_cfg` | keyword-only | `não anotado` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `config_dir` | keyword-only | `não anotado` | `obrigatório/ausente` | Parâmetro `config_dir` recebido pela função; não há descrição individual explícita no código. |
| `abstract_bundle` | keyword-only | `não anotado` | `obrigatório/ausente` | Parâmetro `abstract_bundle` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `(result, warnings)`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `bib_path.exists`
- `bib_path.resolve`
- `bool`
- `cfg.get`
- `doc_cfg.get`
- `docx_cfg.get`
- `isinstance`
- `language_bib.resolve`
- `language_dir.mkdir`
- `latex_cfg.get`
- `quality.get`
- `runtime['build_quality_report']`
- `runtime['inject_paper_abstracts_into_docx']`
- `runtime['inject_paper_abstracts_into_org']`
- `runtime['raise_if_errors']`
- `runtime['render_docx']`
- `runtime['render_org_latex']`
- `runtime['requested_translation_languages']`
- `runtime['resolve_path']`
- `runtime['run_compile_sequence']`
- `runtime['shutil'].copy2`
- `runtime['stage']`
- `runtime['translate_document_model']`
- `runtime['translation_batch_size']`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.document.json`
- `.translation_audit.json`
- `.org`
- `.docx`
- `.quality_report.md`

#### Complexidade estrutural observada

```yaml
branches: 11
loops: 1
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `62a8eeb2caa7a0170c83618112e432eec1512fc0e7bd6502f857b93be7bb8ae2`
**LOC do arquivo-fonte:** 437


### `academic_pipeline.document_orchestration:_refs_disabled_impl`

**Assinatura:**
```python
def _refs_disabled_impl(runtime, cfg)
```

**Arquivo:** `academic_pipeline/document_orchestration.py`
**Linhas:** 173–183
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_refs_disabled_impl` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `cfg` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `False`; `not bool(bibliography.get('ativo'))`; `not bool(document.get('referencias_formais'))`; `local.get('auto_detect_bib') is False and local.get('gerar_bib_revisado_ia') is False and (document.get('usar_citacoes_latex_diretas') is False)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `bibliography.get`
- `bool`
- `cfg.get`
- `document.get`
- `isinstance`
- `local.get`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `62a8eeb2caa7a0170c83618112e432eec1512fc0e7bd6502f857b93be7bb8ae2`
**LOC do arquivo-fonte:** 437


### `academic_pipeline.document_orchestration:_refs_apply_runtime_policy_impl`

**Assinatura:**
```python
def _refs_apply_runtime_policy_impl(runtime, cfg)
```

**Arquivo:** `academic_pipeline/document_orchestration.py`
**Linhas:** 185–217
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_refs_apply_runtime_policy_impl` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `cfg` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `cfg`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `(current + '\n\n' + instruction).strip`
- `cfg.setdefault`
- `isinstance`
- `orientations.get`
- `runtime['_refs_disabled']`
- `str`
- `str(orientations.get('inline') or '').strip`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `Não inclua citações no corpo do texto, notas bibliográficas, seção Referências, lista bibliográfica ou arquivo .bib. Use exclusivamente o corpus local e não invente fontes.`

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `62a8eeb2caa7a0170c83618112e432eec1512fc0e7bd6502f857b93be7bb8ae2`
**LOC do arquivo-fonte:** 437


### `academic_pipeline.document_orchestration:build_bibliography_impl`

**Assinatura:**
```python
def build_bibliography_impl(runtime, cfg, docs, out_dir, prefix, client, model)
```

**Arquivo:** `academic_pipeline/document_orchestration.py`
**Linhas:** 219–223
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Constrói a responsabilidade representada por `bibliography_impl` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `cfg` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `docs` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Coleção de documentos/entradas processados pela rotina. |
| `out_dir` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `out_dir` recebido pela função; não há descrição individual explícita no código. |
| `prefix` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Prefixo canônico usado para compor nomes de artefatos. |
| `client` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Cliente de serviço externo/IA utilizado pela rotina; pode ser opcional conforme o fluxo. |
| `model` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Identificador do modelo utilizado pela etapa de IA. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `SimpleNamespace(bib_path=runtime['Path'](out_dir) / f'{prefix}.bib', keys=[])`; `runtime['_refs_original_build_bibliography'](cfg, docs, out_dir, prefix, client, model)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `SimpleNamespace`
- `runtime['Path']`
- `runtime['_refs_disabled']`
- `runtime['_refs_original_build_bibliography']`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.bib`

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `62a8eeb2caa7a0170c83618112e432eec1512fc0e7bd6502f857b93be7bb8ae2`
**LOC do arquivo-fonte:** 437


### `academic_pipeline.document_orchestration:_refs_clear_document_bibliography_impl`

**Assinatura:**
```python
def _refs_clear_document_bibliography_impl(runtime, document)
```

**Arquivo:** `academic_pipeline/document_orchestration.py`
**Linhas:** 225–236
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_refs_clear_document_bibliography_impl` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `document` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `document` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `document`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `getattr`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 2
```

**SHA-256 do arquivo-fonte:** `62a8eeb2caa7a0170c83618112e432eec1512fc0e7bd6502f857b93be7bb8ae2`
**LOC do arquivo-fonte:** 437


### `academic_pipeline.document_orchestration:_refs_strip_org_impl`

**Assinatura:**
```python
def _refs_strip_org_impl(runtime, text)
```

**Arquivo:** `academic_pipeline/document_orchestration.py`
**Linhas:** 238–246
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_refs_strip_org_impl` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `text` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_re.sub('\\n{3,}', '\n\n', text).strip() + '\n'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_re.sub`
- `_re.sub('\\n{3,}', '\n\n', text).strip`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `62a8eeb2caa7a0170c83618112e432eec1512fc0e7bd6502f857b93be7bb8ae2`
**LOC do arquivo-fonte:** 437


### `academic_pipeline.document_orchestration:render_org_latex_impl`

**Assinatura:**
```python
def render_org_latex_impl(runtime, document, org_path, bib_filename, *, cfg, bib_keys)
```

**Arquivo:** `academic_pipeline/document_orchestration.py`
**Linhas:** 248–255
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Renderiza a responsabilidade representada por `org_latex_impl` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `document` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `document` recebido pela função; não há descrição individual explícita no código. |
| `org_path` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `bib_filename` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `bib_filename` recebido pela função; não há descrição individual explícita no código. |
| `cfg` | keyword-only | `não anotado` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `bib_keys` | keyword-only | `não anotado` | `obrigatório/ausente` | Parâmetro `bib_keys` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `runtime['_refs_original_render_org_latex'](document, org_path, bib_filename, cfg=cfg, bib_keys=bib_keys)`; `clean`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `runtime['Path']`
- `runtime['Path'](org_path).write_text`
- `runtime['_refs_clear_document_bibliography']`
- `runtime['_refs_disabled']`
- `runtime['_refs_original_render_org_latex']`
- `runtime['_refs_strip_org']`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `62a8eeb2caa7a0170c83618112e432eec1512fc0e7bd6502f857b93be7bb8ae2`
**LOC do arquivo-fonte:** 437


### `academic_pipeline.document_orchestration:run_document_stage_001`

**Assinatura:**
```python
def run_document_stage_001(args, runtime)
```

**Arquivo:** `academic_pipeline/document_orchestration.py`
**Linhas:** 257–282
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `document_stage_001` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `runtime` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `DocumentStageResult(True, 0 if report.get('ok') else 1, {})`; `DocumentStageResult(False, None, _ap003d_values)`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `DocumentStageResult`
- `RuntimeError`
- `bib_entry_key`
- `bib_path.exists`
- `bib_path.read_text`
- `document_json.with_suffix`
- `list`
- `locals`
- `print`
- `report.get`
- `runtime['Path']`
- `runtime['Path'](args.bib).expanduser`
- `runtime['Path'](args.bib).expanduser().resolve`
- `runtime['Path'](args.document_json).expanduser`
- `runtime['Path'](args.document_json).expanduser().resolve`
- `runtime['Path'](args.org).expanduser`
- `runtime['Path'](args.org).expanduser().resolve`
- `runtime['build_quality_report']`
- `runtime['load_existing_document_json']`
- `runtime['write_quality_report']`
- `split_bib_entries`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('--quality-report exige --document-json caminho/document.json')`

#### Opções CLI mencionadas diretamente no corpo

- `--quality-report exige --document-json caminho/document.json`

#### Artefatos/caminhos literais reconhecidos no corpo

- `--quality-report exige --document-json caminho/document.json`
- `.quality_report.md`

#### Complexidade estrutural observada

```yaml
branches: 8
loops: 1
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `62a8eeb2caa7a0170c83618112e432eec1512fc0e7bd6502f857b93be7bb8ae2`
**LOC do arquivo-fonte:** 437


### `academic_pipeline.document_orchestration:run_document_stage_002`

**Assinatura:**
```python
def run_document_stage_002(args, runtime)
```

**Arquivo:** `academic_pipeline/document_orchestration.py`
**Linhas:** 284–290
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `document_stage_002` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `runtime` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `DocumentStageResult(False, None, _ap003d_values)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `DocumentStageResult`
- `isinstance`
- `locals`
- `runtime['cfg'].get`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `62a8eeb2caa7a0170c83618112e432eec1512fc0e7bd6502f857b93be7bb8ae2`
**LOC do arquivo-fonte:** 437


### `academic_pipeline.document_orchestration:run_document_stage_003`

**Assinatura:**
```python
def run_document_stage_003(args, runtime)
```

**Arquivo:** `academic_pipeline/document_orchestration.py`
**Linhas:** 292–335
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `document_stage_003` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `runtime` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `DocumentStageResult(True, 0, {})`; `DocumentStageResult(False, None, _ap003d_values)`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `(runtime['mm_diag'] or {}).get`
- `DocumentStageResult`
- `FileNotFoundError`
- `RuntimeError`
- `bool`
- `dict`
- `document.model_dump`
- `locals`
- `print`
- `runtime['Path']`
- `runtime['attach_existing_mindmap_if_available']`
- `runtime['cfg'].get`
- `runtime['delete_existing_mindmap_outputs']`
- `runtime['document_json_path'].exists`
- `runtime['generate_and_attach_mindmap']`
- `runtime['json'].dumps`
- `runtime['load_existing_document_json']`
- `runtime['make_client']`
- `runtime['make_run_report']`
- `runtime['print_outputs']`
- `runtime['should_generate_mindmap']`
- `runtime['stage']`
- `runtime['warnings'].append`
- `runtime['write_json']`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `FileNotFoundError(f"document.json não encontrado para --somente-mapa-mental: {runtime['document_json_path']}")`
- `RuntimeError('[mapa_mental] não está ativo no TOML. Ative gerar=true/ativo=true para usar --somente-mapa-mental.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `document.json não encontrado para --somente-mapa-mental: `
- `Carregando document.json existente`
- `Salvando document.json atualizado`
- `.run_report.json`
- `.outputs.txt`

#### Complexidade estrutural observada

```yaml
branches: 12
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `62a8eeb2caa7a0170c83618112e432eec1512fc0e7bd6502f857b93be7bb8ae2`
**LOC do arquivo-fonte:** 437


### `academic_pipeline.document_orchestration:run_document_stage_004`

**Assinatura:**
```python
def run_document_stage_004(args, runtime)
```

**Arquivo:** `academic_pipeline/document_orchestration.py`
**Linhas:** 337–343
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `document_stage_004` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `runtime` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `DocumentStageResult(False, None, _ap003d_values)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `DocumentStageResult`
- `locals`
- `runtime['stage']`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `62a8eeb2caa7a0170c83618112e432eec1512fc0e7bd6502f857b93be7bb8ae2`
**LOC do arquivo-fonte:** 437


### `academic_pipeline.document_orchestration:run_document_stage_005`

**Assinatura:**
```python
def run_document_stage_005(args, runtime)
```

**Arquivo:** `academic_pipeline/document_orchestration.py`
**Linhas:** 345–356
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `document_stage_005` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `runtime` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `DocumentStageResult(False, None, _ap003d_values)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `DocumentStageResult`
- `locals`
- `runtime['inject_paper_abstracts_into_org']`
- `runtime['main_document_abstract_languages']`
- `runtime['raise_if_errors']`
- `runtime['render_org_latex']`
- `runtime['stage']`
- `runtime['validate_org_text']`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.bib`

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `62a8eeb2caa7a0170c83618112e432eec1512fc0e7bd6502f857b93be7bb8ae2`
**LOC do arquivo-fonte:** 437


### `academic_pipeline.document_orchestration:run_document_stage_006`

**Assinatura:**
```python
def run_document_stage_006(args, runtime)
```

**Arquivo:** `academic_pipeline/document_orchestration.py`
**Linhas:** 358–369
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `document_stage_006` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `runtime` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `DocumentStageResult(False, None, _ap003d_values)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `DocumentStageResult`
- `bool`
- `locals`
- `runtime['doc_cfg'].get`
- `runtime['latex_cfg'].get`
- `runtime['resolve_path']`
- `runtime['run_compile_sequence']`
- `runtime['stage']`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `62a8eeb2caa7a0170c83618112e432eec1512fc0e7bd6502f857b93be7bb8ae2`
**LOC do arquivo-fonte:** 437


### `academic_pipeline.document_orchestration:run_document_stage_007`

**Assinatura:**
```python
def run_document_stage_007(args, runtime)
```

**Arquivo:** `academic_pipeline/document_orchestration.py`
**Linhas:** 371–387
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `document_stage_007` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `runtime` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `DocumentStageResult(False, None, _ap003d_values)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `DocumentStageResult`
- `bool`
- `docx_cfg.get`
- `docx_validation.get`
- `isinstance`
- `locals`
- `runtime['cfg'].get`
- `runtime['doc_cfg'].get`
- `runtime['inject_paper_abstracts_into_docx']`
- `runtime['main_document_abstract_languages']`
- `runtime['render_docx']`
- `runtime['resolve_path']`
- `runtime['stage']`
- `runtime['validate_docx_file']`
- `runtime['warnings'].extend`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.docx`

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 1
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `62a8eeb2caa7a0170c83618112e432eec1512fc0e7bd6502f857b93be7bb8ae2`
**LOC do arquivo-fonte:** 437


### `academic_pipeline.document_orchestration:run_document_stage_008`

**Assinatura:**
```python
def run_document_stage_008(args, runtime)
```

**Arquivo:** `academic_pipeline/document_orchestration.py`
**Linhas:** 389–403
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `document_stage_008` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `runtime` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `DocumentStageResult(False, None, _ap003d_values)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `DocumentStageResult`
- `locals`
- `runtime['render_additional_language_versions']`
- `runtime['requested_translation_languages']`
- `runtime['warnings'].append`
- `runtime['warnings'].extend`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `Versões adicionais por IA não foram atualizadas no modo --somente-renderizar. Execute a geração completa para traduzir o document.json canônico.`

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 1
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `62a8eeb2caa7a0170c83618112e432eec1512fc0e7bd6502f857b93be7bb8ae2`
**LOC do arquivo-fonte:** 437


### `academic_pipeline.document_orchestration:run_document_stage_009`

**Assinatura:**
```python
def run_document_stage_009(args, runtime)
```

**Arquivo:** `academic_pipeline/document_orchestration.py`
**Linhas:** 405–411
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `document_stage_009` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `runtime` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `DocumentStageResult(False, None, _ap003d_values)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `DocumentStageResult`
- `locals`
- `runtime['run_institution_compliance']`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `62a8eeb2caa7a0170c83618112e432eec1512fc0e7bd6502f857b93be7bb8ae2`
**LOC do arquivo-fonte:** 437


### `academic_pipeline.document_orchestration:run_document_stage_010`

**Assinatura:**
```python
def run_document_stage_010(args, runtime)
```

**Arquivo:** `academic_pipeline/document_orchestration.py`
**Linhas:** 413–419
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `document_stage_010` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `runtime` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `DocumentStageResult(False, None, _ap003d_values)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `DocumentStageResult`
- `locals`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `62a8eeb2caa7a0170c83618112e432eec1512fc0e7bd6502f857b93be7bb8ae2`
**LOC do arquivo-fonte:** 437


### `academic_pipeline.document_orchestration:run_document_stage_011`

**Assinatura:**
```python
def run_document_stage_011(args, runtime)
```

**Arquivo:** `academic_pipeline/document_orchestration.py`
**Linhas:** 421–427
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `document_stage_011` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `runtime` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `DocumentStageResult(False, None, _ap003d_values)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `DocumentStageResult`
- `locals`
- `runtime['build_quality_report']`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `62a8eeb2caa7a0170c83618112e432eec1512fc0e7bd6502f857b93be7bb8ae2`
**LOC do arquivo-fonte:** 437


### `academic_pipeline.document_orchestration:run_document_stage_012`

**Assinatura:**
```python
def run_document_stage_012(args, runtime)
```

**Arquivo:** `academic_pipeline/document_orchestration.py`
**Linhas:** 429–436
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `document_stage_012` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `runtime` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `DocumentStageResult(False, None, _ap003d_values)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `DocumentStageResult`
- `locals`
- `runtime['Path']`
- `runtime['cfg'].get`
- `runtime['make_run_report']`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `62a8eeb2caa7a0170c83618112e432eec1512fc0e7bd6502f857b93be7bb8ae2`
**LOC do arquivo-fonte:** 437


### `academic_pipeline.doi_manifest_runtime:_iter_zip_sources`

**Assinatura:**
```python
def _iter_zip_sources(input_zip: Path) -> list[str]
```

**Arquivo:** `academic_pipeline/doi_manifest_runtime.py`
**Linhas:** 15–26
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_iter_zip_sources` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `input_zip` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `input_zip` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `sorted(dict.fromkeys(names))`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `FileNotFoundError`
- `Path`
- `Path(name).suffix.lower`
- `dict.fromkeys`
- `input_zip.exists`
- `name.endswith`
- `names.append`
- `sorted`
- `zf.namelist`
- `zipfile.ZipFile`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `FileNotFoundError(f'ZIP não encontrado: {input_zip}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9d4dc1a4a9cee30a69effc31424ccc3ead1cf041bf3fbd09a679659e34fe015b`
**LOC do arquivo-fonte:** 170


### `academic_pipeline.doi_manifest_runtime:_iter_dir_sources`

**Assinatura:**
```python
def _iter_dir_sources(input_dir: Path, recursive: bool = True) -> list[str]
```

**Arquivo:** `academic_pipeline/doi_manifest_runtime.py`
**Linhas:** 28–39
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_iter_dir_sources` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `input_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `input_dir` recebido pela função; não há descrição individual explícita no código. |
| `recursive` | positional-or-keyword | `bool` | `True` | Parâmetro `recursive` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `sorted(dict.fromkeys(out))`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `FileNotFoundError`
- `dict.fromkeys`
- `input_dir.exists`
- `input_dir.glob`
- `input_dir.is_dir`
- `input_dir.rglob`
- `out.append`
- `path.is_file`
- `path.relative_to`
- `path.suffix.lower`
- `sorted`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `FileNotFoundError(f'Diretório não encontrado: {input_dir}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `9d4dc1a4a9cee30a69effc31424ccc3ead1cf041bf3fbd09a679659e34fe015b`
**LOC do arquivo-fonte:** 170


### `academic_pipeline.doi_manifest_runtime:make_doi_manifest`

**Assinatura:**
```python
def make_doi_manifest(input_zip: Path | None, input_dir: Path | None, output: Path, overwrite: bool = True) -> dict[str, Any]
```

**Arquivo:** `academic_pipeline/doi_manifest_runtime.py`
**Linhas:** 41–58
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Cria a responsabilidade representada por `doi_manifest` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `input_zip` | positional-or-keyword | `Path \| None` | `obrigatório/ausente` | Parâmetro `input_zip` recebido pela função; não há descrição individual explícita no código. |
| `input_dir` | positional-or-keyword | `Path \| None` | `obrigatório/ausente` | Parâmetro `input_dir` recebido pela função; não há descrição individual explícita no código. |
| `output` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `output` recebido pela função; não há descrição individual explícita no código. |
| `overwrite` | positional-or-keyword | `bool` | `True` | Parâmetro `overwrite` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `{'source': source, 'output': str(output), 'total_files': len(files), 'files': files}`.

#### Efeitos colaterais e fronteiras externas

- Abre arquivo para leitura ou inspeção.
- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `FileExistsError`
- `ValueError`
- `_iter_dir_sources`
- `_iter_zip_sources`
- `csv.writer`
- `len`
- `output.exists`
- `output.open`
- `output.parent.mkdir`
- `str`
- `writer.writerow`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `ValueError('Informe --input-zip ou --input-dir.')`
- `FileExistsError(f'Arquivo já existe: {output}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9d4dc1a4a9cee30a69effc31424ccc3ead1cf041bf3fbd09a679659e34fe015b`
**LOC do arquivo-fonte:** 170


### `academic_pipeline.doi_manifest_runtime:_normalize_argv`

**Assinatura:**
```python
def _normalize_argv(argv: Sequence[str] | None) -> tuple[str, ...]
```

**Arquivo:** `academic_pipeline/doi_manifest_runtime.py`
**Linhas:** 68–70
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Normaliza a responsabilidade representada por `argv` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `Sequence[str] \| None` | `obrigatório/ausente` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |

#### Retorno

Anotação declarada: `tuple[str, ...]`. Retornos observados estaticamente incluem: `tuple((str(item) for item in source))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `str`
- `tuple`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9d4dc1a4a9cee30a69effc31424ccc3ead1cf041bf3fbd09a679659e34fe015b`
**LOC do arquivo-fonte:** 170


### `academic_pipeline.doi_manifest_runtime:_build_parser`

**Assinatura:**
```python
def _build_parser() -> argparse.ArgumentParser
```

**Arquivo:** `academic_pipeline/doi_manifest_runtime.py`
**Linhas:** 73–78
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Constrói a responsabilidade representada por `parser` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `argparse.ArgumentParser`. Retornos observados estaticamente incluem: `parser`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `DoiManifestRuntimeError`
- `cli_parser.build_parser`
- `isinstance`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `DoiManifestRuntimeError('build_parser não retornou ArgumentParser')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9d4dc1a4a9cee30a69effc31424ccc3ead1cf041bf3fbd09a679659e34fe015b`
**LOC do arquivo-fonte:** 170


### `academic_pipeline.doi_manifest_runtime:_validate_option_surface`

**Assinatura:**
```python
def _validate_option_surface(argv: Sequence[str]) -> None
```

**Arquivo:** `academic_pipeline/doi_manifest_runtime.py`
**Linhas:** 81–128
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Valida a responsabilidade representada por `option_surface` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `Sequence[str]` | `obrigatório/ausente` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `DoiManifestRuntimeError`
- `len`
- `seen_value_options.add`
- `set`
- `str`
- `str(argv[index + 1]).startswith`
- `token.startswith`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.doi_manifest_runtime:run_make_doi_manifest_command`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `DoiManifestRuntimeError('--make-doi-manifest duplicado')`
- `DoiManifestRuntimeError(f'{option} duplicado')`
- `DoiManifestRuntimeError(f'valor ausente para {option}')`
- `DoiManifestRuntimeError(f'argumento não suportado: {token}')`

#### Opções CLI mencionadas diretamente no corpo

- `--`
- `--input-dir`
- `--input-zip`
- `--make-doi-manifest duplicado`
- `--output`

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 9
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9d4dc1a4a9cee30a69effc31424ccc3ead1cf041bf3fbd09a679659e34fe015b`
**LOC do arquivo-fonte:** 170


### `academic_pipeline.doi_manifest_runtime:_request_paths`

**Assinatura:**
```python
def _request_paths(args: argparse.Namespace) -> tuple[Path | None, Path | None, Path]
```

**Arquivo:** `academic_pipeline/doi_manifest_runtime.py`
**Linhas:** 131–147
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_request_paths` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `argparse.Namespace` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Anotação declarada: `tuple[Path | None, Path | None, Path]`. Retornos observados estaticamente incluem: `(input_zip, input_dir, output)`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `DoiManifestRuntimeError`
- `Path`
- `Path(args.input_dir).expanduser`
- `Path(args.input_zip).expanduser`
- `Path(args.output).expanduser`
- `bool`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.doi_manifest_runtime:run_make_doi_manifest_command`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `DoiManifestRuntimeError('run_make_doi_manifest_command exige --make-doi-manifest')`
- `DoiManifestRuntimeError('informe exatamente uma origem: --input-zip ou --input-dir')`
- `DoiManifestRuntimeError('--output é obrigatório')`

#### Opções CLI mencionadas diretamente no corpo

- `--output é obrigatório`

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9d4dc1a4a9cee30a69effc31424ccc3ead1cf041bf3fbd09a679659e34fe015b`
**LOC do arquivo-fonte:** 170


### `academic_pipeline.doi_manifest_runtime:run_make_doi_manifest_command`

**Assinatura:**
```python
def run_make_doi_manifest_command(argv: Sequence[str] | None = None) -> int
```

**Arquivo:** `academic_pipeline/doi_manifest_runtime.py`
**Linhas:** 150–162
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `make_doi_manifest_command` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `Sequence[str] \| None` | `None` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `0`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `_build_parser`
- `_build_parser().parse_args`
- `_normalize_argv`
- `_request_paths`
- `_validate_option_surface`
- `json.dumps`
- `list`
- `make_doi_manifest`
- `print`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.runtime:_run_native_doi_manifest`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9d4dc1a4a9cee30a69effc31424ccc3ead1cf041bf3fbd09a679659e34fe015b`
**LOC do arquivo-fonte:** 170


### `academic_pipeline.institution_compliance_runtime:default_institution_compliance_runtime_context`

**Assinatura:**
```python
def default_institution_compliance_runtime_context() -> InstitutionComplianceRuntimeContext
```

**Arquivo:** `academic_pipeline/institution_compliance_runtime.py`
**Linhas:** 46–56
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `default_institution_compliance_runtime_context` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `InstitutionComplianceRuntimeContext`. Retornos observados estaticamente incluem: `InstitutionComplianceRuntimeContext(load_config=base.load_config, apply_cli_path_overrides=base.apply_cli_path_overrides, output_paths=base.output_paths, run_institution_compliance=run_institution_compliance, write_compliance_reports=write_compliance_reports, render_compliance_markdown=render_compliance_markdown)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `InstitutionComplianceRuntimeContext`
- `default_doctor_runtime_context`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.institution_compliance_runtime:run_institution_compliance_command`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `41b323120b03a64a317ef6288df3cecbdbc042532d7b739c0485ee04b3e94117`
**LOC do arquivo-fonte:** 155


### `academic_pipeline.institution_compliance_runtime:_normalize_argv`

**Assinatura:**
```python
def _normalize_argv(argv: Sequence[str] | None) -> tuple[str, ...]
```

**Arquivo:** `academic_pipeline/institution_compliance_runtime.py`
**Linhas:** 59–63
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Normaliza a responsabilidade representada por `argv` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `Sequence[str] \| None` | `obrigatório/ausente` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |

#### Retorno

Anotação declarada: `tuple[str, ...]`. Retornos observados estaticamente incluem: `tuple((str(item) for item in source))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `str`
- `tuple`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `41b323120b03a64a317ef6288df3cecbdbc042532d7b739c0485ee04b3e94117`
**LOC do arquivo-fonte:** 155


### `academic_pipeline.institution_compliance_runtime:_build_parser`

**Assinatura:**
```python
def _build_parser() -> argparse.ArgumentParser
```

**Arquivo:** `academic_pipeline/institution_compliance_runtime.py`
**Linhas:** 66–75
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Constrói a responsabilidade representada por `parser` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `argparse.ArgumentParser`. Retornos observados estaticamente incluem: `parser`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `InstitutionComplianceRuntimeError`
- `cli_parser.build_parser`
- `isinstance`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `InstitutionComplianceRuntimeError('build_parser não retornou ArgumentParser')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `41b323120b03a64a317ef6288df3cecbdbc042532d7b739c0485ee04b3e94117`
**LOC do arquivo-fonte:** 155


### `academic_pipeline.institution_compliance_runtime:_prepare_config`

**Assinatura:**
```python
def _prepare_config(args: argparse.Namespace, context: InstitutionComplianceRuntimeContext) -> dict[str, Any]
```

**Arquivo:** `academic_pipeline/institution_compliance_runtime.py`
**Linhas:** 78–95
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prepare_config` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `argparse.Namespace` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `context` | positional-or-keyword | `InstitutionComplianceRuntimeContext` | `obrigatório/ausente` | Contexto explícito de dependências utilizado para desacoplar a rotina de imports/globais. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `{}`; `prepared`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `InstitutionComplianceRuntimeError`
- `Path`
- `Path(args.config).expanduser`
- `Path(args.config).expanduser().resolve`
- `context.apply_cli_path_overrides`
- `context.load_config`
- `isinstance`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `InstitutionComplianceRuntimeError('load_config não retornou dict')`
- `InstitutionComplianceRuntimeError('apply_cli_path_overrides não retornou dict')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `41b323120b03a64a317ef6288df3cecbdbc042532d7b739c0485ee04b3e94117`
**LOC do arquivo-fonte:** 155


### `academic_pipeline.institution_compliance_runtime:_dispatch_runtime`

**Assinatura:**
```python
def _dispatch_runtime(cfg: Mapping[str, Any], context: InstitutionComplianceRuntimeContext) -> Mapping[str, Any]
```

**Arquivo:** `academic_pipeline/institution_compliance_runtime.py`
**Linhas:** 98–117
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Encaminha a responsabilidade representada por `runtime` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `Mapping[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `context` | positional-or-keyword | `InstitutionComplianceRuntimeContext` | `obrigatório/ausente` | Contexto explícito de dependências utilizado para desacoplar a rotina de imports/globais. |

#### Retorno

Anotação declarada: `Mapping[str, Any]`. Retornos observados estaticamente incluem: `MappingProxyType({'Path': Path, 'cfg': dict(cfg), 'output_paths': context.output_paths, 'run_institution_compliance': context.run_institution_compliance, 'write_compliance_reports': context.write_compliance_reports, 'render_compliance_markdown': context.render_compliance_markdown})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `MappingProxyType`
- `dict`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.institution_compliance_runtime:run_institution_compliance_command`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `41b323120b03a64a317ef6288df3cecbdbc042532d7b739c0485ee04b3e94117`
**LOC do arquivo-fonte:** 155


### `academic_pipeline.institution_compliance_runtime:run_institution_compliance_command`

**Assinatura:**
```python
def run_institution_compliance_command(argv: Sequence[str] | None = None, *, context: InstitutionComplianceRuntimeContext | None = None) -> int
```

**Arquivo:** `academic_pipeline/institution_compliance_runtime.py`
**Linhas:** 120–146
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `institution_compliance_command` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `Sequence[str] \| None` | `None` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |
| `context` | keyword-only | `InstitutionComplianceRuntimeContext \| None` | `None` | Contexto explícito de dependências utilizado para desacoplar a rotina de imports/globais. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `int(result.value)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `InstitutionComplianceRuntimeError`
- `_build_parser`
- `_build_parser().parse_args`
- `_dispatch_runtime`
- `_normalize_argv`
- `_prepare_config`
- `bool`
- `default_institution_compliance_runtime_context`
- `dispatch_stage_015`
- `int`
- `list`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.runtime:_run_native_institution_compliance`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `InstitutionComplianceRuntimeError('run_institution_compliance_command exige --check-institution-compliance')`
- `InstitutionComplianceRuntimeError('dispatch_stage_015 não tratou --check-institution-compliance')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `41b323120b03a64a317ef6288df3cecbdbc042532d7b739c0485ee04b3e94117`
**LOC do arquivo-fonte:** 155


### `academic_pipeline.list_profiles_runtime:print_profiles`

**Assinatura:**
```python
def print_profiles() -> None
```

**Arquivo:** `academic_pipeline/list_profiles_runtime.py`
**Linhas:** 23–27
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Exibe a responsabilidade representada por `profiles` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `print`
- `textwrap.fill`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9a6bbbd980c0111067af17613b97a30b4bc9c333852cc30279c33999653e333d`
**LOC do arquivo-fonte:** 38


### `academic_pipeline.list_profiles_runtime:run_list_profiles_command`

**Assinatura:**
```python
def run_list_profiles_command(argv: Sequence[str] | None = None) -> int
```

**Arquivo:** `academic_pipeline/list_profiles_runtime.py`
**Linhas:** 31–38
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `list_profiles_command` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `Sequence[str] \| None` | `None` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `0`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `ValueError`
- `list`
- `print_profiles`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.runtime:_run_native_list_profiles`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `ValueError(f'argumentos não suportados: {unexpected!r}')`

#### Opções CLI mencionadas diretamente no corpo

- `--list-profiles`

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9a6bbbd980c0111067af17613b97a30b4bc9c333852cc30279c33999653e333d`
**LOC do arquivo-fonte:** 38


### `academic_pipeline.prisma_generic_orchestration:_invoke_with_runtime`

**Assinatura:**
```python
def _invoke_with_runtime(function: Any, runtime: Mapping[str, Any], args: tuple[Any, ...], kwargs: dict[str, Any]) -> Any
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 13–31
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_invoke_with_runtime` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `function` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `function` recebido pela função; não há descrição individual explícita no código. |
| `runtime` | positional-or-keyword | `Mapping[str, Any]` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `args` | positional-or-keyword | `tuple[Any, ...]` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `kwargs` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `kwargs` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Any`. Retornos observados estaticamente incluem: `function(*args, **kwargs)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `added.append`
- `function`
- `namespace.pop`
- `previous.items`
- `runtime.items`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:json_or_none_with_runtime`
- `academic_pipeline.prisma_generic_orchestration:make_client_with_runtime`
- `academic_pipeline.prisma_generic_orchestration:prisma_artigo_generico_get_arg_with_runtime`
- `academic_pipeline.prisma_generic_orchestration:prisma_artigo_generico_out_dir_with_runtime`
- `academic_pipeline.prisma_generic_orchestration:prisma_artigo_generico_run_export_with_runtime`
- `academic_pipeline.prisma_generic_orchestration:prisma_artigo_generico_run_freeze_with_runtime`
- `academic_pipeline.prisma_generic_orchestration:prisma_artigo_generico_strip_with_runtime`
- `academic_pipeline.prisma_generic_orchestration:prisma_curadoria_arg_with_runtime`
- `academic_pipeline.prisma_generic_orchestration:prisma_curadoria_build_cmd_with_runtime`
- `academic_pipeline.prisma_generic_orchestration:prisma_curadoria_config_from_args_with_runtime`
- `academic_pipeline.prisma_generic_orchestration:prisma_curadoria_default_config_with_runtime`
- `academic_pipeline.prisma_generic_orchestration:prisma_curadoria_default_out_dir_with_runtime`
- `academic_pipeline.prisma_generic_orchestration:prisma_curadoria_default_prompt_with_runtime`
- `academic_pipeline.prisma_generic_orchestration:prisma_curadoria_dispatch_with_runtime`
- `academic_pipeline.prisma_generic_orchestration:prisma_curadoria_fluxo_completo_with_runtime`
- `academic_pipeline.prisma_generic_orchestration:prisma_curadoria_importar_no_pipeline_with_runtime`
- `academic_pipeline.prisma_generic_orchestration:prisma_curadoria_input_from_args_with_runtime`
- `academic_pipeline.prisma_generic_orchestration:prisma_curadoria_menu_with_runtime`
- `academic_pipeline.prisma_generic_orchestration:prisma_curadoria_mostrar_caminhos_with_runtime`
- `academic_pipeline.prisma_generic_orchestration:prisma_curadoria_out_from_args_with_runtime`
- `academic_pipeline.prisma_generic_orchestration:prisma_curadoria_pipeline_supports_flag_with_runtime`
- `academic_pipeline.prisma_generic_orchestration:prisma_curadoria_prompt_from_args_with_runtime`
- `academic_pipeline.prisma_generic_orchestration:prisma_curadoria_reexportar_xlsx_with_runtime`
- `academic_pipeline.prisma_generic_orchestration:prisma_curadoria_run_command_with_runtime`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 3
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_ap003e_body_stage_1`

**Assinatura:**
```python
def _ap003e_body_stage_1(message)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 33–35
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Mostra etapas de execução em tempo real.

**Docstring resumida:** Mostra etapas de execução em tempo real.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `message` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `message` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `print`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:stage_with_runtime`

**Assinatura:**
```python
def stage_with_runtime(runtime, /, message)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 37–38
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `stage_with_runtime` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `message` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `message` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_invoke_with_runtime(_ap003e_body_stage_1, runtime, (message,), {})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_invoke_with_runtime`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:stage_impl_001`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:stage_impl_001`

**Assinatura:**
```python
def stage_impl_001(runtime, /, message)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 40–41
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `stage_impl_001` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `message` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `message` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `stage_with_runtime(runtime, message)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `stage_with_runtime`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_ap003e_body__json_or_none_1`

**Assinatura:**
```python
def _ap003e_body__json_or_none_1(value)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 43–49
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_ap003e_body__json_or_none_1` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `None`; `json.loads(value)`; `value`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `json.loads`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:json_or_none_with_runtime`

**Assinatura:**
```python
def json_or_none_with_runtime(runtime, /, value)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 51–52
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `json_or_none_with_runtime` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `value` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_invoke_with_runtime(_ap003e_body__json_or_none_1, runtime, (value,), {})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_invoke_with_runtime`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_json_or_none_impl_001`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_json_or_none_impl_001`

**Assinatura:**
```python
def _json_or_none_impl_001(runtime, /, value)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 54–55
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_json_or_none_impl_001` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `value` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `json_or_none_with_runtime(runtime, value)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `json_or_none_with_runtime`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_ap003e_body_make_client_1`

**Assinatura:**
```python
def _ap003e_body_make_client_1(model_override)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 57–62
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_ap003e_body_make_client_1` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `model_override` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `model_override` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `(OpenAI(api_key=os.getenv('OPENAI_API_KEY')), model_override or os.getenv('OPENAI_MODEL') or DEFAULT_MODEL)`.

#### Efeitos colaterais e fronteiras externas

- Lê configuração/credenciais do ambiente do processo.
- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `OpenAI`
- `RuntimeError`
- `load_dotenv`
- `os.getenv`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('OPENAI_API_KEY não encontrado no ambiente/.env.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:make_client_with_runtime`

**Assinatura:**
```python
def make_client_with_runtime(runtime, /, model_override)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 64–65
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Cria a responsabilidade representada por `client_with_runtime` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `model_override` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `model_override` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_invoke_with_runtime(_ap003e_body_make_client_1, runtime, (model_override,), {})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_invoke_with_runtime`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:make_client_impl_001`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:make_client_impl_001`

**Assinatura:**
```python
def make_client_impl_001(runtime, /, model_override)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 67–68
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Cria a responsabilidade representada por `client_impl_001` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `model_override` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `model_override` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `make_client_with_runtime(runtime, model_override)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `make_client_with_runtime`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_ap003e_body__section_1`

**Assinatura:**
```python
def _ap003e_body__section_1(cfg, name)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 70–72
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_ap003e_body__section_1` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `name` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `name` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `sec if isinstance(sec, dict) else {}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `cfg.get`
- `isinstance`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:section_with_runtime`

**Assinatura:**
```python
def section_with_runtime(runtime, /, cfg, name)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 74–75
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `section_with_runtime` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `cfg` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `name` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `name` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_invoke_with_runtime(_ap003e_body__section_1, runtime, (cfg, name), {})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_invoke_with_runtime`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_section_impl_001`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_section_impl_001`

**Assinatura:**
```python
def _section_impl_001(runtime, /, cfg, name)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 77–78
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_section_impl_001` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `cfg` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `name` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `name` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `section_with_runtime(runtime, cfg, name)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `section_with_runtime`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_ap003e_body_research_output_paths_1`

**Assinatura:**
```python
def _ap003e_body_research_output_paths_1(cfg)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 80–97
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Resolve a saída canônica da busca e consolidação PRISMA.

**Docstring resumida:** Resolve a saída canônica da busca e consolidação PRISMA.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `(out_dir, prefix)`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(str(cfg.get('__config_dir__') or Path.cwd())).resolve`
- `Path.cwd`
- `_section`
- `bool`
- `cfg.get`
- `out_dir.mkdir`
- `paths.get`
- `projeto.get`
- `resolve_path`
- `str`
- `str(paths.get('research_prefix') or default_prefix).strip`
- `str(projeto.get('nome') or '').strip`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:research_output_paths_with_runtime`

**Assinatura:**
```python
def research_output_paths_with_runtime(runtime, /, cfg)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 99–100
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `research_output_paths_with_runtime` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `cfg` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_invoke_with_runtime(_ap003e_body_research_output_paths_1, runtime, (cfg,), {})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_invoke_with_runtime`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:research_output_paths_impl_001`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:research_output_paths_impl_001`

**Assinatura:**
```python
def research_output_paths_impl_001(runtime, /, cfg)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 102–103
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `research_output_paths_impl_001` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `cfg` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `research_output_paths_with_runtime(runtime, cfg)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `research_output_paths_with_runtime`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.doctor_runtime:_research_output_paths`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_ap003e_body_render_external_prisma_outputs_1`

**Assinatura:**
```python
def _ap003e_body_render_external_prisma_outputs_1(cfg, out_dir, prefix, prisma_payload, *, phase)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 105–136
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Renderiza relatório PRISMA externo em ORG e, quando solicitado, em PDF.

**Docstring resumida:** Renderiza relatório PRISMA externo em ORG e, quando solicitado, em PDF.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `out_dir` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `out_dir` recebido pela função; não há descrição individual explícita no código. |
| `prefix` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Prefixo canônico usado para compor nomes de artefatos. |
| `prisma_payload` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Payload estruturado recebido ou produzido pela operação. |
| `phase` | keyword-only | `não anotado` | `obrigatório/ausente` | Parâmetro `phase` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `(None, None)`; `(org_path, pdf_path)`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(str(cfg.get('__config_dir__') or Path.cwd())).resolve`
- `Path.cwd`
- `bool`
- `cfg.get`
- `ensure_prisma_flow_diagram`
- `isinstance`
- `latex_cfg.get`
- `print`
- `render_external_prisma_org_report`
- `report_cfg.get`
- `resolve_path`
- `run_compile_sequence`
- `stage`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:render_external_prisma_outputs_with_runtime`

**Assinatura:**
```python
def render_external_prisma_outputs_with_runtime(runtime, /, cfg, out_dir, prefix, prisma_payload, *, phase)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 138–139
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Renderiza a responsabilidade representada por `external_prisma_outputs_with_runtime` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `cfg` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `out_dir` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `out_dir` recebido pela função; não há descrição individual explícita no código. |
| `prefix` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Prefixo canônico usado para compor nomes de artefatos. |
| `prisma_payload` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Payload estruturado recebido ou produzido pela operação. |
| `phase` | keyword-only | `não anotado` | `obrigatório/ausente` | Parâmetro `phase` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_invoke_with_runtime(_ap003e_body_render_external_prisma_outputs_1, runtime, (cfg, out_dir, prefix, prisma_payload), {'phase': phase})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_invoke_with_runtime`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:render_external_prisma_outputs_impl_001`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:render_external_prisma_outputs_impl_001`

**Assinatura:**
```python
def render_external_prisma_outputs_impl_001(runtime, /, cfg, out_dir, prefix, prisma_payload, *, phase)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 141–142
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Renderiza a responsabilidade representada por `external_prisma_outputs_impl_001` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `cfg` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `out_dir` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `out_dir` recebido pela função; não há descrição individual explícita no código. |
| `prefix` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Prefixo canônico usado para compor nomes de artefatos. |
| `prisma_payload` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Payload estruturado recebido ou produzido pela operação. |
| `phase` | keyword-only | `não anotado` | `obrigatório/ausente` | Parâmetro `phase` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `render_external_prisma_outputs_with_runtime(runtime, cfg, out_dir, prefix, prisma_payload, phase=phase)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `render_external_prisma_outputs_with_runtime`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_default_config_1`

**Assinatura:**
```python
def _ap003e_body__prisma_curadoria_default_config_1()
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 144–145
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_ap003e_body__prisma_curadoria_default_config_1` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `'app_bundle/projetos/prisma_fluxo_pmf/prisma_fluxo_pmf.toml'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

Nenhum identificado pela análise estática.

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `app_bundle/projetos/prisma_fluxo_pmf/prisma_fluxo_pmf.toml`

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:prisma_curadoria_default_config_with_runtime`

**Assinatura:**
```python
def prisma_curadoria_default_config_with_runtime(runtime, /)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 147–148
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `prisma_curadoria_default_config_with_runtime` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_invoke_with_runtime(_ap003e_body__prisma_curadoria_default_config_1, runtime, (), {})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_invoke_with_runtime`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_prisma_curadoria_default_config_impl_001`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_prisma_curadoria_default_config_impl_001`

**Assinatura:**
```python
def _prisma_curadoria_default_config_impl_001(runtime, /)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 150–151
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_curadoria_default_config_impl_001` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `prisma_curadoria_default_config_with_runtime(runtime)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `prisma_curadoria_default_config_with_runtime`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_default_out_dir_1`

**Assinatura:**
```python
def _ap003e_body__prisma_curadoria_default_out_dir_1()
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 153–154
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_ap003e_body__prisma_curadoria_default_out_dir_1` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `'app_bundle/projetos/prisma_fluxo_pmf/output_pesquisa/relatorio_prisma_prisma_fluxo_pmf'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

Nenhum identificado pela análise estática.

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:prisma_curadoria_default_out_dir_with_runtime`

**Assinatura:**
```python
def prisma_curadoria_default_out_dir_with_runtime(runtime, /)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 156–157
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `prisma_curadoria_default_out_dir_with_runtime` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_invoke_with_runtime(_ap003e_body__prisma_curadoria_default_out_dir_1, runtime, (), {})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_invoke_with_runtime`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_prisma_curadoria_default_out_dir_impl_001`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_prisma_curadoria_default_out_dir_impl_001`

**Assinatura:**
```python
def _prisma_curadoria_default_out_dir_impl_001(runtime, /)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 159–160
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_curadoria_default_out_dir_impl_001` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `prisma_curadoria_default_out_dir_with_runtime(runtime)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `prisma_curadoria_default_out_dir_with_runtime`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_default_prompt_1`

**Assinatura:**
```python
def _ap003e_body__prisma_curadoria_default_prompt_1()
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 162–163
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_ap003e_body__prisma_curadoria_default_prompt_1` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `''`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

Nenhum identificado pela análise estática.

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:prisma_curadoria_default_prompt_with_runtime`

**Assinatura:**
```python
def prisma_curadoria_default_prompt_with_runtime(runtime, /)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 165–166
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `prisma_curadoria_default_prompt_with_runtime` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_invoke_with_runtime(_ap003e_body__prisma_curadoria_default_prompt_1, runtime, (), {})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_invoke_with_runtime`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_prisma_curadoria_default_prompt_impl_001`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_prisma_curadoria_default_prompt_impl_001`

**Assinatura:**
```python
def _prisma_curadoria_default_prompt_impl_001(runtime, /)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 168–169
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_curadoria_default_prompt_impl_001` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `prisma_curadoria_default_prompt_with_runtime(runtime)`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `prisma_curadoria_default_prompt_with_runtime`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_script_path_1`

**Assinatura:**
```python
def _ap003e_body__prisma_curadoria_script_path_1()
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 171–172
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_ap003e_body__prisma_curadoria_script_path_1` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `'app_bundle/scripts/pipeline/prisma_curadoria_ia_referencias.py'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

Nenhum identificado pela análise estática.

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:prisma_curadoria_script_path_with_runtime`

**Assinatura:**
```python
def prisma_curadoria_script_path_with_runtime(runtime, /)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 174–175
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `prisma_curadoria_script_path_with_runtime` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_invoke_with_runtime(_ap003e_body__prisma_curadoria_script_path_1, runtime, (), {})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_invoke_with_runtime`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_prisma_curadoria_script_path_impl_001`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_prisma_curadoria_script_path_impl_001`

**Assinatura:**
```python
def _prisma_curadoria_script_path_impl_001(runtime, /)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 177–178
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_curadoria_script_path_impl_001` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `prisma_curadoria_script_path_with_runtime(runtime)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `prisma_curadoria_script_path_with_runtime`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_arg_1`

**Assinatura:**
```python
def _ap003e_body__prisma_curadoria_arg_1(args, name, default)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 180–181
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_ap003e_body__prisma_curadoria_arg_1` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `name` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `name` recebido pela função; não há descrição individual explícita no código. |
| `default` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `default` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `getattr(args, name, default)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `getattr`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:prisma_curadoria_arg_with_runtime`

**Assinatura:**
```python
def prisma_curadoria_arg_with_runtime(runtime, /, args, name, default)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 183–184
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `prisma_curadoria_arg_with_runtime` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `name` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `name` recebido pela função; não há descrição individual explícita no código. |
| `default` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `default` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_invoke_with_runtime(_ap003e_body__prisma_curadoria_arg_1, runtime, (args, name, default), {})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_invoke_with_runtime`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_prisma_curadoria_arg_impl_001`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_prisma_curadoria_arg_impl_001`

**Assinatura:**
```python
def _prisma_curadoria_arg_impl_001(runtime, /, args, name, default)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 186–187
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_curadoria_arg_impl_001` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `name` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `name` recebido pela função; não há descrição individual explícita no código. |
| `default` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `default` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `prisma_curadoria_arg_with_runtime(runtime, args, name, default)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `prisma_curadoria_arg_with_runtime`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_config_from_args_1`

**Assinatura:**
```python
def _ap003e_body__prisma_curadoria_config_from_args_1(args)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 189–190
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_ap003e_body__prisma_curadoria_config_from_args_1` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_prisma_curadoria_arg(args, 'config', None) or _prisma_curadoria_arg(args, 'cfg', None) or _prisma_curadoria_default_config()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_prisma_curadoria_arg`
- `_prisma_curadoria_default_config`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:prisma_curadoria_config_from_args_with_runtime`

**Assinatura:**
```python
def prisma_curadoria_config_from_args_with_runtime(runtime, /, args)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 192–193
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `prisma_curadoria_config_from_args_with_runtime` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_invoke_with_runtime(_ap003e_body__prisma_curadoria_config_from_args_1, runtime, (args,), {})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_invoke_with_runtime`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_prisma_curadoria_config_from_args_impl_001`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_prisma_curadoria_config_from_args_impl_001`

**Assinatura:**
```python
def _prisma_curadoria_config_from_args_impl_001(runtime, /, args)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 195–196
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_curadoria_config_from_args_impl_001` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `prisma_curadoria_config_from_args_with_runtime(runtime, args)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `prisma_curadoria_config_from_args_with_runtime`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_out_from_args_1`

**Assinatura:**
```python
def _ap003e_body__prisma_curadoria_out_from_args_1(args)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 198–199
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_ap003e_body__prisma_curadoria_out_from_args_1` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_prisma_curadoria_arg(args, 'prisma_curadoria_out_dir', None) or _prisma_curadoria_default_out_dir()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_prisma_curadoria_arg`
- `_prisma_curadoria_default_out_dir`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:prisma_curadoria_out_from_args_with_runtime`

**Assinatura:**
```python
def prisma_curadoria_out_from_args_with_runtime(runtime, /, args)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 201–202
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `prisma_curadoria_out_from_args_with_runtime` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_invoke_with_runtime(_ap003e_body__prisma_curadoria_out_from_args_1, runtime, (args,), {})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_invoke_with_runtime`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_prisma_curadoria_out_from_args_impl_001`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_prisma_curadoria_out_from_args_impl_001`

**Assinatura:**
```python
def _prisma_curadoria_out_from_args_impl_001(runtime, /, args)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 204–205
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_curadoria_out_from_args_impl_001` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `prisma_curadoria_out_from_args_with_runtime(runtime, args)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `prisma_curadoria_out_from_args_with_runtime`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_prompt_from_args_1`

**Assinatura:**
```python
def _ap003e_body__prisma_curadoria_prompt_from_args_1(args)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 207–208
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_ap003e_body__prisma_curadoria_prompt_from_args_1` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_prisma_curadoria_arg(args, 'prisma_curadoria_prompt', None) or _prisma_curadoria_default_prompt()`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `_prisma_curadoria_arg`
- `_prisma_curadoria_default_prompt`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:prisma_curadoria_prompt_from_args_with_runtime`

**Assinatura:**
```python
def prisma_curadoria_prompt_from_args_with_runtime(runtime, /, args)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 210–211
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `prisma_curadoria_prompt_from_args_with_runtime` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_invoke_with_runtime(_ap003e_body__prisma_curadoria_prompt_from_args_1, runtime, (args,), {})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_invoke_with_runtime`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_prisma_curadoria_prompt_from_args_impl_001`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_prisma_curadoria_prompt_from_args_impl_001`

**Assinatura:**
```python
def _prisma_curadoria_prompt_from_args_impl_001(runtime, /, args)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 213–214
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_curadoria_prompt_from_args_impl_001` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `prisma_curadoria_prompt_from_args_with_runtime(runtime, args)`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `prisma_curadoria_prompt_from_args_with_runtime`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_input_from_args_1`

**Assinatura:**
```python
def _ap003e_body__prisma_curadoria_input_from_args_1(args, *, default_xlsx)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 216–223
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_ap003e_body__prisma_curadoria_input_from_args_1` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `default_xlsx` | keyword-only | `não anotado` | `obrigatório/ausente` | Parâmetro `default_xlsx` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `explicit`; `str(Path(_prisma_curadoria_out_from_args(args)) / 'relatorio_prisma_prisma_fluxo_pmf.curadoria_ia_referencias.xlsx')`; `''`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `_prisma_curadoria_arg`
- `_prisma_curadoria_out_from_args`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `relatorio_prisma_prisma_fluxo_pmf.curadoria_ia_referencias.xlsx`

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:prisma_curadoria_input_from_args_with_runtime`

**Assinatura:**
```python
def prisma_curadoria_input_from_args_with_runtime(runtime, /, args, *, default_xlsx)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 225–226
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `prisma_curadoria_input_from_args_with_runtime` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `default_xlsx` | keyword-only | `não anotado` | `obrigatório/ausente` | Parâmetro `default_xlsx` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_invoke_with_runtime(_ap003e_body__prisma_curadoria_input_from_args_1, runtime, (args,), {'default_xlsx': default_xlsx})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_invoke_with_runtime`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_prisma_curadoria_input_from_args_impl_001`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_prisma_curadoria_input_from_args_impl_001`

**Assinatura:**
```python
def _prisma_curadoria_input_from_args_impl_001(runtime, /, args, *, default_xlsx)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 228–229
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_curadoria_input_from_args_impl_001` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `default_xlsx` | keyword-only | `não anotado` | `obrigatório/ausente` | Parâmetro `default_xlsx` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `prisma_curadoria_input_from_args_with_runtime(runtime, args, default_xlsx=default_xlsx)`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `prisma_curadoria_input_from_args_with_runtime`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_run_command_1`

**Assinatura:**
```python
def _ap003e_body__prisma_curadoria_run_command_1(cmd)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 231–242
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_ap003e_body__prisma_curadoria_run_command_1` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cmd` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `cmd` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `proc.returncode`.

#### Efeitos colaterais e fronteiras externas

- Executa processo ou ferramenta externa.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `print`
- `subprocess.run`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:prisma_curadoria_run_command_with_runtime`

**Assinatura:**
```python
def prisma_curadoria_run_command_with_runtime(runtime, /, cmd)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 244–245
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `prisma_curadoria_run_command_with_runtime` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `cmd` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `cmd` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_invoke_with_runtime(_ap003e_body__prisma_curadoria_run_command_1, runtime, (cmd,), {})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_invoke_with_runtime`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_prisma_curadoria_run_command_impl_001`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_prisma_curadoria_run_command_impl_001`

**Assinatura:**
```python
def _prisma_curadoria_run_command_impl_001(runtime, /, cmd)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 247–248
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_curadoria_run_command_impl_001` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `cmd` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `cmd` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `prisma_curadoria_run_command_with_runtime(runtime, cmd)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `prisma_curadoria_run_command_with_runtime`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_build_cmd_1`

**Assinatura:**
```python
def _ap003e_body__prisma_curadoria_build_cmd_1(args, *, usar_ia, reexportar_xlsx)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 250–278
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_ap003e_body__prisma_curadoria_build_cmd_1` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `usar_ia` | keyword-only | `não anotado` | `obrigatório/ausente` | Parâmetro `usar_ia` recebido pela função; não há descrição individual explícita no código. |
| `reexportar_xlsx` | keyword-only | `não anotado` | `obrigatório/ausente` | Parâmetro `reexportar_xlsx` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `cmd`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `SystemExit`
- `_prisma_curadoria_arg`
- `_prisma_curadoria_config_from_args`
- `_prisma_curadoria_input_from_args`
- `_prisma_curadoria_out_from_args`
- `_prisma_curadoria_prompt_from_args`
- `_prisma_curadoria_script_path`
- `script.exists`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `SystemExit(f'Script de curadoria IA não encontrado: {script}. Rode o aplicador da curadoria IA v2 antes.')`

#### Opções CLI mencionadas diretamente no corpo

- `--config`
- `--input`
- `--limiar-minimo-inclusao`
- `--max-incluir`
- `--out-dir`
- `--prompt-curadoria`
- `--reexportar-xlsx`
- `--top-n-candidatos`
- `--usar-ia`

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 8
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:prisma_curadoria_build_cmd_with_runtime`

**Assinatura:**
```python
def prisma_curadoria_build_cmd_with_runtime(runtime, /, args, *, usar_ia, reexportar_xlsx)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 280–281
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `prisma_curadoria_build_cmd_with_runtime` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `usar_ia` | keyword-only | `não anotado` | `obrigatório/ausente` | Parâmetro `usar_ia` recebido pela função; não há descrição individual explícita no código. |
| `reexportar_xlsx` | keyword-only | `não anotado` | `obrigatório/ausente` | Parâmetro `reexportar_xlsx` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_invoke_with_runtime(_ap003e_body__prisma_curadoria_build_cmd_1, runtime, (args,), {'usar_ia': usar_ia, 'reexportar_xlsx': reexportar_xlsx})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_invoke_with_runtime`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_prisma_curadoria_build_cmd_impl_001`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_prisma_curadoria_build_cmd_impl_001`

**Assinatura:**
```python
def _prisma_curadoria_build_cmd_impl_001(runtime, /, args, *, usar_ia, reexportar_xlsx)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 283–284
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_curadoria_build_cmd_impl_001` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `usar_ia` | keyword-only | `não anotado` | `obrigatório/ausente` | Parâmetro `usar_ia` recebido pela função; não há descrição individual explícita no código. |
| `reexportar_xlsx` | keyword-only | `não anotado` | `obrigatório/ausente` | Parâmetro `reexportar_xlsx` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `prisma_curadoria_build_cmd_with_runtime(runtime, args, usar_ia=usar_ia, reexportar_xlsx=reexportar_xlsx)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `prisma_curadoria_build_cmd_with_runtime`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_run_ia_1`

**Assinatura:**
```python
def _ap003e_body__prisma_curadoria_run_ia_1(args, *, usar_ia)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 286–288
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_ap003e_body__prisma_curadoria_run_ia_1` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `usar_ia` | keyword-only | `não anotado` | `obrigatório/ausente` | Parâmetro `usar_ia` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_prisma_curadoria_run_command(cmd)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_prisma_curadoria_build_cmd`
- `_prisma_curadoria_run_command`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:prisma_curadoria_run_ia_with_runtime`

**Assinatura:**
```python
def prisma_curadoria_run_ia_with_runtime(runtime, /, args, *, usar_ia)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 290–291
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `prisma_curadoria_run_ia_with_runtime` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `usar_ia` | keyword-only | `não anotado` | `obrigatório/ausente` | Parâmetro `usar_ia` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_invoke_with_runtime(_ap003e_body__prisma_curadoria_run_ia_1, runtime, (args,), {'usar_ia': usar_ia})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_invoke_with_runtime`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_prisma_curadoria_run_ia_impl_001`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_prisma_curadoria_run_ia_impl_001`

**Assinatura:**
```python
def _prisma_curadoria_run_ia_impl_001(runtime, /, args, *, usar_ia)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 293–294
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_curadoria_run_ia_impl_001` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `usar_ia` | keyword-only | `não anotado` | `obrigatório/ausente` | Parâmetro `usar_ia` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `prisma_curadoria_run_ia_with_runtime(runtime, args, usar_ia=usar_ia)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `prisma_curadoria_run_ia_with_runtime`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_reexportar_xlsx_1`

**Assinatura:**
```python
def _ap003e_body__prisma_curadoria_reexportar_xlsx_1(args)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 296–298
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_ap003e_body__prisma_curadoria_reexportar_xlsx_1` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_prisma_curadoria_run_command(cmd)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_prisma_curadoria_build_cmd`
- `_prisma_curadoria_run_command`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:prisma_curadoria_reexportar_xlsx_with_runtime`

**Assinatura:**
```python
def prisma_curadoria_reexportar_xlsx_with_runtime(runtime, /, args)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 300–301
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `prisma_curadoria_reexportar_xlsx_with_runtime` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_invoke_with_runtime(_ap003e_body__prisma_curadoria_reexportar_xlsx_1, runtime, (args,), {})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_invoke_with_runtime`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_prisma_curadoria_reexportar_xlsx_impl_001`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_prisma_curadoria_reexportar_xlsx_impl_001`

**Assinatura:**
```python
def _prisma_curadoria_reexportar_xlsx_impl_001(runtime, /, args)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 303–304
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_curadoria_reexportar_xlsx_impl_001` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `prisma_curadoria_reexportar_xlsx_with_runtime(runtime, args)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `prisma_curadoria_reexportar_xlsx_with_runtime`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_pipeline_supports_flag_1`

**Assinatura:**
```python
def _ap003e_body__prisma_curadoria_pipeline_supports_flag_1(flag)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 306–313
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_ap003e_body__prisma_curadoria_pipeline_supports_flag_1` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `flag` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `flag` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `False`; `flag in (proc.stdout or '')`.

#### Efeitos colaterais e fronteiras externas

- Executa processo ou ferramenta externa.

#### Chamadas realizadas pelo corpo direto

- `subprocess.run`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `--help`

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:prisma_curadoria_pipeline_supports_flag_with_runtime`

**Assinatura:**
```python
def prisma_curadoria_pipeline_supports_flag_with_runtime(runtime, /, flag)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 315–316
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `prisma_curadoria_pipeline_supports_flag_with_runtime` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `flag` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `flag` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_invoke_with_runtime(_ap003e_body__prisma_curadoria_pipeline_supports_flag_1, runtime, (flag,), {})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_invoke_with_runtime`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_prisma_curadoria_pipeline_supports_flag_impl_001`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_prisma_curadoria_pipeline_supports_flag_impl_001`

**Assinatura:**
```python
def _prisma_curadoria_pipeline_supports_flag_impl_001(runtime, /, flag)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 318–319
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_curadoria_pipeline_supports_flag_impl_001` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `flag` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `flag` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `prisma_curadoria_pipeline_supports_flag_with_runtime(runtime, flag)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `prisma_curadoria_pipeline_supports_flag_with_runtime`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_importar_no_pipeline_1`

**Assinatura:**
```python
def _ap003e_body__prisma_curadoria_importar_no_pipeline_1(args)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 321–337
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_ap003e_body__prisma_curadoria_importar_no_pipeline_1` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `1`; `_prisma_curadoria_run_command(cmd)`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `_prisma_curadoria_config_from_args`
- `_prisma_curadoria_out_from_args`
- `_prisma_curadoria_pipeline_supports_flag`
- `_prisma_curadoria_run_command`
- `print`
- `str`
- `triagem.exists`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `--config`
- `--prisma-importar-triagem`

#### Artefatos/caminhos literais reconhecidos no corpo

- `relatorio_prisma_prisma_fluxo_pmf.triagem_humana.csv`

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:prisma_curadoria_importar_no_pipeline_with_runtime`

**Assinatura:**
```python
def prisma_curadoria_importar_no_pipeline_with_runtime(runtime, /, args)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 339–340
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `prisma_curadoria_importar_no_pipeline_with_runtime` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_invoke_with_runtime(_ap003e_body__prisma_curadoria_importar_no_pipeline_1, runtime, (args,), {})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_invoke_with_runtime`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_prisma_curadoria_importar_no_pipeline_impl_001`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_prisma_curadoria_importar_no_pipeline_impl_001`

**Assinatura:**
```python
def _prisma_curadoria_importar_no_pipeline_impl_001(runtime, /, args)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 342–343
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_curadoria_importar_no_pipeline_impl_001` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `prisma_curadoria_importar_no_pipeline_with_runtime(runtime, args)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `prisma_curadoria_importar_no_pipeline_with_runtime`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_fluxo_completo_1`

**Assinatura:**
```python
def _ap003e_body__prisma_curadoria_fluxo_completo_1(args)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 345–349
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_ap003e_body__prisma_curadoria_fluxo_completo_1` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `rc`; `_prisma_curadoria_importar_no_pipeline(args)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_prisma_curadoria_arg`
- `_prisma_curadoria_importar_no_pipeline`
- `_prisma_curadoria_run_ia`
- `bool`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:prisma_curadoria_fluxo_completo_with_runtime`

**Assinatura:**
```python
def prisma_curadoria_fluxo_completo_with_runtime(runtime, /, args)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 351–352
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `prisma_curadoria_fluxo_completo_with_runtime` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_invoke_with_runtime(_ap003e_body__prisma_curadoria_fluxo_completo_1, runtime, (args,), {})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_invoke_with_runtime`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_prisma_curadoria_fluxo_completo_impl_001`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_prisma_curadoria_fluxo_completo_impl_001`

**Assinatura:**
```python
def _prisma_curadoria_fluxo_completo_impl_001(runtime, /, args)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 354–355
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_curadoria_fluxo_completo_impl_001` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `prisma_curadoria_fluxo_completo_with_runtime(runtime, args)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `prisma_curadoria_fluxo_completo_with_runtime`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_mostrar_caminhos_1`

**Assinatura:**
```python
def _ap003e_body__prisma_curadoria_mostrar_caminhos_1(args)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 357–375
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_ap003e_body__prisma_curadoria_mostrar_caminhos_1` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Sem anotação de retorno explícita. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `_prisma_curadoria_config_from_args`
- `_prisma_curadoria_out_from_args`
- `_prisma_curadoria_prompt_from_args`
- `_prisma_curadoria_script_path`
- `print`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `relatorio_prisma_prisma_fluxo_pmf.triagem_titulo_resumo.xlsx`
- `relatorio_prisma_prisma_fluxo_pmf.curadoria_ia_referencias.xlsx`
- `relatorio_prisma_prisma_fluxo_pmf.triagem_humana.csv`
- `relatorio_prisma_prisma_fluxo_pmf.referencias_incluidas_seminario.csv`
- `relatorio_prisma_prisma_fluxo_pmf.curadoria_ia_resumo.txt`
- `relatorio_prisma_prisma_fluxo_pmf.curadoria_ia_log.json`

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:prisma_curadoria_mostrar_caminhos_with_runtime`

**Assinatura:**
```python
def prisma_curadoria_mostrar_caminhos_with_runtime(runtime, /, args)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 377–378
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `prisma_curadoria_mostrar_caminhos_with_runtime` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_invoke_with_runtime(_ap003e_body__prisma_curadoria_mostrar_caminhos_1, runtime, (args,), {})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_invoke_with_runtime`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_prisma_curadoria_mostrar_caminhos_impl_001`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_prisma_curadoria_mostrar_caminhos_impl_001`

**Assinatura:**
```python
def _prisma_curadoria_mostrar_caminhos_impl_001(runtime, /, args)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 380–381
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_curadoria_mostrar_caminhos_impl_001` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `prisma_curadoria_mostrar_caminhos_with_runtime(runtime, args)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `prisma_curadoria_mostrar_caminhos_with_runtime`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_menu_1`

**Assinatura:**
```python
def _ap003e_body__prisma_curadoria_menu_1(args)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 383–416
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_ap003e_body__prisma_curadoria_menu_1` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `0`; `rc`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `_prisma_curadoria_fluxo_completo`
- `_prisma_curadoria_importar_no_pipeline`
- `_prisma_curadoria_mostrar_caminhos`
- `_prisma_curadoria_reexportar_xlsx`
- `_prisma_curadoria_run_ia`
- `input`
- `input('Escolha uma opção: ').strip`
- `print`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `3. Reexportar XLSX revisado para triagem_humana.csv`
- `4. Importar triagem_humana.csv e gerar PRISMA final`

#### Complexidade estrutural observada

```yaml
branches: 8
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:prisma_curadoria_menu_with_runtime`

**Assinatura:**
```python
def prisma_curadoria_menu_with_runtime(runtime, /, args)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 418–419
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `prisma_curadoria_menu_with_runtime` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_invoke_with_runtime(_ap003e_body__prisma_curadoria_menu_1, runtime, (args,), {})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_invoke_with_runtime`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_prisma_curadoria_menu_impl_001`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_prisma_curadoria_menu_impl_001`

**Assinatura:**
```python
def _prisma_curadoria_menu_impl_001(runtime, /, args)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 421–422
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_curadoria_menu_impl_001` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `prisma_curadoria_menu_with_runtime(runtime, args)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `prisma_curadoria_menu_with_runtime`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_curadoria_dispatch_1`

**Assinatura:**
```python
def _ap003e_body__prisma_curadoria_dispatch_1(args)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 424–436
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_ap003e_body__prisma_curadoria_dispatch_1` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_prisma_curadoria_menu(args)`; `_prisma_curadoria_reexportar_xlsx(args)`; `_prisma_curadoria_fluxo_completo(args)`; `_prisma_curadoria_importar_no_pipeline(args)`; `_prisma_curadoria_run_ia(args, usar_ia=usar_ia)`; `0`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_prisma_curadoria_arg`
- `_prisma_curadoria_fluxo_completo`
- `_prisma_curadoria_importar_no_pipeline`
- `_prisma_curadoria_menu`
- `_prisma_curadoria_reexportar_xlsx`
- `_prisma_curadoria_run_ia`
- `bool`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:prisma_curadoria_dispatch_with_runtime`

**Assinatura:**
```python
def prisma_curadoria_dispatch_with_runtime(runtime, /, args)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 438–439
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `prisma_curadoria_dispatch_with_runtime` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_invoke_with_runtime(_ap003e_body__prisma_curadoria_dispatch_1, runtime, (args,), {})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_invoke_with_runtime`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_prisma_curadoria_dispatch_impl_001`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_prisma_curadoria_dispatch_impl_001`

**Assinatura:**
```python
def _prisma_curadoria_dispatch_impl_001(runtime, /, args)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 441–442
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_curadoria_dispatch_impl_001` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `prisma_curadoria_dispatch_with_runtime(runtime, args)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `prisma_curadoria_dispatch_with_runtime`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_artigo_generico_get_arg_1`

**Assinatura:**
```python
def _ap003e_body__prisma_artigo_generico_get_arg_1(argv, name)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 444–450
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_ap003e_body__prisma_artigo_generico_get_arg_1` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |
| `name` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `name` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `argv[i + 1]`; `item.split('=', 1)[1]`; `None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `enumerate`
- `item.split`
- `item.startswith`
- `len`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:prisma_artigo_generico_get_arg_with_runtime`

**Assinatura:**
```python
def prisma_artigo_generico_get_arg_with_runtime(runtime, /, argv, name)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 452–453
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `prisma_artigo_generico_get_arg_with_runtime` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `argv` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |
| `name` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `name` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_invoke_with_runtime(_ap003e_body__prisma_artigo_generico_get_arg_1, runtime, (argv, name), {})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_invoke_with_runtime`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_prisma_artigo_generico_get_arg_impl_001`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_prisma_artigo_generico_get_arg_impl_001`

**Assinatura:**
```python
def _prisma_artigo_generico_get_arg_impl_001(runtime, /, argv, name)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 455–456
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_artigo_generico_get_arg_impl_001` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `argv` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |
| `name` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `name` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `prisma_artigo_generico_get_arg_with_runtime(runtime, argv, name)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `prisma_artigo_generico_get_arg_with_runtime`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_artigo_generico_strip_1`

**Assinatura:**
```python
def _ap003e_body__prisma_artigo_generico_strip_1(argv)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 458–476
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_ap003e_body__prisma_artigo_generico_strip_1` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `result`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `any`
- `item.startswith`
- `len`
- `result.append`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `--prisma-artigo-dir`
- `--prisma-artigo-prefix`
- `--prisma-artigo-toml-output`
- `--prisma-autor-artigo`
- `--prisma-bib-input`
- `--prisma-bib-output`
- `--prisma-congelamento-dir`
- `--prisma-congelar-artigo`
- `--prisma-csl-path`
- `--prisma-dados-pesquisa-path`
- `--prisma-exportar-bib`
- `--prisma-gerar-artigo-final`
- `--prisma-gerar-toml-artigo`
- `--prisma-openai-model-artigo`
- `--prisma-professor-artigo`

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:prisma_artigo_generico_strip_with_runtime`

**Assinatura:**
```python
def prisma_artigo_generico_strip_with_runtime(runtime, /, argv)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 478–479
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `prisma_artigo_generico_strip_with_runtime` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `argv` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_invoke_with_runtime(_ap003e_body__prisma_artigo_generico_strip_1, runtime, (argv,), {})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_invoke_with_runtime`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_prisma_artigo_generico_strip_impl_001`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_prisma_artigo_generico_strip_impl_001`

**Assinatura:**
```python
def _prisma_artigo_generico_strip_impl_001(runtime, /, argv)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 481–482
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_artigo_generico_strip_impl_001` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `argv` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `prisma_artigo_generico_strip_with_runtime(runtime, argv)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `prisma_artigo_generico_strip_with_runtime`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_artigo_generico_out_dir_1`

**Assinatura:**
```python
def _ap003e_body__prisma_artigo_generico_out_dir_1(argv)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 484–494
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_ap003e_body__prisma_artigo_generico_out_dir_1` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `Path(out_arg)`; `cfg_path.resolve().parent / 'output_pesquisa' / f'relatorio_prisma_{cfg_path.stem}'`; `Path('app_bundle/projetos/prisma_fluxo_pmf/output_pesquisa/relatorio_prisma_prisma_fluxo_pmf')`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `_prisma_artigo_generico_get_arg`
- `cfg_path.exists`
- `cfg_path.resolve`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `--config`
- `--prisma-curadoria-out-dir`
- `--prisma-out-dir`

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:prisma_artigo_generico_out_dir_with_runtime`

**Assinatura:**
```python
def prisma_artigo_generico_out_dir_with_runtime(runtime, /, argv)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 496–497
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `prisma_artigo_generico_out_dir_with_runtime` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `argv` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_invoke_with_runtime(_ap003e_body__prisma_artigo_generico_out_dir_1, runtime, (argv,), {})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_invoke_with_runtime`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_prisma_artigo_generico_out_dir_impl_001`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_prisma_artigo_generico_out_dir_impl_001`

**Assinatura:**
```python
def _prisma_artigo_generico_out_dir_impl_001(runtime, /, argv)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 499–500
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_artigo_generico_out_dir_impl_001` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `argv` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `prisma_artigo_generico_out_dir_with_runtime(runtime, argv)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `prisma_artigo_generico_out_dir_with_runtime`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_artigo_generico_run_export_1`

**Assinatura:**
```python
def _ap003e_body__prisma_artigo_generico_run_export_1(argv, silent)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 502–518
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_ap003e_body__prisma_artigo_generico_run_export_1` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |
| `silent` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `silent` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `proc.returncode`.

#### Efeitos colaterais e fronteiras externas

- Executa processo ou ferramenta externa.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `SystemExit`
- `_prisma_artigo_generico_get_arg`
- `_prisma_artigo_generico_out_dir`
- `out_dir.name.startswith`
- `print`
- `str`
- `subprocess.run`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `SystemExit(proc.returncode)`

#### Opções CLI mencionadas diretamente no corpo

- `--input`
- `--out-dir`
- `--output`
- `--prefix`
- `--prisma-bib-input`
- `--prisma-bib-output`

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:prisma_artigo_generico_run_export_with_runtime`

**Assinatura:**
```python
def prisma_artigo_generico_run_export_with_runtime(runtime, /, argv, silent)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 520–521
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `prisma_artigo_generico_run_export_with_runtime` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `argv` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |
| `silent` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `silent` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_invoke_with_runtime(_ap003e_body__prisma_artigo_generico_run_export_1, runtime, (argv, silent), {})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_invoke_with_runtime`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_prisma_artigo_generico_run_export_impl_001`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_prisma_artigo_generico_run_export_impl_001`

**Assinatura:**
```python
def _prisma_artigo_generico_run_export_impl_001(runtime, /, argv, silent)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 523–524
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_artigo_generico_run_export_impl_001` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `argv` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |
| `silent` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `silent` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `prisma_artigo_generico_run_export_with_runtime(runtime, argv, silent)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `prisma_artigo_generico_run_export_with_runtime`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_ap003e_body__prisma_artigo_generico_run_freeze_1`

**Assinatura:**
```python
def _ap003e_body__prisma_artigo_generico_run_freeze_1(argv, silent)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 526–547
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_ap003e_body__prisma_artigo_generico_run_freeze_1` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |
| `silent` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `silent` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `proc.returncode`.

#### Efeitos colaterais e fronteiras externas

- Executa processo ou ferramenta externa.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `SystemExit`
- `_prisma_artigo_generico_get_arg`
- `_prisma_artigo_generico_out_dir`
- `cmd.append`
- `out_dir.name.startswith`
- `print`
- `str`
- `subprocess.run`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `SystemExit(proc.returncode)`

#### Opções CLI mencionadas diretamente no corpo

- `--artigo-dir`
- `--artigo-prefix`
- `--autor`
- `--config`
- `--csl-path`
- `--dados-pesquisa-path`
- `--dest-dir`
- `--gerar-artigo-final`
- `--gerar-toml-artigo`
- `--openai-model`
- `--out-dir`
- `--prefix`
- `--prisma-artigo-dir`
- `--prisma-artigo-prefix`
- `--prisma-artigo-toml-output`
- `--prisma-autor-artigo`
- `--prisma-config`
- `--prisma-congelamento-dir`
- `--prisma-csl-path`
- `--prisma-dados-pesquisa-path`
- `--prisma-gerar-artigo-final`
- `--prisma-gerar-toml-artigo`
- `--prisma-openai-model-artigo`
- `--prisma-professor-artigo`
- `--professor`
- `--toml-output`

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 7
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:prisma_artigo_generico_run_freeze_with_runtime`

**Assinatura:**
```python
def prisma_artigo_generico_run_freeze_with_runtime(runtime, /, argv, silent)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 549–550
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `prisma_artigo_generico_run_freeze_with_runtime` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `argv` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |
| `silent` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `silent` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_invoke_with_runtime(_ap003e_body__prisma_artigo_generico_run_freeze_1, runtime, (argv, silent), {})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_invoke_with_runtime`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_prisma_artigo_generico_run_freeze_impl_001`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_prisma_artigo_generico_run_freeze_impl_001`

**Assinatura:**
```python
def _prisma_artigo_generico_run_freeze_impl_001(runtime, /, argv, silent)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 552–553
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_artigo_generico_run_freeze_impl_001` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `argv` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |
| `silent` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `silent` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `prisma_artigo_generico_run_freeze_with_runtime(runtime, argv, silent)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `prisma_artigo_generico_run_freeze_with_runtime`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:run_prisma_stage_001`

**Assinatura:**
```python
def run_prisma_stage_001(args, runtime)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 555–564
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `prisma_stage_001` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `runtime` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `PrismaStageResult(True, _prisma_curadoria_dispatch(args), {})`; `PrismaStageResult(False, None, _ap003e_values)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `PrismaStageResult`
- `_prisma_curadoria_dispatch`
- `getattr`
- `locals`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:run_prisma_stage_002`

**Assinatura:**
```python
def run_prisma_stage_002(args, runtime)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 566–583
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `prisma_stage_002` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `runtime` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `PrismaStageResult(False, None, _ap003e_values)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `PrismaStageResult`
- `external_search_enabled`
- `locals`
- `output_paths`
- `research_output_paths`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 7
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:run_prisma_stage_003`

**Assinatura:**
```python
def run_prisma_stage_003(args, runtime)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 585–593
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `prisma_stage_003` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `runtime` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `PrismaStageResult(False, None, _ap003e_values)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `PrismaStageResult`
- `locals`
- `stage`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:run_prisma_stage_004`

**Assinatura:**
```python
def run_prisma_stage_004(args, runtime)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 595–696
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `prisma_stage_004` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `runtime` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `PrismaStageResult(True, 0, {})`; `PrismaStageResult(False, None, _ap003e_values)`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `PrismaStageResult`
- `RuntimeError`
- `artifacts.get`
- `bool`
- `cfg.get`
- `isinstance`
- `locals`
- `make_client`
- `make_run_report`
- `print_outputs`
- `prisma_outputs.setdefault`
- `render_external_prisma_outputs`
- `run_external_prisma_search`
- `search_cfg.get`
- `stage`
- `str`
- `write_json`
- `write_outputs_manifest`
- `write_prompt_lock`
- `write_prompt_lock_markdown`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('O perfil de busca PRISMA não produz document.json; use a geração normal ou --prisma-importar-triagem.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `O perfil de busca PRISMA não produz document.json; use a geração normal ou --prisma-importar-triagem.`
- `.prompt_lock.json`
- `.prompt_lock.md`
- `.run_report.json`
- `.outputs.txt`

#### Complexidade estrutural observada

```yaml
branches: 45
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:run_prisma_stage_005`

**Assinatura:**
```python
def run_prisma_stage_005(args, runtime)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 698–704
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `prisma_stage_005` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `runtime` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `PrismaStageResult(False, None, _ap003e_values)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `PrismaStageResult`
- `locals`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:run_prisma_stage_006`

**Assinatura:**
```python
def run_prisma_stage_006(args, runtime)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 706–736
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `prisma_stage_006` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `runtime` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `PrismaStageResult(False, None, _ap003e_values)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `PrismaStageResult`
- `_json_or_none`
- `getattr`
- `locals`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 17
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:run_prisma_stage_007`

**Assinatura:**
```python
def run_prisma_stage_007(args, runtime)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 738–746
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `prisma_stage_007` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `runtime` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `PrismaStageResult(False, None, _ap003e_values)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `PrismaStageResult`
- `locals`
- `stage`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:run_prisma_stage_008`

**Assinatura:**
```python
def run_prisma_stage_008(args, runtime)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 748–756
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `prisma_stage_008` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `runtime` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `PrismaStageResult(False, None, _ap003e_values)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `PrismaStageResult`
- `locals`
- `stage`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:_ap003e_body_main_2`

**Assinatura:**
```python
def _ap003e_body_main_2(*args, **kwargs)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 758–784
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_ap003e_body_main_2` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | var-positional | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `kwargs` | var-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `kwargs` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_prisma_artigo_generico_run_export(original_argv, silent=False)`; `_prisma_artigo_generico_run_freeze(original_argv, silent=False)`; `rc`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_ap003f_pipeline_core`
- `_prisma_artigo_generico_run_export`
- `_prisma_artigo_generico_run_freeze`
- `_prisma_artigo_generico_strip`
- `list`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `--prisma-congelar-artigo`
- `--prisma-curadoria-fluxo-completo`
- `--prisma-curadoria-importar`
- `--prisma-exportar-bib`
- `--prisma-gerar-artigo-final`
- `--prisma-gerar-toml-artigo`

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:run_prisma_generic_with_runtime`

**Assinatura:**
```python
def run_prisma_generic_with_runtime(runtime, /, *args, **kwargs)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 786–787
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `prisma_generic_with_runtime` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `args` | var-positional | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `kwargs` | var-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `kwargs` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `_invoke_with_runtime(_ap003e_body_main_2, runtime, (*args,), {**kwargs})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_invoke_with_runtime`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:run_prisma_generic_entrypoint`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.prisma_generic_orchestration:run_prisma_generic_entrypoint`

**Assinatura:**
```python
def run_prisma_generic_entrypoint(runtime, /, *args, **kwargs)
```

**Arquivo:** `academic_pipeline/prisma_generic_orchestration.py`
**Linhas:** 789–790
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `prisma_generic_entrypoint` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `runtime` | positional-only | `não anotado` | `obrigatório/ausente` | Parâmetro `runtime` recebido pela função; não há descrição individual explícita no código. |
| `args` | var-positional | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `kwargs` | var-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `kwargs` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `run_prisma_generic_with_runtime(runtime, *args, **kwargs)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `run_prisma_generic_with_runtime`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.default_runtime:run_default`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `46096b64f84bc33a08232a2326d483b8d40abf73cba405e319f92daa6b887bc4`
**LOC do arquivo-fonte:** 790


### `academic_pipeline.repository_paths:_is_project_root`

**Assinatura:**
```python
def _is_project_root(candidate: Path) -> bool
```

**Arquivo:** `academic_pipeline/repository_paths.py`
**Linhas:** 14–20
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Verifica se `project_root` conforme as regras codificadas na rotina.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `candidate` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `candidate` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `candidate.is_dir() and (candidate / 'pyproject.toml').is_file() and (candidate / 'academic_pipeline').is_dir() and (candidate / 'app_bundle').is_dir()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `(candidate / 'academic_pipeline').is_dir`
- `(candidate / 'app_bundle').is_dir`
- `(candidate / 'pyproject.toml').is_file`
- `candidate.is_dir`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.repository_paths:repository_project_root`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `pyproject.toml`

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f762fec50168d8a56ea53e1d58a35bfe293d4175f2ae42b9c15b26c04ee3278f`
**LOC do arquivo-fonte:** 74


### `academic_pipeline.repository_paths:_candidate_chain`

**Assinatura:**
```python
def _candidate_chain(start: Path) -> Iterable[Path]
```

**Arquivo:** `academic_pipeline/repository_paths.py`
**Linhas:** 23–26
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_candidate_chain` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `start` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `start` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Iterable[Path]`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `start.is_dir`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.repository_paths:repository_project_root`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f762fec50168d8a56ea53e1d58a35bfe293d4175f2ae42b9c15b26c04ee3278f`
**LOC do arquivo-fonte:** 74


### `academic_pipeline.repository_paths:repository_project_root`

**Assinatura:**
```python
def repository_project_root(start: str | os.PathLike[str] | None = None) -> Path
```

**Arquivo:** `academic_pipeline/repository_paths.py`
**Linhas:** 29–53
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `repository_project_root` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `start` | positional-or-keyword | `str \| os.PathLike[str] \| None` | `None` | Parâmetro `start` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `candidate`.

#### Efeitos colaterais e fronteiras externas

- Lê configuração/credenciais do ambiente do processo.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(override).expanduser`
- `Path(override).expanduser().resolve`
- `Path.cwd`
- `RepositoryRootError`
- `_candidate_chain`
- `_is_project_root`
- `anchor.expanduser`
- `anchor.is_absolute`
- `anchor.resolve`
- `os.environ.get`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.repository_paths:repository_resource`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RepositoryRootError(f'{PROJECT_ROOT_ENV} does not point to a valid project root: {candidate}')`
- `RepositoryRootError(f'Could not resolve an Academic Pipeline repository project root. Set {PROJECT_ROOT_ENV} explicitly for repository-backed resources.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f762fec50168d8a56ea53e1d58a35bfe293d4175f2ae42b9c15b26c04ee3278f`
**LOC do arquivo-fonte:** 74


### `academic_pipeline.repository_paths:repository_resource`

**Assinatura:**
```python
def repository_resource(*parts: str, start: str | os.PathLike[str] | None = None, must_exist: bool = True) -> Path
```

**Arquivo:** `academic_pipeline/repository_paths.py`
**Linhas:** 56–74
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `repository_resource` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `parts` | var-positional | `str` | `obrigatório/ausente` | Parâmetro `parts` recebido pela função; não há descrição individual explícita no código. |
| `start` | keyword-only | `str \| os.PathLike[str] \| None` | `None` | Parâmetro `start` recebido pela função; não há descrição individual explícita no código. |
| `must_exist` | keyword-only | `bool` | `True` | Parâmetro `must_exist` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `candidate`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `FileNotFoundError`
- `RepositoryRootError`
- `candidate.exists`
- `candidate.relative_to`
- `repository_project_root`
- `root.joinpath`
- `root.joinpath(*parts).resolve`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RepositoryRootError(f'Repository resource escapes the project root: {candidate}')`
- `FileNotFoundError(candidate)`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `f762fec50168d8a56ea53e1d58a35bfe293d4175f2ae42b9c15b26c04ee3278f`
**LOC do arquivo-fonte:** 74


### `academic_pipeline.runtime:RuntimeContext.as_dispatch_mapping`

**Assinatura:**
```python
def as_dispatch_mapping(self) -> Mapping[str, Any]
```

**Arquivo:** `academic_pipeline/runtime.py`
**Linhas:** 177–189
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `RuntimeContext`
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Adapta campos tipados ao contrato histórico dos dispatchers.

**Docstring resumida:** Adapta campos tipados ao contrato histórico dos dispatchers.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Mapping[str, Any]`. Retornos observados estaticamente incluem: `MappingProxyType({'Path': self.path_type, 'available_layouts': self.available_layouts, 'describe_institution_profiles': self.describe_institution_profiles, 'explain_profile': self.explain_profile, 'load_config': self.load_config, 'resolve_layout_spec': self.resolve_layout_spec})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `MappingProxyType`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.runtime:_run_native_first_wave`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3e2192ea4522be375e0a2ea2fb22a0654fc4107ec581712372ccae3068dfb8f9`
**LOC do arquivo-fonte:** 645


### `academic_pipeline.runtime:_load_config`

**Assinatura:**
```python
def _load_config(path: Path) -> dict[str, Any]
```

**Arquivo:** `academic_pipeline/runtime.py`
**Linhas:** 196–218
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Carrega a responsabilidade representada por `config` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `profiled`.

#### Efeitos colaterais e fronteiras externas

- Abre arquivo para leitura ou inspeção.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `NativeRuntimeError`
- `Path`
- `Path(path).expanduser`
- `Path(path).expanduser().resolve`
- `apply_institution_profile`
- `config_path.open`
- `isinstance`
- `str`
- `tomllib.load`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `NativeRuntimeError(f'Configuração TOML inválida: {config_path}')`
- `NativeRuntimeError('apply_institution_profile retornou configuração inválida')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3e2192ea4522be375e0a2ea2fb22a0654fc4107ec581712372ccae3068dfb8f9`
**LOC do arquivo-fonte:** 645


### `academic_pipeline.runtime:default_runtime_context`

**Assinatura:**
```python
def default_runtime_context() -> RuntimeContext
```

**Arquivo:** `academic_pipeline/runtime.py`
**Linhas:** 221–242
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Carrega dependências por imports canônicos, sem ponte para o legado.

**Docstring resumida:** Carrega dependências por imports canônicos, sem ponte para o legado.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `RuntimeContext`. Retornos observados estaticamente incluem: `RuntimeContext(path_type=Path, load_config=_load_config, describe_institution_profiles=describe_institution_profiles, available_layouts=available_layouts, resolve_layout_spec=resolve_layout_spec, explain_profile=explain_profile)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `RuntimeContext`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.doctor_runtime:default_doctor_runtime_context`
- `academic_pipeline.runtime:_run_native_first_wave`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3e2192ea4522be375e0a2ea2fb22a0654fc4107ec581712372ccae3068dfb8f9`
**LOC do arquivo-fonte:** 645


### `academic_pipeline.runtime:_normalize_argv`

**Assinatura:**
```python
def _normalize_argv(argv: Sequence[str] | None) -> list[str]
```

**Arquivo:** `academic_pipeline/runtime.py`
**Linhas:** 245–247
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Normaliza a responsabilidade representada por `argv` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `Sequence[str] \| None` | `obrigatório/ausente` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `[str(item) for item in source]`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3e2192ea4522be375e0a2ea2fb22a0654fc4107ec581712372ccae3068dfb8f9`
**LOC do arquivo-fonte:** 645


### `academic_pipeline.runtime:_matches_option`

**Assinatura:**
```python
def _matches_option(token: str, option: str) -> bool
```

**Arquivo:** `academic_pipeline/runtime.py`
**Linhas:** 250–251
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_matches_option` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `token` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `token` recebido pela função; não há descrição individual explícita no código. |
| `option` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `option` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `token == option or token.startswith(f'{option}=')`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `token.startswith`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.runtime:_has_prisma_generic_wrapper_trigger`
- `academic_pipeline.runtime:select_runtime_route`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3e2192ea4522be375e0a2ea2fb22a0654fc4107ec581712372ccae3068dfb8f9`
**LOC do arquivo-fonte:** 645


### `academic_pipeline.runtime:_namespace_has_preceding_trigger`

**Assinatura:**
```python
def _namespace_has_preceding_trigger(argv: Sequence[str]) -> bool
```

**Arquivo:** `academic_pipeline/runtime.py`
**Linhas:** 254–261
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_namespace_has_preceding_trigger` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `Sequence[str]` | `obrigatório/ausente` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `any((bool(getattr(args, dest, None)) for dest in DOCTOR_PRECEDING_TRIGGER_DESTS))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_build_parser`
- `_build_parser().parse_args`
- `any`
- `bool`
- `getattr`
- `list`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.runtime:select_runtime_route`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3e2192ea4522be375e0a2ea2fb22a0654fc4107ec581712372ccae3068dfb8f9`
**LOC do arquivo-fonte:** 645


### `academic_pipeline.runtime:_namespace_has_check_config_preceding_trigger`

**Assinatura:**
```python
def _namespace_has_check_config_preceding_trigger(argv: Sequence[str]) -> bool
```

**Arquivo:** `academic_pipeline/runtime.py`
**Linhas:** 264–271
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_namespace_has_check_config_preceding_trigger` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `Sequence[str]` | `obrigatório/ausente` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `any((bool(getattr(args, dest, None)) for dest in CHECK_CONFIG_PRECEDING_TRIGGER_DESTS))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_build_parser`
- `_build_parser().parse_args`
- `any`
- `bool`
- `getattr`
- `list`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.runtime:select_runtime_route`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3e2192ea4522be375e0a2ea2fb22a0654fc4107ec581712372ccae3068dfb8f9`
**LOC do arquivo-fonte:** 645


### `academic_pipeline.runtime:_is_exact_list_profiles_invocation`

**Assinatura:**
```python
def _is_exact_list_profiles_invocation(argv: Sequence[str]) -> bool
```

**Arquivo:** `academic_pipeline/runtime.py`
**Linhas:** 274–280
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Verifica se `exact_list_profiles_invocation` conforme as regras codificadas na rotina.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `Sequence[str]` | `obrigatório/ausente` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `bool(argv) and all((str(token) in LIST_PROFILES_OPTIONS for token in argv))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `all`
- `bool`
- `str`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.runtime:select_runtime_route`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3e2192ea4522be375e0a2ea2fb22a0654fc4107ec581712372ccae3068dfb8f9`
**LOC do arquivo-fonte:** 645


### `academic_pipeline.runtime:_is_exact_institution_compliance_invocation`

**Assinatura:**
```python
def _is_exact_institution_compliance_invocation(argv: Sequence[str]) -> bool
```

**Arquivo:** `academic_pipeline/runtime.py`
**Linhas:** 283–312
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Verifica se `exact_institution_compliance_invocation` conforme as regras codificadas na rotina.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `Sequence[str]` | `obrigatório/ausente` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `False`; `seen_command`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `len`
- `str`
- `token.startswith`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.runtime:select_runtime_route`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3e2192ea4522be375e0a2ea2fb22a0654fc4107ec581712372ccae3068dfb8f9`
**LOC do arquivo-fonte:** 645


### `academic_pipeline.runtime:_is_exact_doi_manifest_invocation`

**Assinatura:**
```python
def _is_exact_doi_manifest_invocation(argv: Sequence[str]) -> bool
```

**Arquivo:** `academic_pipeline/runtime.py`
**Linhas:** 315–346
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Verifica se `exact_doi_manifest_invocation` conforme as regras codificadas na rotina.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `Sequence[str]` | `obrigatório/ausente` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `False`; `seen_command`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `len`
- `str`
- `token.startswith`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 7
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3e2192ea4522be375e0a2ea2fb22a0654fc4107ec581712372ccae3068dfb8f9`
**LOC do arquivo-fonte:** 645


### `academic_pipeline.runtime:_has_prisma_generic_wrapper_trigger`

**Assinatura:**
```python
def _has_prisma_generic_wrapper_trigger(argv: Sequence[str]) -> bool
```

**Arquivo:** `academic_pipeline/runtime.py`
**Linhas:** 349–354
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Verifica a presença/condição de `prisma_generic_wrapper_trigger` conforme as regras codificadas na rotina.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `Sequence[str]` | `obrigatório/ausente` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `any((_matches_option(token, option) for token in argv for option in PRISMA_GENERIC_WRAPPER_OPTIONS))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_matches_option`
- `any`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.runtime:_run_native_default`
- `academic_pipeline.runtime:select_runtime_route`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3e2192ea4522be375e0a2ea2fb22a0654fc4107ec581712372ccae3068dfb8f9`
**LOC do arquivo-fonte:** 645


### `academic_pipeline.runtime:select_runtime_route`

**Assinatura:**
```python
def select_runtime_route(argv: Sequence[str]) -> RuntimeRoute
```

**Arquivo:** `academic_pipeline/runtime.py`
**Linhas:** 357–418
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Seleciona a responsabilidade representada por `runtime_route` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `Sequence[str]` | `obrigatório/ausente` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |

#### Retorno

Anotação declarada: `RuntimeRoute`. Retornos observados estaticamente incluem: `RuntimeRoute.NATIVE_FIRST_WAVE`; `RuntimeRoute.NATIVE_INSTITUTION_COMPLIANCE`; `RuntimeRoute.INSTITUTION_COMPLIANCE_ERROR`; `RuntimeRoute.NATIVE_DOI_MANIFEST`; `RuntimeRoute.DOCTOR_COMBINATION_ERROR`; `RuntimeRoute.NATIVE_DOCTOR`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_build_parser`
- `_build_parser().parse_args`
- `_has_prisma_generic_wrapper_trigger`
- `_is_exact_institution_compliance_invocation`
- `_is_exact_list_profiles_invocation`
- `_matches_option`
- `_namespace_has_check_config_preceding_trigger`
- `_namespace_has_preceding_trigger`
- `any`
- `list`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.runtime:run`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 11
loops: 1
comprehensions: 6
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3e2192ea4522be375e0a2ea2fb22a0654fc4107ec581712372ccae3068dfb8f9`
**LOC do arquivo-fonte:** 645


### `academic_pipeline.runtime:_build_parser`

**Assinatura:**
```python
def _build_parser() -> argparse.ArgumentParser
```

**Arquivo:** `academic_pipeline/runtime.py`
**Linhas:** 422–435
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Constrói a responsabilidade representada por `parser` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `argparse.ArgumentParser`. Retornos observados estaticamente incluem: `parser`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `NativeRuntimeError`
- `cli_parser.build_parser`
- `isinstance`
- `parser.add_argument`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `NativeRuntimeError('build_parser não retornou ArgumentParser')`

#### Opções CLI mencionadas diretamente no corpo

- `--list-profiles`

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3e2192ea4522be375e0a2ea2fb22a0654fc4107ec581712372ccae3068dfb8f9`
**LOC do arquivo-fonte:** 645


### `academic_pipeline.runtime:_normalize_dispatch_code`

**Assinatura:**
```python
def _normalize_dispatch_code(value: Any) -> int
```

**Arquivo:** `academic_pipeline/runtime.py`
**Linhas:** 438–447
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Normaliza a responsabilidade representada por `dispatch_code` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `0`; `int(value)`; `value`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `NativeRuntimeError`
- `int`
- `isinstance`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.runtime:_run_native_first_wave`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `NativeRuntimeError(f'Dispatcher retornou código inválido: {value!r}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3e2192ea4522be375e0a2ea2fb22a0654fc4107ec581712372ccae3068dfb8f9`
**LOC do arquivo-fonte:** 645


### `academic_pipeline.runtime:_run_native_first_wave`

**Assinatura:**
```python
def _run_native_first_wave(argv: Sequence[str], *, context: RuntimeContext | None = None) -> int
```

**Arquivo:** `academic_pipeline/runtime.py`
**Linhas:** 450–474
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `native_first_wave` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `Sequence[str]` | `obrigatório/ausente` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |
| `context` | keyword-only | `RuntimeContext \| None` | `None` | Contexto explícito de dependências utilizado para desacoplar a rotina de imports/globais. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `_normalize_dispatch_code(getattr(result, 'value'))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `(context or default_runtime_context()).as_dispatch_mapping`
- `NativeRuntimeError`
- `_build_parser`
- `_normalize_dispatch_code`
- `bool`
- `default_runtime_context`
- `dispatch`
- `getattr`
- `list`
- `parser.parse_args`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.runtime:run`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `NativeRuntimeError('Uma opção da primeira onda foi selecionada, mas nenhum dispatcher a reconheceu.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3e2192ea4522be375e0a2ea2fb22a0654fc4107ec581712372ccae3068dfb8f9`
**LOC do arquivo-fonte:** 645


### `academic_pipeline.runtime:_run_native_doctor`

**Assinatura:**
```python
def _run_native_doctor(argv: Sequence[str]) -> int
```

**Arquivo:** `academic_pipeline/runtime.py`
**Linhas:** 477–480
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `native_doctor` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `Sequence[str]` | `obrigatório/ausente` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `int(run_doctor_command(argv))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `int`
- `run_doctor_command`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.runtime:run`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3e2192ea4522be375e0a2ea2fb22a0654fc4107ec581712372ccae3068dfb8f9`
**LOC do arquivo-fonte:** 645


### `academic_pipeline.runtime:_run_native_check_config`

**Assinatura:**
```python
def _run_native_check_config(argv: Sequence[str]) -> int
```

**Arquivo:** `academic_pipeline/runtime.py`
**Linhas:** 483–490
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `native_check_config` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `Sequence[str]` | `obrigatório/ausente` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `int(run_check_config_command(argv))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `int`
- `run_check_config_command`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.runtime:run`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3e2192ea4522be375e0a2ea2fb22a0654fc4107ec581712372ccae3068dfb8f9`
**LOC do arquivo-fonte:** 645


### `academic_pipeline.runtime:_run_check_config_combination_error`

**Assinatura:**
```python
def _run_check_config_combination_error() -> int
```

**Arquivo:** `academic_pipeline/runtime.py`
**Linhas:** 493–497
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `check_config_combination_error` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `1`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `print`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.runtime:run`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3e2192ea4522be375e0a2ea2fb22a0654fc4107ec581712372ccae3068dfb8f9`
**LOC do arquivo-fonte:** 645


### `academic_pipeline.runtime:_run_list_profiles_combination_error`

**Assinatura:**
```python
def _run_list_profiles_combination_error() -> int
```

**Arquivo:** `academic_pipeline/runtime.py`
**Linhas:** 500–504
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `list_profiles_combination_error` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `1`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `print`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.runtime:run`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3e2192ea4522be375e0a2ea2fb22a0654fc4107ec581712372ccae3068dfb8f9`
**LOC do arquivo-fonte:** 645


### `academic_pipeline.runtime:_run_native_list_profiles`

**Assinatura:**
```python
def _run_native_list_profiles(argv: Sequence[str]) -> int
```

**Arquivo:** `academic_pipeline/runtime.py`
**Linhas:** 507–514
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `native_list_profiles` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `Sequence[str]` | `obrigatório/ausente` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `int(run_list_profiles_command(argv))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `int`
- `run_list_profiles_command`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.runtime:_run_native_default`
- `academic_pipeline.runtime:run`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3e2192ea4522be375e0a2ea2fb22a0654fc4107ec581712372ccae3068dfb8f9`
**LOC do arquivo-fonte:** 645


### `academic_pipeline.runtime:_run_native_institution_compliance`

**Assinatura:**
```python
def _run_native_institution_compliance(argv: Sequence[str]) -> int
```

**Arquivo:** `academic_pipeline/runtime.py`
**Linhas:** 517–531
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `native_institution_compliance` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `Sequence[str]` | `obrigatório/ausente` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `int(run_institution_compliance_command(argv))`; `1`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `int`
- `print`
- `run_institution_compliance_command`
- `str`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.runtime:run`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `3e2192ea4522be375e0a2ea2fb22a0654fc4107ec581712372ccae3068dfb8f9`
**LOC do arquivo-fonte:** 645


### `academic_pipeline.runtime:_run_native_doi_manifest`

**Assinatura:**
```python
def _run_native_doi_manifest(argv: Sequence[str]) -> int
```

**Arquivo:** `academic_pipeline/runtime.py`
**Linhas:** 534–548
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `native_doi_manifest` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `Sequence[str]` | `obrigatório/ausente` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `int(run_make_doi_manifest_command(argv))`; `1`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `int`
- `print`
- `run_make_doi_manifest_command`
- `str`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.runtime:run`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `3e2192ea4522be375e0a2ea2fb22a0654fc4107ec581712372ccae3068dfb8f9`
**LOC do arquivo-fonte:** 645


### `academic_pipeline.runtime:_run_native_default`

**Assinatura:**
```python
def _run_native_default(argv: Sequence[str]) -> int
```

**Arquivo:** `academic_pipeline/runtime.py`
**Linhas:** 551–559
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `native_default` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `Sequence[str]` | `obrigatório/ausente` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `_run_native_list_profiles(['--list-profiles'])`; `int(run_default(argv))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_build_parser`
- `_build_parser().parse_args`
- `_has_prisma_generic_wrapper_trigger`
- `_run_native_list_profiles`
- `bool`
- `getattr`
- `int`
- `list`
- `run_default`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.runtime:run`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `--list-profiles`

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3e2192ea4522be375e0a2ea2fb22a0654fc4107ec581712372ccae3068dfb8f9`
**LOC do arquivo-fonte:** 645


### `academic_pipeline.runtime:_run_doctor_combination_error`

**Assinatura:**
```python
def _run_doctor_combination_error() -> int
```

**Arquivo:** `academic_pipeline/runtime.py`
**Linhas:** 562–566
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `doctor_combination_error` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `1`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `print`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.runtime:run`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3e2192ea4522be375e0a2ea2fb22a0654fc4107ec581712372ccae3068dfb8f9`
**LOC do arquivo-fonte:** 645


### `academic_pipeline.runtime:_run_institution_compliance_error`

**Assinatura:**
```python
def _run_institution_compliance_error() -> int
```

**Arquivo:** `academic_pipeline/runtime.py`
**Linhas:** 569–573
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `institution_compliance_error` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `1`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `print`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.runtime:run`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3e2192ea4522be375e0a2ea2fb22a0654fc4107ec581712372ccae3068dfb8f9`
**LOC do arquivo-fonte:** 645


### `academic_pipeline.runtime:run`

**Assinatura:**
```python
def run(argv: Sequence[str] | None = None, *, context: RuntimeContext | None = None) -> int
```

**Arquivo:** `academic_pipeline/runtime.py`
**Linhas:** 576–619
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Executa todas as rotas por superfícies canônicas nativas.

**Docstring resumida:** Executa todas as rotas por superfícies canônicas nativas.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `Sequence[str] \| None` | `None` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |
| `context` | keyword-only | `RuntimeContext \| None` | `None` | Contexto explícito de dependências utilizado para desacoplar a rotina de imports/globais. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `_run_native_first_wave(forwarded, context=context)`; `_run_native_doctor(forwarded)`; `_run_doctor_combination_error()`; `_run_native_check_config(forwarded)`; `_run_check_config_combination_error()`; `_run_native_list_profiles(forwarded)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `NativeRuntimeError`
- `_normalize_argv`
- `_run_check_config_combination_error`
- `_run_doctor_combination_error`
- `_run_institution_compliance_error`
- `_run_list_profiles_combination_error`
- `_run_native_check_config`
- `_run_native_default`
- `_run_native_doctor`
- `_run_native_doi_manifest`
- `_run_native_first_wave`
- `_run_native_institution_compliance`
- `_run_native_list_profiles`
- `select_runtime_route`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `NativeRuntimeError(f'Rota não tratada: {route!r}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 11
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3e2192ea4522be375e0a2ea2fb22a0654fc4107ec581712372ccae3068dfb8f9`
**LOC do arquivo-fonte:** 645


### `app_bundle.scripts.pipeline.academic_pipeline_gui:_safe_resolve`

**Assinatura:**
```python
def _safe_resolve(path: str | Path) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 97–98
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_safe_resolve` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `str \| Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `Path(path).expanduser().resolve()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(path).expanduser`
- `Path(path).expanduser().resolve`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:_existing_path`

**Assinatura:**
```python
def _existing_path(raw: str) -> Path | None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 101–109
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_existing_path` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `raw` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `raw` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path | None`. Retornos observados estaticamente incluem: `None`; `path.resolve() if path.exists() else None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `(raw or '').strip`
- `Path`
- `Path(value).expanduser`
- `path.exists`
- `path.resolve`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._choose_dir`
- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._choose_file`
- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._validate_step`
- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI.output_dir`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:_is_zip_path`

**Assinatura:**
```python
def _is_zip_path(raw: str) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 112–113
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Verifica se `zip_path` conforme as regras codificadas na rotina.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `raw` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `raw` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `(raw or '').strip().lower().endswith('.zip')`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `(raw or '').strip`
- `(raw or '').strip().lower`
- `(raw or '').strip().lower().endswith`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:_path_display`

**Assinatura:**
```python
def _path_display(path: Path) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 116–121
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_path_display` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'~' + str(path.resolve()).removeprefix(str(home)) if str(path.resolve()).startswith(str(home)) else str(path)`; `str(path)`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `Path.home`
- `Path.home().resolve`
- `path.resolve`
- `str`
- `str(path.resolve()).removeprefix`
- `str(path.resolve()).startswith`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:path_suggestions`

**Assinatura:**
```python
def path_suggestions(raw: str, *, limit: int = 12) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 124–161
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Retorna sugestões de caminho sem realizar varredura custosa no disco.

**Docstring resumida:** Retorna sugestões de caminho sem realizar varredura custosa no disco.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `raw` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `raw` recebido pela função; não há descrição individual explícita no código. |
| `limit` | keyword-only | `int` | `12` | Parâmetro `limit` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `[str(p) + (os.sep if p.is_dir() else '') for p in candidates if p.exists()]`; `[]`; `results`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `(raw or '').strip`
- `Path`
- `Path.home`
- `candidate.removeprefix`
- `expanded.endswith`
- `item.is_dir`
- `item.name.lower`
- `item.name.lower().startswith`
- `len`
- `os.path.basename`
- `os.path.dirname`
- `os.path.expanduser`
- `p.exists`
- `p.is_dir`
- `p.name.lower`
- `parent.exists`
- `parent.is_dir`
- `parent.iterdir`
- `prefix.lower`
- `results.append`
- `sorted`
- `str`
- `value.startswith`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AutocompletePathEntry.show_suggestions`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 8
loops: 1
comprehensions: 1
exception_handlers: 2
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AutocompletePathEntry.__init__`

**Assinatura:**
```python
def __init__(self, master: tk.Misc, variable: tk.StringVar, **kwargs: Any) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 167–178
**Tipo:** method
**Visibilidade:** especial/dunder
**Classe proprietária:** `AutocompletePathEntry`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `__init__` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `master` | positional-or-keyword | `tk.Misc` | `obrigatório/ausente` | Parâmetro `master` recebido pela função; não há descrição individual explícita no código. |
| `variable` | positional-or-keyword | `tk.StringVar` | `obrigatório/ausente` | Parâmetro `variable` recebido pela função; não há descrição individual explícita no código. |
| `kwargs` | var-keyword | `Any` | `obrigatório/ausente` | Parâmetro `kwargs` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `self.bind`
- `super`
- `super().__init__`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AutocompletePathEntry._on_key_release`

**Assinatura:**
```python
def _on_key_release(self, event: tk.Event) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 180–185
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AutocompletePathEntry`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_on_key_release` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `event` | positional-or-keyword | `tk.Event` | `obrigatório/ausente` | Parâmetro `event` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `self.after`
- `self.after_cancel`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AutocompletePathEntry._on_focus_out`

**Assinatura:**
```python
def _on_focus_out(self, _event: tk.Event) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 187–188
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AutocompletePathEntry`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_on_focus_out` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `_event` | positional-or-keyword | `tk.Event` | `obrigatório/ausente` | Parâmetro `_event` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `self.after`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AutocompletePathEntry._on_tab`

**Assinatura:**
```python
def _on_tab(self, _event: tk.Event) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 190–195
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AutocompletePathEntry`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_on_tab` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `_event` | positional-or-keyword | `tk.Event` | `obrigatório/ausente` | Parâmetro `_event` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'break'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `self._choose`
- `self.show_suggestions`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AutocompletePathEntry._on_down`

**Assinatura:**
```python
def _on_down(self, _event: tk.Event) -> str | None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 197–204
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AutocompletePathEntry`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_on_down` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `_event` | positional-or-keyword | `tk.Event` | `obrigatório/ausente` | Parâmetro `_event` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str | None`. Retornos observados estaticamente incluem: `'break'`; `None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `self.listbox.activate`
- `self.listbox.focus_set`
- `self.listbox.selection_clear`
- `self.listbox.selection_set`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AutocompletePathEntry._on_escape`

**Assinatura:**
```python
def _on_escape(self, _event: tk.Event) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 206–208
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AutocompletePathEntry`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_on_escape` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `_event` | positional-or-keyword | `tk.Event` | `obrigatório/ausente` | Parâmetro `_event` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'break'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `self.hide_suggestions`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AutocompletePathEntry.show_suggestions`

**Assinatura:**
```python
def show_suggestions(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 210–251
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AutocompletePathEntry`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `show_suggestions` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `len`
- `max`
- `min`
- `path_suggestions`
- `self.after`
- `self.hide_suggestions`
- `self.listbox.bind`
- `self.listbox.configure`
- `self.listbox.delete`
- `self.listbox.insert`
- `self.listbox.pack`
- `self.popup.attributes`
- `self.popup.deiconify`
- `self.popup.geometry`
- `self.popup.overrideredirect`
- `self.popup.update_idletasks`
- `self.variable.get`
- `self.winfo_height`
- `self.winfo_rootx`
- `self.winfo_rooty`
- `self.winfo_width`
- `tk.Listbox`
- `tk.Toplevel`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AutocompletePathEntry._on_tab`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AutocompletePathEntry.hide_suggestions`

**Assinatura:**
```python
def hide_suggestions(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 253–255
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AutocompletePathEntry`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `hide_suggestions` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `self.popup.withdraw`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AutocompletePathEntry._choose`
- `app_bundle.scripts.pipeline.academic_pipeline_gui:AutocompletePathEntry._on_escape`
- `app_bundle.scripts.pipeline.academic_pipeline_gui:AutocompletePathEntry.show_suggestions`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AutocompletePathEntry._choose_selected`

**Assinatura:**
```python
def _choose_selected(self, _event: tk.Event | None = None) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 257–263
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AutocompletePathEntry`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_choose_selected` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `_event` | positional-or-keyword | `tk.Event \| None` | `None` | Parâmetro `_event` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'break'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `int`
- `self._choose`
- `self.listbox.curselection`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AutocompletePathEntry._choose`

**Assinatura:**
```python
def _choose(self, index: int) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 265–272
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AutocompletePathEntry`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_choose` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `index` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `index` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `len`
- `max`
- `min`
- `self.focus_set`
- `self.hide_suggestions`
- `self.icursor`
- `self.variable.set`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AutocompletePathEntry._choose_selected`
- `app_bundle.scripts.pipeline.academic_pipeline_gui:AutocompletePathEntry._on_tab`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:ScrollableFrame.__init__`

**Assinatura:**
```python
def __init__(self, master: tk.Misc, **kwargs: Any) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 278–293
**Tipo:** method
**Visibilidade:** especial/dunder
**Classe proprietária:** `ScrollableFrame`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `__init__` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `master` | positional-or-keyword | `tk.Misc` | `obrigatório/ausente` | Parâmetro `master` recebido pela função; não há descrição individual explícita no código. |
| `kwargs` | var-keyword | `Any` | `obrigatório/ausente` | Parâmetro `kwargs` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `self.canvas.bind`
- `self.canvas.bind_all`
- `self.canvas.configure`
- `self.canvas.create_window`
- `self.canvas.grid`
- `self.canvas.yview_scroll`
- `self.content.bind`
- `self.grid_columnconfigure`
- `self.grid_rowconfigure`
- `self.scrollbar.grid`
- `super`
- `super().__init__`
- `tk.Canvas`
- `ttk.Frame`
- `ttk.Scrollbar`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:ScrollableFrame._sync_scroll_region`

**Assinatura:**
```python
def _sync_scroll_region(self, _event: tk.Event | None = None) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 295–296
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `ScrollableFrame`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_sync_scroll_region` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `_event` | positional-or-keyword | `tk.Event \| None` | `None` | Parâmetro `_event` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `self.canvas.bbox`
- `self.canvas.configure`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:ScrollableFrame._sync_width`

**Assinatura:**
```python
def _sync_width(self, event: tk.Event) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 298–299
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `ScrollableFrame`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_sync_width` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `event` | positional-or-keyword | `tk.Event` | `obrigatório/ausente` | Parâmetro `event` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `self.canvas.itemconfigure`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:ScrollableFrame._on_mousewheel`

**Assinatura:**
```python
def _on_mousewheel(self, event: tk.Event) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 301–307
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `ScrollableFrame`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_on_mousewheel` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `event` | positional-or-keyword | `tk.Event` | `obrigatório/ausente` | Parâmetro `event` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `int`
- `self.canvas.yview_scroll`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:ScrollableFrame.to_top`

**Assinatura:**
```python
def to_top(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 309–310
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `ScrollableFrame`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `to_top` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `self.canvas.yview_moveto`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._render_step`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI.__init__`

**Assinatura:**
```python
def __init__(self, root: tk.Tk) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 316–333
**Tipo:** method
**Visibilidade:** especial/dunder
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `__init__` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `root` | positional-or-keyword | `tk.Tk` | `obrigatório/ausente` | Parâmetro `root` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `queue.Queue`
- `self._build_layout`
- `self._build_variables`
- `self._configure_style`
- `self._render_step`
- `self.root.after`
- `self.root.configure`
- `self.root.geometry`
- `self.root.minsize`
- `self.root.title`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._build_variables`

**Assinatura:**
```python
def _build_variables(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 339–377
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Constrói a responsabilidade representada por `variables` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `str`
- `time.localtime`
- `tk.BooleanVar`
- `tk.StringVar`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI.__init__`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `atividade_config.toml`

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._preset`

**Assinatura:**
```python
def _preset(self) -> Any
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 379–383
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_preset` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Any`. Retornos observados estaticamente incluem: `preset`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `RuntimeError`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._activity_data`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('Preset atividade_local_fgv não encontrado no gerador.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI.project_slug`

**Assinatura:**
```python
def project_slug(self) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 385–386
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `project_slug` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `slugify(self.project_name.get())`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `self.project_name.get`
- `slugify`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._activity_data`
- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI.output_dir`
- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI.project_dir`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI.project_dir`

**Assinatura:**
```python
def project_dir(self) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 388–389
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `project_dir` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `APP / 'projetos' / self.project_slug()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `self.project_slug`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._activity_data`
- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI.config_path`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI.config_path`

**Assinatura:**
```python
def config_path(self) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 391–395
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `config_path` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `self.project_dir() / name`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `name.lower`
- `name.lower().endswith`
- `self.project_dir`
- `self.toml_name.get`
- `self.toml_name.get().strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._activity_data`
- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._summary_text`
- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI.generate_activity`
- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI.validate_config`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `atividade_config.toml`
- `.toml`

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI.output_dir`

**Assinatura:**
```python
def output_dir(self) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 397–401
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `output_dir` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `base / self.project_slug()`; `APP / 'output' / 'documento' / self.project_slug()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_existing_path`
- `self.output_override.get`
- `self.project_slug`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._activity_data`

**Assinatura:**
```python
def _activity_data(self) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 403–504
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_activity_data` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `data`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `bool`
- `latex_class_for_layout`
- `orient_paths.append`
- `project_dir.mkdir`
- `rel_for_toml`
- `seen.add`
- `self._preset`
- `self.academic_year.get`
- `self.academic_year.get().strip`
- `self.activity_data_path.get`
- `self.activity_data_path.get().strip`
- `self.author.get`
- `self.author.get().strip`
- `self.city.get`
- `self.city.get().strip`
- `self.class_group.get`
- `self.class_group.get().strip`
- `self.config_path`
- `self.conformity.get`
- `self.corpus_mode.get`
- `self.corpus_path.get`
- `self.corpus_path.get().strip`
- `self.course.get`
- `self.course.get().strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._write_toml`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 15
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._write_toml`

**Assinatura:**
```python
def _write_toml(self) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 506–512
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Grava a responsabilidade representada por `toml` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `config_path`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `render_toml`
- `self._activity_data`
- `self.config_path_var.set`
- `self.output_dir`
- `self.output_path_var.set`
- `str`
- `write_text`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI.save_config`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._configure_style`

**Assinatura:**
```python
def _configure_style(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 518–555
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_configure_style` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `style.configure`
- `style.map`
- `style.theme_use`
- `ttk.Style`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI.__init__`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._build_layout`

**Assinatura:**
```python
def _build_layout(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 557–582
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Constrói a responsabilidade representada por `layout` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `main.grid`
- `main.grid_columnconfigure`
- `main.grid_rowconfigure`
- `self._build_footer`
- `self._build_header`
- `self._build_progress`
- `self._build_sidebar`
- `self._build_step_pages`
- `self.content_stack.grid`
- `self.content_stack.grid_columnconfigure`
- `self.content_stack.grid_rowconfigure`
- `self.root.grid_columnconfigure`
- `self.root.grid_rowconfigure`
- `shell.grid`
- `shell.grid_columnconfigure`
- `shell.grid_rowconfigure`
- `sidebar.grid`
- `sidebar.grid_propagate`
- `ttk.Frame`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI.__init__`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._build_sidebar`

**Assinatura:**
```python
def _build_sidebar(self, sidebar: ttk.Frame) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 584–600
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Constrói a responsabilidade representada por `sidebar` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `sidebar` | positional-or-keyword | `ttk.Frame` | `obrigatório/ausente` | Parâmetro `sidebar` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `button.pack`
- `lower.pack`
- `self._open_path`
- `self._render_step`
- `spacer.pack`
- `top.pack`
- `ttk.Button`
- `ttk.Button(lower, text='Abrir pasta do bundle', style='Secondary.TButton', command=lambda: self._open_path(ROOT)).pack`
- `ttk.Frame`
- `ttk.Label`
- `ttk.Label(lower, text='Atalhos', style='SidebarTitle.TLabel', font=('DejaVu Sans', 10, 'bold')).pack`
- `ttk.Label(lower, text='Tab completa caminhos\nCtrl+S salva o TOML\nCtrl+Enter gera', style='SidebarText.TLabel').pack`
- `ttk.Label(top, text='Academic Pipeline', style='SidebarTitle.TLabel').pack`
- `ttk.Label(top, text='Atividades acadêmicas FGV', style='SidebarText.TLabel').pack`
- `ttk.Separator`
- `ttk.Separator(sidebar, orient=tk.HORIZONTAL).pack`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._build_layout`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._build_header`

**Assinatura:**
```python
def _build_header(self, main: ttk.Frame) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 602–626
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Constrói a responsabilidade representada por `header` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `main` | positional-or-keyword | `ttk.Frame` | `obrigatório/ausente` | Parâmetro `main` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `header.grid`
- `header.grid_columnconfigure`
- `image.subsample`
- `logo_frame.grid`
- `logo_path.exists`
- `right.grid`
- `str`
- `texts.grid`
- `tk.Label`
- `tk.Label(logo_frame, image=self._logo, bg=FGV_CARD, borderwidth=0).pack`
- `tk.Label(texts, text='Central de atividades acadêmicas', bg=FGV_CARD, fg=FGV_NAVY, font=('DejaVu Sans', 17, 'bold')).pack`
- `tk.Label(texts, text='Crie, valide e gere sua atividade em um fluxo guiado, sem depender de perguntas no terminal.', bg=FGV_CARD, fg=FGV_MUTED, font=('DejaVu Sans', 10)).pack`
- `tk.PhotoImage`
- `ttk.Button`
- `ttk.Button(right, text='Salvar TOML', style='Secondary.TButton', command=self.save_config).pack`
- `ttk.Frame`
- `ttk.Label`
- `ttk.Label(logo_frame, text='FGV', style='Heading.TLabel').pack`
- `ttk.Label(right, textvariable=self.status, style='Status.TLabel', wraplength=280, justify='right').pack`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._build_layout`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `fgv.png`

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._build_progress`

**Assinatura:**
```python
def _build_progress(self, main: ttk.Frame) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 628–637
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Constrói a responsabilidade representada por `progress` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `main` | positional-or-keyword | `ttk.Frame` | `obrigatório/ausente` | Parâmetro `main` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `bar.grid`
- `bar.grid_columnconfigure`
- `self.progress_hint.grid`
- `self.progress_indicator.grid`
- `self.progress_title.grid`
- `ttk.Frame`
- `ttk.Label`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._build_layout`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._build_footer`

**Assinatura:**
```python
def _build_footer(self, main: ttk.Frame) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 639–650
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Constrói a responsabilidade representada por `footer` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `main` | positional-or-keyword | `ttk.Frame` | `obrigatório/ausente` | Parâmetro `main` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `footer.grid`
- `footer.grid_columnconfigure`
- `self.back_button.grid`
- `self.footer_status.grid`
- `self.generate_activity`
- `self.next_button.grid`
- `self.root.bind_all`
- `self.save_config`
- `ttk.Button`
- `ttk.Frame`
- `ttk.Label`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._build_layout`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._build_step_pages`

**Assinatura:**
```python
def _build_step_pages(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 656–668
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Constrói a responsabilidade representada por `step_pages` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `ScrollableFrame`
- `builders[step.key]`
- `frame.grid`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._build_layout`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._card`

**Assinatura:**
```python
def _card(self, parent: tk.Misc, *, title: str, subtitle: str = '') -> ttk.Frame
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 670–676
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_card` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `parent` | positional-or-keyword | `tk.Misc` | `obrigatório/ausente` | Parâmetro `parent` recebido pela função; não há descrição individual explícita no código. |
| `title` | keyword-only | `str` | `obrigatório/ausente` | Parâmetro `title` recebido pela função; não há descrição individual explícita no código. |
| `subtitle` | keyword-only | `str` | `''` | Parâmetro `subtitle` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `ttk.Frame`. Retornos observados estaticamente incluem: `outer`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `outer.pack`
- `ttk.Frame`
- `ttk.Label`
- `ttk.Label(outer, text=subtitle, style='CardMuted.TLabel', wraplength=920, justify='left').pack`
- `ttk.Label(outer, text=title, style='Card.TLabel', font=('DejaVu Sans', 12, 'bold')).pack`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._page_activity`
- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._page_inputs`
- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._page_outputs`
- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._page_project`
- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._page_review`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._field`

**Assinatura:**
```python
def _field(self, parent: tk.Misc, label: str, variable: tk.StringVar, *, help_text: str = '', path_kind: str | None = None, width: int = 68) -> tk.Widget
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 678–699
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_field` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `parent` | positional-or-keyword | `tk.Misc` | `obrigatório/ausente` | Parâmetro `parent` recebido pela função; não há descrição individual explícita no código. |
| `label` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `label` recebido pela função; não há descrição individual explícita no código. |
| `variable` | positional-or-keyword | `tk.StringVar` | `obrigatório/ausente` | Parâmetro `variable` recebido pela função; não há descrição individual explícita no código. |
| `help_text` | keyword-only | `str` | `''` | Conteúdo textual de entrada. |
| `path_kind` | keyword-only | `str \| None` | `None` | Parâmetro `path_kind` recebido pela função; não há descrição individual explícita no código. |
| `width` | keyword-only | `int` | `68` | Parâmetro `width` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `tk.Widget`. Retornos observados estaticamente incluem: `widget`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `AutocompletePathEntry`
- `entry.pack`
- `row.pack`
- `self._choose_dir`
- `self._choose_file`
- `self._filetypes_for`
- `ttk.Button`
- `ttk.Button(row, text='Arquivo', style='Secondary.TButton', command=lambda: self._choose_file(variable)).pack`
- `ttk.Button(row, text='Pasta', style='Secondary.TButton', command=lambda: self._choose_dir(variable)).pack`
- `ttk.Button(row, text='Procurar arquivo', style='Secondary.TButton', command=lambda: self._choose_file(variable, filetypes=self._filetypes_for(path_kind))).pack`
- `ttk.Button(row, text='Procurar pasta', style='Secondary.TButton', command=lambda: self._choose_dir(variable)).pack`
- `ttk.Entry`
- `ttk.Frame`
- `ttk.Label`
- `ttk.Label(parent, text=help_text, style='CardMuted.TLabel', wraplength=840, justify='left').pack`
- `ttk.Label(row, text=label, style='Card.TLabel', width=29, anchor='w').pack`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._page_inputs`
- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._page_outputs`
- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._page_project`
- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._refresh_corpus_mode`
- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._refresh_data_mode`
- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._refresh_orientation_mode`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._text_field`

**Assinatura:**
```python
def _text_field(self, parent: tk.Misc, label: str, *, height: int = 5, help_text: str = '') -> tk.Text
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 701–707
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_text_field` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `parent` | positional-or-keyword | `tk.Misc` | `obrigatório/ausente` | Parâmetro `parent` recebido pela função; não há descrição individual explícita no código. |
| `label` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `label` recebido pela função; não há descrição individual explícita no código. |
| `height` | keyword-only | `int` | `5` | Parâmetro `height` recebido pela função; não há descrição individual explícita no código. |
| `help_text` | keyword-only | `str` | `''` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `tk.Text`. Retornos observados estaticamente incluem: `text`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `text.pack`
- `tk.Text`
- `ttk.Label`
- `ttk.Label(parent, text=help_text, style='CardMuted.TLabel', wraplength=900, justify='left').pack`
- `ttk.Label(parent, text=label, style='Card.TLabel').pack`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._refresh_data_mode`
- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._refresh_orientation_mode`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._choose_file`

**Assinatura:**
```python
def _choose_file(self, variable: tk.StringVar, *, filetypes: list[tuple[str, str]] | None = None) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 709–714
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_choose_file` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `variable` | positional-or-keyword | `tk.StringVar` | `obrigatório/ausente` | Parâmetro `variable` recebido pela função; não há descrição individual explícita no código. |
| `filetypes` | keyword-only | `list[tuple[str, str]] \| None` | `None` | Parâmetro `filetypes` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `Path.home`
- `_existing_path`
- `current.is_dir`
- `filedialog.askopenfilename`
- `str`
- `variable.get`
- `variable.set`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._field`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._choose_dir`

**Assinatura:**
```python
def _choose_dir(self, variable: tk.StringVar) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 716–721
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_choose_dir` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `variable` | positional-or-keyword | `tk.StringVar` | `obrigatório/ausente` | Parâmetro `variable` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `Path.home`
- `_existing_path`
- `current.is_dir`
- `filedialog.askdirectory`
- `str`
- `variable.get`
- `variable.set`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._field`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._filetypes_for`

**Assinatura:**
```python
def _filetypes_for(kind: str) -> list[tuple[str, str]]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 724–727
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** `staticmethod`
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_filetypes_for` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `kind` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `kind` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[tuple[str, str]]`. Retornos observados estaticamente incluem: `[('Arquivos ZIP', '*.zip'), ('Todos os arquivos', '*.*')]`; `[('Documentos e arquivos', '*.txt *.md *.org *.pdf *.docx *.zip'), ('Todos os arquivos', '*.*')]`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

Nenhum identificado pela análise estática.

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._field`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `*.txt *.md *.org *.pdf *.docx *.zip`

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._page_project`

**Assinatura:**
```python
def _page_project(self, parent: ttk.Frame) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 733–745
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_page_project` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `parent` | positional-or-keyword | `ttk.Frame` | `obrigatório/ausente` | Parâmetro `parent` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `self._card`
- `self._field`
- `ttk.Separator`
- `ttk.Separator(card).pack`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `O padrão é atividade_config.toml. Um TOML organiza os insumos, as saídas e as regras da atividade.`

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._page_activity`

**Assinatura:**
```python
def _page_activity(self, parent: ttk.Frame) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 747–767
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_page_activity` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `parent` | positional-or-keyword | `ttk.Frame` | `obrigatório/ausente` | Parâmetro `parent` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `line.pack`
- `radios.pack`
- `self._card`
- `self._refresh_data_mode`
- `self.data_dynamic.pack`
- `ttk.Frame`
- `ttk.Label`
- `ttk.Label(keywords, text='✓ geração automática ativada', style='Card.TLabel', foreground=FGV_SUCCESS, font=('DejaVu Sans', 10, 'bold')).pack`
- `ttk.Label(line, text=hint, style='CardMuted.TLabel', wraplength=720, justify='left').pack`
- `ttk.Radiobutton`
- `ttk.Radiobutton(line, text=label, variable=self.data_mode, value=value, command=self._refresh_data_mode).pack`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._refresh_data_mode`

**Assinatura:**
```python
def _refresh_data_mode(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 769–789
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_refresh_data_mode` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `child.destroy`
- `self._capture_manual_instruction`
- `self._field`
- `self._text_field`
- `self.data_dynamic.winfo_children`
- `self.data_mode.get`
- `self.manual_instruction.get`
- `self.manual_instruction_text.bind`
- `self.manual_instruction_text.insert`
- `ttk.Label`
- `ttk.Label(self.data_dynamic, text='A IA inferirá os dados da atividade no momento da geração. Para obter um resultado fiel, informe um corpus local e, quando houver, o enunciado ou roteiro da aula na próxima etapa.', style='CardMuted.TLabel', wraplength=860, justify='left').pack`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._page_activity`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._page_inputs`

**Assinatura:**
```python
def _page_inputs(self, parent: ttk.Frame) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 791–816
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_page_inputs` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `parent` | positional-or-keyword | `ttk.Frame` | `obrigatório/ausente` | Parâmetro `parent` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `combo.bind`
- `combo.pack`
- `combo_or.bind`
- `combo_or.pack`
- `mode_line.pack`
- `orientation_mode_line.pack`
- `self._card`
- `self._field`
- `self._refresh_corpus_mode`
- `self._refresh_orientation_mode`
- `self.corpus_dynamic.pack`
- `self.orientation_dynamic.pack`
- `ttk.Combobox`
- `ttk.Frame`
- `ttk.Label`
- `ttk.Label(mode_line, text='Tipo de entrada', style='Card.TLabel', width=29, anchor='w').pack`
- `ttk.Label(orientation_mode_line, text='Forma de fornecimento', style='Card.TLabel', width=29, anchor='w').pack`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `Exemplo: prompt_base_atividade_aula_4.txt. Deixe em branco se não houver instrução adicional.`
- `Opcional. Informe apenas se houver um doi_manifest.csv revisado para o corpus.`

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._refresh_corpus_mode`

**Assinatura:**
```python
def _refresh_corpus_mode(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 818–824
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_refresh_corpus_mode` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `child.destroy`
- `self._field`
- `self.corpus_dynamic.winfo_children`
- `self.corpus_mode.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._page_inputs`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._refresh_orientation_mode`

**Assinatura:**
```python
def _refresh_orientation_mode(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 826–839
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_refresh_orientation_mode` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `child.destroy`
- `self._capture_orientation_inline`
- `self._field`
- `self._text_field`
- `self.orientation_dynamic.winfo_children`
- `self.orientation_mode.get`
- `self.orientation_text.bind`
- `self.orientation_text.insert`
- `ttk.Label`
- `ttk.Label(self.orientation_dynamic, text='Nenhuma orientação adicional será anexada. Isso é adequado quando o próprio arquivo de dados da atividade ou o prompt específico já contém todas as instruções relevantes.', style='CardMuted.TLabel', wraplength=860, justify='left').pack`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._page_inputs`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._capture_orientation_inline`

**Assinatura:**
```python
def _capture_orientation_inline(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 841–844
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_capture_orientation_inline` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `getattr`
- `widget.get`
- `widget.winfo_exists`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._capture_multiline_fields`
- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._refresh_orientation_mode`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._capture_manual_instruction`

**Assinatura:**
```python
def _capture_manual_instruction(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 846–849
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_capture_manual_instruction` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `getattr`
- `self.manual_instruction.set`
- `widget.get`
- `widget.winfo_exists`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._capture_multiline_fields`
- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._refresh_data_mode`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._capture_multiline_fields`

**Assinatura:**
```python
def _capture_multiline_fields(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 851–853
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_capture_multiline_fields` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `self._capture_manual_instruction`
- `self._capture_orientation_inline`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._render_step`
- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._validate_step`
- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI.save_config`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._page_outputs`

**Assinatura:**
```python
def _page_outputs(self, parent: ttk.Frame) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 855–873
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_page_outputs` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `parent` | positional-or-keyword | `ttk.Frame` | `obrigatório/ausente` | Parâmetro `parent` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `checks.pack`
- `engine_line.pack`
- `self._card`
- `self._field`
- `ttk.Checkbutton`
- `ttk.Checkbutton(checks, text='Gerar DOCX', variable=self.export_docx).grid`
- `ttk.Checkbutton(checks, text='Gerar ORG editável', variable=self.export_org).grid`
- `ttk.Checkbutton(checks, text='Gerar PDF', variable=self.export_pdf).grid`
- `ttk.Checkbutton(exports, text='Buscar/enriquecer metadados por DOI quando disponível', variable=self.enrich_metadata).pack`
- `ttk.Checkbutton(exports, text='Gerar relatório de conformidade institucional', variable=self.conformity).pack`
- `ttk.Checkbutton(exports, text='Gerar relatório de qualidade', variable=self.quality).pack`
- `ttk.Checkbutton(exports, text='Tentar extrair DOI dos PDFs', variable=self.extract_doi).pack`
- `ttk.Combobox`
- `ttk.Combobox(engine_line, textvariable=self.pdf_engine, values=['lualatex', 'xelatex', 'pdflatex'], state='readonly', width=16).pack`
- `ttk.Frame`
- `ttk.Label`
- `ttk.Label(engine_line, text='Engine do PDF', style='Card.TLabel', width=29, anchor='w').pack`
- `ttk.Separator`
- `ttk.Separator(exports).pack`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `A geração cria um document.json rastreável e, conforme a seleção abaixo, os formatos finais para entrega e edição.`

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._page_review`

**Assinatura:**
```python
def _page_review(self, parent: ttk.Frame) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 875–891
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_page_review` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `parent` | positional-or-keyword | `ttk.Frame` | `obrigatório/ausente` | Parâmetro `parent` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `action_bar.pack`
- `scrolledtext.ScrolledText`
- `self._card`
- `self._open_path`
- `self.log_text.configure`
- `self.log_text.pack`
- `self.output_dir`
- `self.review_text.configure`
- `self.review_text.pack`
- `tk.Text`
- `ttk.Button`
- `ttk.Button(action_bar, text='Abrir saída', style='Secondary.TButton', command=lambda: self._open_path(self.output_dir())).pack`
- `ttk.Button(action_bar, text='Gerar atividade', style='Primary.TButton', command=self.generate_activity).pack`
- `ttk.Button(action_bar, text='Salvar TOML', style='Secondary.TButton', command=self.save_config).pack`
- `ttk.Button(action_bar, text='Validar', style='Secondary.TButton', command=self.validate_config).pack`
- `ttk.Frame`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._step_index`

**Assinatura:**
```python
def _step_index(self, key: str) -> int
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 897–898
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_step_index` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `key` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `key` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `next((index for index, step in enumerate(STEPS) if step.key == key))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `enumerate`
- `next`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._render_step`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._render_step`

**Assinatura:**
```python
def _render_step(self, key: str) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 900–921
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Renderiza a responsabilidade representada por `step` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `key` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `key` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `frame.tkraise`
- `frame.to_top`
- `len`
- `self._capture_multiline_fields`
- `self._refresh_review`
- `self._step_index`
- `self._validate_through`
- `self.back_button.configure`
- `self.footer_status.configure`
- `self.next_button.configure`
- `self.progress_hint.configure`
- `self.progress_indicator.configure`
- `self.progress_title.configure`
- `self.step_buttons[item.key].configure`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI.__init__`
- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._build_sidebar`
- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._set_busy`
- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI.next_step`
- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI.previous_step`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI.previous_step`

**Assinatura:**
```python
def previous_step(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 923–925
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `previous_step` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `self._render_step`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI.next_step`

**Assinatura:**
```python
def next_step(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 927–931
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `next_step` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `len`
- `self._render_step`
- `self._validate_step`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._validate_through`

**Assinatura:**
```python
def _validate_through(self, final_index: int, *, show_message: bool) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 933–937
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Valida a responsabilidade representada por `through` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `final_index` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `final_index` recebido pela função; não há descrição individual explícita no código. |
| `show_message` | keyword-only | `bool` | `obrigatório/ausente` | Parâmetro `show_message` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `False`; `True`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `range`
- `self._validate_step`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._render_step`
- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI.save_config`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._validate_step`

**Assinatura:**
```python
def _validate_step(self, index: int, *, show_message: bool) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 939–988
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Valida a responsabilidade representada por `step` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `index` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `index` recebido pela função; não há descrição individual explícita no código. |
| `show_message` | keyword-only | `bool` | `obrigatório/ausente` | Parâmetro `show_message` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `False`; `True`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `Path`
- `Path(override).expanduser`
- `Path(override).expanduser().mkdir`
- `_existing_path`
- `any`
- `corpus.is_dir`
- `corpus.is_file`
- `errors.append`
- `messagebox.showwarning`
- `self._capture_multiline_fields`
- `self.activity_data_path.get`
- `self.author.get`
- `self.author.get().strip`
- `self.corpus_mode.get`
- `self.corpus_path.get`
- `self.data_mode.get`
- `self.doi_manifest_path.get`
- `self.doi_manifest_path.get().strip`
- `self.export_docx.get`
- `self.export_org.get`
- `self.export_pdf.get`
- `self.manual_question.get`
- `self.manual_question.get().strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._validate_through`
- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI.next_step`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 22
loops: 0
comprehensions: 1
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._summary_text`

**Assinatura:**
```python
def _summary_text(self) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 994–1026
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_summary_text` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'\n'.join(['REVISÃO DA ATIVIDADE', '=' * 72, f"Projeto: {self.project_name.get().strip() or '—'}", f'TOML: {self.config_path()}', f"Título: {self.title.get().strip() or '—'}", f"Disciplina: {self.discipline.get().strip() or '—'}", f"Professor: {self.professor.get().strip() or '—'}", '', 'DADOS DA ATIVIDADE', f'Fonte: {data_mode_label}', f"Dados por IA: {('sim' if self.data_mode.get() == 'ia' else 'não')}", f'Palavras-chave: inferência automática pela IA', '', 'INSUMOS', f"Corpus ({self.corpus_mode.get()}): {self.corpus_path.get().strip() or '—'}", f'Orientações gerais: {orientation_label}', f"Prompt específico: {self.prompt_path.get().strip() or 'não informado'}", f"Manifesto DOI: {self.doi_manifest_path.get().strip() or 'não informado'}", '', 'SAÍDAS', f"Formatos: {outputs or '—'}", f'Pasta de saída: {self.output_dir()}', f"Conformidade: {('ativada' if self.conformity.get() else 'desativada')}", f"Qualidade: {('ativada' if self.quality.get() else 'desativada')}", '', 'AÇÃO RECOMENDADA', '1. Salve o TOML.  2. Valide.  3. Gere a atividade.'])`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `', '.join`
- `'\n'.join`
- `self.config_path`
- `self.conformity.get`
- `self.corpus_mode.get`
- `self.corpus_path.get`
- `self.corpus_path.get().strip`
- `self.data_mode.get`
- `self.discipline.get`
- `self.discipline.get().strip`
- `self.doi_manifest_path.get`
- `self.doi_manifest_path.get().strip`
- `self.export_docx.get`
- `self.export_org.get`
- `self.export_pdf.get`
- `self.orientation_mode.get`
- `self.output_dir`
- `self.professor.get`
- `self.professor.get().strip`
- `self.project_name.get`
- `self.project_name.get().strip`
- `self.prompt_path.get`
- `self.prompt_path.get().strip`
- `self.quality.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._refresh_review`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._refresh_review`

**Assinatura:**
```python
def _refresh_review(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 1028–1034
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_refresh_review` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `hasattr`
- `self._summary_text`
- `self.review_text.configure`
- `self.review_text.delete`
- `self.review_text.insert`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._poll_worker_queue`
- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._render_step`
- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI.save_config`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI.save_config`

**Assinatura:**
```python
def save_config(self) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 1036–1049
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Salva a responsabilidade representada por `config` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `False`; `True`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `messagebox.showerror`
- `self._append_log`
- `self._capture_multiline_fields`
- `self._refresh_review`
- `self._validate_through`
- `self._write_toml`
- `self.status.set`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._build_footer`
- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI.generate_activity`
- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI.validate_config`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI.validate_config`

**Assinatura:**
```python
def validate_config(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 1051–1057
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Valida a responsabilidade representada por `config` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `self._run_command`
- `self.config_path`
- `self.save_config`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `--check-config`
- `--config`

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI.generate_activity`

**Assinatura:**
```python
def generate_activity(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 1059–1070
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Gera a responsabilidade representada por `activity` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(self.output_override.get()).expanduser`
- `command.extend`
- `messagebox.askyesno`
- `self._run_command`
- `self.config_path`
- `self.output_override.get`
- `self.output_override.get().strip`
- `self.save_config`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._build_footer`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `--config`
- `--output-dir`

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._run_command`

**Assinatura:**
```python
def _run_command(self, command: list[str], *, label: str) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 1072–1093
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `command` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `command` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `command` recebido pela função; não há descrição individual explícita no código. |
| `label` | keyword-only | `str` | `obrigatório/ausente` | Parâmetro `label` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `self._append_log`
- `self._set_busy`
- `self.status.set`
- `threading.Thread`
- `threading.Thread(target=worker, name='academic-pipeline-gui-worker', daemon=True).start`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI.generate_activity`
- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI.validate_config`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._run_command.worker`

**Assinatura:**
```python
def worker() -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 1081–1091
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `worker` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Executa processo ou ferramenta externa.

#### Chamadas realizadas pelo corpo direto

- `process.wait`
- `self._worker_queue.put`
- `str`
- `subprocess.Popen`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._poll_worker_queue`

**Assinatura:**
```python
def _poll_worker_queue(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 1095–1121
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_poll_worker_queue` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `messagebox.showerror`
- `messagebox.showinfo`
- `self._append_log`
- `self._refresh_review`
- `self._set_busy`
- `self._worker_queue.get_nowait`
- `self.output_dir`
- `self.root.after`
- `self.status.set`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 1
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._set_busy`

**Assinatura:**
```python
def _set_busy(self, busy: bool) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 1123–1131
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_set_busy` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `busy` | positional-or-keyword | `bool` | `obrigatório/ausente` | Parâmetro `busy` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `button.configure`
- `self._render_step`
- `self.back_button.configure`
- `self.next_button.configure`
- `self.step_buttons.values`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._poll_worker_queue`
- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._run_command`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._append_log`

**Assinatura:**
```python
def _append_log(self, text: str) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 1133–1139
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_append_log` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `hasattr`
- `self.log_text.configure`
- `self.log_text.insert`
- `self.log_text.see`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._poll_worker_queue`
- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._run_command`
- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI.save_config`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._open_path`

**Assinatura:**
```python
def _open_path(self, path: Path) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 1145–1160
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineGUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_open_path` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Executa processo ou ferramenta externa.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `RuntimeError`
- `messagebox.showwarning`
- `os.startfile`
- `path.mkdir`
- `str`
- `subprocess.Popen`
- `sys.platform.startswith`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._build_sidebar`
- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._page_review`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('Sistema operacional não suportado para abrir arquivos automaticamente.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 0
exception_handlers: 2
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:run_gui`

**Assinatura:**
```python
def run_gui() -> int
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 1163–1175
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Inicializa a interface gráfica, com diagnóstico claro em falta de Tk.

**Docstring resumida:** Inicializa a interface gráfica, com diagnóstico claro em falta de Tk.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `0`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `AcademicPipelineGUI`
- `RuntimeError`
- `repr`
- `root.mainloop`
- `tk.Tk`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.command_dispatch:dispatch_stage_001`
- `app_bundle.scripts.pipeline.academic_pipeline_gui:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('A interface gráfica precisa do módulo tkinter. No Debian/Ubuntu, instale os pacotes do Tk e garanta que o Python usado pelo Pipenv tenha sido compilado com suporte a Tk.\n\nTeste: pipenv run python -c "import tkinter; print(tkinter.TkVersion)"\nDetalhe técnico: ' + repr(_TK_IMPORT_ERROR))`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_gui:main`

**Assinatura:**
```python
def main(argv: list[str] | None = None) -> int
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_gui.py`
**Linhas:** 1178–1181
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa o ponto de entrada correspondente e coordena o fluxo associado.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `list[str] \| None` | `None` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `run_gui()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `argparse.ArgumentParser`
- `parser.parse_args`
- `run_gui`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f26fabb06247063b756902e112e187164b497400095327db0601093f8914df01`
**LOC do arquivo-fonte:** 1185


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator:source_for_profile`

**Assinatura:**
```python
def source_for_profile(profile: str) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator.py`
**Linhas:** 21–32
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `source_for_profile` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `profile` | positional-or-keyword | `str` | `obrigatório/ausente` | Perfil institucional ou preset utilizado pela operação. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `EXAMPLES / 'paper_exemplo.toml'`; `EXAMPLES / 'atividade_exemplo.toml'`; `EXAMPLES / 'relatorio_prisma_exemplo.toml'`; `base`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `ValueError`
- `base.exists`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `ValueError(f'Perfil desconhecido: {profile}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `paper_exemplo.toml`
- `atividade_exemplo.toml`
- `relatorio_prisma_exemplo.toml`

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `97470090c33aa8ff641e6117d4c0094ec5823c09e9cabd387c71b90c29cf15dc`
**LOC do arquivo-fonte:** 95


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator:append_prisma_block_if_needed`

**Assinatura:**
```python
def append_prisma_block_if_needed(content: str, profile: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator.py`
**Linhas:** 35–66
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `append_prisma_block_if_needed` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `content` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `content` recebido pela função; não há descrição individual explícita no código. |
| `profile` | positional-or-keyword | `str` | `obrigatório/ausente` | Perfil institucional ou preset utilizado pela operação. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `content`; `content.rstrip() + '\n\n[relatorio_pesquisa]\nativo = true\ntipo = "prisma"\noutput_dir = "./output/pesquisa"\nprefixo = "relatorio_prisma_paper"\ncriar_subdiretorio = true\nexportar_json = true\nexportar_org = true\nexportar_pdf = true\nexportar_docx = true\nexportar_xlsx = true\nexportar_fluxograma = true\nvalidar = true\nfalhar_se_invalido = false\nprisma_json_path = ""\npesquisa_dir_existente = ""\ncriterios_inclusao = [\n  "Aderência substantiva ao tema, recorte e objetivo.",\n  "Disponibilidade de metadados mínimos ou DOI para identificação bibliográfica.",\n  "Texto acadêmico ou técnico-científico relacionado ao problema de pesquisa."\n]\ncriterios_exclusao = [\n  "Fora do tema ou do recorte.",\n  "Duplicado.",\n  "Ausência de relação substantiva com a pergunta de pesquisa."\n]\n'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `content.rstrip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `97470090c33aa8ff641e6117d4c0094ec5823c09e9cabd387c71b90c29cf15dc`
**LOC do arquivo-fonte:** 95


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator:main`

**Assinatura:**
```python
def main() -> int
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator.py`
**Linhas:** 69–91
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa o ponto de entrada correspondente e coordena o fluxo associado.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `0`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(args.output).expanduser`
- `Path(args.output).expanduser().resolve`
- `Path.cwd`
- `append_prisma_block_if_needed`
- `argparse.ArgumentParser`
- `out.parent.mkdir`
- `out.write_text`
- `parser.add_argument`
- `parser.parse_args`
- `print`
- `source_for_profile`
- `src.read_text`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `--output`
- `--perfil`

#### Artefatos/caminhos literais reconhecidos no corpo

- `paper_config_rc10.toml`
- `atividade_config_rc10.toml`
- `relatorio_prisma_config_rc10.toml`
- `atividade_prisma_config_rc10.toml`
- `paper_prisma_config_rc10.toml`

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `97470090c33aa8ff641e6117d4c0094ec5823c09e9cabd387c71b90c29cf15dc`
**LOC do arquivo-fonte:** 95


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:find_app_bundle`

**Assinatura:**
```python
def find_app_bundle(start: Path | None = None) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 77–88
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Localiza a responsabilidade representada por `app_bundle` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `start` | positional-or-keyword | `Path \| None` | `None` | Parâmetro `start` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `candidate`; `(candidate / 'app_bundle').resolve()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `(candidate / 'app_bundle').is_dir`
- `(candidate / 'app_bundle').resolve`
- `(start or Path.cwd()).resolve`
- `Path`
- `Path(__file__).resolve`
- `Path.cwd`
- `RuntimeError`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('Não consegui localizar app_bundle. Execute a partir da raiz do academic_pipeline ou de app_bundle.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:slugify`

**Assinatura:**
```python
def slugify(value: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 100–114
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `slugify` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `value or 'projeto'`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `(value or '').strip`
- `(value or '').strip().lower`
- `re.sub`
- `re.sub('_+', '_', value).strip`
- `repl.items`
- `value.replace`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:toml_escape`

**Assinatura:**
```python
def toml_escape(value: Any) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 117–125
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `toml_escape` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `s.replace('\\', '\\\\').replace('\r', '\\r').replace('\n', '\\n').replace('\t', '\\t').replace('"', '\\"')`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `s.replace`
- `s.replace('\\', '\\\\').replace`
- `s.replace('\\', '\\\\').replace('\r', '\\r').replace`
- `s.replace('\\', '\\\\').replace('\r', '\\r').replace('\n', '\\n').replace`
- `s.replace('\\', '\\\\').replace('\r', '\\r').replace('\n', '\\n').replace('\t', '\\t').replace`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:tstr`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:tstr`

**Assinatura:**
```python
def tstr(value: Any) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 127–128
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `tstr` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `f'"{toml_escape(value)}"'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `toml_escape`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:render_toml`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:tlist`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:tbool`

**Assinatura:**
```python
def tbool(value: bool) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 131–132
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `tbool` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `bool` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'true' if bool(value) else 'false'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `bool`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:render_toml`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:tlist`

**Assinatura:**
```python
def tlist(items: list[str], indent: str = '') -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 135–139
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `tlist` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `items` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `items` recebido pela função; não há descrição individual explícita no código. |
| `indent` | positional-or-keyword | `str` | `''` | Parâmetro `indent` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'[]'`; `'[\n' + body + f'\n{indent}]'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `',\n'.join`
- `tstr`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:render_toml`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:rel_for_toml`

**Assinatura:**
```python
def rel_for_toml(path: Path | str, config_dir: Path) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 142–155
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `rel_for_toml` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path \| str` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `config_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `config_dir` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `''`; `raw`; `raw.replace('\\', '/')`; `os.path.relpath(p.resolve(), config_dir.resolve()).replace('\\', '/')`; `str(p).replace('\\', '/')`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(raw).expanduser`
- `config_dir.resolve`
- `os.path.relpath`
- `os.path.relpath(p.resolve(), config_dir.resolve()).replace`
- `p.is_absolute`
- `p.resolve`
- `raw.replace`
- `raw.startswith`
- `str`
- `str(p).replace`
- `str(path or '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._activity_data`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_apply_structured_prisma_protocol`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_keywords`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_optional_hypothesis`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:bundle_rel`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_outputs_and_options`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_paper_local_research`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_prisma_busca_orientada`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_research`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_sources`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:render_toml`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ensure_project_dir`

**Assinatura:**
```python
def ensure_project_dir(project_slug: str) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 158–161
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Garante a responsabilidade representada por `project_dir` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `project_slug` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `project_slug` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `p`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `p.mkdir`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:write_text`

**Assinatura:**
```python
def write_text(path: Path, text: str) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 164–166
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Grava a responsabilidade representada por `text` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `path.parent.mkdir`
- `path.write_text`
- `text.rstrip`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:maybe_copy_to_project`

**Assinatura:**
```python
def maybe_copy_to_project(src: str, dst: Path) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 169–179
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `maybe_copy_to_project` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `src` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `src` recebido pela função; não há descrição individual explícita no código. |
| `dst` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `dst` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `str(dst)`; `src`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `(src or '').strip`
- `Path`
- `Path(src).expanduser`
- `dst.parent.mkdir`
- `dst.resolve`
- `p.exists`
- `p.is_file`
- `p.resolve`
- `shutil.copy2`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_sources`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:install_path_completion`

**Assinatura:**
```python
def install_path_completion() -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 182–207
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `install_path_completion` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `readline.parse_and_bind`
- `readline.set_completer`
- `readline.set_completer_delims`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:generate_interactive`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:set_wizard_project_dir`

**Assinatura:**
```python
def set_wizard_project_dir(value: str | Path | None) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 221–224
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `set_wizard_project_dir` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `str \| Path \| None` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(raw).expanduser`
- `Path(raw).expanduser().resolve`
- `str`
- `str(value or '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:generate_interactive`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:bundle_rel`

**Assinatura:**
```python
def bundle_rel(data: dict[str, Any], relative_to_bundle: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 227–229
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Gera caminho relativo ao TOML, inclusive quando salvo fora de app_bundle.

**Docstring resumida:** Gera caminho relativo ao TOML, inclusive quando salvo fora de app_bundle.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `data` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Estrutura de dados de entrada. |
| `relative_to_bundle` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `relative_to_bundle` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `rel_for_toml(APP / relative_to_bundle, data['config_dir'])`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `rel_for_toml`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_outputs_and_options`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:prompt_paths_for_type`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:render_toml`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:set_tui_theme`

**Assinatura:**
```python
def set_tui_theme(value: str | None) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 232–235
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Ativa o front-end prompt_toolkit para o wizard quando solicitado.

**Docstring resumida:** Ativa o front-end prompt_toolkit para o wizard quando solicitado.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `str \| None` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `str`
- `str(value or '').strip`
- `str(value or '').strip().lower`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:tui_theme_enabled`

**Assinatura:**
```python
def tui_theme_enabled() -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 238–239
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `tui_theme_enabled` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `TUI_THEME == 'fgv'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

Nenhum identificado pela análise estática.

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_notify_structured_input_error`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_show_imported_paper_values`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_show_imported_prisma_protocol`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_show_imported_research_values`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_bool`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_choice`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_final_action`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_many_choice`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_metadata_sources`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_required`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_stage_action`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_stage_number`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:clear_screen`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_sources`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:generate_interactive`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:show_summary`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:wizard_header`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_fgv_ui`

**Assinatura:**
```python
def _fgv_ui() -> Any
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 242–253
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_fgv_ui` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `Any`. Retornos observados estaticamente incluem: `widgets`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `RuntimeError`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_notify_structured_input_error`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_show_imported_paper_values`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_show_imported_prisma_protocol`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_show_imported_research_values`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_bool`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_choice`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_final_action`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_many_choice`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_metadata_sources`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_required`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_stage_action`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_stage_number`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:generate_interactive`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:show_summary`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('O tema visual FGV requer prompt-toolkit. Execute: pipenv install prompt-toolkit')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_dialog_title`

**Assinatura:**
```python
def _dialog_title() -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 256–257
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_dialog_title` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `f'Academic Pipeline — {TUI_STAGE_TITLE}'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

Nenhum identificado pela análise estática.

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_notify_structured_input_error`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_show_imported_paper_values`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_show_imported_prisma_protocol`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_show_imported_research_values`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_bool`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_choice`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_final_action`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_many_choice`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_metadata_sources`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_required`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_stage_action`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_stage_number`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:show_summary`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_looks_like_path`

**Assinatura:**
```python
def _looks_like_path(prompt: str) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 260–263
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_looks_like_path` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `prompt` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `prompt` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `any((token in value for token in tokens))`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `any`
- `str`
- `str(prompt or '').lower`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `document.json`
- `.pdf`
- `.org`

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_path_picker_suffixes`

**Assinatura:**
```python
def _path_picker_suffixes(prompt: str) -> tuple[str, ...]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 266–289
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Restringe o navegador visual quando o rótulo indica um tipo de arquivo.

**Docstring resumida:** Restringe o navegador visual quando o rótulo indica um tipo de arquivo.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `prompt` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `prompt` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `tuple[str, ...]`. Retornos observados estaticamente incluem: `('.toml', '.json', '.yaml', '.yml', '.md', '.org', '.txt')`; `('.zip',)`; `('.json',)`; `('.csv',)`; `('.png', '.jpg', '.jpeg', '.svg')`; `('.jar',)`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `str`
- `str(prompt or '').lower`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.toml`
- `.json`
- `.md`
- `.org`
- `.txt`
- `document.json`
- `.csv`
- `.png`
- `.svg`
- `.pdf`

#### Complexidade estrutural observada

```yaml
branches: 8
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_path_picker_directories_only`

**Assinatura:**
```python
def _path_picker_directories_only(prompt: str) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 292–297
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_path_picker_directories_only` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `prompt` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `prompt` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `('pasta' in value or 'diretório' in value or 'diretorio' in value) and (not any((token in value for token in ('arquivo', 'zip', 'document.json', '.pdf', '.org'))))`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `any`
- `str`
- `str(prompt or '').lower`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `document.json`
- `.pdf`
- `.org`

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask`

**Assinatura:**
```python
def ask(prompt: str, default: str = '') -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 300–317
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `ask` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `prompt` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `prompt` recebido pela função; não há descrição individual explícita no código. |
| `default` | positional-or-keyword | `str` | `''` | Parâmetro `default` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `value if value else default`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `SystemExit`
- `_dialog_title`
- `_fgv_ui`
- `_fgv_ui().input_text`
- `_looks_like_path`
- `_path_picker_directories_only`
- `_path_picker_suffixes`
- `input`
- `input(f'{prompt}{suffix}: ').strip`
- `tui_theme_enabled`
- `value.strip`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `SystemExit('Geração de TOML cancelada pelo usuário.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_required`

**Assinatura:**
```python
def ask_required(prompt: str, default: str = '') -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 320–328
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `ask_required` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `prompt` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `prompt` recebido pela função; não há descrição individual explícita no código. |
| `default` | positional-or-keyword | `str` | `''` | Parâmetro `default` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `value.strip()`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `_dialog_title`
- `_fgv_ui`
- `_fgv_ui().message`
- `ask`
- `print`
- `tui_theme_enabled`
- `value.strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_choose_project_model_destination`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_choose_project_paper_model_destination`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_optional_hypothesis`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_paper_abstracts`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_paper_language_versions`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_prisma_search_strategy`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_prisma_strategy_and_criteria`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_structured_paper_source`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_structured_research_file`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_common`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_research`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_sources`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_bool`

**Assinatura:**
```python
def ask_bool(prompt: str, default: bool = True) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 331–343
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `ask_bool` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `prompt` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `prompt` recebido pela função; não há descrição individual explícita no código. |
| `default` | positional-or-keyword | `bool` | `True` | Parâmetro `default` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `bool(_fgv_ui().confirm(_dialog_title(), prompt, default=default))`; `default`; `True`; `False`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `_dialog_title`
- `_fgv_ui`
- `_fgv_ui().confirm`
- `bool`
- `input`
- `input(f'{prompt} (s/n) [{d}]: ').strip`
- `input(f'{prompt} (s/n) [{d}]: ').strip().lower`
- `print`
- `tui_theme_enabled`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_keywords`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_optional_hypothesis`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_paper_abstracts`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_paper_language_versions`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_prisma_search_strategy`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_configure_reference_policy`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:choose_advanced_preset`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_outputs_and_options`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_paper_local_research`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_prisma_busca_orientada`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_research`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_sources`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:generate_interactive`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:review_imported_paper_values`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:review_imported_research_values`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_choice`

**Assinatura:**
```python
def ask_choice(prompt: str, choices: list[str], default: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 346–363
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `ask_choice` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `prompt` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `prompt` recebido pela função; não há descrição individual explícita no código. |
| `choices` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `choices` recebido pela função; não há descrição individual explícita no código. |
| `default` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `default` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `str(selected)`; `choices_norm[value]`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `'/'.join`
- `SystemExit`
- `_dialog_title`
- `_fgv_ui`
- `_fgv_ui().select_one`
- `ask`
- `ask(prompt + ' (' + '/'.join(choices) + ')', default).lower`
- `c.lower`
- `print`
- `str`
- `tui_theme_enabled`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_choose_project_model_destination`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_choose_project_paper_model_destination`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_keywords`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_optional_hypothesis`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_paper_abstracts`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_paper_language_versions`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_prisma_search_strategy`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_prisma_strategy_and_criteria`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_open_project_model_in_editor`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_structured_paper_source`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_structured_research_source`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:choose_advanced_preset`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_common`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_outputs_and_options`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_paper_local_research`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_prisma_busca_orientada`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_research`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_sources`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `SystemExit('Geração de TOML cancelada pelo usuário.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 1
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_list`

**Assinatura:**
```python
def ask_list(prompt: str, default: list[str] | None = None) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 366–370
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `ask_list` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `prompt` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `prompt` recebido pela função; não há descrição individual explícita no código. |
| `default` | positional-or-keyword | `list[str] \| None` | `None` | Parâmetro `default` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `[x.strip() for x in raw.split(';') if x.strip()]`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'; '.join`
- `ask`
- `raw.split`
- `x.strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_keywords`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_manual_prisma_criteria`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_paper_local_research`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_prisma_busca_orientada`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_research`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:review_imported_paper_values`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:review_imported_research_values`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_many_choice`

**Assinatura:**
```python
def ask_many_choice(prompt: str, choices: list[tuple[str, str]], default: list[str]) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 373–413
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Seleciona múltiplos itens e expande a opção explícita ``Todas``.

**Docstring resumida:** Seleciona múltiplos itens e expande a opção explícita ``Todas``.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `prompt` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `prompt` recebido pela função; não há descrição individual explícita no código. |
| `choices` | positional-or-keyword | `list[tuple[str, str]]` | `obrigatório/ausente` | Parâmetro `choices` recebido pela função; não há descrição individual explícita no código. |
| `default` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `default` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `ask_many_choice(prompt, choices, default)`; `expanded`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `', '.join`
- `'; '.join`
- `SystemExit`
- `_dialog_title`
- `_fgv_ui`
- `_fgv_ui().select_many`
- `_notify_structured_input_error`
- `ask`
- `ask_many_choice`
- `expand_provider_selection`
- `item.strip`
- `item.strip().lower`
- `item.strip().lower().replace`
- `item.strip().lower().replace('-', '_').replace`
- `key.lower`
- `print`
- `raw.split`
- `str`
- `tui_theme_enabled`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_many_choice`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_prisma_busca_orientada`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `SystemExit('Geração de TOML cancelada pelo usuário.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 1
comprehensions: 9
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_metadata_sources`

**Assinatura:**
```python
def ask_metadata_sources(default: list[str] | None = None) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 416–464
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Seleciona fontes implementadas para metadados de PDFs/DOIs locais.

**Docstring resumida:** Seleciona fontes implementadas para metadados de PDFs/DOIs locais.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `default` | positional-or-keyword | `list[str] \| None` | `None` | Parâmetro `default` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `ask_metadata_sources(default)`; `expanded`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `', '.join`
- `'; '.join`
- `SystemExit`
- `_dialog_title`
- `_fgv_ui`
- `_fgv_ui().select_many`
- `_notify_structured_input_error`
- `ask`
- `ask_metadata_sources`
- `expand_metadata_provider_selection`
- `item.strip`
- `item.strip().lower`
- `item.strip().lower().replace`
- `item.strip().lower().replace('-', '_').replace`
- `key.lower`
- `metadata_provider_selection_choices`
- `metadata_provider_statuses`
- `print`
- `raw.split`
- `str`
- `tui_theme_enabled`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_metadata_sources`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_outputs_and_options`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `SystemExit('Geração de TOML cancelada pelo usuário.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 2
comprehensions: 10
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_positive_int`

**Assinatura:**
```python
def ask_positive_int(prompt: str, default: int, *, minimum: int = 1, maximum: int = 1000) -> int
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 467–478
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `ask_positive_int` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `prompt` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `prompt` recebido pela função; não há descrição individual explícita no código. |
| `default` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `default` recebido pela função; não há descrição individual explícita no código. |
| `minimum` | keyword-only | `int` | `1` | Parâmetro `minimum` recebido pela função; não há descrição individual explícita no código. |
| `maximum` | keyword-only | `int` | `1000` | Parâmetro `maximum` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `value`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `_notify_structured_input_error`
- `ask`
- `ask(prompt, str(default)).strip`
- `int`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_prisma_search_strategy`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_prisma_busca_orientada`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_normalise_research_key`

**Assinatura:**
```python
def _normalise_research_key(value: Any) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 552–557
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_normalise_research_key` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `re.sub('_+', '_', text).strip('_')`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `''.join`
- `re.sub`
- `re.sub('_+', '_', text).strip`
- `str`
- `text.lower`
- `text.lower().strip`
- `unicodedata.combining`
- `unicodedata.normalize`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_canonical_paper_field`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_canonical_research_field`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_prisma_search_strategy`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_iter_mapping_layers`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_normalise_prisma_block`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_protocol_layers`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_read_structured_paper_file`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_read_structured_prisma_protocol_file`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_read_structured_research_file`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_canonical_research_field`

**Assinatura:**
```python
def _canonical_research_field(value: Any) -> str | None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 560–565
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_canonical_research_field` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str | None`. Retornos observados estaticamente incluem: `field`; `None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `RESEARCH_FIELD_ALIASES.items`
- `_normalise_research_key`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_parse_labelled_paper_text`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_parse_labelled_research_text`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_persist_research_values_in_project_model`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_canonical_paper_field`

**Assinatura:**
```python
def _canonical_paper_field(value: Any) -> str | None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 568–573
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_canonical_paper_field` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str | None`. Retornos observados estaticamente incluem: `field`; `None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `PAPER_FIELD_ALIASES.items`
- `_normalise_research_key`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_parse_labelled_paper_text`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_coerce_research_text`

**Assinatura:**
```python
def _coerce_research_text(value: Any) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 576–584
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_coerce_research_text` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `''`; `'; '.join((item for item in parts if item))`; `str(value).strip()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'; '.join`
- `_coerce_research_text`
- `isinstance`
- `str`
- `str(value).strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_coerce_research_text`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_optional_hypothesis`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_read_structured_paper_file`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_read_structured_research_file`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_toml_literal_for_paper_field`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_toml_literal_for_research_field`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:review_imported_paper_values`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:review_imported_research_values`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_coerce_keywords`

**Assinatura:**
```python
def _coerce_keywords(value: Any) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 587–601
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_coerce_keywords` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `[]`; `result`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `clean.lower`
- `isinstance`
- `re.split`
- `re.sub`
- `re.sub('^[\\-\\*\\u2022]\\s*', '', str(item)).strip`
- `result.append`
- `seen.add`
- `set`
- `str`
- `str(item).strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_coerce_paper_list`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_keywords`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_parse_ai_keyword_response`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_parse_labelled_paper_text`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_parse_labelled_research_text`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_read_structured_paper_file`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_read_structured_research_file`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_toml_literal_for_paper_field`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_toml_literal_for_research_field`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:review_imported_paper_values`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:review_imported_research_values`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_coerce_paper_list`

**Assinatura:**
```python
def _coerce_paper_list(value: Any) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 604–606
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Normaliza listas de seções e argumentos do modelo do paper.

**Docstring resumida:** Normaliza listas de seções e argumentos do modelo do paper.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `_coerce_keywords(value)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_coerce_keywords`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_parse_labelled_paper_text`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_read_structured_paper_file`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:build_orientacao_inline`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_iter_mapping_layers`

**Assinatura:**
```python
def _iter_mapping_layers(value: Any) -> list[dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 609–636
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Percorre dicionários priorizando blocos usuais de metadados de pesquisa.

**Docstring resumida:** Percorre dicionários priorizando blocos usuais de metadados de pesquisa.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[dict[str, Any]]`. Retornos observados estaticamente incluem: `[]`; `layers`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_normalise_research_key`
- `add`
- `isinstance`
- `list`
- `set`
- `value.items`
- `walk`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_protocol_layers`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_read_structured_paper_file`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_read_structured_research_file`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_iter_mapping_layers.add`

**Assinatura:**
```python
def add(mapping: dict[str, Any]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 616–620
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `add` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `mapping` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `mapping` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `id`
- `layers.append`
- `seen.add`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_iter_mapping_layers.walk`

**Assinatura:**
```python
def walk(mapping: dict[str, Any]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 628–632
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `walk` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `mapping` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `mapping` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `add`
- `id`
- `isinstance`
- `mapping.values`
- `walk`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_parse_labelled_research_text`

**Assinatura:**
```python
def _parse_labelled_research_text(text: str) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 639–662
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Lê campos simples de Markdown, Org, TXT ou YAML sem dependência extra.

**Docstring resumida:** Lê campos simples de Markdown, Org, TXT ou YAML sem dependência extra.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `result`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_canonical_research_field`
- `_coerce_keywords`
- `existing.extend`
- `match.group`
- `match.group(2).strip`
- `raw_line.strip`
- `re.match`
- `re.sub`
- `result.get`
- `str`
- `str(text or '').splitlines`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_parse_simple_yaml`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_read_structured_research_file`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_parse_simple_yaml`

**Assinatura:**
```python
def _parse_simple_yaml(text: str) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 665–667
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Fallback deliberadamente simples para YAML de metadados sem PyYAML.

**Docstring resumida:** Fallback deliberadamente simples para YAML de metadados sem PyYAML.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `_parse_labelled_research_text(text)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_parse_labelled_research_text`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_read_structured_prisma_protocol_file`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_read_structured_research_file`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_parse_labelled_paper_text`

**Assinatura:**
```python
def _parse_labelled_paper_text(text: str) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 670–696
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Lê rótulos de pesquisa e de redação de paper em MD/Org/TXT/YAML.

**Docstring resumida:** Lê rótulos de pesquisa e de redação de paper em MD/Org/TXT/YAML.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `result`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_canonical_paper_field`
- `_canonical_research_field`
- `_coerce_keywords`
- `_coerce_paper_list`
- `existing.extend`
- `match.group`
- `match.group(2).strip`
- `raw_line.strip`
- `re.match`
- `re.sub`
- `result.get`
- `str`
- `str(text or '').splitlines`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_read_structured_paper_file`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_read_structured_paper_file`

**Assinatura:**
```python
def _read_structured_paper_file(raw_path: str, *, allow_declared_empty_fields: bool = False) -> tuple[Path, dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 699–767
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Lê um arquivo de dados e diretrizes para ``paper_local_fgv``.

**Docstring resumida:** Lê um arquivo de dados e diretrizes para ``paper_local_fgv``.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `raw_path` | positional-or-keyword | `str` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `allow_declared_empty_fields` | keyword-only | `bool` | `False` | Parâmetro `allow_declared_empty_fields` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `tuple[Path, dict[str, Any]]`. Retornos observados estaticamente incluem: `(path, values)`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `', '.join`
- `PAPER_FIELD_ALIASES.items`
- `Path`
- `Path(str(raw_path or '')).expanduser`
- `Path(str(raw_path or '')).expanduser().resolve`
- `RESEARCH_FIELD_ALIASES.items`
- `ValueError`
- `_coerce_keywords`
- `_coerce_paper_list`
- `_coerce_research_text`
- `_iter_mapping_layers`
- `_normalise_research_key`
- `_parse_labelled_paper_text`
- `all_labels.values`
- `any`
- `declared_fields.add`
- `isinstance`
- `json.loads`
- `layer.items`
- `path.exists`
- `path.is_file`
- `path.read_text`
- `path.suffix.lower`
- `set`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_structured_paper_source`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `ValueError(f'Arquivo inexistente ou não é arquivo regular: {path}')`
- `ValueError(f'Formato não suportado. Use um destes: {allowed}')`
- `ValueError(f'Não foi possível ler o arquivo estruturado: {exc}')`
- `ValueError('O arquivo estruturado deve conter um objeto/dicionário ou campos identificados por rótulos.')`
- `ValueError(f'Não encontrei campos reconhecidos. Use rótulos como: {readable}.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.toml`
- `.json`

#### Complexidade estrutural observada

```yaml
branches: 15
loops: 5
comprehensions: 4
exception_handlers: 2
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_read_structured_research_file`

**Assinatura:**
```python
def _read_structured_research_file(raw_path: str, *, allow_declared_empty_fields: bool = False) -> tuple[Path, dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 770–827
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Lê metadados estruturados de pesquisa.

**Docstring resumida:** Lê metadados estruturados de pesquisa.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `raw_path` | positional-or-keyword | `str` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `allow_declared_empty_fields` | keyword-only | `bool` | `False` | Parâmetro `allow_declared_empty_fields` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `tuple[Path, dict[str, Any]]`. Retornos observados estaticamente incluem: `(path, values)`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `', '.join`
- `Path`
- `Path(str(raw_path or '')).expanduser`
- `Path(str(raw_path or '')).expanduser().resolve`
- `RESEARCH_FIELD_ALIASES.items`
- `RESEARCH_FIELD_LABELS.values`
- `ValueError`
- `_coerce_keywords`
- `_coerce_research_text`
- `_iter_mapping_layers`
- `_normalise_research_key`
- `_parse_labelled_research_text`
- `_parse_simple_yaml`
- `any`
- `declared_fields.add`
- `isinstance`
- `json.loads`
- `layer.items`
- `path.exists`
- `path.is_file`
- `path.read_text`
- `path.suffix.lower`
- `set`
- `sorted`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_structured_research_file`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_structured_research_source`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `ValueError(f'Arquivo inexistente ou não é arquivo regular: {path}')`
- `ValueError(f'Formato não suportado. Use um destes: {allowed}')`
- `ValueError(f'Não foi possível ler o arquivo estruturado: {exc}')`
- `ValueError('O arquivo estruturado deve conter um objeto/dicionário ou campos identificados por rótulos.')`
- `ValueError(f'Não encontrei campos reconhecidos. Use rótulos como: {readable}.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.toml`
- `.json`

#### Complexidade estrutural observada

```yaml
branches: 11
loops: 3
comprehensions: 3
exception_handlers: 2
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_notify_structured_input_error`

**Assinatura:**
```python
def _notify_structured_input_error(message: str) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 830–834
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_notify_structured_input_error` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `message` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `message` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `_dialog_title`
- `_fgv_ui`
- `_fgv_ui().message`
- `print`
- `tui_theme_enabled`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_choose_project_model_destination`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_choose_project_paper_model_destination`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_keywords`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_optional_hypothesis`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_prisma_search_strategy`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_prisma_strategy_and_criteria`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_open_project_model_in_editor`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_many_choice`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_metadata_sources`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_positive_int`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_structured_paper_source`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_structured_research_file`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_structured_research_source`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_common`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:generate_interactive`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_structured_research_file`

**Assinatura:**
```python
def ask_structured_research_file(prompt: str, *, required_fields: tuple[str, ...] = ()) -> tuple[str, dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 837–852
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Solicita um arquivo e repete a seleção se não houver os campos necessários.

**Docstring resumida:** Solicita um arquivo e repete a seleção se não houver os campos necessários.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `prompt` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `prompt` recebido pela função; não há descrição individual explícita no código. |
| `required_fields` | keyword-only | `tuple[str, ...]` | `()` | Parâmetro `required_fields` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `tuple[str, dict[str, Any]]`. Retornos observados estaticamente incluem: `(raw, values)`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `', '.join`
- `_notify_structured_input_error`
- `_read_structured_research_file`
- `ask_required`
- `str`
- `values.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_structured_research_source`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 1
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_show_imported_research_values`

**Assinatura:**
```python
def _show_imported_research_values(path: str, values: dict[str, Any], fields: tuple[str, ...]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 855–865
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_show_imported_research_values` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `str` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `values` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `values` recebido pela função; não há descrição individual explícita no código. |
| `fields` | positional-or-keyword | `tuple[str, ...]` | `obrigatório/ausente` | Parâmetro `fields` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `'; '.join`
- `'\n'.join`
- `Path`
- `Path(path).expanduser`
- `_dialog_title`
- `_fgv_ui`
- `_fgv_ui().message`
- `isinstance`
- `lines.append`
- `print`
- `str`
- `tui_theme_enabled`
- `values.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_keywords`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:review_imported_research_values`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:review_imported_research_values`

**Assinatura:**
```python
def review_imported_research_values(path: str, values: dict[str, Any], fields: tuple[str, ...], *, default_edit: bool = False) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 868–885
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Permite revisar valores extraídos antes de salvá-los no TOML.

**Docstring resumida:** Permite revisar valores extraídos antes de salvá-los no TOML.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `str` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `values` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `values` recebido pela função; não há descrição individual explícita no código. |
| `fields` | positional-or-keyword | `tuple[str, ...]` | `obrigatório/ausente` | Parâmetro `fields` recebido pela função; não há descrição individual explícita no código. |
| `default_edit` | keyword-only | `bool` | `False` | Parâmetro `default_edit` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `values`; `reviewed`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_coerce_keywords`
- `_coerce_research_text`
- `_show_imported_research_values`
- `ask`
- `ask_bool`
- `ask_list`
- `dict`
- `reviewed.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_structured_research_source`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_prisma_busca_orientada`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_institution_template_path`

**Assinatura:**
```python
def _institution_template_path(data: dict[str, Any], candidates: list[str], *, description: str) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 888–908
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Localiza um modelo no diretório ``misc/<instituicao>/``.

**Docstring resumida:** Localiza um modelo no diretório ``misc/<instituicao>/``.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `data` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Estrutura de dados de entrada. |
| `candidates` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `candidates` recebido pela função; não há descrição individual explícita no código. |
| `description` | keyword-only | `str` | `obrigatório/ausente` | Parâmetro `description` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `candidate`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'\n- '.join`
- `(APP / 'misc' / institution).resolve`
- `ValueError`
- `candidate.is_file`
- `data.get`
- `slugify`
- `slugify(str(data.get('institution') or '')).strip`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_structured_paper_template_path`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_structured_research_template_path`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `ValueError('Nenhum perfil institucional foi definido para localizar o modelo estruturado.')`
- `ValueError(f"O modelo institucional de {description} não foi encontrado para o perfil '{institution}'. Esperado em:\n- {readable}")`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_structured_research_template_path`

**Assinatura:**
```python
def _structured_research_template_path(data: dict[str, Any]) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 911–916
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_structured_research_template_path` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `data` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Estrutura de dados de entrada. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `_institution_template_path(data, [STRUCTURED_RESEARCH_TEMPLATE_FILENAME], description='dados de pesquisa PRISMA')`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_institution_template_path`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_prisma_strategy_and_criteria`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_structured_research_source`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_structured_paper_template_path`

**Assinatura:**
```python
def _structured_paper_template_path(data: dict[str, Any]) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 919–925
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_structured_paper_template_path` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `data` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Estrutura de dados de entrada. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `_institution_template_path(data, [f'{STRUCTURED_PAPER_TEMPLATE_BASENAME}_{institution}.toml', f'{STRUCTURED_PAPER_TEMPLATE_BASENAME}.toml'], description='diretrizes de paper')`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_institution_template_path`
- `data.get`
- `slugify`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_structured_paper_source`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.toml`

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_open_project_model_in_editor`

**Assinatura:**
```python
def _open_project_model_in_editor(path: Path) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 928–969
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Abre uma cópia de modelo no editor configurado, com fallback guiado.

**Docstring resumida:** Abre uma cópia de modelo no editor configurado, com fallback guiado.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `False`; `True`.

#### Efeitos colaterais e fronteiras externas

- Executa processo ou ferramenta externa.
- Interage com o usuário ou escreve saída em terminal/interface.
- Lê configuração/credenciais do ambiente do processo.

#### Chamadas realizadas pelo corpo direto

- `_notify_structured_input_error`
- `ask_choice`
- `os.getenv`
- `shlex.split`
- `str`
- `str(os.getenv('VISUAL') or os.getenv('EDITOR') or '').strip`
- `subprocess.run`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_prisma_strategy_and_criteria`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_structured_paper_source`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_structured_research_source`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 0
exception_handlers: 2
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_toml_literal_for_research_field`

**Assinatura:**
```python
def _toml_literal_for_research_field(field: str, value: Any) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 972–976
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_toml_literal_for_research_field` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `field` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `field` recebido pela função; não há descrição individual explícita no código. |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'[' + ', '.join((json.dumps(item, ensure_ascii=False) for item in items)) + ']'`; `json.dumps(_coerce_research_text(value), ensure_ascii=False)`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `', '.join`
- `_coerce_keywords`
- `_coerce_research_text`
- `json.dumps`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_persist_research_values_in_project_model`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_persist_research_values_in_project_model`

**Assinatura:**
```python
def _persist_research_values_in_project_model(path: Path, values: dict[str, Any], fields: tuple[str, ...]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 979–1026
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Atualiza apenas os campos de [pesquisa] da cópia TOML do projeto.

**Docstring resumida:** Atualiza apenas os campos de [pesquisa] da cópia TOML do projeto.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `values` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `values` recebido pela função; não há descrição individual explícita no código. |
| `fields` | positional-or-keyword | `tuple[str, ...]` | `obrigatório/ausente` | Parâmetro `fields` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `''.join`
- `''.join(lines).rstrip`
- `_canonical_research_field`
- `_toml_literal_for_research_field`
- `assignment.match`
- `enumerate`
- `len`
- `lines.append`
- `lines[-1].endswith`
- `lines[-1].strip`
- `match.group`
- `path.exists`
- `path.read_text`
- `path.suffix.lower`
- `path.write_text`
- `range`
- `re.compile`
- `re.match`
- `set`
- `text.splitlines`
- `updated.add`
- `values.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_structured_research_source`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `Atualiza apenas os campos de [pesquisa] da cópia TOML do projeto.      O método preserva os comentários e o restante do modelo. O recurso é     restrito a .toml porque o modelo padrão distribuído pelo bundle é TOML.     `
- `.toml`

#### Complexidade estrutural observada

```yaml
branches: 10
loops: 2
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_project_model_destination`

**Assinatura:**
```python
def _project_model_destination(data: dict[str, Any], template: Path) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 1029–1032
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_project_model_destination` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `data` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Estrutura de dados de entrada. |
| `template` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `template` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `project_dir / PROJECT_RESEARCH_MODEL_FILENAME`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(data['project_dir']).resolve`
- `project_dir.mkdir`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_choose_project_model_destination`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_choose_project_model_destination`

**Assinatura:**
```python
def _choose_project_model_destination(data: dict[str, Any], template: Path) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 1035–1062
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_choose_project_model_destination` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `data` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Estrutura de dados de entrada. |
| `template` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `template` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `target`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `(Path(data['project_dir']).resolve() / raw_name).resolve`
- `Path`
- `Path(data['project_dir']).resolve`
- `_notify_structured_input_error`
- `_project_model_destination`
- `ask_choice`
- `ask_required`
- `candidate.suffix.lower`
- `shutil.copy2`
- `target.exists`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_prisma_strategy_and_criteria`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_structured_research_source`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.toml`
- `O modelo estruturado deve usar a extensão .toml.`

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_structured_research_source`

**Assinatura:**
```python
def ask_structured_research_source(data: dict[str, Any], prompt: str, *, required_fields: tuple[str, ...] = (), editable_fields: tuple[str, ...] = (), source_override: str | None = None) -> tuple[str, dict[str, Any], bool]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 1065–1120
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Escolhe arquivo existente ou uma cópia editável do modelo institucional.

**Docstring resumida:** Escolhe arquivo existente ou uma cópia editável do modelo institucional.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `data` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Estrutura de dados de entrada. |
| `prompt` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `prompt` recebido pela função; não há descrição individual explícita no código. |
| `required_fields` | keyword-only | `tuple[str, ...]` | `()` | Parâmetro `required_fields` recebido pela função; não há descrição individual explícita no código. |
| `editable_fields` | keyword-only | `tuple[str, ...]` | `()` | Parâmetro `editable_fields` recebido pela função; não há descrição individual explícita no código. |
| `source_override` | keyword-only | `str \| None` | `None` | Parâmetro `source_override` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `tuple[str, dict[str, Any], bool]`. Retornos observados estaticamente incluem: `(raw_path, values, False)`; `(str(destination), reviewed, True)`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `', '.join`
- `_choose_project_model_destination`
- `_notify_structured_input_error`
- `_open_project_model_in_editor`
- `_persist_research_values_in_project_model`
- `_read_structured_research_file`
- `_structured_research_template_path`
- `ask_choice`
- `ask_structured_research_file`
- `review_imported_research_values`
- `reviewed.get`
- `str`
- `tuple`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_keywords`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_optional_hypothesis`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_prisma_busca_orientada`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 1
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_paper_field_label`

**Assinatura:**
```python
def _paper_field_label(field: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 1146–1147
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_paper_field_label` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `field` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `field` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `PAPER_FIELD_LABELS.get(field) or RESEARCH_FIELD_LABELS.get(field) or field`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `PAPER_FIELD_LABELS.get`
- `RESEARCH_FIELD_LABELS.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_show_imported_paper_values`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_structured_paper_source`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:review_imported_paper_values`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_show_imported_paper_values`

**Assinatura:**
```python
def _show_imported_paper_values(path: str, values: dict[str, Any], fields: tuple[str, ...]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 1150–1160
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_show_imported_paper_values` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `str` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `values` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `values` recebido pela função; não há descrição individual explícita no código. |
| `fields` | positional-or-keyword | `tuple[str, ...]` | `obrigatório/ausente` | Parâmetro `fields` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `'; '.join`
- `'\n'.join`
- `Path`
- `Path(path).expanduser`
- `_dialog_title`
- `_fgv_ui`
- `_fgv_ui().message`
- `_paper_field_label`
- `isinstance`
- `lines.append`
- `print`
- `str`
- `tui_theme_enabled`
- `values.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:review_imported_paper_values`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:review_imported_paper_values`

**Assinatura:**
```python
def review_imported_paper_values(path: str, values: dict[str, Any], fields: tuple[str, ...], *, default_edit: bool = False) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 1163–1181
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Mostra e permite ajustar os dados estruturados antes de salvar o TOML.

**Docstring resumida:** Mostra e permite ajustar os dados estruturados antes de salvar o TOML.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `str` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `values` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `values` recebido pela função; não há descrição individual explícita no código. |
| `fields` | positional-or-keyword | `tuple[str, ...]` | `obrigatório/ausente` | Parâmetro `fields` recebido pela função; não há descrição individual explícita no código. |
| `default_edit` | keyword-only | `bool` | `False` | Parâmetro `default_edit` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `values`; `reviewed`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_coerce_keywords`
- `_coerce_research_text`
- `_paper_field_label`
- `_show_imported_paper_values`
- `ask`
- `ask_bool`
- `ask_list`
- `dict`
- `reviewed.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_structured_paper_source`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_paper_local_research`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_toml_literal_for_paper_field`

**Assinatura:**
```python
def _toml_literal_for_paper_field(field: str, value: Any) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 1184–1188
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_toml_literal_for_paper_field` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `field` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `field` recebido pela função; não há descrição individual explícita no código. |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'[' + ', '.join((json.dumps(item, ensure_ascii=False) for item in items)) + ']'`; `json.dumps(_coerce_research_text(value), ensure_ascii=False)`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `', '.join`
- `_coerce_keywords`
- `_coerce_research_text`
- `json.dumps`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_persist_toml_section_fields`

**Assinatura:**
```python
def _persist_toml_section_fields(path: Path, section_name: str, values: dict[str, Any], fields: tuple[str, ...], canonical_field: Any, literal: Any) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 1191–1257
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Atualiza campos de uma seção TOML preservando comentários não relacionados.

**Docstring resumida:** Atualiza campos de uma seção TOML preservando comentários não relacionados.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `section_name` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `section_name` recebido pela função; não há descrição individual explícita no código. |
| `values` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `values` recebido pela função; não há descrição individual explícita no código. |
| `fields` | positional-or-keyword | `tuple[str, ...]` | `obrigatório/ausente` | Parâmetro `fields` recebido pela função; não há descrição individual explícita no código. |
| `canonical_field` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `canonical_field` recebido pela função; não há descrição individual explícita no código. |
| `literal` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `literal` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `''.join`
- `''.join(before + new_body + after).rstrip`
- `assignment.match`
- `body[index].count`
- `canonical_field`
- `enumerate`
- `header.match`
- `len`
- `line.split`
- `lines.append`
- `lines[-1].endswith`
- `lines[-1].strip`
- `literal`
- `match.group`
- `new_body.append`
- `path.exists`
- `path.read_text`
- `path.suffix.lower`
- `path.write_text`
- `re.compile`
- `re.escape`
- `re.match`
- `rhs.count`
- `set`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_structured_paper_source`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.toml`

#### Complexidade estrutural observada

```yaml
branches: 10
loops: 4
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_project_paper_model_destination`

**Assinatura:**
```python
def _project_paper_model_destination(data: dict[str, Any], template: Path) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 1260–1263
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_project_paper_model_destination` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `data` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Estrutura de dados de entrada. |
| `template` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `template` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `project_dir / PROJECT_PAPER_MODEL_FILENAME`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(data['project_dir']).resolve`
- `project_dir.mkdir`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_choose_project_paper_model_destination`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_choose_project_paper_model_destination`

**Assinatura:**
```python
def _choose_project_paper_model_destination(data: dict[str, Any], template: Path) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 1266–1293
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_choose_project_paper_model_destination` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `data` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Estrutura de dados de entrada. |
| `template` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `template` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `target`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `(Path(data['project_dir']).resolve() / raw_name).resolve`
- `Path`
- `Path(data['project_dir']).resolve`
- `_notify_structured_input_error`
- `_project_paper_model_destination`
- `ask_choice`
- `ask_required`
- `candidate.suffix.lower`
- `shutil.copy2`
- `target.exists`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_structured_paper_source`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.toml`
- `O modelo estruturado do paper deve usar a extensão .toml.`

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_structured_paper_source`

**Assinatura:**
```python
def ask_structured_paper_source(data: dict[str, Any], prompt: str, *, required_fields: tuple[str, ...] = (), editable_fields: tuple[str, ...] = PAPER_STRUCTURED_FIELDS) -> tuple[str, dict[str, Any], bool]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 1296–1375
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Seleciona ou cria a fonte estruturada de dados e diretrizes do paper.

**Docstring resumida:** Seleciona ou cria a fonte estruturada de dados e diretrizes do paper.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `data` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Estrutura de dados de entrada. |
| `prompt` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `prompt` recebido pela função; não há descrição individual explícita no código. |
| `required_fields` | keyword-only | `tuple[str, ...]` | `()` | Parâmetro `required_fields` recebido pela função; não há descrição individual explícita no código. |
| `editable_fields` | keyword-only | `tuple[str, ...]` | `PAPER_STRUCTURED_FIELDS` | Parâmetro `editable_fields` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `tuple[str, dict[str, Any], bool]`. Retornos observados estaticamente incluem: `(raw_path, values, False)`; `(str(destination), reviewed, True)`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `', '.join`
- `_choose_project_paper_model_destination`
- `_notify_structured_input_error`
- `_open_project_model_in_editor`
- `_paper_field_label`
- `_persist_toml_section_fields`
- `_read_structured_paper_file`
- `_structured_paper_template_path`
- `ask_choice`
- `ask_required`
- `review_imported_paper_values`
- `reviewed.get`
- `str`
- `tuple`
- `values.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_paper_local_research`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 1
comprehensions: 4
exception_handlers: 2
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:set_wizard_no_clear`

**Assinatura:**
```python
def set_wizard_no_clear(value: bool) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 1385–1387
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `set_wizard_no_clear` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `bool` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `bool`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:generate_interactive`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:clear_screen`

**Assinatura:**
```python
def clear_screen() -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 1390–1397
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `clear_screen` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Executa processo ou ferramenta externa.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `os.system`
- `print`
- `tui_theme_enabled`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:wizard_header`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:wizard_header`

**Assinatura:**
```python
def wizard_header(title: str, index: int | None = None, total: int | None = None, preset: 'Preset | None' = None) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 1400–1418
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `wizard_header` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `title` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `title` recebido pela função; não há descrição individual explícita no código. |
| `index` | positional-or-keyword | `int \| None` | `None` | Parâmetro `index` recebido pela função; não há descrição individual explícita no código. |
| `total` | positional-or-keyword | `int \| None` | `None` | Parâmetro `total` recebido pela função; não há descrição individual explícita no código. |
| `preset` | positional-or-keyword | `'Preset \| None'` | `None` | Parâmetro `preset` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `clear_screen`
- `print`
- `tui_theme_enabled`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:generate_interactive`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:short_value`

**Assinatura:**
```python
def short_value(value: Any, max_len: int = 110) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 1421–1429
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `short_value` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |
| `max_len` | positional-or-keyword | `int` | `110` | Parâmetro `max_len` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `''`; `s if len(s) <= max_len else s[:max_len - 1] + '…'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'; '.join`
- `isinstance`
- `len`
- `re.sub`
- `re.sub('\\s+', ' ', s).strip`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:wizard_summary`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:wizard_summary`

**Assinatura:**
```python
def wizard_summary(data: dict[str, Any]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 1432–1538
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `wizard_summary` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `data` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Estrutura de dados de entrada. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'\n'.join(out).rstrip()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `'\n'.join(out).rstrip`
- `any`
- `data.get`
- `getattr`
- `out.append`
- `short_value`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:generate_interactive`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:show_summary`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 2
comprehensions: 4
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:show_summary`

**Assinatura:**
```python
def show_summary(data: dict[str, Any]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 1541–1548
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `show_summary` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `data` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Estrutura de dados de entrada. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `_dialog_title`
- `_fgv_ui`
- `_fgv_ui().message`
- `print`
- `tui_theme_enabled`
- `wizard_summary`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:generate_interactive`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_stage_action`

**Assinatura:**
```python
def ask_stage_action(allow_back: bool = True) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 1551–1580
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `ask_stage_action` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `allow_back` | positional-or-keyword | `bool` | `True` | Parâmetro `allow_back` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `str(selected or 'cancelar')`; `'proxima'`; `'voltar' if allow_back else 'refazer'`; `'refazer'`; `'resumo'`; `'cancelar'`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `_dialog_title`
- `_fgv_ui`
- `_fgv_ui().select_one`
- `input`
- `input('Ação [próxima/voltar/refazer/resumo/cancelar] (Enter=próxima): ').strip`
- `input('Ação [próxima/voltar/refazer/resumo/cancelar] (Enter=próxima): ').strip().lower`
- `print`
- `str`
- `tui_theme_enabled`
- `values.append`
- `values.insert`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:generate_interactive`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 10
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_final_action`

**Assinatura:**
```python
def ask_final_action() -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 1583–1604
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `ask_final_action` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `str(selected or 'cancelar')`; `'salvar'`; `'editar'`; `'cancelar'`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `_dialog_title`
- `_fgv_ui`
- `_fgv_ui().select_one`
- `input`
- `input('Salvar TOML? [s=salvar / e=editar etapa / n=cancelar] [s]: ').strip`
- `input('Salvar TOML? [s=salvar / e=editar etapa / n=cancelar] [s]: ').strip().lower`
- `print`
- `str`
- `tui_theme_enabled`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:generate_interactive`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_stage_number`

**Assinatura:**
```python
def ask_stage_number(total: int) -> int
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 1607–1622
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `ask_stage_number` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `total` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `total` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `int(selected)`; `int(raw) - 1`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `SystemExit`
- `_dialog_title`
- `_fgv_ui`
- `_fgv_ui().select_one`
- `input`
- `input(f'Qual etapa deseja editar? [1-{total}]: ').strip`
- `int`
- `print`
- `range`
- `raw.isdigit`
- `tui_theme_enabled`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:generate_interactive`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `SystemExit('Geração de TOML cancelada pelo usuário.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:list_institutions`

**Assinatura:**
```python
def list_institutions() -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 1624–1629
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Lista a responsabilidade representada por `institutions` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `['fgv']`; `items or ['fgv']`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `base.exists`
- `base.iterdir`
- `p.is_dir`
- `sorted`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_common`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:load_institution_profile_toml`

**Assinatura:**
```python
def load_institution_profile_toml(institution: str) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 1632–1640
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Carrega a responsabilidade representada por `institution_profile_toml` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `institution` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `institution` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `{}`; `tomllib.load(f)`.

#### Efeitos colaterais e fronteiras externas

- Abre arquivo para leitura ou inspeção.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `path.exists`
- `path.open`
- `tomllib.load`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:available_layouts_for_institution`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:default_layout_for_preset`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `institution_profile.toml`

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:available_layouts_for_institution`

**Assinatura:**
```python
def available_layouts_for_institution(institution: str) -> dict[str, dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 1643–1646
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `available_layouts_for_institution` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `institution` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `institution` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, dict[str, Any]]`. Retornos observados estaticamente incluem: `{str(k): v for k, v in layouts.items() if isinstance(v, dict)}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `isinstance`
- `layouts.items`
- `load_institution_profile_toml`
- `profile.get`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_common`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:latex_class_for_layout`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:default_layout_for_preset`

**Assinatura:**
```python
def default_layout_for_preset(institution: str, preset: 'Preset') -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 1649–1665
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `default_layout_for_preset` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `institution` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `institution` recebido pela função; não há descrição individual explícita no código. |
| `preset` | positional-or-keyword | `'Preset'` | `obrigatório/ausente` | Parâmetro `preset` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `val`; `layout_id`; `{'atividade': 'atividade_fgv', 'paper': 'paper_fgv', 'dissertacao': 'dissertacao_fgv'}.get(genero, 'paper_fgv')`; `genero`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `content_types.get`
- `content_types['resumo_artigos'].get`
- `isinstance`
- `layouts.items`
- `load_institution_profile_toml`
- `profile.get`
- `spec.get`
- `str`
- `str(content_types['resumo_artigos'].get('default_layout') or '').strip`
- `str(spec.get('genero_academico') or spec.get('genero') or '').strip`
- `str(spec.get('genero_academico') or spec.get('genero') or '').strip().lower`
- `{'atividade': 'atividade_fgv', 'paper': 'paper_fgv', 'dissertacao': 'dissertacao_fgv'}.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_common`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 7
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:latex_class_for_layout`

**Assinatura:**
```python
def latex_class_for_layout(institution: str, layout_id: str, genero: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 1668–1675
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `latex_class_for_layout` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `institution` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `institution` recebido pela função; não há descrição individual explícita no código. |
| `layout_id` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `layout_id` recebido pela função; não há descrição individual explícita no código. |
| `genero` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `genero` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `val`; `'fgv-dissertacao' if institution == 'fgv' else 'article'`; `'fgv-paper' if institution == 'fgv' else 'article'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `available_layouts_for_institution`
- `available_layouts_for_institution(institution).get`
- `spec.get`
- `str`
- `str(spec.get('classe_latex') or spec.get('latex_class') or '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._activity_data`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_common`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:choose_preset`

**Assinatura:**
```python
def choose_preset() -> Preset
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 1803–1822
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `choose_preset` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `Preset`. Retornos observados estaticamente incluem: `PRESETS[n - 1]`; `choose_advanced_preset()`; `by_key[choice]`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `ask`
- `choice.isdigit`
- `choose_advanced_preset`
- `enumerate`
- `int`
- `len`
- `print`
- `textwrap.fill`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:generate_interactive`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 2
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:choose_advanced_preset`

**Assinatura:**
```python
def choose_advanced_preset() -> Preset
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 1825–1843
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `choose_advanced_preset` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `Preset`. Retornos observados estaticamente incluem: `Preset(key='custom_avancado', label='Custom avançado', description='Perfil customizado montado por componentes.', document_type=document_type, local_corpus=local_corpus, prisma_report=prisma_report, executar_documento=executar_documento, executar_pesquisa=executar_pesquisa, default_toml=default_toml)`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `Preset`
- `ask_bool`
- `ask_choice`
- `print`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:choose_preset`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `_config.toml`
- `relatorio_prisma_config.toml`

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_common`

**Assinatura:**
```python
def collect_common(preset: Preset) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 1851–1925
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Coleta a responsabilidade representada por `common` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `preset` | positional-or-keyword | `Preset` | `obrigatório/ausente` | Parâmetro `preset` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `data`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `'; '.join`
- `(project_dir / candidate.name).resolve`
- `Path`
- `Path(raw_project_dir).expanduser`
- `Path(toml_name).expanduser`
- `_notify_structured_input_error`
- `ask`
- `ask('Nome do arquivo TOML (vazio para usar o padrão interno)', '').strip`
- `ask_choice`
- `ask_required`
- `available_layouts_for_institution`
- `candidate.is_absolute`
- `candidate.suffix.casefold`
- `default_layout_for_preset`
- `iter`
- `latex_class_for_layout`
- `layouts.items`
- `layouts.keys`
- `len`
- `list`
- `list_institutions`
- `next`
- `print`
- `project_dir.mkdir`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:generate_interactive.stage_common`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.toml`
- `O arquivo final deve ter a extensão .toml.`

#### Complexidade estrutural observada

```yaml
branches: 12
loops: 2
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_metadata`

**Assinatura:**
```python
def collect_metadata(data: dict[str, Any]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 1928–1967
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Coleta a responsabilidade representada por `metadata` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `data` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Estrutura de dados de entrada. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `ask`
- `data.get`
- `date.today`
- `print`
- `str`
- `{'atividade': 'Atividade acadêmica', 'paper': 'Título do paper', 'dissertacao': 'Título da dissertação: subtítulo se houver', 'relatorio_prisma': 'Relatório de Pesquisa PRISMA'}.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:generate_interactive`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_print_default_block`

**Assinatura:**
```python
def _print_default_block(title: str, value: str) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 1997–1999
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Exibe a responsabilidade representada por `default_block` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `title` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `title` recebido pela função; não há descrição individual explícita no código. |
| `value` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `print`
- `textwrap.fill`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_research`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_parse_ai_keyword_response`

**Assinatura:**
```python
def _parse_ai_keyword_response(content: Any) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 2002–2015
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Interpreta a responsabilidade representada por `ai_keyword_response` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `content` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `content` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `[]`; `_coerce_keywords(payload[key])`; `_coerce_keywords(payload)`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `_coerce_keywords`
- `isinstance`
- `json.loads`
- `re.sub`
- `re.sub('^```(?:json)?\\s*|\\s*```$', '', text, flags=re.IGNORECASE | re.DOTALL).strip`
- `str`
- `str(content or '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:suggest_prisma_keywords_with_ai`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:suggest_prisma_keywords_with_ai`

**Assinatura:**
```python
def suggest_prisma_keywords_with_ai(context: dict[str, Any]) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 2018–2061
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Gera termos candidatos no wizard e devolve-os para revisão explícita.

**Docstring resumida:** Gera termos candidatos no wizard e devolve-os para revisão explícita.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `context` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Contexto explícito de dependências utilizado para desacoplar a rotina de imports/globais. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `keywords`.

#### Efeitos colaterais e fronteiras externas

- Lê configuração/credenciais do ambiente do processo.
- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `OpenAI`
- `RuntimeError`
- `_parse_ai_keyword_response`
- `client.chat.completions.create`
- `context.get`
- `load_dotenv`
- `os.getenv`
- `os.getenv('OPENAI_API_KEY', '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_keywords`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('A sugestão por IA requer as dependências openai e python-dotenv do ambiente Pipenv.')`
- `RuntimeError('OPENAI_API_KEY não foi encontrada no ambiente ou no arquivo .env.')`
- `RuntimeError(f'Não foi possível obter sugestões de palavras-chave pela IA: {exc}')`
- `RuntimeError('A IA não retornou palavras-chave utilizáveis.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 0
exception_handlers: 2
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_optional_hypothesis`

**Assinatura:**
```python
def _collect_optional_hypothesis(data: dict[str, Any], *, primary_path: str, primary_values: dict[str, Any]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 2064–2112
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Coleta a responsabilidade representada por `optional_hypothesis` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `data` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Estrutura de dados de entrada. |
| `primary_path` | keyword-only | `str` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `primary_values` | keyword-only | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `primary_values` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `_coerce_research_text`
- `_notify_structured_input_error`
- `ask_bool`
- `ask_choice`
- `ask_required`
- `ask_structured_research_source`
- `choices.insert`
- `imported.get`
- `primary_values.get`
- `rel_for_toml`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_prisma_busca_orientada`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_keywords`

**Assinatura:**
```python
def _collect_keywords(data: dict[str, Any], *, primary_path: str, primary_values: dict[str, Any]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 2115–2175
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Coleta a responsabilidade representada por `keywords` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `data` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Estrutura de dados de entrada. |
| `primary_path` | keyword-only | `str` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `primary_values` | keyword-only | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `primary_values` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `_coerce_keywords`
- `_notify_structured_input_error`
- `_show_imported_research_values`
- `ask_bool`
- `ask_choice`
- `ask_list`
- `ask_structured_research_source`
- `choices.insert`
- `imported.get`
- `primary_values.get`
- `rel_for_toml`
- `str`
- `suggest_prisma_keywords_with_ai`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_prisma_busca_orientada`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 8
loops: 1
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_balanced_prisma_keyword_blocks`

**Assinatura:**
```python
def _balanced_prisma_keyword_blocks(keywords: list[str], *, groups: int = 3) -> list[list[str]]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 2178–2196
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Distribui termos em blocos curtos para consultas independentes.

**Docstring resumida:** Distribui termos em blocos curtos para consultas independentes.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `keywords` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `keywords` recebido pela função; não há descrição individual explícita no código. |
| `groups` | keyword-only | `int` | `3` | Parâmetro `groups` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[list[str]]`. Retornos observados estaticamente incluem: `[]`; `[chunk for chunk in chunks if chunk]`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `chunks.append`
- `divmod`
- `int`
- `len`
- `max`
- `min`
- `range`
- `str`
- `str(item).strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_default_prisma_search_blocks`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_default_prisma_search_blocks`

**Assinatura:**
```python
def _default_prisma_search_blocks(data: dict[str, Any]) -> list[dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 2199–2253
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Propõe blocos complementares com consultas curtas e editáveis.

**Docstring resumida:** Propõe blocos complementares com consultas curtas e editáveis.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `data` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Estrutura de dados de entrada. |

#### Retorno

Anotação declarada: `list[dict[str, Any]]`. Retornos observados estaticamente incluem: `[{'id': 'incapacidade_avaliacao', 'rotulo': 'Benefícios por incapacidade e avaliação médica', 'consultas': ['disability benefits', 'work disability', 'medical disability assessment']}, {'id': 'analise_documental_telepericia', 'rotulo': 'Análise documental e avaliação remota', 'consultas': ['documentary review', 'telemedicine', 'telehealth', 'remote medical assessment']}, {'id': 'filas_capacidade_alocacao', 'rotulo': 'Filas, capacidade e alocação de serviços', 'consultas': ['waiting time', 'backlog', 'service capacity', 'case allocation']}]`; `[{'id': f'bloco_{index}', 'rotulo': f'Bloco temático {index}', 'consultas': chunk} for index, chunk in enumerate(chunks, 1)]`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `' '.join([str(data.get('tema') or ''), *keywords]).casefold`
- `_balanced_prisma_keyword_blocks`
- `any`
- `data.get`
- `enumerate`
- `str`
- `str(data.get('tema') or '').strip`
- `str(item).strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_prisma_search_strategy`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 3
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_coerce_text_list`

**Assinatura:**
```python
def _coerce_text_list(value: Any) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 2269–2284
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Normaliza listas textuais sem quebrar critérios que contenham vírgulas.

**Docstring resumida:** Normaliza listas textuais sem quebrar critérios que contenham vírgulas.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `[]`; `result`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `isinstance`
- `item.casefold`
- `item.strip`
- `re.split`
- `result.append`
- `seen.add`
- `set`
- `str`
- `str(item).strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_apply_structured_prisma_protocol`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_normalise_prisma_block`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_read_structured_prisma_protocol_file`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_render_prisma_protocol_model_section`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_normalise_prisma_block`

**Assinatura:**
```python
def _normalise_prisma_block(value: Any, index: int) -> dict[str, Any] | None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 2287–2317
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_normalise_prisma_block` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |
| `index` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `index` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, Any] | None`. Retornos observados estaticamente incluem: `None`; `{'id': identifier or f'bloco_{index}', 'rotulo': label, 'consultas': queries}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_coerce_text_list`
- `_normalise_research_key`
- `any`
- `isinstance`
- `normalised.get`
- `query.casefold`
- `slugify`
- `str`
- `str(normalised.get('id') or normalised.get('identificador') or '').strip`
- `str(normalised.get('rotulo') or normalised.get('label') or normalised.get('nome') or normalised.get('titulo') or '').strip`
- `value.items`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_read_structured_prisma_protocol_file`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 3
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_protocol_layers`

**Assinatura:**
```python
def _protocol_layers(payload: Any) -> list[dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 2320–2339
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Prioriza seções de protocolo, sem deixar de aceitar arquivos simples.

**Docstring resumida:** Prioriza seções de protocolo, sem deixar de aceitar arquivos simples.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `payload` | positional-or-keyword | `Any` | `obrigatório/ausente` | Payload estruturado recebido ou produzido pela operação. |

#### Retorno

Anotação declarada: `list[dict[str, Any]]`. Retornos observados estaticamente incluem: `[]`; `result`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_iter_mapping_layers`
- `_normalise_research_key`
- `add`
- `isinstance`
- `payload.items`
- `set`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_read_structured_prisma_protocol_file`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_protocol_layers.add`

**Assinatura:**
```python
def add(item: Any) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 2327–2330
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `add` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `item` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `item` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `id`
- `isinstance`
- `result.append`
- `seen.add`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_read_structured_prisma_protocol_file`

**Assinatura:**
```python
def _read_structured_prisma_protocol_file(raw_path: str, *, allow_declared_empty_fields: bool = False) -> tuple[Path, dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 2342–2439
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Lê critérios e estratégia de busca de um arquivo estruturado.

**Docstring resumida:** Lê critérios e estratégia de busca de um arquivo estruturado.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `raw_path` | positional-or-keyword | `str` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `allow_declared_empty_fields` | keyword-only | `bool` | `False` | Parâmetro `allow_declared_empty_fields` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `tuple[Path, dict[str, Any]]`. Retornos observados estaticamente incluem: `(path, values)`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `', '.join`
- `Path`
- `Path(str(raw_path or '')).expanduser`
- `Path(str(raw_path or '')).expanduser().resolve`
- `ValueError`
- `_coerce_text_list`
- `_normalise_prisma_block`
- `_normalise_research_key`
- `_parse_simple_yaml`
- `_protocol_layers`
- `aliases.items`
- `bool`
- `declared.add`
- `enumerate`
- `isinstance`
- `json.loads`
- `layer.items`
- `next`
- `path.exists`
- `path.is_file`
- `path.read_text`
- `path.suffix.lower`
- `set`
- `sorted`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_prisma_strategy_and_criteria`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `ValueError(f'Arquivo inexistente ou não é arquivo regular: {path}')`
- `ValueError(f'Formato não suportado. Use um destes: {allowed}')`
- `ValueError(f'Não foi possível ler o arquivo estruturado: {exc}')`
- `ValueError('O protocolo estruturado deve conter um objeto/dicionário.')`
- `ValueError('Não encontrei critérios ou estratégia de busca. Use [busca_prisma] com criterios_inclusao, criterios_exclusao e [[busca_prisma.estrategias]].')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.toml`
- `.json`

#### Complexidade estrutural observada

```yaml
branches: 19
loops: 2
comprehensions: 4
exception_handlers: 2
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_coerce_protocol_limit`

**Assinatura:**
```python
def _coerce_protocol_limit(value: Any, *, label: str, default: int, minimum: int, maximum: int) -> int
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 2442–2451
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_coerce_protocol_limit` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |
| `label` | keyword-only | `str` | `obrigatório/ausente` | Parâmetro `label` recebido pela função; não há descrição individual explícita no código. |
| `default` | keyword-only | `int` | `obrigatório/ausente` | Parâmetro `default` recebido pela função; não há descrição individual explícita no código. |
| `minimum` | keyword-only | `int` | `obrigatório/ausente` | Parâmetro `minimum` recebido pela função; não há descrição individual explícita no código. |
| `maximum` | keyword-only | `int` | `obrigatório/ausente` | Parâmetro `maximum` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `default`; `number`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `ValueError`
- `int`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_apply_structured_prisma_protocol`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `ValueError(f'{label} deve ser um número inteiro entre {minimum} e {maximum}.')`
- `ValueError(f'{label} deve estar entre {minimum} e {maximum}.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_show_imported_prisma_protocol`

**Assinatura:**
```python
def _show_imported_prisma_protocol(path: str, values: dict[str, Any]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 2454–2474
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_show_imported_prisma_protocol` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `str` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `values` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `values` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `'; '.join`
- `'\n'.join`
- `Path`
- `Path(path).expanduser`
- `_dialog_title`
- `_fgv_ui`
- `_fgv_ui().message`
- `block.get`
- `lines.append`
- `lines.extend`
- `print`
- `str`
- `tui_theme_enabled`
- `values.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_prisma_strategy_and_criteria`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_apply_structured_prisma_protocol`

**Assinatura:**
```python
def _apply_structured_prisma_protocol(data: dict[str, Any], path: str, values: dict[str, Any], *, source: str) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 2477–2514
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Aplica a responsabilidade representada por `structured_prisma_protocol` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `data` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Estrutura de dados de entrada. |
| `path` | positional-or-keyword | `str` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `values` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `values` recebido pela função; não há descrição individual explícita no código. |
| `source` | keyword-only | `str` | `obrigatório/ausente` | Origem dos dados ou arquivo de entrada. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `ValueError`
- `_coerce_protocol_limit`
- `_coerce_text_list`
- `int`
- `list`
- `min`
- `rel_for_toml`
- `str`
- `str(values.get('consulta_geral') or '').strip`
- `str(values.get('estrategia_busca') or '').strip`
- `values.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_prisma_strategy_and_criteria`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `ValueError('A estratégia por blocos exige ao menos um bloco com rótulo e uma consulta.')`
- `ValueError('A estratégia de consulta única exige consulta_geral preenchida.')`
- `ValueError("Defina estrategia = 'blocos_tematicos' ou 'consulta_unica' no arquivo estruturado.")`
- `ValueError('O arquivo estruturado deve conter ao menos um critério de inclusão e um de exclusão.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 9
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_manual_prisma_criteria`

**Assinatura:**
```python
def _collect_manual_prisma_criteria(data: dict[str, Any]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 2517–2523
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Coleta a responsabilidade representada por `manual_prisma_criteria` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `data` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Estrutura de dados de entrada. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `ask_list`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_guided_prisma_protocol_values`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_prisma_strategy_and_criteria`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_toml_array_literal`

**Assinatura:**
```python
def _toml_array_literal(values: list[str], indent: str = '') -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 2527–2530
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_toml_array_literal` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `values` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `values` recebido pela função; não há descrição individual explícita no código. |
| `indent` | positional-or-keyword | `str` | `''` | Parâmetro `indent` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'[]'`; `'[\n' + ',\n'.join((f'{indent}  {json.dumps(item, ensure_ascii=False)}' for item in values)) + f'\n{indent}]'`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `',\n'.join`
- `json.dumps`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_render_prisma_protocol_model_section`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_render_prisma_protocol_model_section`

**Assinatura:**
```python
def _render_prisma_protocol_model_section(values: dict[str, Any]) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 2533–2556
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Renderiza a seção editável do modelo, sem incluir credenciais.

**Docstring resumida:** Renderiza a seção editável do modelo, sem incluir credenciais.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `values` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `values` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `lines`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `_coerce_text_list`
- `_toml_array_literal`
- `block.get`
- `int`
- `isinstance`
- `json.dumps`
- `lines.extend`
- `list`
- `str`
- `values.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_persist_prisma_protocol_in_project_model`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_persist_prisma_protocol_in_project_model`

**Assinatura:**
```python
def _persist_prisma_protocol_in_project_model(path: Path, values: dict[str, Any]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 2559–2593
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Substitui somente o bloco [busca_prisma] da cópia do projeto.

**Docstring resumida:** Substitui somente o bloco [busca_prisma] da cópia do projeto.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `values` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `values` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `''.join`
- `''.join(lines + rendered).rstrip`
- `''.join(lines[:start] + rendered + lines[end:]).rstrip`
- `_render_prisma_protocol_model_section`
- `any_header.match`
- `child_header.match`
- `enumerate`
- `header_name.startswith`
- `len`
- `lines.append`
- `lines[-1].strip`
- `match.group`
- `match.group(1).strip`
- `match.group(1).strip().casefold`
- `path.exists`
- `path.read_text`
- `path.suffix.lower`
- `path.write_text`
- `re.compile`
- `section_header.match`
- `source.splitlines`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_prisma_strategy_and_criteria`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.toml`

#### Complexidade estrutural observada

```yaml
branches: 9
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_guided_prisma_protocol_values`

**Assinatura:**
```python
def _collect_guided_prisma_protocol_values(data: dict[str, Any]) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 2596–2609
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Mantém uma via assistida quando EDITOR/VISUAL não estiver configurado.

**Docstring resumida:** Mantém uma via assistida quando EDITOR/VISUAL não estiver configurado.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `data` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Estrutura de dados de entrada. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `{'estrategia_busca': temporary.get('estrategia_busca'), 'consulta_geral': temporary.get('consulta_geral', ''), 'blocos_busca': temporary.get('blocos_busca', []), 'limite_por_base': temporary.get('limite_por_base'), 'limite_scopus_por_consulta': temporary.get('limite_scopus_por_consulta'), 'criterios_inclusao': temporary.get('criterios_inclusao', []), 'criterios_exclusao': temporary.get('criterios_exclusao', [])}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_collect_manual_prisma_criteria`
- `_collect_prisma_search_strategy`
- `dict`
- `temporary.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_prisma_strategy_and_criteria`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_prisma_strategy_and_criteria`

**Assinatura:**
```python
def _collect_prisma_strategy_and_criteria(data: dict[str, Any], *, primary_path: str = '') -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 2612–2652
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Coleta a responsabilidade representada por `prisma_strategy_and_criteria` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `data` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Estrutura de dados de entrada. |
| `primary_path` | keyword-only | `str` | `''` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`; `_collect_prisma_strategy_and_criteria(data, primary_path=primary_path)`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `_apply_structured_prisma_protocol`
- `_choose_project_model_destination`
- `_collect_guided_prisma_protocol_values`
- `_collect_manual_prisma_criteria`
- `_collect_prisma_search_strategy`
- `_collect_prisma_strategy_and_criteria`
- `_notify_structured_input_error`
- `_open_project_model_in_editor`
- `_persist_prisma_protocol_in_project_model`
- `_read_structured_prisma_protocol_file`
- `_show_imported_prisma_protocol`
- `_structured_research_template_path`
- `ask_choice`
- `ask_required`
- `choices.insert`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_prisma_strategy_and_criteria`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_prisma_busca_orientada`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 0
comprehensions: 0
exception_handlers: 2
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_prisma_search_strategy`

**Assinatura:**
```python
def _collect_prisma_search_strategy(data: dict[str, Any]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 2655–2708
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Coleta estratégia reprodutível sem juntar todas as palavras-chave.

**Docstring resumida:** Coleta estratégia reprodutível sem juntar todas as palavras-chave.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `data` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Estrutura de dados de entrada. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`; `_collect_prisma_search_strategy(data)`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `' '.join(data.get('palavras_chave', [])).strip`
- `'; '.join`
- `_collect_prisma_search_strategy`
- `_default_prisma_search_blocks`
- `_normalise_research_key`
- `_notify_structured_input_error`
- `ask`
- `ask_bool`
- `ask_choice`
- `ask_positive_int`
- `ask_required`
- `blocks.append`
- `data.get`
- `enumerate`
- `int`
- `item.get`
- `label.strip`
- `min`
- `print`
- `raw_queries.split`
- `re.sub`
- `re.sub('[^a-z0-9_]+', '_', _normalise_research_key(label)).strip`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_guided_prisma_protocol_values`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_prisma_search_strategy`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_prisma_strategy_and_criteria`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_prisma_busca_orientada`

**Assinatura:**
```python
def collect_prisma_busca_orientada(data: dict[str, Any]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 2711–2833
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Coleta exclusiva do perfil novo; não altera o fluxo legado de PRISMA.

**Docstring resumida:** Coleta exclusiva do perfil novo; não altera o fluxo legado de PRISMA.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `data` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Estrutura de dados de entrada. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `_collect_keywords`
- `_collect_optional_hypothesis`
- `_collect_prisma_strategy_and_criteria`
- `ask`
- `ask_bool`
- `ask_choice`
- `ask_list`
- `ask_many_choice`
- `ask_positive_int`
- `ask_structured_research_source`
- `bool`
- `data.get`
- `primary_values.get`
- `print`
- `provider_selection_choices`
- `provider_statuses`
- `rel_for_toml`
- `review_imported_research_values`
- `str`
- `str(data.get('tipo_estudo') or '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_research`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 8
loops: 2
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_set_default_paper_directives`

**Assinatura:**
```python
def _set_default_paper_directives(data: dict[str, Any]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 2836–2843
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_set_default_paper_directives` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `data` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Estrutura de dados de entrada. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `data.setdefault`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_paper_local_research`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_paper_structured_bool`

**Assinatura:**
```python
def _paper_structured_bool(value: Any, default: bool) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 2848–2856
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_paper_structured_bool` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |
| `default` | positional-or-keyword | `bool` | `obrigatório/ausente` | Parâmetro `default` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `value`; `True`; `False`; `default`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `isinstance`
- `str`
- `str(value or '').strip`
- `str(value or '').strip().casefold`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_apply_paper_structured_render_config`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_paper_structured_list`

**Assinatura:**
```python
def _paper_structured_list(value: Any) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 2859–2864
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_paper_structured_list` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `[]`; `[str(item).strip() for item in value if str(item).strip()]`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `isinstance`
- `str`
- `str(item).strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_apply_paper_structured_render_config`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_paper_structured_int`

**Assinatura:**
```python
def _paper_structured_int(value: Any, default: int, low: int, high: int) -> int
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 2867–2872
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_paper_structured_int` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |
| `default` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `default` recebido pela função; não há descrição individual explícita no código. |
| `low` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `low` recebido pela função; não há descrição individual explícita no código. |
| `high` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `high` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `max(low, min(candidate, high))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `int`
- `max`
- `min`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_apply_paper_structured_render_config`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_read_paper_structured_render_config`

**Assinatura:**
```python
def _read_paper_structured_render_config(raw_path: str) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 2875–2897
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Lê somente as seções de saída/resumo do modelo estruturado do paper.

**Docstring resumida:** Lê somente as seções de saída/resumo do modelo estruturado do paper.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `raw_path` | positional-or-keyword | `str` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `{}`; `payload if isinstance(payload, dict) else {}`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(str(raw_path or '')).expanduser`
- `Path(str(raw_path or '')).expanduser().resolve`
- `isinstance`
- `json.loads`
- `path.exists`
- `path.is_file`
- `path.read_text`
- `path.suffix.lower`
- `str`
- `tomllib.loads`
- `yaml.safe_load`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_apply_paper_structured_render_config`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.toml`
- `.json`

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 0
comprehensions: 0
exception_handlers: 2
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_apply_paper_structured_render_config`

**Assinatura:**
```python
def _apply_paper_structured_render_config(data: dict[str, Any], raw_path: str) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 2900–2929
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Importa título e escolhas de saída do mesmo TOML estruturado do paper.

**Docstring resumida:** Importa título e escolhas de saída do mesmo TOML estruturado do paper.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `data` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Estrutura de dados de entrada. |
| `raw_path` | positional-or-keyword | `str` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_paper_structured_bool`
- `_paper_structured_int`
- `_paper_structured_list`
- `_read_paper_structured_render_config`
- `abstracts.get`
- `data.get`
- `isinstance`
- `metadata.get`
- `output.get`
- `payload.get`
- `str`
- `str(abstracts.get('principal') or data.get('idioma_principal_saida') or 'pt-BR').strip`
- `str(metadata.get('subtitulo') or '').strip`
- `str(metadata.get('titulo_trabalho') or metadata.get('titulo') or '').strip`
- `str(output.get('principal') or 'pt-BR').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_paper_local_research`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 8
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_paper_local_research`

**Assinatura:**
```python
def collect_paper_local_research(data: dict[str, Any]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 2931–2994
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Coleta dados e diretrizes do ``paper_local_fgv`` sem alterar outros perfis.

**Docstring resumida:** Coleta dados e diretrizes do ``paper_local_fgv`` sem alterar outros perfis.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `data` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Estrutura de dados de entrada. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `_apply_paper_structured_render_config`
- `_set_default_paper_directives`
- `ask`
- `ask_bool`
- `ask_choice`
- `ask_list`
- `ask_structured_paper_source`
- `data.get`
- `print`
- `rel_for_toml`
- `review_imported_paper_values`
- `str`
- `str(data.get('tipo_estudo') or '').strip`
- `str(data.get('tom_de_redacao') or '').strip`
- `values.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_research`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 7
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_research`

**Assinatura:**
```python
def collect_research(data: dict[str, Any]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 2997–3142
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Coleta a responsabilidade representada por `research` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `data` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Estrutura de dados de entrada. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `_print_default_block`
- `ask`
- `ask_bool`
- `ask_choice`
- `ask_list`
- `ask_required`
- `collect_paper_local_research`
- `collect_prisma_busca_orientada`
- `print`
- `rel_for_toml`
- `str`
- `str(data['resumo_nivel_detalhamento']).strip`
- `str(data['resumo_nivel_detalhamento']).strip().lower`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:generate_interactive`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 16
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_sources`

**Assinatura:**
```python
def collect_sources(data: dict[str, Any]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 3144–3266
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Coleta a responsabilidade representada por `sources` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `data` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Estrutura de dados de entrada. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(doi).expanduser`
- `Path(prompt_path).expanduser`
- `Path(prompt_path).expanduser().exists`
- `ask`
- `ask('Formato do corpus local (zip/dir)', 'zip').strip`
- `ask('Formato do corpus local (zip/dir)', 'zip').strip().lower`
- `ask_bool`
- `ask_choice`
- `ask_required`
- `data.get`
- `data.setdefault`
- `doi.strip`
- `doi_path.exists`
- `doi_path.is_absolute`
- `extra_prompts.append`
- `maybe_copy_to_project`
- `mode_label.startswith`
- `orient.strip`
- `orient_paths.append`
- `orient_paths.extend`
- `print`
- `prompt_path.strip`
- `rel_for_toml`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:generate_interactive`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `Caminho do document.json existente`
- `doi_manifest.csv`
- `Caminho do doi_manifest.csv (vazio se não houver)`
- `Criar doi_manifest.csv vazio nesse caminho?`
- `prompt_base_resumo_artigos.txt`
- `prompt_base_atividade.txt`
- `prompt_extra.txt`

#### Complexidade estrutural observada

```yaml
branches: 23
loops: 1
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_paper_abstracts`

**Assinatura:**
```python
def _collect_paper_abstracts(data: dict[str, Any]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 3276–3318
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Configura resumo obrigatório no idioma principal e versões adicionais opcionais.

**Docstring resumida:** Configura resumo obrigatório no idioma principal e versões adicionais opcionais.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `data` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Estrutura de dados de entrada. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `ask_bool`
- `ask_choice`
- `ask_required`
- `bool`
- `data.get`
- `data.setdefault`
- `list`
- `print`
- `selected.append`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_outputs_and_options`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `Os resumos serão sintetizados a partir do document.json canônico já validado. A escolha de resumo adicional não cria uma versão integral traduzida do paper.`

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_collect_paper_language_versions`

**Assinatura:**
```python
def _collect_paper_language_versions(data: dict[str, Any]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 3321–3358
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Configura versões adicionais traduzidas sem duplicar a geração do paper.

**Docstring resumida:** Configura versões adicionais traduzidas sem duplicar a geração do paper.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `data` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Estrutura de dados de entrada. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `ask_bool`
- `ask_choice`
- `ask_required`
- `bool`
- `data.get`
- `data.setdefault`
- `list`
- `print`
- `selected.append`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_outputs_and_options`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `As versões adicionais serão traduzidas a partir do document.json canônico já gerado. As referências, citações, DOI, URLs, fórmulas, números, siglas e nomes próprios serão preservados.`

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_outputs_and_options @ L3361`

**Assinatura:**
```python
def collect_outputs_and_options(data: dict[str, Any]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 3361–3511
**Tipo:** function
**Visibilidade:** pública por nome
**Status de binding estático:** definição anterior no mesmo namespace; é candidata a ser sombreada pela redefinição posterior em `L4911` sob execução sequencial normal do módulo.
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Coleta a responsabilidade representada por `outputs_and_options` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `data` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Estrutura de dados de entrada. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(raw_document_output).expanduser`
- `_collect_paper_abstracts`
- `_collect_paper_language_versions`
- `ask`
- `ask_bool`
- `ask_choice`
- `ask_metadata_sources`
- `bool`
- `bundle_rel`
- `data.get`
- `document_output.resolve`
- `print`
- `raw_document_output.strip`
- `rel_for_toml`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `prisma_report.json existente (vazio se não houver)`

#### Complexidade estrutural observada

```yaml
branches: 12
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:document_type_name`

**Assinatura:**
```python
def document_type_name(tipo: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 3519–3528
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `document_type_name` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `tipo` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `tipo` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'Resumo analítico de artigos'`; `'Atividade acadêmica'`; `'Dissertação'`; `'Relatório de Pesquisa PRISMA'`; `'Paper acadêmico'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

Nenhum identificado pela análise estática.

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:render_toml`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:prompt_paths_for_type`

**Assinatura:**
```python
def prompt_paths_for_type(data: dict[str, Any]) -> dict[str, list[str]]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 3531–3570
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Resolve os prompts do bundle relativamente ao TOML ativo.

**Docstring resumida:** Resolve os prompts do bundle relativamente ao TOML ativo.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `data` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Estrutura de dados de entrada. |

#### Retorno

Anotação declarada: `dict[str, list[str]]`. Retornos observados estaticamente incluem: `{'paper_paths': paper, 'atividade_paths': atividade, 'resumo_artigos_paths': resumo_artigos, 'dissertacao_paths': dissertacao, 'prisma_paths': prisma}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `bundle_rel`
- `data.get`
- `prisma.append`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:render_toml`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `prompts/document/atividade.txt`
- `prompts/document/resumo_artigos.txt`
- `prompts/document/paper.txt`
- `prompts/document/dissertacao.txt`
- `prompts/prisma/relatorio_prisma.txt`

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:build_orientacao_inline`

**Assinatura:**
```python
def build_orientacao_inline(data: dict[str, Any]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 3572–3640
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Constrói a responsabilidade representada por `orientacao_inline` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `data` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Estrutura de dados de entrada. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'\n'.join(parts)`; `''`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `' → '.join`
- `'; '.join`
- `'\n'.join`
- `_coerce_paper_list`
- `bool`
- `data.get`
- `parts.append`
- `str`
- `str(data.get('instrucoes_adicionais') or '').strip`
- `str(data.get('limites_do_escopo') or '').strip`
- `str(data.get('orientacao_geral_inline') or '').strip`
- `str(data.get('orientacao_professor') or '').strip`
- `str(data.get('orientacoes_metodologicas') or '').strip`
- `str(data.get('tese_central') or '').strip`
- `str(data.get('tom_de_redacao') or '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:render_toml`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 19
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:render_toml @ L3642`

**Assinatura:**
```python
def render_toml(data: dict[str, Any]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 3642–4054
**Tipo:** function
**Visibilidade:** pública por nome
**Status de binding estático:** definição anterior no mesmo namespace; é candidata a ser sombreada pela redefinição posterior em `L4963` sob execução sequencial normal do módulo.
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Renderiza a responsabilidade representada por `toml` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `data` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Estrutura de dados de entrada. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `rendered_toml`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.
- Lê arquivos ou artefatos do filesystem.
- Lê configuração/credenciais do ambiente do processo.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `'\n'.join(lines).rstrip`
- `RuntimeError`
- `block.get`
- `bool`
- `build_orientacao_inline`
- `bundle_rel`
- `data.get`
- `document_type_name`
- `int`
- `isinstance`
- `lines.append`
- `min`
- `os.getenv`
- `prompt_paths_for_type`
- `rel_for_toml`
- `str`
- `str(block.get('consulta') or '').strip`
- `str(block.get('consulta')).strip`
- `str(data.get('resumo_max_chars_por_documento') or 14000).strip`
- `str(data.get('resumo_max_chars_total_corpus') or 95000).strip`
- `str(data.get('resumo_min_palavras_comparacao') or 1100).strip`
- `str(data.get('resumo_min_palavras_por_artigo') or 1200).strip`
- `str(data.get('resumo_min_palavras_sintese') or 900).strip`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('O assistente gerou um TOML inválido antes de salvar o arquivo. Nenhum TOML foi escrito; revise a configuração do wizard.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `# Quando true, o pipeline gera o document.json em chamadas separadas:`
- `# Isso exibe progresso no terminal e salva checkpoints .checkpoint_*.json no output.`
- `# A tradução atua no document.json canônico; não recria análise, corpus ou bibliografia.`
- `# O resumo é sintetizado do document.json validado e inserido no ORG, PDF e DOCX.`
- `# O .bib é neutro. O estilo visual é aplicado na renderização.`
- `/docx/reference_fgv.docx`
- `misc/fgv.png`
- `prompts/global/orientacao_geral_execucao.txt`
- `institution_paths = ["profile://prompts/fgv_geral.txt"]`
- `prompts/research/triagem_prompt.txt`
- `prompts/research/diretivas_extras.txt`

#### Complexidade estrutural observada

```yaml
branches: 25
loops: 2
comprehensions: 7
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:generate_interactive`

**Assinatura:**
```python
def generate_interactive(non_interactive_profile: str | None = None, project_name: str | None = None, no_clear: bool = False, project_dir: str | Path | None = None) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 4062–4231
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Executa o gerador em modo assistente por etapas.

**Docstring resumida:** Executa o gerador em modo assistente por etapas.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `non_interactive_profile` | positional-or-keyword | `str \| None` | `None` | Perfil institucional ou preset utilizado pela operação. |
| `project_name` | positional-or-keyword | `str \| None` | `None` | Parâmetro `project_name` recebido pela função; não há descrição individual explícita no código. |
| `no_clear` | positional-or-keyword | `bool` | `False` | Parâmetro `no_clear` recebido pela função; não há descrição individual explícita no código. |
| `project_dir` | positional-or-keyword | `str \| Path \| None` | `None` | Parâmetro `project_dir` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `config_path`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `', '.join`
- `'\n'.join`
- `(project_dir / candidate.name).resolve`
- `Path`
- `Path(alt).expanduser`
- `Path(data['config_path']).expanduser`
- `Path(data['config_path']).expanduser().resolve`
- `Path(data['project_dir']).expanduser`
- `Path(data['project_dir']).expanduser().resolve`
- `Path(raw).expanduser`
- `Path(str(raw)).expanduser`
- `RuntimeError`
- `SystemExit`
- `_fgv_ui`
- `_fgv_ui().message`
- `_notify_structured_input_error`
- `ask`
- `ask('Informe outro nome de arquivo (vazio para config_novo.toml)', '').strip`
- `ask_bool`
- `ask_final_action`
- `ask_stage_action`
- `ask_stage_number`
- `candidate.is_absolute`
- `candidate.resolve`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.command_dispatch:dispatch_stage_004`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError(f"Preset não encontrado: {non_interactive_profile}. Opções: {', '.join(preset_map)}")`
- `SystemExit('Geração de TOML cancelada pelo usuário.')`
- `RuntimeError(f'O TOML final deve ficar dentro do diretório do projeto. Diretório do projeto: {project_dir}; destino informado: {config_path}.')`
- `RuntimeError("O TOML final não pode sobrescrever o arquivo estruturado usado como fonte de pesquisa. Escolha um nome distinto, por exemplo 'prisma_fluxo_pmf.toml', dentro da pasta do projeto.")`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `O TOML final não pode sobrescrever o arquivo estruturado usado como fonte de pesquisa. Escolha um nome distinto, por exemplo 'prisma_fluxo_pmf.toml', dentro da pasta do projeto.`
- `Informe outro nome de arquivo (vazio para config_novo.toml)`
- `config_novo.toml`
- `.toml`
- `O arquivo final deve ter a extensão .toml.`
- `CAMINHO/document.document.json`

#### Complexidade estrutural observada

```yaml
branches: 27
loops: 7
comprehensions: 1
exception_handlers: 2
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:generate_interactive.stage_common`

**Assinatura:**
```python
def stage_common() -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 4084–4088
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `stage_common` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `collect_common`
- `data.clear`
- `data.update`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:print_profiles`

**Assinatura:**
```python
def print_profiles() -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 4233–4237
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Exibe a responsabilidade representada por `profiles` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `print`
- `textwrap.fill`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:main`

**Assinatura:**
```python
def main(argv: list[str] | None = None) -> int
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 4240–4259
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa o ponto de entrada correspondente e coordena o fluxo associado.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `list[str] \| None` | `None` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `0`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `argparse.ArgumentParser`
- `bool`
- `generate_interactive`
- `parser.add_argument`
- `parser.parse_args`
- `print_profiles`
- `set_tui_theme`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `--list-profiles`
- `--no-clear`
- `--profile`
- `--project-dir`
- `--project-name`
- `--tui-theme`

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_wiz_norm`

**Assinatura:**
```python
def _wiz_norm(value: object) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 4278–4285
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_wiz_norm` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `object` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `''.join((char for char in _unicodedata.normalize('NFD', text) if _unicodedata.category(char) != 'Mn'))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `''.join`
- `_unicodedata.category`
- `_unicodedata.normalize`
- `str`
- `str(value or '').strip`
- `str(value or '').strip().lower`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_WizInputController._ask_text_ready_file_and_optional_reuse`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_WizInputController._ensure_reference_policy`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_WizInputController._prepare_known_corpus`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_WizInputController.ask`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_WizLocalFlowState.__init__`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_wiz_run_generate_interactive_with_local_flow`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_wiz_default_from_prompt`

**Assinatura:**
```python
def _wiz_default_from_prompt(prompt: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 4288–4292
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_wiz_default_from_prompt` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `prompt` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `prompt` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `values[-1].strip() if values else ''`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_re.findall`
- `values[-1].strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_WizInputController.ask`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_wiz_toml_section_set`

**Assinatura:**
```python
def _wiz_toml_section_set(text: str, section: str, key: str, literal: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 4295–4318
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Define uma chave TOML sem alterar as demais chaves da seção.

**Docstring resumida:** Define uma chave TOML sem alterar as demais chaves da seção.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |
| `section` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `section` recebido pela função; não há descrição individual explícita no código. |
| `key` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `key` recebido pela função; não há descrição individual explícita no código. |
| `literal` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `literal` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `section_re.sub(_replace, text, count=1)`; `text + suffix + f'\n[{section}]\n{key} = {literal}\n'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_re.compile`
- `_re.escape`
- `section_re.search`
- `section_re.sub`
- `text.endswith`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_wiz_toml_set_bool`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_wiz_toml_set_str`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_wiz_toml_section_set._replace`

**Assinatura:**
```python
def _replace(match: object) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 4303–4313
**Tipo:** nested-function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `_replace` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `match` | positional-or-keyword | `object` | `obrigatório/ausente` | Parâmetro `match` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `header + body`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_re.compile`
- `_re.escape`
- `body.endswith`
- `key_re.search`
- `key_re.sub`
- `match.group`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_wiz_toml_set_bool`

**Assinatura:**
```python
def _wiz_toml_set_bool(text: str, section: str, key: str, value: bool) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 4321–4322
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_wiz_toml_set_bool` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |
| `section` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `section` recebido pela função; não há descrição individual explícita no código. |
| `key` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `key` recebido pela função; não há descrição individual explícita no código. |
| `value` | positional-or-keyword | `bool` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `_wiz_toml_section_set(text, section, key, 'true' if value else 'false')`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_wiz_toml_section_set`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_wiz_apply_text_ready_policy`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_wiz_disable_references`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_wiz_toml_set_str`

**Assinatura:**
```python
def _wiz_toml_set_str(text: str, section: str, key: str, value: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 4325–4327
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_wiz_toml_set_str` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |
| `section` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `section` recebido pela função; não há descrição individual explícita no código. |
| `key` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `key` recebido pela função; não há descrição individual explícita no código. |
| `value` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `_wiz_toml_section_set(text, section, key, f'"{escaped}"')`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `_wiz_toml_section_set`
- `value.replace`
- `value.replace('\\', '\\\\').replace`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_wiz_apply_state_to_toml`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_wiz_apply_text_ready_policy`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_wiz_disable_references`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_wiz_append_orientation`

**Assinatura:**
```python
def _wiz_append_orientation(text: str, instruction: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 4330–4364
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Acrescenta uma instrução ao [orientacoes].inline sem duplicá-la.

**Docstring resumida:** Acrescenta uma instrução ao [orientacoes].inline sem duplicá-la.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |
| `instruction` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `instruction` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `text`; `section_re.sub(_replace, text, count=1)`; `text + suffix + f'\n[orientacoes]\ninline = "{escaped}"\n'`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `_re.compile`
- `instruction.replace`
- `instruction.replace('\\', '\\\\').replace`
- `section_re.search`
- `section_re.sub`
- `text.endswith`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_wiz_apply_text_ready_policy`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_wiz_disable_references`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_wiz_append_orientation._replace`

**Assinatura:**
```python
def _replace(match: object) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 4339–4358
**Tipo:** nested-function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `_replace` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `match` | positional-or-keyword | `object` | `obrigatório/ausente` | Parâmetro `match` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `header + body[:triple.start()] + rendered + body[triple.end():]`; `header + body[:single.start()] + rendered + body[single.end():]`; `header + body + f'inline = "{escaped}"\n'`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `_re.compile`
- `body.endswith`
- `instruction.replace`
- `instruction.replace('\\', '\\\\').replace`
- `match.group`
- `single.end`
- `single.group`
- `single.group(2).rstrip`
- `single.start`
- `single_re.search`
- `triple.end`
- `triple.group`
- `triple.group(2).rstrip`
- `triple.start`
- `triple_re.search`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_wiz_is_local_toml`

**Assinatura:**
```python
def _wiz_is_local_toml(text: str) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 4367–4373
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_wiz_is_local_toml` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `bool(_re.search('(?m)^\\s*modo_entrada\\s*=\\s*"documentos_locais"\\s*$', text) or '[documentos_locais]' in text)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_re.search`
- `bool`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_wiz_apply_state_to_toml`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_wiz_run_generate_interactive_with_local_flow._write_text_wrapper`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_wiz_disable_references @ L4376`

**Assinatura:**
```python
def _wiz_disable_references(text: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 4376–4405
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Status de binding estático:** definição anterior no mesmo namespace; é candidata a ser sombreada pela redefinição posterior em `L5000` sob execução sequencial normal do módulo.
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_wiz_disable_references` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `_wiz_append_orientation(text, 'Não inclua citações no corpo do texto, notas bibliográficas, seção Referências, lista bibliográfica ou arquivo .bib. Use exclusivamente as informações fornecidas no corpus local e não invente fontes, autores, obras, links ou dados.')`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_wiz_append_orientation`
- `_wiz_toml_set_bool`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `Não inclua citações no corpo do texto, notas bibliográficas, seção Referências, lista bibliográfica ou arquivo .bib. Use exclusivamente as informações fornecidas no corpus local e não invente fontes, autores, obras, links ou dados.`

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_wiz_apply_text_ready_policy`

**Assinatura:**
```python
def _wiz_apply_text_ready_policy(text: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 4408–4418
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_wiz_apply_text_ready_policy` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `_wiz_append_orientation(text, 'O corpus local foi fornecido como texto pronto. Preserve a estrutura, os argumentos, as distinções e as ressalvas do texto-base. Limite-se à revisão, organização e adequação ao formato institucional; não acrescente dados, exemplos, fontes ou interpretações externas.')`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_wiz_append_orientation`
- `_wiz_toml_set_bool`
- `_wiz_toml_set_str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_wiz_apply_state_to_toml`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_WizLocalFlowState.__init__`

**Assinatura:**
```python
def __init__(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 4422–4443
**Tipo:** method
**Visibilidade:** especial/dunder
**Classe proprietária:** `_WizLocalFlowState`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `__init__` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `_wiz_norm`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_WizLocalFlowState.local`

**Assinatura:**
```python
def local(self) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 4446–4447
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `_WizLocalFlowState`
**Decoradores:** `property`
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `local` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `self.local_candidate or self.local_confirmed`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

Nenhum identificado pela análise estática.

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_WizInputController.__init__`

**Assinatura:**
```python
def __init__(self, original_input: object, state: _WizLocalFlowState) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 4453–4456
**Tipo:** method
**Visibilidade:** especial/dunder
**Classe proprietária:** `_WizInputController`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `__init__` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `original_input` | positional-or-keyword | `object` | `obrigatório/ausente` | Parâmetro `original_input` recebido pela função; não há descrição individual explícita no código. |
| `state` | positional-or-keyword | `_WizLocalFlowState` | `obrigatório/ausente` | Parâmetro `state` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

Nenhum identificado pela análise estática.

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_WizInputController.ask`

**Assinatura:**
```python
def ask(self, prompt: str = '') -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 4458–4603
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `_WizInputController`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `ask` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `prompt` | positional-or-keyword | `str` | `''` | Parâmetro `prompt` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `answer`; `'fgv'`; `answer.strip() or 'atividade_fgv'`; `answer.strip() or default_name`; `aliases.get(_wiz_norm(answer), answer)`; `self.ask(prompt)`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `_Path`
- `_Path(rendered).expanduser`
- `_wiz_default_from_prompt`
- `_wiz_norm`
- `aliases.get`
- `answer.strip`
- `default_name.endswith`
- `normalized.startswith`
- `print`
- `self._ask_and_prepare_corpus`
- `self._ask_text_ready_file_and_optional_reuse`
- `self._ensure_reference_policy`
- `self._print_skip_notice`
- `self.ask`
- `self.original_input`
- `self.original_input('[Enter] Continuar | [v] Voltar | [e] Editar etapa | [r] Resumo | [c] Cancelar: ').strip`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.toml`

#### Complexidade estrutural observada

```yaml
branches: 34
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_WizInputController._ask_supported_file`

**Assinatura:**
```python
def _ask_supported_file(self, label: str) -> object
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 4605–4621
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `_WizInputController`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_ask_supported_file` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `label` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `label` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `object`. Retornos observados estaticamente incluem: `candidate.resolve()`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `', '.join`
- `_Path`
- `_Path(raw).expanduser`
- `candidate.exists`
- `candidate.is_file`
- `candidate.resolve`
- `candidate.suffix.lower`
- `print`
- `self.original_input`
- `self.original_input(label).strip`
- `sorted`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_WizInputController._ask_text_ready_file_and_optional_reuse`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_WizInputController._ask_text_ready_file_and_optional_reuse`

**Assinatura:**
```python
def _ask_text_ready_file_and_optional_reuse(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 4623–4638
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `_WizInputController`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_ask_text_ready_file_and_optional_reuse` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `_wiz_norm`
- `print`
- `self._ask_supported_file`
- `self._prepare_known_corpus`
- `self.original_input`
- `self.original_input('Usar este mesmo arquivo de texto pronto como corpus local? [S]: ').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_WizInputController.ask`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `Caminho do texto pronto (.txt/.md/.org/.docx/.pdf): `

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_WizInputController._ensure_reference_policy`

**Assinatura:**
```python
def _ensure_reference_policy(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 4640–4650
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `_WizInputController`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Garante a responsabilidade representada por `reference_policy` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `_wiz_norm`
- `print`
- `self.original_input`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `Configuração sem referências selecionada. Perguntas de DOI, estilo, .bib e mapa mental serão ignoradas.`

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_WizInputController._print_skip_notice`

**Assinatura:**
```python
def _print_skip_notice(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 4652–4655
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `_WizInputController`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Exibe a responsabilidade representada por `skip_notice` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `print`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_WizInputController.ask`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_WizInputController._ask_and_prepare_corpus`

**Assinatura:**
```python
def _ask_and_prepare_corpus(self) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 4657–4683
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `_WizInputController`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_ask_and_prepare_corpus` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `self._prepare_known_corpus(candidate.resolve(), self.state.corpus_mode)`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `_Path`
- `_Path(raw).expanduser`
- `candidate.exists`
- `candidate.is_dir`
- `candidate.is_file`
- `candidate.resolve`
- `candidate.suffix.lower`
- `print`
- `self._prepare_known_corpus`
- `self.original_input`
- `self.original_input('Caminho do conteúdo local (arquivo/zip/dir): ').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_WizInputController.ask`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `Para 'arquivo', informe um .pdf, .docx, .txt, .md ou .org existente.`

#### Complexidade estrutural observada

```yaml
branches: 7
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_WizInputController._prepare_known_corpus`

**Assinatura:**
```python
def _prepare_known_corpus(self, candidate: object, corpus_mode: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 4685–4748
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `_WizInputController`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_prepare_known_corpus` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `candidate` | positional-or-keyword | `object` | `obrigatório/ausente` | Parâmetro `candidate` recebido pela função; não há descrição individual explícita no código. |
| `corpus_mode` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `corpus_mode` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `str(candidate)`; `str(destination)`; `str(candidate.parent)`; `str(link_dir)`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `RuntimeError`
- `_Path`
- `_Path(candidate).resolve`
- `_Path(project_dir).expanduser`
- `_Path.cwd`
- `_shutil.copy2`
- `_shutil.copytree`
- `_wiz_norm`
- `destination.mkdir`
- `destination.resolve`
- `destination.resolve().relative_to`
- `link_dir.mkdir`
- `link_path.exists`
- `link_path.is_symlink`
- `link_path.symlink_to`
- `link_path.unlink`
- `print`
- `self.original_input`
- `self.original_input('Armazenar uma cópia do conteúdo-base dentro do projeto? [s]: ').strip`
- `str`
- `target.exists`
- `target.resolve`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_WizInputController._ask_and_prepare_corpus`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_WizInputController._ask_text_ready_file_and_optional_reuse`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('O diretório do projeto está dentro do diretório do corpus. Escolha outro diretório de projeto ou não copie o corpus para dentro dele.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 7
loops: 0
comprehensions: 0
exception_handlers: 2
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_wiz_apply_state_to_toml`

**Assinatura:**
```python
def _wiz_apply_state_to_toml(text: str, state: _WizLocalFlowState) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 4751–4765
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_wiz_apply_state_to_toml` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |
| `state` | positional-or-keyword | `_WizLocalFlowState` | `obrigatório/ausente` | Parâmetro `state` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `text`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_wiz_apply_text_ready_policy`
- `_wiz_disable_references`
- `_wiz_is_local_toml`
- `_wiz_toml_set_str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_wiz_run_generate_interactive_with_local_flow._write_text_wrapper`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_wiz_run_generate_interactive_with_local_flow`

**Assinatura:**
```python
def _wiz_run_generate_interactive_with_local_flow(target: object, *args: object, **kwargs: object) -> object
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 4768–4827
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Aplica o fluxo local ao ponto de entrada real do wizard.

**Docstring resumida:** Aplica o fluxo local ao ponto de entrada real do wizard.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `target` | positional-or-keyword | `object` | `obrigatório/ausente` | Destino ou alvo da operação. |
| `args` | var-positional | `object` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `kwargs` | var-keyword | `object` | `obrigatório/ausente` | Parâmetro `kwargs` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `object`. Retornos observados estaticamente incluem: `target(*args, **kwargs)`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `_WizInputController`
- `_WizLocalFlowState`
- `_wiz_norm`
- `kwargs.get`
- `set`
- `str`
- `target`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_generate_interactive_with_wizard_documentos_locais`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_wiz_run_generate_interactive_with_local_flow._input_wrapper`

**Assinatura:**
```python
def _input_wrapper(prompt: str = '') -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 4799–4800
**Tipo:** nested-function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `_input_wrapper` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `prompt` | positional-or-keyword | `str` | `''` | Parâmetro `prompt` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `controller.ask(prompt)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `controller.ask`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_wiz_run_generate_interactive_with_local_flow._write_text_wrapper`

**Assinatura:**
```python
def _write_text_wrapper(path: object, data: object, *write_args: object, **write_kwargs: object) -> int
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 4802–4819
**Tipo:** nested-function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Grava a responsabilidade representada por `text_wrapper` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `object` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `data` | positional-or-keyword | `object` | `obrigatório/ausente` | Estrutura de dados de entrada. |
| `write_args` | var-positional | `object` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `write_kwargs` | var-keyword | `object` | `obrigatório/ausente` | Parâmetro `write_kwargs` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `original_write_text(path, rendered, *write_args, **write_kwargs)`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `_wiz_apply_state_to_toml`
- `_wiz_is_local_toml`
- `controller._ensure_reference_policy`
- `isinstance`
- `original_write_text`
- `path.resolve`
- `processed_paths.add`
- `str`
- `str(path).lower`
- `str(path).lower().endswith`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.toml`

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_generate_interactive_with_wizard_documentos_locais`

**Assinatura:**
```python
def _generate_interactive_with_wizard_documentos_locais(*args: object, **kwargs: object) -> object
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 4833–4841
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Gera a responsabilidade representada por `interactive_with_wizard_documentos_locais` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | var-positional | `object` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `kwargs` | var-keyword | `object` | `obrigatório/ausente` | Parâmetro `kwargs` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `object`. Retornos observados estaticamente incluem: `_wiz_run_generate_interactive_with_local_flow(_generate_interactive_before_wizard_documentos_locais, *args, **kwargs)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_wiz_run_generate_interactive_with_local_flow`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_is_local_document`

**Assinatura:**
```python
def _is_local_document(data: dict[str, Any]) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 4858–4864
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Verifica se `local_document` conforme as regras codificadas na rotina.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `data` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Estrutura de dados de entrada. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `bool(getattr(preset, 'local_corpus', False) and (not getattr(preset, 'render_only', False)) and (str(getattr(preset, 'key', '')) != 'relatorio_prisma_busca_orientada_fgv'))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `bool`
- `data.get`
- `getattr`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_outputs_and_options`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:render_toml`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_reference_default`

**Assinatura:**
```python
def _reference_default(data: dict[str, Any]) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 4867–4871
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Atividade local começa sem referências; paper/dissertação preservam o padrão acadêmico.

**Docstring resumida:** Atividade local começa sem referências; paper/dissertação preservam o padrão acadêmico.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `data` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Estrutura de dados de entrada. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `key in {'paper_local_fgv', 'paper_prisma_fgv', 'dissertacao_local_fgv', 'dissertacao_prisma_fgv', 'resumo_artigos_local_fgv'}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `data.get`
- `getattr`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_configure_reference_policy`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_normalise_prompt`

**Assinatura:**
```python
def _normalise_prompt(value: object) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 4874–4881
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_normalise_prompt` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `object` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `''.join((char for char in _unicodedata.normalize('NFD', raw) if _unicodedata.category(char) != 'Mn'))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `''.join`
- `_unicodedata.category`
- `_unicodedata.normalize`
- `str`
- `str(value or '').strip`
- `str(value or '').strip().casefold`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_outputs_and_options.policy_bool`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_outputs_and_options.policy_choice`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_configure_reference_policy`

**Assinatura:**
```python
def _configure_reference_policy(data: dict[str, Any]) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 4884–4904
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Pergunta uma única vez e armazena a decisão no próprio estado do wizard.

**Docstring resumida:** Pergunta uma única vez e armazena a decisão no próprio estado do wizard.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `data` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Estrutura de dados de entrada. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `value`; `bool(value)`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `_reference_default`
- `ask_bool`
- `bool`
- `print`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_outputs_and_options`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `- Política sem referências selecionada: citações, .bib, DOI, estilo bibliográfico e mapa mental após referências serão desativados no TOML.`

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_outputs_and_options @ L4911`

**Assinatura:**
```python
def collect_outputs_and_options(data: dict[str, Any]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 4911–4956
**Tipo:** function
**Visibilidade:** pública por nome
**Status de binding estático:** última definição textual desse nome qualificado; é candidata ao binding final após importação sequencial. Definições anteriores: `L3361`.
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Coleta saídas e suprime perguntas bibliográficas quando a política for sem referências.

**Docstring resumida:** Coleta saídas e suprime perguntas bibliográficas quando a política for sem referências.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `data` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Estrutura de dados de entrada. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `_captured_collect_outputs_and_options(data)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_captured_collect_outputs_and_options`
- `_configure_reference_policy`
- `_is_local_document`
- `globals`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_outputs_and_options.policy_bool`

**Assinatura:**
```python
def policy_bool(prompt: str, default: bool = True) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 4925–4935
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `policy_bool` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `prompt` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `prompt` recebido pela função; não há descrição individual explícita no código. |
| `default` | positional-or-keyword | `bool` | `True` | Parâmetro `default` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `False`; `original_bool(prompt, default)`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `_normalise_prompt`
- `any`
- `original_bool`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_outputs_and_options.policy_choice`

**Assinatura:**
```python
def policy_choice(prompt: str, choices: list[str], default: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 4937–4940
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `policy_choice` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `prompt` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `prompt` recebido pela função; não há descrição individual explícita no código. |
| `choices` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `choices` recebido pela função; não há descrição individual explícita no código. |
| `default` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `default` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'abnt'`; `original_choice(prompt, choices, default)`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `_normalise_prompt`
- `original_choice`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:render_toml @ L4963`

**Assinatura:**
```python
def render_toml(data: dict[str, Any]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 4963–4972
**Tipo:** function
**Visibilidade:** pública por nome
**Status de binding estático:** última definição textual desse nome qualificado; é candidata ao binding final após importação sequencial. Definições anteriores: `L3642`.
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Renderiza valores coerentes, sem depender de mutação posterior do arquivo.

**Docstring resumida:** Renderiza valores coerentes, sem depender de mutação posterior do arquivo.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `data` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Estrutura de dados de entrada. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `text`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `RuntimeError`
- `_captured_render_toml`
- `_is_local_document`
- `_wiz_disable_references`
- `bool`
- `data.get`
- `tomllib.loads`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('A política de referências gerou TOML inválido.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_ensure_reference_policy`

**Assinatura:**
```python
def _ensure_reference_policy(self: object) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 4981–4986
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Garante a responsabilidade representada por `reference_policy` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `object` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`; `_captured_wiz_input_ensure_reference_policy(self)`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `_captured_wiz_input_ensure_reference_policy`
- `bool`
- `globals`
- `globals().get`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_wiz_disable_references @ L5000`

**Assinatura:**
```python
def _wiz_disable_references(text: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_toml_generator_interativo.py`
**Linhas:** 5000–5010
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Status de binding estático:** última definição textual desse nome qualificado; é candidata ao binding final após importação sequencial. Definições anteriores: `L4376`.
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_wiz_disable_references` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `text`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_captured_wiz_disable_references`
- `_wiz_toml_set_bool`
- `_wiz_toml_set_str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c43e38c469a46d1a2aa9c80c622b6cb42a2890f5e1b0e49cbcbb798c1f92ceff`
**LOC do arquivo-fonte:** 5024


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.__init__`

**Assinatura:**
```python
def __init__(self, *, no_clear: bool = False) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 64–68
**Tipo:** method
**Visibilidade:** especial/dunder
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `__init__` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `no_clear` | keyword-only | `bool` | `False` | Parâmetro `no_clear` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `bool`
- `self._load_state`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI._load_state`

**Assinatura:**
```python
def _load_state(self) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 74–81
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Carrega a responsabilidade representada por `state` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `{}`; `value if isinstance(value, dict) else {}`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `STATE_PATH.exists`
- `STATE_PATH.read_text`
- `isinstance`
- `json.loads`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.__init__`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI._save_state`

**Assinatura:**
```python
def _save_state(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 83–85
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Salva a responsabilidade representada por `state` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `STATE_PATH.parent.mkdir`
- `STATE_PATH.write_text`
- `json.dumps`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI._ask_article_final_settings`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.create_or_edit_briefing`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.run_prisma_ai_cut`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.set_active_config`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.active_config`

**Assinatura:**
```python
def active_config(self) -> Path | None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 87–92
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `active_config` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path | None`. Retornos observados estaticamente incluem: `None`; `path.resolve() if path.exists() and path.is_file() else None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(raw).expanduser`
- `path.exists`
- `path.is_file`
- `path.resolve`
- `self.state.get`
- `str`
- `str(self.state.get('active_config') or '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI._article_workflow`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI._global_project_context_text`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.choose_existing_config`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.list_project_configs`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.menu_prisma_article_flow`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.require_config`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.run_configured_flow`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.run_guided_flow`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.run_new_document_flow`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.set_active_config`

**Assinatura:**
```python
def set_active_config(self, path: Path | None) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 94–104
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `set_active_config` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `path` | positional-or-keyword | `Path \| None` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `known.insert`
- `path.resolve`
- `self._save_state`
- `self.state.get`
- `self.state.pop`
- `str`
- `str(x).strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.choose_existing_config`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.menu_project`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.run_wizard`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.load_toml`

**Assinatura:**
```python
def load_toml(path: Path) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 107–110
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** `staticmethod`
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Carrega a responsabilidade representada por `toml` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `data if isinstance(data, dict) else {}`.

#### Efeitos colaterais e fronteiras externas

- Abre arquivo para leitura ou inspeção.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `isinstance`
- `path.open`
- `tomllib.load`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI._global_project_context_text`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.cfg_output_dir`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.cfg_research_output_dir`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.choose_existing_config`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.generate_full`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.generate_prisma_final_from_review`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.import_prisma_triage`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.input_rows`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.is_project_config`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.open_prisma_review_xlsx`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.recompile_org`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.rerender_existing`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.run_prisma_ai_cut`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.section`

**Assinatura:**
```python
def section(cfg: dict[str, Any], name: str) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 113–115
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** `staticmethod`
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `section` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `name` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `name` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `value if isinstance(value, dict) else {}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `cfg.get`
- `isinstance`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.resolve_config_path`

**Assinatura:**
```python
def resolve_config_path(config_path: Path, raw: Any) -> Path | None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 118–125
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** `staticmethod`
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Resolve a responsabilidade representada por `config_path` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `config_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `raw` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `raw` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path | None`. Retornos observados estaticamente incluem: `None`; `path.resolve()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(value).expanduser`
- `path.is_absolute`
- `path.resolve`
- `str`
- `str(raw or '').strip`
- `value.startswith`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.cfg_output_dir`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.cfg_research_output_dir`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.input_rows.add_path`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.is_project_config`

**Assinatura:**
```python
def is_project_config(cls, path: Path) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 128–133
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** `classmethod`
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Verifica se `project_config` conforme as regras codificadas na rotina.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cls` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `cls` recebido pela função; não há descrição individual explícita no código. |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `False`; `bool(cls.section(cfg, 'projeto')) and bool(cls.section(cfg, 'documento'))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `bool`
- `cls.load_toml`
- `cls.section`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.is_activity_config`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.list_project_configs`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.run_wizard`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.is_activity_config`

**Assinatura:**
```python
def is_activity_config(cls, path: Path) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 136–138
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** `classmethod`
**Base da explicação:** docstring + AST

**Finalidade:** Compatibilidade com integrações antigas da rc10.7.39.

**Docstring resumida:** Compatibilidade com integrações antigas da rc10.7.39.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cls` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `cls` recebido pela função; não há descrição individual explícita no código. |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `cls.is_project_config(path)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `cls.is_project_config`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.list_project_configs`

**Assinatura:**
```python
def list_project_configs(self) -> list[Path]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 140–153
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Lista a responsabilidade representada por `project_configs` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[Path]`. Retornos observados estaticamente incluem: `sorted(values, key=lambda p: (p.stat().st_mtime, str(p)), reverse=True)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(str(raw)).expanduser`
- `base.exists`
- `base.rglob`
- `candidates.add`
- `candidates.update`
- `p.is_file`
- `p.resolve`
- `p.stat`
- `path.exists`
- `path.is_file`
- `path.resolve`
- `self.active_config`
- `self.is_project_config`
- `self.state.get`
- `set`
- `sorted`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.choose_existing_config`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.list_activity_configs`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `*.toml`

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.list_activity_configs`

**Assinatura:**
```python
def list_activity_configs(self) -> list[Path]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 155–156
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Lista a responsabilidade representada por `activity_configs` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[Path]`. Retornos observados estaticamente incluem: `self.list_project_configs()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `self.list_project_configs`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.cfg_output_dir`

**Assinatura:**
```python
def cfg_output_dir(self, config_path: Path, cfg: dict[str, Any] | None = None) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 158–165
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `cfg_output_dir` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `config_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `cfg` | positional-or-keyword | `dict[str, Any] \| None` | `None` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `base / prefix if bool(paths.get('create_document_subdir', True)) else base`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `bool`
- `paths.get`
- `project.get`
- `self.load_toml`
- `self.resolve_config_path`
- `self.section`
- `str`
- `str(paths.get('document_prefix') or project.get('nome') or 'documento').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI._global_project_context_text`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.import_prisma_triage`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.list_output_files`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.recompile_org`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.rerender_existing`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.cfg_prefix`

**Assinatura:**
```python
def cfg_prefix(self, cfg: dict[str, Any]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 167–170
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `cfg_prefix` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `str(paths.get('document_prefix') or project.get('nome') or 'documento').strip() or 'documento'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `paths.get`
- `project.get`
- `self.section`
- `str`
- `str(paths.get('document_prefix') or project.get('nome') or 'documento').strip`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.input_rows`

**Assinatura:**
```python
def input_rows(self, config_path: Path, cfg: dict[str, Any] | None = None) -> list[tuple[str, str, str]]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 176–241
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `input_rows` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `config_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `cfg` | positional-or-keyword | `dict[str, Any] \| None` | `None` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `list[tuple[str, str, str]]`. Retornos observados estaticamente incluem: `rows`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `activity.get`
- `add_path`
- `bool`
- `enumerate`
- `isinstance`
- `local.get`
- `orientations.get`
- `project.get`
- `prompts.get`
- `research.get`
- `rows.append`
- `self.load_toml`
- `self.section`
- `self.section(cfg, 'documento').get`
- `str`
- `str(activity.get('fonte_dados_atividade') or '').strip`
- `str(local.get('input_dir') or '').strip`
- `str(local.get('input_zip') or '').strip`
- `str(orientations.get('inline') or '').strip`
- `str(research.get(key) or '').strip`
- `str(self.section(cfg, 'documento').get('tipo_documento') or '').strip`
- `str(self.section(cfg, 'documento').get('tipo_documento') or '').strip().lower`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI._global_project_context_text`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.show_inputs`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 9
loops: 4
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.input_rows.add_path`

**Assinatura:**
```python
def add_path(label: str, raw: Any, *, required: bool = False) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 184–193
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `add_path` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `label` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `label` recebido pela função; não há descrição individual explícita no código. |
| `raw` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `raw` recebido pela função; não há descrição individual explícita no código. |
| `required` | keyword-only | `bool` | `False` | Parâmetro `required` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `path.exists`
- `rows.append`
- `self.resolve_config_path`
- `str`
- `str(raw or '').strip`
- `value.startswith`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.input_rows`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI._global_project_context_text`

**Assinatura:**
```python
def _global_project_context_text(self) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 244–283
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Resumo do TOML global usado pelo fluxo tradicional da pipeline.

**Docstring resumida:** Resumo do TOML global usado pelo fluxo tradicional da pipeline.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'Contexto global da pipeline:\nProjeto global selecionado: nenhum\nTOML global: —\nSaída global: —'`; `f'Contexto global da pipeline:\nTOML global ilegível: {path}\nErro: {exc}'`; `f'Contexto global da pipeline:\nProjeto global selecionado: {name}\nTítulo global: {title}\nTOML global: {path}\nInsumos pendentes do TOML global: {missing}\nArtefatos na saída global: {artifacts}\nSaída global: {out_dir}'`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `document.get`
- `len`
- `out_dir.exists`
- `out_dir.glob`
- `p.is_file`
- `project.get`
- `self.active_config`
- `self.cfg_output_dir`
- `self.input_rows`
- `self.load_toml`
- `self.section`
- `self.section(cfg, 'atividade').get`
- `str`
- `sum`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.status_text`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 2
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI._article_context_text`

**Assinatura:**
```python
def _article_context_text(self) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 285–323
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Resumo do artigo final controlado pelo fluxo PRISMA robusto.

**Docstring resumida:** Resumo do artigo final controlado pelo fluxo PRISMA robusto.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'Contexto do artigo PRISMA:\nArtigo ativo: nenhum\nPasta do artigo: —\nTOML do artigo final: —\nEstado estrutural: ainda não criado'`; `f"Contexto do artigo PRISMA:\nArtigo ativo: {(art_dir if art_dir else 'pasta não definida')}\nPasta do artigo: {(art_dir if art_dir else '—')}\nTOML do artigo final: {(cfg_art if cfg_art else '—')}\nEstado estrutural: {state_status}\nPDF final: {pdf_status}"`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(raw_cfg).expanduser`
- `Path(raw_cfg).expanduser().resolve`
- `Path(raw_dir).expanduser`
- `Path(raw_dir).expanduser().resolve`
- `out_dir.exists`
- `out_dir.glob`
- `p.stat`
- `pdf.exists`
- `self.state.get`
- `sorted`
- `state_file.exists`
- `str`
- `str(self.state.get('last_article_cfg') or '').strip`
- `str(self.state.get('last_article_dir') or '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.menu_prisma_article_flow`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.status_text`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `artigo_state.json`
- `.pdf`
- `*.pdf`

#### Complexidade estrutural observada

```yaml
branches: 14
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.status_text`

**Assinatura:**
```python
def status_text(self) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 325–335
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `status_text` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'CENTRAL OPERACIONAL FGV — DOCUMENTOS ACADÊMICOS\nFluxo completo: escolher categoria → configurar → conferir → validar → gerar → revisar\n\nEsta tela separa o TOML global da pipeline do artigo final PRISMA.\nO projeto global é usado pelos fluxos gerais e pela busca PRISMA preliminar; o artigo PRISMA tem pasta, TOML final e estado estrutural próprios.\n\n' + self._global_project_context_text() + '\n\n' + self._article_context_text()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `self._article_context_text`
- `self._global_project_context_text`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.main_menu`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.menu_project`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.require_config`

**Assinatura:**
```python
def require_config(self) -> Path | None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 337–341
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `require_config` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path | None`. Retornos observados estaticamente incluem: `path`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `self.active_config`
- `ui.message`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.generate_full`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.generate_prisma_final_from_review`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.import_prisma_triage`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.list_output_files`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.menu_project`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.open_prisma_review_xlsx`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.recompile_org`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.rerender_existing`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.run_prisma_ai_cut`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.run_prisma_preliminary_search`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.show_inputs`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.show_prompts`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.validate_config`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.write_prompt_lock`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI._write_log`

**Assinatura:**
```python
def _write_log(self, label: str, command: list[str], stdout: str, returncode: int) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 347–363
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Grava a responsabilidade representada por `log` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `label` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `label` recebido pela função; não há descrição individual explícita no código. |
| `command` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `command` recebido pela função; não há descrição individual explícita no código. |
| `stdout` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `stdout` recebido pela função; não há descrição individual explícita no código. |
| `returncode` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `returncode` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `log_path`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `'\n'.join`
- `LOG_DIR.mkdir`
- `datetime.now`
- `datetime.now().isoformat`
- `datetime.now().strftime`
- `log_path.write_text`
- `stdout.rstrip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI._run_python_file`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.run_command`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.run_command`

**Assinatura:**
```python
def run_command(self, args: list[str], *, label: str, title: str) -> int
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 365–391
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `command` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `args` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `label` | keyword-only | `str` | `obrigatório/ausente` | Parâmetro `label` recebido pela função; não há descrição individual explícita no código. |
| `title` | keyword-only | `str` | `obrigatório/ausente` | Parâmetro `title` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `1`; `int(proc.returncode)`.

#### Efeitos colaterais e fronteiras externas

- Executa processo ou ferramenta externa.

#### Chamadas realizadas pelo corpo direto

- `int`
- `self._write_log`
- `str`
- `subprocess.run`
- `ui.message`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.generate_full`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.generate_prisma_final_from_review`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.import_prisma_triage`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.recompile_org`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.rerender_existing`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.run_prisma_ai_cut`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.show_prompts`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.validate_config`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.write_prompt_lock`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.choose_existing_config`

**Assinatura:**
```python
def choose_existing_config(self) -> Path | None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 397–427
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `choose_existing_config` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path | None`. Retornos observados estaticamente incluem: `None`; `path`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(str(selected)).resolve`
- `document.get`
- `project.get`
- `self.active_config`
- `self.list_project_configs`
- `self.load_toml`
- `self.section`
- `self.set_active_config`
- `str`
- `ui.message`
- `ui.select_one`
- `values.append`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.main_menu`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.menu_prisma_article_flow`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.menu_project`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.run_guided_flow`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.choose_document_profile`

**Assinatura:**
```python
def choose_document_profile(self) -> str | None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 429–439
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `choose_document_profile` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str | None`. Retornos observados estaticamente incluem: `ui.select_one('Academic Pipeline — passo 1/8: tipo de documento', 'Escolha o fluxo que será montado. Depois a TUI solicitará o diretório do projeto, abrirá as perguntas específicas do perfil e seguirá até a geração final.', DOCUMENT_PROFILES, default='atividade_local_fgv', width=126)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `ui.select_one`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.run_wizard`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.choose_project_directory`

**Assinatura:**
```python
def choose_project_directory(self) -> Path | None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 441–471
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `choose_project_directory` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path | None`. Retornos observados estaticamente incluem: `None`; `candidate`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `(Path.cwd() / candidate).resolve`
- `Path`
- `Path(raw).expanduser`
- `Path.cwd`
- `candidate.exists`
- `candidate.is_absolute`
- `candidate.is_dir`
- `candidate.mkdir`
- `candidate.resolve`
- `str`
- `ui.confirm`
- `ui.input_text`
- `ui.message`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.run_wizard`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.run_wizard`

**Assinatura:**
```python
def run_wizard(self, profile_key: str | None = None) -> int
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 473–516
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `wizard` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `profile_key` | positional-or-keyword | `str \| None` | `None` | Parâmetro `profile_key` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `1`; `int(rc)`.

#### Efeitos colaterais e fronteiras externas

- Executa processo ou ferramenta externa.

#### Chamadas realizadas pelo corpo direto

- `int`
- `path.is_file`
- `path.resolve`
- `path.stat`
- `project_dir.glob`
- `self.choose_document_profile`
- `self.choose_project_directory`
- `self.is_project_config`
- `self.set_active_config`
- `sorted`
- `str`
- `subprocess.run`
- `ui.message`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.menu_prisma_article_flow`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.menu_project`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.run_new_document_flow`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `--profile`
- `--project-dir`
- `--tui-theme`

#### Artefatos/caminhos literais reconhecidos no corpo

- `*.toml`

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 0
comprehensions: 3
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.menu_project`

**Assinatura:**
```python
def menu_project(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 518–543
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `menu_project` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `self.choose_existing_config`
- `self.open_path`
- `self.require_config`
- `self.run_wizard`
- `self.set_active_config`
- `self.status_text`
- `ui.confirm`
- `ui.menu`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.main_menu`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 7
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.show_inputs`

**Assinatura:**
```python
def show_inputs(self) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 549–568
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `show_inputs` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `False`; `not missing_corpus`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `lines.append`
- `self.input_rows`
- `self.require_config`
- `ui.message`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.main_menu`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.run_configured_flow`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 1
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.validate_config`

**Assinatura:**
```python
def validate_config(self) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 570–575
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Valida a responsabilidade representada por `config` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `False`; `rc == 0`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `self.require_config`
- `self.run_command`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `--check-config`
- `--config`

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.show_prompts`

**Assinatura:**
```python
def show_prompts(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 577–580
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `show_prompts` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `self.require_config`
- `self.run_command`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.main_menu`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `--config`
- `--show-prompts`

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.write_prompt_lock`

**Assinatura:**
```python
def write_prompt_lock(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 582–585
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Grava a responsabilidade representada por `prompt_lock` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `self.require_config`
- `self.run_command`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `--config`
- `--write-prompt-lock`

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.generate_full`

**Assinatura:**
```python
def generate_full(self) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 587–608
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Gera a responsabilidade representada por `full` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `False`; `rc == 0`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `bool`
- `busca.get`
- `self.load_toml`
- `self.require_config`
- `self.run_command`
- `self.section`
- `str`
- `str(busca.get('modo') or '').strip`
- `str(busca.get('modo') or '').strip().lower`
- `ui.confirm`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.main_menu`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.run_configured_flow`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.run_prisma_preliminary_search`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `--config`

#### Artefatos/caminhos literais reconhecidos no corpo

- `A geração completa consultará a IA e produzirá document.json, ORG, PDF/DOCX conforme o TOML, além dos relatórios de controle. Confirmar agora?`

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.import_prisma_triage`

**Assinatura:**
```python
def import_prisma_triage(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 610–643
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Importa a responsabilidade representada por `prisma_triage` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(raw).expanduser`
- `bool`
- `busca.get`
- `item.stat`
- `output_dir.exists`
- `output_dir.glob`
- `self.cfg_output_dir`
- `self.load_toml`
- `self.require_config`
- `self.run_command`
- `self.section`
- `sorted`
- `str`
- `str(busca.get('modo') or '').strip`
- `str(busca.get('modo') or '').strip().lower`
- `ui.input_text`
- `ui.message`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.main_menu`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `--config`
- `--prisma-importar-triagem`

#### Artefatos/caminhos literais reconhecidos no corpo

- `*.triagem_titulo_resumo.csv`
- `.csv`

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.rerender_existing`

**Assinatura:**
```python
def rerender_existing(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 645–666
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `rerender_existing` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(raw).expanduser`
- `output_dir.exists`
- `output_dir.glob`
- `p.stat`
- `self.cfg_output_dir`
- `self.load_toml`
- `self.require_config`
- `self.run_command`
- `sorted`
- `str`
- `ui.input_text`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.main_menu`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `--config`
- `--document-json`
- `--somente-renderizar`

#### Artefatos/caminhos literais reconhecidos no corpo

- `*.document.json`
- `Caminho do document.json existente:`

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.recompile_org`

**Assinatura:**
```python
def recompile_org(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 668–685
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `recompile_org` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(raw).expanduser`
- `output_dir.exists`
- `output_dir.glob`
- `p.stat`
- `self.cfg_output_dir`
- `self.load_toml`
- `self.require_config`
- `self.run_command`
- `sorted`
- `str`
- `ui.input_text`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.main_menu`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `--config`
- `--org`
- `--recompile`

#### Artefatos/caminhos literais reconhecidos no corpo

- `*.org`

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.cfg_research_output_dir`

**Assinatura:**
```python
def cfg_research_output_dir(self, config_path: Path, cfg: dict[str, Any] | None = None) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 692–700
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Diretório do relatório PRISMA, distinto da saída do documento.

**Docstring resumida:** Diretório do relatório PRISMA, distinto da saída do documento.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `config_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `cfg` | positional-or-keyword | `dict[str, Any] \| None` | `None` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `base / prefix if bool(paths.get('create_research_subdir', True)) else base`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `bool`
- `paths.get`
- `project.get`
- `self.load_toml`
- `self.resolve_config_path`
- `self.section`
- `str`
- `str(paths.get('research_prefix') or f"relatorio_prisma_{project.get('nome') or config_path.stem}").strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.generate_prisma_final_from_review`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.open_prisma_review_xlsx`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.run_prisma_ai_cut`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI._run_python_file`

**Assinatura:**
```python
def _run_python_file(self, script: Path, args: list[str], *, label: str, title: str) -> int
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 702–725
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `python_file` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `script` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `script` recebido pela função; não há descrição individual explícita no código. |
| `args` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `label` | keyword-only | `str` | `obrigatório/ausente` | Parâmetro `label` recebido pela função; não há descrição individual explícita no código. |
| `title` | keyword-only | `str` | `obrigatório/ausente` | Parâmetro `title` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `1`; `int(proc.returncode)`.

#### Efeitos colaterais e fronteiras externas

- Executa processo ou ferramenta externa.

#### Chamadas realizadas pelo corpo direto

- `int`
- `script.exists`
- `self._write_log`
- `str`
- `subprocess.run`
- `ui.message`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.generate_article_final_unified`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI._input_int`

**Assinatura:**
```python
def _input_int(self, title: str, text: str, default: int) -> int | None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 727–742
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_input_int` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `title` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `title` recebido pela função; não há descrição individual explícita no código. |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |
| `default` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `default` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `int | None`. Retornos observados estaticamente incluem: `None`; `default`; `value`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `int`
- `str`
- `str(raw).strip`
- `ui.input_text`
- `ui.message`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI._ask_article_final_settings`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.run_prisma_ai_cut`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI._article_workflow`

**Assinatura:**
```python
def _article_workflow(self, *, require_art_dir: bool = False)
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 745–761
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_article_workflow` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `require_art_dir` | keyword-only | `bool` | `False` | Parâmetro `require_art_dir` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `None`; `ArticleWorkflow(art_dir, cfg_art=cfg_art, prisma_cfg=prisma_cfg)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `ArticleWorkflow`
- `Path`
- `Path(cfg_raw).expanduser`
- `Path(cfg_raw).expanduser().resolve`
- `Path(raw).expanduser`
- `Path(raw).expanduser().resolve`
- `self.active_config`
- `self.state.get`
- `str`
- `str(self.state.get('last_article_cfg') or '').strip`
- `str(self.state.get('last_article_dir') or '').strip`
- `ui.message`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI._article_workflow_can_run_or_message`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI._article_workflow_mark_ok`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI._article_workflow_status_text`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI._ask_article_final_settings`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.generate_article_final_unified`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.mark_prisma_xlsx_reviewed`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI._article_workflow_status_text`

**Assinatura:**
```python
def _article_workflow_status_text(self) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 763–770
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_article_workflow_status_text` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'Status estrutural: pasta de artigo ainda não definida. Comece pelo briefing.'`; `wf.format_status()`; `f'Status estrutural indisponível: {exc}'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `self._article_workflow`
- `wf.format_status`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.menu_prisma_article_flow`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI._article_workflow_mark_ok`

**Assinatura:**
```python
def _article_workflow_mark_ok(self, stage: str, *, evidence: list[str] | None = None, message: str = '') -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 772–779
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_article_workflow_mark_ok` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `stage` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `stage` recebido pela função; não há descrição individual explícita no código. |
| `evidence` | keyword-only | `list[str] \| None` | `None` | Parâmetro `evidence` recebido pela função; não há descrição individual explícita no código. |
| `message` | keyword-only | `str` | `''` | Parâmetro `message` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `self._article_workflow`
- `wf.mark_stage_ok`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.create_or_edit_briefing`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.generate_prisma_final_from_review`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.run_prisma_ai_cut`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.run_prisma_preliminary_search`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI._article_workflow_can_run_or_message`

**Assinatura:**
```python
def _article_workflow_can_run_or_message(self, stage: str, title: str) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 781–793
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_article_workflow_can_run_or_message` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `stage` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `stage` recebido pela função; não há descrição individual explícita no código. |
| `title` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `title` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `True`; `False`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `self._article_workflow`
- `ui.message`
- `wf.can_run`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.generate_article_final_unified`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.generate_prisma_final_from_review`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.mark_prisma_xlsx_reviewed`

**Assinatura:**
```python
def mark_prisma_xlsx_reviewed(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 795–825
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `mark_prisma_xlsx_reviewed` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(str(selected)).expanduser`
- `Path(str(selected)).expanduser().resolve`
- `self._article_workflow`
- `str`
- `ui.confirm`
- `ui.message`
- `ui.select_one`
- `wf.find_prisma_artifacts`
- `wf.mark_human_review`
- `wf.refresh_from_files`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.menu_prisma_article_flow`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `*.curadoria_ia_referencias.xlsx`

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 1
exception_handlers: 2
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.create_or_edit_briefing`

**Assinatura:**
```python
def create_or_edit_briefing(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 827–850
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Cria a responsabilidade representada por `or_edit_briefing` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(raw_dir).expanduser`
- `Path(raw_dir).expanduser().resolve`
- `Path(self.state.get('last_article_dir') or Path.home()).expanduser`
- `Path.home`
- `art_dir.mkdir`
- `briefing.exists`
- `briefing.write_text`
- `self._article_workflow_mark_ok`
- `self._save_state`
- `self.open_path`
- `self.state.get`
- `str`
- `ui.input_text`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.menu_prisma_article_flow`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `briefing_artigo.txt`

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.run_prisma_preliminary_search`

**Assinatura:**
```python
def run_prisma_preliminary_search(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 852–860
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `prisma_preliminary_search` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `self._article_workflow_mark_ok`
- `self.generate_full`
- `self.require_config`
- `self.validate_config`
- `ui.message`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.menu_prisma_article_flow`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI._curadoria_prompt_default`

**Assinatura:**
```python
def _curadoria_prompt_default(self, config_path: Path, research_out: Path) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 862–872
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_curadoria_prompt_default` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `config_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `research_out` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `research_out` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `str(p.resolve())`; `''`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(raw_last).expanduser`
- `candidates.extend`
- `candidates.insert`
- `config_path.parent.glob`
- `p.exists`
- `p.is_file`
- `p.resolve`
- `research_out.exists`
- `research_out.glob`
- `self.state.get`
- `sorted`
- `str`
- `str(self.state.get('last_curadoria_prompt') or '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.run_prisma_ai_cut`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.run_prisma_ai_cut`

**Assinatura:**
```python
def run_prisma_ai_cut(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 874–922
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `prisma_ai_cut` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(prompt).expanduser`
- `Path(prompt).expanduser().resolve`
- `args.extend`
- `int`
- `self._article_workflow_mark_ok`
- `self._curadoria_prompt_default`
- `self._input_int`
- `self._save_state`
- `self.cfg_research_output_dir`
- `self.load_toml`
- `self.require_config`
- `self.run_command`
- `self.state.get`
- `str`
- `str(prompt).strip`
- `ui.input_text`
- `ui.message`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.menu_prisma_article_flow`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `--config`
- `--prisma-curadoria-ia`
- `--prisma-curadoria-limiar-minimo`
- `--prisma-curadoria-max-incluir`
- `--prisma-curadoria-out-dir`
- `--prisma-curadoria-prompt`
- `--prisma-curadoria-top-n-candidatos`

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 8
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.open_prisma_review_xlsx`

**Assinatura:**
```python
def open_prisma_review_xlsx(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 924–946
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `open_prisma_review_xlsx` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `candidates.extend`
- `p.stat`
- `research_out.exists`
- `research_out.glob`
- `self.cfg_research_output_dir`
- `self.load_toml`
- `self.open_path`
- `self.require_config`
- `set`
- `sorted`
- `str`
- `ui.message`
- `ui.select_one`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.menu_prisma_article_flow`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `*.curadoria_ia_referencias.xlsx`
- `*.triagem_titulo_resumo.xlsx`
- `*.matriz_estudos_incluidos.xlsx`

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 1
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.generate_prisma_final_from_review`

**Assinatura:**
```python
def generate_prisma_final_from_review(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 948–982
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Gera a responsabilidade representada por `prisma_final_from_review` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(raw).expanduser`
- `Path(raw).expanduser().resolve`
- `p.stat`
- `research_out.exists`
- `research_out.glob`
- `self._article_workflow_can_run_or_message`
- `self._article_workflow_mark_ok`
- `self.cfg_research_output_dir`
- `self.load_toml`
- `self.require_config`
- `self.run_command`
- `sorted`
- `str`
- `ui.input_text`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.menu_prisma_article_flow`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `--config`
- `--prisma-curadoria-importar`
- `--prisma-curadoria-input`
- `--prisma-curadoria-out-dir`
- `--prisma-curadoria-reexportar-xlsx`

#### Artefatos/caminhos literais reconhecidos no corpo

- `*.curadoria_ia_referencias.xlsx`
- `Selecione o XLSX revisado. O sistema reexportará para triagem_humana.csv e importará no PRISMA final.`
- `.xlsx`
- `.csv`

#### Complexidade estrutural observada

```yaml
branches: 7
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI._ask_article_final_settings`

**Assinatura:**
```python
def _ask_article_final_settings(self) -> tuple[Path, Path, int, int, int] | None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 984–1035
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_ask_article_final_settings` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `tuple[Path, Path, int, int, int] | None`. Retornos observados estaticamente incluem: `None`; `(art_dir, cfg_art, target_n, min_palavras, chars_pdf)`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(raw_cfg).expanduser`
- `Path(raw_cfg).expanduser().resolve`
- `Path(raw_dir).expanduser`
- `Path(raw_dir).expanduser().resolve`
- `Path.home`
- `art_dir.exists`
- `art_dir.glob`
- `art_dir.mkdir`
- `int`
- `p.stat`
- `self._article_workflow`
- `self._input_int`
- `self._save_state`
- `self.state.get`
- `sorted`
- `str`
- `ui.confirm`
- `ui.input_text`
- `wf.refresh_from_files`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.generate_article_final_unified`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.open_article_output_folder`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `*.toml`
- `artigo_final.toml`
- `.toml`

#### Complexidade estrutural observada

```yaml
branches: 9
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.generate_article_final_unified`

**Assinatura:**
```python
def generate_article_final_unified(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 1037–1066
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Gera a responsabilidade representada por `article_final_unified` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `cfg_art.exists`
- `pdf.exists`
- `self._article_workflow`
- `self._article_workflow_can_run_or_message`
- `self._ask_article_final_settings`
- `self._run_python_file`
- `self.open_path`
- `str`
- `ui.confirm`
- `ui.message`
- `wf.refresh_from_files`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.menu_prisma_article_flow`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `--art-dir`
- `--cfg-art`
- `--chars-por-pdf`
- `--min-palavras`
- `--quiet`
- `--target-n`

#### Artefatos/caminhos literais reconhecidos no corpo

- `.pdf`

#### Complexidade estrutural observada

```yaml
branches: 7
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.open_article_output_folder`

**Assinatura:**
```python
def open_article_output_folder(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 1068–1081
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `open_article_output_folder` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(raw).expanduser`
- `Path(raw).expanduser().resolve`
- `out.exists`
- `self._ask_article_final_settings`
- `self.open_path`
- `self.state.get`
- `str`
- `str(self.state.get('last_article_dir') or '').strip`
- `ui.message`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.menu_prisma_article_flow`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.menu_prisma_article_flow`

**Assinatura:**
```python
def menu_prisma_article_flow(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 1084–1140
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `menu_prisma_article_flow` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `self._article_context_text`
- `self._article_workflow_status_text`
- `self.active_config`
- `self.choose_existing_config`
- `self.create_or_edit_briefing`
- `self.generate_article_final_unified`
- `self.generate_prisma_final_from_review`
- `self.mark_prisma_xlsx_reviewed`
- `self.open_article_output_folder`
- `self.open_prisma_review_xlsx`
- `self.run_prisma_ai_cut`
- `self.run_prisma_preliminary_search`
- `self.run_wizard`
- `ui.menu`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.main_menu`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 12
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.open_path`

**Assinatura:**
```python
def open_path(self, path: Path) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 1146–1159
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `open_path` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Executa processo ou ferramenta externa.

#### Chamadas realizadas pelo corpo direto

- `path.exists`
- `shutil.which`
- `str`
- `subprocess.Popen`
- `ui.message`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.create_or_edit_briefing`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.generate_article_final_unified`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.menu_outputs`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.menu_project`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.open_article_output_folder`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.open_prisma_review_xlsx`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.run_configured_flow`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.list_output_files`

**Assinatura:**
```python
def list_output_files(self) -> tuple[Path | None, list[Path]]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 1161–1171
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Lista a responsabilidade representada por `output_files` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `tuple[Path | None, list[Path]]`. Retornos observados estaticamente incluem: `(None, [])`; `(output_dir, files)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `output_dir.exists`
- `output_dir.glob`
- `p.is_file`
- `p.stat`
- `self.cfg_output_dir`
- `self.require_config`
- `sorted`
- `ui.message`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.menu_outputs`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.run_configured_flow`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 1
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.menu_outputs`

**Assinatura:**
```python
def menu_outputs(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 1173–1215
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `menu_outputs` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `Path`
- `datetime.fromtimestamp`
- `datetime.fromtimestamp(file.stat().st_mtime).strftime`
- `description.append`
- `file.stat`
- `p.name.lower`
- `p.suffix.lower`
- `self.list_output_files`
- `self.open_path`
- `str`
- `ui.menu`
- `ui.message`
- `ui.select_one`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.main_menu`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.md`
- `.txt`
- `.json`

#### Complexidade estrutural observada

```yaml
branches: 10
loops: 2
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.run_configured_flow`

**Assinatura:**
```python
def run_configured_flow(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 1221–1261
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Etapas 7 e 8: conferir, validar, gerar e abrir os resultados.

**Docstring resumida:** Etapas 7 e 8: conferir, validar, gerar e abrir os resultados.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `self.active_config`
- `self.generate_full`
- `self.list_output_files`
- `self.open_path`
- `self.show_inputs`
- `self.validate_config`
- `ui.confirm`
- `ui.message`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.run_guided_flow`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.run_new_document_flow`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 8
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.run_new_document_flow`

**Assinatura:**
```python
def run_new_document_flow(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 1263–1267
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Fluxo completo para um novo documento, sem reutilizar TOML ativo.

**Docstring resumida:** Fluxo completo para um novo documento, sem reutilizar TOML ativo.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `self.active_config`
- `self.run_configured_flow`
- `self.run_wizard`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.main_menu`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.run_guided_flow`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.run_guided_flow`

**Assinatura:**
```python
def run_guided_flow(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 1269–1289
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Retoma o fluxo de um TOML existente ou permite selecioná-lo.

**Docstring resumida:** Retoma o fluxo de um TOML existente ou permite selecioná-lo.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `self.active_config`
- `self.choose_existing_config`
- `self.run_configured_flow`
- `self.run_new_document_flow`
- `ui.select_one`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.main_menu`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.main_menu`

**Assinatura:**
```python
def main_menu(self) -> int
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 1292–1433
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicPipelineTUI`
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Menu principal reorganizado por categorias de uso.

**Docstring resumida:** Menu principal reorganizado por categorias de uso.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `0`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `lines.append`
- `re.sub`
- `self.choose_existing_config`
- `self.generate_full`
- `self.import_prisma_triage`
- `self.menu_outputs`
- `self.menu_prisma_article_flow`
- `self.menu_project`
- `self.recompile_org`
- `self.rerender_existing`
- `self.run_guided_flow`
- `self.run_new_document_flow`
- `self.show_inputs`
- `self.show_prompts`
- `self.status_text`
- `self.validate_config`
- `self.write_prompt_lock`
- `ui.menu`
- `ui.message`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:run_tui`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `Exemplos: atividade de disciplina, resumo de artigos, paper local, dissertação local ou renderização de document.json.`
- `[3] Somente renderizar document.json existente`

#### Complexidade estrutural observada

```yaml
branches: 24
loops: 6
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:run_tui`

**Assinatura:**
```python
def run_tui(*, no_clear: bool = False) -> int
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 1440–1445
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `tui` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `no_clear` | keyword-only | `bool` | `False` | Parâmetro `no_clear` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `AcademicPipelineTUI(no_clear=no_clear).main_menu()`; `2`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `AcademicPipelineTUI`
- `AcademicPipelineTUI(no_clear=no_clear).main_menu`
- `print`
- `str`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.command_dispatch:dispatch_stage_002`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui:main`

**Assinatura:**
```python
def main(argv: Iterable[str] | None = None) -> int
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui.py`
**Linhas:** 1448–1452
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa o ponto de entrada correspondente e coordena o fluxo associado.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `Iterable[str] \| None` | `None` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `run_tui(no_clear=bool(args.no_clear))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `argparse.ArgumentParser`
- `bool`
- `list`
- `parser.add_argument`
- `parser.parse_args`
- `run_tui`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `--no-clear`

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e9127305a7bd7630a937e242ddb045513b108aabdbb72f440c1b0a296f849325`
**LOC do arquivo-fonte:** 1456


### `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:_imports`

**Assinatura:**
```python
def _imports() -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui_widgets.py`
**Linhas:** 26–75
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_imports` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `locals()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Style.from_dict`
- `TUIUnavailable`
- `locals`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:confirm`
- `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:input_text`
- `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:menu`
- `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:message`
- `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:select_many`
- `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:select_one`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `TUIUnavailable('A TUI visual requer prompt-toolkit. Instale uma única vez com: pipenv install prompt-toolkit')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `b66fc9fb26f3359c6efb6960684426a0c7a571c6ea58162fa75a65ab1569389f`
**LOC do arquivo-fonte:** 814


### `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:_history_path`

**Assinatura:**
```python
def _history_path() -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui_widgets.py`
**Linhas:** 78–81
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_history_path` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `root / 'tui_input_history.txt'`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `Path.home`
- `root.mkdir`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:input_text`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `tui_input_history.txt`

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b66fc9fb26f3359c6efb6960684426a0c7a571c6ea58162fa75a65ab1569389f`
**LOC do arquivo-fonte:** 814


### `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:_title`

**Assinatura:**
```python
def _title(title: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui_widgets.py`
**Linhas:** 84–86
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_title` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `title` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `title` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `clean if clean.startswith('FGV') else f'FGV | {clean}'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `clean.startswith`
- `str`
- `str(title or 'Academic Pipeline').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:confirm`
- `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:input_text`
- `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:menu`
- `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:message`
- `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:select_many`
- `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:select_one`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b66fc9fb26f3359c6efb6960684426a0c7a571c6ea58162fa75a65ab1569389f`
**LOC do arquivo-fonte:** 814


### `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:_menu_height`

**Assinatura:**
```python
def _menu_height(count: int) -> int
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui_widgets.py`
**Linhas:** 89–91
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Reserva linhas suficientes para a lista não desaparecer no diálogo.

**Docstring resumida:** Reserva linhas suficientes para a lista não desaparecer no diálogo.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `count` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `count` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `max(8, min(24, count + 3))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `max`
- `min`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:menu`
- `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:select_many`
- `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:select_one`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b66fc9fb26f3359c6efb6960684426a0c7a571c6ea58162fa75a65ab1569389f`
**LOC do arquivo-fonte:** 814


### `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:_run_application`

**Assinatura:**
```python
def _run_application(ui: dict[str, Any], *, layout: Any, key_bindings: Any, style: Any, input_obj: Any | None = None, output_obj: Any | None = None) -> Any
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui_widgets.py`
**Linhas:** 94–115
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Executa aplicação e permite injeção de I/O nos testes.

**Docstring resumida:** Executa aplicação e permite injeção de I/O nos testes.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `ui` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `ui` recebido pela função; não há descrição individual explícita no código. |
| `layout` | keyword-only | `Any` | `obrigatório/ausente` | Layout institucional/documental selecionado. |
| `key_bindings` | keyword-only | `Any` | `obrigatório/ausente` | Parâmetro `key_bindings` recebido pela função; não há descrição individual explícita no código. |
| `style` | keyword-only | `Any` | `obrigatório/ausente` | Parâmetro `style` recebido pela função; não há descrição individual explícita no código. |
| `input_obj` | keyword-only | `Any \| None` | `None` | Parâmetro `input_obj` recebido pela função; não há descrição individual explícita no código. |
| `output_obj` | keyword-only | `Any \| None` | `None` | Parâmetro `output_obj` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Any`. Retornos observados estaticamente incluem: `ui['Application'](**kwargs)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `ui['Application']`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:confirm`
- `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:input_text`
- `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:menu`
- `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:message`
- `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:select_many`
- `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:select_one`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b66fc9fb26f3359c6efb6960684426a0c7a571c6ea58162fa75a65ab1569389f`
**LOC do arquivo-fonte:** 814


### `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:_direct_click_radio`

**Assinatura:**
```python
def _direct_click_radio(ui: dict[str, Any], values: Sequence[tuple[Any, str]], *, default: Any, on_click: Any) -> Any
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui_widgets.py`
**Linhas:** 118–157
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Cria RadioList que confirma a opção em um único clique.

**Docstring resumida:** Cria RadioList que confirma a opção em um único clique.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `ui` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `ui` recebido pela função; não há descrição individual explícita no código. |
| `values` | positional-or-keyword | `Sequence[tuple[Any, str]]` | `obrigatório/ausente` | Parâmetro `values` recebido pela função; não há descrição individual explícita no código. |
| `default` | keyword-only | `Any` | `obrigatório/ausente` | Parâmetro `default` recebido pela função; não há descrição individual explícita no código. |
| `on_click` | keyword-only | `Any` | `obrigatório/ausente` | Parâmetro `on_click` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Any`. Retornos observados estaticamente incluem: `DirectClickRadioList(values=list(values), default=default, show_scrollbar=True, select_on_focus=True)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `DirectClickRadioList`
- `list`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:menu`
- `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:select_one`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b66fc9fb26f3359c6efb6960684426a0c7a571c6ea58162fa75a65ab1569389f`
**LOC do arquivo-fonte:** 814


### `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:_direct_click_radio.DirectClickRadioList._get_text_fragments`

**Assinatura:**
```python
def _get_text_fragments(self) -> Any
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui_widgets.py`
**Linhas:** 136–150
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `_direct_click_radio.DirectClickRadioList`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_get_text_fragments` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Any`. Retornos observados estaticamente incluem: `[(style, text, mouse_handler) for style, text, _handler in fragments]`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `super`
- `super()._get_text_fragments`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:_direct_click_radio.DirectClickRadioList._get_text_fragments`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b66fc9fb26f3359c6efb6960684426a0c7a571c6ea58162fa75a65ab1569389f`
**LOC do arquivo-fonte:** 814


### `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:_direct_click_radio.DirectClickRadioList._get_text_fragments.mouse_handler`

**Assinatura:**
```python
def mouse_handler(mouse_event: Any) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui_widgets.py`
**Linhas:** 139–147
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `_direct_click_radio.DirectClickRadioList`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `mouse_handler` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `mouse_event` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `mouse_event` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `getattr`
- `int`
- `len`
- `on_click`
- `self._handle_enter`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b66fc9fb26f3359c6efb6960684426a0c7a571c6ea58162fa75a65ab1569389f`
**LOC do arquivo-fonte:** 814


### `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:message`

**Assinatura:**
```python
def message(title: str, text: str, *, width: int = 116, _input: Any | None = None, _output: Any | None = None) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui_widgets.py`
**Linhas:** 160–211
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Mostra texto rolável em tela inteira.

**Docstring resumida:** Mostra texto rolável em tela inteira.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `title` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `title` recebido pela função; não há descrição individual explícita no código. |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |
| `width` | keyword-only | `int` | `116` | Parâmetro `width` recebido pela função; não há descrição individual explícita no código. |
| `_input` | keyword-only | `Any \| None` | `None` | Parâmetro `_input` recebido pela função; não há descrição individual explícita no código. |
| `_output` | keyword-only | `Any \| None` | `None` | Parâmetro `_output` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `_imports`
- `_run_application`
- `_title`
- `app.run`
- `str`
- `ui['Box']`
- `ui['Button']`
- `ui['Dialog']`
- `ui['Dimension']`
- `ui['KeyBindings']`
- `ui['Layout']`
- `ui['TextArea']`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_notify_structured_input_error`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_show_imported_paper_values`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_show_imported_prisma_protocol`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_show_imported_research_values`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_required`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:generate_interactive`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:show_summary`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI._article_workflow`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI._article_workflow_can_run_or_message`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI._input_int`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI._run_python_file`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.choose_existing_config`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.choose_project_directory`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.generate_article_final_unified`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.import_prisma_triage`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.list_output_files`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.main_menu`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.mark_prisma_xlsx_reviewed`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.menu_outputs`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.open_article_output_folder`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.open_path`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.open_prisma_review_xlsx`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.require_config`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.run_command`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b66fc9fb26f3359c6efb6960684426a0c7a571c6ea58162fa75a65ab1569389f`
**LOC do arquivo-fonte:** 814


### `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:message.finish`

**Assinatura:**
```python
def finish() -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui_widgets.py`
**Linhas:** 172–173
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `finish` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `app.exit`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b66fc9fb26f3359c6efb6960684426a0c7a571c6ea58162fa75a65ab1569389f`
**LOC do arquivo-fonte:** 814


### `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:message._close`

**Assinatura:**
```python
def _close(event: Any) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui_widgets.py`
**Linhas:** 182–183
**Tipo:** nested-function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** `kb.add('enter', eager=True)`, `kb.add('escape', eager=True)`, `kb.add('q', eager=True)`, `kb.add('c-c', eager=True)`
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `_close` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `event` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `event` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `finish`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b66fc9fb26f3359c6efb6960684426a0c7a571c6ea58162fa75a65ab1569389f`
**LOC do arquivo-fonte:** 814


### `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:confirm`

**Assinatura:**
```python
def confirm(title: str, text: str, *, yes_text: str = '[S] Sim', no_text: str = '[N] Não', default: bool = False, _input: Any | None = None, _output: Any | None = None) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui_widgets.py`
**Linhas:** 214–270
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `confirm` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `title` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `title` recebido pela função; não há descrição individual explícita no código. |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |
| `yes_text` | keyword-only | `str` | `'[S] Sim'` | Conteúdo textual de entrada. |
| `no_text` | keyword-only | `str` | `'[N] Não'` | Conteúdo textual de entrada. |
| `default` | keyword-only | `bool` | `False` | Parâmetro `default` recebido pela função; não há descrição individual explícita no código. |
| `_input` | keyword-only | `Any \| None` | `None` | Parâmetro `_input` recebido pela função; não há descrição individual explícita no código. |
| `_output` | keyword-only | `Any \| None` | `None` | Parâmetro `_output` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `bool(app.run())`; `False`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `_imports`
- `_run_application`
- `_title`
- `app.run`
- `bool`
- `finish`
- `str`
- `ui['Box']`
- `ui['Button']`
- `ui['Dialog']`
- `ui['Dimension']`
- `ui['KeyBindings']`
- `ui['Label']`
- `ui['Layout']`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_bool`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI._ask_article_final_settings`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.choose_project_directory`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.generate_article_final_unified`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.generate_full`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.mark_prisma_xlsx_reviewed`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.menu_project`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.run_configured_flow`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `b66fc9fb26f3359c6efb6960684426a0c7a571c6ea58162fa75a65ab1569389f`
**LOC do arquivo-fonte:** 814


### `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:confirm.finish`

**Assinatura:**
```python
def finish(value: bool) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui_widgets.py`
**Linhas:** 227–228
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `finish` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `bool` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `app.exit`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b66fc9fb26f3359c6efb6960684426a0c7a571c6ea58162fa75a65ab1569389f`
**LOC do arquivo-fonte:** 814


### `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:confirm._yes`

**Assinatura:**
```python
def _yes(event: Any) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui_widgets.py`
**Linhas:** 236–237
**Tipo:** nested-function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** `kb.add('s', eager=True)`, `kb.add('y', eager=True)`
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `_yes` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `event` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `event` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `finish`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b66fc9fb26f3359c6efb6960684426a0c7a571c6ea58162fa75a65ab1569389f`
**LOC do arquivo-fonte:** 814


### `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:confirm._no`

**Assinatura:**
```python
def _no(event: Any) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui_widgets.py`
**Linhas:** 243–244
**Tipo:** nested-function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** `kb.add('n', eager=True)`, `kb.add('escape', eager=True)`, `kb.add('q', eager=True)`, `kb.add('c-c', eager=True)`
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `_no` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `event` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `event` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `finish`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b66fc9fb26f3359c6efb6960684426a0c7a571c6ea58162fa75a65ab1569389f`
**LOC do arquivo-fonte:** 814


### `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:confirm._default`

**Assinatura:**
```python
def _default(event: Any) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui_widgets.py`
**Linhas:** 247–248
**Tipo:** nested-function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** `kb.add('enter', eager=True)`
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `_default` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `event` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `event` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `bool`
- `finish`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b66fc9fb26f3359c6efb6960684426a0c7a571c6ea58162fa75a65ab1569389f`
**LOC do arquivo-fonte:** 814


### `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:_browse_root`

**Assinatura:**
```python
def _browse_root(start_path: str | Path | None) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui_widgets.py`
**Linhas:** 273–285
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Resolve um ponto de partida existente para o navegador de caminhos.

**Docstring resumida:** Resolve um ponto de partida existente para o navegador de caminhos.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `start_path` | positional-or-keyword | `str \| Path \| None` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `candidate.parent`; `candidate if candidate.is_dir() else Path.cwd()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `(Path.cwd() / candidate).resolve`
- `Path`
- `Path(raw).expanduser`
- `Path.cwd`
- `candidate.exists`
- `candidate.is_absolute`
- `candidate.is_dir`
- `candidate.is_file`
- `candidate.resolve`
- `str`
- `str(start_path or '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:browse_path`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b66fc9fb26f3359c6efb6960684426a0c7a571c6ea58162fa75a65ab1569389f`
**LOC do arquivo-fonte:** 814


### `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:_browser_entries`

**Assinatura:**
```python
def _browser_entries(directory: Path, *, only_directories: bool, allowed_suffixes: Sequence[str]) -> list[Path]
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui_widgets.py`
**Linhas:** 288–306
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Lista entradas visíveis e filtradas do navegador.

**Docstring resumida:** Lista entradas visíveis e filtradas do navegador.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `directory` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `directory` recebido pela função; não há descrição individual explícita no código. |
| `only_directories` | keyword-only | `bool` | `obrigatório/ausente` | Parâmetro `only_directories` recebido pela função; não há descrição individual explícita no código. |
| `allowed_suffixes` | keyword-only | `Sequence[str]` | `obrigatório/ausente` | Parâmetro `allowed_suffixes` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[Path]`. Retornos observados estaticamente incluem: `[]`; `sorted(entries, key=lambda p: (not p.is_dir(), p.name.lower()))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `directory.iterdir`
- `entries.append`
- `list`
- `p.is_dir`
- `p.name.lower`
- `path.is_dir`
- `path.name.startswith`
- `path.suffix.lower`
- `sorted`
- `str`
- `str(s).lower`
- `str(s).strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:browse_path`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 1
exception_handlers: 2
```

**SHA-256 do arquivo-fonte:** `b66fc9fb26f3359c6efb6960684426a0c7a571c6ea58162fa75a65ab1569389f`
**LOC do arquivo-fonte:** 814


### `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:browse_path`

**Assinatura:**
```python
def browse_path(title: str, text: str, *, start_path: str | Path | None = None, only_directories: bool = False, allowed_suffixes: Sequence[str] = (), _input: Any | None = None, _output: Any | None = None) -> str | None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui_widgets.py`
**Linhas:** 309–376
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Navegador de arquivos/diretórios integrado à TUI.

**Docstring resumida:** Navegador de arquivos/diretórios integrado à TUI.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `title` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `title` recebido pela função; não há descrição individual explícita no código. |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |
| `start_path` | keyword-only | `str \| Path \| None` | `None` | Caminho de arquivo ou diretório utilizado pela operação. |
| `only_directories` | keyword-only | `bool` | `False` | Parâmetro `only_directories` recebido pela função; não há descrição individual explícita no código. |
| `allowed_suffixes` | keyword-only | `Sequence[str]` | `()` | Parâmetro `allowed_suffixes` recebido pela função; não há descrição individual explícita no código. |
| `_input` | keyword-only | `Any \| None` | `None` | Parâmetro `_input` recebido pela função; não há descrição individual explícita no código. |
| `_output` | keyword-only | `Any \| None` | `None` | Parâmetro `_output` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str | None`. Retornos observados estaticamente incluem: `None`; `str(current.resolve())`; `str(Path(str(choice)[5:]).expanduser().resolve())`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `', '.join`
- `'\n'.join`
- `Path`
- `Path(str(choice)[4:]).expanduser`
- `Path(str(choice)[4:]).expanduser().resolve`
- `Path(str(choice)[5:]).expanduser`
- `Path(str(choice)[5:]).expanduser().resolve`
- `_browse_root`
- `_browser_entries`
- `current.resolve`
- `entry.is_dir`
- `entry.resolve`
- `header.append`
- `menu`
- `options.append`
- `str`
- `str(choice).startswith`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:input_text`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 10
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b66fc9fb26f3359c6efb6960684426a0c7a571c6ea58162fa75a65ab1569389f`
**LOC do arquivo-fonte:** 814


### `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:input_text`

**Assinatura:**
```python
def input_text(title: str, text: str, *, default: str = '', multiline: bool = False, path_completion: bool = False, only_directories: bool = False, allowed_suffixes: Sequence[str] = (), width: int = 116, _input: Any | None = None, _output: Any | None = None) -> str | None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui_widgets.py`
**Linhas:** 379–497
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Campo editável com histórico, Tab e navegador de caminhos por F2.

**Docstring resumida:** Campo editável com histórico, Tab e navegador de caminhos por F2.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `title` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `title` recebido pela função; não há descrição individual explícita no código. |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |
| `default` | keyword-only | `str` | `''` | Parâmetro `default` recebido pela função; não há descrição individual explícita no código. |
| `multiline` | keyword-only | `bool` | `False` | Parâmetro `multiline` recebido pela função; não há descrição individual explícita no código. |
| `path_completion` | keyword-only | `bool` | `False` | Parâmetro `path_completion` recebido pela função; não há descrição individual explícita no código. |
| `only_directories` | keyword-only | `bool` | `False` | Parâmetro `only_directories` recebido pela função; não há descrição individual explícita no código. |
| `allowed_suffixes` | keyword-only | `Sequence[str]` | `()` | Parâmetro `allowed_suffixes` recebido pela função; não há descrição individual explícita no código. |
| `width` | keyword-only | `int` | `116` | Parâmetro `width` recebido pela função; não há descrição individual explícita no código. |
| `_input` | keyword-only | `Any \| None` | `None` | Parâmetro `_input` recebido pela função; não há descrição individual explícita no código. |
| `_output` | keyword-only | `Any \| None` | `None` | Parâmetro `_output` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str | None`. Retornos observados estaticamente incluem: `None`; `result`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `_history_path`
- `_imports`
- `_run_application`
- `_title`
- `app.run`
- `bool`
- `browse_path`
- `buttons.append`
- `finish`
- `isinstance`
- `len`
- `str`
- `ui['AutoSuggestFromHistory']`
- `ui['Box']`
- `ui['Button']`
- `ui['Dialog']`
- `ui['Dimension']`
- `ui['FileHistory']`
- `ui['HSplit']`
- `ui['KeyBindings']`
- `ui['Label']`
- `ui['Layout']`
- `ui['PathCompleter']`
- `ui['TextArea']`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI._ask_article_final_settings`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI._input_int`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.choose_project_directory`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.create_or_edit_briefing`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.generate_prisma_final_from_review`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.import_prisma_triage`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.recompile_org`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.rerender_existing`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.run_prisma_ai_cut`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 9
loops: 1
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `b66fc9fb26f3359c6efb6960684426a0c7a571c6ea58162fa75a65ab1569389f`
**LOC do arquivo-fonte:** 814


### `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:_bind_shortcut`

**Assinatura:**
```python
def _bind_shortcut(kb: Any, key: str, handler: Any) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui_widgets.py`
**Linhas:** 500–506
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Registra atalho de forma segura e com precedência sobre RadioList.

**Docstring resumida:** Registra atalho de forma segura e com precedência sobre RadioList.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `kb` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `kb` recebido pela função; não há descrição individual explícita no código. |
| `key` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `key` recebido pela função; não há descrição individual explícita no código. |
| `handler` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `handler` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `False`; `True`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `kb.add`
- `kb.add(key, eager=True)`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:menu`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `b66fc9fb26f3359c6efb6960684426a0c7a571c6ea58162fa75a65ab1569389f`
**LOC do arquivo-fonte:** 814


### `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:menu`

**Assinatura:**
```python
def menu(title: str, text: str, options: Sequence[tuple[str, str, Sequence[str]]], *, width: int = 122, _input: Any | None = None, _output: Any | None = None) -> str | None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui_widgets.py`
**Linhas:** 509–596
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Menu vertical com atalhos diretos e confirmação por clique único.

**Docstring resumida:** Menu vertical com atalhos diretos e confirmação por clique único.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `title` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `title` recebido pela função; não há descrição individual explícita no código. |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |
| `options` | positional-or-keyword | `Sequence[tuple[str, str, Sequence[str]]]` | `obrigatório/ausente` | Parâmetro `options` recebido pela função; não há descrição individual explícita no código. |
| `width` | keyword-only | `int` | `122` | Parâmetro `width` recebido pela função; não há descrição individual explícita no código. |
| `_input` | keyword-only | `Any \| None` | `None` | Parâmetro `_input` recebido pela função; não há descrição individual explícita no código. |
| `_output` | keyword-only | `Any \| None` | `None` | Parâmetro `_output` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str | None`. Retornos observados estaticamente incluem: `None`; `app.run()`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `_bind_shortcut`
- `_direct_click_radio`
- `_imports`
- `_menu_height`
- `_run_application`
- `_title`
- `app.run`
- `finish`
- `len`
- `min`
- `str`
- `str(key).strip`
- `str(key).strip().lower`
- `ui['Box']`
- `ui['Button']`
- `ui['Dialog']`
- `ui['Dimension']`
- `ui['HSplit']`
- `ui['KeyBindings']`
- `ui['Label']`
- `ui['Layout']`
- `used.add`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.main_menu`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.menu_outputs`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.menu_prisma_article_flow`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.menu_project`
- `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:browse_path`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 2
comprehensions: 1
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `b66fc9fb26f3359c6efb6960684426a0c7a571c6ea58162fa75a65ab1569389f`
**LOC do arquivo-fonte:** 814


### `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:menu.finish`

**Assinatura:**
```python
def finish(value: str | None) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui_widgets.py`
**Linhas:** 525–526
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `finish` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `str \| None` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `app.exit`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b66fc9fb26f3359c6efb6960684426a0c7a571c6ea58162fa75a65ab1569389f`
**LOC do arquivo-fonte:** 814


### `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:menu._select`

**Assinatura:**
```python
def _select(event: Any) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui_widgets.py`
**Linhas:** 537–538
**Tipo:** nested-function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** `kb.add('enter', eager=True)`, `kb.add('s', eager=True)`
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Seleciona a responsabilidade representada por `select` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `event` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `event` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `finish`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b66fc9fb26f3359c6efb6960684426a0c7a571c6ea58162fa75a65ab1569389f`
**LOC do arquivo-fonte:** 814


### `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:menu._cancel`

**Assinatura:**
```python
def _cancel(event: Any) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui_widgets.py`
**Linhas:** 556–557
**Tipo:** nested-function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** `kb.add('escape', eager=True)`, `kb.add('q', eager=True)`, `kb.add('c-c', eager=True)`
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `_cancel` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `event` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `event` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `finish`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b66fc9fb26f3359c6efb6960684426a0c7a571c6ea58162fa75a65ab1569389f`
**LOC do arquivo-fonte:** 814


### `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:select_one`

**Assinatura:**
```python
def select_one(title: str, text: str, values: Sequence[tuple[Any, str]], *, default: Any | None = None, width: int = 116, _input: Any | None = None, _output: Any | None = None) -> Any | None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui_widgets.py`
**Linhas:** 599–674
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Lista de escolha única: Enter, teclas e clique concluem a tela.

**Docstring resumida:** Lista de escolha única: Enter, teclas e clique concluem a tela.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `title` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `title` recebido pela função; não há descrição individual explícita no código. |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |
| `values` | positional-or-keyword | `Sequence[tuple[Any, str]]` | `obrigatório/ausente` | Parâmetro `values` recebido pela função; não há descrição individual explícita no código. |
| `default` | keyword-only | `Any \| None` | `None` | Parâmetro `default` recebido pela função; não há descrição individual explícita no código. |
| `width` | keyword-only | `int` | `116` | Parâmetro `width` recebido pela função; não há descrição individual explícita no código. |
| `_input` | keyword-only | `Any \| None` | `None` | Parâmetro `_input` recebido pela função; não há descrição individual explícita no código. |
| `_output` | keyword-only | `Any \| None` | `None` | Parâmetro `_output` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Any | None`. Retornos observados estaticamente incluem: `None`; `app.run()`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `_direct_click_radio`
- `_imports`
- `_menu_height`
- `_run_application`
- `_title`
- `app.run`
- `finish`
- `len`
- `list`
- `min`
- `str`
- `ui['Box']`
- `ui['Button']`
- `ui['Dialog']`
- `ui['Dimension']`
- `ui['HSplit']`
- `ui['KeyBindings']`
- `ui['Label']`
- `ui['Layout']`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_choice`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_final_action`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_stage_action`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_stage_number`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.choose_document_profile`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.choose_existing_config`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.mark_prisma_xlsx_reviewed`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.menu_outputs`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.open_prisma_review_xlsx`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.run_guided_flow`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `b66fc9fb26f3359c6efb6960684426a0c7a571c6ea58162fa75a65ab1569389f`
**LOC do arquivo-fonte:** 814


### `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:select_one.finish`

**Assinatura:**
```python
def finish(value: Any | None) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui_widgets.py`
**Linhas:** 616–617
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `finish` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any \| None` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `app.exit`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b66fc9fb26f3359c6efb6960684426a0c7a571c6ea58162fa75a65ab1569389f`
**LOC do arquivo-fonte:** 814


### `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:select_one._select`

**Assinatura:**
```python
def _select(event: Any) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui_widgets.py`
**Linhas:** 628–629
**Tipo:** nested-function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** `kb.add('s', eager=True)`, `kb.add('enter', eager=True)`
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Seleciona a responsabilidade representada por `select` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `event` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `event` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `finish`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b66fc9fb26f3359c6efb6960684426a0c7a571c6ea58162fa75a65ab1569389f`
**LOC do arquivo-fonte:** 814


### `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:select_one._cancel`

**Assinatura:**
```python
def _cancel(event: Any) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui_widgets.py`
**Linhas:** 634–635
**Tipo:** nested-function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** `kb.add('escape', eager=True)`, `kb.add('q', eager=True)`, `kb.add('c-c', eager=True)`
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `_cancel` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `event` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `event` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `finish`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b66fc9fb26f3359c6efb6960684426a0c7a571c6ea58162fa75a65ab1569389f`
**LOC do arquivo-fonte:** 814


### `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:select_many`

**Assinatura:**
```python
def select_many(title: str, text: str, values: Sequence[tuple[Any, str]], *, defaults: Iterable[Any] = (), select_all_value: Any | None = None, select_all_values: Iterable[Any] = (), width: int = 116, _input: Any | None = None, _output: Any | None = None) -> list[Any] | None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui_widgets.py`
**Linhas:** 677–814
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Lista de múltipla escolha com opção de marcar todos, quando configurada.

**Docstring resumida:** Lista de múltipla escolha com opção de marcar todos, quando configurada.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `title` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `title` recebido pela função; não há descrição individual explícita no código. |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |
| `values` | positional-or-keyword | `Sequence[tuple[Any, str]]` | `obrigatório/ausente` | Parâmetro `values` recebido pela função; não há descrição individual explícita no código. |
| `defaults` | keyword-only | `Iterable[Any]` | `()` | Parâmetro `defaults` recebido pela função; não há descrição individual explícita no código. |
| `select_all_value` | keyword-only | `Any \| None` | `None` | Parâmetro `select_all_value` recebido pela função; não há descrição individual explícita no código. |
| `select_all_values` | keyword-only | `Iterable[Any]` | `()` | Parâmetro `select_all_values` recebido pela função; não há descrição individual explícita no código. |
| `width` | keyword-only | `int` | `116` | Parâmetro `width` recebido pela função; não há descrição individual explícita no código. |
| `_input` | keyword-only | `Any \| None` | `None` | Parâmetro `_input` recebido pela função; não há descrição individual explícita no código. |
| `_output` | keyword-only | `Any \| None` | `None` | Parâmetro `_output` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[Any] | None`. Retornos observados estaticamente incluem: `app.run()`; `None`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `SelectAllCheckboxList`
- `_imports`
- `_menu_height`
- `_run_application`
- `_title`
- `app.run`
- `base_checkbox`
- `checklist._sync_select_all`
- `finish`
- `len`
- `list`
- `min`
- `str`
- `ui['Box']`
- `ui['Button']`
- `ui['Dialog']`
- `ui['Dimension']`
- `ui['HSplit']`
- `ui['KeyBindings']`
- `ui['Label']`
- `ui['Layout']`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_many_choice`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_metadata_sources`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 3
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `b66fc9fb26f3359c6efb6960684426a0c7a571c6ea58162fa75a65ab1569389f`
**LOC do arquivo-fonte:** 814


### `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:select_many.finish`

**Assinatura:**
```python
def finish(value: list[Any] | None) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui_widgets.py`
**Linhas:** 741–742
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `finish` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `list[Any] \| None` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `app.exit`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b66fc9fb26f3359c6efb6960684426a0c7a571c6ea58162fa75a65ab1569389f`
**LOC do arquivo-fonte:** 814


### `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:select_many.all_items`

**Assinatura:**
```python
def all_items() -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui_widgets.py`
**Linhas:** 744–746
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `all_items` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `app.invalidate`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:select_many._all`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b66fc9fb26f3359c6efb6960684426a0c7a571c6ea58162fa75a65ab1569389f`
**LOC do arquivo-fonte:** 814


### `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:select_many.clear_items`

**Assinatura:**
```python
def clear_items() -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui_widgets.py`
**Linhas:** 748–750
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `clear_items` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `app.invalidate`
- `checklist.current_values.clear`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:select_many._clear`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b66fc9fb26f3359c6efb6960684426a0c7a571c6ea58162fa75a65ab1569389f`
**LOC do arquivo-fonte:** 814


### `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:select_many._all`

**Assinatura:**
```python
def _all(event: Any) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui_widgets.py`
**Linhas:** 787–788
**Tipo:** nested-function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** `kb.add('a', eager=True)`
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `_all` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `event` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `event` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `all_items`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b66fc9fb26f3359c6efb6960684426a0c7a571c6ea58162fa75a65ab1569389f`
**LOC do arquivo-fonte:** 814


### `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:select_many._clear`

**Assinatura:**
```python
def _clear(event: Any) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui_widgets.py`
**Linhas:** 791–792
**Tipo:** nested-function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** `kb.add('l', eager=True)`
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `_clear` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `event` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `event` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `clear_items`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b66fc9fb26f3359c6efb6960684426a0c7a571c6ea58162fa75a65ab1569389f`
**LOC do arquivo-fonte:** 814


### `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:select_many._continue`

**Assinatura:**
```python
def _continue(event: Any) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui_widgets.py`
**Linhas:** 795–796
**Tipo:** nested-function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** `kb.add('c-s', eager=True)`
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `_continue` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `event` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `event` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `finish`
- `list`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b66fc9fb26f3359c6efb6960684426a0c7a571c6ea58162fa75a65ab1569389f`
**LOC do arquivo-fonte:** 814


### `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:select_many._cancel`

**Assinatura:**
```python
def _cancel(event: Any) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/academic_pipeline_tui_widgets.py`
**Linhas:** 800–801
**Tipo:** nested-function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** `kb.add('escape', eager=True)`, `kb.add('c-c', eager=True)`
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `_cancel` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `event` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `event` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `finish`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b66fc9fb26f3359c6efb6960684426a0c7a571c6ea58162fa75a65ab1569389f`
**LOC do arquivo-fonte:** 814


### `app_bundle.scripts.pipeline.article_workflow.state:StageRecord.to_dict`

**Assinatura:**
```python
def to_dict(self) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/state.py`
**Linhas:** 46–52
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `StageRecord`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `to_dict` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `{'status': self.status, 'evidence': list(self.evidence), 'message': self.message, 'updated_at': self.updated_at}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `list`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.article_workflow.state:WorkflowState._default`
- `app_bundle.scripts.pipeline.article_workflow.state:WorkflowState._normalize`
- `app_bundle.scripts.pipeline.article_workflow.state:WorkflowState.mark`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f681a4157b3fa2b7a1cca07dd7b49de015e559cb38a422e8e4aab6ba7a71a6cd`
**LOC do arquivo-fonte:** 181


### `app_bundle.scripts.pipeline.article_workflow.state:StageRecord.from_dict`

**Assinatura:**
```python
def from_dict(cls, raw: Any) -> 'StageRecord'
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/state.py`
**Linhas:** 55–66
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `StageRecord`
**Decoradores:** `classmethod`
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `from_dict` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cls` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `cls` recebido pela função; não há descrição individual explícita no código. |
| `raw` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `raw` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `'StageRecord'`. Retornos observados estaticamente incluem: `cls()`; `cls(status=str(raw.get('status') or STATUS_BLOCKED), evidence=[str(x) for x in evidence], message=str(raw.get('message') or ''), updated_at=str(raw.get('updated_at') or ''))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `cls`
- `isinstance`
- `raw.get`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui_widgets:_imports`
- `app_bundle.scripts.pipeline.article_workflow.state:WorkflowState._normalize`
- `app_bundle.scripts.pipeline.article_workflow.state:WorkflowState.record`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f681a4157b3fa2b7a1cca07dd7b49de015e559cb38a422e8e4aab6ba7a71a6cd`
**LOC do arquivo-fonte:** 181


### `app_bundle.scripts.pipeline.article_workflow.state:WorkflowState.__init__`

**Assinatura:**
```python
def __init__(self, path: Path) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/state.py`
**Linhas:** 72–75
**Tipo:** method
**Visibilidade:** especial/dunder
**Classe proprietária:** `WorkflowState`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `__init__` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `self._default`
- `self.load`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f681a4157b3fa2b7a1cca07dd7b49de015e559cb38a422e8e4aab6ba7a71a6cd`
**LOC do arquivo-fonte:** 181


### `app_bundle.scripts.pipeline.article_workflow.state:WorkflowState._now`

**Assinatura:**
```python
def _now() -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/state.py`
**Linhas:** 78–79
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `WorkflowState`
**Decoradores:** `staticmethod`
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_now` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `datetime.now().isoformat(timespec='seconds')`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `datetime.now`
- `datetime.now().isoformat`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f681a4157b3fa2b7a1cca07dd7b49de015e559cb38a422e8e4aab6ba7a71a6cd`
**LOC do arquivo-fonte:** 181


### `app_bundle.scripts.pipeline.article_workflow.state:WorkflowState._default`

**Assinatura:**
```python
def _default() -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/state.py`
**Linhas:** 82–92
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `WorkflowState`
**Decoradores:** `staticmethod`
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_default` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `{'schema_version': 1, 'created_at': datetime.now().isoformat(timespec='seconds'), 'updated_at': datetime.now().isoformat(timespec='seconds'), 'current_stage': 'briefing', 'metadata': {}, 'stages': {key: StageRecord().to_dict() for key, _label, _deps in STAGES}, 'human_review': {}, 'history': []}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `StageRecord`
- `StageRecord().to_dict`
- `datetime.now`
- `datetime.now().isoformat`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f681a4157b3fa2b7a1cca07dd7b49de015e559cb38a422e8e4aab6ba7a71a6cd`
**LOC do arquivo-fonte:** 181


### `app_bundle.scripts.pipeline.article_workflow.state:WorkflowState.load`

**Assinatura:**
```python
def load(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/state.py`
**Linhas:** 94–106
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `WorkflowState`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Carrega a responsabilidade representada por `load` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `isinstance`
- `json.loads`
- `self._default`
- `self._normalize`
- `self.path.exists`
- `self.path.read_text`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.default_runtime:load_config`
- `academic_pipeline.runtime:_load_config`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:load_institution_profile_toml`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.load_toml`
- `app_bundle.scripts.pipeline.article_workflow.state:WorkflowState.__init__`
- `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow._read_toml`
- `app_bundle.scripts.pipeline.institution_compliance:_load_toml`
- `app_bundle.scripts.pipeline.institution_explainer:explain_profile`
- `app_bundle.scripts.pipeline.institution_profiles:describe_institution_profiles`
- `app_bundle.scripts.pipeline.institution_profiles:load_institution_profile`
- `app_bundle.scripts.pipeline.preparar_artigo_longo_fulltext:patch_toml`
- `app_bundle.scripts.pipeline.preparar_artigo_longo_fulltext:read_toml_dict`
- `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:read_toml`
- `app_bundle.scripts.pipeline.prisma_congelar_artigo:read_toml`
- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:read_toml`
- `configurar_pretriagem_ia_prisma:main`
- `diagnosticar_fontes_prisma:_load_config`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `f681a4157b3fa2b7a1cca07dd7b49de015e559cb38a422e8e4aab6ba7a71a6cd`
**LOC do arquivo-fonte:** 181


### `app_bundle.scripts.pipeline.article_workflow.state:WorkflowState._normalize`

**Assinatura:**
```python
def _normalize(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/state.py`
**Linhas:** 108–123
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `WorkflowState`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Normaliza a responsabilidade representada por `normalize` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `StageRecord.from_dict`
- `StageRecord.from_dict(stages.get(key)).to_dict`
- `self._apply_dependency_blocks`
- `self._now`
- `self.data.setdefault`
- `self.next_pending_stage`
- `stages.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.article_workflow.state:WorkflowState.can_run`
- `app_bundle.scripts.pipeline.article_workflow.state:WorkflowState.load`
- `app_bundle.scripts.pipeline.article_workflow.state:WorkflowState.record`
- `app_bundle.scripts.pipeline.article_workflow.state:WorkflowState.save`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f681a4157b3fa2b7a1cca07dd7b49de015e559cb38a422e8e4aab6ba7a71a6cd`
**LOC do arquivo-fonte:** 181


### `app_bundle.scripts.pipeline.article_workflow.state:WorkflowState._apply_dependency_blocks`

**Assinatura:**
```python
def _apply_dependency_blocks(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/state.py`
**Linhas:** 125–136
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `WorkflowState`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Aplica a responsabilidade representada por `dependency_blocks` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `any`
- `stages.get`
- `stages.get(dep, {}).get`
- `stages[key].get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.article_workflow.state:WorkflowState._normalize`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 1
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f681a4157b3fa2b7a1cca07dd7b49de015e559cb38a422e8e4aab6ba7a71a6cd`
**LOC do arquivo-fonte:** 181


### `app_bundle.scripts.pipeline.article_workflow.state:WorkflowState.save`

**Assinatura:**
```python
def save(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/state.py`
**Linhas:** 138–142
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `WorkflowState`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Salva a responsabilidade representada por `save` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `json.dumps`
- `self._normalize`
- `self._now`
- `self.path.parent.mkdir`
- `self.path.write_text`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.article_workflow.state:WorkflowState.mark`
- `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow._store_metadata`
- `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:ensure_reference_docx`
- `app_bundle.scripts.pipeline.paper_abstracts:inject_paper_abstracts_into_docx`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_write_xlsx`
- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:write_xlsx`
- `app_bundle.scripts.pipeline.render_docx:render_docx`
- `app_bundle.scripts.pipeline.render_docx_canonico:render_docx_for_article`
- `app_bundle.scripts.pipeline.render_prisma_docx:render_prisma_docx`
- `app_bundle.scripts.pipeline.render_prisma_xlsx:render_prisma_xlsx`
- `gerar_artigo_final_unificado:ensure_reference_docx`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f681a4157b3fa2b7a1cca07dd7b49de015e559cb38a422e8e4aab6ba7a71a6cd`
**LOC do arquivo-fonte:** 181


### `app_bundle.scripts.pipeline.article_workflow.state:WorkflowState.record`

**Assinatura:**
```python
def record(self, stage: str) -> StageRecord
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/state.py`
**Linhas:** 144–146
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `WorkflowState`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `record` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `stage` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `stage` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `StageRecord`. Retornos observados estaticamente incluem: `StageRecord.from_dict(self.data['stages'].get(stage))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `StageRecord.from_dict`
- `self._normalize`
- `self.data['stages'].get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.article_workflow.state:WorkflowState.status`
- `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.format_status`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f681a4157b3fa2b7a1cca07dd7b49de015e559cb38a422e8e4aab6ba7a71a6cd`
**LOC do arquivo-fonte:** 181


### `app_bundle.scripts.pipeline.article_workflow.state:WorkflowState.status`

**Assinatura:**
```python
def status(self, stage: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/state.py`
**Linhas:** 148–149
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `WorkflowState`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `status` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `stage` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `stage` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `self.record(stage).status`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `self.record`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.article_workflow.state:WorkflowState.is_ok`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f681a4157b3fa2b7a1cca07dd7b49de015e559cb38a422e8e4aab6ba7a71a6cd`
**LOC do arquivo-fonte:** 181


### `app_bundle.scripts.pipeline.article_workflow.state:WorkflowState.is_ok`

**Assinatura:**
```python
def is_ok(self, stage: str) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/state.py`
**Linhas:** 151–152
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `WorkflowState`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Verifica se `ok` conforme as regras codificadas na rotina.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `stage` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `stage` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `self.status(stage) == STATUS_OK`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `self.status`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.article_workflow.state:WorkflowState.can_run`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f681a4157b3fa2b7a1cca07dd7b49de015e559cb38a422e8e4aab6ba7a71a6cd`
**LOC do arquivo-fonte:** 181


### `app_bundle.scripts.pipeline.article_workflow.state:WorkflowState.can_run`

**Assinatura:**
```python
def can_run(self, stage: str) -> tuple[bool, str]
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/state.py`
**Linhas:** 154–160
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `WorkflowState`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Verifica se é possível `run` conforme as regras codificadas na rotina.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `stage` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `stage` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `tuple[bool, str]`. Retornos observados estaticamente incluem: `(False, 'Etapa bloqueada. Antes conclua: ' + ', '.join(missing))`; `(True, '')`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `', '.join`
- `STAGE_DEPS.get`
- `STAGE_LABELS.get`
- `self._normalize`
- `self.is_ok`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f681a4157b3fa2b7a1cca07dd7b49de015e559cb38a422e8e4aab6ba7a71a6cd`
**LOC do arquivo-fonte:** 181


### `app_bundle.scripts.pipeline.article_workflow.state:WorkflowState.mark`

**Assinatura:**
```python
def mark(self, stage: str, status: str, *, evidence: list[str] | None = None, message: str = '') -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/state.py`
**Linhas:** 162–174
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `WorkflowState`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `mark` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `stage` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `stage` recebido pela função; não há descrição individual explícita no código. |
| `status` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `status` recebido pela função; não há descrição individual explícita no código. |
| `evidence` | keyword-only | `list[str] \| None` | `None` | Parâmetro `evidence` recebido pela função; não há descrição individual explícita no código. |
| `message` | keyword-only | `str` | `''` | Parâmetro `message` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `KeyError`
- `StageRecord`
- `rec.to_dict`
- `self._now`
- `self.data['history'].append`
- `self.save`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.mark_human_review`
- `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.mark_stage_ok`
- `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.refresh_from_files`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `KeyError(f'Etapa desconhecida: {stage}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f681a4157b3fa2b7a1cca07dd7b49de015e559cb38a422e8e4aab6ba7a71a6cd`
**LOC do arquivo-fonte:** 181


### `app_bundle.scripts.pipeline.article_workflow.state:WorkflowState.next_pending_stage`

**Assinatura:**
```python
def next_pending_stage(self) -> str | None
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/state.py`
**Linhas:** 176–181
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `WorkflowState`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `next_pending_stage` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str | None`. Retornos observados estaticamente incluem: `key`; `None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `self.data.get`
- `stages.get`
- `stages.get(key, {}).get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.article_workflow.state:WorkflowState._normalize`
- `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.format_status`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f681a4157b3fa2b7a1cca07dd7b49de015e559cb38a422e8e4aab6ba7a71a6cd`
**LOC do arquivo-fonte:** 181


### `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.__init__`

**Assinatura:**
```python
def __init__(self, art_dir: Path, *, cfg_art: Path | None = None, prisma_cfg: Path | None = None) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/validators.py`
**Linhas:** 45–50
**Tipo:** method
**Visibilidade:** especial/dunder
**Classe proprietária:** `ArticleWorkflow`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `__init__` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `art_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `art_dir` recebido pela função; não há descrição individual explícita no código. |
| `cfg_art` | keyword-only | `Path \| None` | `None` | Parâmetro `cfg_art` recebido pela função; não há descrição individual explícita no código. |
| `prisma_cfg` | keyword-only | `Path \| None` | `None` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(art_dir).expanduser`
- `Path(art_dir).expanduser().resolve`
- `Path(cfg_art).expanduser`
- `Path(cfg_art).expanduser().resolve`
- `Path(prisma_cfg).expanduser`
- `Path(prisma_cfg).expanduser().resolve`
- `WorkflowState`
- `self._guess_article_cfg`
- `self._store_metadata`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `artigo_state.json`

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f2eb8379ca042a32eddabfe972a74fe5df9850d646ab04734b0339bf68211a83`
**LOC do arquivo-fonte:** 305


### `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow._store_metadata`

**Assinatura:**
```python
def _store_metadata(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/validators.py`
**Linhas:** 52–59
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `ArticleWorkflow`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_store_metadata` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `self.state.data.setdefault`
- `self.state.save`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.__init__`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f2eb8379ca042a32eddabfe972a74fe5df9850d646ab04734b0339bf68211a83`
**LOC do arquivo-fonte:** 305


### `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow._guess_article_cfg`

**Assinatura:**
```python
def _guess_article_cfg(self) -> Path | None
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/validators.py`
**Linhas:** 61–63
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `ArticleWorkflow`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_guess_article_cfg` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path | None`. Retornos observados estaticamente incluem: `tomls[0].resolve() if tomls else None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `p.stat`
- `self.art_dir.exists`
- `self.art_dir.glob`
- `sorted`
- `tomls[0].resolve`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.__init__`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `*.toml`

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f2eb8379ca042a32eddabfe972a74fe5df9850d646ab04734b0339bf68211a83`
**LOC do arquivo-fonte:** 305


### `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.output_dir`

**Assinatura:**
```python
def output_dir(self) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/validators.py`
**Linhas:** 66–67
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `ArticleWorkflow`
**Decoradores:** `property`
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `output_dir` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `self.art_dir / 'output'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

Nenhum identificado pela análise estática.

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f2eb8379ca042a32eddabfe972a74fe5df9850d646ab04734b0339bf68211a83`
**LOC do arquivo-fonte:** 305


### `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.dados_prisma_dir`

**Assinatura:**
```python
def dados_prisma_dir(self) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/validators.py`
**Linhas:** 70–71
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `ArticleWorkflow`
**Decoradores:** `property`
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `dados_prisma_dir` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `self.art_dir / 'dados_prisma'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

Nenhum identificado pela análise estática.

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f2eb8379ca042a32eddabfe972a74fe5df9850d646ab04734b0339bf68211a83`
**LOC do arquivo-fonte:** 305


### `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.prefix`

**Assinatura:**
```python
def prefix(self) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/validators.py`
**Linhas:** 74–77
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `ArticleWorkflow`
**Decoradores:** `property`
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `prefix` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `self.cfg_art.stem`; `'artigo_final'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

Nenhum identificado pela análise estática.

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f2eb8379ca042a32eddabfe972a74fe5df9850d646ab04734b0339bf68211a83`
**LOC do arquivo-fonte:** 305


### `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.org_path`

**Assinatura:**
```python
def org_path(self) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/validators.py`
**Linhas:** 80–81
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `ArticleWorkflow`
**Decoradores:** `property`
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `org_path` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `self.output_dir / f'{self.prefix}.org'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

Nenhum identificado pela análise estática.

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.org`

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f2eb8379ca042a32eddabfe972a74fe5df9850d646ab04734b0339bf68211a83`
**LOC do arquivo-fonte:** 305


### `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.bib_path`

**Assinatura:**
```python
def bib_path(self) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/validators.py`
**Linhas:** 84–85
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `ArticleWorkflow`
**Decoradores:** `property`
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `bib_path` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `self.output_dir / f'{self.prefix}.bib'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

Nenhum identificado pela análise estática.

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.bib`

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f2eb8379ca042a32eddabfe972a74fe5df9850d646ab04734b0339bf68211a83`
**LOC do arquivo-fonte:** 305


### `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.pdf_path`

**Assinatura:**
```python
def pdf_path(self) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/validators.py`
**Linhas:** 88–89
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `ArticleWorkflow`
**Decoradores:** `property`
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `pdf_path` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `self.output_dir / f'{self.prefix}.pdf'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

Nenhum identificado pela análise estática.

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.pdf`

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f2eb8379ca042a32eddabfe972a74fe5df9850d646ab04734b0339bf68211a83`
**LOC do arquivo-fonte:** 305


### `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow._read_toml`

**Assinatura:**
```python
def _read_toml(self, path: Path) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/validators.py`
**Linhas:** 91–94
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `ArticleWorkflow`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Lê a responsabilidade representada por `toml` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `raw if isinstance(raw, dict) else {}`.

#### Efeitos colaterais e fronteiras externas

- Abre arquivo para leitura ou inspeção.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `isinstance`
- `path.open`
- `tomllib.load`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.research_output_dir`
- `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.validate_toml_prisma`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f2eb8379ca042a32eddabfe972a74fe5df9850d646ab04734b0339bf68211a83`
**LOC do arquivo-fonte:** 305


### `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow._section`

**Assinatura:**
```python
def _section(cfg: dict[str, Any], name: str) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/validators.py`
**Linhas:** 97–99
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `ArticleWorkflow`
**Decoradores:** `staticmethod`
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_section` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `name` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `name` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `value if isinstance(value, dict) else {}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `cfg.get`
- `isinstance`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f2eb8379ca042a32eddabfe972a74fe5df9850d646ab04734b0339bf68211a83`
**LOC do arquivo-fonte:** 305


### `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow._resolve_config_path`

**Assinatura:**
```python
def _resolve_config_path(config_path: Path, raw: Any) -> Path | None
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/validators.py`
**Linhas:** 102–109
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `ArticleWorkflow`
**Decoradores:** `staticmethod`
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Resolve a responsabilidade representada por `config_path` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `config_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `raw` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `raw` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path | None`. Retornos observados estaticamente incluem: `None`; `path.resolve()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(value).expanduser`
- `path.is_absolute`
- `path.resolve`
- `str`
- `str(raw or '').strip`
- `value.startswith`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.research_output_dir`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f2eb8379ca042a32eddabfe972a74fe5df9850d646ab04734b0339bf68211a83`
**LOC do arquivo-fonte:** 305


### `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.research_output_dir`

**Assinatura:**
```python
def research_output_dir(self) -> Path | None
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/validators.py`
**Linhas:** 111–123
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `ArticleWorkflow`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `research_output_dir` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path | None`. Retornos observados estaticamente incluem: `None`; `base / prefix if bool(paths.get('create_research_subdir', True)) else base`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `bool`
- `paths.get`
- `project.get`
- `self._read_toml`
- `self._resolve_config_path`
- `self._section`
- `self.prisma_cfg.exists`
- `str`
- `str(paths.get('research_prefix') or f"relatorio_prisma_{project.get('nome') or self.prisma_cfg.stem}").strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.find_prisma_artifacts`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `f2eb8379ca042a32eddabfe972a74fe5df9850d646ab04734b0339bf68211a83`
**LOC do arquivo-fonte:** 305


### `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow._glob_any`

**Assinatura:**
```python
def _glob_any(self, bases: list[Path], patterns: list[str]) -> list[Path]
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/validators.py`
**Linhas:** 125–132
**Tipo:** method
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `ArticleWorkflow`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `_glob_any` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `bases` | positional-or-keyword | `list[Path]` | `obrigatório/ausente` | Parâmetro `bases` recebido pela função; não há descrição individual explícita no código. |
| `patterns` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `patterns` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[Path]`. Retornos observados estaticamente incluem: `sorted(set(found), key=lambda p: p.stat().st_mtime, reverse=True)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `base.exists`
- `base.glob`
- `found.extend`
- `p.is_file`
- `p.stat`
- `set`
- `sorted`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.find_prisma_artifacts`
- `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.validate_briefing`
- `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.validate_fulltext`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 2
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f2eb8379ca042a32eddabfe972a74fe5df9850d646ab04734b0339bf68211a83`
**LOC do arquivo-fonte:** 305


### `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.find_prisma_artifacts`

**Assinatura:**
```python
def find_prisma_artifacts(self, patterns: list[str]) -> list[Path]
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/validators.py`
**Linhas:** 134–139
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `ArticleWorkflow`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Localiza a responsabilidade representada por `prisma_artifacts` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `patterns` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `patterns` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[Path]`. Retornos observados estaticamente incluem: `self._glob_any(bases, patterns)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `bases.insert`
- `self._glob_any`
- `self.research_output_dir`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.mark_prisma_xlsx_reviewed`
- `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.validate_prisma_final`
- `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.validate_prisma_preliminar`
- `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.validate_revisao_humana`
- `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.validate_xlsx_cut`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f2eb8379ca042a32eddabfe972a74fe5df9850d646ab04734b0339bf68211a83`
**LOC do arquivo-fonte:** 305


### `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.validate_briefing`

**Assinatura:**
```python
def validate_briefing(self) -> StageValidation
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/validators.py`
**Linhas:** 141–149
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `ArticleWorkflow`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Valida a responsabilidade representada por `briefing` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `StageValidation`. Retornos observados estaticamente incluem: `StageValidation('briefing', True, STATUS_OK, 'Briefing localizado e preenchido.', [str(p)])`; `StageValidation('briefing', False, STATUS_PENDING, 'Crie ou preencha o briefing do artigo.', [str(p) for p in candidates[:3]])`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `StageValidation`
- `len`
- `p.read_text`
- `p.read_text(encoding='utf-8', errors='ignore').strip`
- `self._glob_any`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `briefing_artigo.txt`
- `briefing*.txt`
- `briefing*.md`
- `tema*.txt`

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 1
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `f2eb8379ca042a32eddabfe972a74fe5df9850d646ab04734b0339bf68211a83`
**LOC do arquivo-fonte:** 305


### `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.validate_toml_prisma`

**Assinatura:**
```python
def validate_toml_prisma(self) -> StageValidation
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/validators.py`
**Linhas:** 151–160
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `ArticleWorkflow`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Valida a responsabilidade representada por `toml_prisma` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `StageValidation`. Retornos observados estaticamente incluem: `StageValidation('toml_prisma', False, STATUS_PENDING, 'Selecione ou gere o TOML PRISMA preliminar.', [])`; `StageValidation('toml_prisma', False, STATUS_ERROR, f'TOML PRISMA ilegível: {exc}', [str(self.prisma_cfg)])`; `StageValidation('toml_prisma', False, STATUS_ERROR, 'TOML não parece ser configuração válida da pipeline.', [str(self.prisma_cfg)])`; `StageValidation('toml_prisma', True, STATUS_OK, 'TOML PRISMA válido.', [str(self.prisma_cfg)])`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `StageValidation`
- `self._read_toml`
- `self._section`
- `self.prisma_cfg.exists`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `f2eb8379ca042a32eddabfe972a74fe5df9850d646ab04734b0339bf68211a83`
**LOC do arquivo-fonte:** 305


### `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.validate_prisma_preliminar`

**Assinatura:**
```python
def validate_prisma_preliminar(self) -> StageValidation
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/validators.py`
**Linhas:** 162–166
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `ArticleWorkflow`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Valida a responsabilidade representada por `prisma_preliminar` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `StageValidation`. Retornos observados estaticamente incluem: `StageValidation('prisma_preliminar', True, STATUS_OK, 'Saídas da busca PRISMA preliminar localizadas.', [str(p) for p in files[:5]])`; `StageValidation('prisma_preliminar', False, STATUS_PENDING, 'Rode a busca PRISMA preliminar.', [])`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `StageValidation`
- `self.find_prisma_artifacts`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `*.busca_prisma_log.json`
- `*.triagem_titulo_resumo.csv`
- `*.relatorio_prisma_preliminar.pdf`

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f2eb8379ca042a32eddabfe972a74fe5df9850d646ab04734b0339bf68211a83`
**LOC do arquivo-fonte:** 305


### `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.validate_xlsx_cut`

**Assinatura:**
```python
def validate_xlsx_cut(self) -> StageValidation
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/validators.py`
**Linhas:** 168–172
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `ArticleWorkflow`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Valida a responsabilidade representada por `xlsx_cut` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `StageValidation`. Retornos observados estaticamente incluem: `StageValidation('xlsx_cut', True, STATUS_OK, 'XLSX cut de curadoria IA localizado.', [str(files[0])])`; `StageValidation('xlsx_cut', False, STATUS_PENDING, 'Gere o XLSX cut de referências pela IA.', [])`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `StageValidation`
- `self.find_prisma_artifacts`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `*.curadoria_ia_referencias.xlsx`

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f2eb8379ca042a32eddabfe972a74fe5df9850d646ab04734b0339bf68211a83`
**LOC do arquivo-fonte:** 305


### `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.validate_revisao_humana`

**Assinatura:**
```python
def validate_revisao_humana(self) -> StageValidation
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/validators.py`
**Linhas:** 174–183
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `ArticleWorkflow`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Valida a responsabilidade representada por `revisao_humana` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `StageValidation`. Retornos observados estaticamente incluem: `StageValidation('revisao_humana_xlsx', True, STATUS_OK, 'Revisão humana do XLSX confirmada.', [str(xlsx_path)])`; `StageValidation('revisao_humana_xlsx', False, STATUS_PENDING, 'Abra, revise, salve e confirme a revisão humana do XLSX.', [str(files[0])])`; `StageValidation('revisao_humana_xlsx', False, STATUS_BLOCKED, 'Aguardando geração do XLSX cut.', [])`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(str(hr.get('xlsx_path') or '')).expanduser`
- `StageValidation`
- `bool`
- `hr.get`
- `isinstance`
- `self.find_prisma_artifacts`
- `self.state.data.get`
- `str`
- `xlsx_path.exists`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `*.curadoria_ia_referencias.xlsx`

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f2eb8379ca042a32eddabfe972a74fe5df9850d646ab04734b0339bf68211a83`
**LOC do arquivo-fonte:** 305


### `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.validate_prisma_final`

**Assinatura:**
```python
def validate_prisma_final(self) -> StageValidation
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/validators.py`
**Linhas:** 185–189
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `ArticleWorkflow`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Valida a responsabilidade representada por `prisma_final` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `StageValidation`. Retornos observados estaticamente incluem: `StageValidation('prisma_final', True, STATUS_OK, 'PRISMA final e referências incluídas localizados.', [str(p) for p in files[:5]])`; `StageValidation('prisma_final', False, STATUS_PENDING, 'Gere/importa o PRISMA final após a revisão humana.', [])`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `StageValidation`
- `self.find_prisma_artifacts`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `*.referencias_incluidas.bib`
- `*.referencias_incluidas.csv`
- `*.triagem_humana.csv`
- `*.relatorio_prisma_final.pdf`

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f2eb8379ca042a32eddabfe972a74fe5df9850d646ab04734b0339bf68211a83`
**LOC do arquivo-fonte:** 305


### `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.validate_fulltext`

**Assinatura:**
```python
def validate_fulltext(self) -> StageValidation
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/validators.py`
**Linhas:** 191–200
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `ArticleWorkflow`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Valida a responsabilidade representada por `fulltext` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `StageValidation`. Retornos observados estaticamente incluem: `StageValidation('fulltext', True, STATUS_OK, 'Corpus/full text preparado localizado.', [str(p) for p in files[:5]])`; `StageValidation('fulltext', False, STATUS_PENDING, 'Prepare full text/corpus pelo gerador unificado.', [])`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `StageValidation`
- `self._glob_any`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `artigo_longo_fulltext/corpus_fulltext_compilado.md`
- `artigo_longo_fulltext/referencias_incluidas_*.md`
- `fulltext_garantido/*.referencias_incluidas_fulltext_garantido.bib`
- `fulltext_garantido/*.matriz_prisma_fulltext_garantido.csv`

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f2eb8379ca042a32eddabfe972a74fe5df9850d646ab04734b0339bf68211a83`
**LOC do arquivo-fonte:** 305


### `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.validate_artigo_org`

**Assinatura:**
```python
def validate_artigo_org(self) -> StageValidation
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/validators.py`
**Linhas:** 202–220
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `ArticleWorkflow`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Valida a responsabilidade representada por `artigo_org` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `StageValidation`. Retornos observados estaticamente incluem: `StageValidation('artigo_org', False, STATUS_ERROR, '; '.join(problems), evidence)`; `StageValidation('artigo_org', True, STATUS_OK, 'ORG/BIB finalizados em padrão ABNT/Biber.', evidence)`; `StageValidation('artigo_org', False, STATUS_PENDING, 'Gere o artigo ORG/BIB final.', evidence)`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `'; '.join`
- `StageValidation`
- `evidence.append`
- `problems.append`
- `self.bib_path.exists`
- `self.org_path.exists`
- `self.org_path.read_text`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 7
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f2eb8379ca042a32eddabfe972a74fe5df9850d646ab04734b0339bf68211a83`
**LOC do arquivo-fonte:** 305


### `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.validate_pdf_final`

**Assinatura:**
```python
def validate_pdf_final(self) -> StageValidation
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/validators.py`
**Linhas:** 222–239
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `ArticleWorkflow`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Valida a responsabilidade representada por `pdf_final` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `StageValidation`. Retornos observados estaticamente incluem: `StageValidation('pdf_final', False, STATUS_PENDING, 'PDF final ainda não localizado.', [])`; `StageValidation('pdf_final', False, STATUS_ERROR, 'pdftotext falhou ao validar PDF.', evidence)`; `StageValidation('pdf_final', False, STATUS_ERROR, 'PDF contém padrões inválidos: ' + ' | '.join(hits[:3]), evidence)`; `StageValidation('pdf_final', True, STATUS_OK, 'PDF final validado.', evidence)`.

#### Efeitos colaterais e fronteiras externas

- Executa processo ou ferramenta externa.

#### Chamadas realizadas pelo corpo direto

- `' | '.join`
- `(proc.stdout or b'').decode`
- `StageValidation`
- `enumerate`
- `hits.append`
- `re.search`
- `self.pdf_path.exists`
- `shutil.which`
- `str`
- `subprocess.run`
- `txt.splitlines`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f2eb8379ca042a32eddabfe972a74fe5df9850d646ab04734b0339bf68211a83`
**LOC do arquivo-fonte:** 305


### `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.validations`

**Assinatura:**
```python
def validations(self) -> list[StageValidation]
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/validators.py`
**Linhas:** 241–253
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `ArticleWorkflow`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `validations` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[StageValidation]`. Retornos observados estaticamente incluem: `[fn() for fn in validators]`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `fn`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.refresh_from_files`
- `app_bundle.scripts.pipeline.artigo_prisma_workflow:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f2eb8379ca042a32eddabfe972a74fe5df9850d646ab04734b0339bf68211a83`
**LOC do arquivo-fonte:** 305


### `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.refresh_from_files`

**Assinatura:**
```python
def refresh_from_files(self) -> list[StageValidation]
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/validators.py`
**Linhas:** 255–265
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `ArticleWorkflow`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `refresh_from_files` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[StageValidation]`. Retornos observados estaticamente incluem: `results`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `self.state.mark`
- `self.validations`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI._ask_article_final_settings`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.generate_article_final_unified`
- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.mark_prisma_xlsx_reviewed`
- `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.can_run`
- `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.format_status`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f2eb8379ca042a32eddabfe972a74fe5df9850d646ab04734b0339bf68211a83`
**LOC do arquivo-fonte:** 305


### `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.mark_human_review`

**Assinatura:**
```python
def mark_human_review(self, xlsx_path: Path) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/validators.py`
**Linhas:** 267–276
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `ArticleWorkflow`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `mark_human_review` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `xlsx_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(xlsx_path).expanduser`
- `Path(xlsx_path).expanduser().resolve`
- `self.state._now`
- `self.state.data.setdefault`
- `self.state.mark`
- `str`
- `xlsx.exists`
- `xlsx.stat`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI.mark_prisma_xlsx_reviewed`
- `app_bundle.scripts.pipeline.artigo_prisma_workflow:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f2eb8379ca042a32eddabfe972a74fe5df9850d646ab04734b0339bf68211a83`
**LOC do arquivo-fonte:** 305


### `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.mark_stage_ok`

**Assinatura:**
```python
def mark_stage_ok(self, stage: str, *, evidence: list[str] | None = None, message: str = '') -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/validators.py`
**Linhas:** 278–279
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `ArticleWorkflow`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `mark_stage_ok` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `stage` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `stage` recebido pela função; não há descrição individual explícita no código. |
| `evidence` | keyword-only | `list[str] \| None` | `None` | Parâmetro `evidence` recebido pela função; não há descrição individual explícita no código. |
| `message` | keyword-only | `str` | `''` | Parâmetro `message` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `self.state.mark`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI._article_workflow_mark_ok`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f2eb8379ca042a32eddabfe972a74fe5df9850d646ab04734b0339bf68211a83`
**LOC do arquivo-fonte:** 305


### `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.can_run`

**Assinatura:**
```python
def can_run(self, stage: str) -> tuple[bool, str]
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/validators.py`
**Linhas:** 281–283
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `ArticleWorkflow`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Verifica se é possível `run` conforme as regras codificadas na rotina.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `stage` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `stage` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `tuple[bool, str]`. Retornos observados estaticamente incluem: `self.state.can_run(stage)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `self.refresh_from_files`
- `self.state.can_run`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f2eb8379ca042a32eddabfe972a74fe5df9850d646ab04734b0339bf68211a83`
**LOC do arquivo-fonte:** 305


### `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.format_status`

**Assinatura:**
```python
def format_status(self) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/article_workflow/validators.py`
**Linhas:** 285–305
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `ArticleWorkflow`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Formata a responsabilidade representada por `status` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'\n'.join(lines)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `lines.append`
- `next`
- `rec.status.upper`
- `self.refresh_from_files`
- `self.state.next_pending_stage`
- `self.state.record`
- `{'ok': '[OK]', 'pendente': '[PENDENTE]', 'bloqueado': '[BLOQUEADO]', 'erro': '[ERRO]', 'stale': '[STALE]'}.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_tui:AcademicPipelineTUI._article_workflow_status_text`
- `app_bundle.scripts.pipeline.artigo_prisma_workflow:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f2eb8379ca042a32eddabfe972a74fe5df9850d646ab04734b0339bf68211a83`
**LOC do arquivo-fonte:** 305


### `app_bundle.scripts.pipeline.artigo_prisma_workflow:build_workflow`

**Assinatura:**
```python
def build_workflow(args: argparse.Namespace) -> ArticleWorkflow
```

**Arquivo:** `app_bundle/scripts/pipeline/artigo_prisma_workflow.py`
**Linhas:** 14–19
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Constrói a responsabilidade representada por `workflow` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `argparse.Namespace` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Anotação declarada: `ArticleWorkflow`. Retornos observados estaticamente incluem: `ArticleWorkflow(Path(args.art_dir), cfg_art=Path(args.cfg_art).expanduser().resolve() if args.cfg_art else None, prisma_cfg=Path(args.prisma_cfg).expanduser().resolve() if args.prisma_cfg else None)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `ArticleWorkflow`
- `Path`
- `Path(args.cfg_art).expanduser`
- `Path(args.cfg_art).expanduser().resolve`
- `Path(args.prisma_cfg).expanduser`
- `Path(args.prisma_cfg).expanduser().resolve`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.artigo_prisma_workflow:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `62578a918b0064c7812c2425e90d8652d16d1682379fdf1e1fda0f0a5e49baee`
**LOC do arquivo-fonte:** 51


### `app_bundle.scripts.pipeline.artigo_prisma_workflow:main`

**Assinatura:**
```python
def main(argv: Iterable[str] | None = None) -> int
```

**Arquivo:** `app_bundle/scripts/pipeline/artigo_prisma_workflow.py`
**Linhas:** 22–47
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa o ponto de entrada correspondente e coordena o fluxo associado.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `Iterable[str] \| None` | `None` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `0 if all((item.ok for item in results)) else 1`; `0`; `2`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `SystemExit`
- `all`
- `argparse.ArgumentParser`
- `build_workflow`
- `list`
- `parser.add_argument`
- `parser.parse_args`
- `print`
- `wf.format_status`
- `wf.mark_human_review`
- `wf.validations`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `SystemExit('ERRO: informe --xlsx para confirmar a revisão humana.')`

#### Opções CLI mencionadas diretamente no corpo

- `--art-dir`
- `--cfg-art`
- `--prisma-cfg`
- `--xlsx`

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `62578a918b0064c7812c2425e90d8652d16d1682379fdf1e1fda0f0a5e49baee`
**LOC do arquivo-fonte:** 51


### `app_bundle.scripts.pipeline.bibliography_manager:_metadata_env`

**Assinatura:**
```python
def _metadata_env(*names: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/bibliography_manager.py`
**Linhas:** 59–65
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Lê a primeira variável de ambiente não vazia, sem expor seu valor.

**Docstring resumida:** Lê a primeira variável de ambiente não vazia, sem expor seu valor.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `names` | var-positional | `str` | `obrigatório/ausente` | Parâmetro `names` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `value`; `''`.

#### Efeitos colaterais e fronteiras externas

- Lê configuração/credenciais do ambiente do processo.

#### Chamadas realizadas pelo corpo direto

- `os.getenv`
- `os.getenv(name, '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.bibliography_manager:crossref_by_doi`
- `app_bundle.scripts.pipeline.bibliography_manager:metadata_provider_statuses`
- `app_bundle.scripts.pipeline.bibliography_manager:openalex_by_doi`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `ccf7efa67c51a306a53f604b4bc98e3f43d9434a01f09147138a9c19f7236d16`
**LOC do arquivo-fonte:** 632


### `app_bundle.scripts.pipeline.bibliography_manager:metadata_provider_statuses`

**Assinatura:**
```python
def metadata_provider_statuses() -> dict[str, dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/bibliography_manager.py`
**Linhas:** 68–101
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Informa capacidade de consulta por fonte, sem revelar credenciais.

**Docstring resumida:** Informa capacidade de consulta por fonte, sem revelar credenciais.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `dict[str, dict[str, Any]]`. Retornos observados estaticamente incluem: `{'crossref': {'label': 'Crossref', 'available': True, 'status': 'disponível — e-mail de contato detectado' if crossref_email else 'disponível — sem e-mail de contato no .env'}, 'openalex': {'label': 'OpenAlex', 'available': True, 'status': 'disponível — e-mail de contato detectado' if openalex_email else 'disponível — sem e-mail de contato no .env'}, 'semantic_scholar': {'label': 'Semantic Scholar', 'available': True, 'status': 'chave detectada' if semantic_key else 'disponível sem chave — sujeito a limite público'}, 'scopus': {'label': 'Scopus', 'available': bool(scopus_key), 'status': 'chave detectada' if scopus_key else 'chave não detectada — consulta será ignorada'}}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_metadata_env`
- `bool`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_metadata_sources`
- `app_bundle.scripts.pipeline.bibliography_manager:metadata_provider_selection_choices`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `ccf7efa67c51a306a53f604b4bc98e3f43d9434a01f09147138a9c19f7236d16`
**LOC do arquivo-fonte:** 632


### `app_bundle.scripts.pipeline.bibliography_manager:metadata_provider_selection_choices`

**Assinatura:**
```python
def metadata_provider_selection_choices() -> list[tuple[str, str]]
```

**Arquivo:** `app_bundle/scripts/pipeline/bibliography_manager.py`
**Linhas:** 104–111
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Retorna opções visuais para a tela de fontes de metadados.

**Docstring resumida:** Retorna opções visuais para a tela de fontes de metadados.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `list[tuple[str, str]]`. Retornos observados estaticamente incluem: `choices`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `choices.append`
- `metadata_provider_statuses`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_metadata_sources`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `ccf7efa67c51a306a53f604b4bc98e3f43d9434a01f09147138a9c19f7236d16`
**LOC do arquivo-fonte:** 632


### `app_bundle.scripts.pipeline.bibliography_manager:expand_metadata_provider_selection`

**Assinatura:**
```python
def expand_metadata_provider_selection(values: list[str] | tuple[str, ...] | set[str]) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/bibliography_manager.py`
**Linhas:** 114–125
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Expande a opção mestre ``todas`` para as fontes suportadas.

**Docstring resumida:** Expande a opção mestre ``todas`` para as fontes suportadas.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `values` | positional-or-keyword | `list[str] \| tuple[str, ...] \| set[str]` | `obrigatório/ausente` | Parâmetro `values` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `list(METADATA_SOURCE_ORDER)`; `out`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `list`
- `out.append`
- `str`
- `str(value).strip`
- `str(value).strip().lower`
- `str(value).strip().lower().replace`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_metadata_sources`
- `app_bundle.scripts.pipeline.bibliography_manager:_metadata_sources`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `ccf7efa67c51a306a53f604b4bc98e3f43d9434a01f09147138a9c19f7236d16`
**LOC do arquivo-fonte:** 632


### `app_bundle.scripts.pipeline.bibliography_manager:normalize_doi`

**Assinatura:**
```python
def normalize_doi(value: str | None) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/bibliography_manager.py`
**Linhas:** 156–160
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Normaliza a responsabilidade representada por `doi` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `str \| None` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `doi.strip().strip('.')`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `doi.strip`
- `doi.strip().strip`
- `re.sub`
- `str`
- `str(value or '').strip`
- `str(value or '').strip().lower`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `^https?://(?:dx\.)?doi\.org/`

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `ccf7efa67c51a306a53f604b4bc98e3f43d9434a01f09147138a9c19f7236d16`
**LOC do arquivo-fonte:** 632


### `app_bundle.scripts.pipeline.bibliography_manager:extract_doi_from_text`

**Assinatura:**
```python
def extract_doi_from_text(text: str) -> str | None
```

**Arquivo:** `app_bundle/scripts/pipeline/bibliography_manager.py`
**Linhas:** 163–167
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Extrai a responsabilidade representada por `doi_from_text` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `str | None`. Retornos observados estaticamente incluem: `None`; `normalize_doi(m.group(0).rstrip('.,);]'))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `m.group`
- `m.group(0).rstrip`
- `normalize_doi`
- `re.search`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.bibliography_manager:doi_for_doc`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `ccf7efa67c51a306a53f604b4bc98e3f43d9434a01f09147138a9c19f7236d16`
**LOC do arquivo-fonte:** 632


### `app_bundle.scripts.pipeline.bibliography_manager:load_doi_manifest`

**Assinatura:**
```python
def load_doi_manifest(path: Path | None) -> dict[str, str]
```

**Arquivo:** `app_bundle/scripts/pipeline/bibliography_manager.py`
**Linhas:** 170–185
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Carrega a responsabilidade representada por `doi_manifest` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path \| None` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `dict[str, str]`. Retornos observados estaticamente incluem: `result`.

#### Efeitos colaterais e fronteiras externas

- Abre arquivo para leitura ou inspeção.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `csv.DictReader`
- `filename.lower`
- `normalize_doi`
- `normalize_title_loose`
- `p.name.lower`
- `p.stem.lower`
- `path.exists`
- `path.open`
- `row.get`
- `str`
- `str(row.get('arquivo') or row.get('filename') or row.get('file') or row.get('path') or '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.bibliography_manager:build_bibliography`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `ccf7efa67c51a306a53f604b4bc98e3f43d9434a01f09147138a9c19f7236d16`
**LOC do arquivo-fonte:** 632


### `app_bundle.scripts.pipeline.bibliography_manager:doi_for_doc`

**Assinatura:**
```python
def doi_for_doc(doc: SourceDoc, manifest: dict[str, str]) -> tuple[str | None, str | None]
```

**Arquivo:** `app_bundle/scripts/pipeline/bibliography_manager.py`
**Linhas:** 188–197
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `doi_for_doc` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doc` | positional-or-keyword | `SourceDoc` | `obrigatório/ausente` | Documento/registro individual processado pela rotina. |
| `manifest` | positional-or-keyword | `dict[str, str]` | `obrigatório/ausente` | Parâmetro `manifest` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `tuple[str | None, str | None]`. Retornos observados estaticamente incluem: `(manifest[key], 'manifest')`; `(doi, 'extracted')`; `(None, None)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `extract_doi_from_text`
- `normalize_title_loose`
- `p.name.lower`
- `p.stem.lower`
- `str`
- `str(p).lower`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.bibliography_manager:_match_doc_to_bib_key`
- `app_bundle.scripts.pipeline.bibliography_manager:build_bibliography`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `ccf7efa67c51a306a53f604b4bc98e3f43d9434a01f09147138a9c19f7236d16`
**LOC do arquivo-fonte:** 632


### `app_bundle.scripts.pipeline.bibliography_manager:http_get_json`

**Assinatura:**
```python
def http_get_json(url: str, headers: dict[str, str] | None = None, timeout: int = 25) -> dict[str, Any] | None
```

**Arquivo:** `app_bundle/scripts/pipeline/bibliography_manager.py`
**Linhas:** 200–208
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `http_get_json` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `url` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `url` recebido pela função; não há descrição individual explícita no código. |
| `headers` | positional-or-keyword | `dict[str, str] \| None` | `None` | Cabeçalhos usados em requisição/integração externa. |
| `timeout` | positional-or-keyword | `int` | `25` | Tempo máximo de espera para a operação, quando aplicável. |

#### Retorno

Anotação declarada: `dict[str, Any] | None`. Retornos observados estaticamente incluem: `data if isinstance(data, dict) else None`; `None`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.
- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `isinstance`
- `json.loads`
- `resp.headers.get_content_charset`
- `resp.read`
- `resp.read().decode`
- `urllib.request.Request`
- `urllib.request.urlopen`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.bibliography_manager:crossref_by_doi`
- `app_bundle.scripts.pipeline.bibliography_manager:openalex_by_doi`
- `app_bundle.scripts.pipeline.bibliography_manager:scopus_by_doi`
- `app_bundle.scripts.pipeline.bibliography_manager:semantic_scholar_by_doi`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `ccf7efa67c51a306a53f604b4bc98e3f43d9434a01f09147138a9c19f7236d16`
**LOC do arquivo-fonte:** 632


### `app_bundle.scripts.pipeline.bibliography_manager:_people_crossref`

**Assinatura:**
```python
def _people_crossref(raw: list[dict[str, Any]] | None) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/bibliography_manager.py`
**Linhas:** 211–217
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_people_crossref` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `raw` | positional-or-keyword | `list[dict[str, Any]] \| None` | `obrigatório/ausente` | Parâmetro `raw` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `out`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `' '.join((x for x in [str(a.get('given') or '').strip(), str(a.get('family') or '').strip()] if x)).strip`
- `a.get`
- `out.append`
- `str`
- `str(a.get('family') or '').strip`
- `str(a.get('given') or '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.bibliography_manager:crossref_item_to_meta`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `ccf7efa67c51a306a53f604b4bc98e3f43d9434a01f09147138a9c19f7236d16`
**LOC do arquivo-fonte:** 632


### `app_bundle.scripts.pipeline.bibliography_manager:crossref_item_to_meta`

**Assinatura:**
```python
def crossref_item_to_meta(item: dict[str, Any]) -> BibMetadata
```

**Arquivo:** `app_bundle/scripts/pipeline/bibliography_manager.py`
**Linhas:** 220–246
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `crossref_item_to_meta` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `item` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `item` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `BibMetadata`. Retornos observados estaticamente incluem: `BibMetadata(entry_type=entry_type, title=title, authors=_people_crossref(item.get('author')), editors=_people_crossref(item.get('editor')), year=year, journaltitle=container if entry_type == 'article' else None, booktitle=container if entry_type in {'incollection', 'inbook'} else None, publisher=item.get('publisher'), pages=item.get('page'), volume=item.get('volume'), number=item.get('issue'), doi=normalize_doi(item.get('DOI')), isbn=(item.get('ISBN') or [None])[0] if isinstance(item.get('ISBN'), list) else item.get('ISBN'), url=item.get('URL'))`.

#### Efeitos colaterais e fronteiras externas

- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `' '.join(item.get('container-title') or []).strip`
- `' '.join(item.get('title') or []).strip`
- `BibMetadata`
- `_people_crossref`
- `isinstance`
- `issued.get`
- `item.get`
- `normalize_doi`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.bibliography_manager:crossref_by_doi`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `ccf7efa67c51a306a53f604b4bc98e3f43d9434a01f09147138a9c19f7236d16`
**LOC do arquivo-fonte:** 632


### `app_bundle.scripts.pipeline.bibliography_manager:crossref_by_doi`

**Assinatura:**
```python
def crossref_by_doi(doi: str) -> BibMetadata | None
```

**Arquivo:** `app_bundle/scripts/pipeline/bibliography_manager.py`
**Linhas:** 249–255
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `crossref_by_doi` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doi` | positional-or-keyword | `str` | `obrigatório/ausente` | Digital Object Identifier da referência bibliográfica. |

#### Retorno

Anotação declarada: `BibMetadata | None`. Retornos observados estaticamente incluem: `crossref_item_to_meta(msg) if isinstance(msg, dict) else None`.

#### Efeitos colaterais e fronteiras externas

- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `_metadata_env`
- `crossref_item_to_meta`
- `data.get`
- `http_get_json`
- `isinstance`
- `urllib.parse.quote`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.bibliography_manager:lookup_by_doi`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `https://api.crossref.org/works/`

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `ccf7efa67c51a306a53f604b4bc98e3f43d9434a01f09147138a9c19f7236d16`
**LOC do arquivo-fonte:** 632


### `app_bundle.scripts.pipeline.bibliography_manager:openalex_by_doi`

**Assinatura:**
```python
def openalex_by_doi(doi: str) -> BibMetadata | None
```

**Arquivo:** `app_bundle/scripts/pipeline/bibliography_manager.py`
**Linhas:** 258–289
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `openalex_by_doi` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doi` | positional-or-keyword | `str` | `obrigatório/ausente` | Digital Object Identifier da referência bibliográfica. |

#### Retorno

Anotação declarada: `BibMetadata | None`. Retornos observados estaticamente incluem: `None`; `BibMetadata(entry_type=entry_type, title=str(data.get('display_name') or ''), authors=authors, year=str(data.get('publication_year') or '') or None, journaltitle=source.get('display_name') if entry_type == 'article' else None, publisher=source.get('host_organization_name'), pages=pages, volume=biblio.get('volume'), number=biblio.get('issue'), doi=normalize_doi(data.get('doi')), url=data.get('landing_page_url') or data.get('id'))`.

#### Efeitos colaterais e fronteiras externas

- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `((item or {}).get('author') or {}).get`
- `(data.get('primary_location') or {}).get`
- `(item or {}).get`
- `BibMetadata`
- `_metadata_env`
- `authors.append`
- `biblio.get`
- `data.get`
- `http_get_json`
- `isinstance`
- `normalize_doi`
- `source.get`
- `str`
- `urllib.parse.quote`
- `urllib.parse.urlencode`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.bibliography_manager:lookup_by_doi`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `--`

#### Artefatos/caminhos literais reconhecidos no corpo

- `https://api.openalex.org/works/doi:`

#### Complexidade estrutural observada

```yaml
branches: 8
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `ccf7efa67c51a306a53f604b4bc98e3f43d9434a01f09147138a9c19f7236d16`
**LOC do arquivo-fonte:** 632


### `app_bundle.scripts.pipeline.bibliography_manager:_wait_for_semantic_scholar`

**Assinatura:**
```python
def _wait_for_semantic_scholar() -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/bibliography_manager.py`
**Linhas:** 292–298
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Respeita a cota cumulativa da API Semantic Scholar sem expor a chave.

**Docstring resumida:** Respeita a cota cumulativa da API Semantic Scholar sem expor a chave.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `time.monotonic`
- `time.sleep`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `ccf7efa67c51a306a53f604b4bc98e3f43d9434a01f09147138a9c19f7236d16`
**LOC do arquivo-fonte:** 632


### `app_bundle.scripts.pipeline.bibliography_manager:semantic_scholar_by_doi`

**Assinatura:**
```python
def semantic_scholar_by_doi(doi: str) -> BibMetadata | None
```

**Arquivo:** `app_bundle/scripts/pipeline/bibliography_manager.py`
**Linhas:** 301–317
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `semantic_scholar_by_doi` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doi` | positional-or-keyword | `str` | `obrigatório/ausente` | Digital Object Identifier da referência bibliográfica. |

#### Retorno

Anotação declarada: `BibMetadata | None`. Retornos observados estaticamente incluem: `None`; `BibMetadata(entry_type='article', title=str(data.get('title')), authors=authors, year=str(data.get('year') or '') or None, journaltitle=venue, doi=normalize_doi(doi_found or doi), url=data.get('url'))`.

#### Efeitos colaterais e fronteiras externas

- Lê configuração/credenciais do ambiente do processo.
- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `(data.get('publicationVenue') or {}).get`
- `BibMetadata`
- `_wait_for_semantic_scholar`
- `a.get`
- `data.get`
- `data['externalIds'].get`
- `http_get_json`
- `isinstance`
- `normalize_doi`
- `os.getenv`
- `str`
- `urllib.parse.quote`
- `urllib.parse.urlencode`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.bibliography_manager:lookup_by_doi`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `https://api.semanticscholar.org/graph/v1/paper/DOI:`

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `ccf7efa67c51a306a53f604b4bc98e3f43d9434a01f09147138a9c19f7236d16`
**LOC do arquivo-fonte:** 632


### `app_bundle.scripts.pipeline.bibliography_manager:scopus_by_doi`

**Assinatura:**
```python
def scopus_by_doi(doi: str) -> BibMetadata | None
```

**Arquivo:** `app_bundle/scripts/pipeline/bibliography_manager.py`
**Linhas:** 320–348
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `scopus_by_doi` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doi` | positional-or-keyword | `str` | `obrigatório/ausente` | Digital Object Identifier da referência bibliográfica. |

#### Retorno

Anotação declarada: `BibMetadata | None`. Retornos observados estaticamente incluem: `None`; `BibMetadata(entry_type='article', title=str(title), authors=authors, year=year, journaltitle=item.get('prism:publicationName'), volume=item.get('prism:volume'), number=item.get('prism:issueIdentifier'), doi=normalize_doi(item.get('prism:doi') or doi), url=item.get('prism:url'))`.

#### Efeitos colaterais e fronteiras externas

- Lê configuração/credenciais do ambiente do processo.
- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `((data or {}).get('search-results') or {}).get`
- `(data or {}).get`
- `BibMetadata`
- `http_get_json`
- `isinstance`
- `item.get`
- `m.group`
- `normalize_doi`
- `os.getenv`
- `re.search`
- `str`
- `urllib.parse.urlencode`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.bibliography_manager:lookup_by_doi`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `ccf7efa67c51a306a53f604b4bc98e3f43d9434a01f09147138a9c19f7236d16`
**LOC do arquivo-fonte:** 632


### `app_bundle.scripts.pipeline.bibliography_manager:_metadata_sources`

**Assinatura:**
```python
def _metadata_sources(cfg: dict[str, Any]) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/bibliography_manager.py`
**Linhas:** 351–357
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_metadata_sources` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `out or ['crossref', 'openalex']`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `cfg.get`
- `expand_metadata_provider_selection`
- `isinstance`
- `list`
- `local.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.bibliography_manager:lookup_by_doi`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `ccf7efa67c51a306a53f604b4bc98e3f43d9434a01f09147138a9c19f7236d16`
**LOC do arquivo-fonte:** 632


### `app_bundle.scripts.pipeline.bibliography_manager:lookup_by_doi`

**Assinatura:**
```python
def lookup_by_doi(doi: str, cfg: dict[str, Any]) -> tuple[BibMetadata | None, str]
```

**Arquivo:** `app_bundle/scripts/pipeline/bibliography_manager.py`
**Linhas:** 360–377
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Consulta a responsabilidade representada por `by_doi` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doi` | positional-or-keyword | `str` | `obrigatório/ausente` | Digital Object Identifier da referência bibliográfica. |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `tuple[BibMetadata | None, str]`. Retornos observados estaticamente incluem: `(meta, source)`; `(None, 'doi_lookup_failed')`.

#### Efeitos colaterais e fronteiras externas

- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `_metadata_sources`
- `crossref_by_doi`
- `openalex_by_doi`
- `scopus_by_doi`
- `semantic_scholar_by_doi`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.bibliography_manager:build_bibliography`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 1
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `ccf7efa67c51a306a53f604b4bc98e3f43d9434a01f09147138a9c19f7236d16`
**LOC do arquivo-fonte:** 632


### `app_bundle.scripts.pipeline.bibliography_manager:infer_bib_metadata_ai`

**Assinatura:**
```python
def infer_bib_metadata_ai(client: Any, model: str, doc: SourceDoc, doi: str | None = None, cfg: dict[str, Any] | None = None) -> BibMetadata
```

**Arquivo:** `app_bundle/scripts/pipeline/bibliography_manager.py`
**Linhas:** 380–402
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Infere a responsabilidade representada por `bib_metadata_ai` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `client` | positional-or-keyword | `Any` | `obrigatório/ausente` | Cliente de serviço externo/IA utilizado pela rotina; pode ser opcional conforme o fluxo. |
| `model` | positional-or-keyword | `str` | `obrigatório/ausente` | Identificador do modelo utilizado pela etapa de IA. |
| `doc` | positional-or-keyword | `SourceDoc` | `obrigatório/ausente` | Documento/registro individual processado pela rotina. |
| `doi` | positional-or-keyword | `str \| None` | `None` | Digital Object Identifier da referência bibliográfica. |
| `cfg` | positional-or-keyword | `dict[str, Any] \| None` | `None` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `BibMetadata`. Retornos observados estaticamente incluem: `meta`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.
- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `RuntimeError`
- `client.responses.parse`
- `f"\nExtraia metadados bibliográficos do documento abaixo e retorne JSON no schema solicitado.\nRegras: não invente DOI, páginas, periódico, editora ou autores. Se souber que é livro, use entry_type=book; se capítulo, incollection; se artigo, article.\nNão inclua observações técnicas.\n\nDiretivas complementares carregadas pelo prompt bank:\n{prompt_extras}\n\nDOI conhecido, se houver: {doi or ''}\nArquivo: {doc.label}\nTexto extraído:\n{shorten_text(doc.extracted_text, 25000)}\n".strip`
- `load_prompt_bundle`
- `shorten_text`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.bibliography_manager:build_bibliography`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('IA não retornou metadados bibliográficos estruturados.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `ccf7efa67c51a306a53f604b4bc98e3f43d9434a01f09147138a9c19f7236d16`
**LOC do arquivo-fonte:** 632


### `app_bundle.scripts.pipeline.bibliography_manager:make_bib_key`

**Assinatura:**
```python
def make_bib_key(meta: BibMetadata, used: set[str]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/bibliography_manager.py`
**Linhas:** 405–419
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Cria a responsabilidade representada por `bib_key` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `meta` | positional-or-keyword | `BibMetadata` | `obrigatório/ausente` | Parâmetro `meta` recebido pela função; não há descrição individual explícita no código. |
| `used` | positional-or-keyword | `set[str]` | `obrigatório/ausente` | Parâmetro `used` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `key`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'_'.join`
- `len`
- `normalize_title_loose`
- `normalize_title_loose(author).split`
- `normalize_title_loose(meta.title).split`
- `re.search`
- `str`
- `used.add`
- `year.group`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.bibliography_manager:build_bibliography`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 1
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `ccf7efa67c51a306a53f604b4bc98e3f43d9434a01f09147138a9c19f7236d16`
**LOC do arquivo-fonte:** 632


### `app_bundle.scripts.pipeline.bibliography_manager:bibtex_escape`

**Assinatura:**
```python
def bibtex_escape(text: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/bibliography_manager.py`
**Linhas:** 422–425
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `bibtex_escape` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `''.join((repl.get(ch, ch) for ch in text))`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `''.join`
- `repl.get`
- `str`
- `str(text or '').replace`
- `str(text or '').replace('\n', ' ').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.bibliography_manager:render_bib_entry`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `ccf7efa67c51a306a53f604b4bc98e3f43d9434a01f09147138a9c19f7236d16`
**LOC do arquivo-fonte:** 632


### `app_bundle.scripts.pipeline.bibliography_manager:render_bib_entry`

**Assinatura:**
```python
def render_bib_entry(key: str, meta: BibMetadata) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/bibliography_manager.py`
**Linhas:** 428–443
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Renderiza a responsabilidade representada por `bib_entry` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `key` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `key` recebido pela função; não há descrição individual explícita no código. |
| `meta` | positional-or-keyword | `BibMetadata` | `obrigatório/ausente` | Parâmetro `meta` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `f'@{entry_type}{{{key},\n  {body}\n}}'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `' and '.join`
- `',\n  '.join`
- `bibtex_escape`
- `fields.append`
- `getattr`
- `meta.entry_type.lower`
- `meta.entry_type.lower().strip`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.bibliography_manager:build_bibliography`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 1
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `ccf7efa67c51a306a53f604b4bc98e3f43d9434a01f09147138a9c19f7236d16`
**LOC do arquivo-fonte:** 632


### `app_bundle.scripts.pipeline.bibliography_manager:split_bib_entries`

**Assinatura:**
```python
def split_bib_entries(text: str) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/bibliography_manager.py`
**Linhas:** 446–468
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `split_bib_entries` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `entries`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `entries.append`
- `len`
- `range`
- `text.find`
- `text[at:j + 1].strip`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.document_orchestration:run_document_stage_001`
- `app_bundle.scripts.pipeline.bibliography_manager:build_bibliography`
- `app_bundle.scripts.pipeline.institution_compliance:_check_bib`
- `app_bundle.scripts.pipeline.prisma_builder:parse_bib_metadata`
- `app_bundle.scripts.pipeline.project_tools:inspect_bib`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `ccf7efa67c51a306a53f604b4bc98e3f43d9434a01f09147138a9c19f7236d16`
**LOC do arquivo-fonte:** 632


### `app_bundle.scripts.pipeline.bibliography_manager:bib_entry_key`

**Assinatura:**
```python
def bib_entry_key(entry: str) -> str | None
```

**Arquivo:** `app_bundle/scripts/pipeline/bibliography_manager.py`
**Linhas:** 471–473
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `bib_entry_key` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `entry` | positional-or-keyword | `str` | `obrigatório/ausente` | Entrada bibliográfica ou registro individual. |

#### Retorno

Anotação declarada: `str | None`. Retornos observados estaticamente incluem: `m.group(1).strip() if m else None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `m.group`
- `m.group(1).strip`
- `re.match`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.document_orchestration:run_document_stage_001`
- `app_bundle.scripts.pipeline.bibliography_manager:_match_doc_to_bib_key`
- `app_bundle.scripts.pipeline.bibliography_manager:build_bibliography`
- `app_bundle.scripts.pipeline.bibliography_manager:deduplicate_entries`
- `app_bundle.scripts.pipeline.bibliography_manager:entry_identity`
- `app_bundle.scripts.pipeline.institution_compliance:_check_bib`
- `app_bundle.scripts.pipeline.prisma_builder:parse_bib_metadata`
- `app_bundle.scripts.pipeline.project_tools:inspect_bib`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `ccf7efa67c51a306a53f604b4bc98e3f43d9434a01f09147138a9c19f7236d16`
**LOC do arquivo-fonte:** 632


### `app_bundle.scripts.pipeline.bibliography_manager:extract_field`

**Assinatura:**
```python
def extract_field(entry: str, name: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/bibliography_manager.py`
**Linhas:** 476–494
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Extrai a responsabilidade representada por `field` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `entry` | positional-or-keyword | `str` | `obrigatório/ausente` | Entrada bibliográfica ou registro individual. |
| `name` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `name` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `''`; `re.sub('\\s+', ' ', entry[start:j]).strip()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `entry[i].isspace`
- `len`
- `m.end`
- `range`
- `re.escape`
- `re.search`
- `re.sub`
- `re.sub('\\s+', ' ', entry[start:j]).strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.bibliography_manager:_match_doc_to_bib_key`
- `app_bundle.scripts.pipeline.bibliography_manager:_parse_bib_meta`
- `app_bundle.scripts.pipeline.bibliography_manager:entry_identity`
- `app_bundle.scripts.pipeline.bibliography_manager:entry_quality`
- `app_bundle.scripts.pipeline.project_tools:inspect_bib`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `ccf7efa67c51a306a53f604b4bc98e3f43d9434a01f09147138a9c19f7236d16`
**LOC do arquivo-fonte:** 632


### `app_bundle.scripts.pipeline.bibliography_manager:entry_identity`

**Assinatura:**
```python
def entry_identity(entry: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/bibliography_manager.py`
**Linhas:** 497–506
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `entry_identity` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `entry` | positional-or-keyword | `str` | `obrigatório/ausente` | Entrada bibliográfica ou registro individual. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'doi:' + doi`; `'title:' + '|'.join([title, year, author.split()[0] if author.split() else ''])`; `'key:' + str(bib_entry_key(entry))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'|'.join`
- `author.split`
- `bib_entry_key`
- `extract_field`
- `normalize_doi`
- `normalize_title_loose`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.bibliography_manager:deduplicate_entries`
- `app_bundle.scripts.pipeline.project_tools:inspect_bib`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `ccf7efa67c51a306a53f604b4bc98e3f43d9434a01f09147138a9c19f7236d16`
**LOC do arquivo-fonte:** 632


### `app_bundle.scripts.pipeline.bibliography_manager:entry_quality`

**Assinatura:**
```python
def entry_quality(entry: str) -> tuple[int, int]
```

**Arquivo:** `app_bundle/scripts/pipeline/bibliography_manager.py`
**Linhas:** 509–517
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `entry_quality` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `entry` | positional-or-keyword | `str` | `obrigatório/ausente` | Entrada bibliográfica ou registro individual. |

#### Retorno

Anotação declarada: `tuple[int, int]`. Retornos observados estaticamente incluem: `(score, len(entry))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `(extract_field(entry, 'note') + ' ' + extract_field(entry, 'author')).lower`
- `extract_field`
- `len`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `ccf7efa67c51a306a53f604b4bc98e3f43d9434a01f09147138a9c19f7236d16`
**LOC do arquivo-fonte:** 632


### `app_bundle.scripts.pipeline.bibliography_manager:deduplicate_entries`

**Assinatura:**
```python
def deduplicate_entries(entries: list[str]) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/bibliography_manager.py`
**Linhas:** 520–525
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Deduplica a responsabilidade representada por `entries` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `entries` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Coleção de entradas bibliográficas/registros. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `[max(group, key=entry_quality) for group in groups.values()]`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `bib_entry_key`
- `entry_identity`
- `groups.setdefault`
- `groups.setdefault(entry_identity(e), []).append`
- `groups.values`
- `max`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.bibliography_manager:build_bibliography`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `ccf7efa67c51a306a53f604b4bc98e3f43d9434a01f09147138a9c19f7236d16`
**LOC do arquivo-fonte:** 632


### `app_bundle.scripts.pipeline.bibliography_manager:_parse_bib_meta`

**Assinatura:**
```python
def _parse_bib_meta(entry: str) -> dict[str, str]
```

**Arquivo:** `app_bundle/scripts/pipeline/bibliography_manager.py`
**Linhas:** 528–529
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Interpreta a responsabilidade representada por `bib_meta` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `entry` | positional-or-keyword | `str` | `obrigatório/ausente` | Entrada bibliográfica ou registro individual. |

#### Retorno

Anotação declarada: `dict[str, str]`. Retornos observados estaticamente incluem: `{name: extract_field(entry, name) for name in ['title', 'doi', 'year', 'author', 'editor']}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `extract_field`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `ccf7efa67c51a306a53f604b4bc98e3f43d9434a01f09147138a9c19f7236d16`
**LOC do arquivo-fonte:** 632


### `app_bundle.scripts.pipeline.bibliography_manager:_match_doc_to_bib_key`

**Assinatura:**
```python
def _match_doc_to_bib_key(doc: SourceDoc, entries: list[str], manifest: dict[str, str]) -> str | None
```

**Arquivo:** `app_bundle/scripts/pipeline/bibliography_manager.py`
**Linhas:** 532–558
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Associa a responsabilidade representada por `doc_to_bib_key` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doc` | positional-or-keyword | `SourceDoc` | `obrigatório/ausente` | Documento/registro individual processado pela rotina. |
| `entries` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Coleção de entradas bibliográficas/registros. |
| `manifest` | positional-or-keyword | `dict[str, str]` | `obrigatório/ausente` | Parâmetro `manifest` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str | None`. Retornos observados estaticamente incluem: `bib_entry_key(e)`; `key`; `best_key if best_score >= 0.45 else None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `' '.join(file_norms).split`
- `Path`
- `any`
- `bib_entry_key`
- `doi_for_doc`
- `extract_field`
- `len`
- `max`
- `min`
- `normalize_doi`
- `normalize_title_loose`
- `set`
- `title_norm.split`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.bibliography_manager:build_bibliography`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 7
loops: 3
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `ccf7efa67c51a306a53f604b4bc98e3f43d9434a01f09147138a9c19f7236d16`
**LOC do arquivo-fonte:** 632


### `app_bundle.scripts.pipeline.bibliography_manager:build_bibliography`

**Assinatura:**
```python
def build_bibliography(cfg: dict[str, Any], docs: list[SourceDoc], output_dir: Path, prefix: str, client: Any | None, model: str) -> BibBuildResult
```

**Arquivo:** `app_bundle/scripts/pipeline/bibliography_manager.py`
**Linhas:** 561–632
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Constrói a responsabilidade representada por `bibliography` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `docs` | positional-or-keyword | `list[SourceDoc]` | `obrigatório/ausente` | Coleção de documentos/entradas processados pela rotina. |
| `output_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Diretório onde a rotina grava ou procura os artefatos de saída. |
| `prefix` | positional-or-keyword | `str` | `obrigatório/ausente` | Prefixo canônico usado para compor nomes de artefatos. |
| `client` | positional-or-keyword | `Any \| None` | `obrigatório/ausente` | Cliente de serviço externo/IA utilizado pela rotina; pode ser opcional conforme o fluxo. |
| `model` | positional-or-keyword | `str` | `obrigatório/ausente` | Identificador do modelo utilizado pela etapa de IA. |

#### Retorno

Anotação declarada: `BibBuildResult`. Retornos observados estaticamente incluem: `BibBuildResult(bib_path=bib_path, keys=keys, entries=entries, key_by_doc_path=key_by_doc_path)`; `BibBuildResult(bib_path=bib_path, keys=keys, diagnostics_path=diag_path, key_by_doc_path=key_by_doc_path, entries=entries)`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `'\n\n'.join`
- `'\n\n'.join(entries).strip`
- `BibBuildResult`
- `BibMetadata`
- `Path`
- `Path(doc.path).resolve`
- `Path(doc.path).stem.replace`
- `Path(doc.path).stem.replace('_', ' ').replace`
- `Path(doc.path).stem.replace('_', ' ').replace('-', ' ').title`
- `Path(str(cfg.get('__config_dir__') or Path.cwd())).resolve`
- `Path.cwd`
- `_match_doc_to_bib_key`
- `bib_entry_key`
- `bool`
- `cfg.get`
- `deduplicate_entries`
- `diagnostics.append`
- `doi_for_doc`
- `entries.append`
- `existing_bib.read_text`
- `infer_bib_metadata_ai`
- `isinstance`
- `load_doi_manifest`
- `local.get`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.bib`
- `_bibliografia_diagnostico.json`

#### Complexidade estrutural observada

```yaml
branches: 12
loops: 4
comprehensions: 2
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `ccf7efa67c51a306a53f604b4bc98e3f43d9434a01f09147138a9c19f7236d16`
**LOC do arquivo-fonte:** 632


### `app_bundle.scripts.pipeline.citation_renderer:render_latex_citation`

**Assinatura:**
```python
def render_latex_citation(citation: Citation) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/citation_renderer.py`
**Linhas:** 14–32
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Renderiza a responsabilidade representada por `latex_citation` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `citation` | positional-or-keyword | `Citation` | `obrigatório/ausente` | Parâmetro `citation` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `''`; `f'{citation.prefix}{body}{citation.suffix}'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `', '.join`
- `','.join`
- `k.strip`
- `k.strip().lstrip`
- `len`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.citation_renderer:render_latex_inlines`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 0
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e543ed12787cbaf014b18d112c287d0848da20d17815caa24679959efeea4229`
**LOC do arquivo-fonte:** 68


### `app_bundle.scripts.pipeline.citation_renderer:render_latex_inlines`

**Assinatura:**
```python
def render_latex_inlines(content: list[Inline]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/citation_renderer.py`
**Linhas:** 35–47
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Renderiza a responsabilidade representada por `latex_inlines` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `content` | positional-or-keyword | `list[Inline]` | `obrigatório/ausente` | Parâmetro `content` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `''.join(parts)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `''.join`
- `getattr`
- `isinstance`
- `parts.append`
- `render_latex_citation`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:render_block_org`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e543ed12787cbaf014b18d112c287d0848da20d17815caa24679959efeea4229`
**LOC do arquivo-fonte:** 68


### `app_bundle.scripts.pipeline.citation_renderer:extract_cited_keys_from_model_inline`

**Assinatura:**
```python
def extract_cited_keys_from_model_inline(content: list[Inline]) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/citation_renderer.py`
**Linhas:** 50–58
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Extrai a responsabilidade representada por `cited_keys_from_model_inline` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `content` | positional-or-keyword | `list[Inline]` | `obrigatório/ausente` | Parâmetro `content` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `keys`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `getattr`
- `isinstance`
- `keys.append`
- `str`
- `str(k).strip`
- `str(k).strip().lstrip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_validator:cited_keys_from_document_model`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e543ed12787cbaf014b18d112c287d0848da20d17815caa24679959efeea4229`
**LOC do arquivo-fonte:** 68


### `app_bundle.scripts.pipeline.citation_renderer:extract_latex_cited_keys`

**Assinatura:**
```python
def extract_latex_cited_keys(text: str) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/citation_renderer.py`
**Linhas:** 61–68
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Extrai a responsabilidade representada por `latex_cited_keys` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `out`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `group.split`
- `key.strip`
- `key.strip().lstrip`
- `out.append`
- `re.findall`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_validator:validate_org_text`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e543ed12787cbaf014b18d112c287d0848da20d17815caa24679959efeea4229`
**LOC do arquivo-fonte:** 68


### `app_bundle.scripts.pipeline.clean_bundle:find_app_bundle`

**Assinatura:**
```python
def find_app_bundle(start: Path | None = None) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/clean_bundle.py`
**Linhas:** 29–48
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Localiza app_bundle a partir de start/cwd/script.

**Docstring resumida:** Localiza app_bundle a partir de start/cwd/script.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `start` | positional-or-keyword | `Path \| None` | `None` | Parâmetro `start` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `c`; `(c / 'app_bundle').resolve()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `(c / 'app_bundle' / 'scripts' / 'pipeline').exists`
- `(c / 'app_bundle').resolve`
- `(c / 'scripts' / 'pipeline').exists`
- `Path`
- `Path(__file__).resolve`
- `Path.cwd`
- `Path.cwd().resolve`
- `RuntimeError`
- `candidates.extend`
- `seen.add`
- `set`
- `start.expanduser`
- `start.expanduser().resolve`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('Não foi possível localizar app_bundle.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b30cc9df1eb311edf90a16279c9f33fed3beddfeffed79199212d688db87d301`
**LOC do arquivo-fonte:** 280


### `app_bundle.scripts.pipeline.clean_bundle:_rel`

**Assinatura:**
```python
def _rel(app: Path, p: Path) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/clean_bundle.py`
**Linhas:** 51–55
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_rel` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `app` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `app` recebido pela função; não há descrição individual explícita no código. |
| `p` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `p` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `str(p.relative_to(app)).replace('\\', '/')`; `str(p)`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `p.relative_to`
- `str`
- `str(p.relative_to(app)).replace`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.clean_bundle:_add_create`
- `app_bundle.scripts.pipeline.clean_bundle:_add_delete`
- `app_bundle.scripts.pipeline.clean_bundle:_add_move`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `b30cc9df1eb311edf90a16279c9f33fed3beddfeffed79199212d688db87d301`
**LOC do arquivo-fonte:** 280


### `app_bundle.scripts.pipeline.clean_bundle:_add_create`

**Assinatura:**
```python
def _add_create(actions: list[CleanAction], app: Path, path: Path, note: str = '') -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/clean_bundle.py`
**Linhas:** 58–59
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_add_create` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `actions` | positional-or-keyword | `list[CleanAction]` | `obrigatório/ausente` | Parâmetro `actions` recebido pela função; não há descrição individual explícita no código. |
| `app` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `app` recebido pela função; não há descrição individual explícita no código. |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `note` | positional-or-keyword | `str` | `''` | Parâmetro `note` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `CleanAction`
- `_rel`
- `actions.append`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.clean_bundle:build_clean_plan`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b30cc9df1eb311edf90a16279c9f33fed3beddfeffed79199212d688db87d301`
**LOC do arquivo-fonte:** 280


### `app_bundle.scripts.pipeline.clean_bundle:_add_delete`

**Assinatura:**
```python
def _add_delete(actions: list[CleanAction], app: Path, path: Path, note: str = '') -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/clean_bundle.py`
**Linhas:** 62–63
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_add_delete` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `actions` | positional-or-keyword | `list[CleanAction]` | `obrigatório/ausente` | Parâmetro `actions` recebido pela função; não há descrição individual explícita no código. |
| `app` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `app` recebido pela função; não há descrição individual explícita no código. |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `note` | positional-or-keyword | `str` | `''` | Parâmetro `note` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `CleanAction`
- `_rel`
- `actions.append`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.clean_bundle:build_clean_plan`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b30cc9df1eb311edf90a16279c9f33fed3beddfeffed79199212d688db87d301`
**LOC do arquivo-fonte:** 280


### `app_bundle.scripts.pipeline.clean_bundle:_add_move`

**Assinatura:**
```python
def _add_move(actions: list[CleanAction], app: Path, src: Path, dst: Path, note: str = '') -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/clean_bundle.py`
**Linhas:** 66–67
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_add_move` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `actions` | positional-or-keyword | `list[CleanAction]` | `obrigatório/ausente` | Parâmetro `actions` recebido pela função; não há descrição individual explícita no código. |
| `app` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `app` recebido pela função; não há descrição individual explícita no código. |
| `src` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `src` recebido pela função; não há descrição individual explícita no código. |
| `dst` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `dst` recebido pela função; não há descrição individual explícita no código. |
| `note` | positional-or-keyword | `str` | `''` | Parâmetro `note` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `CleanAction`
- `_rel`
- `actions.append`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.clean_bundle:build_clean_plan`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b30cc9df1eb311edf90a16279c9f33fed3beddfeffed79199212d688db87d301`
**LOC do arquivo-fonte:** 280


### `app_bundle.scripts.pipeline.clean_bundle:build_clean_plan`

**Assinatura:**
```python
def build_clean_plan(app_bundle: Path, *, remove_outputs: bool = False, remove_projects: bool = False, remove_backups: bool = True, remove_legacy_examples: bool = True) -> list[CleanAction]
```

**Arquivo:** `app_bundle/scripts/pipeline/clean_bundle.py`
**Linhas:** 70–148
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Gera plano de limpeza/normalização.

**Docstring resumida:** Gera plano de limpeza/normalização.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `app_bundle` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `app_bundle` recebido pela função; não há descrição individual explícita no código. |
| `remove_outputs` | keyword-only | `bool` | `False` | Parâmetro `remove_outputs` recebido pela função; não há descrição individual explícita no código. |
| `remove_projects` | keyword-only | `bool` | `False` | Parâmetro `remove_projects` recebido pela função; não há descrição individual explícita no código. |
| `remove_backups` | keyword-only | `bool` | `True` | Parâmetro `remove_backups` recebido pela função; não há descrição individual explícita no código. |
| `remove_legacy_examples` | keyword-only | `bool` | `True` | Parâmetro `remove_legacy_examples` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[CleanAction]`. Retornos observados estaticamente incluem: `actions`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `(app / 'config').exists`
- `(app / 'output').exists`
- `(app / 'projetos').exists`
- `CleanAction`
- `_add_create`
- `_add_delete`
- `_add_move`
- `actions.append`
- `app.rglob`
- `app_bundle.resolve`
- `b.exists`
- `csv.exists`
- `legacy_examples.exists`
- `legacy_examples.glob`
- `p.exists`
- `root.glob`
- `sorted`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.clean_bundle:clean_institutional_tree`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `doi_manifest_template.csv`
- `doi_manifest_template_com_exemplos.csv`
- `*.toml`
- `template_atividade.org`
- `template_paper.org`
- `template_dissertacao.org`
- `reference_fgv.docx`
- `fgv.png`

#### Complexidade estrutural observada

```yaml
branches: 11
loops: 6
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b30cc9df1eb311edf90a16279c9f33fed3beddfeffed79199212d688db87d301`
**LOC do arquivo-fonte:** 280


### `app_bundle.scripts.pipeline.clean_bundle:_rm_empty_dir`

**Assinatura:**
```python
def _rm_empty_dir(path: Path) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/clean_bundle.py`
**Linhas:** 151–156
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_rm_empty_dir` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `True`; `False`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `path.rmdir`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.clean_bundle:apply_clean_plan`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `b30cc9df1eb311edf90a16279c9f33fed3beddfeffed79199212d688db87d301`
**LOC do arquivo-fonte:** 280


### `app_bundle.scripts.pipeline.clean_bundle:apply_clean_plan`

**Assinatura:**
```python
def apply_clean_plan(app_bundle: Path, actions: list[CleanAction], *, dry_run: bool = True) -> list[CleanAction]
```

**Arquivo:** `app_bundle/scripts/pipeline/clean_bundle.py`
**Linhas:** 159–221
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Aplica a responsabilidade representada por `clean_plan` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `app_bundle` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `app_bundle` recebido pela função; não há descrição individual explícita no código. |
| `actions` | positional-or-keyword | `list[CleanAction]` | `obrigatório/ausente` | Parâmetro `actions` recebido pela função; não há descrição individual explícita no código. |
| `dry_run` | keyword-only | `bool` | `True` | Parâmetro `dry_run` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[CleanAction]`. Retornos observados estaticamente incluem: `executed`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `CleanAction`
- `Path`
- `Path(act.path).is_absolute`
- `_rm_empty_dir`
- `app_bundle.resolve`
- `asdict`
- `dst.exists`
- `dst.parent.mkdir`
- `executed.append`
- `p.exists`
- `p.is_dir`
- `p.mkdir`
- `p.unlink`
- `resolve_action_path`
- `shutil.move`
- `shutil.rmtree`
- `src.exists`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.clean_bundle:clean_institutional_tree`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 16
loops: 1
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `b30cc9df1eb311edf90a16279c9f33fed3beddfeffed79199212d688db87d301`
**LOC do arquivo-fonte:** 280


### `app_bundle.scripts.pipeline.clean_bundle:apply_clean_plan.resolve_action_path`

**Assinatura:**
```python
def resolve_action_path(raw: str) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/clean_bundle.py`
**Linhas:** 164–168
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Resolve a responsabilidade representada por `action_path` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `raw` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `raw` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `p`; `app / raw`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `p.is_absolute`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.clean_bundle:apply_clean_plan`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b30cc9df1eb311edf90a16279c9f33fed3beddfeffed79199212d688db87d301`
**LOC do arquivo-fonte:** 280


### `app_bundle.scripts.pipeline.clean_bundle:render_clean_report`

**Assinatura:**
```python
def render_clean_report(actions: list[CleanAction], *, dry_run: bool) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/clean_bundle.py`
**Linhas:** 224–231
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Renderiza a responsabilidade representada por `clean_report` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `actions` | positional-or-keyword | `list[CleanAction]` | `obrigatório/ausente` | Parâmetro `actions` recebido pela função; não há descrição individual explícita no código. |
| `dry_run` | keyword-only | `bool` | `obrigatório/ausente` | Parâmetro `dry_run` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'\n'.join(lines)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `lines.append`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b30cc9df1eb311edf90a16279c9f33fed3beddfeffed79199212d688db87d301`
**LOC do arquivo-fonte:** 280


### `app_bundle.scripts.pipeline.clean_bundle:clean_institutional_tree`

**Assinatura:**
```python
def clean_institutional_tree(*, base_dir: Path | None = None, apply: bool = False, remove_outputs: bool = False, remove_projects: bool = False, remove_backups: bool = True, remove_legacy_examples: bool = True, write_report: bool = True) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/clean_bundle.py`
**Linhas:** 234–266
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Limpa a responsabilidade representada por `institutional_tree` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `base_dir` | keyword-only | `Path \| None` | `None` | Parâmetro `base_dir` recebido pela função; não há descrição individual explícita no código. |
| `apply` | keyword-only | `bool` | `False` | Parâmetro `apply` recebido pela função; não há descrição individual explícita no código. |
| `remove_outputs` | keyword-only | `bool` | `False` | Parâmetro `remove_outputs` recebido pela função; não há descrição individual explícita no código. |
| `remove_projects` | keyword-only | `bool` | `False` | Parâmetro `remove_projects` recebido pela função; não há descrição individual explícita no código. |
| `remove_backups` | keyword-only | `bool` | `True` | Parâmetro `remove_backups` recebido pela função; não há descrição individual explícita no código. |
| `remove_legacy_examples` | keyword-only | `bool` | `True` | Parâmetro `remove_legacy_examples` recebido pela função; não há descrição individual explícita no código. |
| `write_report` | keyword-only | `bool` | `True` | Parâmetro `write_report` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `report`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `apply_clean_plan`
- `asdict`
- `bool`
- `build_clean_plan`
- `find_app_bundle`
- `json.dumps`
- `report_path.write_text`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `clean_institutional_tree_report.json`

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b30cc9df1eb311edf90a16279c9f33fed3beddfeffed79199212d688db87d301`
**LOC do arquivo-fonte:** 280


### `app_bundle.scripts.pipeline.corpus_manager:safe_extract_zip`

**Assinatura:**
```python
def safe_extract_zip(zip_path: Path, dest_dir: Path) -> list[Path]
```

**Arquivo:** `app_bundle/scripts/pipeline/corpus_manager.py`
**Linhas:** 47–62
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `safe_extract_zip` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `zip_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `dest_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `dest_dir` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[Path]`. Retornos observados estaticamente incluem: `out`.

#### Efeitos colaterais e fronteiras externas

- Abre arquivo para leitura ou inspeção.
- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `(dest_dir / info.filename).resolve`
- `RuntimeError`
- `dest_dir.mkdir`
- `dest_dir.resolve`
- `info.filename.endswith`
- `out.append`
- `shutil.copyfileobj`
- `str`
- `str(target).startswith`
- `target.open`
- `target.parent.mkdir`
- `zf.infolist`
- `zf.open`
- `zipfile.ZipFile`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.corpus_manager:collect_orientation_docs`
- `app_bundle.scripts.pipeline.corpus_manager:discover_local_documents`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError(f'Entrada insegura no ZIP: {info.filename}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b240573df7e930283e26d91a8808eff81280b3da854faf75bc5ce33d12679712`
**LOC do arquivo-fonte:** 279


### `app_bundle.scripts.pipeline.corpus_manager:_CaptureLogHandler.__init__`

**Assinatura:**
```python
def __init__(self) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/corpus_manager.py`
**Linhas:** 66–68
**Tipo:** method
**Visibilidade:** especial/dunder
**Classe proprietária:** `_CaptureLogHandler`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `__init__` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `super`
- `super().__init__`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b240573df7e930283e26d91a8808eff81280b3da854faf75bc5ce33d12679712`
**LOC do arquivo-fonte:** 279


### `app_bundle.scripts.pipeline.corpus_manager:_CaptureLogHandler.emit`

**Assinatura:**
```python
def emit(self, record: logging.LogRecord) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/corpus_manager.py`
**Linhas:** 70–76
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `_CaptureLogHandler`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `emit` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `record` | positional-or-keyword | `logging.LogRecord` | `obrigatório/ausente` | Parâmetro `record` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `record.getMessage`
- `self.format`
- `self.messages.append`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `b240573df7e930283e26d91a8808eff81280b3da854faf75bc5ce33d12679712`
**LOC do arquivo-fonte:** 279


### `app_bundle.scripts.pipeline.corpus_manager:_dedupe_messages`

**Assinatura:**
```python
def _dedupe_messages(messages: list[str], limit: int = 60) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/corpus_manager.py`
**Linhas:** 79–91
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_dedupe_messages` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `messages` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `messages` recebido pela função; não há descrição individual explícita no código. |
| `limit` | positional-or-keyword | `int` | `60` | Parâmetro `limit` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `out`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `len`
- `out.append`
- `seen.add`
- `set`
- `str`
- `str(msg or '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.corpus_manager:read_text_file_with_diagnostics`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b240573df7e930283e26d91a8808eff81280b3da854faf75bc5ce33d12679712`
**LOC do arquivo-fonte:** 279


### `app_bundle.scripts.pipeline.corpus_manager:capture_pdf_parser_warnings`

**Assinatura:**
```python
def capture_pdf_parser_warnings() -> Any
```

**Arquivo:** `app_bundle/scripts/pipeline/corpus_manager.py`
**Linhas:** 95–120
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** `contextlib.contextmanager`
**Base da explicação:** docstring + AST

**Finalidade:** Captura warnings/logs ruidosos do pypdf por arquivo.

**Docstring resumida:** Captura warnings/logs ruidosos do pypdf por arquivo.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `Any`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `_CaptureLogHandler`
- `contextlib.redirect_stderr`
- `handler.setFormatter`
- `io.StringIO`
- `logger.addHandler`
- `logger.removeHandler`
- `logger.setLevel`
- `logging.Formatter`
- `logging.getLogger`
- `warnings.catch_warnings`
- `warnings.simplefilter`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.corpus_manager:read_text_file_with_diagnostics`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b240573df7e930283e26d91a8808eff81280b3da854faf75bc5ce33d12679712`
**LOC do arquivo-fonte:** 279


### `app_bundle.scripts.pipeline.corpus_manager:read_text_file_with_diagnostics`

**Assinatura:**
```python
def read_text_file_with_diagnostics(path: Path, max_chars: int = 40000) -> tuple[str, list[str]]
```

**Arquivo:** `app_bundle/scripts/pipeline/corpus_manager.py`
**Linhas:** 123–151
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Lê a responsabilidade representada por `text_file_with_diagnostics` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `max_chars` | positional-or-keyword | `int` | `40000` | Parâmetro `max_chars` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `tuple[str, list[str]]`. Retornos observados estaticamente incluem: `(shorten_text(path.read_text(encoding='utf-8', errors='ignore'), max_chars), [])`; `(shorten_text('\n'.join(chunks), max_chars), _dedupe_messages(captured_messages))`; `(shorten_text('\n'.join((p.text for p in d.paragraphs if p.text)), max_chars), [])`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `PdfReader`
- `RuntimeError`
- `_dedupe_messages`
- `capture_pdf_parser_warnings`
- `captured_messages.extend`
- `chunks.append`
- `docx.Document`
- `len`
- `line.strip`
- `page.extract_text`
- `path.read_text`
- `path.suffix.lower`
- `shorten_text`
- `stderr_buffer.getvalue`
- `stderr_buffer.getvalue().splitlines`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.corpus_manager:collect_orientation_docs`
- `app_bundle.scripts.pipeline.corpus_manager:discover_local_documents`
- `app_bundle.scripts.pipeline.corpus_manager:read_text_file`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('pypdf não está disponível.')`
- `RuntimeError('python-docx não está disponível.')`
- `RuntimeError(f'Extensão não suportada: {path.suffix}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.pdf`
- `.docx`

#### Complexidade estrutural observada

```yaml
branches: 7
loops: 1
comprehensions: 3
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b240573df7e930283e26d91a8808eff81280b3da854faf75bc5ce33d12679712`
**LOC do arquivo-fonte:** 279


### `app_bundle.scripts.pipeline.corpus_manager:read_text_file`

**Assinatura:**
```python
def read_text_file(path: Path, max_chars: int = 40000) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/corpus_manager.py`
**Linhas:** 154–156
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Lê a responsabilidade representada por `text_file` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `max_chars` | positional-or-keyword | `int` | `40000` | Parâmetro `max_chars` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `text`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `read_text_file_with_diagnostics`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b240573df7e930283e26d91a8808eff81280b3da854faf75bc5ce33d12679712`
**LOC do arquivo-fonte:** 279


### `app_bundle.scripts.pipeline.corpus_manager:_print_pdf_warning_summary`

**Assinatura:**
```python
def _print_pdf_warning_summary(label: str, messages: list[str]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/corpus_manager.py`
**Linhas:** 159–166
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Exibe a responsabilidade representada por `pdf_warning_summary` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `label` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `label` recebido pela função; não há descrição individual explícita no código. |
| `messages` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `messages` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `len`
- `print`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.corpus_manager:collect_orientation_docs`
- `app_bundle.scripts.pipeline.corpus_manager:discover_local_documents`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b240573df7e930283e26d91a8808eff81280b3da854faf75bc5ce33d12679712`
**LOC do arquivo-fonte:** 279


### `app_bundle.scripts.pipeline.corpus_manager:discover_local_documents`

**Assinatura:**
```python
def discover_local_documents(cfg: dict[str, Any], work_dir: Path) -> tuple[list[SourceDoc], dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/corpus_manager.py`
**Linhas:** 169–216
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Descobre a responsabilidade representada por `local_documents` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `work_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Diretório de trabalho temporário/intermediário. |

#### Retorno

Anotação declarada: `tuple[list[SourceDoc], dict[str, Any]]`. Retornos observados estaticamente incluem: `(docs, source_info)`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(str(cfg.get('__config_dir__') or Path.cwd())).resolve`
- `Path.cwd`
- `RuntimeError`
- `SourceDoc`
- `_print_pdf_warning_summary`
- `bool`
- `cfg.get`
- `docs.append`
- `input_dir.exists`
- `input_dir.glob`
- `input_dir.rglob`
- `input_zip.exists`
- `int`
- `isinstance`
- `len`
- `local.get`
- `p.is_file`
- `p.resolve`
- `p.suffix.lower`
- `read_text_file_with_diagnostics`
- `resolve_path`
- `safe_extract_zip`
- `shutil.rmtree`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.default_runtime:_ap003f_pipeline_core`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('[documentos_locais] precisa de input_zip ou input_dir válido.')`
- `RuntimeError('Nenhum documento local legível encontrado.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.pdf`
- `.docx`
- `.txt`
- `.md`
- `.org`

#### Complexidade estrutural observada

```yaml
branches: 7
loops: 1
comprehensions: 4
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `b240573df7e930283e26d91a8808eff81280b3da854faf75bc5ce33d12679712`
**LOC do arquivo-fonte:** 279


### `app_bundle.scripts.pipeline.corpus_manager:collect_orientation_docs`

**Assinatura:**
```python
def collect_orientation_docs(cfg: dict[str, Any], work_dir: Path) -> list[SourceDoc]
```

**Arquivo:** `app_bundle/scripts/pipeline/corpus_manager.py`
**Linhas:** 219–254
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Coleta a responsabilidade representada por `orientation_docs` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `work_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Diretório de trabalho temporário/intermediário. |

#### Retorno

Anotação declarada: `list[SourceDoc]`. Retornos observados estaticamente incluem: `docs`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(str(cfg.get('__config_dir__') or Path.cwd())).resolve`
- `Path.cwd`
- `SourceDoc`
- `_print_pdf_warning_summary`
- `cfg.get`
- `docs.append`
- `isinstance`
- `len`
- `orient.get`
- `p.exists`
- `p.is_dir`
- `p.is_file`
- `p.rglob`
- `p.suffix.lower`
- `paths.extend`
- `read_text_file_with_diagnostics`
- `resolve_path`
- `safe_extract_zip`
- `slugify`
- `sorted`
- `str`
- `str(orient.get('inline') or '').strip`
- `x.is_file`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.default_runtime:_ap003f_pipeline_core`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 8
loops: 2
comprehensions: 2
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `b240573df7e930283e26d91a8808eff81280b3da854faf75bc5ce33d12679712`
**LOC do arquivo-fonte:** 279


### `app_bundle.scripts.pipeline.corpus_manager:copy_documents_to_fulltext_cache`

**Assinatura:**
```python
def copy_documents_to_fulltext_cache(docs: list[SourceDoc], cache: Path, clean: bool = True) -> list[Path]
```

**Arquivo:** `app_bundle/scripts/pipeline/corpus_manager.py`
**Linhas:** 257–279
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Copia documentos para o diretório exato de cache resolvido em [paths].cache_dir.

**Docstring resumida:** Copia documentos para o diretório exato de cache resolvido em [paths].cache_dir.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `docs` | positional-or-keyword | `list[SourceDoc]` | `obrigatório/ausente` | Coleção de documentos/entradas processados pela rotina. |
| `cache` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `cache` recebido pela função; não há descrição individual explícita no código. |
| `clean` | positional-or-keyword | `bool` | `True` | Parâmetro `clean` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[Path]`. Retornos observados estaticamente incluem: `copied`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `(cache / f'{stem}_{i}{suffix}').exists`
- `Path`
- `cache.exists`
- `cache.mkdir`
- `copied.append`
- `p.exists`
- `p.is_file`
- `seen.add`
- `set`
- `shutil.copy2`
- `shutil.rmtree`
- `str`
- `target.exists`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.default_runtime:_ap003f_pipeline_core`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b240573df7e930283e26d91a8808eff81280b3da854faf75bc5ce33d12679712`
**LOC do arquivo-fonte:** 279


### `app_bundle.scripts.pipeline.diagnostics:now_iso`

**Assinatura:**
```python
def now_iso() -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/diagnostics.py`
**Linhas:** 41–42
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `now_iso` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `datetime.now(timezone.utc).astimezone().isoformat(timespec='seconds')`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `datetime.now`
- `datetime.now(timezone.utc).astimezone`
- `datetime.now(timezone.utc).astimezone().isoformat`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.diagnostics:check_config`
- `app_bundle.scripts.pipeline.diagnostics:make_run_report`
- `app_bundle.scripts.pipeline.diagnostics:run_doctor`
- `app_bundle.scripts.pipeline.institution_compliance:run_institution_compliance`
- `app_bundle.scripts.pipeline.prompt_lock:build_prompt_lock`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `347f7137c96e87af21470088a621cd14fc9b66a5b7389bc273a39a69d48fc173`
**LOC do arquivo-fonte:** 526


### `app_bundle.scripts.pipeline.diagnostics:file_sha256`

**Assinatura:**
```python
def file_sha256(path: Path | None, *, max_bytes: int | None = None) -> str | None
```

**Arquivo:** `app_bundle/scripts/pipeline/diagnostics.py`
**Linhas:** 45–60
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `file_sha256` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path \| None` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `max_bytes` | keyword-only | `int \| None` | `None` | Parâmetro `max_bytes` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str | None`. Retornos observados estaticamente incluem: `None`; `h.hexdigest()`.

#### Efeitos colaterais e fronteiras externas

- Abre arquivo para leitura ou inspeção.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `f.read`
- `h.hexdigest`
- `h.update`
- `hashlib.sha256`
- `iter`
- `len`
- `max`
- `path.exists`
- `path.is_file`
- `path.open`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.diagnostics:make_run_report`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `347f7137c96e87af21470088a621cd14fc9b66a5b7389bc273a39a69d48fc173`
**LOC do arquivo-fonte:** 526


### `app_bundle.scripts.pipeline.diagnostics:command_exists`

**Assinatura:**
```python
def command_exists(name: str) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/diagnostics.py`
**Linhas:** 63–64
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `command_exists` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `name` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `name` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `shutil.which(name) is not None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `shutil.which`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.diagnostics:check_config`
- `app_bundle.scripts.pipeline.diagnostics:run_doctor`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `347f7137c96e87af21470088a621cd14fc9b66a5b7389bc273a39a69d48fc173`
**LOC do arquivo-fonte:** 526


### `app_bundle.scripts.pipeline.diagnostics:command_version`

**Assinatura:**
```python
def command_version(name: str, args: list[str] | None = None) -> str | None
```

**Arquivo:** `app_bundle/scripts/pipeline/diagnostics.py`
**Linhas:** 67–76
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `command_version` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `name` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `name` recebido pela função; não há descrição individual explícita no código. |
| `args` | positional-or-keyword | `list[str] \| None` | `None` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Anotação declarada: `str | None`. Retornos observados estaticamente incluem: `None`; `text[0].strip() if text else exe`; `exe`.

#### Efeitos colaterais e fronteiras externas

- Executa processo ou ferramenta externa.

#### Chamadas realizadas pelo corpo direto

- `(proc.stdout or proc.stderr or '').splitlines`
- `shutil.which`
- `subprocess.run`
- `text[0].strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.diagnostics:run_doctor`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `--version`

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `347f7137c96e87af21470088a621cd14fc9b66a5b7389bc273a39a69d48fc173`
**LOC do arquivo-fonte:** 526


### `app_bundle.scripts.pipeline.diagnostics:kpsewhich_exists`

**Assinatura:**
```python
def kpsewhich_exists(filename: str) -> bool | None
```

**Arquivo:** `app_bundle/scripts/pipeline/diagnostics.py`
**Linhas:** 79–87
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `kpsewhich_exists` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `filename` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `filename` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `bool | None`. Retornos observados estaticamente incluem: `None`; `proc.returncode == 0 and bool(proc.stdout.strip())`.

#### Efeitos colaterais e fronteiras externas

- Executa processo ou ferramenta externa.

#### Chamadas realizadas pelo corpo direto

- `bool`
- `proc.stdout.strip`
- `shutil.which`
- `subprocess.run`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.diagnostics:run_doctor`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `347f7137c96e87af21470088a621cd14fc9b66a5b7389bc273a39a69d48fc173`
**LOC do arquivo-fonte:** 526


### `app_bundle.scripts.pipeline.diagnostics:_cfg_section`

**Assinatura:**
```python
def _cfg_section(cfg: dict[str, Any], name: str) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/diagnostics.py`
**Linhas:** 90–92
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_cfg_section` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `name` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `name` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `sec if isinstance(sec, dict) else {}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `cfg.get`
- `isinstance`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `347f7137c96e87af21470088a621cd14fc9b66a5b7389bc273a39a69d48fc173`
**LOC do arquivo-fonte:** 526


### `app_bundle.scripts.pipeline.diagnostics:_config_dir`

**Assinatura:**
```python
def _config_dir(cfg: dict[str, Any]) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/diagnostics.py`
**Linhas:** 95–96
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_config_dir` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `Path(str(cfg.get('__config_dir__') or Path.cwd())).resolve()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(str(cfg.get('__config_dir__') or Path.cwd())).resolve`
- `Path.cwd`
- `cfg.get`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `347f7137c96e87af21470088a621cd14fc9b66a5b7389bc273a39a69d48fc173`
**LOC do arquivo-fonte:** 526


### `app_bundle.scripts.pipeline.diagnostics:_style_from_cfg`

**Assinatura:**
```python
def _style_from_cfg(cfg: dict[str, Any]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/diagnostics.py`
**Linhas:** 99–102
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_style_from_cfg` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `str(bib.get('latex_style') or bib.get('estilo_citacao') or doc.get('estilo_citacao') or 'apa').strip().lower()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_cfg_section`
- `bib.get`
- `doc.get`
- `str`
- `str(bib.get('latex_style') or bib.get('estilo_citacao') or doc.get('estilo_citacao') or 'apa').strip`
- `str(bib.get('latex_style') or bib.get('estilo_citacao') or doc.get('estilo_citacao') or 'apa').strip().lower`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `347f7137c96e87af21470088a621cd14fc9b66a5b7389bc273a39a69d48fc173`
**LOC do arquivo-fonte:** 526


### `app_bundle.scripts.pipeline.diagnostics:_doc_type`

**Assinatura:**
```python
def _doc_type(cfg: dict[str, Any]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/diagnostics.py`
**Linhas:** 105–107
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_doc_type` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `str(doc.get('tipo_documento') or 'paper').strip().lower()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_cfg_section`
- `doc.get`
- `str`
- `str(doc.get('tipo_documento') or 'paper').strip`
- `str(doc.get('tipo_documento') or 'paper').strip().lower`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `347f7137c96e87af21470088a621cd14fc9b66a5b7389bc273a39a69d48fc173`
**LOC do arquivo-fonte:** 526


### `app_bundle.scripts.pipeline.diagnostics:_out_dir_and_prefix_from_cfg`

**Assinatura:**
```python
def _out_dir_and_prefix_from_cfg(cfg: dict[str, Any]) -> tuple[Path, str]
```

**Arquivo:** `app_bundle/scripts/pipeline/diagnostics.py`
**Linhas:** 110–122
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Resolve a saída final do documento a partir da seção [paths].

**Docstring resumida:** Resolve a saída final do documento a partir da seção [paths].

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `tuple[Path, str]`. Retornos observados estaticamente incluem: `(out_dir, prefix)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_cfg_section`
- `_config_dir`
- `bool`
- `paths.get`
- `projeto.get`
- `resolve_path`
- `str`
- `str(paths.get('document_prefix') or projeto.get('nome') or 'documento').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.diagnostics:check_config`
- `app_bundle.scripts.pipeline.diagnostics:run_doctor`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `347f7137c96e87af21470088a621cd14fc9b66a5b7389bc273a39a69d48fc173`
**LOC do arquivo-fonte:** 526


### `app_bundle.scripts.pipeline.diagnostics:_work_cache_dirs_from_cfg`

**Assinatura:**
```python
def _work_cache_dirs_from_cfg(cfg: dict[str, Any], prefix: str) -> tuple[Path, Path]
```

**Arquivo:** `app_bundle/scripts/pipeline/diagnostics.py`
**Linhas:** 125–132
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_work_cache_dirs_from_cfg` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `prefix` | positional-or-keyword | `str` | `obrigatório/ausente` | Prefixo canônico usado para compor nomes de artefatos. |

#### Retorno

Anotação declarada: `tuple[Path, Path]`. Retornos observados estaticamente incluem: `(work_dir, cache_dir)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_cfg_section`
- `_config_dir`
- `bool`
- `paths.get`
- `resolve_path`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `347f7137c96e87af21470088a621cd14fc9b66a5b7389bc273a39a69d48fc173`
**LOC do arquivo-fonte:** 526


### `app_bundle.scripts.pipeline.diagnostics:run_doctor`

**Assinatura:**
```python
def run_doctor(cfg: dict[str, Any] | None = None) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/diagnostics.py`
**Linhas:** 139–232
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Diagnostica ambiente local. Nunca exige OpenAI se cfg não exigir geração.

**Docstring resumida:** Diagnostica ambiente local. Nunca exige OpenAI se cfg não exigir geração.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any] \| None` | `None` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `{'version': PIPELINE_VERSION, 'generated_at': now_iso(), 'ok': not errors, 'errors': errors, 'warnings': warnings, 'checks': checks}`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Lê configuração/credenciais do ambiente do processo.

#### Chamadas realizadas pelo corpo direto

- `(p / 'fgv-dissertacao.sty').exists`
- `(p / 'fgv-paper.sty').exists`
- `Path.cwd`
- `Path.cwd().resolve`
- `__import__`
- `_cfg_section`
- `_cfg_section(cfg, 'documento').get`
- `_config_dir`
- `_out_dir_and_prefix_from_cfg`
- `_style_from_cfg`
- `add`
- `aw.exists`
- `bool`
- `cfg.get`
- `command_exists`
- `command_version`
- `docx.get`
- `jar.exists`
- `kpsewhich_exists`
- `latex.get`
- `latex_extra.exists`
- `latex_extra.is_dir`
- `logo.exists`
- `mm.get`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.doctor_runtime:run_doctor_command`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 31
loops: 2
comprehensions: 2
exception_handlers: 2
```

**SHA-256 do arquivo-fonte:** `347f7137c96e87af21470088a621cd14fc9b66a5b7389bc273a39a69d48fc173`
**LOC do arquivo-fonte:** 526


### `app_bundle.scripts.pipeline.diagnostics:run_doctor.add`

**Assinatura:**
```python
def add(name: str, ok: bool, detail: str = '', severity: str = 'error', value: Any = None) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/diagnostics.py`
**Linhas:** 151–152
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `add` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `name` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `name` recebido pela função; não há descrição individual explícita no código. |
| `ok` | positional-or-keyword | `bool` | `obrigatório/ausente` | Parâmetro `ok` recebido pela função; não há descrição individual explícita no código. |
| `detail` | positional-or-keyword | `str` | `''` | Parâmetro `detail` recebido pela função; não há descrição individual explícita no código. |
| `severity` | positional-or-keyword | `str` | `'error'` | Parâmetro `severity` recebido pela função; não há descrição individual explícita no código. |
| `value` | positional-or-keyword | `Any` | `None` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `bool`
- `checks.append`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `347f7137c96e87af21470088a621cd14fc9b66a5b7389bc273a39a69d48fc173`
**LOC do arquivo-fonte:** 526


### `app_bundle.scripts.pipeline.diagnostics:print_doctor_report`

**Assinatura:**
```python
def print_doctor_report(report: dict[str, Any]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/diagnostics.py`
**Linhas:** 235–240
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Exibe a responsabilidade representada por `doctor_report` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `report` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `report` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `c.get`
- `print`
- `report.get`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.doctor_runtime:run_doctor_command`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `347f7137c96e87af21470088a621cd14fc9b66a5b7389bc273a39a69d48fc173`
**LOC do arquivo-fonte:** 526


### `app_bundle.scripts.pipeline.diagnostics:check_config`

**Assinatura:**
```python
def check_config(cfg: dict[str, Any]) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/diagnostics.py`
**Linhas:** 247–379
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Verifica a responsabilidade representada por `config` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `{'version': PIPELINE_VERSION, 'generated_at': now_iso(), 'ok': not errors, 'errors': errors, 'warnings': warnings}`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `_cfg_section`
- `_cfg_section(cfg, 'atividade').get`
- `_config_dir`
- `_doc_type`
- `_out_dir_and_prefix_from_cfg`
- `_style_from_cfg`
- `aw.exists`
- `bib.get`
- `bool`
- `cfg.get`
- `command_exists`
- `csl.exists`
- `doc.get`
- `docx.get`
- `doi_manifest.exists`
- `err`
- `input_dir.exists`
- `input_zip.exists`
- `inst.get`
- `latex.get`
- `latex_extra.exists`
- `local.get`
- `normalize_title_loose`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.default_runtime:_ap003f_pipeline_core`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `[latex].pdf_engine inválido: `

#### Complexidade estrutural observada

```yaml
branches: 31
loops: 5
comprehensions: 0
exception_handlers: 3
```

**SHA-256 do arquivo-fonte:** `347f7137c96e87af21470088a621cd14fc9b66a5b7389bc273a39a69d48fc173`
**LOC do arquivo-fonte:** 526


### `app_bundle.scripts.pipeline.diagnostics:check_config.err`

**Assinatura:**
```python
def err(msg: str) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/diagnostics.py`
**Linhas:** 252–253
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `err` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `msg` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `msg` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `errors.append`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.diagnostics:check_config`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `347f7137c96e87af21470088a621cd14fc9b66a5b7389bc273a39a69d48fc173`
**LOC do arquivo-fonte:** 526


### `app_bundle.scripts.pipeline.diagnostics:check_config.warn`

**Assinatura:**
```python
def warn(msg: str) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/diagnostics.py`
**Linhas:** 255–256
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `warn` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `msg` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `msg` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `warnings.append`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.diagnostics:check_config`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `347f7137c96e87af21470088a621cd14fc9b66a5b7389bc273a39a69d48fc173`
**LOC do arquivo-fonte:** 526


### `app_bundle.scripts.pipeline.diagnostics:print_check_config_report`

**Assinatura:**
```python
def print_check_config_report(report: dict[str, Any]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/diagnostics.py`
**Linhas:** 382–390
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Exibe a responsabilidade representada por `check_config_report` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `report` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `report` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `print`
- `report.get`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `347f7137c96e87af21470088a621cd14fc9b66a5b7389bc273a39a69d48fc173`
**LOC do arquivo-fonte:** 526


### `app_bundle.scripts.pipeline.diagnostics:validate_docx_file`

**Assinatura:**
```python
def validate_docx_file(path: Path | None, expected_title: str | None = None, *, require_references: bool = False) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/diagnostics.py`
**Linhas:** 397–442
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Validação leve do DOCX gerado.

**Docstring resumida:** Validação leve do DOCX gerado.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path \| None` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `expected_title` | positional-or-keyword | `str \| None` | `None` | Parâmetro `expected_title` recebido pela função; não há descrição individual explícita no código. |
| `require_references` | keyword-only | `bool` | `False` | Parâmetro `require_references` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `result`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `Document`
- `cell.text.strip`
- `len`
- `normalize_title_loose`
- `p.text.strip`
- `path.exists`
- `path.stat`
- `result['warnings'].append`
- `str`
- `table_texts.append`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 7
loops: 3
comprehensions: 1
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `347f7137c96e87af21470088a621cd14fc9b66a5b7389bc273a39a69d48fc173`
**LOC do arquivo-fonte:** 526


### `app_bundle.scripts.pipeline.diagnostics:make_run_report`

**Assinatura:**
```python
def make_run_report(*, cfg: dict[str, Any], config_path: Path | None, out_dir: Path, prefix: str, model: str | None, outputs: dict[str, Any], warnings: list[str] | None = None, extra: dict[str, Any] | None = None) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/diagnostics.py`
**Linhas:** 444–491
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Cria a responsabilidade representada por `run_report` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | keyword-only | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `config_path` | keyword-only | `Path \| None` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `out_dir` | keyword-only | `Path` | `obrigatório/ausente` | Parâmetro `out_dir` recebido pela função; não há descrição individual explícita no código. |
| `prefix` | keyword-only | `str` | `obrigatório/ausente` | Prefixo canônico usado para compor nomes de artefatos. |
| `model` | keyword-only | `str \| None` | `obrigatório/ausente` | Identificador do modelo utilizado pela etapa de IA. |
| `outputs` | keyword-only | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `outputs` recebido pela função; não há descrição individual explícita no código. |
| `warnings` | keyword-only | `list[str] \| None` | `None` | Parâmetro `warnings` recebido pela função; não há descrição individual explícita no código. |
| `extra` | keyword-only | `dict[str, Any] \| None` | `None` | Parâmetro `extra` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `{'version': PIPELINE_VERSION, 'generated_at': now_iso(), 'config_path': str(config_path) if config_path else cfg.get('__config_path__'), 'output_dir': str(out_dir), 'prefixo': prefix, 'tipo_documento': _doc_type(cfg), 'instituicao_perfil': cfg.get('__institution_profile_name__'), 'instituicao_profile_path': cfg.get('__institution_profile_path__'), 'modelo_openai': model, 'estilo_bibliografico': _style_from_cfg(cfg), 'pdf_engine': latex.get('pdf_engine') or 'lualatex', 'input_zip': str(input_zip) if input_zip else None, 'input_zip_sha256': file_sha256(input_zip) if input_zip else None, 'input_dir': str(input_dir) if input_dir else None, 'doi_manifest': str(doi_manifest) if doi_manifest else None, 'doi_manifest_sha256': file_sha256(doi_manifest) if doi_manifest else None, 'outputs': resolved_outputs, 'warnings': warnings or [], 'prompts': prompt_report_for_cfg(cfg), 'extra': extra or {}}`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `(outputs or {}).items`
- `_cfg_section`
- `_config_dir`
- `_doc_type`
- `_style_from_cfg`
- `cfg.get`
- `file_sha256`
- `isinstance`
- `latex.get`
- `local.get`
- `now_iso`
- `prompt_report_for_cfg`
- `resolve_path`
- `str`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.default_runtime:_ap003f_pipeline_core`
- `academic_pipeline.prisma_generic_orchestration:run_prisma_stage_004`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 11
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `347f7137c96e87af21470088a621cd14fc9b66a5b7389bc273a39a69d48fc173`
**LOC do arquivo-fonte:** 526


### `app_bundle.scripts.pipeline.diagnostics:write_outputs_manifest`

**Assinatura:**
```python
def write_outputs_manifest(path: Path, outputs: dict[str, Any]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/diagnostics.py`
**Linhas:** 494–503
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Grava a responsabilidade representada por `outputs_manifest` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `outputs` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `outputs` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `'\n'.join(lines).strip`
- `isinstance`
- `lines.append`
- `outputs.items`
- `value.items`
- `write_text`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.default_runtime:_ap003f_pipeline_core`
- `academic_pipeline.prisma_generic_orchestration:run_prisma_stage_004`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `347f7137c96e87af21470088a621cd14fc9b66a5b7389bc273a39a69d48fc173`
**LOC do arquivo-fonte:** 526


### `app_bundle.scripts.pipeline.diagnostics:print_outputs`

**Assinatura:**
```python
def print_outputs(outputs: dict[str, Any], *, title: str = 'Saídas') -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/diagnostics.py`
**Linhas:** 506–509
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Exibe a responsabilidade representada por `outputs` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `outputs` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `outputs` recebido pela função; não há descrição individual explícita no código. |
| `title` | keyword-only | `str` | `'Saídas'` | Parâmetro `title` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `outputs.items`
- `print`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.default_runtime:_ap003f_pipeline_core`
- `academic_pipeline.prisma_generic_orchestration:run_prisma_stage_004`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `347f7137c96e87af21470088a621cd14fc9b66a5b7389bc273a39a69d48fc173`
**LOC do arquivo-fonte:** 526


### `app_bundle.scripts.pipeline.diagnostics:clean_aux_files`

**Assinatura:**
```python
def clean_aux_files(org_path: Path) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/diagnostics.py`
**Linhas:** 516–526
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Limpa a responsabilidade representada por `aux_files` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `org_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `removed`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `file.unlink`
- `org_path.parent.glob`
- `removed.append`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `*.tex`

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 2
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `347f7137c96e87af21470088a621cd14fc9b66a5b7389bc273a39a69d48fc173`
**LOC do arquivo-fonte:** 526


### `app_bundle.scripts.pipeline.document_builder:_is_resumo_artigos_profile`

**Assinatura:**
```python
def _is_resumo_artigos_profile(cfg: dict[str, Any]) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/document_builder.py`
**Linhas:** 47–50
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Verifica se `resumo_artigos_profile` conforme as regras codificadas na rotina.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `bool(resumo.get('ativo')) or str(projeto.get('preset') or '').strip() == 'resumo_artigos_local_fgv'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `bool`
- `cfg.get`
- `isinstance`
- `projeto.get`
- `resumo.get`
- `str`
- `str(projeto.get('preset') or '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_builder:_resumo_artigos_depth_directives`
- `app_bundle.scripts.pipeline.document_builder:build_document_model`
- `app_bundle.scripts.pipeline.document_builder:build_prompt`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4342a2454736eb84a52335af282120bbd31d58630f6b0b487a3beb2ffcf83c0d`
**LOC do arquivo-fonte:** 715


### `app_bundle.scripts.pipeline.document_builder:_safe_int`

**Assinatura:**
```python
def _safe_int(value: Any, default: int, minimum: int = 0, maximum: int | None = None) -> int
```

**Arquivo:** `app_bundle/scripts/pipeline/document_builder.py`
**Linhas:** 53–61
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_safe_int` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |
| `default` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `default` recebido pela função; não há descrição individual explícita no código. |
| `minimum` | positional-or-keyword | `int` | `0` | Parâmetro `minimum` recebido pela função; não há descrição individual explícita no código. |
| `maximum` | positional-or-keyword | `int \| None` | `None` | Parâmetro `maximum` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `parsed`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `int`
- `max`
- `min`
- `str`
- `str(value).strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_builder:_resumo_artigos_depth_directives`
- `app_bundle.scripts.pipeline.document_builder:_resumo_limits`
- `app_bundle.scripts.pipeline.document_builder:build_prompt`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `4342a2454736eb84a52335af282120bbd31d58630f6b0b487a3beb2ffcf83c0d`
**LOC do arquivo-fonte:** 715


### `app_bundle.scripts.pipeline.document_builder:_resumo_artigos_depth_directives`

**Assinatura:**
```python
def _resumo_artigos_depth_directives(cfg: dict[str, Any], docs_count: int) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/document_builder.py`
**Linhas:** 64–119
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_resumo_artigos_depth_directives` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `docs_count` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `docs_count` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `''`; `f'\n\nDiretrizes específicas de profundidade para o perfil resumo_artigos_local_fgv:\n- Profundidade analítica solicitada: {nivel}. O documento deve ser substancial, interpretativo e comparativo; não deve parecer ementa, sinopse ou resumo escolar curto.\n- Para cada artigo/texto, produza pelo menos {min_paragraphs} parágrafos analíticos e aproximadamente {min_words} palavras, salvo se o texto-base for muito curto.\n{matriz_line}\n- Em cada resumo individual, diferencie claramente: exposição fiel do argumento do autor; análise crítica/interpretativa; contribuição para a disciplina; e limites/tensões.\n{dialogo_line}\n{sinoptico_line}\n{tabela_line}\n- A comparação entre os textos deve ter pelo menos {min_comparison} palavras e discutir convergências, divergências, diferenças de escala, método, objeto, pressupostos teóricos, arquitetura conceitual e contribuição para a disciplina.\n- A síntese analítica deve ter pelo menos {min_synthesis} palavras e apresentar interpretação própria do corpus, não apenas repetir os resumos.\n- Use citações ao longo das análises individuais e da comparação, mas nunca escreva chaves BibTeX cruas no texto visível.\n- Não numere manualmente títulos de seções ou subseções. Use títulos como "Introdução", "Resumo individual dos textos" e o nome de cada artigo; o Org/LaTeX fará a numeração automaticamente.\n'.strip()`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `'; '.join`
- `_is_resumo_artigos_profile`
- `_safe_int`
- `bool`
- `cfg.get`
- `f'\n\nDiretrizes específicas de profundidade para o perfil resumo_artigos_local_fgv:\n- Profundidade analítica solicitada: {nivel}. O documento deve ser substancial, interpretativo e comparativo; não deve parecer ementa, sinopse ou resumo escolar curto.\n- Para cada artigo/texto, produza pelo menos {min_paragraphs} parágrafos analíticos e aproximadamente {min_words} palavras, salvo se o texto-base for muito curto.\n{matriz_line}\n- Em cada resumo individual, diferencie claramente: exposição fiel do argumento do autor; análise crítica/interpretativa; contribuição para a disciplina; e limites/tensões.\n{dialogo_line}\n{sinoptico_line}\n{tabela_line}\n- A comparação entre os textos deve ter pelo menos {min_comparison} palavras e discutir convergências, divergências, diferenças de escala, método, objeto, pressupostos teóricos, arquitetura conceitual e contribuição para a disciplina.\n- A síntese analítica deve ter pelo menos {min_synthesis} palavras e apresentar interpretação própria do corpus, não apenas repetir os resumos.\n- Use citações ao longo das análises individuais e da comparação, mas nunca escreva chaves BibTeX cruas no texto visível.\n- Não numere manualmente títulos de seções ou subseções. Use títulos como "Introdução", "Resumo individual dos textos" e o nome de cada artigo; o Org/LaTeX fará a numeração automaticamente.\n'.strip`
- `isinstance`
- `resumo.get`
- `str`
- `str(e).strip`
- `str(resumo.get('nivel_detalhamento') or resumo.get('profundidade_analitica') or 'profundo').strip`
- `str(resumo.get('nivel_detalhamento') or resumo.get('profundidade_analitica') or 'profundo').strip().lower`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_builder:_common_generation_rules`
- `app_bundle.scripts.pipeline.document_builder:build_prompt`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 27
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4342a2454736eb84a52335af282120bbd31d58630f6b0b487a3beb2ffcf83c0d`
**LOC do arquivo-fonte:** 715


### `app_bundle.scripts.pipeline.document_builder:_meta_from_cfg`

**Assinatura:**
```python
def _meta_from_cfg(cfg: dict[str, Any]) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/document_builder.py`
**Linhas:** 122–142
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_meta_from_cfg` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `{'tipo_documento': documento.get('tipo_documento') or 'paper', 'titulo': documento.get('titulo_trabalho') or atividade.get('titulo_trabalho') or pesquisa.get('titulo_sugerido') or '', 'autor': documento.get('autor') or atividade.get('aluno') or 'Gustavo M. Mendes de Tarso', 'instituicao': documento.get('institution_name') or 'Fundação Getúlio Vargas', 'programa': documento.get('program_name') or '', 'curso': documento.get('course_name') or atividade.get('curso') or 'Mestrado Acadêmico em Políticas Públicas e Governo', 'turma': atividade.get('turma') or '', 'polo': atividade.get('polo') or 'Brasília', 'disciplina': documento.get('discipline_name') or atividade.get('disciplina') or '', 'professor': documento.get('professor_name') or atividade.get('professor') or '', 'cidade': documento.get('city_name') or atividade.get('polo') or 'Brasília', 'ano': documento.get('ano') or atividade.get('data') or '', 'data': atividade.get('data') or documento.get('data') or '', 'tipo_trabalho': documento.get('papertype') or ('Paper acadêmico' if str(documento.get('tipo_documento') or 'paper') == 'paper' else 'Atividade acadêmica'), 'nota_capa': documento.get('covernote') or 'Trabalho acadêmico elaborado para a disciplina.'}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `atividade.get`
- `cfg.get`
- `documento.get`
- `isinstance`
- `pesquisa.get`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_builder:_build_resumo_artigos_document_model_staged`
- `app_bundle.scripts.pipeline.document_builder:build_document_model`
- `app_bundle.scripts.pipeline.document_builder:build_prompt`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4342a2454736eb84a52335af282120bbd31d58630f6b0b487a3beb2ffcf83c0d`
**LOC do arquivo-fonte:** 715


### `app_bundle.scripts.pipeline.document_builder:build_prompt`

**Assinatura:**
```python
def build_prompt(cfg: dict[str, Any], docs: list[SourceDoc], orientations: list[SourceDoc], bib_keys: list[str], bib_path: Path) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/document_builder.py`
**Linhas:** 145–226
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Constrói a responsabilidade representada por `prompt` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `docs` | positional-or-keyword | `list[SourceDoc]` | `obrigatório/ausente` | Coleção de documentos/entradas processados pela rotina. |
| `orientations` | positional-or-keyword | `list[SourceDoc]` | `obrigatório/ausente` | Parâmetro `orientations` recebido pela função; não há descrição individual explícita no código. |
| `bib_keys` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `bib_keys` recebido pela função; não há descrição individual explícita no código. |
| `bib_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `textwrap.dedent(f'\n    Você é um gerador acadêmico estruturado. Gere um AcademicDocument JSON canônico, seguindo estritamente o schema.\n\n    Regras obrigatórias:\n    1. Não gere ORG, LaTeX, DOCX, Markdown ou PDF. Gere apenas o objeto estruturado AcademicDocument.\n    2. Use citações como objetos do tipo citation, nunca como texto cru.\n    3. Toda citação deve usar exclusivamente chaves BibTeX da lista permitida.\n    3.1. Nunca escreva a chave BibTeX literal no texto, em títulos, listas, tabelas ou parágrafos. Exemplo proibido: geet2019_policy_design. Para citar, use objeto citation com keys=["geet2019_policy_design"].\n    3.2. Nunca use citações numéricas em colchetes como [1], [2] ou [1, 2, 3]. Para documentos FGV/ABNT/APA, a saída deve ficar em autor-data, gerada pelo renderizador a partir de objetos citation.\n    3.3. Se quiser citar vários textos no mesmo ponto, use um único objeto citation com várias chaves, por exemplo keys=["chave1", "chave2"], e nunca texto como [1, 2].\n    4. Não mencione pipeline, cache, OCR, fulltext_cache, metadados incompletos, metadados inferidos ou limitações técnicas de extração.\n    5. Se houver mapa mental, não o crie como seção textual comum; ele será renderizado por outro módulo.\n    6. O conteúdo deve seguir as orientações e o tipo do documento.\n    7. Mantenha coerência acadêmica: pergunta, objetivo, tese/hipótese, desenvolvimento e conclusão.\n    8. Para paper, produza estrutura acadêmica enxuta; para atividade, responda ao roteiro e sintetize/comparare os textos conforme orientação.\n    9. Não invente referência fora do .bib.\n    9.1. Não crie seção final chamada REFERÊNCIAS, BIBLIOGRAFIA ou similar; o renderizador insere a bibliografia automaticamente com base no .bib.\n    10. Inclua metadata conforme os dados fornecidos.\n    10.1. Se tema, recorte, objetivo ou pergunta orientadora estiverem vazios no TOML, mas estiverem presentes nas orientações carregadas, infira-os fielmente dessas orientações; não invente um novo enunciado.\n    10.2. Se pesquisa.gerar_palavras_chave_ia=true e pesquisa.palavras_chave estiver vazio, infira de 4 a 6 palavras-chave acadêmicas a partir do tema, da pergunta orientadora, do enunciado, das orientações e do corpus local. Use-as no campo de palavras-chave do documento quando esse campo for aplicável, sem criar uma seção artificial apenas para listá-las.\n\n    Diretivas complementares carregadas pelo prompt bank:\n    {prompt_extras}\n\n    {depth_directives}\n\n    Metadados sugeridos:\n    {json.dumps(meta, ensure_ascii=False, indent=2)}\n\n    Pesquisa/TOML:\n    {json.dumps(pesquisa, ensure_ascii=False, indent=2)}\n\n    Configuração do documento:\n    {json.dumps(documento, ensure_ascii=False, indent=2)}\n\n    Chaves BibTeX permitidas:\n    {json.dumps(bib_keys, ensure_ascii=False)}\n\n    Caminho do .bib:\n    {bib_path.name}\n\n    Orientações:\n    {shorten_text(json.dumps(orientation_summaries, ensure_ascii=False, indent=2), 25000)}\n\n    Documentos-base:\n    {shorten_text(json.dumps(doc_summaries, ensure_ascii=False, indent=2), total_corpus_chars)}\n    ').strip()`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `_is_resumo_artigos_profile`
- `_meta_from_cfg`
- `_resumo_artigos_depth_directives`
- `_safe_int`
- `cfg.get`
- `doc_summaries.append`
- `isinstance`
- `json.dumps`
- `len`
- `load_prompt_bundle`
- `max`
- `meta.get`
- `min`
- `orientation_summaries.append`
- `resumo_cfg.get`
- `shorten_text`
- `str`
- `textwrap.dedent`
- `textwrap.dedent(f'\n    Você é um gerador acadêmico estruturado. Gere um AcademicDocument JSON canônico, seguindo estritamente o schema.\n\n    Regras obrigatórias:\n    1. Não gere ORG, LaTeX, DOCX, Markdown ou PDF. Gere apenas o objeto estruturado AcademicDocument.\n    2. Use citações como objetos do tipo citation, nunca como texto cru.\n    3. Toda citação deve usar exclusivamente chaves BibTeX da lista permitida.\n    3.1. Nunca escreva a chave BibTeX literal no texto, em títulos, listas, tabelas ou parágrafos. Exemplo proibido: geet2019_policy_design. Para citar, use objeto citation com keys=["geet2019_policy_design"].\n    3.2. Nunca use citações numéricas em colchetes como [1], [2] ou [1, 2, 3]. Para documentos FGV/ABNT/APA, a saída deve ficar em autor-data, gerada pelo renderizador a partir de objetos citation.\n    3.3. Se quiser citar vários textos no mesmo ponto, use um único objeto citation com várias chaves, por exemplo keys=["chave1", "chave2"], e nunca texto como [1, 2].\n    4. Não mencione pipeline, cache, OCR, fulltext_cache, metadados incompletos, metadados inferidos ou limitações técnicas de extração.\n    5. Se houver mapa mental, não o crie como seção textual comum; ele será renderizado por outro módulo.\n    6. O conteúdo deve seguir as orientações e o tipo do documento.\n    7. Mantenha coerência acadêmica: pergunta, objetivo, tese/hipótese, desenvolvimento e conclusão.\n    8. Para paper, produza estrutura acadêmica enxuta; para atividade, responda ao roteiro e sintetize/comparare os textos conforme orientação.\n    9. Não invente referência fora do .bib.\n    9.1. Não crie seção final chamada REFERÊNCIAS, BIBLIOGRAFIA ou similar; o renderizador insere a bibliografia automaticamente com base no .bib.\n    10. Inclua metadata conforme os dados fornecidos.\n    10.1. Se tema, recorte, objetivo ou pergunta orientadora estiverem vazios no TOML, mas estiverem presentes nas orientações carregadas, infira-os fielmente dessas orientações; não invente um novo enunciado.\n    10.2. Se pesquisa.gerar_palavras_chave_ia=true e pesquisa.palavras_chave estiver vazio, infira de 4 a 6 palavras-chave acadêmicas a partir do tema, da pergunta orientadora, do enunciado, das orientações e do corpus local. Use-as no campo de palavras-chave do documento quando esse campo for aplicável, sem criar uma seção artificial apenas para listá-las.\n\n    Diretivas complementares carregadas pelo prompt bank:\n    {prompt_extras}\n\n    {depth_directives}\n\n    Metadados sugeridos:\n    {json.dumps(meta, ensure_ascii=False, indent=2)}\n\n    Pesquisa/TOML:\n    {json.dumps(pesquisa, ensure_ascii=False, indent=2)}\n\n    Configuração do documento:\n    {json.dumps(documento, ensure_ascii=False, indent=2)}\n\n    Chaves BibTeX permitidas:\n    {json.dumps(bib_keys, ensure_ascii=False)}\n\n    Caminho do .bib:\n    {bib_path.name}\n\n    Orientações:\n    {shorten_text(json.dumps(orientation_summaries, ensure_ascii=False, indent=2), 25000)}\n\n    Documentos-base:\n    {shorten_text(json.dumps(doc_summaries, ensure_ascii=False, indent=2), total_corpus_chars)}\n    ').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_builder:build_document_model`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `      Caminho do .bib:     `

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4342a2454736eb84a52335af282120bbd31d58630f6b0b487a3beb2ffcf83c0d`
**LOC do arquivo-fonte:** 715


### `app_bundle.scripts.pipeline.document_builder:_is_substantive_text`

**Assinatura:**
```python
def _is_substantive_text(value: Any) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/document_builder.py`
**Linhas:** 236–239
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Verifica se `substantive_text` conforme as regras codificadas na rotina.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `len(text) >= 30 and bool(re.search('[A-Za-zÀ-ÿ]{4,}', text))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `bool`
- `len`
- `re.search`
- `str`
- `str(value or '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_builder:_content_has_substantive_text`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4342a2454736eb84a52335af282120bbd31d58630f6b0b487a3beb2ffcf83c0d`
**LOC do arquivo-fonte:** 715


### `app_bundle.scripts.pipeline.document_builder:_content_has_substantive_text`

**Assinatura:**
```python
def _content_has_substantive_text(content: list[Any] | None) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/document_builder.py`
**Linhas:** 242–246
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_content_has_substantive_text` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `content` | positional-or-keyword | `list[Any] \| None` | `obrigatório/ausente` | Parâmetro `content` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `True`; `False`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_is_substantive_text`
- `getattr`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_builder:_section_plain_text`
- `app_bundle.scripts.pipeline.document_builder:repair_section_blocks`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4342a2454736eb84a52335af282120bbd31d58630f6b0b487a3beb2ffcf83c0d`
**LOC do arquivo-fonte:** 715


### `app_bundle.scripts.pipeline.document_builder:_content_citations`

**Assinatura:**
```python
def _content_citations(content: list[Any] | None) -> list[Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/document_builder.py`
**Linhas:** 249–254
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_content_citations` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `content` | positional-or-keyword | `list[Any] \| None` | `obrigatório/ausente` | Parâmetro `content` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[Any]`. Retornos observados estaticamente incluem: `out`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `getattr`
- `out.append`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_builder:_section_plain_text`
- `app_bundle.scripts.pipeline.document_builder:repair_section_blocks`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4342a2454736eb84a52335af282120bbd31d58630f6b0b487a3beb2ffcf83c0d`
**LOC do arquivo-fonte:** 715


### `app_bundle.scripts.pipeline.document_builder:_strip_numeric_citation_markers`

**Assinatura:**
```python
def _strip_numeric_citation_markers(text: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/document_builder.py`
**Linhas:** 257–263
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Remove a responsabilidade representada por `numeric_citation_markers` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `cleaned`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `re.sub`
- `re.sub('\\s{2,}', ' ', cleaned).strip`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4342a2454736eb84a52335af282120bbd31d58630f6b0b487a3beb2ffcf83c0d`
**LOC do arquivo-fonte:** 715


### `app_bundle.scripts.pipeline.document_builder:repair_section_blocks`

**Assinatura:**
```python
def repair_section_blocks(section: Section) -> Section
```

**Arquivo:** `app_bundle/scripts/pipeline/document_builder.py`
**Linhas:** 266–291
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Repara parágrafos retornados pela IA em Structured Outputs.

**Docstring resumida:** Repara parágrafos retornados pela IA em Structured Outputs.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `section` | positional-or-keyword | `Section` | `obrigatório/ausente` | Parâmetro `section` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Section`. Retornos observados estaticamente incluem: `section`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `TextSpan`
- `_content_citations`
- `_content_has_substantive_text`
- `_strip_numeric_citation_markers`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_builder:_parse_section`
- `app_bundle.scripts.pipeline.document_builder:repair_section_bundle`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 1
comprehensions: 4
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4342a2454736eb84a52335af282120bbd31d58630f6b0b487a3beb2ffcf83c0d`
**LOC do arquivo-fonte:** 715


### `app_bundle.scripts.pipeline.document_builder:repair_section_bundle`

**Assinatura:**
```python
def repair_section_bundle(bundle: SectionBundle) -> SectionBundle
```

**Arquivo:** `app_bundle/scripts/pipeline/document_builder.py`
**Linhas:** 294–297
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `repair_section_bundle` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `bundle` | positional-or-keyword | `SectionBundle` | `obrigatório/ausente` | Parâmetro `bundle` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `SectionBundle`. Retornos observados estaticamente incluem: `bundle`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `repair_section_blocks`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_builder:_parse_section_bundle`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4342a2454736eb84a52335af282120bbd31d58630f6b0b487a3beb2ffcf83c0d`
**LOC do arquivo-fonte:** 715


### `app_bundle.scripts.pipeline.document_builder:_progress`

**Assinatura:**
```python
def _progress(progress: Callable[[str], None] | None, message: str) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/document_builder.py`
**Linhas:** 300–302
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_progress` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `progress` | positional-or-keyword | `Callable[[str], None] \| None` | `obrigatório/ausente` | Parâmetro `progress` recebido pela função; não há descrição individual explícita no código. |
| `message` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `message` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `progress`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_builder:_build_resumo_artigos_document_model_staged`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4342a2454736eb84a52335af282120bbd31d58630f6b0b487a3beb2ffcf83c0d`
**LOC do arquivo-fonte:** 715


### `app_bundle.scripts.pipeline.document_builder:_section_plain_text`

**Assinatura:**
```python
def _section_plain_text(section: Section, limit: int = 12000) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/document_builder.py`
**Linhas:** 305–342
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_section_plain_text` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `section` | positional-or-keyword | `Section` | `obrigatório/ausente` | Parâmetro `section` recebido pela função; não há descrição individual explícita no código. |
| `limit` | positional-or-keyword | `int` | `12000` | Parâmetro `limit` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `shorten_text('\n'.join((x for x in parts if x)), limit)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `' | '.join`
- `''.join`
- `','.join`
- `'\n'.join`
- `(raw + (' ' + ' '.join(citations) if citations else '')).strip`
- `_content_citations`
- `_content_has_substantive_text`
- `_strip_numeric_citation_markers`
- `buf.append`
- `citations.append`
- `getattr`
- `parts.append`
- `parts.extend`
- `shorten_text`
- `str`
- `str(section.title or '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_builder:_generate_comparison_section`
- `app_bundle.scripts.pipeline.document_builder:_generate_intro_synthesis_bundle`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 13
loops: 4
comprehensions: 3
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4342a2454736eb84a52335af282120bbd31d58630f6b0b487a3beb2ffcf83c0d`
**LOC do arquivo-fonte:** 715


### `app_bundle.scripts.pipeline.document_builder:_model_dump_for_checkpoint`

**Assinatura:**
```python
def _model_dump_for_checkpoint(obj: Any) -> Any
```

**Arquivo:** `app_bundle/scripts/pipeline/document_builder.py`
**Linhas:** 345–348
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_model_dump_for_checkpoint` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `obj` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `obj` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Any`. Retornos observados estaticamente incluem: `obj.model_dump()`; `obj`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `hasattr`
- `obj.model_dump`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_builder:_write_checkpoint`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4342a2454736eb84a52335af282120bbd31d58630f6b0b487a3beb2ffcf83c0d`
**LOC do arquivo-fonte:** 715


### `app_bundle.scripts.pipeline.document_builder:_write_checkpoint`

**Assinatura:**
```python
def _write_checkpoint(checkpoint_dir: Path | None, prefix: str, name: str, payload: Any) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/document_builder.py`
**Linhas:** 351–356
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Grava a responsabilidade representada por `checkpoint` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `checkpoint_dir` | positional-or-keyword | `Path \| None` | `obrigatório/ausente` | Parâmetro `checkpoint_dir` recebido pela função; não há descrição individual explícita no código. |
| `prefix` | positional-or-keyword | `str` | `obrigatório/ausente` | Prefixo canônico usado para compor nomes de artefatos. |
| `name` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `name` recebido pela função; não há descrição individual explícita no código. |
| `payload` | positional-or-keyword | `Any` | `obrigatório/ausente` | Payload estruturado recebido ou produzido pela operação. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `_model_dump_for_checkpoint`
- `slugify`
- `write_json`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_builder:_build_resumo_artigos_document_model_staged`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.json`

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4342a2454736eb84a52335af282120bbd31d58630f6b0b487a3beb2ffcf83c0d`
**LOC do arquivo-fonte:** 715


### `app_bundle.scripts.pipeline.document_builder:_resumo_limits`

**Assinatura:**
```python
def _resumo_limits(cfg: dict[str, Any], docs_count: int) -> tuple[int, int]
```

**Arquivo:** `app_bundle/scripts/pipeline/document_builder.py`
**Linhas:** 359–365
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_resumo_limits` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `docs_count` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `docs_count` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `tuple[int, int]`. Retornos observados estaticamente incluem: `(per_doc_chars, total_corpus_chars)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_safe_int`
- `cfg.get`
- `isinstance`
- `max`
- `min`
- `resumo_cfg.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_builder:_generate_article_section`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4342a2454736eb84a52335af282120bbd31d58630f6b0b487a3beb2ffcf83c0d`
**LOC do arquivo-fonte:** 715


### `app_bundle.scripts.pipeline.document_builder:_common_generation_rules`

**Assinatura:**
```python
def _common_generation_rules(cfg: dict[str, Any], bib_keys: list[str], bib_path: Path, docs_count: int) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/document_builder.py`
**Linhas:** 368–398
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_common_generation_rules` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `bib_keys` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `bib_keys` recebido pela função; não há descrição individual explícita no código. |
| `bib_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `docs_count` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `docs_count` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `textwrap.dedent(f'\n    Regras obrigatórias para a geração estruturada:\n    1. Gere apenas o objeto estruturado solicitado. Não gere Markdown, ORG, LaTeX ou texto fora do JSON estruturado.\n    2. Use citações como objetos do tipo citation, nunca como texto cru.\n    3. Toda citação deve usar exclusivamente chaves BibTeX da lista permitida.\n    4. Nunca escreva a chave BibTeX literal no texto visível, em títulos, listas, tabelas ou parágrafos.\n    5. Nunca use citações numéricas em colchetes, como [1], [2] ou [1, 2, 3]. A saída final deve ser autor-data pelo renderizador.\n    6. Não crie seção final chamada REFERÊNCIAS, BIBLIOGRAFIA ou similar; o renderizador insere a bibliografia automaticamente com base no .bib.\n    7. Não numere manualmente títulos de seções ou subseções. O Org/LaTeX fará a numeração automaticamente.\n    8. Não mencione pipeline, cache, OCR, fulltext_cache, metadados incompletos, metadados inferidos ou limitações técnicas de extração.\n    9. Não invente referência fora do .bib.\n\n    Diretivas complementares carregadas pelo prompt bank:\n    {prompt_extras}\n\n    {depth_directives}\n\n    Chaves BibTeX permitidas:\n    {json.dumps(bib_keys, ensure_ascii=False)}\n\n    Caminho do .bib:\n    {bib_path.name}\n    ').strip()`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `(cfg.get('documento', {}) if isinstance(cfg.get('documento'), dict) else {}).get`
- `_resumo_artigos_depth_directives`
- `cfg.get`
- `isinstance`
- `json.dumps`
- `load_prompt_bundle`
- `str`
- `textwrap.dedent`
- `textwrap.dedent(f'\n    Regras obrigatórias para a geração estruturada:\n    1. Gere apenas o objeto estruturado solicitado. Não gere Markdown, ORG, LaTeX ou texto fora do JSON estruturado.\n    2. Use citações como objetos do tipo citation, nunca como texto cru.\n    3. Toda citação deve usar exclusivamente chaves BibTeX da lista permitida.\n    4. Nunca escreva a chave BibTeX literal no texto visível, em títulos, listas, tabelas ou parágrafos.\n    5. Nunca use citações numéricas em colchetes, como [1], [2] ou [1, 2, 3]. A saída final deve ser autor-data pelo renderizador.\n    6. Não crie seção final chamada REFERÊNCIAS, BIBLIOGRAFIA ou similar; o renderizador insere a bibliografia automaticamente com base no .bib.\n    7. Não numere manualmente títulos de seções ou subseções. O Org/LaTeX fará a numeração automaticamente.\n    8. Não mencione pipeline, cache, OCR, fulltext_cache, metadados incompletos, metadados inferidos ou limitações técnicas de extração.\n    9. Não invente referência fora do .bib.\n\n    Diretivas complementares carregadas pelo prompt bank:\n    {prompt_extras}\n\n    {depth_directives}\n\n    Chaves BibTeX permitidas:\n    {json.dumps(bib_keys, ensure_ascii=False)}\n\n    Caminho do .bib:\n    {bib_path.name}\n    ').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_builder:_generate_article_section`
- `app_bundle.scripts.pipeline.document_builder:_generate_comparison_section`
- `app_bundle.scripts.pipeline.document_builder:_generate_intro_synthesis_bundle`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `      Caminho do .bib:     `

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4342a2454736eb84a52335af282120bbd31d58630f6b0b487a3beb2ffcf83c0d`
**LOC do arquivo-fonte:** 715


### `app_bundle.scripts.pipeline.document_builder:_orientation_context`

**Assinatura:**
```python
def _orientation_context(orientations: list[SourceDoc], limit: int = 18000) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/document_builder.py`
**Linhas:** 401–405
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_orientation_context` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `orientations` | positional-or-keyword | `list[SourceDoc]` | `obrigatório/ausente` | Parâmetro `orientations` recebido pela função; não há descrição individual explícita no código. |
| `limit` | positional-or-keyword | `int` | `18000` | Parâmetro `limit` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `shorten_text(json.dumps(orientation_summaries, ensure_ascii=False, indent=2), limit)`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `json.dumps`
- `orientation_summaries.append`
- `shorten_text`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_builder:_generate_article_section`
- `app_bundle.scripts.pipeline.document_builder:_generate_comparison_section`
- `app_bundle.scripts.pipeline.document_builder:_generate_intro_synthesis_bundle`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4342a2454736eb84a52335af282120bbd31d58630f6b0b487a3beb2ffcf83c0d`
**LOC do arquivo-fonte:** 715


### `app_bundle.scripts.pipeline.document_builder:_docs_index_context`

**Assinatura:**
```python
def _docs_index_context(docs: list[SourceDoc], current: SourceDoc | None = None, limit: int = 18000) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/document_builder.py`
**Linhas:** 408–418
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_docs_index_context` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `docs` | positional-or-keyword | `list[SourceDoc]` | `obrigatório/ausente` | Coleção de documentos/entradas processados pela rotina. |
| `current` | positional-or-keyword | `SourceDoc \| None` | `None` | Parâmetro `current` recebido pela função; não há descrição individual explícita no código. |
| `limit` | positional-or-keyword | `int` | `18000` | Parâmetro `limit` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `shorten_text(json.dumps(rows, ensure_ascii=False, indent=2), limit)`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `bool`
- `enumerate`
- `json.dumps`
- `rows.append`
- `shorten_text`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_builder:_generate_article_section`
- `app_bundle.scripts.pipeline.document_builder:_generate_comparison_section`
- `app_bundle.scripts.pipeline.document_builder:_generate_intro_synthesis_bundle`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4342a2454736eb84a52335af282120bbd31d58630f6b0b487a3beb2ffcf83c0d`
**LOC do arquivo-fonte:** 715


### `app_bundle.scripts.pipeline.document_builder:_parse_section`

**Assinatura:**
```python
def _parse_section(client: Any, model: str, prompt: str, *, section_name: str) -> Section
```

**Arquivo:** `app_bundle/scripts/pipeline/document_builder.py`
**Linhas:** 421–429
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Interpreta a responsabilidade representada por `section` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `client` | positional-or-keyword | `Any` | `obrigatório/ausente` | Cliente de serviço externo/IA utilizado pela rotina; pode ser opcional conforme o fluxo. |
| `model` | positional-or-keyword | `str` | `obrigatório/ausente` | Identificador do modelo utilizado pela etapa de IA. |
| `prompt` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `prompt` recebido pela função; não há descrição individual explícita no código. |
| `section_name` | keyword-only | `str` | `obrigatório/ausente` | Parâmetro `section_name` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Section`. Retornos observados estaticamente incluem: `repair_section_blocks(resp.output_parsed)`.

#### Efeitos colaterais e fronteiras externas

- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `RuntimeError`
- `client.responses.parse`
- `repair_section_blocks`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_builder:_generate_article_section`
- `app_bundle.scripts.pipeline.document_builder:_generate_comparison_section`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError(f'A IA não retornou Section estruturada para {section_name}.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4342a2454736eb84a52335af282120bbd31d58630f6b0b487a3beb2ffcf83c0d`
**LOC do arquivo-fonte:** 715


### `app_bundle.scripts.pipeline.document_builder:_parse_section_bundle`

**Assinatura:**
```python
def _parse_section_bundle(client: Any, model: str, prompt: str, *, bundle_name: str) -> SectionBundle
```

**Arquivo:** `app_bundle/scripts/pipeline/document_builder.py`
**Linhas:** 432–440
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Interpreta a responsabilidade representada por `section_bundle` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `client` | positional-or-keyword | `Any` | `obrigatório/ausente` | Cliente de serviço externo/IA utilizado pela rotina; pode ser opcional conforme o fluxo. |
| `model` | positional-or-keyword | `str` | `obrigatório/ausente` | Identificador do modelo utilizado pela etapa de IA. |
| `prompt` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `prompt` recebido pela função; não há descrição individual explícita no código. |
| `bundle_name` | keyword-only | `str` | `obrigatório/ausente` | Parâmetro `bundle_name` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `SectionBundle`. Retornos observados estaticamente incluem: `repair_section_bundle(resp.output_parsed)`.

#### Efeitos colaterais e fronteiras externas

- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `RuntimeError`
- `client.responses.parse`
- `repair_section_bundle`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_builder:_generate_intro_synthesis_bundle`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError(f'A IA não retornou SectionBundle estruturado para {bundle_name}.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4342a2454736eb84a52335af282120bbd31d58630f6b0b487a3beb2ffcf83c0d`
**LOC do arquivo-fonte:** 715


### `app_bundle.scripts.pipeline.document_builder:_generate_article_section`

**Assinatura:**
```python
def _generate_article_section(client: Any, model: str, cfg: dict[str, Any], doc: SourceDoc, docs: list[SourceDoc], orientations: list[SourceDoc], bib_keys: list[str], bib_path: Path, idx: int, total: int) -> Section
```

**Arquivo:** `app_bundle/scripts/pipeline/document_builder.py`
**Linhas:** 443–495
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Gera a responsabilidade representada por `article_section` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `client` | positional-or-keyword | `Any` | `obrigatório/ausente` | Cliente de serviço externo/IA utilizado pela rotina; pode ser opcional conforme o fluxo. |
| `model` | positional-or-keyword | `str` | `obrigatório/ausente` | Identificador do modelo utilizado pela etapa de IA. |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `doc` | positional-or-keyword | `SourceDoc` | `obrigatório/ausente` | Documento/registro individual processado pela rotina. |
| `docs` | positional-or-keyword | `list[SourceDoc]` | `obrigatório/ausente` | Coleção de documentos/entradas processados pela rotina. |
| `orientations` | positional-or-keyword | `list[SourceDoc]` | `obrigatório/ausente` | Parâmetro `orientations` recebido pela função; não há descrição individual explícita no código. |
| `bib_keys` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `bib_keys` recebido pela função; não há descrição individual explícita no código. |
| `bib_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `idx` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `idx` recebido pela função; não há descrição individual explícita no código. |
| `total` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `total` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Section`. Retornos observados estaticamente incluem: `section`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `_common_generation_rules`
- `_docs_index_context`
- `_orientation_context`
- `_parse_section`
- `_resumo_limits`
- `cfg.get`
- `isinstance`
- `json.dumps`
- `len`
- `shorten_text`
- `textwrap.dedent`
- `textwrap.dedent(f"\n    Você está gerando, em etapas, a seção individual de análise do artigo {idx}/{total}.\n\n    Retorne exatamente uma Section estruturada:\n    - level = 2\n    - title = título do artigo/texto analisado, sem numeração manual\n    - blocks = parágrafos analíticos densos, e tabela apenas se realmente útil\n\n    A seção deve reconstruir profundamente o texto analisado, cobrindo:\n    - problema/questão central;\n    - objetivo e escopo;\n    - argumento ou tese principal;\n    - conceitos e categorias mobilizados;\n    - método, evidências ou estratégia argumentativa;\n    - achados, contribuições e implicações para a disciplina;\n    - limites, tensões e lacunas;\n    - diálogo com o corpus, sem forçar unidade artificial.\n\n    Use preferencialmente a chave do próprio texto analisado: {doc.bib_key}\n    Pode usar outras chaves apenas para diálogo comparativo real.\n\n    {common_rules}\n\n    Pesquisa/TOML:\n    {json.dumps(pesquisa, ensure_ascii=False, indent=2)}\n\n    Orientações do projeto:\n    {_orientation_context(orientations)}\n\n    Índice dos documentos do corpus:\n    {_docs_index_context(docs, current=doc)}\n\n    Texto-base integral/excerto ampliado do artigo atual:\n    {json.dumps({'label': doc.label, 'bib_key': doc.bib_key, 'excerpt': shorten_text(doc.extracted_text, per_doc_chars)}, ensure_ascii=False, indent=2)}\n    ").strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_builder:_build_resumo_artigos_document_model_staged`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4342a2454736eb84a52335af282120bbd31d58630f6b0b487a3beb2ffcf83c0d`
**LOC do arquivo-fonte:** 715


### `app_bundle.scripts.pipeline.document_builder:_generate_comparison_section`

**Assinatura:**
```python
def _generate_comparison_section(client: Any, model: str, cfg: dict[str, Any], docs: list[SourceDoc], orientations: list[SourceDoc], article_sections: list[Section], bib_keys: list[str], bib_path: Path) -> Section
```

**Arquivo:** `app_bundle/scripts/pipeline/document_builder.py`
**Linhas:** 498–545
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Gera a responsabilidade representada por `comparison_section` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `client` | positional-or-keyword | `Any` | `obrigatório/ausente` | Cliente de serviço externo/IA utilizado pela rotina; pode ser opcional conforme o fluxo. |
| `model` | positional-or-keyword | `str` | `obrigatório/ausente` | Identificador do modelo utilizado pela etapa de IA. |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `docs` | positional-or-keyword | `list[SourceDoc]` | `obrigatório/ausente` | Coleção de documentos/entradas processados pela rotina. |
| `orientations` | positional-or-keyword | `list[SourceDoc]` | `obrigatório/ausente` | Parâmetro `orientations` recebido pela função; não há descrição individual explícita no código. |
| `article_sections` | positional-or-keyword | `list[Section]` | `obrigatório/ausente` | Parâmetro `article_sections` recebido pela função; não há descrição individual explícita no código. |
| `bib_keys` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `bib_keys` recebido pela função; não há descrição individual explícita no código. |
| `bib_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `Section`. Retornos observados estaticamente incluem: `section`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `_common_generation_rules`
- `_docs_index_context`
- `_orientation_context`
- `_parse_section`
- `_section_plain_text`
- `cfg.get`
- `isinstance`
- `json.dumps`
- `len`
- `shorten_text`
- `textwrap.dedent`
- `textwrap.dedent(f'\n    Você está gerando a seção de comparação transversal entre os textos do corpus.\n\n    Retorne exatamente uma Section estruturada:\n    - level = 1\n    - title = "Comparação entre os textos"\n    - blocks = tabela comparativa sintética, se útil, seguida de análise em prosa densa.\n\n    A comparação deve discutir, quando aplicável:\n    - convergências e divergências reais;\n    - diferença de objeto, escala analítica e método;\n    - conceitos e categorias de cada texto;\n    - pressupostos teóricos;\n    - contribuição para a disciplina;\n    - limites comparativos;\n    - articulações possíveis sem forçar unidade temática artificial.\n\n    {common_rules}\n\n    Pesquisa/TOML:\n    {json.dumps(pesquisa, ensure_ascii=False, indent=2)}\n\n    Orientações do projeto:\n    {_orientation_context(orientations)}\n\n    Índice dos documentos do corpus:\n    {_docs_index_context(docs)}\n\n    Análises individuais já geradas:\n    {shorten_text(json.dumps(analyses, ensure_ascii=False, indent=2), 50000)}\n    ').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_builder:_build_resumo_artigos_document_model_staged`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4342a2454736eb84a52335af282120bbd31d58630f6b0b487a3beb2ffcf83c0d`
**LOC do arquivo-fonte:** 715


### `app_bundle.scripts.pipeline.document_builder:_generate_intro_synthesis_bundle`

**Assinatura:**
```python
def _generate_intro_synthesis_bundle(client: Any, model: str, cfg: dict[str, Any], docs: list[SourceDoc], orientations: list[SourceDoc], article_sections: list[Section], comparison_section: Section, bib_keys: list[str], bib_path: Path) -> SectionBundle
```

**Arquivo:** `app_bundle/scripts/pipeline/document_builder.py`
**Linhas:** 548–595
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Gera a responsabilidade representada por `intro_synthesis_bundle` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `client` | positional-or-keyword | `Any` | `obrigatório/ausente` | Cliente de serviço externo/IA utilizado pela rotina; pode ser opcional conforme o fluxo. |
| `model` | positional-or-keyword | `str` | `obrigatório/ausente` | Identificador do modelo utilizado pela etapa de IA. |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `docs` | positional-or-keyword | `list[SourceDoc]` | `obrigatório/ausente` | Coleção de documentos/entradas processados pela rotina. |
| `orientations` | positional-or-keyword | `list[SourceDoc]` | `obrigatório/ausente` | Parâmetro `orientations` recebido pela função; não há descrição individual explícita no código. |
| `article_sections` | positional-or-keyword | `list[Section]` | `obrigatório/ausente` | Parâmetro `article_sections` recebido pela função; não há descrição individual explícita no código. |
| `comparison_section` | positional-or-keyword | `Section` | `obrigatório/ausente` | Parâmetro `comparison_section` recebido pela função; não há descrição individual explícita no código. |
| `bib_keys` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `bib_keys` recebido pela função; não há descrição individual explícita no código. |
| `bib_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `SectionBundle`. Retornos observados estaticamente incluem: `bundle`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `_common_generation_rules`
- `_docs_index_context`
- `_orientation_context`
- `_parse_section_bundle`
- `_section_plain_text`
- `cfg.get`
- `isinstance`
- `json.dumps`
- `len`
- `shorten_text`
- `textwrap.dedent`
- `textwrap.dedent(f'\n    Você está gerando as seções de abertura e fechamento analítico do documento.\n\n    Retorne um SectionBundle com exatamente três seções, nesta ordem:\n    1. Section level=1 title="Introdução"\n    2. Section level=1 title="Síntese analítica"\n    3. Section level=1 title="Considerações finais"\n\n    A Introdução deve apresentar o corpus, o foco de leitura, a heterogeneidade ou unidade temática dos textos, o objetivo do documento e o caminho analítico adotado.\n    A Síntese analítica deve produzir uma interpretação própria do corpus, articulando os textos sem repetir mecanicamente a comparação.\n    As Considerações finais devem consolidar os ganhos analíticos e as principais contribuições para a disciplina.\n\n    {common_rules}\n\n    Pesquisa/TOML:\n    {json.dumps(pesquisa, ensure_ascii=False, indent=2)}\n\n    Orientações do projeto:\n    {_orientation_context(orientations)}\n\n    Índice dos documentos do corpus:\n    {_docs_index_context(docs)}\n\n    Análises individuais já geradas:\n    {shorten_text(json.dumps(analyses, ensure_ascii=False, indent=2), 42000)}\n\n    Comparação já gerada:\n    {comparison}\n    ').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_builder:_build_resumo_artigos_document_model_staged`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4342a2454736eb84a52335af282120bbd31d58630f6b0b487a3beb2ffcf83c0d`
**LOC do arquivo-fonte:** 715


### `app_bundle.scripts.pipeline.document_builder:_build_resumo_artigos_document_model_staged`

**Assinatura:**
```python
def _build_resumo_artigos_document_model_staged(client: Any, model: str, cfg: dict[str, Any], docs: list[SourceDoc], orientations: list[SourceDoc], bib_keys: list[str], bib_path: Path, *, progress: Callable[[str], None] | None = None, checkpoint_dir: Path | None = None, prefix: str = 'documento') -> AcademicDocument
```

**Arquivo:** `app_bundle/scripts/pipeline/document_builder.py`
**Linhas:** 598–667
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Constrói a responsabilidade representada por `resumo_artigos_document_model_staged` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `client` | positional-or-keyword | `Any` | `obrigatório/ausente` | Cliente de serviço externo/IA utilizado pela rotina; pode ser opcional conforme o fluxo. |
| `model` | positional-or-keyword | `str` | `obrigatório/ausente` | Identificador do modelo utilizado pela etapa de IA. |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `docs` | positional-or-keyword | `list[SourceDoc]` | `obrigatório/ausente` | Coleção de documentos/entradas processados pela rotina. |
| `orientations` | positional-or-keyword | `list[SourceDoc]` | `obrigatório/ausente` | Parâmetro `orientations` recebido pela função; não há descrição individual explícita no código. |
| `bib_keys` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `bib_keys` recebido pela função; não há descrição individual explícita no código. |
| `bib_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `progress` | keyword-only | `Callable[[str], None] \| None` | `None` | Parâmetro `progress` recebido pela função; não há descrição individual explícita no código. |
| `checkpoint_dir` | keyword-only | `Path \| None` | `None` | Parâmetro `checkpoint_dir` recebido pela função; não há descrição individual explícita no código. |
| `prefix` | keyword-only | `str` | `'documento'` | Prefixo canônico usado para compor nomes de artefatos. |

#### Retorno

Anotação declarada: `AcademicDocument`. Retornos observados estaticamente incluem: `document`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `(cfg.get('bibliografia', {}) if isinstance(cfg.get('bibliografia'), dict) else {}).get`
- `AcademicDocument`
- `Block`
- `RuntimeError`
- `Section`
- `TextSpan`
- `_generate_article_section`
- `_generate_comparison_section`
- `_generate_intro_synthesis_bundle`
- `_meta_from_cfg`
- `_progress`
- `_write_checkpoint`
- `article_sections.append`
- `bool`
- `cfg.get`
- `enumerate`
- `id`
- `isinstance`
- `json.dumps`
- `len`
- `load_prompt_bundle`
- `meta_cfg.get`
- `prompt_bundle.report`
- `sections.append`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_builder:build_document_model`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('O perfil resumo_artigos_local_fgv exige ao menos um documento no corpus local.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 5
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4342a2454736eb84a52335af282120bbd31d58630f6b0b487a3beb2ffcf83c0d`
**LOC do arquivo-fonte:** 715


### `app_bundle.scripts.pipeline.document_builder:build_document_model`

**Assinatura:**
```python
def build_document_model(client: Any, model: str, cfg: dict[str, Any], docs: list[SourceDoc], orientations: list[SourceDoc], bib_keys: list[str], bib_path: Path, *, progress: Callable[[str], None] | None = None, checkpoint_dir: Path | None = None, prefix: str = 'documento') -> AcademicDocument
```

**Arquivo:** `app_bundle/scripts/pipeline/document_builder.py`
**Linhas:** 669–715
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Constrói a responsabilidade representada por `document_model` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `client` | positional-or-keyword | `Any` | `obrigatório/ausente` | Cliente de serviço externo/IA utilizado pela rotina; pode ser opcional conforme o fluxo. |
| `model` | positional-or-keyword | `str` | `obrigatório/ausente` | Identificador do modelo utilizado pela etapa de IA. |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `docs` | positional-or-keyword | `list[SourceDoc]` | `obrigatório/ausente` | Coleção de documentos/entradas processados pela rotina. |
| `orientations` | positional-or-keyword | `list[SourceDoc]` | `obrigatório/ausente` | Parâmetro `orientations` recebido pela função; não há descrição individual explícita no código. |
| `bib_keys` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `bib_keys` recebido pela função; não há descrição individual explícita no código. |
| `bib_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `progress` | keyword-only | `Callable[[str], None] \| None` | `None` | Parâmetro `progress` recebido pela função; não há descrição individual explícita no código. |
| `checkpoint_dir` | keyword-only | `Path \| None` | `None` | Parâmetro `checkpoint_dir` recebido pela função; não há descrição individual explícita no código. |
| `prefix` | keyword-only | `str` | `'documento'` | Prefixo canônico usado para compor nomes de artefatos. |

#### Retorno

Anotação declarada: `AcademicDocument`. Retornos observados estaticamente incluem: `_build_resumo_artigos_document_model_staged(client, model, cfg, docs, orientations, bib_keys, bib_path, progress=progress, checkpoint_dir=checkpoint_dir, prefix=prefix)`; `doc`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.
- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `(cfg.get('documento', {}) if isinstance(cfg.get('documento'), dict) else {}).get`
- `RuntimeError`
- `_build_resumo_artigos_document_model_staged`
- `_is_resumo_artigos_profile`
- `_meta_from_cfg`
- `bool`
- `build_prompt`
- `cfg.get`
- `client.responses.parse`
- `getattr`
- `isinstance`
- `json.dumps`
- `load_prompt_bundle`
- `meta_cfg.items`
- `prompt_bundle.report`
- `resumo_cfg.get`
- `set`
- `setattr`
- `sorted`
- `str`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.default_runtime:_ap003f_pipeline_core`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('A IA não retornou AcademicDocument estruturado.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4342a2454736eb84a52335af282120bbd31d58630f6b0b487a3beb2ffcf83c0d`
**LOC do arquivo-fonte:** 715


### `app_bundle.scripts.pipeline.document_model:Citation.validate_keys`

**Assinatura:**
```python
def validate_keys(cls, value: list[str]) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/document_model.py`
**Linhas:** 49–55
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `Citation`
**Decoradores:** `field_validator('keys')`, `classmethod`
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Valida a responsabilidade representada por `keys` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cls` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `cls` recebido pela função; não há descrição individual explícita no código. |
| `value` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `cleaned`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `cleaned.append`
- `str`
- `str(key).strip`
- `str(key).strip().lstrip`
- `str(key).strip().lstrip('@').strip`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3fdb6646ee253b131be83db8e02f33b71e838db8b0cf323fc63fda530d6db6ae`
**LOC do arquivo-fonte:** 192


### `app_bundle.scripts.pipeline.document_model:Block.normalize_block`

**Assinatura:**
```python
def normalize_block(self) -> 'Block'
```

**Arquivo:** `app_bundle/scripts/pipeline/document_model.py`
**Linhas:** 89–94
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `Block`
**Decoradores:** `model_validator(mode='after')`
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Normaliza a responsabilidade representada por `block` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `'Block'`. Retornos observados estaticamente incluem: `self`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `TextSpan`
- `int`
- `max`
- `min`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3fdb6646ee253b131be83db8e02f33b71e838db8b0cf323fc63fda530d6db6ae`
**LOC do arquivo-fonte:** 192


### `app_bundle.scripts.pipeline.document_model:Section.normalize_section`

**Assinatura:**
```python
def normalize_section(self) -> 'Section'
```

**Arquivo:** `app_bundle/scripts/pipeline/document_model.py`
**Linhas:** 104–108
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `Section`
**Decoradores:** `model_validator(mode='after')`
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Normaliza a responsabilidade representada por `section` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `'Section'`. Retornos observados estaticamente incluem: `self`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `int`
- `max`
- `min`
- `slugify`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3fdb6646ee253b131be83db8e02f33b71e838db8b0cf323fc63fda530d6db6ae`
**LOC do arquivo-fonte:** 192


### `app_bundle.scripts.pipeline.document_model:AcademicDocument.validate_document`

**Assinatura:**
```python
def validate_document(self) -> 'AcademicDocument'
```

**Arquivo:** `app_bundle/scripts/pipeline/document_model.py`
**Linhas:** 176–179
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `AcademicDocument`
**Decoradores:** `model_validator(mode='after')`
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Valida a responsabilidade representada por `document` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `'AcademicDocument'`. Retornos observados estaticamente incluem: `self`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `ValueError`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `ValueError('O documento canônico precisa conter ao menos uma seção.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3fdb6646ee253b131be83db8e02f33b71e838db8b0cf323fc63fda530d6db6ae`
**LOC do arquivo-fonte:** 192


### `app_bundle.scripts.pipeline.document_model:slugify`

**Assinatura:**
```python
def slugify(text: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/document_model.py`
**Linhas:** 182–192
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `slugify` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `text or 'secao'`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `re.sub`
- `re.sub('_+', '_', text).strip`
- `replacements.items`
- `str`
- `str(text or '').strip`
- `str(text or '').strip().lower`
- `text.replace`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3fdb6646ee253b131be83db8e02f33b71e838db8b0cf323fc63fda530d6db6ae`
**LOC do arquivo-fonte:** 192


### `app_bundle.scripts.pipeline.document_translation:_section`

**Assinatura:**
```python
def _section(cfg: dict[str, Any], name: str) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/document_translation.py`
**Linhas:** 65–67
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_section` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `name` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `name` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `value if isinstance(value, dict) else {}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `cfg.get`
- `isinstance`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `daa8dbd13cd2c0c410b680d8632b159fb988522ca3ee90be9b7f3c819ba0810c`
**LOC do arquivo-fonte:** 270


### `app_bundle.scripts.pipeline.document_translation:normalize_language`

**Assinatura:**
```python
def normalize_language(value: Any) -> tuple[str, str]
```

**Arquivo:** `app_bundle/scripts/pipeline/document_translation.py`
**Linhas:** 70–83
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Normaliza idioma para código estável de diretório e nome de exibição.

**Docstring resumida:** Normaliza idioma para código estável de diretório e nome de exibição.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `tuple[str, str]`. Retornos observados estaticamente incluem: `_LANGUAGE_ALIASES[key]`; `(key, raw)`; `(safe, raw)`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `TranslationError`
- `raw.casefold`
- `raw.casefold().replace`
- `re.fullmatch`
- `re.sub`
- `re.sub('[^a-z0-9]+', '-', key).strip`
- `str`
- `str(value or '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_translation:requested_translation_languages`
- `app_bundle.scripts.pipeline.document_translation:translate_document_model`
- `app_bundle.scripts.pipeline.paper_abstracts:_bundle_items`
- `app_bundle.scripts.pipeline.paper_abstracts:_request_abstracts`
- `app_bundle.scripts.pipeline.paper_abstracts:requested_abstract_languages`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `TranslationError('Idioma adicional vazio.')`
- `TranslationError(f'Idioma adicional inválido: {raw!r}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `daa8dbd13cd2c0c410b680d8632b159fb988522ca3ee90be9b7f3c819ba0810c`
**LOC do arquivo-fonte:** 270


### `app_bundle.scripts.pipeline.document_translation:requested_translation_languages`

**Assinatura:**
```python
def requested_translation_languages(cfg: dict[str, Any]) -> list[tuple[str, str]]
```

**Arquivo:** `app_bundle/scripts/pipeline/document_translation.py`
**Linhas:** 86–104
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Retorna idiomas adicionais únicos configurados no TOML.

**Docstring resumida:** Retorna idiomas adicionais únicos configurados no TOML.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `list[tuple[str, str]]`. Retornos observados estaticamente incluem: `[]`; `result`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_section`
- `bool`
- `isinstance`
- `normalize_language`
- `result.append`
- `section.get`
- `seen.add`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.paper_abstracts:requested_abstract_languages`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `daa8dbd13cd2c0c410b680d8632b159fb988522ca3ee90be9b7f3c819ba0810c`
**LOC do arquivo-fonte:** 270


### `app_bundle.scripts.pipeline.document_translation:translation_batch_size`

**Assinatura:**
```python
def translation_batch_size(cfg: dict[str, Any]) -> int
```

**Arquivo:** `app_bundle/scripts/pipeline/document_translation.py`
**Linhas:** 107–113
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `translation_batch_size` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `max(2500, min(value, 24000))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_section`
- `int`
- `max`
- `min`
- `section.get`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `daa8dbd13cd2c0c410b680d8632b159fb988522ca3ee90be9b7f3c819ba0810c`
**LOC do arquivo-fonte:** 270


### `app_bundle.scripts.pipeline.document_translation:_path_is_protected`

**Assinatura:**
```python
def _path_is_protected(path: tuple[str | int, ...]) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/document_translation.py`
**Linhas:** 116–121
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_path_is_protected` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `tuple[str \| int, ...]` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `True`; `leaf in _PROTECTED_LEAF_KEYS`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `isinstance`
- `str`
- `str(item).casefold`
- `str(path[-1]).casefold`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_translation:collect_translatable_strings.walk`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `daa8dbd13cd2c0c410b680d8632b159fb988522ca3ee90be9b7f3c819ba0810c`
**LOC do arquivo-fonte:** 270


### `app_bundle.scripts.pipeline.document_translation:_looks_like_prose`

**Assinatura:**
```python
def _looks_like_prose(value: str) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/document_translation.py`
**Linhas:** 124–132
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_looks_like_prose` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `False`; `True`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `len`
- `re.fullmatch`
- `re.search`
- `value.strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_translation:collect_translatable_strings.walk`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `daa8dbd13cd2c0c410b680d8632b159fb988522ca3ee90be9b7f3c819ba0810c`
**LOC do arquivo-fonte:** 270


### `app_bundle.scripts.pipeline.document_translation:collect_translatable_strings`

**Assinatura:**
```python
def collect_translatable_strings(payload: Any) -> list[tuple[tuple[str | int, ...], str]]
```

**Arquivo:** `app_bundle/scripts/pipeline/document_translation.py`
**Linhas:** 135–152
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Extrai folhas textuais do modelo, preservando campos técnicos.

**Docstring resumida:** Extrai folhas textuais do modelo, preservando campos técnicos.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `payload` | positional-or-keyword | `Any` | `obrigatório/ausente` | Payload estruturado recebido ou produzido pela operação. |

#### Retorno

Anotação declarada: `list[tuple[tuple[str | int, ...], str]]`. Retornos observados estaticamente incluem: `items`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `walk`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_translation:translate_document_model`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `daa8dbd13cd2c0c410b680d8632b159fb988522ca3ee90be9b7f3c819ba0810c`
**LOC do arquivo-fonte:** 270


### `app_bundle.scripts.pipeline.document_translation:collect_translatable_strings.walk`

**Assinatura:**
```python
def walk(value: Any, path: tuple[str | int, ...]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/document_translation.py`
**Linhas:** 139–149
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `walk` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |
| `path` | positional-or-keyword | `tuple[str \| int, ...]` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_looks_like_prose`
- `_path_is_protected`
- `enumerate`
- `isinstance`
- `items.append`
- `str`
- `value.items`
- `walk`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `daa8dbd13cd2c0c410b680d8632b159fb988522ca3ee90be9b7f3c819ba0810c`
**LOC do arquivo-fonte:** 270


### `app_bundle.scripts.pipeline.document_translation:_set_at_path`

**Assinatura:**
```python
def _set_at_path(payload: Any, path: tuple[str | int, ...], value: str) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/document_translation.py`
**Linhas:** 155–159
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_set_at_path` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `payload` | positional-or-keyword | `Any` | `obrigatório/ausente` | Payload estruturado recebido ou produzido pela operação. |
| `path` | positional-or-keyword | `tuple[str \| int, ...]` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `value` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

Nenhum identificado pela análise estática.

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_translation:translate_document_model`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `daa8dbd13cd2c0c410b680d8632b159fb988522ca3ee90be9b7f3c819ba0810c`
**LOC do arquivo-fonte:** 270


### `app_bundle.scripts.pipeline.document_translation:_chunk_items`

**Assinatura:**
```python
def _chunk_items(items: Iterable[tuple[tuple[str | int, ...], str]], max_chars: int) -> list[list[tuple[str, tuple[str | int, ...], str]]]
```

**Arquivo:** `app_bundle/scripts/pipeline/document_translation.py`
**Linhas:** 162–175
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_chunk_items` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `items` | positional-or-keyword | `Iterable[tuple[tuple[str \| int, ...], str]]` | `obrigatório/ausente` | Parâmetro `items` recebido pela função; não há descrição individual explícita no código. |
| `max_chars` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `max_chars` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[list[tuple[str, tuple[str | int, ...], str]]]`. Retornos observados estaticamente incluem: `chunks`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `chunks.append`
- `current.append`
- `enumerate`
- `len`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_translation:translate_document_model`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `daa8dbd13cd2c0c410b680d8632b159fb988522ca3ee90be9b7f3c819ba0810c`
**LOC do arquivo-fonte:** 270


### `app_bundle.scripts.pipeline.document_translation:_extract_response_content`

**Assinatura:**
```python
def _extract_response_content(response: Any) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/document_translation.py`
**Linhas:** 178–188
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Extrai a responsabilidade representada por `response_content` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `response` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `response` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `str(content or '').strip()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `''.join`
- `TranslationError`
- `getattr`
- `isinstance`
- `item.get`
- `str`
- `str(content or '').strip`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `TranslationError('A API de IA não devolveu conteúdo de tradução utilizável.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 1
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `daa8dbd13cd2c0c410b680d8632b159fb988522ca3ee90be9b7f3c819ba0810c`
**LOC do arquivo-fonte:** 270


### `app_bundle.scripts.pipeline.document_translation:_request_translation_batch`

**Assinatura:**
```python
def _request_translation_batch(client: Any, model: str, language_label: str, items: list[tuple[str, tuple[str | int, ...], str]]) -> dict[str, str]
```

**Arquivo:** `app_bundle/scripts/pipeline/document_translation.py`
**Linhas:** 191–234
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_request_translation_batch` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `client` | positional-or-keyword | `Any` | `obrigatório/ausente` | Cliente de serviço externo/IA utilizado pela rotina; pode ser opcional conforme o fluxo. |
| `model` | positional-or-keyword | `str` | `obrigatório/ausente` | Identificador do modelo utilizado pela etapa de IA. |
| `language_label` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `language_label` recebido pela função; não há descrição individual explícita no código. |
| `items` | positional-or-keyword | `list[tuple[str, tuple[str \| int, ...], str]]` | `obrigatório/ausente` | Parâmetro `items` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, str]`. Retornos observados estaticamente incluem: `{item_id: translated[item_id] for item_id in source}`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `', '.join`
- `TranslationError`
- `_extract_response_content`
- `client.chat.completions.create`
- `isinstance`
- `json.dumps`
- `json.loads`
- `parsed.get`
- `row.get`
- `source.items`
- `str`
- `str(row.get('id') or '').strip`
- `text.strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_translation:translate_document_model`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `TranslationError('A IA devolveu tradução fora do formato JSON esperado.')`
- `TranslationError('A IA não devolveu a lista translations esperada.')`
- `TranslationError('A IA não devolveu tradução para todos os campos: ' + ', '.join(missing[:8]))`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 1
comprehensions: 4
exception_handlers: 2
```

**SHA-256 do arquivo-fonte:** `daa8dbd13cd2c0c410b680d8632b159fb988522ca3ee90be9b7f3c819ba0810c`
**LOC do arquivo-fonte:** 270


### `app_bundle.scripts.pipeline.document_translation:translate_document_model`

**Assinatura:**
```python
def translate_document_model(client: Any, model: str, document: Any, target_language: str, *, max_chars: int = 12000) -> tuple[Any, dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/document_translation.py`
**Linhas:** 237–270
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Cria uma cópia traduzida do document model e um registro de auditoria.

**Docstring resumida:** Cria uma cópia traduzida do document model e um registro de auditoria.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `client` | positional-or-keyword | `Any` | `obrigatório/ausente` | Cliente de serviço externo/IA utilizado pela rotina; pode ser opcional conforme o fluxo. |
| `model` | positional-or-keyword | `str` | `obrigatório/ausente` | Identificador do modelo utilizado pela etapa de IA. |
| `document` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `document` recebido pela função; não há descrição individual explícita no código. |
| `target_language` | positional-or-keyword | `str` | `obrigatório/ausente` | Idioma solicitado para geração, tradução ou renderização. |
| `max_chars` | keyword-only | `int` | `12000` | Parâmetro `max_chars` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `tuple[Any, dict[str, Any]]`. Retornos observados estaticamente incluem: `(translated_document, audit)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `TranslationError`
- `_chunk_items`
- `_request_translation_batch`
- `_set_at_path`
- `collect_translatable_strings`
- `copy.deepcopy`
- `document.model_dump`
- `len`
- `model_class.model_validate`
- `normalize_language`
- `sum`
- `type`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `TranslationError('Nenhum campo textual elegível foi encontrado no document.json.')`
- `TranslationError('A tradução não passou na validação do document model.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `Nenhum campo textual elegível foi encontrado no document.json.`
- `tradução do document.json canônico; sem nova análise do corpus`

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 2
comprehensions: 1
exception_handlers: 2
```

**SHA-256 do arquivo-fonte:** `daa8dbd13cd2c0c410b680d8632b159fb988522ca3ee90be9b7f3c819ba0810c`
**LOC do arquivo-fonte:** 270


### `app_bundle.scripts.pipeline.document_validator:_technical_term_pattern`

**Assinatura:**
```python
def _technical_term_pattern(term: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/document_validator.py`
**Linhas:** 53–66
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Regex de termo técnico sobre texto já normalizado.

**Docstring resumida:** Regex de termo técnico sobre texto já normalizado.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `term` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `term` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'a^'`; `f'(?<![a-z0-9]){body}(?![a-z0-9])'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'\\s+'.join`
- `normalize_title_loose`
- `normalized.split`
- `re.escape`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_validator:find_technical_leaks_in_text`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e8fa0bc7f5f22a8adc48dc1946ae28791788d8ad114c394a0f91598453c0a2b6`
**LOC do arquivo-fonte:** 500


### `app_bundle.scripts.pipeline.document_validator:find_technical_leaks_in_text`

**Assinatura:**
```python
def find_technical_leaks_in_text(value: str) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/document_validator.py`
**Linhas:** 69–80
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Retorna termos técnicos realmente presentes no texto visível.

**Docstring resumida:** Retorna termos técnicos realmente presentes no texto visível.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `found`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_technical_term_pattern`
- `found.append`
- `normalize_title_loose`
- `re.search`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_validator:_find_technical_leak_locations_in_model.check`
- `app_bundle.scripts.pipeline.document_validator:validate_document_model`
- `app_bundle.scripts.pipeline.document_validator:validate_org_text`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e8fa0bc7f5f22a8adc48dc1946ae28791788d8ad114c394a0f91598453c0a2b6`
**LOC do arquivo-fonte:** 500


### `app_bundle.scripts.pipeline.document_validator:_clean_visible_text`

**Assinatura:**
```python
def _clean_visible_text(value: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/document_validator.py`
**Linhas:** 83–87
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Limpa a responsabilidade representada por `visible_text` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `text`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `re.sub`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_validator:sanitize_document_model_technical_leaks`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e8fa0bc7f5f22a8adc48dc1946ae28791788d8ad114c394a0f91598453c0a2b6`
**LOC do arquivo-fonte:** 500


### `app_bundle.scripts.pipeline.document_validator:_replace_raw_bibkeys_with_latex_cites`

**Assinatura:**
```python
def _replace_raw_bibkeys_with_latex_cites(value: str, bib_keys: list[str]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/document_validator.py`
**Linhas:** 90–116
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Converte chaves BibTeX cruas deixadas pela IA em comandos \parencite.

**Docstring resumida:** Converte chaves BibTeX cruas deixadas pela IA em comandos \parencite.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |
| `bib_keys` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `bib_keys` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `text`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `re.escape`
- `re.sub`
- `sorted`
- `str`
- `str(k).strip`
- `str(k).strip().lstrip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_validator:sanitize_document_model_raw_bibkeys.clean`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e8fa0bc7f5f22a8adc48dc1946ae28791788d8ad114c394a0f91598453c0a2b6`
**LOC do arquivo-fonte:** 500


### `app_bundle.scripts.pipeline.document_validator:sanitize_document_model_raw_bibkeys`

**Assinatura:**
```python
def sanitize_document_model_raw_bibkeys(doc: AcademicDocument, bib_keys: list[str]) -> tuple[AcademicDocument, list[str]]
```

**Arquivo:** `app_bundle/scripts/pipeline/document_validator.py`
**Linhas:** 119–166
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Saneia chaves BibTeX cruas no conteúdo visível do document_model.

**Docstring resumida:** Saneia chaves BibTeX cruas no conteúdo visível do document_model.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doc` | positional-or-keyword | `AcademicDocument` | `obrigatório/ausente` | Documento/registro individual processado pela rotina. |
| `bib_keys` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `bib_keys` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `tuple[AcademicDocument, list[str]]`. Retornos observados estaticamente incluem: `(doc, unique)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `clean`
- `enumerate`
- `getattr`
- `isinstance`
- `seen.add`
- `set`
- `setattr`
- `unique.append`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.default_runtime:_ap003f_pipeline_core`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `abstract.texto`
- `].text`

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 6
comprehensions: 5
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e8fa0bc7f5f22a8adc48dc1946ae28791788d8ad114c394a0f91598453c0a2b6`
**LOC do arquivo-fonte:** 500


### `app_bundle.scripts.pipeline.document_validator:sanitize_document_model_raw_bibkeys.clean`

**Assinatura:**
```python
def clean(label: str, value: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/document_validator.py`
**Linhas:** 123–127
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Limpa a responsabilidade representada por `clean` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `label` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `label` recebido pela função; não há descrição individual explícita no código. |
| `value` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `new`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `_replace_raw_bibkeys_with_latex_cites`
- `changed.append`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e8fa0bc7f5f22a8adc48dc1946ae28791788d8ad114c394a0f91598453c0a2b6`
**LOC do arquivo-fonte:** 500


### `app_bundle.scripts.pipeline.document_validator:sanitize_document_model_technical_leaks`

**Assinatura:**
```python
def sanitize_document_model_technical_leaks(doc: AcademicDocument) -> tuple[AcademicDocument, list[str]]
```

**Arquivo:** `app_bundle/scripts/pipeline/document_validator.py`
**Linhas:** 169–260
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Remove linguagem operacional do conteúdo visível do documento canônico.

**Docstring resumida:** Remove linguagem operacional do conteúdo visível do documento canônico.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doc` | positional-or-keyword | `AcademicDocument` | `obrigatório/ausente` | Documento/registro individual processado pela rotina. |

#### Retorno

Anotação declarada: `tuple[AcademicDocument, list[str]]`. Retornos observados estaticamente incluem: `(doc, changed)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_clean_visible_text`
- `changed.append`
- `cleaned_kw.append`
- `enumerate`
- `getattr`
- `isinstance`
- `setattr`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.default_runtime:_ap003f_pipeline_core`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `abstract.texto`
- `].text`

#### Complexidade estrutural observada

```yaml
branches: 18
loops: 7
comprehensions: 4
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e8fa0bc7f5f22a8adc48dc1946ae28791788d8ad114c394a0f91598453c0a2b6`
**LOC do arquivo-fonte:** 500


### `app_bundle.scripts.pipeline.document_validator:_visible_strings_from_block`

**Assinatura:**
```python
def _visible_strings_from_block(block: Block) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/document_validator.py`
**Linhas:** 263–282
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_visible_strings_from_block` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `block` | positional-or-keyword | `Block` | `obrigatório/ausente` | Parâmetro `block` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `[v for v in values if str(v).strip()]`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `getattr`
- `isinstance`
- `str`
- `str(v).strip`
- `values.append`
- `values.extend`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_validator:visible_text_from_document_model`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 3
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e8fa0bc7f5f22a8adc48dc1946ae28791788d8ad114c394a0f91598453c0a2b6`
**LOC do arquivo-fonte:** 500


### `app_bundle.scripts.pipeline.document_validator:visible_text_from_document_model`

**Assinatura:**
```python
def visible_text_from_document_model(doc: AcademicDocument) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/document_validator.py`
**Linhas:** 285–306
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Texto acadêmico visível, excluindo diagnostics e paths internos.

**Docstring resumida:** Texto acadêmico visível, excluindo diagnostics e paths internos.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doc` | positional-or-keyword | `AcademicDocument` | `obrigatório/ausente` | Documento/registro individual processado pela rotina. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'\n'.join((v for v in values if str(v).strip()))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `_visible_strings_from_block`
- `getattr`
- `str`
- `str(v).strip`
- `values.append`
- `values.extend`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_validator:validate_document_model`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 4
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e8fa0bc7f5f22a8adc48dc1946ae28791788d8ad114c394a0f91598453c0a2b6`
**LOC do arquivo-fonte:** 500


### `app_bundle.scripts.pipeline.document_validator:cited_keys_from_document_model`

**Assinatura:**
```python
def cited_keys_from_document_model(doc: AcademicDocument) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/document_validator.py`
**Linhas:** 309–319
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `cited_keys_from_document_model` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doc` | positional-or-keyword | `AcademicDocument` | `obrigatório/ausente` | Documento/registro individual processado pela rotina. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `keys`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `extract_cited_keys_from_model_inline`
- `keys.append`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_validator:validate_document_model`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 4
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e8fa0bc7f5f22a8adc48dc1946ae28791788d8ad114c394a0f91598453c0a2b6`
**LOC do arquivo-fonte:** 500


### `app_bundle.scripts.pipeline.document_validator:_find_technical_leak_locations_in_model`

**Assinatura:**
```python
def _find_technical_leak_locations_in_model(doc: AcademicDocument) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/document_validator.py`
**Linhas:** 322–371
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Retorna locais aproximados de vazamento no conteúdo visível.

**Docstring resumida:** Retorna locais aproximados de vazamento no conteúdo visível.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doc` | positional-or-keyword | `AcademicDocument` | `obrigatório/ausente` | Documento/registro individual processado pela rotina. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `locations`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `check`
- `enumerate`
- `getattr`
- `isinstance`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_validator:validate_document_model`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `abstract.texto`
- `].text`

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 10
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e8fa0bc7f5f22a8adc48dc1946ae28791788d8ad114c394a0f91598453c0a2b6`
**LOC do arquivo-fonte:** 500


### `app_bundle.scripts.pipeline.document_validator:_find_technical_leak_locations_in_model.check`

**Assinatura:**
```python
def check(label: str, value: str) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/document_validator.py`
**Linhas:** 329–333
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Verifica a responsabilidade representada por `check` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `label` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `label` recebido pela função; não há descrição individual explícita no código. |
| `value` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `', '.join`
- `find_technical_leaks_in_text`
- `locations.append`
- `normalize_title_loose`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_validator:_find_technical_leak_locations_in_model`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e8fa0bc7f5f22a8adc48dc1946ae28791788d8ad114c394a0f91598453c0a2b6`
**LOC do arquivo-fonte:** 500


### `app_bundle.scripts.pipeline.document_validator:validate_document_model`

**Assinatura:**
```python
def validate_document_model(doc: AcademicDocument, bib_keys: list[str], *, strict: bool = True) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/document_validator.py`
**Linhas:** 374–396
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Valida a responsabilidade representada por `document_model` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doc` | positional-or-keyword | `AcademicDocument` | `obrigatório/ausente` | Documento/registro individual processado pela rotina. |
| `bib_keys` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `bib_keys` recebido pela função; não há descrição individual explícita no código. |
| `strict` | keyword-only | `bool` | `True` | Parâmetro `strict` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `errors`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `', '.join`
- `'; '.join`
- `_find_technical_leak_locations_in_model`
- `cited_keys_from_document_model`
- `errors.append`
- `find_technical_leaks_in_text`
- `normalize_title_loose`
- `set`
- `str`
- `str(doc.metadata.tipo_documento).lower`
- `visible_text_from_document_model`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.default_runtime:_ap003f_pipeline_core`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `Chaves citadas ausentes no .bib: `

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e8fa0bc7f5f22a8adc48dc1946ae28791788d8ad114c394a0f91598453c0a2b6`
**LOC do arquivo-fonte:** 500


### `app_bundle.scripts.pipeline.document_validator:_strip_nonvisible_org_regions`

**Assinatura:**
```python
def _strip_nonvisible_org_regions(org_text: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/document_validator.py`
**Linhas:** 399–421
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Remove regiões não visíveis antes da validação de vazamento técnico.

**Docstring resumida:** Remove regiões não visíveis antes da validação de vazamento técnico.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `org_text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `text`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `line.lstrip`
- `line.lstrip().startswith`
- `re.sub`
- `text.splitlines`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e8fa0bc7f5f22a8adc48dc1946ae28791788d8ad114c394a0f91598453c0a2b6`
**LOC do arquivo-fonte:** 500


### `app_bundle.scripts.pipeline.document_validator:validate_org_text`

**Assinatura:**
```python
def validate_org_text(org_text: str, bib_keys: list[str]) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/document_validator.py`
**Linhas:** 424–495
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Valida a responsabilidade representada por `org_text` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `org_text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |
| `bib_keys` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `bib_keys` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `errors`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `' ; '.join`
- `', '.join`
- `(numeric_scan_text or '').splitlines`
- `(org_text or '').splitlines`
- `_strip_nonvisible_org_regions`
- `cm.group`
- `details.append`
- `enumerate`
- `errors.append`
- `extract_latex_cited_keys`
- `find_technical_leaks_in_text`
- `len`
- `line.strip`
- `normalize_title_loose`
- `numeric_citation_lines.append`
- `pm.group`
- `pm.group(1).strip`
- `re.escape`
- `re.search`
- `re.sub`
- `set`
- `visible_line.strip`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `ORG cita chaves ausentes no .bib: `

#### Complexidade estrutural observada

```yaml
branches: 13
loops: 2
comprehensions: 3
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e8fa0bc7f5f22a8adc48dc1946ae28791788d8ad114c394a0f91598453c0a2b6`
**LOC do arquivo-fonte:** 500


### `app_bundle.scripts.pipeline.document_validator:raise_if_errors`

**Assinatura:**
```python
def raise_if_errors(errors: list[str], title: str = 'Validação falhou') -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/document_validator.py`
**Linhas:** 498–500
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `raise_if_errors` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `errors` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `errors` recebido pela função; não há descrição individual explícita no código. |
| `title` | positional-or-keyword | `str` | `'Validação falhou'` | Parâmetro `title` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'\n- '.join`
- `RuntimeError`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.default_runtime:_ap003f_pipeline_core`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError(title + ':\n- ' + '\n- '.join(errors))`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e8fa0bc7f5f22a8adc48dc1946ae28791788d8ad114c394a0f91598453c0a2b6`
**LOC do arquivo-fonte:** 500


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:now_stamp`

**Assinatura:**
```python
def now_stamp() -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 30–31
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `now_stamp` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `datetime.now().strftime('%Y%m%d_%H%M%S')`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `datetime.now`
- `datetime.now().strftime`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:backup`

**Assinatura:**
```python
def backup(path: Path, tag: str, quiet: bool = False) -> Path | None
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 34–41
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `backup` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `tag` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `tag` recebido pela função; não há descrição individual explícita no código. |
| `quiet` | positional-or-keyword | `bool` | `False` | Parâmetro `quiet` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path | None`. Retornos observados estaticamente incluem: `None`; `bak`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `now_stamp`
- `path.exists`
- `path.with_name`
- `print`
- `shutil.copy2`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:run`

**Assinatura:**
```python
def run(cmd: list[str], cwd: Path | None = None, env: dict[str, str] | None = None, quiet: bool = False) -> tuple[int, str]
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 44–57
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `run` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cmd` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `cmd` recebido pela função; não há descrição individual explícita no código. |
| `cwd` | positional-or-keyword | `Path \| None` | `None` | Parâmetro `cwd` recebido pela função; não há descrição individual explícita no código. |
| `env` | positional-or-keyword | `dict[str, str] \| None` | `None` | Parâmetro `env` recebido pela função; não há descrição individual explícita no código. |
| `quiet` | positional-or-keyword | `bool` | `False` | Parâmetro `quiet` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `tuple[int, str]`. Retornos observados estaticamente incluem: `(p.returncode, out)`.

#### Efeitos colaterais e fronteiras externas

- Executa processo ou ferramenta externa.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `(p.stdout or b'').decode`
- `print`
- `str`
- `subprocess.run`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:call_module_main`

**Assinatura:**
```python
def call_module_main(module_name: str, argv: list[str], quiet: bool = False) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 60–81
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `call_module_main` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `module_name` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `module_name` recebido pela função; não há descrição individual explícita no código. |
| `argv` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |
| `quiet` | positional-or-keyword | `bool` | `False` | Parâmetro `quiet` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `captured`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `RuntimeError`
- `buf.getvalue`
- `contextlib.nullcontext`
- `contextlib.redirect_stderr`
- `contextlib.redirect_stdout`
- `hasattr`
- `importlib.import_module`
- `int`
- `io.StringIO`
- `mod.main`
- `module_name.rsplit`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError(f'O módulo {module_name} não possui função main().')`
- `RuntimeError(f'Falha ao executar {module_name} com código {code}.\n{captured[-4000:]}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:ensure_cfg`

**Assinatura:**
```python
def ensure_cfg(cfg: Path, template_cfg: Path | None, quiet: bool = False) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 84–94
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Garante a responsabilidade representada por `cfg` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `Path` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `template_cfg` | positional-or-keyword | `Path \| None` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `quiet` | positional-or-keyword | `bool` | `False` | Parâmetro `quiet` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `SystemExit`
- `cfg.exists`
- `cfg.parent.mkdir`
- `print`
- `shutil.copy2`
- `template_cfg.exists`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `SystemExit(f'ERRO: TOML não encontrado: {cfg}\nInforme --template-cfg para criar o TOML a partir de um modelo.')`
- `SystemExit(f'ERRO: template TOML não encontrado: {template_cfg}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:set_toml_value_if_present`

**Assinatura:**
```python
def set_toml_value_if_present(txt: str, key: str, value: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 97–101
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `set_toml_value_if_present` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `txt` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `txt` recebido pela função; não há descrição individual explícita no código. |
| `key` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `key` recebido pela função; não há descrição individual explícita no código. |
| `value` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `txt`; `pat.sub(lambda m: m.group(1) + value, txt)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `m.group`
- `pat.search`
- `pat.sub`
- `re.compile`
- `re.escape`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:prepare_cfg`

**Assinatura:**
```python
def prepare_cfg(cfg: Path, target_n: int, min_palavras: int, quiet: bool = False) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 104–119
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `prepare_cfg` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `Path` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `target_n` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `target_n` recebido pela função; não há descrição individual explícita no código. |
| `min_palavras` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `min_palavras` recebido pela função; não há descrição individual explícita no código. |
| `quiet` | positional-or-keyword | `bool` | `False` | Parâmetro `quiet` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `backup`
- `cfg.read_text`
- `cfg.write_text`
- `print`
- `set_toml_value_if_present`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:parse_bib_keys`

**Assinatura:**
```python
def parse_bib_keys(bib: Path) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 122–126
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Interpreta a responsabilidade representada por `bib_keys` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `bib` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `bib` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `[]`; `re.findall('@\\w+\\s*\\{\\s*([^,\\s]+)', txt)`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `bib.exists`
- `bib.read_text`
- `re.findall`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:sanitize_bib`

**Assinatura:**
```python
def sanitize_bib(bib: Path, quiet: bool = False) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 129–146
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Saneia a responsabilidade representada por `bib` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `bib` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `bib` recebido pela função; não há descrição individual explícita no código. |
| `quiet` | positional-or-keyword | `bool` | `False` | Parâmetro `quiet` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `SystemExit`
- `backup`
- `bib.exists`
- `bib.read_text`
- `bib.write_text`
- `pat.sub`
- `print`
- `re.compile`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `SystemExit(f'ERRO: BIB não encontrado: {bib}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:convert_literal_citations`

**Assinatura:**
```python
def convert_literal_citations(txt: str, keys: list[str]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 149–168
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Converte a responsabilidade representada por `literal_citations` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `txt` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `txt` recebido pela função; não há descrição individual explícita no código. |
| `keys` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `keys` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `txt`; `re.sub('(?<![A-Za-z0-9])\\[([0-9]+(?:\\s*,\\s*[0-9]+)*)\\](?![A-Za-z0-9])', repl_num, txt)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `re.sub`
- `set`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:convert_literal_citations.repl_key`

**Assinatura:**
```python
def repl_key(m: re.Match[str]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 153–155
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `repl_key` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `m` | positional-or-keyword | `re.Match[str]` | `obrigatório/ausente` | Parâmetro `m` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `f'\\parencite{{{key}}}' if key in keyset else m.group(0)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `m.group`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:convert_literal_citations.repl_num`

**Assinatura:**
```python
def repl_num(m: re.Match[str]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 157–167
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `repl_num` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `m` | positional-or-keyword | `re.Match[str]` | `obrigatório/ausente` | Parâmetro `m` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `m.group(0)`; `f"\\parencite{{{','.join((keys[n - 1] for n in nums))}}}"`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `','.join`
- `all`
- `int`
- `len`
- `m.group`
- `nums.append`
- `piece.isdigit`
- `piece.strip`
- `raw.split`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:generic_text_fixes`

**Assinatura:**
```python
def generic_text_fixes(txt: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 171–188
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `generic_text_fixes` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `txt` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `txt` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `txt`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `re.sub`
- `txt.replace`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:remove_org_bib_headers`

**Assinatura:**
```python
def remove_org_bib_headers(txt: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 191–197
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Remove a responsabilidade representada por `org_bib_headers` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `txt` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `txt` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `txt`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `re.sub`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:insert_org_bib_headers`

**Assinatura:**
```python
def insert_org_bib_headers(txt: str, bib_name: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 200–212
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `insert_org_bib_headers` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `txt` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `txt` recebido pela função; não há descrição individual explícita no código. |
| `bib_name` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `bib_name` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'\n'.join(lines).rstrip() + '\n'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `'\n'.join(lines).rstrip`
- `enumerate`
- `line.startswith`
- `lines.insert`
- `reversed`
- `txt.splitlines`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:ensure_org_references`

**Assinatura:**
```python
def ensure_org_references(txt: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 215–220
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Garante a responsabilidade representada por `org_references` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `txt` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `txt` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `txt`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `re.sub`
- `txt.rstrip`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:patch_org`

**Assinatura:**
```python
def patch_org(org: Path, bib_name: str, keys: list[str], quiet: bool = False) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 223–236
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `patch_org` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `org` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `org` recebido pela função; não há descrição individual explícita no código. |
| `bib_name` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `bib_name` recebido pela função; não há descrição individual explícita no código. |
| `keys` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `keys` recebido pela função; não há descrição individual explícita no código. |
| `quiet` | positional-or-keyword | `bool` | `False` | Parâmetro `quiet` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `SystemExit`
- `backup`
- `convert_literal_citations`
- `ensure_org_references`
- `generic_text_fixes`
- `insert_org_bib_headers`
- `org.exists`
- `org.read_text`
- `org.write_text`
- `print`
- `remove_org_bib_headers`
- `txt.replace`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `SystemExit(f'ERRO: ORG não encontrado: {org}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:_lisp_string`

**Assinatura:**
```python
def _lisp_string(value: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 239–240
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_lisp_string` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `json.dumps(value, ensure_ascii=False)`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `json.dumps`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:export_org_to_tex`

**Assinatura:**
```python
def export_org_to_tex(org: Path, out: Path, project_root: Path, quiet: bool = False) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 243–289
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Exporta a responsabilidade representada por `org_to_tex` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `org` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `org` recebido pela função; não há descrição individual explícita no código. |
| `out` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `out` recebido pela função; não há descrição individual explícita no código. |
| `project_root` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `project_root` recebido pela função; não há descrição individual explícita no código. |
| `quiet` | positional-or-keyword | `bool` | `False` | Parâmetro `quiet` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `Path`
- `SystemExit`
- `_lisp_string`
- `fh.write`
- `org.exists`
- `run`
- `script_path.unlink`
- `shutil.which`
- `str`
- `tempfile.NamedTemporaryFile`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `SystemExit(f'ERRO: ORG não encontrado: {org}')`
- `SystemExit('ERRO: emacs não encontrado no PATH.')`
- `SystemExit(f'ERRO: falha no export Org -> TeX.\n{output[-4000:]}')`

#### Opções CLI mencionadas diretamente no corpo

- `--batch`

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:remove_tex_bib_config`

**Assinatura:**
```python
def remove_tex_bib_config(tex: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 292–299
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Remove a responsabilidade representada por `tex_bib_config` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `tex` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `tex` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `tex`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `re.sub`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:insert_tex_bib_config`

**Assinatura:**
```python
def insert_tex_bib_config(tex: str, bib_name: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 302–306
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `insert_tex_bib_config` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `tex` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `tex` recebido pela função; não há descrição individual explícita no código. |
| `bib_name` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `bib_name` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `tex.replace('\\begin{document}', insert + '\\begin{document}', 1)`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `SystemExit`
- `tex.replace`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `SystemExit('ERRO: não encontrei \\begin{document} no TEX.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:ensure_tex_references`

**Assinatura:**
```python
def ensure_tex_references(tex: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 309–314
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Garante a responsabilidade representada por `tex_references` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `tex` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `tex` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `tex`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `re.sub`
- `tex.replace`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:patch_tex`

**Assinatura:**
```python
def patch_tex(tex_path: Path, bib_name: str, keys: list[str], quiet: bool = False) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 317–330
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `patch_tex` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `tex_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `bib_name` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `bib_name` recebido pela função; não há descrição individual explícita no código. |
| `keys` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `keys` recebido pela função; não há descrição individual explícita no código. |
| `quiet` | positional-or-keyword | `bool` | `False` | Parâmetro `quiet` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `SystemExit`
- `backup`
- `convert_literal_citations`
- `ensure_tex_references`
- `generic_text_fixes`
- `insert_tex_bib_config`
- `print`
- `remove_tex_bib_config`
- `tex.replace`
- `tex_path.exists`
- `tex_path.read_text`
- `tex_path.write_text`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `SystemExit(f'ERRO: TEX não encontrado: {tex_path}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:compile_pdf`

**Assinatura:**
```python
def compile_pdf(out: Path, prefix: str, project_root: Path, quiet: bool = False) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 333–357
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Compila a responsabilidade representada por `pdf` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `out` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `out` recebido pela função; não há descrição individual explícita no código. |
| `prefix` | positional-or-keyword | `str` | `obrigatório/ausente` | Prefixo canônico usado para compor nomes de artefatos. |
| `project_root` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `project_root` recebido pela função; não há descrição individual explícita no código. |
| `quiet` | positional-or-keyword | `bool` | `False` | Parâmetro `quiet` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `pdf`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Lê configuração/credenciais do ambiente do processo.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `':'.join`
- `SystemExit`
- `env.get`
- `os.environ.copy`
- `p.exists`
- `p.unlink`
- `pdf.exists`
- `run`
- `str`
- `tex.exists`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `SystemExit(f'ERRO: TEX não encontrado: {tex}')`
- `SystemExit(f"ERRO: falha em {' '.join(cmd)}\n{output[-4000:]}")`
- `SystemExit(f'ERRO: PDF não foi gerado: {pdf}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.tex`
- `.pdf`

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:validate_pdf`

**Assinatura:**
```python
def validate_pdf(pdf: Path, quiet: bool = False) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 360–385
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Valida a responsabilidade representada por `pdf` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `pdf` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `pdf` recebido pela função; não há descrição individual explícita no código. |
| `quiet` | positional-or-keyword | `bool` | `False` | Parâmetro `quiet` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `SystemExit`
- `any`
- `enumerate`
- `hits.append`
- `pdf.exists`
- `print`
- `re.search`
- `run`
- `shutil.which`
- `str`
- `txt.splitlines`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `SystemExit(f'ERRO: PDF não encontrado para validação: {pdf}')`
- `SystemExit('ERRO: pdftotext falhou na validação.')`
- `SystemExit('ERRO: validação encontrou problemas no PDF final:\n' + '\n'.join(hits[:40]))`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 7
loops: 1
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:_strip_braces_quotes`

**Assinatura:**
```python
def _strip_braces_quotes(value: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 389–400
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Remove a responsabilidade representada por `braces_quotes` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `value`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `len`
- `re.sub`
- `re.sub('\\s+', ' ', value).strip`
- `value.replace`
- `value.replace('{', '').replace`
- `value.strip`
- `value.strip().rstrip`
- `value.strip().rstrip(',').strip`
- `value[1:-1].strip`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:parse_bib_entries_for_docx`

**Assinatura:**
```python
def parse_bib_entries_for_docx(bib: Path) -> dict[str, dict[str, str]]
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 403–433
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Extrai metadados suficientes para materializar citações e referências no DOCX.

**Docstring resumida:** Extrai metadados suficientes para materializar citações e referências no DOCX.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `bib` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `bib` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, dict[str, str]]`. Retornos observados estaticamente incluem: `{}`; `entries`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `_strip_braces_quotes`
- `bib.exists`
- `bib.read_text`
- `field_pat.finditer`
- `fields.get`
- `fm.group`
- `fm.group(1).lower`
- `len`
- `m.end`
- `m.group`
- `m.group(2).strip`
- `max`
- `re.compile`
- `re.finditer`
- `re.search`
- `ym.group`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 3
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:_split_authors`

**Assinatura:**
```python
def _split_authors(author_field: str) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 436–446
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_split_authors` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `author_field` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `author_field` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `[]`; `[p.strip() for p in parts if p.strip()]`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_strip_braces_quotes`
- `author_field.split`
- `p.strip`
- `re.split`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:_author_surname`

**Assinatura:**
```python
def _author_surname(author: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 449–461
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_author_surname` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `author` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `author` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `''`; `author.split(',', 1)[0].strip()`; `tokens[0]`; `author.title()`; `tokens[-1]`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_strip_braces_quotes`
- `author.split`
- `author.split(',', 1)[0].strip`
- `author.title`
- `author.upper`
- `len`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:_citation_authors`

**Assinatura:**
```python
def _citation_authors(fields: dict[str, str], textual: bool = False) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 464–476
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_citation_authors` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `fields` | positional-or-keyword | `dict[str, str]` | `obrigatório/ausente` | Parâmetro `fields` recebido pela função; não há descrição individual explícita no código. |
| `textual` | positional-or-keyword | `bool` | `False` | Parâmetro `textual` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `title.split(':', 1)[0][:60]`; `authors[0]`; `f'{authors[0]} e {authors[1]}' if textual else f'{authors[0]}; {authors[1]}'`; `f'{authors[0]}, {authors[1]} e {authors[2]}' if textual else f'{authors[0]}; {authors[1]}; {authors[2]}'`; `f'{authors[0]} et al.'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_author_surname`
- `_split_authors`
- `fields.get`
- `len`
- `title.split`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 0
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:_cite_text_for_key`

**Assinatura:**
```python
def _cite_text_for_key(key: str, entries: dict[str, dict[str, str]], textual: bool = False) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 479–483
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_cite_text_for_key` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `key` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `key` recebido pela função; não há descrição individual explícita no código. |
| `entries` | positional-or-keyword | `dict[str, dict[str, str]]` | `obrigatório/ausente` | Coleção de entradas bibliográficas/registros. |
| `textual` | positional-or-keyword | `bool` | `False` | Parâmetro `textual` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `f'{authors} ({year})' if textual else f'{authors}, {year}'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_citation_authors`
- `entries.get`
- `fields.get`
- `key.strip`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:materialize_citations_for_docx`

**Assinatura:**
```python
def materialize_citations_for_docx(txt: str, entries: dict[str, dict[str, str]]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 486–498
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `materialize_citations_for_docx` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `txt` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `txt` recebido pela função; não há descrição individual explícita no código. |
| `entries` | positional-or-keyword | `dict[str, dict[str, str]]` | `obrigatório/ausente` | Coleção de entradas bibliográficas/registros. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `txt`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `re.sub`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:materialize_citations_for_docx.repl_textcite`

**Assinatura:**
```python
def repl_textcite(m: re.Match[str]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 487–489
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `repl_textcite` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `m` | positional-or-keyword | `re.Match[str]` | `obrigatório/ausente` | Parâmetro `m` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'; '.join((_cite_text_for_key(k, entries, textual=True) for k in keys))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'; '.join`
- `_cite_text_for_key`
- `k.strip`
- `m.group`
- `m.group(1).split`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:materialize_citations_for_docx.repl_parencite`

**Assinatura:**
```python
def repl_parencite(m: re.Match[str]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 491–493
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `repl_parencite` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `m` | positional-or-keyword | `re.Match[str]` | `obrigatório/ausente` | Parâmetro `m` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'(' + '; '.join((_cite_text_for_key(k, entries, textual=False) for k in keys)) + ')'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'; '.join`
- `_cite_text_for_key`
- `k.strip`
- `m.group`
- `m.group(1).split`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:_reference_line`

**Assinatura:**
```python
def _reference_line(fields: dict[str, str]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 501–520
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_reference_line` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `fields` | positional-or-keyword | `dict[str, str]` | `obrigatório/ausente` | Parâmetro `fields` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `' '.join(parts)`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `_strip_braces_quotes`
- `authors.upper`
- `fields.get`
- `fields.get('ID', 'REFERÊNCIA').upper`
- `fields.get('author', '').replace`
- `parts.append`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:render_references_for_docx`

**Assinatura:**
```python
def render_references_for_docx(entries: dict[str, dict[str, str]]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 523–527
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Renderiza a responsabilidade representada por `references_for_docx` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `entries` | positional-or-keyword | `dict[str, dict[str, str]]` | `obrigatório/ausente` | Coleção de entradas bibliográficas/registros. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `''`; `'\n\n'.join(lines)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'\n\n'.join`
- `_reference_line`
- `entries.items`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:make_docx_source_org`

**Assinatura:**
```python
def make_docx_source_org(org: Path, bib: Path, out: Path, prefix: str) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 530–559
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Cria a responsabilidade representada por `docx_source_org` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `org` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `org` recebido pela função; não há descrição individual explícita no código. |
| `bib` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `bib` recebido pela função; não há descrição individual explícita no código. |
| `out` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `out` recebido pela função; não há descrição individual explícita no código. |
| `prefix` | positional-or-keyword | `str` | `obrigatório/ausente` | Prefixo canônico usado para compor nomes de artefatos. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `tmp`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `cleaned_lines.append`
- `generic_text_fixes`
- `line.strip`
- `materialize_citations_for_docx`
- `org.read_text`
- `parse_bib_entries_for_docx`
- `re.match`
- `re.sub`
- `re.subn`
- `render_references_for_docx`
- `tmp.write_text`
- `txt.replace`
- `txt.replace('\\%', '%').replace`
- `txt.rstrip`
- `txt.splitlines`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.docx_source.org`

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:ensure_reference_docx`

**Assinatura:**
```python
def ensure_reference_docx(reference_docx: Path, quiet: bool = False) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 562–648
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Garante a responsabilidade representada por `reference_docx` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `reference_docx` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `reference_docx` recebido pela função; não há descrição individual explícita no código. |
| `quiet` | positional-or-keyword | `bool` | `False` | Parâmetro `quiet` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `reference_docx`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `Cm`
- `Document`
- `Pt`
- `SystemExit`
- `doc.add_paragraph`
- `doc.save`
- `name.startswith`
- `print`
- `reference_docx.exists`
- `reference_docx.parent.mkdir`
- `set_style_font`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `SystemExit(f'ERRO: python-docx não está disponível para criar o reference DOCX.\nInstale no ambiente do projeto com: pipenv install python-docx\nDetalhe: {exc}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 2
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:ensure_reference_docx.set_style_font`

**Assinatura:**
```python
def set_style_font(style, name: str = 'Times New Roman', size_pt: int = 12, bold: bool | None = None) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 578–589
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `set_style_font` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `style` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `style` recebido pela função; não há descrição individual explícita no código. |
| `name` | positional-or-keyword | `str` | `'Times New Roman'` | Parâmetro `name` recebido pela função; não há descrição individual explícita no código. |
| `size_pt` | positional-or-keyword | `int` | `12` | Parâmetro `size_pt` recebido pela função; não há descrição individual explícita no código. |
| `bold` | positional-or-keyword | `bool \| None` | `None` | Parâmetro `bold` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `OxmlElement`
- `Pt`
- `qn`
- `rfonts.set`
- `rpr.append`
- `style._element.get_or_add_rPr`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:validate_docx`

**Assinatura:**
```python
def validate_docx(docx_path: Path) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 651–660
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Valida a responsabilidade representada por `docx` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `docx_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `SystemExit`
- `docx_path.exists`
- `docx_path.stat`
- `set`
- `zf.namelist`
- `zipfile.ZipFile`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `SystemExit(f'ERRO: DOCX não foi gerado corretamente: {docx_path}')`
- `SystemExit(f'ERRO: DOCX inválido, word/document.xml ausente: {docx_path}')`
- `SystemExit(f'ERRO: arquivo DOCX inválido: {docx_path}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:emit_docx_stage`

**Assinatura:**
```python
def emit_docx_stage(art: Path, prefix: str, project_root: Path, reference_docx: Path | None = None, quiet: bool = False) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 663–672
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Gera DOCX canônico por python-docx, usando ORG/document.json/BIB finais.

**Docstring resumida:** Gera DOCX canônico por python-docx, usando ORG/document.json/BIB finais.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `art` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `art` recebido pela função; não há descrição individual explícita no código. |
| `prefix` | positional-or-keyword | `str` | `obrigatório/ausente` | Prefixo canônico usado para compor nomes de artefatos. |
| `project_root` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `project_root` recebido pela função; não há descrição individual explícita no código. |
| `reference_docx` | positional-or-keyword | `Path \| None` | `None` | Parâmetro `reference_docx` recebido pela função; não há descrição individual explícita no código. |
| `quiet` | positional-or-keyword | `bool` | `False` | Parâmetro `quiet` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `render_docx_for_article(art=art, prefix=prefix, cfg=None, output=None, quiet=quiet)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `render_docx_for_article`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:prepare_fulltext_stage`

**Assinatura:**
```python
def prepare_fulltext_stage(art: Path, cfg: Path, target_n: int, min_palavras: int, chars_por_pdf: int, quiet: bool = False) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 674–681
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `prepare_fulltext_stage` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `art` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `art` recebido pela função; não há descrição individual explícita no código. |
| `cfg` | positional-or-keyword | `Path` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `target_n` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `target_n` recebido pela função; não há descrição individual explícita no código. |
| `min_palavras` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `min_palavras` recebido pela função; não há descrição individual explícita no código. |
| `chars_por_pdf` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `chars_por_pdf` recebido pela função; não há descrição individual explícita no código. |
| `quiet` | positional-or-keyword | `bool` | `False` | Parâmetro `quiet` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `call_module_main`
- `print`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `--art-dir`
- `--cfg-art`
- `--chars-por-pdf`
- `--min-palavras`
- `--target-n`

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:generate_article_stage`

**Assinatura:**
```python
def generate_article_stage(art: Path, cfg: Path, quiet: bool = False) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 684–691
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Gera a responsabilidade representada por `article_stage` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `art` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `art` recebido pela função; não há descrição individual explícita no código. |
| `cfg` | positional-or-keyword | `Path` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `quiet` | positional-or-keyword | `bool` | `False` | Parâmetro `quiet` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `call_module_main`
- `print`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `--art-dir`
- `--cfg-art`

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:final_fix_stage`

**Assinatura:**
```python
def final_fix_stage(art: Path, prefix: str, project_root: Path, quiet: bool = False) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 694–714
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `final_fix_stage` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `art` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `art` recebido pela função; não há descrição individual explícita no código. |
| `prefix` | positional-or-keyword | `str` | `obrigatório/ausente` | Prefixo canônico usado para compor nomes de artefatos. |
| `project_root` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `project_root` recebido pela função; não há descrição individual explícita no código. |
| `quiet` | positional-or-keyword | `bool` | `False` | Parâmetro `quiet` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `pdf`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `SystemExit`
- `bib.exists`
- `compile_pdf`
- `export_org_to_tex`
- `org.exists`
- `out.exists`
- `parse_bib_keys`
- `patch_org`
- `patch_tex`
- `sanitize_bib`
- `tex.exists`
- `validate_pdf`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `SystemExit(f'ERRO: pasta output não encontrada: {out}')`
- `SystemExit(f'ERRO: ORG não encontrado: {org}')`
- `SystemExit(f'ERRO: BIB não encontrado: {bib}')`
- `SystemExit(f'ERRO: TEX não foi gerado: {tex}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.org`
- `.tex`
- `.bib`

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:main`

**Assinatura:**
```python
def main() -> int
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_final_unificado.py`
**Linhas:** 717–771
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa o ponto de entrada correspondente e coordena o fluxo associado.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `0`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(__file__).resolve`
- `Path(args.art_dir).resolve`
- `Path(args.cfg_art).resolve`
- `Path(args.reference_docx).resolve`
- `Path(args.template_cfg).resolve`
- `SystemExit`
- `ap.add_argument`
- `ap.parse_args`
- `argparse.ArgumentParser`
- `art.exists`
- `emit_docx_stage`
- `ensure_cfg`
- `final_fix_stage`
- `generate_article_stage`
- `prepare_cfg`
- `prepare_fulltext_stage`
- `print`
- `str`
- `sys.path.insert`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `SystemExit(f'ERRO: diretório do artigo não encontrado: {art}')`

#### Opções CLI mencionadas diretamente no corpo

- `--art-dir`
- `--cfg-art`
- `--chars-por-pdf`
- `--docx-only`
- `--emit-docx`
- `--min-palavras`
- `--prefix`
- `--quiet`
- `--reference-docx`
- `--skip-generate`
- `--skip-prepare`
- `--target-n`
- `--template-cfg`

#### Artefatos/caminhos literais reconhecidos no corpo

- `Modelo DOCX de referência. Se omitido, cria/usa app_bundle/templates/docx/reference_abnt_fgv.docx.`

#### Complexidade estrutural observada

```yaml
branches: 10
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `app_bundle.scripts.pipeline.gerar_artigo_longo_fulltext_secional:load_env`

**Assinatura:**
```python
def load_env(root: Path) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_longo_fulltext_secional.py`
**Linhas:** 14–25
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Carrega a responsabilidade representada por `env` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `root` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `root` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.
- Lê configuração/credenciais do ambiente do processo.

#### Chamadas realizadas pelo corpo direto

- `Path.cwd`
- `candidate.exists`
- `candidate.read_text`
- `candidate.read_text(encoding='utf-8', errors='ignore').splitlines`
- `k.strip`
- `line.split`
- `line.startswith`
- `line.strip`
- `os.environ.setdefault`
- `v.strip`
- `v.strip().strip`
- `v.strip().strip('"').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.gerar_artigo_longo_fulltext_secional:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3af681f2cfdcb1158ea0cce858d6c2fd76d488b408fdb19b5200b64026f8c4dc`
**LOC do arquivo-fonte:** 439


### `app_bundle.scripts.pipeline.gerar_artigo_longo_fulltext_secional:word_count`

**Assinatura:**
```python
def word_count(text: str) -> int
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_longo_fulltext_secional.py`
**Linhas:** 27–31
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `word_count` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `len(re.findall('\\b[\\wÀ-ÿ-]+\\b', text))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `len`
- `re.findall`
- `re.sub`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.gerar_artigo_longo_fulltext_secional:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3af681f2cfdcb1158ea0cce858d6c2fd76d488b408fdb19b5200b64026f8c4dc`
**LOC do arquivo-fonte:** 439


### `app_bundle.scripts.pipeline.gerar_artigo_longo_fulltext_secional:read_text`

**Assinatura:**
```python
def read_text(path: Path, default: str = '') -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_longo_fulltext_secional.py`
**Linhas:** 33–36
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Lê a responsabilidade representada por `text` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `default` | positional-or-keyword | `str` | `''` | Parâmetro `default` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `path.read_text(encoding='utf-8', errors='ignore')`; `default`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `path.exists`
- `path.read_text`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3af681f2cfdcb1158ea0cce858d6c2fd76d488b408fdb19b5200b64026f8c4dc`
**LOC do arquivo-fonte:** 439


### `app_bundle.scripts.pipeline.gerar_artigo_longo_fulltext_secional:backup`

**Assinatura:**
```python
def backup(path: Path) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_longo_fulltext_secional.py`
**Linhas:** 38–43
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `backup` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `path.exists`
- `path.with_name`
- `print`
- `shutil.copy2`
- `time.strftime`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3af681f2cfdcb1158ea0cce858d6c2fd76d488b408fdb19b5200b64026f8c4dc`
**LOC do arquivo-fonte:** 439


### `app_bundle.scripts.pipeline.gerar_artigo_longo_fulltext_secional:parse_bib_entries`

**Assinatura:**
```python
def parse_bib_entries(bib_text: str)
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_longo_fulltext_secional.py`
**Linhas:** 45–73
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Interpreta a responsabilidade representada por `bib_entries` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `bib_text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `entries`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `entries.append`
- `enumerate`
- `field`
- `km.group`
- `km.group(1).strip`
- `len`
- `m.start`
- `re.finditer`
- `re.search`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3af681f2cfdcb1158ea0cce858d6c2fd76d488b408fdb19b5200b64026f8c4dc`
**LOC do arquivo-fonte:** 439


### `app_bundle.scripts.pipeline.gerar_artigo_longo_fulltext_secional:select_context`

**Assinatura:**
```python
def select_context(corpus: str, keywords, max_chars = 55000) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_longo_fulltext_secional.py`
**Linhas:** 75–98
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Seleciona a responsabilidade representada por `context` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `corpus` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `corpus` recebido pela função; não há descrição individual explícita no código. |
| `keywords` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `keywords` recebido pela função; não há descrição individual explícita no código. |
| `max_chars` | positional-or-keyword | `não anotado` | `55000` | Parâmetro `max_chars` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `corpus[:max_chars]`; `'\n\n'.join(out)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'\n\n'.join`
- `k.lower`
- `len`
- `low.count`
- `out.append`
- `p.lower`
- `p.strip`
- `re.split`
- `scored.append`
- `scored.sort`
- `sum`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.gerar_artigo_longo_fulltext_secional:make_prompt`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 2
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3af681f2cfdcb1158ea0cce858d6c2fd76d488b408fdb19b5200b64026f8c4dc`
**LOC do arquivo-fonte:** 439


### `app_bundle.scripts.pipeline.gerar_artigo_longo_fulltext_secional:openai_chat`

**Assinatura:**
```python
def openai_chat(prompt: str, max_tokens = 3600) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_longo_fulltext_secional.py`
**Linhas:** 100–151
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `openai_chat` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `prompt` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `prompt` recebido pela função; não há descrição individual explícita no código. |
| `max_tokens` | positional-or-keyword | `não anotado` | `3600` | Parâmetro `max_tokens` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `data['choices'][0]['message']['content'].strip()`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Lê arquivos ou artefatos do filesystem.
- Lê configuração/credenciais do ambiente do processo.
- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `RuntimeError`
- `base.endswith`
- `base.rstrip`
- `data['choices'][0]['message']['content'].strip`
- `e.read`
- `e.read().decode`
- `json.dumps`
- `json.dumps(payload).encode`
- `json.loads`
- `os.environ.get`
- `os.environ.get('OPENAI_API_BASE', 'https://api.openai.com/v1/chat/completions').strip`
- `os.environ.get('OPENAI_API_KEY', '').strip`
- `resp.read`
- `resp.read().decode`
- `urllib.request.Request`
- `urllib.request.urlopen`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.gerar_artigo_longo_fulltext_secional:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('OPENAI_API_KEY ausente no .env.')`
- `RuntimeError(f'Erro HTTP OpenAI {e.code}: {detail}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `3af681f2cfdcb1158ea0cce858d6c2fd76d488b408fdb19b5200b64026f8c4dc`
**LOC do arquivo-fonte:** 439


### `app_bundle.scripts.pipeline.gerar_artigo_longo_fulltext_secional:sanitize_section`

**Assinatura:**
```python
def sanitize_section(text: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_longo_fulltext_secional.py`
**Linhas:** 153–157
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Saneia a responsabilidade representada por `section` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `text`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `re.sub`
- `re.sub('\\n{3,}', '\n\n', text).strip`
- `text.replace`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.gerar_artigo_longo_fulltext_secional:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3af681f2cfdcb1158ea0cce858d6c2fd76d488b408fdb19b5200b64026f8c4dc`
**LOC do arquivo-fonte:** 439


### `app_bundle.scripts.pipeline.gerar_artigo_longo_fulltext_secional:make_evidence_matrix`

**Assinatura:**
```python
def make_evidence_matrix(entries)
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_longo_fulltext_secional.py`
**Linhas:** 159–182
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Cria a responsabilidade representada por `evidence_matrix` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `entries` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Coleção de entradas bibliográficas/registros. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `'\n'.join(rows)`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `e['title'].lower`
- `e['title'][:95].replace`
- `enumerate`
- `rows.append`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.gerar_artigo_longo_fulltext_secional:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3af681f2cfdcb1158ea0cce858d6c2fd76d488b408fdb19b5200b64026f8c4dc`
**LOC do arquivo-fonte:** 439


### `app_bundle.scripts.pipeline.gerar_artigo_longo_fulltext_secional:make_prompt`

**Assinatura:**
```python
def make_prompt(title, target_words, keywords, entries, refs_md, stats_md, corpus, extra_instruction = '')
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_longo_fulltext_secional.py`
**Linhas:** 184–208
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Cria a responsabilidade representada por `prompt` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `title` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `title` recebido pela função; não há descrição individual explícita no código. |
| `target_words` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `target_words` recebido pela função; não há descrição individual explícita no código. |
| `keywords` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `keywords` recebido pela função; não há descrição individual explícita no código. |
| `entries` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Coleção de entradas bibliográficas/registros. |
| `refs_md` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `refs_md` recebido pela função; não há descrição individual explícita no código. |
| `stats_md` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `stats_md` recebido pela função; não há descrição individual explícita no código. |
| `corpus` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `corpus` recebido pela função; não há descrição individual explícita no código. |
| `extra_instruction` | positional-or-keyword | `não anotado` | `''` | Parâmetro `extra_instruction` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `f'\nEscreva a seção "{title}" de um artigo científico longo sobre ATESTMED, saúde digital e decisão baseada em evidências na Perícia Médica Federal.\n\nTamanho mínimo da seção: {target_words} palavras.\nNão escreva resumo. Não escreva em tópicos, salvo quando indispensável.\nUse parágrafos densos, com encadeamento argumentativo.\nUse o termo "full text" ao tratar da curadoria de textos completos quando pertinente.\nCite estudos usando apenas chaves BibTeX existentes, no formato \\cite{{chave}}.\nChaves BibTeX disponíveis: {keys}\n\nInstrução adicional da seção:\n{extra_instruction}\n\nEstatísticas/metodologia PRISMA disponíveis:\n{stats_md[:12000]}\n\nReferências incluídas e metadados:\n{refs_md[:18000]}\n\nExcertos selecionados do corpus full text:\n{context}\n'.strip()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `', '.join`
- `f'\nEscreva a seção "{title}" de um artigo científico longo sobre ATESTMED, saúde digital e decisão baseada em evidências na Perícia Médica Federal.\n\nTamanho mínimo da seção: {target_words} palavras.\nNão escreva resumo. Não escreva em tópicos, salvo quando indispensável.\nUse parágrafos densos, com encadeamento argumentativo.\nUse o termo "full text" ao tratar da curadoria de textos completos quando pertinente.\nCite estudos usando apenas chaves BibTeX existentes, no formato \\cite{{chave}}.\nChaves BibTeX disponíveis: {keys}\n\nInstrução adicional da seção:\n{extra_instruction}\n\nEstatísticas/metodologia PRISMA disponíveis:\n{stats_md[:12000]}\n\nReferências incluídas e metadados:\n{refs_md[:18000]}\n\nExcertos selecionados do corpus full text:\n{context}\n'.strip`
- `select_context`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.gerar_artigo_longo_fulltext_secional:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3af681f2cfdcb1158ea0cce858d6c2fd76d488b408fdb19b5200b64026f8c4dc`
**LOC do arquivo-fonte:** 439


### `app_bundle.scripts.pipeline.gerar_artigo_longo_fulltext_secional:extract_org_header`

**Assinatura:**
```python
def extract_org_header(old_org: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_longo_fulltext_secional.py`
**Linhas:** 210–225
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Extrai a responsabilidade representada por `org_header` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `old_org` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `old_org` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `header.rstrip() + '\n\n'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `header.rstrip`
- `m.start`
- `old_org.strip`
- `old_org[:m.start()].rstrip`
- `re.search`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.gerar_artigo_longo_fulltext_secional:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3af681f2cfdcb1158ea0cce858d6c2fd76d488b408fdb19b5200b64026f8c4dc`
**LOC do arquivo-fonte:** 439


### `app_bundle.scripts.pipeline.gerar_artigo_longo_fulltext_secional:compile_pdf`

**Assinatura:**
```python
def compile_pdf(out_dir: Path, org_path: Path) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_longo_fulltext_secional.py`
**Linhas:** 227–248
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Compila a responsabilidade representada por `pdf` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `out_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `out_dir` recebido pela função; não há descrição individual explícita no código. |
| `org_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Executa processo ou ferramenta externa.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `RuntimeError`
- `commands.append`
- `export_el.exists`
- `print`
- `str`
- `subprocess.run`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('Não foi possível compilar o PDF por Emacs/Org.')`

#### Opções CLI mencionadas diretamente no corpo

- `--batch`
- `--eval`

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `3af681f2cfdcb1158ea0cce858d6c2fd76d488b408fdb19b5200b64026f8c4dc`
**LOC do arquivo-fonte:** 439


### `app_bundle.scripts.pipeline.gerar_artigo_longo_fulltext_secional:try_docx`

**Assinatura:**
```python
def try_docx(org_path: Path, docx_path: Path, bib_path: Path) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_longo_fulltext_secional.py`
**Linhas:** 250–256
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `try_docx` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `org_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `docx_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `bib_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Executa processo ou ferramenta externa.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `print`
- `str`
- `subprocess.run`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.gerar_artigo_longo_fulltext_secional:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `--bibliography`

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `3af681f2cfdcb1158ea0cce858d6c2fd76d488b408fdb19b5200b64026f8c4dc`
**LOC do arquivo-fonte:** 439


### `app_bundle.scripts.pipeline.gerar_artigo_longo_fulltext_secional:main`

**Assinatura:**
```python
def main()
```

**Arquivo:** `app_bundle/scripts/pipeline/gerar_artigo_longo_fulltext_secional.py`
**Linhas:** 258–436
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa o ponto de entrada correspondente e coordena o fluxo associado.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Sem anotação de retorno explícita. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.
- Lê arquivos ou artefatos do filesystem.
- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `'\n* Referências\n\n\\bibliographystyle{abntex2-alf}\n\\bibliography{artigo_final_atestmed_abnt}\n'.strip`
- `Path`
- `Path.cwd`
- `SystemExit`
- `ap.add_argument`
- `ap.parse_args`
- `argparse.ArgumentParser`
- `backup`
- `bib_path.exists`
- `body.strip`
- `compile_pdf`
- `document_json_sections.append`
- `extract_org_header`
- `generated_sections.append`
- `json.dumps`
- `json_path.write_text`
- `len`
- `load_env`
- `make_evidence_matrix`
- `make_prompt`
- `max`
- `min`
- `openai_chat`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `SystemExit(f'ERRO: BibTeX com {len(entries)} entradas; esperado mínimo de 20.')`

#### Opções CLI mencionadas diretamente no corpo

- `--art-dir`
- `--compile`
- `--min-palavras`

#### Artefatos/caminhos literais reconhecidos no corpo

- `artigo_final_atestmed_abnt.org`
- `artigo_final_atestmed_abnt.bib`
- `artigo_final_atestmed_abnt.pdf`
- `artigo_final_atestmed_abnt.docx`
- `artigo_final_atestmed_abnt.document.json`
- `dados_prisma/artigo_longo_fulltext/referencias_incluidas_20.md`
- `dados_prisma/artigo_longo_fulltext/estatisticas_prisma_fulltext.md`
- `dados_prisma/artigo_longo_fulltext/corpus_fulltext_compilado.md`
- `dados_prisma/relatorio_prisma_prisma_fluxo_pmf.referencias_incluidas.bib`
- `[OK] document.json longo regravado:`

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3af681f2cfdcb1158ea0cce858d6c2fd76d488b408fdb19b5200b64026f8c4dc`
**LOC do arquivo-fonte:** 439


### `app_bundle.scripts.pipeline.institution_compliance:_cfg_section`

**Assinatura:**
```python
def _cfg_section(cfg: dict[str, Any], name: str) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/institution_compliance.py`
**Linhas:** 49–51
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_cfg_section` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `name` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `name` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `sec if isinstance(sec, dict) else {}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `cfg.get`
- `isinstance`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `5117477811204e8ab59642e7bfe63c361995b089cc7985f8aa747932109fae4a`
**LOC do arquivo-fonte:** 396


### `app_bundle.scripts.pipeline.institution_compliance:_config_dir`

**Assinatura:**
```python
def _config_dir(cfg: dict[str, Any]) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/institution_compliance.py`
**Linhas:** 54–55
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_config_dir` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `Path(str(cfg.get('__config_dir__') or Path.cwd())).resolve()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(str(cfg.get('__config_dir__') or Path.cwd())).resolve`
- `Path.cwd`
- `cfg.get`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `5117477811204e8ab59642e7bfe63c361995b089cc7985f8aa747932109fae4a`
**LOC do arquivo-fonte:** 396


### `app_bundle.scripts.pipeline.institution_compliance:_doc_type`

**Assinatura:**
```python
def _doc_type(cfg: dict[str, Any]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/institution_compliance.py`
**Linhas:** 58–60
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_doc_type` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `{'dissertação': 'dissertacao', 'dissertation': 'dissertacao'}.get(v, v)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_cfg_section`
- `_cfg_section(cfg, 'documento').get`
- `str`
- `str(_cfg_section(cfg, 'documento').get('tipo_documento') or 'paper').strip`
- `str(_cfg_section(cfg, 'documento').get('tipo_documento') or 'paper').strip().lower`
- `{'dissertação': 'dissertacao', 'dissertation': 'dissertacao'}.get`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `5117477811204e8ab59642e7bfe63c361995b089cc7985f8aa747932109fae4a`
**LOC do arquivo-fonte:** 396


### `app_bundle.scripts.pipeline.institution_compliance:_profile_dir`

**Assinatura:**
```python
def _profile_dir(cfg: dict[str, Any]) -> Path | None
```

**Arquivo:** `app_bundle/scripts/pipeline/institution_compliance.py`
**Linhas:** 63–68
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_profile_dir` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `Path | None`. Retornos observados estaticamente incluem: `None`; `p.parent if p.exists() else None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(str(raw)).expanduser`
- `Path(str(raw)).expanduser().resolve`
- `cfg.get`
- `p.exists`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `5117477811204e8ab59642e7bfe63c361995b089cc7985f8aa747932109fae4a`
**LOC do arquivo-fonte:** 396


### `app_bundle.scripts.pipeline.institution_compliance:_load_toml`

**Assinatura:**
```python
def _load_toml(path: Path) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/institution_compliance.py`
**Linhas:** 71–75
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Carrega a responsabilidade representada por `toml` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `{}`; `tomllib.load(f)`.

#### Efeitos colaterais e fronteiras externas

- Abre arquivo para leitura ou inspeção.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `path.exists`
- `path.open`
- `tomllib.load`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.institution_compliance:load_institution_rules`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `5117477811204e8ab59642e7bfe63c361995b089cc7985f8aa747932109fae4a`
**LOC do arquivo-fonte:** 396


### `app_bundle.scripts.pipeline.institution_compliance:load_institution_rules`

**Assinatura:**
```python
def load_institution_rules(cfg: dict[str, Any]) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/institution_compliance.py`
**Linhas:** 78–92
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Carrega a responsabilidade representada por `institution_rules` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `{}`; `merged`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_doc_type`
- `_load_toml`
- `_profile_dir`
- `data.items`
- `isinstance`
- `merged.get`
- `merged[k].update`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.institution_compliance:run_institution_compliance`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `fgv_rules.toml`
- `_rules.toml`

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `5117477811204e8ab59642e7bfe63c361995b089cc7985f8aa747932109fae4a`
**LOC do arquivo-fonte:** 396


### `app_bundle.scripts.pipeline.institution_compliance:_style_from_cfg`

**Assinatura:**
```python
def _style_from_cfg(cfg: dict[str, Any]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/institution_compliance.py`
**Linhas:** 95–98
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_style_from_cfg` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `str(bib.get('latex_style') or bib.get('estilo_citacao') or doc.get('estilo_citacao') or '').strip().lower()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_cfg_section`
- `bib.get`
- `doc.get`
- `str`
- `str(bib.get('latex_style') or bib.get('estilo_citacao') or doc.get('estilo_citacao') or '').strip`
- `str(bib.get('latex_style') or bib.get('estilo_citacao') or doc.get('estilo_citacao') or '').strip().lower`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `5117477811204e8ab59642e7bfe63c361995b089cc7985f8aa747932109fae4a`
**LOC do arquivo-fonte:** 396


### `app_bundle.scripts.pipeline.institution_compliance:_read`

**Assinatura:**
```python
def _read(path: Path | None) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/institution_compliance.py`
**Linhas:** 101–104
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Lê a responsabilidade representada por `read` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path \| None` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `''`; `path.read_text(encoding='utf-8', errors='ignore')`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `path.exists`
- `path.read_text`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.institution_compliance:_check_bib`
- `app_bundle.scripts.pipeline.institution_compliance:_check_org`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `5117477811204e8ab59642e7bfe63c361995b089cc7985f8aa747932109fae4a`
**LOC do arquivo-fonte:** 396


### `app_bundle.scripts.pipeline.institution_compliance:_add`

**Assinatura:**
```python
def _add(items: list[ComplianceItem], status: str, id_: str, message: str, detail: str = '') -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/institution_compliance.py`
**Linhas:** 107–108
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_add` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `items` | positional-or-keyword | `list[ComplianceItem]` | `obrigatório/ausente` | Parâmetro `items` recebido pela função; não há descrição individual explícita no código. |
| `status` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `status` recebido pela função; não há descrição individual explícita no código. |
| `id_` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `id_` recebido pela função; não há descrição individual explícita no código. |
| `message` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `message` recebido pela função; não há descrição individual explícita no código. |
| `detail` | positional-or-keyword | `str` | `''` | Parâmetro `detail` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `ComplianceItem`
- `items.append`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.institution_compliance:_check_bib`
- `app_bundle.scripts.pipeline.institution_compliance:_check_config`
- `app_bundle.scripts.pipeline.institution_compliance:_check_docx`
- `app_bundle.scripts.pipeline.institution_compliance:_check_org`
- `app_bundle.scripts.pipeline.institution_compliance:run_institution_compliance`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `5117477811204e8ab59642e7bfe63c361995b089cc7985f8aa747932109fae4a`
**LOC do arquivo-fonte:** 396


### `app_bundle.scripts.pipeline.institution_compliance:_contains`

**Assinatura:**
```python
def _contains(text: str, pattern: str) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/institution_compliance.py`
**Linhas:** 111–112
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_contains` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |
| `pattern` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `pattern` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `re.search(pattern, text or '', flags=re.IGNORECASE | re.MULTILINE) is not None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `re.search`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.institution_compliance:_check_org`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `5117477811204e8ab59642e7bfe63c361995b089cc7985f8aa747932109fae4a`
**LOC do arquivo-fonte:** 396


### `app_bundle.scripts.pipeline.institution_compliance:_strip_nonvisible_org_regions`

**Assinatura:**
```python
def _strip_nonvisible_org_regions(org_text: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/institution_compliance.py`
**Linhas:** 115–132
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Retorna apenas o conteúdo aproximadamente visível do ORG.

**Docstring resumida:** Retorna apenas o conteúdo aproximadamente visível do ORG.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `org_text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `text`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `line.lstrip`
- `line.lstrip().startswith`
- `re.sub`
- `text.splitlines`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `5117477811204e8ab59642e7bfe63c361995b089cc7985f8aa747932109fae4a`
**LOC do arquivo-fonte:** 396


### `app_bundle.scripts.pipeline.institution_compliance:_find_technical_terms_visible`

**Assinatura:**
```python
def _find_technical_terms_visible(org_text: str) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/institution_compliance.py`
**Linhas:** 135–143
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Localiza a responsabilidade representada por `technical_terms_visible` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `org_text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `[name for name, pat in patterns.items() if re.search(pat, visible)]`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_strip_nonvisible_org_regions`
- `patterns.items`
- `re.search`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.institution_compliance:_check_org`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `5117477811204e8ab59642e7bfe63c361995b089cc7985f8aa747932109fae4a`
**LOC do arquivo-fonte:** 396


### `app_bundle.scripts.pipeline.institution_compliance:_check_config`

**Assinatura:**
```python
def _check_config(cfg: dict[str, Any], rules: dict[str, Any], items: list[ComplianceItem]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/institution_compliance.py`
**Linhas:** 146–196
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Verifica a responsabilidade representada por `config` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `rules` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `rules` recebido pela função; não há descrição individual explícita no código. |
| `items` | positional-or-keyword | `list[ComplianceItem]` | `obrigatório/ausente` | Parâmetro `items` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_add`
- `_cfg_section`
- `_cfg_section(cfg, 'atividade').get`
- `_config_dir`
- `_doc_type`
- `_style_from_cfg`
- `aw.exists`
- `bool`
- `cfg.get`
- `doc.get`
- `isinstance`
- `latex.get`
- `normalize_title_loose`
- `rbib.get`
- `resolve_path`
- `rules.get`
- `str`
- `str(rbib.get('estilo_padrao') or '').strip`
- `str(rbib.get('estilo_padrao') or '').strip().lower`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.institution_compliance:run_institution_compliance`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 13
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `5117477811204e8ab59642e7bfe63c361995b089cc7985f8aa747932109fae4a`
**LOC do arquivo-fonte:** 396


### `app_bundle.scripts.pipeline.institution_compliance:_check_org`

**Assinatura:**
```python
def _check_org(cfg: dict[str, Any], rules: dict[str, Any], org_path: Path | None, items: list[ComplianceItem]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/institution_compliance.py`
**Linhas:** 199–269
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Verifica a responsabilidade representada por `org` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `rules` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `rules` recebido pela função; não há descrição individual explícita no código. |
| `org_path` | positional-or-keyword | `Path \| None` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `items` | positional-or-keyword | `list[ComplianceItem]` | `obrigatório/ausente` | Parâmetro `items` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `', '.join`
- `_add`
- `_contains`
- `_doc_type`
- `_find_technical_terms_visible`
- `_read`
- `_style_from_cfg`
- `bool`
- `checks.items`
- `id_.endswith`
- `lower.rfind`
- `normalize_title_loose`
- `re.escape`
- `re.search`
- `str`
- `text.upper`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.institution_compliance:run_institution_compliance`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `org.biblatex_style`

#### Complexidade estrutural observada

```yaml
branches: 15
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `5117477811204e8ab59642e7bfe63c361995b089cc7985f8aa747932109fae4a`
**LOC do arquivo-fonte:** 396


### `app_bundle.scripts.pipeline.institution_compliance:_check_bib`

**Assinatura:**
```python
def _check_bib(bib_path: Path | None, items: list[ComplianceItem]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/institution_compliance.py`
**Linhas:** 272–292
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Verifica a responsabilidade representada por `bib` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `bib_path` | positional-or-keyword | `Path \| None` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `items` | positional-or-keyword | `list[ComplianceItem]` | `obrigatório/ausente` | Parâmetro `items` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `', '.join`
- `_add`
- `_read`
- `bib_entry_key`
- `len`
- `set`
- `split_bib_entries`
- `str`
- `t.lower`
- `text.lower`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.institution_compliance:run_institution_compliance`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `5117477811204e8ab59642e7bfe63c361995b089cc7985f8aa747932109fae4a`
**LOC do arquivo-fonte:** 396


### `app_bundle.scripts.pipeline.institution_compliance:_check_docx`

**Assinatura:**
```python
def _check_docx(docx_path: Path | None, items: list[ComplianceItem]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/institution_compliance.py`
**Linhas:** 295–316
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Verifica a responsabilidade representada por `docx` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `docx_path` | positional-or-keyword | `Path \| None` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `items` | positional-or-keyword | `list[ComplianceItem]` | `obrigatório/ausente` | Parâmetro `items` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Document`
- `_add`
- `abs`
- `docx_path.exists`
- `round`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.institution_compliance:run_institution_compliance`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `5117477811204e8ab59642e7bfe63c361995b089cc7985f8aa747932109fae4a`
**LOC do arquivo-fonte:** 396


### `app_bundle.scripts.pipeline.institution_compliance:run_institution_compliance`

**Assinatura:**
```python
def run_institution_compliance(cfg: dict[str, Any], *, org_path: Path | None = None, bib_path: Path | None = None, docx_path: Path | None = None, pdf_path: Path | None = None) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/institution_compliance.py`
**Linhas:** 319–358
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `institution_compliance` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `org_path` | keyword-only | `Path \| None` | `None` | Caminho de arquivo ou diretório utilizado pela operação. |
| `bib_path` | keyword-only | `Path \| None` | `None` | Caminho de arquivo ou diretório utilizado pela operação. |
| `docx_path` | keyword-only | `Path \| None` | `None` | Caminho de arquivo ou diretório utilizado pela operação. |
| `pdf_path` | keyword-only | `Path \| None` | `None` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `{'version': PIPELINE_VERSION, 'generated_at': now_iso(), 'ok': not errors, 'institution_profile': cfg.get('__institution_profile_name__'), 'institution_profile_path': cfg.get('__institution_profile_path__'), 'document_type': _doc_type(cfg), 'rules_loaded': bool(rules), 'artifacts': {'org': str(org_path) if org_path else None, 'bib': str(bib_path) if bib_path else None, 'docx': str(docx_path) if docx_path else None, 'pdf': str(pdf_path) if pdf_path else None}, 'errors': errors, 'warnings': warnings, 'items': [asdict(i) for i in items]}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_add`
- `_check_bib`
- `_check_config`
- `_check_docx`
- `_check_org`
- `_doc_type`
- `asdict`
- `bool`
- `cfg.get`
- `load_institution_rules`
- `now_iso`
- `pdf_path.exists`
- `pdf_path.stat`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 0
comprehensions: 3
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `5117477811204e8ab59642e7bfe63c361995b089cc7985f8aa747932109fae4a`
**LOC do arquivo-fonte:** 396


### `app_bundle.scripts.pipeline.institution_compliance:render_compliance_markdown`

**Assinatura:**
```python
def render_compliance_markdown(report: dict[str, Any]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/institution_compliance.py`
**Linhas:** 361–388
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Renderiza a responsabilidade representada por `compliance_markdown` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `report` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `report` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'\n'.join(lines).strip() + '\n'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `'\n'.join(lines).strip`
- `(report.get('artifacts') or {}).items`
- `i.get`
- `item.get`
- `lines.append`
- `lines.extend`
- `report.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.institution_compliance:write_compliance_reports`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 3
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `5117477811204e8ab59642e7bfe63c361995b089cc7985f8aa747932109fae4a`
**LOC do arquivo-fonte:** 396


### `app_bundle.scripts.pipeline.institution_compliance:write_compliance_reports`

**Assinatura:**
```python
def write_compliance_reports(report: dict[str, Any], output_prefix: Path) -> tuple[Path, Path]
```

**Arquivo:** `app_bundle/scripts/pipeline/institution_compliance.py`
**Linhas:** 391–396
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Grava a responsabilidade representada por `compliance_reports` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `report` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `report` recebido pela função; não há descrição individual explícita no código. |
| `output_prefix` | positional-or-keyword | `Path` | `obrigatório/ausente` | Prefixo canônico usado para compor nomes de artefatos. |

#### Retorno

Anotação declarada: `tuple[Path, Path]`. Retornos observados estaticamente incluem: `(md_path, json_path)`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `output_prefix.with_suffix`
- `render_compliance_markdown`
- `write_json`
- `write_text`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.default_runtime:_ap003f_pipeline_core`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.compliance_report.json`
- `.compliance_report.md`

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `5117477811204e8ab59642e7bfe63c361995b089cc7985f8aa747932109fae4a`
**LOC do arquivo-fonte:** 396


### `app_bundle.scripts.pipeline.institution_explainer:_fmt_list`

**Assinatura:**
```python
def _fmt_list(values: list[str] | tuple[str, ...] | None) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/institution_explainer.py`
**Linhas:** 17–20
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_fmt_list` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `values` | positional-or-keyword | `list[str] \| tuple[str, ...] \| None` | `obrigatório/ausente` | Parâmetro `values` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'nenhum'`; `', '.join((str(v) for v in values))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `', '.join`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `989eea81a522d2f88beb5471b4dc085af9b447c07f3bc8b5ba5e17bb182df2bf`
**LOC do arquivo-fonte:** 83


### `app_bundle.scripts.pipeline.institution_explainer:explain_profile`

**Assinatura:**
```python
def explain_profile(profile_name: str = 'fgv', app_bundle: Path | None = None) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/institution_explainer.py`
**Linhas:** 23–83
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `explain_profile` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `profile_name` | positional-or-keyword | `str` | `'fgv'` | Parâmetro `profile_name` recebido pela função; não há descrição individual explícita no código. |
| `app_bundle` | positional-or-keyword | `Path \| None` | `None` | Parâmetro `app_bundle` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'\n'.join(lines).strip() + '\n'`.

#### Efeitos colaterais e fronteiras externas

- Abre arquivo para leitura ou inspeção.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `', '.join`
- `'\n'.join`
- `'\n'.join(lines).strip`
- `(profile_name or 'fgv').strip`
- `(profile_name or 'fgv').strip().lower`
- `FileNotFoundError`
- `available_institution_profiles`
- `bib.get`
- `cfg.get`
- `defaults.get`
- `doc.get`
- `document_types.items`
- `docx.get`
- `f"Manual: {meta.get('manual', '')} {meta.get('versao_manual', '')}".strip`
- `find_app_bundle`
- `fmt.get`
- `isinstance`
- `latex.get`
- `lines.append`
- `lines.extend`
- `meta.get`
- `profile.get`
- `profile_path.exists`
- `profile_path.open`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `FileNotFoundError(f'Perfil institucional não encontrado: {profile_name}. Disponíveis: {available}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `institution_profile.toml`

#### Complexidade estrutural observada

```yaml
branches: 10
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `989eea81a522d2f88beb5471b4dc085af9b447c07f3bc8b5ba5e17bb182df2bf`
**LOC do arquivo-fonte:** 83


### `app_bundle.scripts.pipeline.institution_layouts:_section`

**Assinatura:**
```python
def _section(obj: dict[str, Any] | None, name: str) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/institution_layouts.py`
**Linhas:** 45–49
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_section` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `obj` | positional-or-keyword | `dict[str, Any] \| None` | `obrigatório/ausente` | Parâmetro `obj` recebido pela função; não há descrição individual explícita no código. |
| `name` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `name` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `{}`; `sec if isinstance(sec, dict) else {}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `isinstance`
- `obj.get`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `5103320fca74fe14814431370e06a20f8d3436bf98ada5ce47613a6b39343cb6`
**LOC do arquivo-fonte:** 149


### `app_bundle.scripts.pipeline.institution_layouts:_doctype_alias`

**Assinatura:**
```python
def _doctype_alias(value: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/institution_layouts.py`
**Linhas:** 52–62
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_doctype_alias` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `aliases.get(v, v)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `aliases.get`
- `str`
- `str(value or '').strip`
- `str(value or '').strip().lower`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `5103320fca74fe14814431370e06a20f8d3436bf98ada5ce47613a6b39343cb6`
**LOC do arquivo-fonte:** 149


### `app_bundle.scripts.pipeline.institution_layouts:available_layouts`

**Assinatura:**
```python
def available_layouts(cfg: dict[str, Any]) -> dict[str, dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/institution_layouts.py`
**Linhas:** 65–68
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `available_layouts` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `dict[str, dict[str, Any]]`. Retornos observados estaticamente incluem: `{str(k): v for k, v in layouts.items() if isinstance(v, dict)}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `cfg.get`
- `isinstance`
- `layouts.items`
- `profile.get`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.institution_layouts:_default_layout_for_genero`
- `app_bundle.scripts.pipeline.institution_layouts:resolve_layout_spec`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `5103320fca74fe14814431370e06a20f8d3436bf98ada5ce47613a6b39343cb6`
**LOC do arquivo-fonte:** 149


### `app_bundle.scripts.pipeline.institution_layouts:_default_layout_for_genero`

**Assinatura:**
```python
def _default_layout_for_genero(cfg: dict[str, Any], genero: str, tipo_conteudo: str = '') -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/institution_layouts.py`
**Linhas:** 71–99
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_default_layout_for_genero` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `genero` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `genero` recebido pela função; não há descrição individual explícita no código. |
| `tipo_conteudo` | positional-or-keyword | `str` | `''` | Parâmetro `tipo_conteudo` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `default_layout`; `layout_id`; `'atividade_fgv' if 'atividade_fgv' in layouts else 'atividade'`; `'dissertacao_fgv' if 'dissertacao_fgv' in layouts else 'dissertacao'`; `'paper_fgv' if 'paper_fgv' in layouts else 'paper'`; `genero or 'paper'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_doctype_alias`
- `available_layouts`
- `cfg.get`
- `content_types.get`
- `content_types[tipo].get`
- `doc_types.get`
- `doc_types[genero].get`
- `isinstance`
- `layouts.items`
- `profile.get`
- `spec.get`
- `str`
- `str(content_types[tipo].get('default_layout') or '').strip`
- `str(doc_types[genero].get('default_layout') or '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.institution_layouts:resolve_layout_spec`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 14
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `5103320fca74fe14814431370e06a20f8d3436bf98ada5ce47613a6b39343cb6`
**LOC do arquivo-fonte:** 149


### `app_bundle.scripts.pipeline.institution_layouts:resolve_layout_spec`

**Assinatura:**
```python
def resolve_layout_spec(cfg: dict[str, Any], doc: Any | None = None) -> LayoutSpec
```

**Arquivo:** `app_bundle/scripts/pipeline/institution_layouts.py`
**Linhas:** 102–149
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Resolve a responsabilidade representada por `layout_spec` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `doc` | positional-or-keyword | `Any \| None` | `None` | Documento/registro individual processado pela rotina. |

#### Retorno

Anotação declarada: `LayoutSpec`. Retornos observados estaticamente incluem: `LayoutSpec(id=layout_id, institution=institution, genero_academico=str(raw.get('genero_academico') or genero), tipo_conteudo=tipo_conteudo, front_matter=front, classe_latex=classe, template=template, validator=validator, raw=raw)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `LayoutSpec`
- `_default_layout_for_genero`
- `_doctype_alias`
- `_section`
- `available_layouts`
- `cfg.get`
- `dict`
- `documento.get`
- `getattr`
- `inst.get`
- `layouts.get`
- `raw.get`
- `str`
- `str(documento.get('classe_latex') or raw.get('classe_latex') or raw.get('latex_class') or '').strip`
- `str(documento.get('layout') or documento.get('layout_id') or '').strip`
- `str(documento.get('template_org') or documento.get('template_path') or raw.get('template') or raw.get('template_org') or '').strip`
- `str(inst.get('perfil') or cfg.get('__institution_profile_name__') or '').strip`
- `str(raw.get('front_matter') or raw.get('front_matter_renderer') or layout_id or genero).strip`
- `str(raw.get('validator') or raw.get('validator_rules') or '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:_layout_spec`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 7
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `5103320fca74fe14814431370e06a20f8d3436bf98ada5ce47613a6b39343cb6`
**LOC do arquivo-fonte:** 149


### `app_bundle.scripts.pipeline.institution_profiles:_cfg_section`

**Assinatura:**
```python
def _cfg_section(cfg: dict[str, Any], name: str) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/institution_profiles.py`
**Linhas:** 23–25
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_cfg_section` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `name` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `name` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `sec if isinstance(sec, dict) else {}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `cfg.get`
- `isinstance`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e6f37ebbaa90beb1dee3d44f55b1455d29f0fbf0447fdca0b3156b133c00928f`
**LOC do arquivo-fonte:** 211


### `app_bundle.scripts.pipeline.institution_profiles:_config_dir`

**Assinatura:**
```python
def _config_dir(cfg: dict[str, Any]) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/institution_profiles.py`
**Linhas:** 28–29
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_config_dir` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `Path(str(cfg.get('__config_dir__') or Path.cwd())).resolve()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(str(cfg.get('__config_dir__') or Path.cwd())).resolve`
- `Path.cwd`
- `cfg.get`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e6f37ebbaa90beb1dee3d44f55b1455d29f0fbf0447fdca0b3156b133c00928f`
**LOC do arquivo-fonte:** 211


### `app_bundle.scripts.pipeline.institution_profiles:find_app_bundle`

**Assinatura:**
```python
def find_app_bundle(config_dir: Path | None = None) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/institution_profiles.py`
**Linhas:** 32–51
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Localiza app_bundle a partir do TOML, do cwd ou do próprio script.

**Docstring resumida:** Localiza app_bundle a partir do TOML, do cwd ou do próprio script.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `config_dir` | positional-or-keyword | `Path \| None` | `None` | Parâmetro `config_dir` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `candidate`; `(candidate / 'app_bundle').resolve()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `(candidate / 'app_bundle').exists`
- `(candidate / 'app_bundle').resolve`
- `Path`
- `Path(__file__).resolve`
- `Path.cwd`
- `Path.cwd().resolve`
- `RuntimeError`
- `candidates.extend`
- `config_dir.resolve`
- `seen.add`
- `set`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('Não foi possível localizar app_bundle para carregar perfis institucionais.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e6f37ebbaa90beb1dee3d44f55b1455d29f0fbf0447fdca0b3156b133c00928f`
**LOC do arquivo-fonte:** 211


### `app_bundle.scripts.pipeline.institution_profiles:available_institution_profiles`

**Assinatura:**
```python
def available_institution_profiles(app_bundle: Path | None = None) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/institution_profiles.py`
**Linhas:** 54–62
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `available_institution_profiles` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `app_bundle` | positional-or-keyword | `Path \| None` | `None` | Parâmetro `app_bundle` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `[]`; `sorted((p.name for p in base.iterdir() if p.is_dir() and (p / 'institution_profile.toml').exists()))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `(p / 'institution_profile.toml').exists`
- `base.exists`
- `base.iterdir`
- `find_app_bundle`
- `p.is_dir`
- `sorted`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.institution_explainer:explain_profile`
- `app_bundle.scripts.pipeline.institution_profiles:describe_institution_profiles`
- `app_bundle.scripts.pipeline.institution_profiles:load_institution_profile`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `institution_profile.toml`

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e6f37ebbaa90beb1dee3d44f55b1455d29f0fbf0447fdca0b3156b133c00928f`
**LOC do arquivo-fonte:** 211


### `app_bundle.scripts.pipeline.institution_profiles:_deep_merge_missing`

**Assinatura:**
```python
def _deep_merge_missing(target: dict[str, Any], defaults: dict[str, Any]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/institution_profiles.py`
**Linhas:** 65–77
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Merge recursivo que só preenche chaves ausentes/vazias.

**Docstring resumida:** Merge recursivo que só preenche chaves ausentes/vazias.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `target` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Destino ou alvo da operação. |
| `defaults` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `defaults` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_deep_merge_missing`
- `defaults.items`
- `isinstance`
- `target.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.institution_profiles:_deep_merge_missing`
- `app_bundle.scripts.pipeline.institution_profiles:apply_institution_profile`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e6f37ebbaa90beb1dee3d44f55b1455d29f0fbf0447fdca0b3156b133c00928f`
**LOC do arquivo-fonte:** 211


### `app_bundle.scripts.pipeline.institution_profiles:_doctype_alias`

**Assinatura:**
```python
def _doctype_alias(value: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/institution_profiles.py`
**Linhas:** 80–89
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_doctype_alias` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `aliases.get(v, v)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `aliases.get`
- `str`
- `str(value or 'paper').strip`
- `str(value or 'paper').strip().lower`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e6f37ebbaa90beb1dee3d44f55b1455d29f0fbf0447fdca0b3156b133c00928f`
**LOC do arquivo-fonte:** 211


### `app_bundle.scripts.pipeline.institution_profiles:_expand_refs`

**Assinatura:**
```python
def _expand_refs(value: Any, *, app_bundle: Path, profile_dir: Path, config_dir: Path) -> Any
```

**Arquivo:** `app_bundle/scripts/pipeline/institution_profiles.py`
**Linhas:** 92–106
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_expand_refs` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |
| `app_bundle` | keyword-only | `Path` | `obrigatório/ausente` | Parâmetro `app_bundle` recebido pela função; não há descrição individual explícita no código. |
| `profile_dir` | keyword-only | `Path` | `obrigatório/ausente` | Parâmetro `profile_dir` recebido pela função; não há descrição individual explícita no código. |
| `config_dir` | keyword-only | `Path` | `obrigatório/ausente` | Parâmetro `config_dir` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Any`. Retornos observados estaticamente incluem: `{k: _expand_refs(v, app_bundle=app_bundle, profile_dir=profile_dir, config_dir=config_dir) for k, v in value.items()}`; `[_expand_refs(v, app_bundle=app_bundle, profile_dir=profile_dir, config_dir=config_dir) for v in value]`; `value`; `str((profile_dir / text.removeprefix('profile://')).resolve())`; `str((app_bundle / text.removeprefix('app://')).resolve())`; `str((config_dir / text.removeprefix('config://')).resolve())`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `(app_bundle / text.removeprefix('app://')).resolve`
- `(config_dir / text.removeprefix('config://')).resolve`
- `(profile_dir / text.removeprefix('profile://')).resolve`
- `_expand_refs`
- `isinstance`
- `str`
- `text.removeprefix`
- `text.startswith`
- `value.items`
- `value.strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.institution_profiles:_expand_refs`
- `app_bundle.scripts.pipeline.institution_profiles:load_institution_profile`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 0
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e6f37ebbaa90beb1dee3d44f55b1455d29f0fbf0447fdca0b3156b133c00928f`
**LOC do arquivo-fonte:** 211


### `app_bundle.scripts.pipeline.institution_profiles:load_institution_profile`

**Assinatura:**
```python
def load_institution_profile(cfg: dict[str, Any]) -> tuple[str | None, dict[str, Any] | None, Path | None]
```

**Arquivo:** `app_bundle/scripts/pipeline/institution_profiles.py`
**Linhas:** 109–123
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Carrega a responsabilidade representada por `institution_profile` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `tuple[str | None, dict[str, Any] | None, Path | None]`. Retornos observados estaticamente incluem: `(None, None, None)`; `(profile_name, profile, profile_path)`.

#### Efeitos colaterais e fronteiras externas

- Abre arquivo para leitura ou inspeção.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `', '.join`
- `FileNotFoundError`
- `_cfg_section`
- `_config_dir`
- `_expand_refs`
- `available_institution_profiles`
- `find_app_bundle`
- `inst.get`
- `profile_path.exists`
- `profile_path.open`
- `str`
- `str(inst.get('perfil') or inst.get('profile') or '').strip`
- `str(inst.get('perfil') or inst.get('profile') or '').strip().lower`
- `tomllib.load`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.institution_profiles:apply_institution_profile`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `FileNotFoundError(f'Perfil institucional não encontrado: {profile_name}. Disponíveis: {available}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `institution_profile.toml`

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e6f37ebbaa90beb1dee3d44f55b1455d29f0fbf0447fdca0b3156b133c00928f`
**LOC do arquivo-fonte:** 211


### `app_bundle.scripts.pipeline.institution_profiles:apply_institution_profile`

**Assinatura:**
```python
def apply_institution_profile(cfg: dict[str, Any]) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/institution_profiles.py`
**Linhas:** 126–191
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Aplica defaults do perfil institucional ao TOML já carregado.

**Docstring resumida:** Aplica defaults do perfil institucional ao TOML já carregado.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `out`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_cfg_section`
- `_deep_merge_missing`
- `_doctype_alias`
- `candidate.get`
- `content_types.get`
- `copy.deepcopy`
- `ct_spec.get`
- `doc.get`
- `doc.setdefault`
- `dt_spec.get`
- `inst.get`
- `isinstance`
- `layout_spec.get`
- `layouts.get`
- `layouts.items`
- `load_institution_profile`
- `out.setdefault`
- `per_doc.get`
- `profile.get`
- `str`
- `str(ct_spec.get('default_layout') or dt_spec.get('default_layout') or '').strip`
- `str(doc.get('layout') or '').strip`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.default_runtime:load_config`
- `academic_pipeline.runtime:_load_config`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 21
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e6f37ebbaa90beb1dee3d44f55b1455d29f0fbf0447fdca0b3156b133c00928f`
**LOC do arquivo-fonte:** 211


### `app_bundle.scripts.pipeline.institution_profiles:describe_institution_profiles`

**Assinatura:**
```python
def describe_institution_profiles(app_bundle: Path | None = None) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/institution_profiles.py`
**Linhas:** 194–211
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `describe_institution_profiles` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `app_bundle` | positional-or-keyword | `Path \| None` | `None` | Parâmetro `app_bundle` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'Nenhum perfil institucional encontrado.'`; `'\n'.join(lines)`.

#### Efeitos colaterais e fronteiras externas

- Abre arquivo para leitura ou inspeção.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `available_institution_profiles`
- `find_app_bundle`
- `isinstance`
- `lines.append`
- `meta.get`
- `path.open`
- `profile.get`
- `tomllib.load`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `institution_profile.toml`

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `e6f37ebbaa90beb1dee3d44f55b1455d29f0fbf0447fdca0b3156b133c00928f`
**LOC do arquivo-fonte:** 211


### `app_bundle.scripts.pipeline.latex_compile:_safe_engine`

**Assinatura:**
```python
def _safe_engine(raw: str | None) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/latex_compile.py`
**Linhas:** 14–20
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_safe_engine` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `raw` | positional-or-keyword | `str \| None` | `obrigatório/ausente` | Parâmetro `raw` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `engine`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `', '.join`
- `RuntimeError`
- `ValueError`
- `shutil.which`
- `sorted`
- `str`
- `str(raw or 'lualatex').strip`
- `str(raw or 'lualatex').strip().lower`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.latex_compile:run_compile_sequence`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `ValueError(f"pdf_engine inválido: {engine}. Use um de: {', '.join(sorted(_ALLOWED_ENGINES))}")`
- `RuntimeError(f'Engine LaTeX não encontrado no PATH: {engine}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `ad4dd565018e387f79b76f3e635550b99ffc29969092a69af61ec7db3e06f42d`
**LOC do arquivo-fonte:** 78


### `app_bundle.scripts.pipeline.latex_compile:run_compile_sequence`

**Assinatura:**
```python
def run_compile_sequence(org_path: Path, academic_writing: Path | None = None, latex_extra_path: Path | None = None, pdf_engine: str = 'lualatex') -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/latex_compile.py`
**Linhas:** 23–78
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Exporta ORG para PDF via Emacs batch + LaTeX/Biber.

**Docstring resumida:** Exporta ORG para PDF via Emacs batch + LaTeX/Biber.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `org_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `academic_writing` | positional-or-keyword | `Path \| None` | `None` | Parâmetro `academic_writing` recebido pela função; não há descrição individual explícita no código. |
| `latex_extra_path` | positional-or-keyword | `Path \| None` | `None` | Caminho de arquivo ou diretório utilizado pela operação. |
| `pdf_engine` | positional-or-keyword | `str` | `'lualatex'` | Parâmetro `pdf_engine` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `pdf`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Executa processo ou ferramenta externa.
- Interage com o usuário ou escreve saída em terminal/interface.
- Lê configuração/credenciais do ambiente do processo.

#### Chamadas realizadas pelo corpo direto

- `FileNotFoundError`
- `RuntimeError`
- `_safe_engine`
- `academic_writing.exists`
- `elisp.write_text`
- `env.get`
- `err.write_text`
- `latex_extra_path.exists`
- `org_path.exists`
- `org_path.resolve`
- `org_path.with_name`
- `org_path.with_suffix`
- `os.environ.copy`
- `p.is_file`
- `p.resolve`
- `pdf.exists`
- `print`
- `shutil.which`
- `str`
- `subprocess.run`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_ap003e_body_render_external_prisma_outputs_1`
- `app_bundle.scripts.pipeline.prisma_pipeline:run_prisma_report_outputs`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `FileNotFoundError(org_path)`
- `RuntimeError('Emacs não encontrado no PATH.')`
- `RuntimeError(f'Falha ao exportar PDF. Log: {err}')`
- `RuntimeError(f'PDF não foi criado: {pdf}')`

#### Opções CLI mencionadas diretamente no corpo

- `--batch`

#### Artefatos/caminhos literais reconhecidos no corpo

- `Exporta ORG para PDF via Emacs batch + LaTeX/Biber.      `pdf_engine` agora vem do TOML ([latex].pdf_engine) em vez de ficar     fixo em lualatex. O padrão continua lualatex porque é o fluxo validado.     `
- `_pdf_erro.txt`
- `.pdf`

#### Complexidade estrutural observada

```yaml
branches: 8
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `ad4dd565018e387f79b76f3e635550b99ffc29969092a69af61ec7db3e06f42d`
**LOC do arquivo-fonte:** 78


### `app_bundle.scripts.pipeline.mindmap_manager:_normalize_level_colors`

**Assinatura:**
```python
def _normalize_level_colors(raw: Any) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/mindmap_manager.py`
**Linhas:** 49–75
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Resolve a paleta de cores por nível do mapa mental.

**Docstring resumida:** Resolve a paleta de cores por nível do mapa mental.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `raw` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `raw` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `vals or DEFAULT_MINDMAP_LEVEL_COLORS[:]`; `ordered or DEFAULT_MINDMAP_LEVEL_COLORS[:]`; `DEFAULT_MINDMAP_LEVEL_COLORS[:]`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `int`
- `isinstance`
- `items.append`
- `m.group`
- `raw.items`
- `re.search`
- `sorted`
- `str`
- `str(v).strip`
- `str(x).strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.mindmap_manager:colorize_mindmap_plantuml`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 1
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `8e60d620392d0d3ffc68330dc454a7d9991bef8fff2211b5e8f8e2e2753ccad9`
**LOC do arquivo-fonte:** 360


### `app_bundle.scripts.pipeline.mindmap_manager:colorize_mindmap_plantuml`

**Assinatura:**
```python
def colorize_mindmap_plantuml(code: str, cfg: dict[str, Any]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/mindmap_manager.py`
**Linhas:** 78–122
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Aplica cores determinísticas por nível aos nós do PlantUML mindmap.

**Docstring resumida:** Aplica cores determinísticas por nível aos nós do PlantUML mindmap.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `code` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `code` recebido pela função; não há descrição individual explícita no código. |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `code`; `'\n'.join(out).strip() + '\n'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `'\n'.join(out).strip`
- `(code or '').splitlines`
- `_normalize_level_colors`
- `bool`
- `f'{indent}{markers}[{color}]{sep}{rest_clean}'.rstrip`
- `len`
- `line.lstrip`
- `m.groups`
- `max`
- `min`
- `mindmap_config`
- `mm.get`
- `out.append`
- `re.match`
- `re.sub`
- `rest.lstrip`
- `rest.lstrip().startswith`
- `stripped.startswith`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.mindmap_manager:generate_and_attach_mindmap`
- `app_bundle.scripts.pipeline.mindmap_manager:render_existing_mindmap`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `8e60d620392d0d3ffc68330dc454a7d9991bef8fff2211b5e8f8e2e2753ccad9`
**LOC do arquivo-fonte:** 360


### `app_bundle.scripts.pipeline.mindmap_manager:mindmap_config`

**Assinatura:**
```python
def mindmap_config(cfg: dict[str, Any]) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/mindmap_manager.py`
**Linhas:** 125–127
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `mindmap_config` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `mm`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `cfg.get`
- `isinstance`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.mindmap_manager:attach_existing_mindmap_if_available`
- `app_bundle.scripts.pipeline.mindmap_manager:attach_mindmap_figure`
- `app_bundle.scripts.pipeline.mindmap_manager:colorize_mindmap_plantuml`
- `app_bundle.scripts.pipeline.mindmap_manager:generate_and_attach_mindmap`
- `app_bundle.scripts.pipeline.mindmap_manager:mindmap_artifact_paths`
- `app_bundle.scripts.pipeline.mindmap_manager:mindmap_output_paths`
- `app_bundle.scripts.pipeline.mindmap_manager:render_existing_mindmap`
- `app_bundle.scripts.pipeline.mindmap_manager:render_plantuml`
- `app_bundle.scripts.pipeline.mindmap_manager:should_generate_mindmap`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `8e60d620392d0d3ffc68330dc454a7d9991bef8fff2211b5e8f8e2e2753ccad9`
**LOC do arquivo-fonte:** 360


### `app_bundle.scripts.pipeline.mindmap_manager:should_generate_mindmap`

**Assinatura:**
```python
def should_generate_mindmap(cfg: dict[str, Any]) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/mindmap_manager.py`
**Linhas:** 130–132
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Determina se deve `generate_mindmap` conforme as regras codificadas na rotina.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `bool(mm.get('gerar') or mm.get('enabled') or mm.get('ativo'))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `bool`
- `mindmap_config`
- `mm.get`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.default_runtime:_ap003f_pipeline_core`
- `app_bundle.scripts.pipeline.mindmap_manager:attach_existing_mindmap_if_available`
- `app_bundle.scripts.pipeline.mindmap_manager:generate_and_attach_mindmap`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `8e60d620392d0d3ffc68330dc454a7d9991bef8fff2211b5e8f8e2e2753ccad9`
**LOC do arquivo-fonte:** 360


### `app_bundle.scripts.pipeline.mindmap_manager:sanitize_plantuml`

**Assinatura:**
```python
def sanitize_plantuml(text: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/mindmap_manager.py`
**Linhas:** 135–144
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Saneia a responsabilidade representada por `plantuml` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `raw.strip() + '\n'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `m.group`
- `raw.lower`
- `raw.lower().endswith`
- `raw.lower().startswith`
- `raw.strip`
- `re.search`
- `re.sub`
- `re.sub('(?is)^```(?:plantuml)?\\s*|```$', '', text or '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.mindmap_manager:generate_and_attach_mindmap`
- `app_bundle.scripts.pipeline.mindmap_manager:render_existing_mindmap`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `8e60d620392d0d3ffc68330dc454a7d9991bef8fff2211b5e8f8e2e2753ccad9`
**LOC do arquivo-fonte:** 360


### `app_bundle.scripts.pipeline.mindmap_manager:build_mindmap_prompt`

**Assinatura:**
```python
def build_mindmap_prompt(doc: AcademicDocument, cfg: dict[str, Any] | None = None) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/mindmap_manager.py`
**Linhas:** 147–159
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Constrói a responsabilidade representada por `mindmap_prompt` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doc` | positional-or-keyword | `AcademicDocument` | `obrigatório/ausente` | Documento/registro individual processado pela rotina. |
| `cfg` | positional-or-keyword | `dict[str, Any] \| None` | `None` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `f'\nGere um mapa mental em PlantUML mindmap para o documento canônico abaixo.\nRegras: retorne apenas código PlantUML @startmindmap ... @endmindmap; use rótulos curtos; não use citações; no máximo 45 nós. Não use blocos de estilo complexos; as cores serão aplicadas automaticamente pelo pipeline.\n\nDiretivas complementares carregadas pelo prompt bank:\n{prompt_extras}\n\nDocumento:\n{shorten_text(doc.model_dump_json(), 45000)}\n'.strip()`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `doc.model_dump_json`
- `f'\nGere um mapa mental em PlantUML mindmap para o documento canônico abaixo.\nRegras: retorne apenas código PlantUML @startmindmap ... @endmindmap; use rótulos curtos; não use citações; no máximo 45 nós. Não use blocos de estilo complexos; as cores serão aplicadas automaticamente pelo pipeline.\n\nDiretivas complementares carregadas pelo prompt bank:\n{prompt_extras}\n\nDocumento:\n{shorten_text(doc.model_dump_json(), 45000)}\n'.strip`
- `load_prompt_bundle`
- `shorten_text`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.mindmap_manager:generate_and_attach_mindmap`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `8e60d620392d0d3ffc68330dc454a7d9991bef8fff2211b5e8f8e2e2753ccad9`
**LOC do arquivo-fonte:** 360


### `app_bundle.scripts.pipeline.mindmap_manager:mindmap_artifact_paths`

**Assinatura:**
```python
def mindmap_artifact_paths(cfg: dict[str, Any], output_dir: Path) -> dict[str, Path]
```

**Arquivo:** `app_bundle/scripts/pipeline/mindmap_manager.py`
**Linhas:** 164–183
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Resolve caminhos padrão dos artefatos do mapa mental.

**Docstring resumida:** Resolve caminhos padrão dos artefatos do mapa mental.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `output_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Diretório onde a rotina grava ou procura os artefatos de saída. |

#### Retorno

Anotação declarada: `dict[str, Path]`. Retornos observados estaticamente incluem: `{'images_dir': images_dir, 'puml': images_dir / f'{stem}.puml', 'image': images_dir / f'{stem}.{formato}'}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `mindmap_config`
- `mm.get`
- `slugify`
- `str`
- `str(mm.get('formato') or 'png').lower`
- `str(mm.get('formato') or 'png').lower().lstrip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.mindmap_manager:generate_and_attach_mindmap`
- `app_bundle.scripts.pipeline.mindmap_manager:render_existing_mindmap`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.puml`

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `8e60d620392d0d3ffc68330dc454a7d9991bef8fff2211b5e8f8e2e2753ccad9`
**LOC do arquivo-fonte:** 360


### `app_bundle.scripts.pipeline.mindmap_manager:attach_mindmap_figure`

**Assinatura:**
```python
def attach_mindmap_figure(doc: AcademicDocument, cfg: dict[str, Any], output_dir: Path, image_path: Path) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/mindmap_manager.py`
**Linhas:** 186–201
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Anexa/atualiza a figura do mapa mental no document_model.

**Docstring resumida:** Anexa/atualiza a figura do mapa mental no document_model.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doc` | positional-or-keyword | `AcademicDocument` | `obrigatório/ausente` | Documento/registro individual processado pela rotina. |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `output_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Diretório onde a rotina grava ou procura os artefatos de saída. |
| `image_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `FigureSpec`
- `doc.figures.append`
- `image_path.relative_to`
- `mindmap_config`
- `mm.get`
- `str`
- `str(image_path.relative_to(output_dir)).replace`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.mindmap_manager:generate_and_attach_mindmap`
- `app_bundle.scripts.pipeline.mindmap_manager:render_existing_mindmap`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `8e60d620392d0d3ffc68330dc454a7d9991bef8fff2211b5e8f8e2e2753ccad9`
**LOC do arquivo-fonte:** 360


### `app_bundle.scripts.pipeline.mindmap_manager:render_existing_mindmap`

**Assinatura:**
```python
def render_existing_mindmap(cfg: dict[str, Any], doc: AcademicDocument, output_dir: Path) -> dict[str, Any] | None
```

**Arquivo:** `app_bundle/scripts/pipeline/mindmap_manager.py`
**Linhas:** 204–226
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Recolore e renderiza um .puml existente, sem chamar IA.

**Docstring resumida:** Recolore e renderiza um .puml existente, sem chamar IA.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `doc` | positional-or-keyword | `AcademicDocument` | `obrigatório/ausente` | Documento/registro individual processado pela rotina. |
| `output_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Diretório onde a rotina grava ou procura os artefatos de saída. |

#### Retorno

Anotação declarada: `dict[str, Any] | None`. Retornos observados estaticamente incluem: `None`; `{'mode': 'existing_puml', 'puml_path': str(puml), 'image_path': str(img) if img else None, 'error': err}`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `RuntimeError`
- `attach_mindmap_figure`
- `bool`
- `colorize_mindmap_plantuml`
- `mindmap_artifact_paths`
- `mindmap_config`
- `mindmap_config(cfg).get`
- `puml.exists`
- `puml.read_text`
- `render_plantuml`
- `sanitize_plantuml`
- `str`
- `write_text`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.mindmap_manager:render_or_generate_mindmap`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError(err)`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `Recolore e renderiza um .puml existente, sem chamar IA.      Retorna diagnóstico quando o .puml existe. Retorna None se não há .puml.     `

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `8e60d620392d0d3ffc68330dc454a7d9991bef8fff2211b5e8f8e2e2753ccad9`
**LOC do arquivo-fonte:** 360


### `app_bundle.scripts.pipeline.mindmap_manager:render_or_generate_mindmap`

**Assinatura:**
```python
def render_or_generate_mindmap(client: Any | None, model: str, cfg: dict[str, Any], doc: AcademicDocument, output_dir: Path, *, force_regenerate: bool = False, prefer_existing: bool = True) -> dict[str, Any] | None
```

**Arquivo:** `app_bundle/scripts/pipeline/mindmap_manager.py`
**Linhas:** 229–257
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Renderiza ou gera o mapa mental como etapa autônoma.

**Docstring resumida:** Renderiza ou gera o mapa mental como etapa autônoma.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `client` | positional-or-keyword | `Any \| None` | `obrigatório/ausente` | Cliente de serviço externo/IA utilizado pela rotina; pode ser opcional conforme o fluxo. |
| `model` | positional-or-keyword | `str` | `obrigatório/ausente` | Identificador do modelo utilizado pela etapa de IA. |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `doc` | positional-or-keyword | `AcademicDocument` | `obrigatório/ausente` | Documento/registro individual processado pela rotina. |
| `output_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Diretório onde a rotina grava ou procura os artefatos de saída. |
| `force_regenerate` | keyword-only | `bool` | `False` | Parâmetro `force_regenerate` recebido pela função; não há descrição individual explícita no código. |
| `prefer_existing` | keyword-only | `bool` | `True` | Parâmetro `prefer_existing` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, Any] | None`. Retornos observados estaticamente incluem: `diag`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `RuntimeError`
- `generate_and_attach_mindmap`
- `render_existing_mindmap`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('Mapa mental ainda não possui .puml existente. Para gerar novo mapa, rode com OPENAI_API_KEY disponível ou use --forcar-regeneracao-mapa-mental.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `Mapa mental ainda não possui .puml existente. Para gerar novo mapa, rode com OPENAI_API_KEY disponível ou use --forcar-regeneracao-mapa-mental.`

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `8e60d620392d0d3ffc68330dc454a7d9991bef8fff2211b5e8f8e2e2753ccad9`
**LOC do arquivo-fonte:** 360


### `app_bundle.scripts.pipeline.mindmap_manager:render_plantuml`

**Assinatura:**
```python
def render_plantuml(puml_path: Path, cfg: dict[str, Any]) -> tuple[Path | None, str | None]
```

**Arquivo:** `app_bundle/scripts/pipeline/mindmap_manager.py`
**Linhas:** 259–281
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Renderiza a responsabilidade representada por `plantuml` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `puml_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `tuple[Path | None, str | None]`. Retornos observados estaticamente incluem: `(None, 'PlantUML não encontrado. Informe [mapa_mental].plantuml_jar_path ou instale plantuml.')`; `(output, None)`; `(output if output.exists() else None, '\n---\n'.join(errors))`.

#### Efeitos colaterais e fronteiras externas

- Executa processo ou ferramenta externa.
- Lê configuração/credenciais do ambiente do processo.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `'\n---\n'.join`
- `(cfg.get('documento', {}) if isinstance(cfg.get('documento'), dict) else {}).get`
- `Path`
- `Path(str(jar)).expanduser`
- `Path(str(jar)).expanduser().exists`
- `cfg.get`
- `cmds.append`
- `errors.append`
- `int`
- `isinstance`
- `mindmap_config`
- `mm.get`
- `os.environ.copy`
- `os.getenv`
- `output.exists`
- `puml_path.with_suffix`
- `shutil.which`
- `str`
- `str(mm.get('formato') or 'png').lower`
- `str(mm.get('formato') or 'png').lower().lstrip`
- `subprocess.run`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.mindmap_manager:generate_and_attach_mindmap`
- `app_bundle.scripts.pipeline.mindmap_manager:render_existing_mindmap`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 7
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `8e60d620392d0d3ffc68330dc454a7d9991bef8fff2211b5e8f8e2e2753ccad9`
**LOC do arquivo-fonte:** 360


### `app_bundle.scripts.pipeline.mindmap_manager:mindmap_output_paths`

**Assinatura:**
```python
def mindmap_output_paths(cfg: dict[str, Any], output_dir: Path) -> dict[str, Path]
```

**Arquivo:** `app_bundle/scripts/pipeline/mindmap_manager.py`
**Linhas:** 285–299
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Retorna caminhos previstos do mapa mental sem chamar a IA.

**Docstring resumida:** Retorna caminhos previstos do mapa mental sem chamar a IA.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `output_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Diretório onde a rotina grava ou procura os artefatos de saída. |

#### Retorno

Anotação declarada: `dict[str, Path]`. Retornos observados estaticamente incluem: `{'images_dir': images_dir, 'puml': images_dir / f'{stem}.puml', 'image': images_dir / f'{stem}.{formato}', 'png': images_dir / f'{stem}.png', 'svg': images_dir / f'{stem}.svg'}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `mindmap_config`
- `mm.get`
- `slugify`
- `str`
- `str(mm.get('formato') or 'png').lower`
- `str(mm.get('formato') or 'png').lower().lstrip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.mindmap_manager:attach_existing_mindmap_if_available`
- `app_bundle.scripts.pipeline.mindmap_manager:delete_existing_mindmap_outputs`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.puml`
- `.png`
- `.svg`

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `8e60d620392d0d3ffc68330dc454a7d9991bef8fff2211b5e8f8e2e2753ccad9`
**LOC do arquivo-fonte:** 360


### `app_bundle.scripts.pipeline.mindmap_manager:delete_existing_mindmap_outputs`

**Assinatura:**
```python
def delete_existing_mindmap_outputs(cfg: dict[str, Any], output_dir: Path) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/mindmap_manager.py`
**Linhas:** 302–311
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Remove arquivos conhecidos do mapa mental para forçar regeneração.

**Docstring resumida:** Remove arquivos conhecidos do mapa mental para forçar regeneração.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `output_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Diretório onde a rotina grava ou procura os artefatos de saída. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `sorted(set(removed))`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `mindmap_output_paths`
- `p.exists`
- `p.is_file`
- `p.unlink`
- `paths.get`
- `removed.append`
- `set`
- `sorted`
- `str`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.default_runtime:_ap003f_pipeline_core`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `8e60d620392d0d3ffc68330dc454a7d9991bef8fff2211b5e8f8e2e2753ccad9`
**LOC do arquivo-fonte:** 360


### `app_bundle.scripts.pipeline.mindmap_manager:attach_existing_mindmap_if_available`

**Assinatura:**
```python
def attach_existing_mindmap_if_available(doc: AcademicDocument, cfg: dict[str, Any], output_dir: Path) -> dict[str, Any] | None
```

**Arquivo:** `app_bundle/scripts/pipeline/mindmap_manager.py`
**Linhas:** 314–337
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Anexa ao document_model uma imagem de mapa mental já existente.

**Docstring resumida:** Anexa ao document_model uma imagem de mapa mental já existente.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doc` | positional-or-keyword | `AcademicDocument` | `obrigatório/ausente` | Documento/registro individual processado pela rotina. |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `output_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Diretório onde a rotina grava ou procura os artefatos de saída. |

#### Retorno

Anotação declarada: `dict[str, Any] | None`. Retornos observados estaticamente incluem: `None`; `diag`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `FigureSpec`
- `doc.figures.append`
- `img.relative_to`
- `load_prompt_bundle`
- `load_prompt_bundle(cfg, 'mindmap').report`
- `mindmap_config`
- `mindmap_output_paths`
- `mm.get`
- `next`
- `p.exists`
- `p.is_file`
- `paths.get`
- `should_generate_mindmap`
- `str`
- `str(img.relative_to(output_dir)).replace`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.default_runtime:_ap003f_pipeline_core`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 2
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `8e60d620392d0d3ffc68330dc454a7d9991bef8fff2211b5e8f8e2e2753ccad9`
**LOC do arquivo-fonte:** 360


### `app_bundle.scripts.pipeline.mindmap_manager:generate_and_attach_mindmap`

**Assinatura:**
```python
def generate_and_attach_mindmap(client: Any, model: str, cfg: dict[str, Any], doc: AcademicDocument, output_dir: Path) -> dict[str, Any] | None
```

**Arquivo:** `app_bundle/scripts/pipeline/mindmap_manager.py`
**Linhas:** 339–360
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Gera a responsabilidade representada por `and_attach_mindmap` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `client` | positional-or-keyword | `Any` | `obrigatório/ausente` | Cliente de serviço externo/IA utilizado pela rotina; pode ser opcional conforme o fluxo. |
| `model` | positional-or-keyword | `str` | `obrigatório/ausente` | Identificador do modelo utilizado pela etapa de IA. |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `doc` | positional-or-keyword | `AcademicDocument` | `obrigatório/ausente` | Documento/registro individual processado pela rotina. |
| `output_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Diretório onde a rotina grava ou procura os artefatos de saída. |

#### Retorno

Anotação declarada: `dict[str, Any] | None`. Retornos observados estaticamente incluem: `None`; `diag`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.
- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `RuntimeError`
- `attach_mindmap_figure`
- `bool`
- `build_mindmap_prompt`
- `client.responses.parse`
- `colorize_mindmap_plantuml`
- `images_dir.mkdir`
- `load_prompt_bundle`
- `load_prompt_bundle(cfg, 'mindmap').report`
- `mindmap_artifact_paths`
- `mindmap_config`
- `mm.get`
- `render_plantuml`
- `sanitize_plantuml`
- `should_generate_mindmap`
- `str`
- `write_text`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.default_runtime:_ap003f_pipeline_core`
- `app_bundle.scripts.pipeline.mindmap_manager:render_or_generate_mindmap`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('IA não retornou PlantUML do mapa mental.')`
- `RuntimeError(err)`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `8e60d620392d0d3ffc68330dc454a7d9991bef8fff2211b5e8f8e2e2753ccad9`
**LOC do arquivo-fonte:** 360


### `app_bundle.scripts.pipeline.paper_abstracts:_section`

**Assinatura:**
```python
def _section(cfg: dict[str, Any], name: str) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/paper_abstracts.py`
**Linhas:** 54–56
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_section` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `name` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `name` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `value if isinstance(value, dict) else {}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `cfg.get`
- `isinstance`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e8e0688ef30b7b1be1f424667f9ec676e232f1b262ebe2faae3fd0d4efe02c0e`
**LOC do arquivo-fonte:** 428


### `app_bundle.scripts.pipeline.paper_abstracts:_as_list`

**Assinatura:**
```python
def _as_list(value: Any) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/paper_abstracts.py`
**Linhas:** 59–64
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_as_list` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `[value]`; `[]`; `[str(item).strip() for item in value if str(item).strip()]`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `isinstance`
- `str`
- `str(item).strip`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e8e0688ef30b7b1be1f424667f9ec676e232f1b262ebe2faae3fd0d4efe02c0e`
**LOC do arquivo-fonte:** 428


### `app_bundle.scripts.pipeline.paper_abstracts:paper_abstracts_enabled`

**Assinatura:**
```python
def paper_abstracts_enabled(cfg: dict[str, Any]) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/paper_abstracts.py`
**Linhas:** 67–75
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Retorna se o TOML solicita resumos do paper local.

**Docstring resumida:** Retorna se o TOML solicita resumos do paper local.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `str(project.get('preset') or '').strip() == 'paper_local_fgv' and bool(section.get('ativo', False)) and bool(section.get('gerar_resumo_principal', True))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_section`
- `bool`
- `project.get`
- `section.get`
- `str`
- `str(project.get('preset') or '').strip`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.default_runtime:_ap003f_pipeline_core`
- `app_bundle.scripts.pipeline.paper_abstracts:generate_paper_abstract_bundle`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e8e0688ef30b7b1be1f424667f9ec676e232f1b262ebe2faae3fd0d4efe02c0e`
**LOC do arquivo-fonte:** 428


### `app_bundle.scripts.pipeline.paper_abstracts:abstract_sidecar_path`

**Assinatura:**
```python
def abstract_sidecar_path(out_dir: Path, prefix: str) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/paper_abstracts.py`
**Linhas:** 78–79
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `abstract_sidecar_path` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `out_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `out_dir` recebido pela função; não há descrição individual explícita no código. |
| `prefix` | positional-or-keyword | `str` | `obrigatório/ausente` | Prefixo canônico usado para compor nomes de artefatos. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `out_dir / f'{prefix}.resumos_paper.json'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

Nenhum identificado pela análise estática.

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.default_runtime:_ap003f_pipeline_core`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.resumos_paper.json`

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e8e0688ef30b7b1be1f424667f9ec676e232f1b262ebe2faae3fd0d4efe02c0e`
**LOC do arquivo-fonte:** 428


### `app_bundle.scripts.pipeline.paper_abstracts:requested_abstract_languages`

**Assinatura:**
```python
def requested_abstract_languages(cfg: dict[str, Any], *, include_full_translation_languages: bool = False) -> list[tuple[str, str]]
```

**Arquivo:** `app_bundle/scripts/pipeline/paper_abstracts.py`
**Linhas:** 82–112
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Resolve os idiomas exigidos para a geração do resumo.

**Docstring resumida:** Resolve os idiomas exigidos para a geração do resumo.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `include_full_translation_languages` | keyword-only | `bool` | `False` | Parâmetro `include_full_translation_languages` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[tuple[str, str]]`. Retornos observados estaticamente incluem: `result`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_as_list`
- `_section`
- `_section(cfg, 'idiomas_saida').get`
- `bool`
- `normalize_language`
- `requested_translation_languages`
- `result.append`
- `section.get`
- `seen.add`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.paper_abstracts:generate_paper_abstract_bundle`
- `app_bundle.scripts.pipeline.paper_abstracts:main_document_abstract_languages`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e8e0688ef30b7b1be1f424667f9ec676e232f1b262ebe2faae3fd0d4efe02c0e`
**LOC do arquivo-fonte:** 428


### `app_bundle.scripts.pipeline.paper_abstracts:main_document_abstract_languages`

**Assinatura:**
```python
def main_document_abstract_languages(cfg: dict[str, Any]) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/paper_abstracts.py`
**Linhas:** 115–117
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Idiomas que devem aparecer no paper principal, não nas cópias traduzidas.

**Docstring resumida:** Idiomas que devem aparecer no paper principal, não nas cópias traduzidas.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `[code for code, _label in requested_abstract_languages(cfg, include_full_translation_languages=False)]`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `requested_abstract_languages`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e8e0688ef30b7b1be1f424667f9ec676e232f1b262ebe2faae3fd0d4efe02c0e`
**LOC do arquivo-fonte:** 428


### `app_bundle.scripts.pipeline.paper_abstracts:_labels_for_language`

**Assinatura:**
```python
def _labels_for_language(code: str, label: str) -> tuple[str, str]
```

**Arquivo:** `app_bundle/scripts/pipeline/paper_abstracts.py`
**Linhas:** 120–127
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_labels_for_language` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `code` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `code` recebido pela função; não há descrição individual explícita no código. |
| `label` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `label` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `tuple[str, str]`. Retornos observados estaticamente incluem: `_LANGUAGE_LABELS[key]`; `_LANGUAGE_LABELS[primary]`; `(f'Resumo ({label})', 'Palavras-chave')`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `key.split`
- `str`
- `str(code or '').casefold`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.paper_abstracts:generate_paper_abstract_bundle`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e8e0688ef30b7b1be1f424667f9ec676e232f1b262ebe2faae3fd0d4efe02c0e`
**LOC do arquivo-fonte:** 428


### `app_bundle.scripts.pipeline.paper_abstracts:_is_protected`

**Assinatura:**
```python
def _is_protected(path: tuple[str | int, ...]) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/paper_abstracts.py`
**Linhas:** 130–132
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Verifica se `protected` conforme as regras codificadas na rotina.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `tuple[str \| int, ...]` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `bool(tokens & _PROTECTED_PATH_TOKENS)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `bool`
- `isinstance`
- `str`
- `str(item).casefold`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.paper_abstracts:_collect_source_strings.walk`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e8e0688ef30b7b1be1f424667f9ec676e232f1b262ebe2faae3fd0d4efe02c0e`
**LOC do arquivo-fonte:** 428


### `app_bundle.scripts.pipeline.paper_abstracts:_looks_like_text`

**Assinatura:**
```python
def _looks_like_text(value: str) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/paper_abstracts.py`
**Linhas:** 135–141
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_looks_like_text` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `False`; `True`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `len`
- `re.fullmatch`
- `re.search`
- `value.strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.paper_abstracts:_collect_source_strings.walk`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e8e0688ef30b7b1be1f424667f9ec676e232f1b262ebe2faae3fd0d4efe02c0e`
**LOC do arquivo-fonte:** 428


### `app_bundle.scripts.pipeline.paper_abstracts:_collect_source_strings`

**Assinatura:**
```python
def _collect_source_strings(payload: Any) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/paper_abstracts.py`
**Linhas:** 144–165
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Extrai redação substantiva, excluindo bibliografia e metadados técnicos.

**Docstring resumida:** Extrai redação substantiva, excluindo bibliografia e metadados técnicos.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `payload` | positional-or-keyword | `Any` | `obrigatório/ausente` | Payload estruturado recebido ou produzido pela operação. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `[item for item in values if not (item in seen or seen.add(item))]`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `seen.add`
- `set`
- `walk`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.paper_abstracts:_source_for_abstract`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e8e0688ef30b7b1be1f424667f9ec676e232f1b262ebe2faae3fd0d4efe02c0e`
**LOC do arquivo-fonte:** 428


### `app_bundle.scripts.pipeline.paper_abstracts:_collect_source_strings.walk`

**Assinatura:**
```python
def walk(value: Any, path: tuple[str | int, ...]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/paper_abstracts.py`
**Linhas:** 148–160
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `walk` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |
| `path` | positional-or-keyword | `tuple[str \| int, ...]` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_is_protected`
- `_looks_like_text`
- `enumerate`
- `isinstance`
- `re.sub`
- `re.sub('\\s+', ' ', value).strip`
- `str`
- `value.items`
- `values.append`
- `walk`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e8e0688ef30b7b1be1f424667f9ec676e232f1b262ebe2faae3fd0d4efe02c0e`
**LOC do arquivo-fonte:** 428


### `app_bundle.scripts.pipeline.paper_abstracts:_source_for_abstract`

**Assinatura:**
```python
def _source_for_abstract(document: Any, *, max_chars: int = 65000) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/paper_abstracts.py`
**Linhas:** 168–190
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_source_for_abstract` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `document` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `document` recebido pela função; não há descrição individual explícita no código. |
| `max_chars` | keyword-only | `int` | `65000` | Parâmetro `max_chars` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `source`; `source[:first] + '\n\n[trecho intermediário do paper]\n\n' + source[midpoint:midpoint + middle] + '\n\n[trecho final do paper]\n\n' + source[-last:]`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'\n\n'.join`
- `'\n\n'.join(pieces).strip`
- `PaperAbstractError`
- `_collect_source_strings`
- `document.model_dump`
- `len`
- `max`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.paper_abstracts:generate_paper_abstract_bundle`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `PaperAbstractError('Não encontrei conteúdo textual suficiente no document.json para gerar o resumo.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `Não encontrei conteúdo textual suficiente no document.json para gerar o resumo.`

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `e8e0688ef30b7b1be1f424667f9ec676e232f1b262ebe2faae3fd0d4efe02c0e`
**LOC do arquivo-fonte:** 428


### `app_bundle.scripts.pipeline.paper_abstracts:_extract_response_content`

**Assinatura:**
```python
def _extract_response_content(response: Any) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/paper_abstracts.py`
**Linhas:** 193–203
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Extrai a responsabilidade representada por `response_content` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `response` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `response` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `str(content or '').strip()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `''.join`
- `PaperAbstractError`
- `getattr`
- `isinstance`
- `item.get`
- `str`
- `str(content or '').strip`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `PaperAbstractError('A IA não devolveu conteúdo utilizável para o resumo.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 1
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `e8e0688ef30b7b1be1f424667f9ec676e232f1b262ebe2faae3fd0d4efe02c0e`
**LOC do arquivo-fonte:** 428


### `app_bundle.scripts.pipeline.paper_abstracts:_request_abstracts`

**Assinatura:**
```python
def _request_abstracts(client: Any, model: str, languages: list[tuple[str, str]], source: str, *, max_words: int) -> dict[str, dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/paper_abstracts.py`
**Linhas:** 206–260
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_request_abstracts` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `client` | positional-or-keyword | `Any` | `obrigatório/ausente` | Cliente de serviço externo/IA utilizado pela rotina; pode ser opcional conforme o fluxo. |
| `model` | positional-or-keyword | `str` | `obrigatório/ausente` | Identificador do modelo utilizado pela etapa de IA. |
| `languages` | positional-or-keyword | `list[tuple[str, str]]` | `obrigatório/ausente` | Parâmetro `languages` recebido pela função; não há descrição individual explícita no código. |
| `source` | positional-or-keyword | `str` | `obrigatório/ausente` | Origem dos dados ou arquivo de entrada. |
| `max_words` | keyword-only | `int` | `obrigatório/ausente` | Parâmetro `max_words` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, dict[str, Any]]`. Retornos observados estaticamente incluem: `by_code`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `', '.join`
- `PaperAbstractError`
- `_as_list`
- `_extract_response_content`
- `client.chat.completions.create`
- `isinstance`
- `json.dumps`
- `json.loads`
- `normalize_language`
- `parsed.get`
- `re.sub`
- `re.sub('\\s+', ' ', str(item)).strip`
- `re.sub('\\s+', ' ', str(row.get('abstract') or '')).strip`
- `row.get`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.paper_abstracts:generate_paper_abstract_bundle`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `PaperAbstractError('A IA devolveu o resumo fora do formato JSON esperado.')`
- `PaperAbstractError('A IA não devolveu a lista abstracts esperada.')`
- `PaperAbstractError('A IA não devolveu resumo para todos os idiomas: ' + ', '.join(missing))`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 1
comprehensions: 4
exception_handlers: 2
```

**SHA-256 do arquivo-fonte:** `e8e0688ef30b7b1be1f424667f9ec676e232f1b262ebe2faae3fd0d4efe02c0e`
**LOC do arquivo-fonte:** 428


### `app_bundle.scripts.pipeline.paper_abstracts:generate_paper_abstract_bundle`

**Assinatura:**
```python
def generate_paper_abstract_bundle(client: Any, model: str, document: Any, cfg: dict[str, Any]) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/paper_abstracts.py`
**Linhas:** 263–298
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Gera e estrutura os resumos do paper principal e das versões integrais.

**Docstring resumida:** Gera e estrutura os resumos do paper principal e das versões integrais.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `client` | positional-or-keyword | `Any` | `obrigatório/ausente` | Cliente de serviço externo/IA utilizado pela rotina; pode ser opcional conforme o fluxo. |
| `model` | positional-or-keyword | `str` | `obrigatório/ausente` | Identificador do modelo utilizado pela etapa de IA. |
| `document` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `document` recebido pela função; não há descrição individual explícita no código. |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `{}`; `{'schema_version': '1.0', 'generated_at': datetime.now(timezone.utc).isoformat(timespec='seconds'), 'model': model, 'source_method': 'síntese do document.json canônico; sem nova análise do corpus', 'items': items}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_labels_for_language`
- `_request_abstracts`
- `_section`
- `_source_for_abstract`
- `bool`
- `datetime.now`
- `datetime.now(timezone.utc).isoformat`
- `dict`
- `int`
- `max`
- `min`
- `paper_abstracts_enabled`
- `requested_abstract_languages`
- `row.update`
- `section.get`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.default_runtime:_ap003f_pipeline_core`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `síntese do document.json canônico; sem nova análise do corpus`

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `e8e0688ef30b7b1be1f424667f9ec676e232f1b262ebe2faae3fd0d4efe02c0e`
**LOC do arquivo-fonte:** 428


### `app_bundle.scripts.pipeline.paper_abstracts:read_paper_abstract_bundle`

**Assinatura:**
```python
def read_paper_abstract_bundle(path: Path) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/paper_abstracts.py`
**Linhas:** 301–308
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Lê a responsabilidade representada por `paper_abstract_bundle` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `payload`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `PaperAbstractError`
- `isinstance`
- `json.loads`
- `path.read_text`
- `payload.get`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.default_runtime:_ap003f_pipeline_core`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `PaperAbstractError(f'Não foi possível ler o arquivo de resumos: {path}')`
- `PaperAbstractError('O arquivo de resumos possui formato inválido.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `e8e0688ef30b7b1be1f424667f9ec676e232f1b262ebe2faae3fd0d4efe02c0e`
**LOC do arquivo-fonte:** 428


### `app_bundle.scripts.pipeline.paper_abstracts:write_paper_abstract_bundle`

**Assinatura:**
```python
def write_paper_abstract_bundle(path: Path, bundle: dict[str, Any]) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/paper_abstracts.py`
**Linhas:** 311–314
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Grava a responsabilidade representada por `paper_abstract_bundle` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `bundle` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `bundle` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `path`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `json.dumps`
- `path.parent.mkdir`
- `path.write_text`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.default_runtime:_ap003f_pipeline_core`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e8e0688ef30b7b1be1f424667f9ec676e232f1b262ebe2faae3fd0d4efe02c0e`
**LOC do arquivo-fonte:** 428


### `app_bundle.scripts.pipeline.paper_abstracts:_bundle_items`

**Assinatura:**
```python
def _bundle_items(bundle: dict[str, Any], language_codes: Iterable[str]) -> list[dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/paper_abstracts.py`
**Linhas:** 317–330
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_bundle_items` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `bundle` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `bundle` recebido pela função; não há descrição individual explícita no código. |
| `language_codes` | positional-or-keyword | `Iterable[str]` | `obrigatório/ausente` | Parâmetro `language_codes` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[dict[str, Any]]`. Retornos observados estaticamente incluem: `[]`; `output`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `bundle.get`
- `isinstance`
- `items.get`
- `normalize_language`
- `output.append`
- `row.get`
- `str`
- `str(row.get('abstract') or '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.paper_abstracts:inject_paper_abstracts_into_docx`
- `app_bundle.scripts.pipeline.paper_abstracts:inject_paper_abstracts_into_org`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `e8e0688ef30b7b1be1f424667f9ec676e232f1b262ebe2faae3fd0d4efe02c0e`
**LOC do arquivo-fonte:** 428


### `app_bundle.scripts.pipeline.paper_abstracts:_org_block`

**Assinatura:**
```python
def _org_block(rows: list[dict[str, Any]]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/paper_abstracts.py`
**Linhas:** 337–355
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_org_block` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `rows` | positional-or-keyword | `list[dict[str, Any]]` | `obrigatório/ausente` | Parâmetro `rows` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'\n'.join(parts).rstrip() + '\n\n'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'; '.join`
- `'\n'.join`
- `'\n'.join(parts).rstrip`
- `_as_list`
- `bool`
- `parts.append`
- `parts.extend`
- `row.get`
- `str`
- `str(item).strip`
- `str(row.get('abstract') or '').strip`
- `str(row.get('heading') or 'Resumo').strip`
- `str(row.get('keywords_heading') or 'Palavras-chave').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.paper_abstracts:inject_paper_abstracts_into_org`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e8e0688ef30b7b1be1f424667f9ec676e232f1b262ebe2faae3fd0d4efe02c0e`
**LOC do arquivo-fonte:** 428


### `app_bundle.scripts.pipeline.paper_abstracts:inject_paper_abstracts_into_org`

**Assinatura:**
```python
def inject_paper_abstracts_into_org(org_path: Path, bundle: dict[str, Any], language_codes: Iterable[str]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/paper_abstracts.py`
**Linhas:** 358–380
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Insere blocos de resumo antes da primeira seção do ORG, de modo idempotente.

**Docstring resumida:** Insere blocos de resumo antes da primeira seção do ORG, de modo idempotente.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `org_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `bundle` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `bundle` recebido pela função; não há descrição individual explícita no código. |
| `language_codes` | positional-or-keyword | `Iterable[str]` | `obrigatório/ausente` | Parâmetro `language_codes` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `text`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `(text[:start] + text[end:]).lstrip`
- `_bundle_items`
- `_org_block`
- `len`
- `match.start`
- `org_path.read_text`
- `org_path.write_text`
- `re.search`
- `text.index`
- `text.rstrip`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e8e0688ef30b7b1be1f424667f9ec676e232f1b262ebe2faae3fd0d4efe02c0e`
**LOC do arquivo-fonte:** 428


### `app_bundle.scripts.pipeline.paper_abstracts:_docx_anchor`

**Assinatura:**
```python
def _docx_anchor(document: Any) -> Any | None
```

**Arquivo:** `app_bundle/scripts/pipeline/paper_abstracts.py`
**Linhas:** 383–395
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Busca a primeira seção de conteúdo, preservando a capa do DOCX.

**Docstring resumida:** Busca a primeira seção de conteúdo, preservando a capa do DOCX.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `document` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `document` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Any | None`. Retornos observados estaticamente incluem: `paragraph`; `None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `getattr`
- `re.match`
- `str`
- `str(getattr(getattr(paragraph, 'style', None), 'name', '') or '').casefold`
- `str(getattr(paragraph, 'text', '') or '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.paper_abstracts:inject_paper_abstracts_into_docx`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `e8e0688ef30b7b1be1f424667f9ec676e232f1b262ebe2faae3fd0d4efe02c0e`
**LOC do arquivo-fonte:** 428


### `app_bundle.scripts.pipeline.paper_abstracts:inject_paper_abstracts_into_docx`

**Assinatura:**
```python
def inject_paper_abstracts_into_docx(docx_path: Path, bundle: dict[str, Any], language_codes: Iterable[str]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/paper_abstracts.py`
**Linhas:** 398–428
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Insere os resumos antes da primeira seção do conteúdo no DOCX renderizado.

**Docstring resumida:** Insere os resumos antes da primeira seção do conteúdo no DOCX renderizado.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `docx_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `bundle` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `bundle` recebido pela função; não há descrição individual explícita no código. |
| `language_codes` | positional-or-keyword | `Iterable[str]` | `obrigatório/ausente` | Parâmetro `language_codes` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `'; '.join`
- `Document`
- `PaperAbstractError`
- `_as_list`
- `_bundle_items`
- `_docx_anchor`
- `anchor._p.addprevious`
- `bool`
- `created.append`
- `document.add_paragraph`
- `document.save`
- `heading.add_run`
- `keyword_paragraph.add_run`
- `row.get`
- `str`
- `str(item).strip`
- `str(row.get('abstract') or '').strip`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `PaperAbstractError('python-docx não está disponível para inserir resumos no DOCX.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 2
comprehensions: 1
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `e8e0688ef30b7b1be1f424667f9ec676e232f1b262ebe2faae3fd0d4efe02c0e`
**LOC do arquivo-fonte:** 428


### `app_bundle.scripts.pipeline.pipeline_orchestrator:main`

**Assinatura:**
```python
def main(argv: list[str] | None = None) -> int
```

**Arquivo:** `app_bundle/scripts/pipeline/pipeline_orchestrator.py`
**Linhas:** 21–23
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Executa a superfície pública canônica preservando argumentos da CLI.

**Docstring resumida:** Executa a superfície pública canônica preservando argumentos da CLI.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `list[str] \| None` | `None` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `int(_ap008f2c_canonical_main(argv))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_ap008f2c_canonical_main`
- `int`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b2cece847fdac97db991c8667c97c53d52146f57875e19ddcb93f995781613bd`
**LOC do arquivo-fonte:** 27


### `app_bundle.scripts.pipeline.preparar_artigo_longo_fulltext:backup`

**Assinatura:**
```python
def backup(path: Path) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/preparar_artigo_longo_fulltext.py`
**Linhas:** 19–23
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `backup` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `datetime.now`
- `datetime.now().strftime`
- `path.exists`
- `path.with_suffix`
- `print`
- `shutil.copy2`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6e846516c6c057aa2958fe53cc8c055ca185177d9a2aee450fa99329c86c6f11`
**LOC do arquivo-fonte:** 389


### `app_bundle.scripts.pipeline.preparar_artigo_longo_fulltext:sniff_csv`

**Assinatura:**
```python
def sniff_csv(path: Path)
```

**Arquivo:** `app_bundle/scripts/pipeline/preparar_artigo_longo_fulltext.py`
**Linhas:** 26–31
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `sniff_csv` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `csv.Sniffer().sniff(sample, delimiters=',;\t')`; `csv.excel`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `csv.Sniffer`
- `csv.Sniffer().sniff`
- `path.read_text`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `6e846516c6c057aa2958fe53cc8c055ca185177d9a2aee450fa99329c86c6f11`
**LOC do arquivo-fonte:** 389


### `app_bundle.scripts.pipeline.preparar_artigo_longo_fulltext:read_csv`

**Assinatura:**
```python
def read_csv(path: Path) -> list[dict[str, str]]
```

**Arquivo:** `app_bundle/scripts/pipeline/preparar_artigo_longo_fulltext.py`
**Linhas:** 34–36
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Lê a responsabilidade representada por `csv` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `list[dict[str, str]]`. Retornos observados estaticamente incluem: `list(csv.DictReader(f, dialect=sniff_csv(path)))`.

#### Efeitos colaterais e fronteiras externas

- Abre arquivo para leitura ou inspeção.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `csv.DictReader`
- `list`
- `path.open`
- `sniff_csv`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6e846516c6c057aa2958fe53cc8c055ca185177d9a2aee450fa99329c86c6f11`
**LOC do arquivo-fonte:** 389


### `app_bundle.scripts.pipeline.preparar_artigo_longo_fulltext:norm_key`

**Assinatura:**
```python
def norm_key(v: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/preparar_artigo_longo_fulltext.py`
**Linhas:** 39–40
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `norm_key` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `v` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `v` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `re.sub('[^a-z0-9]+', '_', str(v or '').lower()).strip('_')`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `re.sub`
- `re.sub('[^a-z0-9]+', '_', str(v or '').lower()).strip`
- `str`
- `str(v or '').lower`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6e846516c6c057aa2958fe53cc8c055ca185177d9a2aee450fa99329c86c6f11`
**LOC do arquivo-fonte:** 389


### `app_bundle.scripts.pipeline.preparar_artigo_longo_fulltext:pick`

**Assinatura:**
```python
def pick(row: dict, *names: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/preparar_artigo_longo_fulltext.py`
**Linhas:** 43–49
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `pick` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `row` | positional-or-keyword | `dict` | `obrigatório/ausente` | Parâmetro `row` recebido pela função; não há descrição individual explícita no código. |
| `names` | var-positional | `str` | `obrigatório/ausente` | Parâmetro `names` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `str(v).strip()`; `''`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `d.get`
- `norm_key`
- `row.items`
- `str`
- `str(v).strip`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6e846516c6c057aa2958fe53cc8c055ca185177d9a2aee450fa99329c86c6f11`
**LOC do arquivo-fonte:** 389


### `app_bundle.scripts.pipeline.preparar_artigo_longo_fulltext:extract_pdf_text`

**Assinatura:**
```python
def extract_pdf_text(path: Path, max_chars: int) -> tuple[str, int]
```

**Arquivo:** `app_bundle/scripts/pipeline/preparar_artigo_longo_fulltext.py`
**Linhas:** 52–72
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Extrai a responsabilidade representada por `pdf_text` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `max_chars` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `max_chars` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `tuple[str, int]`. Retornos observados estaticamente incluem: `('[PDF NÃO ENCONTRADO]', 0)`; `(text[:max_chars], len(reader.pages))`; `(f'[ERRO AO EXTRAIR PDF: {e}]', 0)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `'\n'.join(parts).strip`
- `PdfReader`
- `len`
- `page.extract_text`
- `parts.append`
- `path.exists`
- `str`
- `sum`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.preparar_artigo_longo_fulltext:build_prompt_and_corpus`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 1
exception_handlers: 3
```

**SHA-256 do arquivo-fonte:** `6e846516c6c057aa2958fe53cc8c055ca185177d9a2aee450fa99329c86c6f11`
**LOC do arquivo-fonte:** 389


### `app_bundle.scripts.pipeline.preparar_artigo_longo_fulltext:read_toml_dict`

**Assinatura:**
```python
def read_toml_dict(path: Path) -> dict
```

**Arquivo:** `app_bundle/scripts/pipeline/preparar_artigo_longo_fulltext.py`
**Linhas:** 75–82
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Lê a responsabilidade representada por `toml_dict` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `dict`. Retornos observados estaticamente incluem: `{}`; `tomllib.load(f)`.

#### Efeitos colaterais e fronteiras externas

- Abre arquivo para leitura ou inspeção.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `path.exists`
- `path.open`
- `tomllib.load`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.preparar_artigo_longo_fulltext:patch_toml`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `6e846516c6c057aa2958fe53cc8c055ca185177d9a2aee450fa99329c86c6f11`
**LOC do arquivo-fonte:** 389


### `app_bundle.scripts.pipeline.preparar_artigo_longo_fulltext:find_section`

**Assinatura:**
```python
def find_section(lines: list[str], section: str) -> tuple[int | None, int | None]
```

**Arquivo:** `app_bundle/scripts/pipeline/preparar_artigo_longo_fulltext.py`
**Linhas:** 85–100
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Localiza a responsabilidade representada por `section` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `lines` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `lines` recebido pela função; não há descrição individual explícita no código. |
| `section` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `section` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `tuple[int | None, int | None]`. Retornos observados estaticamente incluem: `(None, None)`; `(start, end)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `enumerate`
- `len`
- `lines[j].strip`
- `ln.strip`
- `range`
- `s.endswith`
- `s.startswith`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.preparar_artigo_longo_fulltext:ensure_section`
- `app_bundle.scripts.pipeline.preparar_artigo_longo_fulltext:insert_after_header`
- `app_bundle.scripts.pipeline.preparar_artigo_longo_fulltext:remove_key`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6e846516c6c057aa2958fe53cc8c055ca185177d9a2aee450fa99329c86c6f11`
**LOC do arquivo-fonte:** 389


### `app_bundle.scripts.pipeline.preparar_artigo_longo_fulltext:is_key_line`

**Assinatura:**
```python
def is_key_line(line: str, key: str) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/preparar_artigo_longo_fulltext.py`
**Linhas:** 103–110
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Verifica se `key_line` conforme as regras codificadas na rotina.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `line` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `line` recebido pela função; não há descrição individual explícita no código. |
| `key` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `key` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `False`; `rest.startswith('=')`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `len`
- `line.strip`
- `rest.startswith`
- `s.startswith`
- `s[len(key):].lstrip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.preparar_artigo_longo_fulltext:remove_key`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6e846516c6c057aa2958fe53cc8c055ca185177d9a2aee450fa99329c86c6f11`
**LOC do arquivo-fonte:** 389


### `app_bundle.scripts.pipeline.preparar_artigo_longo_fulltext:skip_value_block`

**Assinatura:**
```python
def skip_value_block(lines: list[str], i: int, section_end: int) -> int
```

**Arquivo:** `app_bundle/scripts/pipeline/preparar_artigo_longo_fulltext.py`
**Linhas:** 113–132
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `skip_value_block` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `lines` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `lines` recebido pela função; não há descrição individual explícita no código. |
| `i` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `i` recebido pela função; não há descrição individual explícita no código. |
| `section_end` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `section_end` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `j + 1`; `section_end`; `i + 1`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `after.count`
- `line.split`
- `lines[j].count`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.preparar_artigo_longo_fulltext:remove_key`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6e846516c6c057aa2958fe53cc8c055ca185177d9a2aee450fa99329c86c6f11`
**LOC do arquivo-fonte:** 389


### `app_bundle.scripts.pipeline.preparar_artigo_longo_fulltext:remove_key`

**Assinatura:**
```python
def remove_key(lines: list[str], section: str, key: str) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/preparar_artigo_longo_fulltext.py`
**Linhas:** 135–148
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Remove a responsabilidade representada por `key` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `lines` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `lines` recebido pela função; não há descrição individual explícita no código. |
| `section` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `section` recebido pela função; não há descrição individual explícita no código. |
| `key` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `key` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `lines`; `out`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `find_section`
- `is_key_line`
- `out.append`
- `out.extend`
- `skip_value_block`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.preparar_artigo_longo_fulltext:set_array`
- `app_bundle.scripts.pipeline.preparar_artigo_longo_fulltext:set_multiline_string`
- `app_bundle.scripts.pipeline.preparar_artigo_longo_fulltext:set_scalar`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6e846516c6c057aa2958fe53cc8c055ca185177d9a2aee450fa99329c86c6f11`
**LOC do arquivo-fonte:** 389


### `app_bundle.scripts.pipeline.preparar_artigo_longo_fulltext:ensure_section`

**Assinatura:**
```python
def ensure_section(lines: list[str], section: str) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/preparar_artigo_longo_fulltext.py`
**Linhas:** 151–158
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Garante a responsabilidade representada por `section` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `lines` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `lines` recebido pela função; não há descrição individual explícita no código. |
| `section` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `section` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `lines`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `find_section`
- `lines.append`
- `lines[-1].endswith`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.preparar_artigo_longo_fulltext:insert_after_header`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6e846516c6c057aa2958fe53cc8c055ca185177d9a2aee450fa99329c86c6f11`
**LOC do arquivo-fonte:** 389


### `app_bundle.scripts.pipeline.preparar_artigo_longo_fulltext:insert_after_header`

**Assinatura:**
```python
def insert_after_header(lines: list[str], section: str, value_line: str) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/preparar_artigo_longo_fulltext.py`
**Linhas:** 161–165
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `insert_after_header` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `lines` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `lines` recebido pela função; não há descrição individual explícita no código. |
| `section` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `section` recebido pela função; não há descrição individual explícita no código. |
| `value_line` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `value_line` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `lines[:start + 1] + [value_line if value_line.endswith('\n') else value_line + '\n'] + lines[start + 1:]`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `ensure_section`
- `find_section`
- `value_line.endswith`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.preparar_artigo_longo_fulltext:set_array`
- `app_bundle.scripts.pipeline.preparar_artigo_longo_fulltext:set_multiline_string`
- `app_bundle.scripts.pipeline.preparar_artigo_longo_fulltext:set_scalar`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6e846516c6c057aa2958fe53cc8c055ca185177d9a2aee450fa99329c86c6f11`
**LOC do arquivo-fonte:** 389


### `app_bundle.scripts.pipeline.preparar_artigo_longo_fulltext:set_scalar`

**Assinatura:**
```python
def set_scalar(lines: list[str], section: str, key: str, value: str) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/preparar_artigo_longo_fulltext.py`
**Linhas:** 168–170
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `set_scalar` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `lines` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `lines` recebido pela função; não há descrição individual explícita no código. |
| `section` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `section` recebido pela função; não há descrição individual explícita no código. |
| `key` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `key` recebido pela função; não há descrição individual explícita no código. |
| `value` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `insert_after_header(lines, section, f'{key} = {value}')`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `insert_after_header`
- `remove_key`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.preparar_artigo_longo_fulltext:patch_toml`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6e846516c6c057aa2958fe53cc8c055ca185177d9a2aee450fa99329c86c6f11`
**LOC do arquivo-fonte:** 389


### `app_bundle.scripts.pipeline.preparar_artigo_longo_fulltext:set_array`

**Assinatura:**
```python
def set_array(lines: list[str], section: str, key: str, values: list[str]) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/preparar_artigo_longo_fulltext.py`
**Linhas:** 173–176
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `set_array` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `lines` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `lines` recebido pela função; não há descrição individual explícita no código. |
| `section` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `section` recebido pela função; não há descrição individual explícita no código. |
| `key` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `key` recebido pela função; não há descrição individual explícita no código. |
| `values` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `values` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `insert_after_header(lines, section, f'{key} = {arr}')`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `insert_after_header`
- `json.dumps`
- `remove_key`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.preparar_artigo_longo_fulltext:patch_toml`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6e846516c6c057aa2958fe53cc8c055ca185177d9a2aee450fa99329c86c6f11`
**LOC do arquivo-fonte:** 389


### `app_bundle.scripts.pipeline.preparar_artigo_longo_fulltext:set_multiline_string`

**Assinatura:**
```python
def set_multiline_string(lines: list[str], section: str, key: str, value: str) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/preparar_artigo_longo_fulltext.py`
**Linhas:** 179–181
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `set_multiline_string` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `lines` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `lines` recebido pela função; não há descrição individual explícita no código. |
| `section` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `section` recebido pela função; não há descrição individual explícita no código. |
| `key` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `key` recebido pela função; não há descrição individual explícita no código. |
| `value` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `insert_after_header(lines, section, f'{key} = """{value.strip()}\n"""')`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `insert_after_header`
- `remove_key`
- `value.strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.preparar_artigo_longo_fulltext:patch_toml`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6e846516c6c057aa2958fe53cc8c055ca185177d9a2aee450fa99329c86c6f11`
**LOC do arquivo-fonte:** 389


### `app_bundle.scripts.pipeline.preparar_artigo_longo_fulltext:patch_toml`

**Assinatura:**
```python
def patch_toml(cfg: Path, art_dir: Path, prompt_path: Path, corpus_dir: Path, target_n: int, min_words: int) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/preparar_artigo_longo_fulltext.py`
**Linhas:** 184–248
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `patch_toml` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `Path` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `art_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `art_dir` recebido pela função; não há descrição individual explícita no código. |
| `prompt_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `corpus_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `corpus_dir` recebido pela função; não há descrição individual explícita no código. |
| `target_n` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `target_n` recebido pela função; não há descrição individual explícita no código. |
| `min_words` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `min_words` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Abre arquivo para leitura ou inspeção.
- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `''.join`
- `(art_dir / 'dados_prisma' / 'orientacoes_fulltext_garantido_artigo.md').resolve`
- `(art_dir / 'dados_prisma').resolve`
- `backup`
- `cfg.open`
- `cfg.read_text`
- `cfg.write_text`
- `cfg_data.get`
- `cfg_data.get('documentos_locais', {}).get`
- `cfg_data.get('orientacoes', {}).get`
- `corpus_dir.resolve`
- `current_docs.append`
- `current_orient.append`
- `isinstance`
- `list`
- `print`
- `prompt_path.resolve`
- `re.sub`
- `read_toml_dict`
- `set_array`
- `set_multiline_string`
- `set_scalar`
- `str`
- `text.splitlines`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.preparar_artigo_longo_fulltext:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `orientacoes_fulltext_garantido_artigo.md`
- `referencias_incluidas_20.md`
- `estatisticas_prisma_fulltext.md`
- `corpus_fulltext_compilado.md`

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6e846516c6c057aa2958fe53cc8c055ca185177d9a2aee450fa99329c86c6f11`
**LOC do arquivo-fonte:** 389


### `app_bundle.scripts.pipeline.preparar_artigo_longo_fulltext:build_prompt_and_corpus`

**Assinatura:**
```python
def build_prompt_and_corpus(art_dir: Path, target_n: int, min_words: int, chars_por_pdf: int) -> dict
```

**Arquivo:** `app_bundle/scripts/pipeline/preparar_artigo_longo_fulltext.py`
**Linhas:** 251–364
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Constrói a responsabilidade representada por `prompt_and_corpus` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `art_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `art_dir` recebido pela função; não há descrição individual explícita no código. |
| `target_n` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `target_n` recebido pela função; não há descrição individual explícita no código. |
| `min_words` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `min_words` recebido pela função; não há descrição individual explícita no código. |
| `chars_por_pdf` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `chars_por_pdf` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict`. Retornos observados estaticamente incluem: `{'rows': len(rows), 'corpus_dir': corpus_dir, 'prompt_md': prompt_md, 'refs_md': refs_md, 'corpus_md': corpus_md, 'stats_md': stats_md}`.

#### Efeitos colaterais e fronteiras externas

- Abre arquivo para leitura ou inspeção.
- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `SystemExit`
- `corpus_dir.mkdir`
- `corpus_md.open`
- `counts.items`
- `counts_path.exists`
- `counts_path.read_text`
- `csv_path.exists`
- `enumerate`
- `extract_pdf_text`
- `f.write`
- `json.loads`
- `len`
- `pick`
- `prompt_md.write_text`
- `read_csv`
- `refs_md.open`
- `stats_md.open`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.preparar_artigo_longo_fulltext:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `SystemExit(f'ERRO: CSV de referências fulltext não encontrado: {csv_path}')`
- `SystemExit(f'ERRO: só há {len(rows)} referências fulltext; alvo solicitado: {target_n}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `relatorio_prisma_prisma_fluxo_pmf.referencias_incluidas_fulltext_garantido.csv`
- `relatorio_prisma_prisma_fluxo_pmf.contagens_prisma_fulltext_garantido.json`
- `relatorio_prisma_prisma_fluxo_pmf.matriz_prisma_fulltext_garantido.csv`
- `relatorio_prisma_prisma_fluxo_pmf.exclusoes_fulltext_nao_recuperado.csv`
- `referencias_incluidas_20.md`
- `corpus_fulltext_compilado.md`
- `prompt_artigo_cientifico_longo.md`
- `estatisticas_prisma_fulltext.md`

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 3
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `6e846516c6c057aa2958fe53cc8c055ca185177d9a2aee450fa99329c86c6f11`
**LOC do arquivo-fonte:** 389


### `app_bundle.scripts.pipeline.preparar_artigo_longo_fulltext:main`

**Assinatura:**
```python
def main()
```

**Arquivo:** `app_bundle/scripts/pipeline/preparar_artigo_longo_fulltext.py`
**Linhas:** 367–385
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa o ponto de entrada correspondente e coordena o fluxo associado.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `0`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(args.art_dir).resolve`
- `Path(args.cfg_art).resolve`
- `ap.add_argument`
- `ap.parse_args`
- `argparse.ArgumentParser`
- `build_prompt_and_corpus`
- `patch_toml`
- `print`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `--art-dir`
- `--cfg-art`
- `--chars-por-pdf`
- `--min-palavras`
- `--target-n`

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6e846516c6c057aa2958fe53cc8c055ca185177d9a2aee450fa99329c86c6f11`
**LOC do arquivo-fonte:** 389


### `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:norm_ascii`

**Assinatura:**
```python
def norm_ascii(v: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_baixar_fulltext_artigos.py`
**Linhas:** 21–24
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `norm_ascii` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `v` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `v` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `''.join((ch for ch in s if not unicodedata.combining(ch)))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `''.join`
- `str`
- `unicodedata.combining`
- `unicodedata.normalize`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9a4b0a12218a3c728bcc57182549973d066035d1a0324522a8d9be5d6758bc62`
**LOC do arquivo-fonte:** 528


### `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:norm_col`

**Assinatura:**
```python
def norm_col(v: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_baixar_fulltext_artigos.py`
**Linhas:** 26–27
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `norm_col` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `v` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `v` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `re.sub('[^a-z0-9]+', '_', norm_ascii(str(v or '').lower())).strip('_')`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `norm_ascii`
- `re.sub`
- `re.sub('[^a-z0-9]+', '_', norm_ascii(str(v or '').lower())).strip`
- `str`
- `str(v or '').lower`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9a4b0a12218a3c728bcc57182549973d066035d1a0324522a8d9be5d6758bc62`
**LOC do arquivo-fonte:** 528


### `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:slug`

**Assinatura:**
```python
def slug(v: str, max_len: int = 110) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_baixar_fulltext_artigos.py`
**Linhas:** 29–31
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `slug` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `v` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `v` recebido pela função; não há descrição individual explícita no código. |
| `max_len` | positional-or-keyword | `int` | `110` | Parâmetro `max_len` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `re.sub('_+', '_', s)[:max_len].strip('_') or 'artigo'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `norm_ascii`
- `norm_ascii(v or '').lower`
- `re.sub`
- `re.sub('[^a-zA-Z0-9]+', '_', norm_ascii(v or '').lower()).strip`
- `re.sub('_+', '_', s)[:max_len].strip`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9a4b0a12218a3c728bcc57182549973d066035d1a0324522a8d9be5d6758bc62`
**LOC do arquivo-fonte:** 528


### `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:sniff_csv`

**Assinatura:**
```python
def sniff_csv(path: Path)
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_baixar_fulltext_artigos.py`
**Linhas:** 33–38
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `sniff_csv` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `csv.Sniffer().sniff(sample, delimiters=',;\t')`; `csv.excel`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `csv.Sniffer`
- `csv.Sniffer().sniff`
- `path.read_text`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `9a4b0a12218a3c728bcc57182549973d066035d1a0324522a8d9be5d6758bc62`
**LOC do arquivo-fonte:** 528


### `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:read_csv`

**Assinatura:**
```python
def read_csv(path: Path) -> list[dict]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_baixar_fulltext_artigos.py`
**Linhas:** 40–42
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Lê a responsabilidade representada por `csv` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `list[dict]`. Retornos observados estaticamente incluem: `list(csv.DictReader(f, dialect=sniff_csv(path)))`.

#### Efeitos colaterais e fronteiras externas

- Abre arquivo para leitura ou inspeção.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `csv.DictReader`
- `list`
- `path.open`
- `sniff_csv`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9a4b0a12218a3c728bcc57182549973d066035d1a0324522a8d9be5d6758bc62`
**LOC do arquivo-fonte:** 528


### `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:pick`

**Assinatura:**
```python
def pick(row: dict, *names: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_baixar_fulltext_artigos.py`
**Linhas:** 44–50
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `pick` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `row` | positional-or-keyword | `dict` | `obrigatório/ausente` | Parâmetro `row` recebido pela função; não há descrição individual explícita no código. |
| `names` | var-positional | `str` | `obrigatório/ausente` | Parâmetro `names` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `str(v).strip()`; `''`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `d.get`
- `norm_col`
- `row.items`
- `str`
- `str(v).strip`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9a4b0a12218a3c728bcc57182549973d066035d1a0324522a8d9be5d6758bc62`
**LOC do arquivo-fonte:** 528


### `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:doi_from_text`

**Assinatura:**
```python
def doi_from_text(text: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_baixar_fulltext_artigos.py`
**Linhas:** 52–56
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `doi_from_text` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `''`; `m.group(0).strip().rstrip('.,;)').replace('\\_', '_').replace('%5C_', '_')`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `m.group`
- `m.group(0).strip`
- `m.group(0).strip().rstrip`
- `m.group(0).strip().rstrip('.,;)').replace`
- `m.group(0).strip().rstrip('.,;)').replace('\\_', '_').replace`
- `re.search`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9a4b0a12218a3c728bcc57182549973d066035d1a0324522a8d9be5d6758bc62`
**LOC do arquivo-fonte:** 528


### `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:normalize_doi`

**Assinatura:**
```python
def normalize_doi(v: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_baixar_fulltext_artigos.py`
**Linhas:** 58–62
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Normaliza a responsabilidade representada por `doi` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `v` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `v` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `doi_from_text(v) or v`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `doi_from_text`
- `re.sub`
- `str`
- `str(v or '').strip`
- `v.replace`
- `v.replace('\\_', '_').replace`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `^https?://(dx\.)?doi\.org/`

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9a4b0a12218a3c728bcc57182549973d066035d1a0324522a8d9be5d6758bc62`
**LOC do arquivo-fonte:** 528


### `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:read_env`

**Assinatura:**
```python
def read_env(root: Path) -> dict
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_baixar_fulltext_artigos.py`
**Linhas:** 64–75
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Lê a responsabilidade representada por `env` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `root` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `root` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict`. Retornos observados estaticamente incluem: `env`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `k.strip`
- `line.split`
- `line.startswith`
- `line.strip`
- `p.exists`
- `p.read_text`
- `p.read_text(encoding='utf-8', errors='ignore').splitlines`
- `v.strip`
- `v.strip().strip`
- `v.strip().strip('"').strip`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9a4b0a12218a3c728bcc57182549973d066035d1a0324522a8d9be5d6758bc62`
**LOC do arquivo-fonte:** 528


### `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:read_toml`

**Assinatura:**
```python
def read_toml(path: Path | None) -> dict
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_baixar_fulltext_artigos.py`
**Linhas:** 77–81
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Lê a responsabilidade representada por `toml` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path \| None` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `dict`. Retornos observados estaticamente incluem: `{}`; `tomllib.load(f)`.

#### Efeitos colaterais e fronteiras externas

- Abre arquivo para leitura ou inspeção.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `path.exists`
- `path.open`
- `tomllib.load`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9a4b0a12218a3c728bcc57182549973d066035d1a0324522a8d9be5d6758bc62`
**LOC do arquivo-fonte:** 528


### `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:resolve_relative`

**Assinatura:**
```python
def resolve_relative(base_file: Path, maybe_path: str) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_baixar_fulltext_artigos.py`
**Linhas:** 83–87
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Resolve a responsabilidade representada por `relative` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `base_file` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `base_file` recebido pela função; não há descrição individual explícita no código. |
| `maybe_path` | positional-or-keyword | `str` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `p.resolve()`; `(base_file.parent / p).resolve()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `(base_file.parent / p).resolve`
- `Path`
- `Path(str(maybe_path)).expanduser`
- `p.is_absolute`
- `p.resolve`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9a4b0a12218a3c728bcc57182549973d066035d1a0324522a8d9be5d6758bc62`
**LOC do arquivo-fonte:** 528


### `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:dados_pesquisa_path_from_config`

**Assinatura:**
```python
def dados_pesquisa_path_from_config(config: Path | None) -> Path | None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_baixar_fulltext_artigos.py`
**Linhas:** 89–100
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `dados_pesquisa_path_from_config` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `config` | positional-or-keyword | `Path \| None` | `obrigatório/ausente` | Configuração ou estrutura de configuração recebida pela rotina. |

#### Retorno

Anotação declarada: `Path | None`. Retornos observados estaticamente incluem: `None`; `resolve_relative(config, str(candidate))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `config.exists`
- `d.get`
- `d.get('busca_prisma', {}).get`
- `d.get('pesquisa', {}).get`
- `read_toml`
- `resolve_relative`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9a4b0a12218a3c728bcc57182549973d066035d1a0324522a8d9be5d6758bc62`
**LOC do arquivo-fonte:** 528


### `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:flatten_text`

**Assinatura:**
```python
def flatten_text(obj) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_baixar_fulltext_artigos.py`
**Linhas:** 102–109
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `flatten_text` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `obj` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `obj` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `''`; `' '.join((flatten_text(v) for v in obj.values()))`; `' '.join((flatten_text(x) for x in obj))`; `str(obj)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `flatten_text`
- `isinstance`
- `obj.values`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9a4b0a12218a3c728bcc57182549973d066035d1a0324522a8d9be5d6758bc62`
**LOC do arquivo-fonte:** 528


### `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:research_context`

**Assinatura:**
```python
def research_context(prisma_config: Path | None, dados_pesquisa_path: Path | None) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_baixar_fulltext_artigos.py`
**Linhas:** 111–121
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `research_context` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `prisma_config` | positional-or-keyword | `Path \| None` | `obrigatório/ausente` | Configuração ou estrutura de configuração recebida pela rotina. |
| `dados_pesquisa_path` | positional-or-keyword | `Path \| None` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `' '.join(pieces)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `cfg.get`
- `dados_pesquisa_path_from_config`
- `flatten_text`
- `meta_path.exists`
- `pieces.append`
- `prisma_config.exists`
- `read_toml`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9a4b0a12218a3c728bcc57182549973d066035d1a0324522a8d9be5d6758bc62`
**LOC do arquivo-fonte:** 528


### `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:fetch_json`

**Assinatura:**
```python
def fetch_json(url: str, headers: dict | None = None, timeout: int = 30) -> dict
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_baixar_fulltext_artigos.py`
**Linhas:** 123–129
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `fetch_json` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `url` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `url` recebido pela função; não há descrição individual explícita no código. |
| `headers` | positional-or-keyword | `dict \| None` | `None` | Cabeçalhos usados em requisição/integração externa. |
| `timeout` | positional-or-keyword | `int` | `30` | Tempo máximo de espera para a operação, quando aplicável. |

#### Retorno

Anotação declarada: `dict`. Retornos observados estaticamente incluem: `json.loads(resp.read().decode('utf-8', errors='replace'))`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.
- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `h.update`
- `json.loads`
- `resp.read`
- `resp.read().decode`
- `urllib.request.Request`
- `urllib.request.urlopen`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9a4b0a12218a3c728bcc57182549973d066035d1a0324522a8d9be5d6758bc62`
**LOC do arquivo-fonte:** 528


### `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:download_pdf`

**Assinatura:**
```python
def download_pdf(url: str, dest: Path, timeout: int = 60) -> tuple[bool, str]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_baixar_fulltext_artigos.py`
**Linhas:** 131–145
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Baixa a responsabilidade representada por `pdf` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `url` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `url` recebido pela função; não há descrição individual explícita no código. |
| `dest` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `dest` recebido pela função; não há descrição individual explícita no código. |
| `timeout` | positional-or-keyword | `int` | `60` | Tempo máximo de espera para a operação, quando aplicável. |

#### Retorno

Anotação declarada: `tuple[bool, str]`. Retornos observados estaticamente incluem: `(False, 'resposta não parece PDF')`; `(True, 'ok')`; `(False, str(e))`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `data[:2048].lstrip`
- `data[:2048].lstrip().startswith`
- `dest.parent.mkdir`
- `dest.write_bytes`
- `resp.read`
- `str`
- `urllib.request.Request`
- `urllib.request.urlopen`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `9a4b0a12218a3c728bcc57182549973d066035d1a0324522a8d9be5d6758bc62`
**LOC do arquivo-fonte:** 528


### `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:collect_pdf_urls_from_unpaywall`

**Assinatura:**
```python
def collect_pdf_urls_from_unpaywall(doi: str, email: str) -> list[dict]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_baixar_fulltext_artigos.py`
**Linhas:** 147–172
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Coleta a responsabilidade representada por `pdf_urls_from_unpaywall` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doi` | positional-or-keyword | `str` | `obrigatório/ausente` | Digital Object Identifier da referência bibliográfica. |
| `email` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `email` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[dict]`. Retornos observados estaticamente incluem: `[]`; `[{'pdf_url': '', 'source': 'unpaywall', 'note': f'falha Unpaywall: {e}'}]`; `out`.

#### Efeitos colaterais e fronteiras externas

- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `data.get`
- `fetch_json`
- `loc.get`
- `locs.append`
- `locs.extend`
- `out.append`
- `u.lower`
- `urllib.parse.quote`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `https://api.unpaywall.org/v2/`
- `.pdf`

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 2
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `9a4b0a12218a3c728bcc57182549973d066035d1a0324522a8d9be5d6758bc62`
**LOC do arquivo-fonte:** 528


### `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:collect_pdf_urls_from_openalex_doi`

**Assinatura:**
```python
def collect_pdf_urls_from_openalex_doi(doi: str, email: str) -> list[dict]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_baixar_fulltext_artigos.py`
**Linhas:** 174–183
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Coleta a responsabilidade representada por `pdf_urls_from_openalex_doi` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doi` | positional-or-keyword | `str` | `obrigatório/ausente` | Digital Object Identifier da referência bibliográfica. |
| `email` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `email` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[dict]`. Retornos observados estaticamente incluem: `[]`; `candidates_from_openalex_work(data, source='openalex_doi')`.

#### Efeitos colaterais e fronteiras externas

- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `candidates_from_openalex_work`
- `fetch_json`
- `mailto.lstrip`
- `urllib.parse.quote`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `https://api.openalex.org/works/doi:`

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `9a4b0a12218a3c728bcc57182549973d066035d1a0324522a8d9be5d6758bc62`
**LOC do arquivo-fonte:** 528


### `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:candidates_from_openalex_work`

**Assinatura:**
```python
def candidates_from_openalex_work(w: dict, source: str = 'openalex') -> list[dict]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_baixar_fulltext_artigos.py`
**Linhas:** 185–203
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `candidates_from_openalex_work` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `w` | positional-or-keyword | `dict` | `obrigatório/ausente` | Parâmetro `w` recebido pela função; não há descrição individual explícita no código. |
| `source` | positional-or-keyword | `str` | `'openalex'` | Origem dos dados ou arquivo de entrada. |

#### Retorno

Anotação declarada: `list[dict]`. Retornos observados estaticamente incluem: `out`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `(w.get('doi') or '').replace`
- `loc.get`
- `locs.append`
- `locs.extend`
- `openalex_abstract`
- `out.append`
- `u.lower`
- `w.get`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.pdf`
- `https://doi.org/`

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9a4b0a12218a3c728bcc57182549973d066035d1a0324522a8d9be5d6758bc62`
**LOC do arquivo-fonte:** 528


### `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:openalex_abstract`

**Assinatura:**
```python
def openalex_abstract(w: dict) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_baixar_fulltext_artigos.py`
**Linhas:** 205–213
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `openalex_abstract` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `w` | positional-or-keyword | `dict` | `obrigatório/ausente` | Parâmetro `w` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `''`; `' '.join((word for _, word in sorted(pos)))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `inv.items`
- `pos.append`
- `sorted`
- `w.get`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 2
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9a4b0a12218a3c728bcc57182549973d066035d1a0324522a8d9be5d6758bc62`
**LOC do arquivo-fonte:** 528


### `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:collect_pdf_urls_from_crossref`

**Assinatura:**
```python
def collect_pdf_urls_from_crossref(doi: str) -> list[dict]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_baixar_fulltext_artigos.py`
**Linhas:** 215–237
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Coleta a responsabilidade representada por `pdf_urls_from_crossref` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doi` | positional-or-keyword | `str` | `obrigatório/ausente` | Digital Object Identifier da referência bibliográfica. |

#### Retorno

Anotação declarada: `list[dict]`. Retornos observados estaticamente incluem: `[]`; `out`.

#### Efeitos colaterais e fronteiras externas

- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `ct.lower`
- `data.get`
- `fetch_json`
- `link.get`
- `msg.get`
- `out.append`
- `u.lower`
- `urllib.parse.quote`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `https://api.crossref.org/works/`
- `.pdf`

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `9a4b0a12218a3c728bcc57182549973d066035d1a0324522a8d9be5d6758bc62`
**LOC do arquivo-fonte:** 528


### `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:collect_pdf_urls_from_semantic_scholar`

**Assinatura:**
```python
def collect_pdf_urls_from_semantic_scholar(doi: str, title: str, api_key: str | None = None) -> list[dict]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_baixar_fulltext_artigos.py`
**Linhas:** 239–285
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Coleta a responsabilidade representada por `pdf_urls_from_semantic_scholar` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doi` | positional-or-keyword | `str` | `obrigatório/ausente` | Digital Object Identifier da referência bibliográfica. |
| `title` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `title` recebido pela função; não há descrição individual explícita no código. |
| `api_key` | positional-or-keyword | `str \| None` | `None` | Parâmetro `api_key` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[dict]`. Retornos observados estaticamente incluem: `out`.

#### Efeitos colaterais e fronteiras externas

- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `(d.get('externalIds') or {}).get`
- `(d.get('openAccessPdf') or {}).get`
- `d.get`
- `data.get`
- `fetch_json`
- `identifiers.append`
- `out.append`
- `urllib.parse.quote`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `https://api.semanticscholar.org/graph/v1/paper/`
- `https://api.semanticscholar.org/graph/v1/paper/search?query=`

#### Complexidade estrutural observada

```yaml
branches: 7
loops: 1
comprehensions: 0
exception_handlers: 2
```

**SHA-256 do arquivo-fonte:** `9a4b0a12218a3c728bcc57182549973d066035d1a0324522a8d9be5d6758bc62`
**LOC do arquivo-fonte:** 528


### `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:collect_pdf_urls_from_europepmc`

**Assinatura:**
```python
def collect_pdf_urls_from_europepmc(doi: str, title: str) -> list[dict]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_baixar_fulltext_artigos.py`
**Linhas:** 287–315
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Coleta a responsabilidade representada por `pdf_urls_from_europepmc` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doi` | positional-or-keyword | `str` | `obrigatório/ausente` | Digital Object Identifier da referência bibliográfica. |
| `title` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `title` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[dict]`. Retornos observados estaticamente incluem: `[]`; `out`.

#### Efeitos colaterais e fronteiras externas

- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `(data.get('resultList') or {}).get`
- `(r.get('fullTextUrlList') or {}).get`
- `data.get`
- `fetch_json`
- `out.append`
- `pdf.lower`
- `r.get`
- `u.get`
- `u.get('documentStyle', '').lower`
- `urllib.parse.quote`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.pdf`

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 2
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `9a4b0a12218a3c728bcc57182549973d066035d1a0324522a8d9be5d6758bc62`
**LOC do arquivo-fonte:** 528


### `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:tokens`

**Assinatura:**
```python
def tokens(text: str) -> set[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_baixar_fulltext_artigos.py`
**Linhas:** 317–321
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `tokens` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `set[str]`. Retornos observados estaticamente incluem: `{w for w in words if w not in stop}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `norm_ascii`
- `norm_ascii(text or '').lower`
- `re.findall`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:score_candidate`
- `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:search_openalex_proxy`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9a4b0a12218a3c728bcc57182549973d066035d1a0324522a8d9be5d6758bc62`
**LOC do arquivo-fonte:** 528


### `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:score_candidate`

**Assinatura:**
```python
def score_candidate(original_title: str, research_ctx: str, cand: dict) -> float
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_baixar_fulltext_artigos.py`
**Linhas:** 323–330
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `score_candidate` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `original_title` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `original_title` recebido pela função; não há descrição individual explícita no código. |
| `research_ctx` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `research_ctx` recebido pela função; não há descrição individual explícita no código. |
| `cand` | positional-or-keyword | `dict` | `obrigatório/ausente` | Parâmetro `cand` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `float`. Retornos observados estaticamente incluem: `round(0.65 * s_title + 0.35 * overlap, 4)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `SequenceMatcher`
- `SequenceMatcher(None, norm_ascii(original_title).lower(), norm_ascii(ctitle).lower()).ratio`
- `cand.get`
- `len`
- `max`
- `norm_ascii`
- `norm_ascii(ctitle).lower`
- `norm_ascii(original_title).lower`
- `round`
- `tokens`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:search_openalex_proxy`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9a4b0a12218a3c728bcc57182549973d066035d1a0324522a8d9be5d6758bc62`
**LOC do arquivo-fonte:** 528


### `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:search_openalex_proxy`

**Assinatura:**
```python
def search_openalex_proxy(title: str, research_ctx: str, email: str, per_page: int = 25) -> list[dict]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_baixar_fulltext_artigos.py`
**Linhas:** 332–358
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `search_openalex_proxy` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `title` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `title` recebido pela função; não há descrição individual explícita no código. |
| `research_ctx` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `research_ctx` recebido pela função; não há descrição individual explícita no código. |
| `email` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `email` recebido pela função; não há descrição individual explícita no código. |
| `per_page` | positional-or-keyword | `int` | `25` | Parâmetro `per_page` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[dict]`. Retornos observados estaticamente incluem: `sorted(out, key=lambda x: x.get('proxy_score', 0), reverse=True)`.

#### Efeitos colaterais e fronteiras externas

- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `c.get`
- `candidates_from_openalex_work`
- `data.get`
- `fetch_json`
- `list`
- `out.append`
- `queries.append`
- `score_candidate`
- `seen.add`
- `set`
- `sorted`
- `time.sleep`
- `tokens`
- `urllib.parse.quote`
- `x.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `https://api.openalex.org/works?search=`

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 3
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `9a4b0a12218a3c728bcc57182549973d066035d1a0324522a8d9be5d6758bc62`
**LOC do arquivo-fonte:** 528


### `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:try_download_candidates`

**Assinatura:**
```python
def try_download_candidates(candidates: list[dict], dest_base: Path, status_ok: str, min_score: float | None = None) -> tuple[str, dict, str]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_baixar_fulltext_artigos.py`
**Linhas:** 360–375
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `try_download_candidates` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `candidates` | positional-or-keyword | `list[dict]` | `obrigatório/ausente` | Parâmetro `candidates` recebido pela função; não há descrição individual explícita no código. |
| `dest_base` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `dest_base` recebido pela função; não há descrição individual explícita no código. |
| `status_ok` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `status_ok` recebido pela função; não há descrição individual explícita no código. |
| `min_score` | positional-or-keyword | `float \| None` | `None` | Parâmetro `min_score` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `tuple[str, dict, str]`. Retornos observados estaticamente incluem: `(status_ok, c, 'download ok')`; `('', {}, 'nenhum candidato baixável')`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `c.get`
- `dest_base.with_name`
- `dict`
- `download_pdf`
- `enumerate`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9a4b0a12218a3c728bcc57182549973d066035d1a0324522a8d9be5d6758bc62`
**LOC do arquivo-fonte:** 528


### `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:find_input_csv`

**Assinatura:**
```python
def find_input_csv(dados_dir: Path) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_baixar_fulltext_artigos.py`
**Linhas:** 377–385
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Localiza a responsabilidade representada por `input_csv` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `dados_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `dados_dir` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `candidates[0]`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `FileNotFoundError`
- `dados_dir.glob`
- `sorted`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `FileNotFoundError(f'Nenhum CSV de referências encontrado em {dados_dir}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `*referencias_incluidas_seminario.csv`
- `*referencias_incluidas*.csv`
- `*triagem_humana.csv`

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9a4b0a12218a3c728bcc57182549973d066035d1a0324522a8d9be5d6758bc62`
**LOC do arquivo-fonte:** 528


### `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:md_escape`

**Assinatura:**
```python
def md_escape(v: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_baixar_fulltext_artigos.py`
**Linhas:** 387–388
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `md_escape` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `v` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `v` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `str(v or '').replace('|', '/').replace('\n', ' ').strip()`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `str`
- `str(v or '').replace`
- `str(v or '').replace('|', '/').replace`
- `str(v or '').replace('|', '/').replace('\n', ' ').strip`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9a4b0a12218a3c728bcc57182549973d066035d1a0324522a8d9be5d6758bc62`
**LOC do arquivo-fonte:** 528


### `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:main`

**Assinatura:**
```python
def main(argv = None)
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_baixar_fulltext_artigos.py`
**Linhas:** 390–525
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa o ponto de entrada correspondente e coordena o fluxo associado.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `não anotado` | `None` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `0`.

#### Efeitos colaterais e fronteiras externas

- Abre arquivo para leitura ou inspeção.
- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.
- Lê arquivos ou artefatos do filesystem.
- Lê configuração/credenciais do ambiente do processo.
- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `Path`
- `Path(args.dados_dir).resolve`
- `Path(args.dados_pesquisa_path).resolve`
- `Path(args.out_dir).resolve`
- `Path(args.prisma_config).resolve`
- `Path(args.project_root).resolve`
- `argparse.ArgumentParser`
- `base.with_name`
- `bool`
- `c.get`
- `chosen.get`
- `collect_pdf_urls_from_crossref`
- `collect_pdf_urls_from_europepmc`
- `collect_pdf_urls_from_openalex_doi`
- `collect_pdf_urls_from_semantic_scholar`
- `collect_pdf_urls_from_unpaywall`
- `csv.DictWriter`
- `doi_from_text`
- `enumerate`
- `env.get`
- `f.write`
- `find_input_csv`
- `len`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `--dados-dir`
- `--dados-pesquisa-path`
- `--email`
- `--fallback-proxy-oa`
- `--max`
- `--min-proxy-score`
- `--out-dir`
- `--prisma-config`
- `--project-root`
- `--semantic-scholar-api-key`
- `--sleep`

#### Artefatos/caminhos literais reconhecidos no corpo

- `.pdf`
- `_PROXY.pdf`
- `manifesto_fulltext_artigos.csv`
- `orientacoes_fulltext_artigos.md`

#### Complexidade estrutural observada

```yaml
branches: 11
loops: 2
comprehensions: 5
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9a4b0a12218a3c728bcc57182549973d066035d1a0324522a8d9be5d6758bc62`
**LOC do arquivo-fonte:** 528


### `app_bundle.scripts.pipeline.prisma_builder:_as_list`

**Assinatura:**
```python
def _as_list(value: Any) -> list[Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_builder.py`
**Linhas:** 56–61
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_as_list` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[Any]`. Retornos observados estaticamente incluem: `[]`; `value`; `[value]`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `isinstance`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `a4ad333b1b98465a27bddc03a81648e052b7016e85b009759da5874031ce336c`
**LOC do arquivo-fonte:** 437


### `app_bundle.scripts.pipeline.prisma_builder:_str_list`

**Assinatura:**
```python
def _str_list(value: Any) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_builder.py`
**Linhas:** 64–70
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_str_list` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `out`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_as_list`
- `out.append`
- `str`
- `str(item or '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_builder:criteria_from_config`
- `app_bundle.scripts.pipeline.prisma_builder:search_strategy_from_config`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `a4ad333b1b98465a27bddc03a81648e052b7016e85b009759da5874031ce336c`
**LOC do arquivo-fonte:** 437


### `app_bundle.scripts.pipeline.prisma_builder:_clean_doi`

**Assinatura:**
```python
def _clean_doi(value: Any) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_builder.py`
**Linhas:** 73–77
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Limpa a responsabilidade representada por `doi` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `doi.strip().strip('.')`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `doi.strip`
- `doi.strip().strip`
- `re.sub`
- `str`
- `str(value or '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_builder:_record_from_any`
- `app_bundle.scripts.pipeline.prisma_builder:parse_bib_metadata`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `^https?://(?:dx\.)?doi\.org/`

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `a4ad333b1b98465a27bddc03a81648e052b7016e85b009759da5874031ce336c`
**LOC do arquivo-fonte:** 437


### `app_bundle.scripts.pipeline.prisma_builder:_extract_bib_field`

**Assinatura:**
```python
def _extract_bib_field(entry: str, field: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_builder.py`
**Linhas:** 80–94
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Extrai a responsabilidade representada por `bib_field` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `entry` | positional-or-keyword | `str` | `obrigatório/ausente` | Entrada bibliográfica ou registro individual. |
| `field` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `field` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `''`; `re.sub('\\s+', ' ', entry[start:j]).strip()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `len`
- `m.end`
- `range`
- `re.escape`
- `re.search`
- `re.sub`
- `re.sub('\\s+', ' ', entry[start:j]).strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_builder:parse_bib_metadata`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `a4ad333b1b98465a27bddc03a81648e052b7016e85b009759da5874031ce336c`
**LOC do arquivo-fonte:** 437


### `app_bundle.scripts.pipeline.prisma_builder:_plain_bib_value`

**Assinatura:**
```python
def _plain_bib_value(value: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_builder.py`
**Linhas:** 97–111
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_plain_bib_value` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `re.sub('\\s+', ' ', text).strip()`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `re.sub`
- `re.sub('\\s+', ' ', text).strip`
- `replacements.items`
- `str`
- `text.replace`
- `text.replace('{', '').replace`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_builder:parse_bib_metadata`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `a4ad333b1b98465a27bddc03a81648e052b7016e85b009759da5874031ce336c`
**LOC do arquivo-fonte:** 437


### `app_bundle.scripts.pipeline.prisma_builder:parse_bib_metadata`

**Assinatura:**
```python
def parse_bib_metadata(bib_path: Path | None) -> dict[str, dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_builder.py`
**Linhas:** 114–134
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Interpreta a responsabilidade representada por `bib_metadata` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `bib_path` | positional-or-keyword | `Path \| None` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `dict[str, dict[str, Any]]`. Retornos observados estaticamente incluem: `{}`; `out`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `_clean_doi`
- `_extract_bib_field`
- `_plain_bib_value`
- `a.strip`
- `bib_entry_key`
- `bib_path.exists`
- `bib_path.read_text`
- `re.split`
- `split_bib_entries`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_builder:build_prisma_report`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `a4ad333b1b98465a27bddc03a81648e052b7016e85b009759da5874031ce336c`
**LOC do arquivo-fonte:** 437


### `app_bundle.scripts.pipeline.prisma_builder:metadata_from_config`

**Assinatura:**
```python
def metadata_from_config(cfg: dict[str, Any]) -> PrismaMetadata
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_builder.py`
**Linhas:** 137–155
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `metadata_from_config` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `PrismaMetadata`. Retornos observados estaticamente incluem: `PrismaMetadata(titulo=str(rel.get('titulo') or 'Relatório de Pesquisa PRISMA'), tema=str(pesquisa.get('tema') or rel.get('tema') or ''), recorte=str(pesquisa.get('recorte') or rel.get('recorte') or ''), objetivo=str(pesquisa.get('objetivo') or rel.get('objetivo') or ''), pergunta_pesquisa=str(pesquisa.get('pergunta_pesquisa') or rel.get('pergunta_pesquisa') or ''), responsavel=str(atividade.get('aluno') or rel.get('responsavel') or 'Gustavo M. Mendes de Tarso'), instituicao=str(rel.get('institution_name') or 'Fundação Getúlio Vargas'), curso=str(atividade.get('curso') or rel.get('course_name') or 'Mestrado Acadêmico em Políticas Públicas e Governo'), disciplina=str(atividade.get('disciplina') or rel.get('discipline_name') or ''), professor=str(atividade.get('professor') or rel.get('professor_name') or ''), cidade=str(atividade.get('polo') or rel.get('city_name') or 'Brasília'), data_execucao=str(rel.get('data_execucao') or datetime.now().strftime('%Y-%m-%d')), tipo_relatorio=str(rel.get('tipo') or 'prisma'))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `PrismaMetadata`
- `atividade.get`
- `cfg.get`
- `datetime.now`
- `datetime.now().strftime`
- `isinstance`
- `pesquisa.get`
- `rel.get`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_builder:build_prisma_report`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `a4ad333b1b98465a27bddc03a81648e052b7016e85b009759da5874031ce336c`
**LOC do arquivo-fonte:** 437


### `app_bundle.scripts.pipeline.prisma_builder:search_strategy_from_config`

**Assinatura:**
```python
def search_strategy_from_config(cfg: dict[str, Any]) -> SearchStrategy
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_builder.py`
**Linhas:** 158–202
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `search_strategy_from_config` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `SearchStrategy`. Retornos observados estaticamente incluem: `SearchStrategy(bases=bases, idiomas=idiomas, periodo=periodo, queries=query_records)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `QueryRecord`
- `QueryRecord.model_validate`
- `SearchStrategy`
- `_as_list`
- `_str_list`
- `bases.append`
- `busca.get`
- `cfg.get`
- `isinstance`
- `key.startswith`
- `local.get`
- `pesquisa.get`
- `queries.items`
- `query_records.append`
- `rel.get`
- `str`
- `str(item).strip`
- `str(q).strip`
- `str(value).strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_builder:build_prisma_report`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 16
loops: 4
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `a4ad333b1b98465a27bddc03a81648e052b7016e85b009759da5874031ce336c`
**LOC do arquivo-fonte:** 437


### `app_bundle.scripts.pipeline.prisma_builder:criteria_from_config`

**Assinatura:**
```python
def criteria_from_config(cfg: dict[str, Any]) -> ScreeningCriteria
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_builder.py`
**Linhas:** 205–218
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `criteria_from_config` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `ScreeningCriteria`. Retornos observados estaticamente incluem: `ScreeningCriteria(inclusao=inclusao, exclusao=exclusao)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `ScreeningCriteria`
- `_str_list`
- `cfg.get`
- `isinstance`
- `rel.get`
- `triagem.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_builder:build_prisma_report`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `a4ad333b1b98465a27bddc03a81648e052b7016e85b009759da5874031ce336c`
**LOC do arquivo-fonte:** 437


### `app_bundle.scripts.pipeline.prisma_builder:study_from_doc`

**Assinatura:**
```python
def study_from_doc(doc: SourceDoc, bib_meta: dict[str, dict[str, Any]], key_by_doc_path: dict[str, str]) -> StudyRecord
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_builder.py`
**Linhas:** 221–238
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `study_from_doc` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doc` | positional-or-keyword | `SourceDoc` | `obrigatório/ausente` | Documento/registro individual processado pela rotina. |
| `bib_meta` | positional-or-keyword | `dict[str, dict[str, Any]]` | `obrigatório/ausente` | Parâmetro `bib_meta` recebido pela função; não há descrição individual explícita no código. |
| `key_by_doc_path` | positional-or-keyword | `dict[str, str]` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `StudyRecord`. Retornos observados estaticamente incluem: `StudyRecord(bib_key=key, titulo=str(meta.get('titulo') or Path(doc.path).stem.replace('_', ' ').replace('-', ' ').strip()), autores=list(meta.get('autores') or []), ano=str(meta.get('ano') or ''), doi=str(meta.get('doi') or ''), url=str(meta.get('url') or ''), base='corpus_local', fonte=str(meta.get('fonte') or 'documento local'), arquivo_local=str(doc.metadata.get('fulltext_cache_path') or doc.path), decisao='incluido', justificativa='Documento-base local incorporado ao corpus e considerado na elaboração do relatório.', resumo=shorten_text(doc.extracted_text, 900), metadados={'kind': doc.kind, **(doc.metadata or {})})`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(doc.path).resolve`
- `Path(doc.path).stem.replace`
- `Path(doc.path).stem.replace('_', ' ').replace`
- `Path(doc.path).stem.replace('_', ' ').replace('-', ' ').strip`
- `StudyRecord`
- `bib_meta.get`
- `doc.metadata.get`
- `key_by_doc_path.get`
- `list`
- `meta.get`
- `shorten_text`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_builder:build_prisma_report`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `a4ad333b1b98465a27bddc03a81648e052b7016e85b009759da5874031ce336c`
**LOC do arquivo-fonte:** 437


### `app_bundle.scripts.pipeline.prisma_builder:_record_from_any`

**Assinatura:**
```python
def _record_from_any(raw: dict[str, Any], *, default_decision: str = 'identificado') -> StudyRecord | None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_builder.py`
**Linhas:** 241–286
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_record_from_any` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `raw` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `raw` recebido pela função; não há descrição individual explícita no código. |
| `default_decision` | keyword-only | `str` | `'identificado'` | Parâmetro `default_decision` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `StudyRecord | None`. Retornos observados estaticamente incluem: `None`; `StudyRecord(bib_key=str(raw.get('bib_key') or raw.get('key') or '').strip().lstrip('@'), titulo=str(title), autores=authors, ano=str(raw.get('ano') or raw.get('year') or ''), doi=_clean_doi(raw.get('doi') or raw.get('DOI')), url=str(raw.get('url') or raw.get('pdf_url') or raw.get('landing_page_url') or ''), base=str(raw.get('base') or raw.get('source') or raw.get('fonte') or ''), fonte=str(raw.get('venue') or raw.get('journal') or raw.get('journaltitle') or raw.get('fonte') or ''), arquivo_local=str(raw.get('downloaded_pdf_path') or raw.get('local_file_path') or raw.get('arquivo_local') or ''), score_aderencia=score_float, decisao=decision_norm, motivo=str(raw.get('motivo') or raw.get('reason') or raw.get('exclusion_reason') or ''), justificativa=str(raw.get('justificativa') or raw.get('justification') or raw.get('rationale') or ''), resumo=shorten_text(str(raw.get('abstract') or raw.get('resumo') or raw.get('summary') or ''), 900), metadados={k: v for k, v in raw.items() if k not in {'abstract', 'resumo', 'summary'}})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `StudyRecord`
- `_clean_doi`
- `a.get`
- `a.strip`
- `float`
- `isinstance`
- `raw.get`
- `raw.items`
- `re.split`
- `shorten_text`
- `str`
- `str(a).strip`
- `str(a.get('name') if isinstance(a, dict) else a).strip`
- `str(raw.get('bib_key') or raw.get('key') or '').strip`
- `str(raw.get('bib_key') or raw.get('key') or '').strip().lstrip`
- `str(raw.get('decisao') or raw.get('decision') or raw.get('status') or default_decision).strip`
- `str(raw.get('decisao') or raw.get('decision') or raw.get('status') or default_decision).strip().lower`
- `str(score).strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_builder:_collect_records_from_json`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 10
loops: 0
comprehensions: 3
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `a4ad333b1b98465a27bddc03a81648e052b7016e85b009759da5874031ce336c`
**LOC do arquivo-fonte:** 437


### `app_bundle.scripts.pipeline.prisma_builder:_collect_records_from_json`

**Assinatura:**
```python
def _collect_records_from_json(value: Any, bucket_name: str = '') -> list[StudyRecord]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_builder.py`
**Linhas:** 289–306
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Coleta a responsabilidade representada por `records_from_json` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |
| `bucket_name` | positional-or-keyword | `str` | `''` | Parâmetro `bucket_name` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[StudyRecord]`. Retornos observados estaticamente incluem: `records`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_collect_records_from_json`
- `_record_from_any`
- `isinstance`
- `normalize_title_loose`
- `records.append`
- `records.extend`
- `str`
- `value.items`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_builder:_collect_records_from_json`
- `app_bundle.scripts.pipeline.prisma_builder:load_records_from_research_dir`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 8
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `a4ad333b1b98465a27bddc03a81648e052b7016e85b009759da5874031ce336c`
**LOC do arquivo-fonte:** 437


### `app_bundle.scripts.pipeline.prisma_builder:load_records_from_research_dir`

**Assinatura:**
```python
def load_records_from_research_dir(path: Path | None) -> tuple[list[StudyRecord], list[str]]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_builder.py`
**Linhas:** 309–334
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Carrega a responsabilidade representada por `records_from_research_dir` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path \| None` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `tuple[list[StudyRecord], list[str]]`. Retornos observados estaticamente incluem: `([], [])`; `(unique, sources)`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `_collect_records_from_json`
- `json.loads`
- `json_path.read_text`
- `normalize_title_loose`
- `path.exists`
- `path.is_dir`
- `path.rglob`
- `rec.doi.lower`
- `records.extend`
- `seen.add`
- `set`
- `sorted`
- `sources.append`
- `str`
- `unique.append`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_builder:build_prisma_report`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `*.json`

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 2
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `a4ad333b1b98465a27bddc03a81648e052b7016e85b009759da5874031ce336c`
**LOC do arquivo-fonte:** 437


### `app_bundle.scripts.pipeline.prisma_builder:classify_records`

**Assinatura:**
```python
def classify_records(records: list[StudyRecord]) -> tuple[list[StudyRecord], list[StudyRecord], list[StudyRecord], list[StudyRecord]]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_builder.py`
**Linhas:** 337–350
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `classify_records` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `records` | positional-or-keyword | `list[StudyRecord]` | `obrigatório/ausente` | Parâmetro `records` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `tuple[list[StudyRecord], list[StudyRecord], list[StudyRecord], list[StudyRecord]]`. Retornos observados estaticamente incluem: `(included, excluded, duplicates, all_records)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `all_records.append`
- `duplicates.append`
- `excluded.append`
- `included.append`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_builder:build_prisma_report`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `a4ad333b1b98465a27bddc03a81648e052b7016e85b009759da5874031ce336c`
**LOC do arquivo-fonte:** 437


### `app_bundle.scripts.pipeline.prisma_builder:build_prisma_report`

**Assinatura:**
```python
def build_prisma_report(cfg: dict[str, Any], docs: list[SourceDoc], orientations: list[SourceDoc], bib_result: BibBuildResult, output_dir: Path, prefix: str) -> PrismaReport
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_builder.py`
**Linhas:** 353–437
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Constrói a responsabilidade representada por `prisma_report` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `docs` | positional-or-keyword | `list[SourceDoc]` | `obrigatório/ausente` | Coleção de documentos/entradas processados pela rotina. |
| `orientations` | positional-or-keyword | `list[SourceDoc]` | `obrigatório/ausente` | Parâmetro `orientations` recebido pela função; não há descrição individual explícita no código. |
| `bib_result` | positional-or-keyword | `BibBuildResult` | `obrigatório/ausente` | Resultado intermediário ou final utilizado pela rotina. |
| `output_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Diretório onde a rotina grava ou procura os artefatos de saída. |
| `prefix` | positional-or-keyword | `str` | `obrigatório/ausente` | Prefixo canônico usado para compor nomes de artefatos. |

#### Retorno

Anotação declarada: `PrismaReport`. Retornos observados estaticamente incluem: `PrismaReport.model_validate_json(external_json.read_text(encoding='utf-8'))`; `PrismaReport(metadata=metadata_from_config(cfg), search_strategy=search_strategy_from_config(cfg), criteria=criteria_from_config(cfg), flow=flow, included_studies=included, excluded_studies=excluded, duplicate_studies=duplicates, all_records=all_records, diagnostics=diagnostics)`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `(cfg.get('pipeline', {}) if isinstance(cfg.get('pipeline'), dict) else {}).get`
- `Diagnostics`
- `Path`
- `Path(str(cfg.get('__config_dir__') or Path.cwd())).resolve`
- `Path.cwd`
- `PrismaFlow`
- `PrismaReport`
- `PrismaReport.model_validate_json`
- `cfg.get`
- `classify_records`
- `criteria_from_config`
- `diagnostics.avisos.append`
- `external_json.exists`
- `external_json.read_text`
- `included.copy`
- `int`
- `isinstance`
- `len`
- `list`
- `load_prompt_bundle`
- `load_prompt_bundle(cfg, 'prisma').report`
- `load_records_from_research_dir`
- `max`
- `meta.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_pipeline:run_prisma_report_outputs`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 9
loops: 2
comprehensions: 4
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `a4ad333b1b98465a27bddc03a81648e052b7016e85b009759da5874031ce336c`
**LOC do arquivo-fonte:** 437


### `app_bundle.scripts.pipeline.prisma_busca_externa:_now`

**Assinatura:**
```python
def _now() -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 151–152
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_now` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `datetime.now(timezone.utc).isoformat(timespec='seconds')`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `datetime.now`
- `datetime.now(timezone.utc).isoformat`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_project_root`

**Assinatura:**
```python
def _project_root() -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 155–157
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_project_root` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `Path(__file__).resolve().parents[3]`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(__file__).resolve`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:load_search_environment`

**Assinatura:**
```python
def load_search_environment() -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 160–166
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Carrega ``.env`` da raiz sem sobrescrever variáveis já exportadas.

**Docstring resumida:** Carrega ``.env`` da raiz sem sobrescrever variáveis já exportadas.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Lê configuração/credenciais do ambiente do processo.

#### Chamadas realizadas pelo corpo direto

- `_project_root`
- `env_path.is_file`
- `load_dotenv`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_env`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_query_config`
- `app_bundle.scripts.pipeline.prisma_busca_externa:provider_statuses`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_has_env`

**Assinatura:**
```python
def _has_env(name: str) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 169–170
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Verifica a presença/condição de `env` conforme as regras codificadas na rotina.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `name` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `name` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `bool(os.getenv(name, '').strip())`.

#### Efeitos colaterais e fronteiras externas

- Lê configuração/credenciais do ambiente do processo.

#### Chamadas realizadas pelo corpo direto

- `bool`
- `os.getenv`
- `os.getenv(name, '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_missing_required_credentials`
- `app_bundle.scripts.pipeline.prisma_busca_externa:provider_statuses`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:provider_statuses`

**Assinatura:**
```python
def provider_statuses() -> dict[str, dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 173–209
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Retorna status de configuração sem revelar valores secretos.

**Docstring resumida:** Retorna status de configuração sem revelar valores secretos.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `dict[str, dict[str, Any]]`. Retornos observados estaticamente incluem: `values`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_PROVIDER_CREDENTIALS.get`
- `_REQUIRED_ENV.get`
- `_has_env`
- `load_search_environment`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_prisma_busca_orientada`
- `app_bundle.scripts.pipeline.prisma_busca_externa:provider_selection_choices`
- `diagnosticar_fontes_prisma:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 1
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:provider_selection_choices`

**Assinatura:**
```python
def provider_selection_choices() -> list[tuple[str, str]]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 212–220
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Opções para o wizard, incluindo o atalho explícito ``Todas``.

**Docstring resumida:** Opções para o wizard, incluindo o atalho explícito ``Todas``.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `list[tuple[str, str]]`. Retornos observados estaticamente incluem: `values`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `provider_statuses`
- `values.append`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:collect_prisma_busca_orientada`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:expand_provider_selection`

**Assinatura:**
```python
def expand_provider_selection(values: Iterable[Any]) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 223–241
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Expande a escolha especial ``todas`` e normaliza aliases históricos.

**Docstring resumida:** Expande a escolha especial ``todas`` e normaliza aliases históricos.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `values` | positional-or-keyword | `Iterable[Any]` | `obrigatório/ausente` | Parâmetro `values` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `list(PROVIDER_ORDER)`; `list(dict.fromkeys((item for item in requested if item in PROVIDER_ORDER)))`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `aliases.get`
- `dict.fromkeys`
- `list`
- `str`
- `str(item).strip`
- `str(item).strip().lower`
- `str(item).strip().lower().replace`
- `str(item).strip().lower().replace('-', '_').replace`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:ask_many_choice`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_query_config`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_section`

**Assinatura:**
```python
def _section(cfg: dict[str, Any], name: str) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 244–246
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_section` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `name` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `name` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `value if isinstance(value, dict) else {}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `cfg.get`
- `isinstance`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:external_search_enabled`

**Assinatura:**
```python
def external_search_enabled(cfg: dict[str, Any]) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 249–251
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `external_search_enabled` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `bool(section.get('ativo', False)) and str(section.get('modo') or '').strip().lower() == 'busca_externa'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_section`
- `bool`
- `section.get`
- `str`
- `str(section.get('modo') or '').strip`
- `str(section.get('modo') or '').strip().lower`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.doctor_runtime:run_doctor_command`
- `academic_pipeline.prisma_generic_orchestration:run_prisma_stage_002`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_list`

**Assinatura:**
```python
def _list(value: Any) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 254–259
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Lista a responsabilidade representada por `list` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `[]`; `[str(item).strip() for item in value if str(item).strip()]`; `[item.strip() for item in re.split('[;,\\n]+', str(value)) if item.strip()]`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `isinstance`
- `item.strip`
- `re.split`
- `str`
- `str(item).strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_merge`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_pretriage_prompt`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_query_config`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_semantic_scholar`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_strategy_rows`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_tag_query_provenance`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_write_csv`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_write_xlsx`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_norm`

**Assinatura:**
```python
def _norm(value: Any) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 262–265
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_norm` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `re.sub('\\s+', ' ', text.lower()).strip()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `''.join`
- `re.sub`
- `re.sub('\\s+', ' ', text.lower()).strip`
- `str`
- `text.lower`
- `unicodedata.combining`
- `unicodedata.normalize`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_decision`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_normalize_ai_recommendation`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_safe_strategy_id`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_score`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_title_key`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_doi`

**Assinatura:**
```python
def _doi(value: Any) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 268–272
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_doi` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `text.rstrip('.,;)')`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `re.sub`
- `str`
- `str(value or '').strip`
- `str(value or '').strip().lower`
- `text.rstrip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_deduplicate`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_enrich_with_unpaywall`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_record`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `^https?://(?:dx\.)?doi\.org/`

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_title_key`

**Assinatura:**
```python
def _title_key(value: Any) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 275–276
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_title_key` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `re.sub('[^a-z0-9]+', '', _norm(value))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_norm`
- `re.sub`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_deduplicate`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_int`

**Assinatura:**
```python
def _int(value: Any) -> int | None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 279–283
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_int` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `int | None`. Retornos observados estaticamente incluem: `int(value)`; `None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `int`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_merge`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_record`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_authors`

**Assinatura:**
```python
def _authors(value: Any) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 286–297
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_authors` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'; '.join(result)`; `str(value or '').strip()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'; '.join`
- `isinstance`
- `item.get`
- `result.append`
- `str`
- `str(item or '').strip`
- `str(item.get('name') or item.get('display_name') or item.get('full_name') or item.get('family') or '').strip`
- `str(value or '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_record`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_openalex_abstract`

**Assinatura:**
```python
def _openalex_abstract(value: Any) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 300–312
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_openalex_abstract` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `''`; `' '.join((token for _pos, token in sorted(parts)))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `int`
- `isinstance`
- `parts.append`
- `sorted`
- `str`
- `value.items`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_openalex`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 2
comprehensions: 1
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_safe_url`

**Assinatura:**
```python
def _safe_url(url: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 315–322
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Remove credenciais e e-mails antes de registrar qualquer URL.

**Docstring resumida:** Remove credenciais e e-mails antes de registrar qualquer URL.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `url` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `url` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `re.sub('([?&](?:apiKey|api_key|key|token|insttoken|mailto|email)=)[^&]+', '\\1REDACTED', str(url or ''), flags=re.I)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `re.sub`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_core`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_crossref`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_europe_pmc`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_openalex`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_pubmed`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_scielo`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_scopus`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_semantic_scholar`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_wos`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_retry_after_seconds`

**Assinatura:**
```python
def _retry_after_seconds(value: Any) -> float | None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 325–340
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Converte Retry-After em segundos, aceitando segundos ou data HTTP.

**Docstring resumida:** Converte Retry-After em segundos, aceitando segundos ou data HTTP.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `float | None`. Retornos observados estaticamente incluem: `None`; `max(0.0, float(raw))`; `max(0.0, (parsed - datetime.now(timezone.utc)).total_seconds())`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `(parsed - datetime.now(timezone.utc)).total_seconds`
- `datetime.now`
- `float`
- `max`
- `parsed.replace`
- `parsedate_to_datetime`
- `str`
- `str(value or '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_request_text`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 2
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_semantic_scholar_debug`

**Assinatura:**
```python
def _semantic_scholar_debug(message: str) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 343–347
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Emite detalhes de espera somente quando o diagnóstico os solicitar.

**Docstring resumida:** Emite detalhes de espera somente quando o diagnóstico os solicitar.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `message` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `message` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.
- Lê configuração/credenciais do ambiente do processo.
- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `os.getenv`
- `print`
- `str`
- `str(os.getenv('SEMANTIC_SCHOLAR_RATE_LIMIT_DEBUG') or '').strip`
- `str(os.getenv('SEMANTIC_SCHOLAR_RATE_LIMIT_DEBUG') or '').strip().lower`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_provider_retry_debug`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_wait_for_semantic_scholar`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_provider_retry_debug`

**Assinatura:**
```python
def _provider_retry_debug(provider: str, message: str) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 350–357
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Emite detalhes de retentativa apenas quando explicitamente solicitado.

**Docstring resumida:** Emite detalhes de retentativa apenas quando explicitamente solicitado.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `provider` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `provider` recebido pela função; não há descrição individual explícita no código. |
| `message` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `message` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.
- Lê configuração/credenciais do ambiente do processo.
- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `PROVIDER_LABELS.get`
- `_semantic_scholar_debug`
- `os.getenv`
- `print`
- `str`
- `str(os.getenv('PRISMA_HTTP_RETRY_DEBUG') or '').strip`
- `str(os.getenv('PRISMA_HTTP_RETRY_DEBUG') or '').strip().lower`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_core`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_request_text.wait_before_retry`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_wait_for_core`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:ProviderUnavailableError.__init__`

**Assinatura:**
```python
def __init__(self, provider: str, message: str) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 363–365
**Tipo:** method
**Visibilidade:** especial/dunder
**Classe proprietária:** `ProviderUnavailableError`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `__init__` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |
| `provider` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `provider` recebido pela função; não há descrição individual explícita no código. |
| `message` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `message` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `super`
- `super().__init__`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_request_text`

**Assinatura:**
```python
def _request_text(url: str, *, headers: dict[str, str] | None = None, timeout: int = 40, before_attempt: Callable[[], None] | None = None, retryable_http_statuses: Iterable[int] = (), http_retries: int = 0, retry_base_seconds: float = 2.0, retry_max_seconds: float = 45.0, retry_provider: str = '') -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 368–435
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Obtém texto de uma API com retentativas explicitamente opt-in.

**Docstring resumida:** Obtém texto de uma API com retentativas explicitamente opt-in.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `url` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `url` recebido pela função; não há descrição individual explícita no código. |
| `headers` | keyword-only | `dict[str, str] \| None` | `None` | Cabeçalhos usados em requisição/integração externa. |
| `timeout` | keyword-only | `int` | `40` | Tempo máximo de espera para a operação, quando aplicável. |
| `before_attempt` | keyword-only | `Callable[[], None] \| None` | `None` | Parâmetro `before_attempt` recebido pela função; não há descrição individual explícita no código. |
| `retryable_http_statuses` | keyword-only | `Iterable[int]` | `()` | Parâmetro `retryable_http_statuses` recebido pela função; não há descrição individual explícita no código. |
| `http_retries` | keyword-only | `int` | `0` | Parâmetro `http_retries` recebido pela função; não há descrição individual explícita no código. |
| `retry_base_seconds` | keyword-only | `float` | `2.0` | Parâmetro `retry_base_seconds` recebido pela função; não há descrição individual explícita no código. |
| `retry_max_seconds` | keyword-only | `float` | `45.0` | Parâmetro `retry_max_seconds` recebido pela função; não há descrição individual explícita no código. |
| `retry_provider` | keyword-only | `str` | `''` | Parâmetro `retry_provider` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `response.read().decode('utf-8', errors='replace')`.

#### Efeitos colaterais e fronteiras externas

- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `Request`
- `RuntimeError`
- `_retry_after_seconds`
- `before_attempt`
- `exc.close`
- `exc.headers.get`
- `exc.read`
- `exc.read().decode`
- `float`
- `getattr`
- `int`
- `max`
- `range`
- `request_headers.update`
- `response.read`
- `response.read().decode`
- `urlopen`
- `wait_before_retry`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_request_json`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_scielo`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError(f'HTTP {exc.code}: {body}')`
- `RuntimeError(f'Falha de rede: {exc.reason}')`
- `RuntimeError('Tempo esgotado na consulta bibliográfica.')`
- `RuntimeError('A consulta bibliográfica terminou sem resposta ou exceção.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 1
comprehensions: 1
exception_handlers: 4
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_request_text.wait_before_retry`

**Assinatura:**
```python
def wait_before_retry(reason: str, attempt: int, retry_after: float | None = None) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 397–405
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `wait_before_retry` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `reason` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `reason` recebido pela função; não há descrição individual explícita no código. |
| `attempt` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `attempt` recebido pela função; não há descrição individual explícita no código. |
| `retry_after` | positional-or-keyword | `float \| None` | `None` | Parâmetro `retry_after` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_provider_retry_debug`
- `max`
- `min`
- `time.sleep`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_request_text`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_request_json`

**Assinatura:**
```python
def _request_json(url: str, *, headers: dict[str, str] | None = None, timeout: int = 40, before_attempt: Callable[[], None] | None = None, retryable_http_statuses: Iterable[int] = (), http_retries: int = 0, retry_base_seconds: float = 2.0, retry_max_seconds: float = 45.0, retry_provider: str = '') -> Any
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 438–464
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_request_json` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `url` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `url` recebido pela função; não há descrição individual explícita no código. |
| `headers` | keyword-only | `dict[str, str] \| None` | `None` | Cabeçalhos usados em requisição/integração externa. |
| `timeout` | keyword-only | `int` | `40` | Tempo máximo de espera para a operação, quando aplicável. |
| `before_attempt` | keyword-only | `Callable[[], None] \| None` | `None` | Parâmetro `before_attempt` recebido pela função; não há descrição individual explícita no código. |
| `retryable_http_statuses` | keyword-only | `Iterable[int]` | `()` | Parâmetro `retryable_http_statuses` recebido pela função; não há descrição individual explícita no código. |
| `http_retries` | keyword-only | `int` | `0` | Parâmetro `http_retries` recebido pela função; não há descrição individual explícita no código. |
| `retry_base_seconds` | keyword-only | `float` | `2.0` | Parâmetro `retry_base_seconds` recebido pela função; não há descrição individual explícita no código. |
| `retry_max_seconds` | keyword-only | `float` | `45.0` | Parâmetro `retry_max_seconds` recebido pela função; não há descrição individual explícita no código. |
| `retry_provider` | keyword-only | `str` | `''` | Parâmetro `retry_provider` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Any`. Retornos observados estaticamente incluem: `json.loads(text)`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `RuntimeError`
- `_request_text`
- `json.loads`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_core.fetch`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_crossref`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_enrich_with_unpaywall`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_europe_pmc`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_openalex`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_pubmed`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_scopus`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_semantic_scholar`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_wos`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('A fonte não retornou JSON válido.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_record`

**Assinatura:**
```python
def _record(source: str, source_id: Any, *, title: Any = '', authors: Any = '', year: Any = '', venue: Any = '', doi: Any = '', url: Any = '', abstract: Any = '', pdf: Any = '', citations: Any = None, kind: Any = '') -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 466–481
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_record` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `source` | positional-or-keyword | `str` | `obrigatório/ausente` | Origem dos dados ou arquivo de entrada. |
| `source_id` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `source_id` recebido pela função; não há descrição individual explícita no código. |
| `title` | keyword-only | `Any` | `''` | Parâmetro `title` recebido pela função; não há descrição individual explícita no código. |
| `authors` | keyword-only | `Any` | `''` | Parâmetro `authors` recebido pela função; não há descrição individual explícita no código. |
| `year` | keyword-only | `Any` | `''` | Parâmetro `year` recebido pela função; não há descrição individual explícita no código. |
| `venue` | keyword-only | `Any` | `''` | Parâmetro `venue` recebido pela função; não há descrição individual explícita no código. |
| `doi` | keyword-only | `Any` | `''` | Digital Object Identifier da referência bibliográfica. |
| `url` | keyword-only | `Any` | `''` | Parâmetro `url` recebido pela função; não há descrição individual explícita no código. |
| `abstract` | keyword-only | `Any` | `''` | Parâmetro `abstract` recebido pela função; não há descrição individual explícita no código. |
| `pdf` | keyword-only | `Any` | `''` | Parâmetro `pdf` recebido pela função; não há descrição individual explícita no código. |
| `citations` | keyword-only | `Any` | `None` | Parâmetro `citations` recebido pela função; não há descrição individual explícita no código. |
| `kind` | keyword-only | `Any` | `''` | Parâmetro `kind` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `{'id_registro': f'{source}:{source_id}', 'titulo': re.sub('\\s+', ' ', str(title or '')).strip(), 'autores': _authors(authors), 'ano': str(year or '').strip(), 'periodico': re.sub('\\s+', ' ', str(venue or '')).strip(), 'doi': _doi(doi), 'url': str(url or '').strip(), 'url_pdf_aberto': str(pdf or '').strip(), 'resumo': re.sub('\\s+', ' ', re.sub('<[^>]+>', ' ', str(abstract or ''))).strip(), 'fontes': [source], 'pontuacao_relevancia': 0.0, 'citacoes': _int(citations), 'tipo_publicacao': str(kind or '').strip()}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_authors`
- `_doi`
- `_int`
- `re.sub`
- `re.sub('\\s+', ' ', re.sub('<[^>]+>', ' ', str(abstract or ''))).strip`
- `re.sub('\\s+', ' ', str(title or '')).strip`
- `re.sub('\\s+', ' ', str(venue or '')).strip`
- `str`
- `str(kind or '').strip`
- `str(pdf or '').strip`
- `str(url or '').strip`
- `str(year or '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_core`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_crossref`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_europe_pmc`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_openalex`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_pubmed`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_scielo`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_scopus`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_semantic_scholar`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_wos`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_env`

**Assinatura:**
```python
def _env(name: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 484–486
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_env` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `name` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `name` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `os.getenv(name, '').strip()`.

#### Efeitos colaterais e fronteiras externas

- Lê configuração/credenciais do ambiente do processo.

#### Chamadas realizadas pelo corpo direto

- `load_search_environment`
- `os.getenv`
- `os.getenv(name, '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_core`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_enrich_with_unpaywall`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_first_env`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_openalex`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_provider_email`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_pubmed`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_scielo`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_scopus`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_wos`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_first_env`

**Assinatura:**
```python
def _first_env(*names: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 489–495
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Retorna a primeira variável não vazia sem expor seu conteúdo.

**Docstring resumida:** Retorna a primeira variável não vazia sem expor seu conteúdo.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `names` | var-positional | `str` | `obrigatório/ausente` | Parâmetro `names` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `value`; `''`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_env`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_semantic_scholar`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_semantic_scholar_min_interval`

**Assinatura:**
```python
def _semantic_scholar_min_interval(config: dict[str, Any]) -> float
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 498–504
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_semantic_scholar_min_interval` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `config` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração ou estrutura de configuração recebida pela rotina. |

#### Retorno

Anotação declarada: `float`. Retornos observados estaticamente incluem: `max(1.0, min(value, 10.0))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `config.get`
- `float`
- `max`
- `min`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_query_config`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_semantic_scholar`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_wait_for_semantic_scholar`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_semantic_scholar_max_retries`

**Assinatura:**
```python
def _semantic_scholar_max_retries(config: dict[str, Any]) -> int
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 507–513
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_semantic_scholar_max_retries` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `config` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração ou estrutura de configuração recebida pela rotina. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `max(0, min(value, 8))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `config.get`
- `int`
- `max`
- `min`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_build_provider_retry_settings`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_query_config`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_semantic_scholar_retry_base_seconds`

**Assinatura:**
```python
def _semantic_scholar_retry_base_seconds(config: dict[str, Any]) -> float
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 516–524
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_semantic_scholar_retry_base_seconds` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `config` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração ou estrutura de configuração recebida pela rotina. |

#### Retorno

Anotação declarada: `float`. Retornos observados estaticamente incluem: `max(0.25, min(value, 60.0))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `config.get`
- `float`
- `max`
- `min`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_build_provider_retry_settings`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_query_config`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_semantic_scholar_retry_max_seconds`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_semantic_scholar_retry_max_seconds`

**Assinatura:**
```python
def _semantic_scholar_retry_max_seconds(config: dict[str, Any]) -> float
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 527–535
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_semantic_scholar_retry_max_seconds` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `config` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração ou estrutura de configuração recebida pela rotina. |

#### Retorno

Anotação declarada: `float`. Retornos observados estaticamente incluem: `max(_semantic_scholar_retry_base_seconds(config), min(value, 300.0))`.

#### Efeitos colaterais e fronteiras externas

- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `_semantic_scholar_retry_base_seconds`
- `config.get`
- `float`
- `max`
- `min`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_build_provider_retry_settings`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_query_config`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_bounded_int`

**Assinatura:**
```python
def _bounded_int(value: Any, default: int, low: int, high: int) -> int
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 538–543
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_bounded_int` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |
| `default` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `default` recebido pela função; não há descrição individual explícita no código. |
| `low` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `low` recebido pela função; não há descrição individual explícita no código. |
| `high` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `high` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `max(low, min(parsed, high))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `int`
- `max`
- `min`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_build_provider_retry_settings`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_core`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_core_degraded_limit`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_core_degraded_max_retries`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_core_timeout`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_query_config`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_bounded_float`

**Assinatura:**
```python
def _bounded_float(value: Any, default: float, low: float, high: float) -> float
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 546–551
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_bounded_float` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |
| `default` | positional-or-keyword | `float` | `obrigatório/ausente` | Parâmetro `default` recebido pela função; não há descrição individual explícita no código. |
| `low` | positional-or-keyword | `float` | `obrigatório/ausente` | Parâmetro `low` recebido pela função; não há descrição individual explícita no código. |
| `high` | positional-or-keyword | `float` | `obrigatório/ausente` | Parâmetro `high` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `float`. Retornos observados estaticamente incluem: `max(low, min(parsed, high))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `float`
- `max`
- `min`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_build_provider_retry_settings`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_core_degraded_cooldown_seconds`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_core_min_interval`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_query_config`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_build_provider_retry_settings`

**Assinatura:**
```python
def _build_provider_retry_settings(search: dict[str, Any], provider: str) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 554–591
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Normaliza políticas de retentativa sem exigir novas perguntas no TUI.

**Docstring resumida:** Normaliza políticas de retentativa sem exigir novas perguntas no TUI.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `search` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `search` recebido pela função; não há descrição individual explícita no código. |
| `provider` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `provider` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `{'max_retries': _semantic_scholar_max_retries(search), 'retry_base_seconds': _semantic_scholar_retry_base_seconds(search), 'retry_max_seconds': _semantic_scholar_retry_max_seconds(search)}`; `{'max_retries': _bounded_int(search.get(f'{prefix}_max_retries'), max_default, 0, 8), 'retry_base_seconds': _bounded_float(search.get(f'{prefix}_retry_base_seconds'), base_default, 0.25, 60.0), 'retry_max_seconds': _bounded_float(search.get(f'{prefix}_retry_max_seconds'), cap_default, base_default, 300.0)}`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `_bounded_float`
- `_bounded_int`
- `_semantic_scholar_max_retries`
- `_semantic_scholar_retry_base_seconds`
- `_semantic_scholar_retry_max_seconds`
- `provider.replace`
- `search.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_provider_retry_settings`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_query_config`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_provider_retry_settings`

**Assinatura:**
```python
def _provider_retry_settings(config: dict[str, Any], provider: str) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 594–600
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_provider_retry_settings` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `config` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração ou estrutura de configuração recebida pela rotina. |
| `provider` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `provider` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `value`; `_build_provider_retry_settings({}, provider)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_build_provider_retry_settings`
- `config.get`
- `configured.get`
- `isinstance`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_core`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_core_degraded_cooldown_seconds`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_core_degraded_limit`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_core_degraded_max_retries`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_core_min_interval`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_core_timeout`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_crossref`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_semantic_scholar`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_core_min_interval`

**Assinatura:**
```python
def _core_min_interval(config: dict[str, Any]) -> float
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 603–610
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_core_min_interval` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `config` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração ou estrutura de configuração recebida pela rotina. |

#### Retorno

Anotação declarada: `float`. Retornos observados estaticamente incluem: `_bounded_float(settings.get('min_interval_seconds'), CORE_DEFAULT_MIN_INTERVAL, 0.25, 10.0)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_bounded_float`
- `_provider_retry_settings`
- `settings.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_core`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_wait_for_core`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_core_timeout`

**Assinatura:**
```python
def _core_timeout(config: dict[str, Any]) -> int
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 613–615
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_core_timeout` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `config` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração ou estrutura de configuração recebida pela rotina. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `_bounded_int(settings.get('timeout_seconds'), CORE_DEFAULT_TIMEOUT, 10, 90)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_bounded_int`
- `_provider_retry_settings`
- `settings.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_core.fetch`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_core_degraded_limit`

**Assinatura:**
```python
def _core_degraded_limit(config: dict[str, Any]) -> int
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 618–625
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_core_degraded_limit` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `config` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração ou estrutura de configuração recebida pela rotina. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `_bounded_int(settings.get('degraded_limit'), CORE_DEFAULT_DEGRADED_LIMIT, 1, 100)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_bounded_int`
- `_provider_retry_settings`
- `settings.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_core`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_core_degraded_cooldown_seconds`

**Assinatura:**
```python
def _core_degraded_cooldown_seconds(config: dict[str, Any]) -> float
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 628–635
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_core_degraded_cooldown_seconds` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `config` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração ou estrutura de configuração recebida pela rotina. |

#### Retorno

Anotação declarada: `float`. Retornos observados estaticamente incluem: `_bounded_float(settings.get('degraded_cooldown_seconds'), CORE_DEFAULT_DEGRADED_COOLDOWN_SECONDS, 1.0, 180.0)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_bounded_float`
- `_provider_retry_settings`
- `settings.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_core`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_core_degraded_max_retries`

**Assinatura:**
```python
def _core_degraded_max_retries(config: dict[str, Any]) -> int
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 638–645
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_core_degraded_max_retries` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `config` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração ou estrutura de configuração recebida pela rotina. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `_bounded_int(settings.get('degraded_max_retries'), CORE_DEFAULT_DEGRADED_MAX_RETRIES, 0, 4)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_bounded_int`
- `_provider_retry_settings`
- `settings.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_core`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_is_retryable_core_failure`

**Assinatura:**
```python
def _is_retryable_core_failure(exc: Exception) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 648–653
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Distingue indisponibilidade transitória de erro de credencial/formato.

**Docstring resumida:** Distingue indisponibilidade transitória de erro de credencial/formato.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `exc` | positional-or-keyword | `Exception` | `obrigatório/ausente` | Parâmetro `exc` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `True`; `any((f'http {status}' in message for status in RETRYABLE_HTTP_STATUS_CODES))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `any`
- `str`
- `str(exc or '').casefold`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_core`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_wait_for_core`

**Assinatura:**
```python
def _wait_for_core(config: dict[str, Any]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 656–665
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Evita sequência agressiva contra a API CORE durante consultas em blocos.

**Docstring resumida:** Evita sequência agressiva contra a API CORE durante consultas em blocos.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `config` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração ou estrutura de configuração recebida pela rotina. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_core_min_interval`
- `_provider_retry_debug`
- `time.monotonic`
- `time.sleep`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_core.fetch`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_wait_for_semantic_scholar`

**Assinatura:**
```python
def _wait_for_semantic_scholar(config: dict[str, Any]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 668–677
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Aplica a cota cumulativa de uma requisição/segundo do Semantic Scholar.

**Docstring resumida:** Aplica a cota cumulativa de uma requisição/segundo do Semantic Scholar.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `config` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração ou estrutura de configuração recebida pela rotina. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `_semantic_scholar_debug`
- `_semantic_scholar_min_interval`
- `time.monotonic`
- `time.sleep`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_provider_email`

**Assinatura:**
```python
def _provider_email(provider: str, fallback: str = '') -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 680–692
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_provider_email` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `provider` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `provider` recebido pela função; não há descrição individual explícita no código. |
| `fallback` | positional-or-keyword | `str` | `''` | Parâmetro `fallback` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `value`; `str(fallback or '').strip()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_env`
- `fields.get`
- `str`
- `str(fallback or '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_query_config`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_safe_strategy_id`

**Assinatura:**
```python
def _safe_strategy_id(value: Any, fallback: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 695–697
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_safe_strategy_id` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |
| `fallback` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `fallback` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `token or fallback`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `_norm`
- `_norm(value).replace`
- `re.sub`
- `re.sub('[^a-z0-9_]+', '_', _norm(value).replace(' ', '_')).strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_strategy_rows`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_strategy_rows`

**Assinatura:**
```python
def _strategy_rows(search: dict[str, Any], *, fallback_query: str) -> list[dict[str, str]]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 700–744
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Normaliza os blocos e executa cada consulta curta independentemente.

**Docstring resumida:** Normaliza os blocos e executa cada consulta curta independentemente.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `search` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `search` recebido pela função; não há descrição individual explícita no código. |
| `fallback_query` | keyword-only | `str` | `obrigatório/ausente` | Parâmetro `fallback_query` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[dict[str, str]]`. Retornos observados estaticamente incluem: `normalized`; `[{'id': 'consulta_unica', 'bloco_id': 'consulta_unica', 'bloco_rotulo': 'Consulta única', 'rotulo': 'Consulta única', 'consulta': fallback_query}]`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_list`
- `_safe_strategy_id`
- `enumerate`
- `isinstance`
- `item.get`
- `len`
- `normalized.append`
- `rows.append`
- `search.get`
- `seen.add`
- `set`
- `single.split`
- `str`
- `str(item.get('consulta') or item.get('query') or '').strip`
- `str(item.get('rotulo') or item.get('label') or f'Bloco temático {block_index}').strip`
- `str(value).strip`
- `value.strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_query_config`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 3
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_query_config`

**Assinatura:**
```python
def _query_config(cfg: dict[str, Any]) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 747–845
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_query_config` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `{'providers': providers, 'query': fallback_query or ' | '.join((item['consulta'] for item in strategy_rows)), 'query_summary': ' | '.join((item['consulta'] for item in strategy_rows)), 'strategy_mode': strategy_mode, 'query_plans': strategy_rows, 'keywords': keywords, 'research_theme': str(research.get('tema') or '').strip(), 'research_scope': str(research.get('recorte') or '').strip(), 'research_objective': str(research.get('objetivo') or '').strip(), 'research_question': str(research.get('pergunta_pesquisa') or '').strip(), 'per_base': per_base, 'scopus_per_query': clamp(search.get('limite_scopus_por_consulta'), min(per_base, 10), 1, 25), 'initial_limit': clamp(search.get('limite_triagem_inicial'), 250, 10, 1000), 'target': clamp(search.get('meta_estudos_incluidos'), 15, 1, 500), 'year_start': str(search.get('ano_inicio') or '').strip(), 'year_end': str(search.get('ano_fim') or '').strip(), 'email': fallback_email, 'emails': {name: _provider_email(name, fallback_email) for name in ('crossref', 'openalex', 'pubmed', 'europe_pmc', 'core')}, 'inclusion': _list(search.get('criterios_inclusao') or _section(cfg, 'relatorio_pesquisa').get('criterios_inclusao')), 'exclusion': _list(search.get('criterios_exclusao') or _section(cfg, 'relatorio_pesquisa').get('criterios_exclusao')), 'corpus_local': str(search.get('corpus_local_opcional') or '').strip(), 'enrich_unpaywall': bool(search.get('enriquecer_unpaywall', False)), 'unpaywall_limit': clamp(search.get('limite_unpaywall'), 100, 0, 250), 'ai_screen_enabled': bool(search.get('pre_triagem_ia', False)), 'ai_screen_model': str(search.get('pre_triagem_ia_modelo') or '').strip(), 'ai_screen_batch_size': clamp(search.get('pre_triagem_ia_lote'), 20, 5, 30), 'ai_screen_max_records': clamp(search.get('pre_triagem_ia_max_registros'), 1500, 10, 5000), 'ai_screen_review_reserve': clamp(search.get('pre_triagem_ia_reserva_incertos'), 40, 0, 1000), 'ai_screen_min_confidence': clamp(search.get('pre_triagem_ia_min_confianca'), 55, 0, 100), 'ai_screen_max_abstract_chars': clamp(search.get('pre_triagem_ia_max_chars_resumo'), 700, 200, 1500), 'semantic_scholar_min_interval': _semantic_scholar_min_interval(search), 'semantic_scholar_max_retries': _semantic_scholar_max_retries(search), 'semantic_scholar_retry_base_seconds': _semantic_scholar_retry_base_seconds(search), 'semantic_scholar_retry_max_seconds': _semantic_scholar_retry_max_seconds(search), 'provider_retry_settings': {'crossref': _build_provider_retry_settings(search, 'crossref'), 'semantic_scholar': _build_provider_retry_settings(search, 'semantic_scholar'), 'core': {**_build_provider_retry_settings(search, 'core'), 'min_interval_seconds': _bounded_float(search.get('core_min_interval_seconds'), CORE_DEFAULT_MIN_INTERVAL, 0.25, 10.0), 'timeout_seconds': _bounded_int(search.get('core_timeout_seconds'), CORE_DEFAULT_TIMEOUT, 10, 90), 'degraded_limit': _bounded_int(search.get('core_degraded_limit'), CORE_DEFAULT_DEGRADED_LIMIT, 1, 100), 'degraded_cooldown_seconds': _bounded_float(search.get('core_degraded_cooldown_seconds'), CORE_DEFAULT_DEGRADED_COOLDOWN_SECONDS, 1.0, 180.0), 'degraded_max_retries': _bounded_int(search.get('core_degraded_max_retries'), CORE_DEFAULT_DEGRADED_MAX_RETRIES, 0, 4)}}}`.

#### Efeitos colaterais e fronteiras externas

- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `' '.join(keywords).strip`
- `' | '.join`
- `RuntimeError`
- `_bounded_float`
- `_bounded_int`
- `_build_provider_retry_settings`
- `_list`
- `_provider_email`
- `_section`
- `_section(cfg, 'relatorio_pesquisa').get`
- `_semantic_scholar_max_retries`
- `_semantic_scholar_min_interval`
- `_semantic_scholar_retry_base_seconds`
- `_semantic_scholar_retry_max_seconds`
- `_strategy_rows`
- `any`
- `bool`
- `clamp`
- `expand_provider_selection`
- `isinstance`
- `item['consulta'].strip`
- `len`
- `load_search_environment`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:import_manual_prisma_triage`
- `app_bundle.scripts.pipeline.prisma_busca_externa:run_external_prisma_search`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('Defina ao menos uma estratégia de busca, consulta_geral, palavras_chave ou tema antes de executar a busca externa.')`
- `RuntimeError('A estratégia de busca não contém consultas utilizáveis.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 4
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_query_config.clamp`

**Assinatura:**
```python
def clamp(value: Any, default: int, low: int, high: int) -> int
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 759–763
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `clamp` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |
| `default` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `default` recebido pela função; não há descrição individual explícita no código. |
| `low` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `low` recebido pela função; não há descrição individual explícita no código. |
| `high` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `high` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `max(low, min(int(value), high))`; `default`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `int`
- `max`
- `min`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_query_config`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_crossref`

**Assinatura:**
```python
def _crossref(query: str, config: dict[str, Any]) -> tuple[list[dict[str, Any]], dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 853–887
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_crossref` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `query` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `query` recebido pela função; não há descrição individual explícita no código. |
| `config` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração ou estrutura de configuração recebida pela rotina. |

#### Retorno

Anotação declarada: `tuple[list[dict[str, Any]], dict[str, Any]]`. Retornos observados estaticamente incluem: `(records, {'provider': 'crossref', 'url': _safe_url(url), 'retrieved': len(records), 'error': ''})`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.
- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `','.join`
- `((_request_json(url, retryable_http_statuses=RETRYABLE_HTTP_STATUS_CODES, http_retries=int(retry.get('max_retries') or 0), retry_base_seconds=float(retry.get('retry_base_seconds') or CROSSREF_DEFAULT_RETRY_BASE_SECONDS), retry_max_seconds=float(retry.get('retry_max_seconds') or CROSSREF_DEFAULT_RETRY_MAX_SECONDS), retry_provider='crossref') or {}).get('message') or {}).get`
- `(_request_json(url, retryable_http_statuses=RETRYABLE_HTTP_STATUS_CODES, http_retries=int(retry.get('max_retries') or 0), retry_base_seconds=float(retry.get('retry_base_seconds') or CROSSREF_DEFAULT_RETRY_BASE_SECONDS), retry_max_seconds=float(retry.get('retry_max_seconds') or CROSSREF_DEFAULT_RETRY_MAX_SECONDS), retry_provider='crossref') or {}).get`
- `(item.get('published-print') or item.get('published-online') or item.get('issued') or {}).get`
- `_provider_retry_settings`
- `_record`
- `_request_json`
- `_safe_url`
- `enumerate`
- `filters.append`
- `float`
- `int`
- `isinstance`
- `item.get`
- `len`
- `records.append`
- `retry.get`
- `urlencode`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `query.bibliographic`
- `https://api.crossref.org/works?`

#### Complexidade estrutural observada

```yaml
branches: 7
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_openalex`

**Assinatura:**
```python
def _openalex(query: str, config: dict[str, Any]) -> tuple[list[dict[str, Any]], dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 890–916
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_openalex` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `query` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `query` recebido pela função; não há descrição individual explícita no código. |
| `config` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração ou estrutura de configuração recebida pela rotina. |

#### Retorno

Anotação declarada: `tuple[list[dict[str, Any]], dict[str, Any]]`. Retornos observados estaticamente incluem: `(records, {'provider': 'openalex', 'url': _safe_url(url), 'retrieved': len(records), 'error': ''})`.

#### Efeitos colaterais e fronteiras externas

- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `','.join`
- `'; '.join`
- `(_request_json(url) or {}).get`
- `(item_author.get('author') or {}).get`
- `_env`
- `_openalex_abstract`
- `_record`
- `_request_json`
- `_safe_url`
- `authors.append`
- `enumerate`
- `filters.append`
- `item.get`
- `item_author.get`
- `len`
- `loc.get`
- `open_access.get`
- `records.append`
- `source.get`
- `str`
- `str((item_author.get('author') or {}).get('display_name') or '').strip`
- `urlencode`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `https://api.openalex.org/works?`

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_semantic_scholar`

**Assinatura:**
```python
def _semantic_scholar(query: str, config: dict[str, Any]) -> tuple[list[dict[str, Any]], dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 919–955
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_semantic_scholar` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `query` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `query` recebido pela função; não há descrição individual explícita no código. |
| `config` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração ou estrutura de configuração recebida pela rotina. |

#### Retorno

Anotação declarada: `tuple[list[dict[str, Any]], dict[str, Any]]`. Retornos observados estaticamente incluem: `(records, {'provider': 'semantic_scholar', 'url': _safe_url(url), 'retrieved': len(records), 'error': '', 'min_interval_seconds': _semantic_scholar_min_interval(config), 'max_retries_on_transient_errors': int(retry.get('max_retries') or 0), 'retry_base_seconds': float(retry.get('retry_base_seconds') or 0.0)})`.

#### Efeitos colaterais e fronteiras externas

- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `'; '.join`
- `(_request_json(url, headers=headers, before_attempt=lambda: _wait_for_semantic_scholar(config), retryable_http_statuses=RETRYABLE_HTTP_STATUS_CODES, http_retries=int(retry.get('max_retries') or 0), retry_base_seconds=float(retry.get('retry_base_seconds') or SEMANTIC_SCHOLAR_DEFAULT_RETRY_BASE_SECONDS), retry_max_seconds=float(retry.get('retry_max_seconds') or SEMANTIC_SCHOLAR_DEFAULT_RETRY_MAX_SECONDS), retry_provider='semantic_scholar') or {}).get`
- `_first_env`
- `_list`
- `_provider_retry_settings`
- `_record`
- `_request_json`
- `_safe_url`
- `_semantic_scholar_min_interval`
- `_wait_for_semantic_scholar`
- `enumerate`
- `float`
- `identifiers.get`
- `int`
- `item.get`
- `len`
- `oa.get`
- `records.append`
- `retry.get`
- `urlencode`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `https://api.semanticscholar.org/graph/v1/paper/search?`

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_scopus`

**Assinatura:**
```python
def _scopus(query: str, config: dict[str, Any]) -> tuple[list[dict[str, Any]], dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 958–971
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_scopus` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `query` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `query` recebido pela função; não há descrição individual explícita no código. |
| `config` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração ou estrutura de configuração recebida pela rotina. |

#### Retorno

Anotação declarada: `tuple[list[dict[str, Any]], dict[str, Any]]`. Retornos observados estaticamente incluem: `(records, {'provider': 'scopus', 'url': _safe_url(url), 'retrieved': len(records), 'error': ''})`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `' OR '.join`
- `((_request_json(url, headers=headers) or {}).get('search-results') or {}).get`
- `(_request_json(url, headers=headers) or {}).get`
- `RuntimeError`
- `_env`
- `_record`
- `_request_json`
- `_safe_url`
- `config.get`
- `enumerate`
- `int`
- `item.get`
- `len`
- `min`
- `part.strip`
- `part.strip().replace`
- `re.split`
- `str`
- `urlencode`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('SCOPUS_API_KEY não configurada; a fonte foi ignorada.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 3
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_wos`

**Assinatura:**
```python
def _wos(query: str, config: dict[str, Any]) -> tuple[list[dict[str, Any]], dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 974–999
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_wos` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `query` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `query` recebido pela função; não há descrição individual explícita no código. |
| `config` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração ou estrutura de configuração recebida pela rotina. |

#### Retorno

Anotação declarada: `tuple[list[dict[str, Any]], dict[str, Any]]`. Retornos observados estaticamente incluem: `(records, {'provider': 'wos', 'url': _safe_url(url), 'retrieved': len(records), 'error': ''})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `(item.get('names') or {}).get`
- `RuntimeError`
- `_env`
- `_record`
- `_request_json`
- `_safe_url`
- `enumerate`
- `identifiers.get`
- `isinstance`
- `item.get`
- `item.get('links', {}).get`
- `len`
- `names.get`
- `payload.get`
- `records.append`
- `source.get`
- `urlencode`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('WOS_API_KEY não configurada; a fonte foi ignorada.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_pubmed`

**Assinatura:**
```python
def _pubmed(query: str, config: dict[str, Any]) -> tuple[list[dict[str, Any]], dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 1002–1037
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_pubmed` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `query` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `query` recebido pela função; não há descrição individual explícita no código. |
| `config` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração ou estrutura de configuração recebida pela rotina. |

#### Retorno

Anotação declarada: `tuple[list[dict[str, Any]], dict[str, Any]]`. Retornos observados estaticamente incluem: `([], {'provider': 'pubmed', 'url': _safe_url(search_url), 'retrieved': 0, 'error': ''})`; `(records, {'provider': 'pubmed', 'url': _safe_url(search_url), 'retrieved': len(records), 'error': ''})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `','.join`
- `(_request_json(search_url) or {}).get`
- `_env`
- `_record`
- `_request_json`
- `_safe_url`
- `enumerate`
- `isinstance`
- `item.get`
- `len`
- `next`
- `payload.get`
- `re.search`
- `records.append`
- `result.get`
- `str`
- `str(value.get('idtype') or '').lower`
- `summary.get`
- `urlencode`
- `value.get`
- `year_match.group`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 9
loops: 1
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_europe_pmc`

**Assinatura:**
```python
def _europe_pmc(query: str, config: dict[str, Any]) -> tuple[list[dict[str, Any]], dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 1040–1057
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_europe_pmc` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `query` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `query` recebido pela função; não há descrição individual explícita no código. |
| `config` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração ou estrutura de configuração recebida pela rotina. |

#### Retorno

Anotação declarada: `tuple[list[dict[str, Any]], dict[str, Any]]`. Retornos observados estaticamente incluem: `(records, {'provider': 'europe_pmc', 'url': _safe_url(url), 'retrieved': len(records), 'error': ''})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `((_request_json(url) or {}).get('resultList') or {}).get`
- `(_request_json(url) or {}).get`
- `_record`
- `_request_json`
- `_safe_url`
- `candidate.get`
- `enumerate`
- `fulltext.get`
- `isinstance`
- `item.get`
- `len`
- `records.append`
- `str`
- `urlencode`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `https://doi.org/`

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_xml_text`

**Assinatura:**
```python
def _xml_text(node: ET.Element | None, *names: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 1060–1067
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_xml_text` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `node` | positional-or-keyword | `ET.Element \| None` | `obrigatório/ausente` | Parâmetro `node` recebido pela função; não há descrição individual explícita no código. |
| `names` | var-positional | `str` | `obrigatório/ausente` | Parâmetro `names` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `''`; `found.text.strip()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `found.text.strip`
- `node.find`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_scielo`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_looks_like_scielo_automation_challenge`

**Assinatura:**
```python
def _looks_like_scielo_automation_challenge(value: Any) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 1070–1078
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_looks_like_scielo_automation_challenge` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `any((marker in text for marker in markers))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `any`
- `str`
- `str(value or '').casefold`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_scielo`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_scielo`

**Assinatura:**
```python
def _scielo(query: str, config: dict[str, Any]) -> tuple[list[dict[str, Any]], dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 1081–1126
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_scielo` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `query` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `query` recebido pela função; não há descrição individual explícita no código. |
| `config` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração ou estrutura de configuração recebida pela rotina. |

#### Retorno

Anotação declarada: `tuple[list[dict[str, Any]], dict[str, Any]]`. Retornos observados estaticamente incluem: `(records, {'provider': 'scielo', 'url': _safe_url(url), 'retrieved': len(records), 'error': ''})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `ET.fromstring`
- `ProviderUnavailableError`
- `RuntimeError`
- `_env`
- `_looks_like_scielo_automation_challenge`
- `_record`
- `_request_text`
- `_safe_url`
- `_xml_text`
- `doi_match.group`
- `enumerate`
- `len`
- `re.search`
- `records.append`
- `root.findall`
- `str`
- `urlencode`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `ProviderUnavailableError('scielo', 'SciELO bloqueou a automação com Bunny Shield; a fonte foi interrompida após a primeira tentativa.')`
- `RuntimeError('A busca SciELO não retornou um sumário XML compatível. Tente novamente ou configure uma rota institucional em SCIELO_SEARCH_URL.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `https://search.scielo.org/`

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 1
comprehensions: 0
exception_handlers: 2
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_core`

**Assinatura:**
```python
def _core(query: str, config: dict[str, Any]) -> tuple[list[dict[str, Any]], dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 1129–1211
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_core` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `query` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `query` recebido pela função; não há descrição individual explícita no código. |
| `config` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração ou estrutura de configuração recebida pela rotina. |

#### Retorno

Anotação declarada: `tuple[list[dict[str, Any]], dict[str, Any]]`. Retornos observados estaticamente incluem: `(records, {'provider': 'core', 'url': _safe_url(url), 'retrieved': len(records), 'error': '', 'min_interval_seconds': _core_min_interval(config), 'max_retries_on_transient_errors': int(retry.get('max_retries') or 0), 'recovery': recovery})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `RuntimeError`
- `_bounded_int`
- `_core_degraded_cooldown_seconds`
- `_core_degraded_limit`
- `_core_degraded_max_retries`
- `_core_min_interval`
- `_env`
- `_is_retryable_core_failure`
- `_provider_retry_debug`
- `_provider_retry_settings`
- `_record`
- `_safe_url`
- `build_url`
- `config.get`
- `enumerate`
- `fetch`
- `int`
- `isinstance`
- `item.get`
- `item.get('identifiers', {}).get`
- `len`
- `min`
- `payload.get`
- `records.append`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('CORE_API_KEY não configurada; a fonte foi ignorada.')`
- `RuntimeError(f'CORE não respondeu após recuperação com limite reduzido ({degraded_limit} resultados): {recovery_exc}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 1
comprehensions: 0
exception_handlers: 2
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_core.build_url`

**Assinatura:**
```python
def build_url(limit: int) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 1137–1143
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Constrói a responsabilidade representada por `url` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `limit` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `limit` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `base + ('&' if '?' in base else '?') + urlencode(params)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `urlencode`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_core`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_core.fetch`

**Assinatura:**
```python
def fetch(url: str, *, retries: int) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 1145–1156
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `fetch` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `url` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `url` recebido pela função; não há descrição individual explícita no código. |
| `retries` | keyword-only | `int` | `obrigatório/ausente` | Parâmetro `retries` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `_request_json(url, headers={'Authorization': f'Bearer {key}'}, timeout=_core_timeout(config), before_attempt=lambda: _wait_for_core(config), retryable_http_statuses=RETRYABLE_HTTP_STATUS_CODES, http_retries=retries, retry_base_seconds=float(retry.get('retry_base_seconds') or CORE_DEFAULT_RETRY_BASE_SECONDS), retry_max_seconds=float(retry.get('retry_max_seconds') or CORE_DEFAULT_RETRY_MAX_SECONDS), retry_provider='core') or {}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_core_timeout`
- `_request_json`
- `_wait_for_core`
- `float`
- `retry.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_core`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_merge`

**Assinatura:**
```python
def _merge(first: dict[str, Any], second: dict[str, Any]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 1219–1225
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Mescla a responsabilidade representada por `merge` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `first` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `first` recebido pela função; não há descrição individual explícita no código. |
| `second` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `second` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_int`
- `_list`
- `first.get`
- `max`
- `second.get`
- `set`
- `sorted`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_deduplicate`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_deduplicate`

**Assinatura:**
```python
def _deduplicate(records: Iterable[dict[str, Any]]) -> tuple[list[dict[str, Any]], int]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 1228–1246
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Deduplica a responsabilidade representada por `deduplicate` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `records` | positional-or-keyword | `Iterable[dict[str, Any]]` | `obrigatório/ausente` | Parâmetro `records` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `tuple[list[dict[str, Any]], int]`. Retornos observados estaticamente incluem: `(out, removed)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_doi`
- `_merge`
- `_title_key`
- `by_doi.get`
- `by_title.get`
- `out.append`
- `record.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:run_external_prisma_search`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_score`

**Assinatura:**
```python
def _score(record: dict[str, Any], keywords: list[str]) -> float
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 1249–1258
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_score` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `record` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `record` recebido pela função; não há descrição individual explícita no código. |
| `keywords` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `keywords` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `float`. Retornos observados estaticamente incluem: `0.0`; `score`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `_norm`
- `record.get`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:run_external_prisma_search`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_truncate_text`

**Assinatura:**
```python
def _truncate_text(value: Any, limit: int) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 1270–1274
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_truncate_text` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |
| `limit` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `limit` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `text`; `text[:max(0, limit - 1)].rstrip() + '…'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `len`
- `max`
- `re.sub`
- `re.sub('\\s+', ' ', str(value or '')).strip`
- `str`
- `text[:max(0, limit - 1)].rstrip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_pretriage_prompt`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_safe_ai_text`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_chunk_rows`

**Assinatura:**
```python
def _chunk_rows(rows: list[dict[str, Any]], size: int) -> list[list[dict[str, Any]]]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 1277–1278
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_chunk_rows` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `rows` | positional-or-keyword | `list[dict[str, Any]]` | `obrigatório/ausente` | Parâmetro `rows` recebido pela função; não há descrição individual explícita no código. |
| `size` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `size` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[list[dict[str, Any]]]`. Retornos observados estaticamente incluem: `[rows[index:index + size] for index in range(0, len(rows), size)]`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `len`
- `range`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_pretriage_with_ai`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_extract_ai_content`

**Assinatura:**
```python
def _extract_ai_content(response: Any) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 1281–1291
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Extrai a responsabilidade representada por `ai_content` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `response` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `response` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `str(content or '').strip()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `''.join`
- `RuntimeError`
- `getattr`
- `isinstance`
- `item.get`
- `str`
- `str(content or '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_pretriage_with_ai`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('A API de IA não devolveu conteúdo utilizável para a pré-triagem.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 1
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_parse_ai_json`

**Assinatura:**
```python
def _parse_ai_json(content: Any) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 1294–1303
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Interpreta a responsabilidade representada por `ai_json` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `content` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `content` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `payload`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `RuntimeError`
- `isinstance`
- `json.loads`
- `re.sub`
- `re.sub('^```(?:json)?\\s*|\\s*```$', '', text, flags=re.IGNORECASE | re.DOTALL).strip`
- `str`
- `str(content or '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_pretriage_with_ai`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('A IA devolveu pré-triagem fora do JSON solicitado.')`
- `RuntimeError('A IA não devolveu o objeto JSON esperado na pré-triagem.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_float_between`

**Assinatura:**
```python
def _float_between(value: Any, *, default: float = 0.0, low: float = 0.0, high: float = 100.0) -> float
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 1306–1311
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_float_between` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |
| `default` | keyword-only | `float` | `0.0` | Parâmetro `default` recebido pela função; não há descrição individual explícita no código. |
| `low` | keyword-only | `float` | `0.0` | Parâmetro `low` recebido pela função; não há descrição individual explícita no código. |
| `high` | keyword-only | `float` | `100.0` | Parâmetro `high` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `float`. Retornos observados estaticamente incluem: `max(low, min(number, high))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `float`
- `max`
- `min`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_apply_ai_pretriage`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_pretriage_sort_key`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_select_triage_records`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_normalize_ai_recommendation`

**Assinatura:**
```python
def _normalize_ai_recommendation(value: Any, score: float, *, metadata_poor: bool) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 1314–1340
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Normaliza a responsabilidade representada por `ai_recommendation` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |
| `score` | positional-or-keyword | `float` | `obrigatório/ausente` | Parâmetro `score` recebido pela função; não há descrição individual explícita no código. |
| `metadata_poor` | keyword-only | `bool` | `obrigatório/ausente` | Parâmetro `metadata_poor` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `normalized`; `'INCERTO_METADADOS'`; `'PRIORIDADE_ALTA'`; `'REVISAR_HUMANO'`; `'PROVAVEL_EXCLUSAO'`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `_norm`
- `_norm(value).replace`
- `aliases.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_apply_ai_pretriage`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_pretriage_prompt`

**Assinatura:**
```python
def _pretriage_prompt(config: dict[str, Any], rows: list[dict[str, Any]]) -> tuple[str, str]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 1343–1379
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_pretriage_prompt` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `config` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração ou estrutura de configuração recebida pela rotina. |
| `rows` | positional-or-keyword | `list[dict[str, Any]]` | `obrigatório/ausente` | Parâmetro `rows` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `tuple[str, str]`. Retornos observados estaticamente incluem: `(system, user)`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `'; '.join`
- `'\n'.join`
- `_list`
- `_truncate_text`
- `compact_rows.append`
- `config.get`
- `int`
- `item.get`
- `json.dumps`
- `row.get`
- `sorted`
- `str`
- `str(item.get('bloco_rotulo') or '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_pretriage_with_ai`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 3
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_safe_ai_text`

**Assinatura:**
```python
def _safe_ai_text(value: Any, limit: int = 900) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 1382–1383
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_safe_ai_text` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |
| `limit` | positional-or-keyword | `int` | `900` | Parâmetro `limit` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `_truncate_text(value, limit).replace('\x00', '')`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `_truncate_text`
- `_truncate_text(value, limit).replace`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_apply_ai_pretriage`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_mark_pretriage_failure`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_mark_pretriage_failure`

**Assinatura:**
```python
def _mark_pretriage_failure(record: dict[str, Any], status: str, detail: str = '') -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 1386–1396
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_mark_pretriage_failure` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `record` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `record` recebido pela função; não há descrição individual explícita no código. |
| `status` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `status` recebido pela função; não há descrição individual explícita no código. |
| `detail` | positional-or-keyword | `str` | `''` | Parâmetro `detail` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_safe_ai_text`
- `record.update`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_pretriage_with_ai`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_apply_ai_pretriage`

**Assinatura:**
```python
def _apply_ai_pretriage(record: dict[str, Any], value: dict[str, Any]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 1399–1417
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Aplica a responsabilidade representada por `ai_pretriage` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `record` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `record` recebido pela função; não há descrição individual explícita no código. |
| `value` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_float_between`
- `_normalize_ai_recommendation`
- `_safe_ai_text`
- `record.get`
- `record.update`
- `round`
- `str`
- `str(record.get('resumo') or '').strip`
- `str(record.get('titulo') or '').strip`
- `value.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_pretriage_with_ai`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_pretriage_with_ai`

**Assinatura:**
```python
def _pretriage_with_ai(records: list[dict[str, Any]], config: dict[str, Any], *, client: Any | None, model: str | None, progress: Progress = None) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 1420–1517
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Prioriza registros antes do corte da planilha, sem decidir elegibilidade.

**Docstring resumida:** Prioriza registros antes do corte da planilha, sem decidir elegibilidade.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `records` | positional-or-keyword | `list[dict[str, Any]]` | `obrigatório/ausente` | Parâmetro `records` recebido pela função; não há descrição individual explícita no código. |
| `config` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração ou estrutura de configuração recebida pela rotina. |
| `client` | keyword-only | `Any \| None` | `obrigatório/ausente` | Cliente de serviço externo/IA utilizado pela rotina; pode ser opcional conforme o fluxo. |
| `model` | keyword-only | `str \| None` | `obrigatório/ausente` | Identificador do modelo utilizado pela etapa de IA. |
| `progress` | keyword-only | `Progress` | `None` | Parâmetro `progress` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `audit`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `RuntimeError`
- `_apply_ai_pretriage`
- `_chunk_rows`
- `_extract_ai_content`
- `_mark_pretriage_failure`
- `_parse_ai_json`
- `_pretriage_prompt`
- `audit['errors'].append`
- `bool`
- `by_id.get`
- `client.chat.completions.create`
- `config.get`
- `enumerate`
- `int`
- `isinstance`
- `item.get`
- `len`
- `min`
- `payload.get`
- `progress`
- `recommendation_counts.get`
- `record.get`
- `record.update`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:run_external_prisma_search`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('A pré-triagem por IA está ativa, mas o cliente OpenAI não foi inicializado. Configure OPENAI_API_KEY ou desative pre_triagem_ia.')`
- `RuntimeError('A pré-triagem por IA está ativa, mas nenhum modelo foi definido em [openai].model, OPENAI_MODEL ou pre_triagem_ia_modelo.')`
- `RuntimeError('A IA não devolveu a lista de registros esperada.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 6
comprehensions: 2
exception_handlers: 2
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_pretriage_sort_key`

**Assinatura:**
```python
def _pretriage_sort_key(record: dict[str, Any]) -> tuple[int, float, float, float, str]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 1520–1534
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_pretriage_sort_key` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `record` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `record` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `tuple[int, float, float, float, str]`. Retornos observados estaticamente incluem: `(priority, -_float_between(record.get('escore_aderencia_ia'), default=0.0), -_float_between(record.get('confianca_ia'), default=0.0), -_float_between(record.get('pontuacao_relevancia'), default=0.0), str(record.get('titulo') or '').casefold())`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_float_between`
- `record.get`
- `str`
- `str(record.get('recomendacao_ia') or '').strip`
- `str(record.get('recomendacao_ia') or '').strip().upper`
- `str(record.get('titulo') or '').casefold`
- `{'PRIORIDADE_ALTA': 0, 'REVISAR_HUMANO': 1, 'INCERTO_METADADOS': 2, 'PROVAVEL_EXCLUSAO': 3}.get`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_select_triage_records`

**Assinatura:**
```python
def _select_triage_records(records: list[dict[str, Any]], config: dict[str, Any]) -> list[dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 1537–1562
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Seleciona a planilha priorizada e preserva uma reserva de incerteza.

**Docstring resumida:** Seleciona a planilha priorizada e preserva uma reserva de incerteza.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `records` | positional-or-keyword | `list[dict[str, Any]]` | `obrigatório/ausente` | Parâmetro `records` recebido pela função; não há descrição individual explícita no código. |
| `config` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração ou estrutura de configuração recebida pela rotina. |

#### Retorno

Anotação declarada: `list[dict[str, Any]]`. Retornos observados estaticamente incluem: `ordered[:limit]`; `sorted(selected, key=_pretriage_sort_key)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_float_between`
- `config.get`
- `float`
- `int`
- `item.get`
- `len`
- `min`
- `selected.append`
- `selected_ids.add`
- `sorted`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:run_external_prisma_search`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_enrich_with_unpaywall`

**Assinatura:**
```python
def _enrich_with_unpaywall(records: list[dict[str, Any]], config: dict[str, Any], *, progress: Progress = None) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 1565–1596
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_enrich_with_unpaywall` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `records` | positional-or-keyword | `list[dict[str, Any]]` | `obrigatório/ausente` | Parâmetro `records` recebido pela função; não há descrição individual explícita no código. |
| `config` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração ou estrutura de configuração recebida pela rotina. |
| `progress` | keyword-only | `Progress` | `None` | Parâmetro `progress` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `{'enabled': False, 'checked': 0, 'enriched': 0, 'reason': 'desativado'}`; `{'enabled': True, 'checked': 0, 'enriched': 0, 'reason': 'UNPAYWALL_EMAIL ausente'}`; `{'enabled': True, 'checked': checked, 'enriched': enriched, 'reason': ''}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_doi`
- `_env`
- `_request_json`
- `config.get`
- `int`
- `isinstance`
- `item.get`
- `len`
- `location.get`
- `min`
- `payload.get`
- `progress`
- `str`
- `str(location.get('url') or '').strip`
- `str(location.get('url_for_pdf') or '').strip`
- `time.sleep`
- `urlencode`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:run_external_prisma_search`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `https://api.unpaywall.org/v2/`

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 2
comprehensions: 1
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_write_json`

**Assinatura:**
```python
def _write_json(path: Path, value: Any) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 1599–1601
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Grava a responsabilidade representada por `json` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `json.dumps`
- `path.parent.mkdir`
- `path.write_text`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:import_manual_prisma_triage`
- `app_bundle.scripts.pipeline.prisma_busca_externa:run_external_prisma_search`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_write_csv`

**Assinatura:**
```python
def _write_csv(path: Path, rows: Iterable[dict[str, Any]]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 1604–1613
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Grava a responsabilidade representada por `csv` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `rows` | positional-or-keyword | `Iterable[dict[str, Any]]` | `obrigatório/ausente` | Parâmetro `rows` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Abre arquivo para leitura ou inspeção.
- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `'; '.join`
- `_list`
- `csv.DictWriter`
- `dict`
- `path.open`
- `path.parent.mkdir`
- `prepared.get`
- `writer.writeheader`
- `writer.writerow`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:import_manual_prisma_triage`
- `app_bundle.scripts.pipeline.prisma_busca_externa:run_external_prisma_search`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 2
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_write_xlsx`

**Assinatura:**
```python
def _write_xlsx(path: Path, rows: list[dict[str, Any]]) -> str | None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 1616–1659
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Grava a responsabilidade representada por `xlsx` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `rows` | positional-or-keyword | `list[dict[str, Any]]` | `obrigatório/ausente` | Parâmetro `rows` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str | None`. Retornos observados estaticamente incluem: `None`; `str(path)`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `'; '.join`
- `Alignment`
- `Font`
- `PatternFill`
- `Workbook`
- `_list`
- `guide.append`
- `guide.iter_rows`
- `len`
- `max`
- `min`
- `row.get`
- `sheet.append`
- `sheet.iter_rows`
- `str`
- `workbook.create_sheet`
- `workbook.save`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:import_manual_prisma_triage`
- `app_bundle.scripts.pipeline.prisma_busca_externa:run_external_prisma_search`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 9
comprehensions: 2
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_protocol_markdown`

**Assinatura:**
```python
def _protocol_markdown(config: dict[str, Any], logs: list[dict[str, Any]], counts: dict[str, int], unpaywall: dict[str, Any], pretriage: dict[str, Any]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 1662–1721
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_protocol_markdown` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `config` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração ou estrutura de configuração recebida pela rotina. |
| `logs` | positional-or-keyword | `list[dict[str, Any]]` | `obrigatório/ausente` | Parâmetro `logs` recebido pela função; não há descrição individual explícita no código. |
| `counts` | positional-or-keyword | `dict[str, int]` | `obrigatório/ausente` | Parâmetro `counts` recebido pela função; não há descrição individual explícita no código. |
| `unpaywall` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `unpaywall` recebido pela função; não há descrição individual explícita no código. |
| `pretriage` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `pretriage` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'\n'.join(lines).rstrip() + '\n'`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `', '.join`
- `'\n'.join`
- `'\n'.join(lines).rstrip`
- `PROVIDER_LABELS.get`
- `_now`
- `counts.items`
- `isinstance`
- `len`
- `lines.append`
- `name.replace`
- `pretriage.get`
- `query_log.get`
- `source_log.get`
- `str`
- `str(source_log.get('status') or '').strip`
- `str(source_log.get('status_detail') or '').strip`
- `unpaywall.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:run_external_prisma_search`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 10
loops: 3
comprehensions: 3
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_tag_query_provenance`

**Assinatura:**
```python
def _tag_query_provenance(records: list[dict[str, Any]], source: str, plan: dict[str, str]) -> list[dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 1724–1728
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_tag_query_provenance` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `records` | positional-or-keyword | `list[dict[str, Any]]` | `obrigatório/ausente` | Parâmetro `records` recebido pela função; não há descrição individual explícita no código. |
| `source` | positional-or-keyword | `str` | `obrigatório/ausente` | Origem dos dados ou arquivo de entrada. |
| `plan` | positional-or-keyword | `dict[str, str]` | `obrigatório/ausente` | Parâmetro `plan` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[dict[str, Any]]`. Retornos observados estaticamente incluem: `records`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_list`
- `record.get`
- `set`
- `sorted`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:run_external_prisma_search`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_missing_required_credentials`

**Assinatura:**
```python
def _missing_required_credentials(provider: str) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 1731–1732
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_missing_required_credentials` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `provider` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `provider` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `[name for name in _REQUIRED_ENV.get(provider, ()) if not _has_env(name)]`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_REQUIRED_ENV.get`
- `_has_env`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:run_external_prisma_search`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_source_log`

**Assinatura:**
```python
def _source_log(provider: str, query_logs: list[dict[str, Any]], *, status: str = '', status_detail: str = '') -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 1735–1757
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_source_log` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `provider` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `provider` recebido pela função; não há descrição individual explícita no código. |
| `query_logs` | positional-or-keyword | `list[dict[str, Any]]` | `obrigatório/ausente` | Parâmetro `query_logs` recebido pela função; não há descrição individual explícita no código. |
| `status` | keyword-only | `str` | `''` | Parâmetro `status` recebido pela função; não há descrição individual explícita no código. |
| `status_detail` | keyword-only | `str` | `''` | Parâmetro `status_detail` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `{'provider': provider, 'retrieved': sum((int(item.get('retrieved') or 0) for item in query_logs)), 'error': '; '.join(errors), 'status': status, 'status_detail': status_detail, 'consultas': query_logs}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'; '.join`
- `int`
- `item.get`
- `len`
- `max`
- `sum`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:run_external_prisma_search`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:run_external_prisma_search`

**Assinatura:**
```python
def run_external_prisma_search(cfg: dict[str, Any], out_dir: Path, prefix: str, *, progress: Progress = None, client: Any | None = None, model: str | None = None) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 1760–1919
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `external_prisma_search` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `out_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `out_dir` recebido pela função; não há descrição individual explícita no código. |
| `prefix` | positional-or-keyword | `str` | `obrigatório/ausente` | Prefixo canônico usado para compor nomes de artefatos. |
| `progress` | keyword-only | `Progress` | `None` | Parâmetro `progress` recebido pela função; não há descrição individual explícita no código. |
| `client` | keyword-only | `Any \| None` | `None` | Cliente de serviço externo/IA utilizado pela rotina; pode ser opcional conforme o fluxo. |
| `model` | keyword-only | `str \| None` | `None` | Identificador do modelo utilizado pela etapa de IA. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `output`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `', '.join`
- `'; '.join`
- `RuntimeError`
- `_deduplicate`
- `_enrich_with_unpaywall`
- `_missing_required_credentials`
- `_now`
- `_pretriage_with_ai`
- `_protocol_markdown`
- `_query_config`
- `_score`
- `_select_triage_records`
- `_source_log`
- `_tag_query_provenance`
- `_write_csv`
- `_write_json`
- `_write_xlsx`
- `config.get`
- `dict`
- `fetchers[source]`
- `int`
- `item.get`
- `item.update`
- `len`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:run_prisma_stage_004`
- `diagnosticar_fontes_prisma:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('Nenhuma fonte bibliográfica respondeu com sucesso. ' + errors)`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.candidatos_brutos.json`
- `.candidatos_deduplicados.json`
- `.triagem_titulo_resumo.csv`
- `.triagem_titulo_resumo.xlsx`
- `.protocolo_busca_prisma.md`
- `.busca_prisma_log.json`
- `.prisma_report.json`
- `.pre_triagem_ia.json`

#### Complexidade estrutural observada

```yaml
branches: 12
loops: 4
comprehensions: 1
exception_handlers: 2
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_prisma_org_text`

**Assinatura:**
```python
def _prisma_org_text(value: Any) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 1927–1931
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Normaliza texto de metadados para uso seguro em linhas ORG.

**Docstring resumida:** Normaliza texto de metadados para uso seguro em linhas ORG.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `text.replace('|', '/')`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `raw.replace`
- `raw.replace('\xa0', ' ').split`
- `str`
- `text.replace`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_prisma_doi`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_prisma_matrix_cell`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_prisma_org_table`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_prisma_reference`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_prisma_source_summary`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_prisma_strategy_org_lines`
- `app_bundle.scripts.pipeline.prisma_busca_externa:render_external_prisma_org_report`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_prisma_org_table`

**Assinatura:**
```python
def _prisma_org_table(headers: list[str], rows: list[list[Any]]) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 1934–1940
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Monta tabela ORG compacta, sem depender do renderizador do paper.

**Docstring resumida:** Monta tabela ORG compacta, sem depender do renderizador do paper.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `headers` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Cabeçalhos usados em requisição/integração externa. |
| `rows` | positional-or-keyword | `list[list[Any]]` | `obrigatório/ausente` | Parâmetro `rows` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `lines`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `' | '.join`
- `'-+-'.join`
- `_prisma_org_text`
- `len`
- `lines.append`
- `max`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_prisma_append_matrix_annex_org`
- `app_bundle.scripts.pipeline.prisma_busca_externa:render_external_prisma_org_report`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 3
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_prisma_doi`

**Assinatura:**
```python
def _prisma_doi(value: Any) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 1943–1948
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_doi` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `raw.strip()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_prisma_org_text`
- `_prisma_org_text(value).strip`
- `len`
- `raw.casefold`
- `raw.casefold().startswith`
- `raw.strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_prisma_reference`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `https://doi.org/`
- `http://doi.org/`

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_prisma_reference`

**Assinatura:**
```python
def _prisma_reference(item: dict[str, Any]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 1951–1962
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_reference` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `item` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `item` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `' '.join(parts)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `_prisma_doi`
- `_prisma_org_text`
- `item.get`
- `parts.append`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:render_external_prisma_org_report`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `DOI: https://doi.org/`

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_prisma_matrix_cell`

**Assinatura:**
```python
def _prisma_matrix_cell(item: dict[str, Any], key: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 1965–1967
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_matrix_cell` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `item` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `item` recebido pela função; não há descrição individual explícita no código. |
| `key` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `key` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `value if value else '—'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_prisma_org_text`
- `item.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_prisma_append_matrix_annex_org`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_prisma_matrix_study_label`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_prisma_matrix_study_label`

**Assinatura:**
```python
def _prisma_matrix_study_label(item: dict[str, Any]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 1970–1975
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_matrix_study_label` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `item` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `item` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `f"{_prisma_matrix_cell(item, 'autores')} ({_prisma_matrix_cell(item, 'ano')}) — {_prisma_matrix_cell(item, 'titulo')}"`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_prisma_matrix_cell`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:_prisma_append_matrix_annex_org`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_prisma_append_matrix_annex_org`

**Assinatura:**
```python
def _prisma_append_matrix_annex_org(lines: list[str], included: list[dict[str, Any]]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 1978–2043
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Acrescenta matriz documental em duas longtables no anexo do relatório.

**Docstring resumida:** Acrescenta matriz documental em duas longtables no anexo do relatório.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `lines` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `lines` recebido pela função; não há descrição individual explícita no código. |
| `included` | positional-or-keyword | `list[dict[str, Any]]` | `obrigatório/ausente` | Parâmetro `included` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_prisma_matrix_cell`
- `_prisma_matrix_study_label`
- `_prisma_org_table`
- `isinstance`
- `lines.extend`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:render_external_prisma_org_report`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_prisma_source_summary`

**Assinatura:**
```python
def _prisma_source_summary(source: dict[str, Any]) -> tuple[Any, str]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 2046–2081
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Resume logs de fonte dos formatos antigo e novo.

**Docstring resumida:** Resume logs de fonte dos formatos antigo e novo.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `source` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Origem dos dados ou arquivo de entrada. |

#### Retorno

Anotação declarada: `tuple[Any, str]`. Retornos observados estaticamente incluem: `(retrieved, status_labels[explicit_status])`; `(retrieved, explicit_status)`; `(retrieved, 'erro')`; `(retrieved, 'parcial')`; `(retrieved, 'ok')`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_prisma_org_text`
- `errors.append`
- `int`
- `isinstance`
- `item.get`
- `len`
- `max`
- `source.get`
- `str`
- `str(source.get('status') or '').strip`
- `sum`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:render_external_prisma_org_report`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 8
loops: 1
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_prisma_strategy_org_lines`

**Assinatura:**
```python
def _prisma_strategy_org_lines(payload: dict[str, Any]) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 2084–2104
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_prisma_strategy_org_lines` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `payload` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Payload estruturado recebido ou produzido pela operação. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `[f'- Consulta geral: ={query}=']`; `lines`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_prisma_org_text`
- `isinstance`
- `lines.append`
- `payload.get`
- `row.get`
- `strategy.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:render_external_prisma_org_report`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:render_external_prisma_org_report`

**Assinatura:**
```python
def render_external_prisma_org_report(cfg: dict[str, Any], out_dir: Path, prefix: str, payload: dict[str, Any], *, phase: str) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 2107–2257
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Gera ORG FGV para a busca inicial ou a consolidação após triagem.

**Docstring resumida:** Gera ORG FGV para a busca inicial ou a consolidação após triagem.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `out_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `out_dir` recebido pela função; não há descrição individual explícita no código. |
| `prefix` | positional-or-keyword | `str` | `obrigatório/ausente` | Prefixo canônico usado para compor nomes de artefatos. |
| `payload` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Payload estruturado recebido ou produzido pela operação. |
| `phase` | keyword-only | `str` | `obrigatório/ausente` | Parâmetro `phase` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `target`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `'; '.join`
- `'\n'.join`
- `'\n'.join(lines).rstrip`
- `ValueError`
- `_prisma_append_matrix_annex_org`
- `_prisma_org_table`
- `_prisma_org_text`
- `_prisma_reference`
- `_prisma_source_summary`
- `_prisma_strategy_org_lines`
- `activity.get`
- `any`
- `artifacts.get`
- `counts.items`
- `document.get`
- `enumerate`
- `globals`
- `globals().get`
- `isinstance`
- `labels.get`
- `lines.append`
- `lines.extend`
- `payload.get`
- `pretriage_payload.get`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_ap003e_body_render_external_prisma_outputs_1`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `ValueError("phase deve ser 'preliminar' ou 'final'.")`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.org`

#### Complexidade estrutural observada

```yaml
branches: 21
loops: 2
comprehensions: 9
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:render_external_prisma_org_report.section`

**Assinatura:**
```python
def section(name: str) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 2120–2122
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `section` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `name` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `name` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `value if isinstance(value, dict) else {}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `cfg.get`
- `isinstance`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_decision`

**Assinatura:**
```python
def _decision(value: Any) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 2260–2263
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_decision` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `aliases.get(text, str(value or '').strip().upper() or 'PENDENTE')`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `_norm`
- `_norm(value).replace`
- `aliases.get`
- `str`
- `str(value or '').strip`
- `str(value or '').strip().upper`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:import_manual_prisma_triage`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_read_triage`

**Assinatura:**
```python
def _read_triage(path: Path) -> list[dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 2266–2275
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Lê a responsabilidade representada por `triage` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `list[dict[str, Any]]`. Retornos observados estaticamente incluem: `[dict(item) for item in reader]`.

#### Efeitos colaterais e fronteiras externas

- Abre arquivo para leitura ou inspeção.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `', '.join`
- `RuntimeError`
- `csv.DictReader`
- `dict`
- `path.open`
- `set`
- `sorted`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:import_manual_prisma_triage`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('A planilha de triagem não contém cabeçalho.')`
- `RuntimeError('A planilha não contém as colunas obrigatórias: ' + ', '.join(missing))`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:_final_markdown`

**Assinatura:**
```python
def _final_markdown(config: dict[str, Any], source: Path, counts: dict[str, int], included: list[dict[str, Any]]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 2278–2286
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_final_markdown` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `config` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração ou estrutura de configuração recebida pela rotina. |
| `source` | positional-or-keyword | `Path` | `obrigatório/ausente` | Origem dos dados ou arquivo de entrada. |
| `counts` | positional-or-keyword | `dict[str, int]` | `obrigatório/ausente` | Parâmetro `counts` recebido pela função; não há descrição individual explícita no código. |
| `included` | positional-or-keyword | `list[dict[str, Any]]` | `obrigatório/ausente` | Parâmetro `included` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'\n'.join(lines).rstrip() + '\n'`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `'\n'.join(lines).rstrip`
- `_now`
- `counts.items`
- `enumerate`
- `item.get`
- `key.replace`
- `lines.append`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_busca_externa:import_manual_prisma_triage`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_busca_externa:import_manual_prisma_triage`

**Assinatura:**
```python
def import_manual_prisma_triage(cfg: dict[str, Any], out_dir: Path, prefix: str, triage_path: Path) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_busca_externa.py`
**Linhas:** 2289–2336
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Importa a responsabilidade representada por `manual_prisma_triage` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `out_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `out_dir` recebido pela função; não há descrição individual explícita no código. |
| `prefix` | positional-or-keyword | `str` | `obrigatório/ausente` | Prefixo canônico usado para compor nomes de artefatos. |
| `triage_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `output`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `', '.join`
- `FileNotFoundError`
- `_decision`
- `_final_markdown`
- `_now`
- `_query_config`
- `_read_triage`
- `_write_csv`
- `_write_json`
- `_write_xlsx`
- `any`
- `counts.update`
- `dict`
- `initial.get`
- `initial_path.is_file`
- `initial_path.read_text`
- `isinstance`
- `item.get`
- `json.loads`
- `len`
- `notices.append`
- `out_dir.mkdir`
- `report_md.write_text`
- `source.is_file`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `FileNotFoundError(f'Planilha de triagem não encontrada: {source}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.matriz_estudos_incluidos.csv`
- `.matriz_estudos_incluidos.xlsx`
- `.relatorio_prisma_final.md`
- `.prisma_report_final.json`
- `.prisma_report.json`

#### Complexidade estrutural observada

```yaml
branches: 9
loops: 0
comprehensions: 7
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `6cdd0f91681e862fc42bfa38f842a17b9d8e7606e437d5e919e4d2b20592a2ec`
**LOC do arquivo-fonte:** 2336


### `app_bundle.scripts.pipeline.prisma_congelar_artigo:sha256_file`

**Assinatura:**
```python
def sha256_file(path)
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_congelar_artigo.py`
**Linhas:** 15–19
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `sha256_file` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `h.hexdigest()`.

#### Efeitos colaterais e fronteiras externas

- Abre arquivo para leitura ou inspeção.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `f.read`
- `h.hexdigest`
- `h.update`
- `hashlib.sha256`
- `iter`
- `path.open`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b17f589816d0fa9aabc471546bc1ae79a30ecb505fd5ee57d771136263d49526`
**LOC do arquivo-fonte:** 204


### `app_bundle.scripts.pipeline.prisma_congelar_artigo:file_info`

**Assinatura:**
```python
def file_info(path, origem = None)
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_congelar_artigo.py`
**Linhas:** 21–23
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `file_info` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `origem` | positional-or-keyword | `não anotado` | `None` | Parâmetro `origem` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `{'arquivo': path.name, 'caminho': str(path), 'origem': origem, 'tamanho_bytes': st.st_size, 'mtime_iso': dt.datetime.fromtimestamp(st.st_mtime).isoformat(timespec='seconds'), 'sha256': sha256_file(path)}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `dt.datetime.fromtimestamp`
- `dt.datetime.fromtimestamp(st.st_mtime).isoformat`
- `path.stat`
- `sha256_file`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_congelar_artigo:copy_one`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b17f589816d0fa9aabc471546bc1ae79a30ecb505fd5ee57d771136263d49526`
**LOC do arquivo-fonte:** 204


### `app_bundle.scripts.pipeline.prisma_congelar_artigo:copy_one`

**Assinatura:**
```python
def copy_one(src, dest_dir, required, copied, missing)
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_congelar_artigo.py`
**Linhas:** 25–29
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Copia a responsabilidade representada por `one` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `src` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `src` recebido pela função; não há descrição individual explícita no código. |
| `dest_dir` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `dest_dir` recebido pela função; não há descrição individual explícita no código. |
| `required` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `required` recebido pela função; não há descrição individual explícita no código. |
| `copied` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `copied` recebido pela função; não há descrição individual explícita no código. |
| `missing` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `missing` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `FileNotFoundError`
- `copied.append`
- `file_info`
- `missing.append`
- `shutil.copy2`
- `src.exists`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_congelar_artigo:freeze`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `FileNotFoundError(f'Arquivo obrigatório não encontrado: {src}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b17f589816d0fa9aabc471546bc1ae79a30ecb505fd5ee57d771136263d49526`
**LOC do arquivo-fonte:** 204


### `app_bundle.scripts.pipeline.prisma_congelar_artigo:esc`

**Assinatura:**
```python
def esc(v)
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_congelar_artigo.py`
**Linhas:** 31–31
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `esc` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `v` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `v` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `str(v or '').replace('\\', '\\\\').replace('"', '\\"')`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `str`
- `str(v or '').replace`
- `str(v or '').replace('\\', '\\\\').replace`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_congelar_artigo:arr`
- `app_bundle.scripts.pipeline.prisma_congelar_artigo:generate_toml`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b17f589816d0fa9aabc471546bc1ae79a30ecb505fd5ee57d771136263d49526`
**LOC do arquivo-fonte:** 204


### `app_bundle.scripts.pipeline.prisma_congelar_artigo:arr`

**Assinatura:**
```python
def arr(items)
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_congelar_artigo.py`
**Linhas:** 32–32
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `arr` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `items` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `items` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `'[' + ', '.join((f'"{esc(x)}"' for x in items)) + ']'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `', '.join`
- `esc`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_congelar_artigo:generate_toml`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b17f589816d0fa9aabc471546bc1ae79a30ecb505fd5ee57d771136263d49526`
**LOC do arquivo-fonte:** 204


### `app_bundle.scripts.pipeline.prisma_congelar_artigo:slug`

**Assinatura:**
```python
def slug(v, max_len = 70)
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_congelar_artigo.py`
**Linhas:** 33–38
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `slug` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `v` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `v` recebido pela função; não há descrição individual explícita no código. |
| `max_len` | positional-or-keyword | `não anotado` | `70` | Parâmetro `max_len` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `re.sub('_+', '_', t)[:max_len].strip('_') or 'artigo_final_abnt'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `''.join`
- `re.sub`
- `re.sub('[^a-zA-Z0-9]+', '_', t.lower()).strip`
- `re.sub('_+', '_', t)[:max_len].strip`
- `t.lower`
- `unicodedata.combining`
- `unicodedata.normalize`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b17f589816d0fa9aabc471546bc1ae79a30ecb505fd5ee57d771136263d49526`
**LOC do arquivo-fonte:** 204


### `app_bundle.scripts.pipeline.prisma_congelar_artigo:read_toml`

**Assinatura:**
```python
def read_toml(path)
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_congelar_artigo.py`
**Linhas:** 40–42
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Lê a responsabilidade representada por `toml` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `{}`; `tomllib.load(f)`.

#### Efeitos colaterais e fronteiras externas

- Abre arquivo para leitura ou inspeção.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `path.exists`
- `path.open`
- `tomllib.load`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b17f589816d0fa9aabc471546bc1ae79a30ecb505fd5ee57d771136263d49526`
**LOC do arquivo-fonte:** 204


### `app_bundle.scripts.pipeline.prisma_congelar_artigo:resolve_relative`

**Assinatura:**
```python
def resolve_relative(base, path)
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_congelar_artigo.py`
**Linhas:** 44–46
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Resolve a responsabilidade representada por `relative` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `base` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `base` recebido pela função; não há descrição individual explícita no código. |
| `path` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `p.resolve() if p.is_absolute() else (base.parent / p).resolve()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `(base.parent / p).resolve`
- `Path`
- `Path(str(path)).expanduser`
- `p.is_absolute`
- `p.resolve`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b17f589816d0fa9aabc471546bc1ae79a30ecb505fd5ee57d771136263d49526`
**LOC do arquivo-fonte:** 204


### `app_bundle.scripts.pipeline.prisma_congelar_artigo:dados_path_from_config`

**Assinatura:**
```python
def dados_path_from_config(config)
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_congelar_artigo.py`
**Linhas:** 48–53
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `dados_path_from_config` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `config` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Configuração ou estrutura de configuração recebida pela rotina. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `None`; `resolve_relative(config, str(item))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `config.exists`
- `d.get`
- `d.get('busca_prisma', {}).get`
- `d.get('pesquisa', {}).get`
- `read_toml`
- `resolve_relative`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_congelar_artigo:generate_toml`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b17f589816d0fa9aabc471546bc1ae79a30ecb505fd5ee57d771136263d49526`
**LOC do arquivo-fonte:** 204


### `app_bundle.scripts.pipeline.prisma_congelar_artigo:deep_find`

**Assinatura:**
```python
def deep_find(data, names)
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_congelar_artigo.py`
**Linhas:** 55–66
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `deep_find` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `data` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Estrutura de dados de entrada. |
| `names` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `names` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `found`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `n.lower`
- `walk`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_congelar_artigo:values`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b17f589816d0fa9aabc471546bc1ae79a30ecb505fd5ee57d771136263d49526`
**LOC do arquivo-fonte:** 204


### `app_bundle.scripts.pipeline.prisma_congelar_artigo:deep_find.walk`

**Assinatura:**
```python
def walk(obj)
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_congelar_artigo.py`
**Linhas:** 57–65
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `walk` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `obj` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `obj` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `isinstance`
- `obj.items`
- `obj.values`
- `str`
- `str(k).lower`
- `walk`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 3
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b17f589816d0fa9aabc471546bc1ae79a30ecb505fd5ee57d771136263d49526`
**LOC do arquivo-fonte:** 204


### `app_bundle.scripts.pipeline.prisma_congelar_artigo:as_text`

**Assinatura:**
```python
def as_text(v)
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_congelar_artigo.py`
**Linhas:** 68–72
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `as_text` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `v` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `v` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `''`; `'; '.join((str(x).strip() for x in v if str(x).strip()))`; `json.dumps(v, ensure_ascii=False, indent=2)`; `str(v).strip()`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `'; '.join`
- `isinstance`
- `json.dumps`
- `str`
- `str(v).strip`
- `str(x).strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_congelar_artigo:values`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b17f589816d0fa9aabc471546bc1ae79a30ecb505fd5ee57d771136263d49526`
**LOC do arquivo-fonte:** 204


### `app_bundle.scripts.pipeline.prisma_congelar_artigo:as_list`

**Assinatura:**
```python
def as_list(v)
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_congelar_artigo.py`
**Linhas:** 74–77
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `as_list` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `v` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `v` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `[]`; `[str(x).strip() for x in v if str(x).strip()]`; `[x.strip() for x in re.split('[;\\n,]+', str(v)) if x.strip()]`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `isinstance`
- `re.split`
- `str`
- `str(x).strip`
- `x.strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_congelar_artigo:values`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b17f589816d0fa9aabc471546bc1ae79a30ecb505fd5ee57d771136263d49526`
**LOC do arquivo-fonte:** 204


### `app_bundle.scripts.pipeline.prisma_congelar_artigo:values`

**Assinatura:**
```python
def values(meta, config, fallback)
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_congelar_artigo.py`
**Linhas:** 79–96
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `values` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `meta` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `meta` recebido pela função; não há descrição individual explícita no código. |
| `config` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Configuração ou estrutura de configuração recebida pela rotina. |
| `fallback` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `fallback` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `locals()`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `as_list`
- `as_text`
- `deep_find`
- `fallback.replace`
- `fallback.replace('_', ' ').title`
- `locals`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.academic_pipeline_gui:AcademicPipelineGUI._set_busy`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_iter_mapping_layers.walk`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_read_structured_paper_file`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:_read_structured_research_file`
- `app_bundle.scripts.pipeline.bibliography_manager:deduplicate_entries`
- `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:flatten_text`
- `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:main`
- `app_bundle.scripts.pipeline.prisma_congelar_artigo:deep_find.walk`
- `app_bundle.scripts.pipeline.prisma_congelar_artigo:generate_toml`
- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:compute_scores`
- `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:recursive_scan_json`
- `app_bundle.scripts.pipeline.prisma_exportar_bib:year_from`
- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:flatten_text`
- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:load_candidates`
- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:status_boost`
- `app_bundle.scripts.pipeline.render_docx_canonico:add_references`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b17f589816d0fa9aabc471546bc1ae79a30ecb505fd5ee57d771136263d49526`
**LOC do arquivo-fonte:** 204


### `app_bundle.scripts.pipeline.prisma_congelar_artigo:render_template`

**Assinatura:**
```python
def render_template(template_path, repl)
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_congelar_artigo.py`
**Linhas:** 98–101
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Renderiza a responsabilidade representada por `template` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `template_path` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `repl` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `repl` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `s`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `repl.items`
- `s.replace`
- `str`
- `template_path.read_text`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_congelar_artigo:generate_toml`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b17f589816d0fa9aabc471546bc1ae79a30ecb505fd5ee57d771136263d49526`
**LOC do arquivo-fonte:** 204


### `app_bundle.scripts.pipeline.prisma_congelar_artigo:generate_toml`

**Assinatura:**
```python
def generate_toml(args, dados_dir)
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_congelar_artigo.py`
**Linhas:** 103–162
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Gera a responsabilidade representada por `toml` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `dados_dir` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `dados_dir` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `toml_out`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `'\n'.join(['Produza um paper acadêmico em português, no layout paper_fgv, com citações autor-data e referências finais em ABNT.', '', f"Arquivo estruturado de pesquisa: {meta_path or ''}", f"Tema: {vals['tema']}", f"Recorte: {vals['recorte']}", f"Objetivo: {vals['objetivo']}", f"Pergunta de pesquisa: {vals['pergunta']}", f"Tese central: {vals['tese']}", '', 'Orientações específicas extraídas do arquivo estruturado:', vals['orientacoes'], '', 'Use exclusivamente os estudos selecionados no PRISMA final e congelados em dados_prisma.', 'Não invente referências, autores, DOI, periódicos, dados empíricos ou conclusões.', 'O método deve explicitar revisão estruturada com fluxo PRISMA, curadoria assistida por IA e revisão humana final.', 'Não crie seção manual de Referências. O renderizador do programa deve inserir a bibliografia automaticamente.']).replace`
- `FileNotFoundError`
- `Path`
- `Path(args.artigo_dir).resolve`
- `Path(args.csl_path).resolve`
- `Path(args.dados_pesquisa_path).resolve`
- `Path(args.prisma_config).resolve`
- `Path(args.root_dir).resolve`
- `Path(args.toml_output).resolve`
- `arr`
- `bool`
- `csl.exists`
- `dados_path_from_config`
- `esc`
- `map`
- `meta_path.exists`
- `p.exists`
- `prefix.startswith`
- `print`
- `prisma_config.exists`
- `read_toml`
- `render_template`
- `slug`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_congelar_artigo:freeze`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `FileNotFoundError('Arquivos obrigatórios ausentes para gerar TOML:\n' + '\n'.join(map(str, missing)))`
- `FileNotFoundError(f'Template não encontrado: {template}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `relatorio_prisma_prisma_fluxo_pmf.referencias_incluidas.bib`
- `relatorio_prisma_prisma_fluxo_pmf.referencias_incluidas_seminario.csv`
- `relatorio_prisma_prisma_fluxo_pmf.relatorio_prisma_final.pdf`
- `app_bundle/templates/prisma_artigo_generico.toml.template`
- `app_bundle/misc/fgv.png`
- `app_bundle/prompts/global/orientacao_geral_execucao.txt`
- `app_bundle/prompts/document/paper.txt`
- `artigo_final_abnt.toml`

#### Complexidade estrutural observada

```yaml
branches: 13
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b17f589816d0fa9aabc471546bc1ae79a30ecb505fd5ee57d771136263d49526`
**LOC do arquivo-fonte:** 204


### `app_bundle.scripts.pipeline.prisma_congelar_artigo:freeze`

**Assinatura:**
```python
def freeze(args)
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_congelar_artigo.py`
**Linhas:** 164–194
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `freeze` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `args` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |

#### Retorno

Sem anotação de retorno explícita. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Executa processo ou ferramenta externa.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `(dest / 'ARQUIVOS_CONGELADOS.txt').write_text`
- `(dest / 'MANIFESTO_ARTIGO.json').write_text`
- `(dest / 'MANIFESTO_SHA256.txt').write_text`
- `Path`
- `Path(args.artigo_dir).resolve`
- `Path(args.dest_dir).resolve`
- `Path(args.out_dir).resolve`
- `Path(args.pipeline_script).resolve`
- `RuntimeError`
- `SystemExit`
- `copy_one`
- `dest.mkdir`
- `dt.datetime.now`
- `dt.datetime.now().isoformat`
- `generate_toml`
- `json.dumps`
- `len`
- `print`
- `sorted`
- `str`
- `subprocess.run`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_congelar_artigo:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('TOML não gerado.')`
- `SystemExit(proc.returncode)`

#### Opções CLI mencionadas diretamente no corpo

- `--config`

#### Artefatos/caminhos literais reconhecidos no corpo

- `.referencias_incluidas.bib`
- `.referencias_incluidas_seminario.csv`
- `.triagem_humana.csv`
- `.relatorio_prisma_final.pdf`
- `.curadoria_ia_referencias.xlsx`
- `.relatorio_prisma_preliminar.pdf`
- `.diagrama_prisma.png`
- `.diagrama_prisma_contagens.json`
- `.busca_prisma_log.json`
- `.triagem_titulo_resumo.csv`
- `.triagem_titulo_resumo.xlsx`
- `.curadoria_ia_resumo.txt`
- `.curadoria_ia_log.json`
- `MANIFESTO_SHA256.txt`
- `ARQUIVOS_CONGELADOS.txt`
- `MANIFESTO_ARTIGO.json`

#### Complexidade estrutural observada

```yaml
branches: 7
loops: 2
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b17f589816d0fa9aabc471546bc1ae79a30ecb505fd5ee57d771136263d49526`
**LOC do arquivo-fonte:** 204


### `app_bundle.scripts.pipeline.prisma_congelar_artigo:main`

**Assinatura:**
```python
def main(argv = None)
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_congelar_artigo.py`
**Linhas:** 196–203
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa o ponto de entrada correspondente e coordena o fluxo associado.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `não anotado` | `None` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `0`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `argparse.ArgumentParser`
- `freeze`
- `p.add_argument`
- `p.parse_args`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `--artigo-dir`
- `--artigo-prefix`
- `--autor`
- `--csl-path`
- `--dados-pesquisa-path`
- `--dest-dir`
- `--gerar-artigo-final`
- `--gerar-toml-artigo`
- `--openai-model`
- `--out-dir`
- `--pipeline-script`
- `--prefix`
- `--prisma-config`
- `--professor`
- `--root-dir`
- `--toml-output`

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `b17f589816d0fa9aabc471546bc1ae79a30ecb505fd5ee57d771136263d49526`
**LOC do arquivo-fonte:** 204


### `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:log`

**Assinatura:**
```python
def log(kind: str, msg: str) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_curadoria_ia_referencias.py`
**Linhas:** 104–105
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `log` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `kind` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `kind` recebido pela função; não há descrição individual explícita no código. |
| `msg` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `msg` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `print`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `73f3e311e2c2d6ab169c77e63d632206aa2a22c74dd8a7f1973887aab01d3006`
**LOC do arquivo-fonte:** 997


### `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:load_dotenv`

**Assinatura:**
```python
def load_dotenv(path: Path = Path('.env')) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_curadoria_ia_referencias.py`
**Linhas:** 108–119
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Carrega a responsabilidade representada por `dotenv` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `Path('.env')` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `k.strip`
- `line.split`
- `line.startswith`
- `path.exists`
- `path.read_text`
- `path.read_text(encoding='utf-8', errors='ignore').splitlines`
- `raw.strip`
- `v.strip`
- `v.strip().strip`
- `v.strip().strip('"').strip`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_ap003e_body_make_client_1`
- `app_bundle.scripts.pipeline.academic_pipeline_toml_generator_interativo:suggest_prisma_keywords_with_ai`
- `app_bundle.scripts.pipeline.prisma_busca_externa:load_search_environment`
- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:apply_ai`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `73f3e311e2c2d6ab169c77e63d632206aa2a22c74dd8a7f1973887aab01d3006`
**LOC do arquivo-fonte:** 997


### `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:norm_text`

**Assinatura:**
```python
def norm_text(s: object) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_curadoria_ia_referencias.py`
**Linhas:** 122–126
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `norm_text` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `s` | positional-or-keyword | `object` | `obrigatório/ausente` | Parâmetro `s` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `re.sub('\\s+', ' ', s).strip()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `''.join`
- `re.sub`
- `re.sub('\\s+', ' ', s).strip`
- `str`
- `str(s or '').strip`
- `unicodedata.combining`
- `unicodedata.normalize`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:norm_key`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `73f3e311e2c2d6ab169c77e63d632206aa2a22c74dd8a7f1973887aab01d3006`
**LOC do arquivo-fonte:** 997


### `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:norm_key`

**Assinatura:**
```python
def norm_key(s: object) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_curadoria_ia_referencias.py`
**Linhas:** 129–130
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `norm_key` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `s` | positional-or-keyword | `object` | `obrigatório/ausente` | Parâmetro `s` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `norm_text(s).lower()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `norm_text`
- `norm_text(s).lower`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `73f3e311e2c2d6ab169c77e63d632206aa2a22c74dd8a7f1973887aab01d3006`
**LOC do arquivo-fonte:** 997


### `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:clean`

**Assinatura:**
```python
def clean(s: object, max_len: int | None = None) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_curadoria_ia_referencias.py`
**Linhas:** 133–137
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Limpa a responsabilidade representada por `clean` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `s` | positional-or-keyword | `object` | `obrigatório/ausente` | Parâmetro `s` recebido pela função; não há descrição individual explícita no código. |
| `max_len` | positional-or-keyword | `int \| None` | `None` | Parâmetro `max_len` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `t[:max_len - 1].rstrip() + '…'`; `t`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `len`
- `re.sub`
- `re.sub('\\s+', ' ', str(s or '')).strip`
- `str`
- `t[:max_len - 1].rstrip`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `73f3e311e2c2d6ab169c77e63d632206aa2a22c74dd8a7f1973887aab01d3006`
**LOC do arquivo-fonte:** 997


### `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:col_idx`

**Assinatura:**
```python
def col_idx(ref: str) -> int
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_curadoria_ia_referencias.py`
**Linhas:** 140–147
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `col_idx` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `ref` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `ref` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `0`; `n - 1`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `m.group`
- `ord`
- `re.match`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:read_xlsx`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `73f3e311e2c2d6ab169c77e63d632206aa2a22c74dd8a7f1973887aab01d3006`
**LOC do arquivo-fonte:** 997


### `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:read_xlsx`

**Assinatura:**
```python
def read_xlsx(path: Path, sheet_name: str | None = None) -> tuple[list[str], list[dict[str, str]], str]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_curadoria_ia_referencias.py`
**Linhas:** 150–215
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Lê a responsabilidade representada por `xlsx` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `sheet_name` | positional-or-keyword | `str \| None` | `None` | Parâmetro `sheet_name` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `tuple[list[str], list[dict[str, str]], str]`. Retornos observados estaticamente incluem: `([], [], chosen_sheet or '')`; `(headers, data, chosen_sheet or '')`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `''.join`
- `ET.fromstring`
- `ValueError`
- `any`
- `available.append`
- `c.attrib.get`
- `c.find`
- `c.findall`
- `col_idx`
- `data.append`
- `int`
- `len`
- `r.findall`
- `range`
- `rel_map.get`
- `rels.findall`
- `resolve_target`
- `root.findall`
- `rows.append`
- `shared.append`
- `sheet.attrib.get`
- `si.findall`
- `str`
- `str(h or '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:load_input`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `ValueError(f"Aba '{sheet_name}' não encontrada em {path}. Abas disponíveis: {available}")`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `{http://schemas.openxmlformats.org/officeDocument/2006/relationships}id`

#### Complexidade estrutural observada

```yaml
branches: 10
loops: 6
comprehensions: 6
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `73f3e311e2c2d6ab169c77e63d632206aa2a22c74dd8a7f1973887aab01d3006`
**LOC do arquivo-fonte:** 997


### `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:read_csv`

**Assinatura:**
```python
def read_csv(path: Path) -> tuple[list[str], list[dict[str, str]], str]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_curadoria_ia_referencias.py`
**Linhas:** 218–226
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Lê a responsabilidade representada por `csv` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `tuple[list[str], list[dict[str, str]], str]`. Retornos observados estaticamente incluem: `(list(reader.fieldnames or []), [dict(r) for r in reader], 'csv')`.

#### Efeitos colaterais e fronteiras externas

- Abre arquivo para leitura ou inspeção.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `SystemExit`
- `csv.DictReader`
- `dict`
- `list`
- `path.open`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `SystemExit(f'Não consegui ler CSV: {path}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 1
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `73f3e311e2c2d6ab169c77e63d632206aa2a22c74dd8a7f1973887aab01d3006`
**LOC do arquivo-fonte:** 997


### `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:load_input`

**Assinatura:**
```python
def load_input(path: Path) -> tuple[list[str], list[dict[str, str]], str]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_curadoria_ia_referencias.py`
**Linhas:** 229–243
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Carrega a responsabilidade representada por `input` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `tuple[list[str], list[dict[str, str]], str]`. Retornos observados estaticamente incluem: `(h, r, f'{path} :: aba {s}')`; `(best_h, best_r, f'{path} :: aba {best_sheet}')`; `(h, r, f'{path} :: {s}')`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `len`
- `path.suffix.lower`
- `read_csv`
- `read_xlsx`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.xlsx`

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `73f3e311e2c2d6ab169c77e63d632206aa2a22c74dd8a7f1973887aab01d3006`
**LOC do arquivo-fonte:** 997


### `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:find_default_input`

**Assinatura:**
```python
def find_default_input(out_dir: Path) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_curadoria_ia_referencias.py`
**Linhas:** 246–256
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Localiza a responsabilidade representada por `default_input` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `out_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `out_dir` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `p`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `SystemExit`
- `p.exists`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `SystemExit('Não encontrei triagem_titulo_resumo.xlsx/csv. Informe --input.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.triagem_titulo_resumo.xlsx`
- `.triagem_titulo_resumo.csv`
- `.curadoria_ia_referencias.xlsx`
- `Não encontrei triagem_titulo_resumo.xlsx/csv. Informe --input.`

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `73f3e311e2c2d6ab169c77e63d632206aa2a22c74dd8a7f1973887aab01d3006`
**LOC do arquivo-fonte:** 997


### `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:parse_number`

**Assinatura:**
```python
def parse_number(text: str, key: str, default: int) -> int
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_curadoria_ia_referencias.py`
**Linhas:** 259–261
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Interpreta a responsabilidade representada por `number` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |
| `key` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `key` recebido pela função; não há descrição individual explícita no código. |
| `default` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `default` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `int(m.group(1)) if m else default`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `int`
- `m.group`
- `re.escape`
- `re.search`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:load_prompt`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `73f3e311e2c2d6ab169c77e63d632206aa2a22c74dd8a7f1973887aab01d3006`
**LOC do arquivo-fonte:** 997


### `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:parse_section_numbers`

**Assinatura:**
```python
def parse_section_numbers(text: str, section: str, defaults: dict[str, int]) -> dict[str, int]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_curadoria_ia_referencias.py`
**Linhas:** 264–279
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Interpreta a responsabilidade representada por `section_numbers` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |
| `section` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `section` recebido pela função; não há descrição individual explícita no código. |
| `defaults` | positional-or-keyword | `dict[str, int]` | `obrigatório/ausente` | Parâmetro `defaults` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, int]`. Retornos observados estaticamente incluem: `out`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `body.splitlines`
- `dict`
- `int`
- `k.strip`
- `line.split`
- `m.group`
- `raw.split`
- `raw.split('#', 1)[0].strip`
- `re.escape`
- `re.match`
- `re.search`
- `v.strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:load_prompt`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `73f3e311e2c2d6ab169c77e63d632206aa2a22c74dd8a7f1973887aab01d3006`
**LOC do arquivo-fonte:** 997


### `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:parse_block_scalar`

**Assinatura:**
```python
def parse_block_scalar(text: str, key: str, default: str = '') -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_curadoria_ia_referencias.py`
**Linhas:** 282–290
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Interpreta a responsabilidade representada por `block_scalar` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |
| `key` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `key` recebido pela função; não há descrição individual explícita no código. |
| `default` | positional-or-keyword | `str` | `''` | Parâmetro `default` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `' '.join((ln for ln in lines if ln))`; `m2.group(1).strip().strip('"').strip("'")`; `default`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `ln.strip`
- `m.group`
- `m.group('body').splitlines`
- `m2.group`
- `m2.group(1).strip`
- `m2.group(1).strip().strip`
- `m2.group(1).strip().strip('"').strip`
- `re.escape`
- `re.search`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:load_prompt`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `73f3e311e2c2d6ab169c77e63d632206aa2a22c74dd8a7f1973887aab01d3006`
**LOC do arquivo-fonte:** 997


### `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:load_prompt`

**Assinatura:**
```python
def load_prompt(path: Path | None) -> dict
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_curadoria_ia_referencias.py`
**Linhas:** 293–316
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Carrega a responsabilidade representada por `prompt` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path \| None` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `dict`. Retornos observados estaticamente incluem: `cfg`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `dict`
- `log`
- `parse_block_scalar`
- `parse_number`
- `parse_section_numbers`
- `path.exists`
- `path.read_text`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `73f3e311e2c2d6ab169c77e63d632206aa2a22c74dd8a7f1973887aab01d3006`
**LOC do arquivo-fonte:** 997


### `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:row_text`

**Assinatura:**
```python
def row_text(row: dict[str, str]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_curadoria_ia_referencias.py`
**Linhas:** 319–324
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `row_text` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `row` | positional-or-keyword | `dict[str, str]` | `obrigatório/ausente` | Parâmetro `row` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `norm_key(' '.join((str(row.get(f, '')) for f in fields)))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `norm_key`
- `row.get`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:category_score`
- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:infer_country`
- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:infer_design`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `73f3e311e2c2d6ab169c77e63d632206aa2a22c74dd8a7f1973887aab01d3006`
**LOC do arquivo-fonte:** 997


### `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:category_score`

**Assinatura:**
```python
def category_score(row: dict[str, str], category: str) -> int
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_curadoria_ia_referencias.py`
**Linhas:** 327–340
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `category_score` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `row` | positional-or-keyword | `dict[str, str]` | `obrigatório/ausente` | Parâmetro `row` recebido pela função; não há descrição individual explícita no código. |
| `category` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `category` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `score`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `TERMS.get`
- `any`
- `len`
- `max`
- `min`
- `norm_key`
- `row_text`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:compute_scores`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 1
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `73f3e311e2c2d6ab169c77e63d632206aa2a22c74dd8a7f1973887aab01d3006`
**LOC do arquivo-fonte:** 997


### `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:compute_scores`

**Assinatura:**
```python
def compute_scores(row: dict[str, str], prompt_cfg: dict) -> dict[str, str]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_curadoria_ia_referencias.py`
**Linhas:** 343–379
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Calcula a responsabilidade representada por `scores` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `row` | positional-or-keyword | `dict[str, str]` | `obrigatório/ausente` | Parâmetro `row` recebido pela função; não há descrição individual explícita no código. |
| `prompt_cfg` | positional-or-keyword | `dict` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `dict[str, str]`. Retornos observados estaticamente incluem: `{'aderencia_ia_automacao': str(scores['ia_automacao']), 'aderencia_telepericia': str(scores['telepericia_avaliacao_remota']), 'aderencia_decisao_evidencias': str(scores['decisao_baseada_em_evidencias']), 'aderencia_auditoria_qualidade': str(scores['auditoria_qualidade_integridade']), 'aderencia_fila_capacidade': str(scores['fila_capacidade_equidade']), 'aderencia_avaliacao_pericial': str(scores['avaliacao_medico_pericial']), 'escore_curadoria': str(weighted), 'categoria_dominante': dominant, 'eixo_seminario': eixo, 'classe_curadoria': classe}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `CATEGORY_LABELS.get`
- `category_score`
- `class_from_scores`
- `int`
- `max`
- `min`
- `norm_key`
- `round`
- `row.get`
- `scores.items`
- `str`
- `sum`
- `weights.get`
- `weights.values`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:curate_rows`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 1
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `73f3e311e2c2d6ab169c77e63d632206aa2a22c74dd8a7f1973887aab01d3006`
**LOC do arquivo-fonte:** 997


### `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:class_from_scores`

**Assinatura:**
```python
def class_from_scores(scores: dict[str, int], dominant: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_curadoria_ia_referencias.py`
**Linhas:** 382–391
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `class_from_scores` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `scores` | positional-or-keyword | `dict[str, int]` | `obrigatório/ausente` | Parâmetro `scores` recebido pela função; não há descrição individual explícita no código. |
| `dominant` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `dominant` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'NUCLEO_TECNOLOGICO'`; `'NUCLEO_DECISORIO'`; `'APOIO_PERICIAL'`; `'APOIO_CONTEXTO'`; `'EXCLUIR'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

Nenhum identificado pela análise estática.

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:compute_scores`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `73f3e311e2c2d6ab169c77e63d632206aa2a22c74dd8a7f1973887aab01d3006`
**LOC do arquivo-fonte:** 997


### `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:infer_design`

**Assinatura:**
```python
def infer_design(row: dict[str, str]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_curadoria_ia_referencias.py`
**Linhas:** 394–412
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Infere a responsabilidade representada por `design` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `row` | positional-or-keyword | `dict[str, str]` | `obrigatório/ausente` | Parâmetro `row` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'Revisão de escopo'`; `'Revisão sistemática'`; `'Estudo qualitativo/análise documental'`; `'Estudo/modelo computacional ou suporte decisório'`; `'Avaliação de estudo-piloto'`; `'Desenvolvimento/validação de instrumento'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `any`
- `row_text`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:default_matrix_fields`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 8
loops: 0
comprehensions: 5
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `73f3e311e2c2d6ab169c77e63d632206aa2a22c74dd8a7f1973887aab01d3006`
**LOC do arquivo-fonte:** 997


### `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:infer_country`

**Assinatura:**
```python
def infer_country(row: dict[str, str]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_curadoria_ia_referencias.py`
**Linhas:** 415–430
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Infere a responsabilidade representada por `country` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `row` | positional-or-keyword | `dict[str, str]` | `obrigatório/ausente` | Parâmetro `row` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `label`; `'Internacional ou não especificado'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `any`
- `row_text`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:default_matrix_fields`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `73f3e311e2c2d6ab169c77e63d632206aa2a22c74dd8a7f1973887aab01d3006`
**LOC do arquivo-fonte:** 997


### `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:abnt_ref`

**Assinatura:**
```python
def abnt_ref(row: dict[str, str]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_curadoria_ia_referencias.py`
**Linhas:** 433–453
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `abnt_ref` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `row` | positional-or-keyword | `dict[str, str]` | `obrigatório/ausente` | Parâmetro `row` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `' '.join(parts)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `autores.upper`
- `clean`
- `parts.append`
- `row.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:default_matrix_fields`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `73f3e311e2c2d6ab169c77e63d632206aa2a22c74dd8a7f1973887aab01d3006`
**LOC do arquivo-fonte:** 997


### `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:default_matrix_fields`

**Assinatura:**
```python
def default_matrix_fields(row: dict[str, str]) -> dict[str, str]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_curadoria_ia_referencias.py`
**Linhas:** 456–467
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `default_matrix_fields` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `row` | positional-or-keyword | `dict[str, str]` | `obrigatório/ausente` | Parâmetro `row` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, str]`. Retornos observados estaticamente incluem: `{'pais_contexto': row.get('pais_contexto') or infer_country(row), 'objetivo_estudo': row.get('objetivo_estudo') or f"Analisar contribuição do estudo para {row.get('eixo_seminario', 'o recorte do seminário')}.", 'desenho_metodo': row.get('desenho_metodo') or infer_design(row), 'amostra_base': row.get('amostra_base') or 'Não detalhado automaticamente; conferir texto completo.', 'achados_principais': row.get('achados_principais') or clean(row.get('resumo', ''), 420), 'limitacoes': row.get('limitacoes') or 'Limitações não detalhadas automaticamente; confirmar no texto completo.', 'contribuicao_pergunta': row.get('contribuicao_pergunta') or 'Contribui para discutir IA, automação, teleperícia, decisão baseada em evidências, qualidade decisória ou gestão de fluxo.', 'como_usar_no_seminario': row.get('como_usar_no_seminario') or f"Usar no eixo: {row.get('eixo_seminario', 'curadoria geral')}.", 'referencia_abnt_rascunho': row.get('referencia_abnt_rascunho') or abnt_ref(row)}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `abnt_ref`
- `clean`
- `infer_country`
- `infer_design`
- `row.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:curate_rows`
- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:manual_reexport`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `73f3e311e2c2d6ab169c77e63d632206aa2a22c74dd8a7f1973887aab01d3006`
**LOC do arquivo-fonte:** 997


### `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:title_for_ai`

**Assinatura:**
```python
def title_for_ai(row: dict[str, str]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_curadoria_ia_referencias.py`
**Linhas:** 470–471
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `title_for_ai` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `row` | positional-or-keyword | `dict[str, str]` | `obrigatório/ausente` | Parâmetro `row` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `clean(row.get('titulo') or row.get('title') or '', 260)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `clean`
- `row.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:call_ai_batch`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `73f3e311e2c2d6ab169c77e63d632206aa2a22c74dd8a7f1973887aab01d3006`
**LOC do arquivo-fonte:** 997


### `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:call_ai_batch`

**Assinatura:**
```python
def call_ai_batch(batch: list[dict[str, str]], prompt_cfg: dict, model: str, api_url: str, api_key: str) -> list[dict[str, str]]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_curadoria_ia_referencias.py`
**Linhas:** 474–567
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `call_ai_batch` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `batch` | positional-or-keyword | `list[dict[str, str]]` | `obrigatório/ausente` | Parâmetro `batch` recebido pela função; não há descrição individual explícita no código. |
| `prompt_cfg` | positional-or-keyword | `dict` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `model` | positional-or-keyword | `str` | `obrigatório/ausente` | Identificador do modelo utilizado pela etapa de IA. |
| `api_url` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `api_url` recebido pela função; não há descrição individual explícita no código. |
| `api_key` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `api_key` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[dict[str, str]]`. Retornos observados estaticamente incluem: `parsed`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.
- Lê arquivos ou artefatos do filesystem.
- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `ValueError`
- `clean`
- `content.startswith`
- `isinstance`
- `items.append`
- `json.dumps`
- `json.dumps({'model': model, 'messages': [{'role': 'system', 'content': system}, {'role': 'user', 'content': json.dumps(user, ensure_ascii=False)}], 'temperature': 0.1}, ensure_ascii=False).encode`
- `json.loads`
- `m.group`
- `obj['choices'][0]['message']['content'].strip`
- `prompt_cfg.get`
- `r.get`
- `re.search`
- `re.sub`
- `re.sub('^```(?:json)?', '', content).strip`
- `re.sub('```$', '', content).strip`
- `resp.read`
- `resp.read().decode`
- `title_for_ai`
- `urllib.request.Request`
- `urllib.request.urlopen`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:apply_ai`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `ValueError('Resposta da IA não contém JSON.')`
- `ValueError('Resposta JSON da IA não é uma lista.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 1
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `73f3e311e2c2d6ab169c77e63d632206aa2a22c74dd8a7f1973887aab01d3006`
**LOC do arquivo-fonte:** 997


### `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:chunked`

**Assinatura:**
```python
def chunked(items, size)
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_curadoria_ia_referencias.py`
**Linhas:** 570–572
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `chunked` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `items` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `items` recebido pela função; não há descrição individual explícita no código. |
| `size` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `size` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `len`
- `range`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:apply_ai`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `73f3e311e2c2d6ab169c77e63d632206aa2a22c74dd8a7f1973887aab01d3006`
**LOC do arquivo-fonte:** 997


### `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:apply_ai`

**Assinatura:**
```python
def apply_ai(rows: list[dict[str, str]], args: argparse.Namespace, prompt_cfg: dict) -> tuple[dict[str, dict[str, str]], list[dict]]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_curadoria_ia_referencias.py`
**Linhas:** 575–611
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Aplica a responsabilidade representada por `ai` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `rows` | positional-or-keyword | `list[dict[str, str]]` | `obrigatório/ausente` | Parâmetro `rows` recebido pela função; não há descrição individual explícita no código. |
| `args` | positional-or-keyword | `argparse.Namespace` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `prompt_cfg` | positional-or-keyword | `dict` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `tuple[dict[str, dict[str, str]], list[dict]]`. Retornos observados estaticamente incluem: `({}, [])`; `(decisions, log_items)`.

#### Efeitos colaterais e fronteiras externas

- Lê configuração/credenciais do ambiente do processo.

#### Chamadas realizadas pelo corpo direto

- `call_ai_batch`
- `chunked`
- `clean`
- `enumerate`
- `int`
- `item.get`
- `item.items`
- `len`
- `load_dotenv`
- `log`
- `log_items.append`
- `min`
- `os.environ.get`
- `r.get`
- `range`
- `sorted`
- `str`
- `str(item.get('row_id', '')).strip`
- `time.sleep`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:curate_rows`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 3
comprehensions: 1
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `73f3e311e2c2d6ab169c77e63d632206aa2a22c74dd8a7f1973887aab01d3006`
**LOC do arquivo-fonte:** 997


### `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:fill_headers`

**Assinatura:**
```python
def fill_headers(headers: list[str]) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_curadoria_ia_referencias.py`
**Linhas:** 614–630
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `fill_headers` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `headers` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Cabeçalhos usados em requisição/integração externa. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `out`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `list`
- `out.append`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:curate_rows`
- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:manual_reexport`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `73f3e311e2c2d6ab169c77e63d632206aa2a22c74dd8a7f1973887aab01d3006`
**LOC do arquivo-fonte:** 997


### `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:manual_reexport`

**Assinatura:**
```python
def manual_reexport(headers: list[str], rows: list[dict[str, str]]) -> tuple[list[str], list[dict[str, str]]]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_curadoria_ia_referencias.py`
**Linhas:** 633–658
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `manual_reexport` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `headers` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Cabeçalhos usados em requisição/integração externa. |
| `rows` | positional-or-keyword | `list[dict[str, str]]` | `obrigatório/ausente` | Parâmetro `rows` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `tuple[list[str], list[dict[str, str]]]`. Retornos observados estaticamente incluem: `(headers, out)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `default_matrix_fields`
- `default_matrix_fields(r).items`
- `dict`
- `fill_headers`
- `out.append`
- `r.get`
- `r.setdefault`
- `str`
- `str(r.get('incluir_final', '')).strip`
- `str(r.get('incluir_final', '')).strip().upper`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `73f3e311e2c2d6ab169c77e63d632206aa2a22c74dd8a7f1973887aab01d3006`
**LOC do arquivo-fonte:** 997


### `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:select_final`

**Assinatura:**
```python
def select_final(rows: list[dict[str, str]], prompt_cfg: dict, max_incluir: int, threshold: int) -> set[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_curadoria_ia_referencias.py`
**Linhas:** 661–701
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Seleciona a responsabilidade representada por `final` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `rows` | positional-or-keyword | `list[dict[str, str]]` | `obrigatório/ausente` | Parâmetro `rows` recebido pela função; não há descrição individual explícita no código. |
| `prompt_cfg` | positional-or-keyword | `dict` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `max_incluir` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `max_incluir` recebido pela função; não há descrição individual explícita no código. |
| `threshold` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `threshold` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `set[str]`. Retornos observados estaticamente incluem: `selected`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `eligible`
- `int`
- `len`
- `minima.items`
- `r.get`
- `selected.add`
- `set`
- `sorted`
- `x.get`
- `{'ia_automacao': 'aderencia_ia_automacao', 'telepericia_avaliacao_remota': 'aderencia_telepericia', 'decisao_baseada_em_evidencias': 'aderencia_decisao_evidencias', 'auditoria_qualidade_integridade': 'aderencia_auditoria_qualidade', 'fila_capacidade_equidade': 'aderencia_fila_capacidade'}.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:curate_rows`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 3
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `73f3e311e2c2d6ab169c77e63d632206aa2a22c74dd8a7f1973887aab01d3006`
**LOC do arquivo-fonte:** 997


### `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:select_final.eligible`

**Assinatura:**
```python
def eligible(r)
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_curadoria_ia_referencias.py`
**Linhas:** 665–666
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `eligible` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `r` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `r` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `int(r.get('escore_curadoria') or 0) >= threshold`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `int`
- `r.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:select_final`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `73f3e311e2c2d6ab169c77e63d632206aa2a22c74dd8a7f1973887aab01d3006`
**LOC do arquivo-fonte:** 997


### `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:curate_rows`

**Assinatura:**
```python
def curate_rows(headers: list[str], rows: list[dict[str, str]], args: argparse.Namespace, prompt_cfg: dict) -> tuple[list[str], list[dict[str, str]], list[dict]]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_curadoria_ia_referencias.py`
**Linhas:** 704–762
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `curate_rows` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `headers` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Cabeçalhos usados em requisição/integração externa. |
| `rows` | positional-or-keyword | `list[dict[str, str]]` | `obrigatório/ausente` | Parâmetro `rows` recebido pela função; não há descrição individual explícita no código. |
| `args` | positional-or-keyword | `argparse.Namespace` | `obrigatório/ausente` | Namespace/objeto com argumentos já interpretados pela interface de chamada. |
| `prompt_cfg` | positional-or-keyword | `dict` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `tuple[list[str], list[dict[str, str]], list[dict]]`. Retornos observados estaticamente incluem: `(headers, rows, ai_log)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `ai.get`
- `ai.items`
- `ai_decisions.get`
- `apply_ai`
- `compute_scores`
- `default_matrix_fields`
- `enumerate`
- `fill_headers`
- `int`
- `row.get`
- `row.update`
- `select_final`
- `sorted`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 7
loops: 4
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `73f3e311e2c2d6ab169c77e63d632206aa2a22c74dd8a7f1973887aab01d3006`
**LOC do arquivo-fonte:** 997


### `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:write_csv`

**Assinatura:**
```python
def write_csv(path: Path, headers: list[str], rows: list[dict[str, str]]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_curadoria_ia_referencias.py`
**Linhas:** 765–772
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Grava a responsabilidade representada por `csv` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `headers` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Cabeçalhos usados em requisição/integração externa. |
| `rows` | positional-or-keyword | `list[dict[str, str]]` | `obrigatório/ausente` | Parâmetro `rows` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Abre arquivo para leitura ou inspeção.
- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `csv.DictWriter`
- `h.startswith`
- `path.open`
- `path.parent.mkdir`
- `r.get`
- `writer.writeheader`
- `writer.writerow`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `73f3e311e2c2d6ab169c77e63d632206aa2a22c74dd8a7f1973887aab01d3006`
**LOC do arquivo-fonte:** 997


### `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:write_xlsx`

**Assinatura:**
```python
def write_xlsx(path: Path, headers: list[str], rows: list[dict[str, str]], prompt_cfg: dict) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_curadoria_ia_referencias.py`
**Linhas:** 775–887
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Grava a responsabilidade representada por `xlsx` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `headers` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Cabeçalhos usados em requisição/integração externa. |
| `rows` | positional-or-keyword | `list[dict[str, str]]` | `obrigatório/ausente` | Parâmetro `rows` recebido pela função; não há descrição individual explícita no código. |
| `prompt_cfg` | positional-or-keyword | `dict` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `Alignment`
- `BarChart`
- `Reference`
- `Table`
- `TableStyleInfo`
- `Workbook`
- `chart.add_data`
- `chart.set_categories`
- `counts.get`
- `counts.items`
- `get_column_letter`
- `h.startswith`
- `int`
- `json.dumps`
- `len`
- `log`
- `max`
- `min`
- `path.parent.mkdir`
- `prompt_cfg.get`
- `r.get`
- `range`
- `sheet.iter_rows`
- `sorted`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 12
comprehensions: 4
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `73f3e311e2c2d6ab169c77e63d632206aa2a22c74dd8a7f1973887aab01d3006`
**LOC do arquivo-fonte:** 997


### `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:write_xlsx.style_header`

**Assinatura:**
```python
def style_header(row)
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_curadoria_ia_referencias.py`
**Linhas:** 811–816
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `style_header` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `row` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `row` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Alignment`
- `Font`
- `PatternFill`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:write_xlsx`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `73f3e311e2c2d6ab169c77e63d632206aa2a22c74dd8a7f1973887aab01d3006`
**LOC do arquivo-fonte:** 997


### `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:write_summary`

**Assinatura:**
```python
def write_summary(path: Path, rows: list[dict[str, str]], source: str, prompt_cfg: dict, usar_ia: bool) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_curadoria_ia_referencias.py`
**Linhas:** 890–915
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Grava a responsabilidade representada por `summary` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `rows` | positional-or-keyword | `list[dict[str, str]]` | `obrigatório/ausente` | Parâmetro `rows` recebido pela função; não há descrição individual explícita no código. |
| `source` | positional-or-keyword | `str` | `obrigatório/ausente` | Origem dos dados ou arquivo de entrada. |
| `prompt_cfg` | positional-or-keyword | `dict` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `usar_ia` | positional-or-keyword | `bool` | `obrigatório/ausente` | Parâmetro `usar_ia` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `counts.get`
- `counts.items`
- `len`
- `lines.append`
- `path.write_text`
- `prompt_cfg.get`
- `prompt_cfg.get('pesos', {}).items`
- `r.get`
- `sorted`
- `str`
- `str(r.get('incluir_final', '')).upper`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 3
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `73f3e311e2c2d6ab169c77e63d632206aa2a22c74dd8a7f1973887aab01d3006`
**LOC do arquivo-fonte:** 997


### `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:main`

**Assinatura:**
```python
def main() -> int
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_curadoria_ia_referencias.py`
**Linhas:** 918–993
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa o ponto de entrada correspondente e coordena o fluxo associado.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `1`; `0`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `argparse.ArgumentParser`
- `bool`
- `curate_rows`
- `dt.datetime.now`
- `dt.datetime.now().isoformat`
- `find_default_input`
- `int`
- `json.dumps`
- `len`
- `load_input`
- `load_prompt`
- `log`
- `log_json.write_text`
- `manual_reexport`
- `out_dir.mkdir`
- `parser.add_argument`
- `parser.parse_args`
- `prompt_cfg.get`
- `r.get`
- `str`
- `str(r.get('incluir_final', '')).upper`
- `write_csv`
- `write_summary`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `--api-url`
- `--batch-size`
- `--config`
- `--input`
- `--limiar-minimo-inclusao`
- `--max-incluir`
- `--max-retries`
- `--model`
- `--out-dir`
- `--prompt-curadoria`
- `--reexportar-xlsx`
- `--top-n-candidatos`
- `--usar-ia`

#### Artefatos/caminhos literais reconhecidos no corpo

- `app_bundle/projetos/prisma_fluxo_pmf/prisma_fluxo_pmf.toml`
- `.triagem_humana.csv`
- `.referencias_incluidas_seminario.csv`
- `.curadoria_ia_referencias.xlsx`
- `.curadoria_ia_resumo.txt`
- `.curadoria_ia_log.json`

#### Complexidade estrutural observada

```yaml
branches: 7
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `73f3e311e2c2d6ab169c77e63d632206aa2a22c74dd8a7f1973887aab01d3006`
**LOC do arquivo-fonte:** 997


### `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:log`

**Assinatura:**
```python
def log(kind: str, msg: str) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_diagrama_fluxo.py`
**Linhas:** 90–91
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `log` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `kind` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `kind` recebido pela função; não há descrição individual explícita no código. |
| `msg` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `msg` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `print`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4eea6a684358943bc75f20322f813921f542f4d534bf3dc23316f01f90138a11`
**LOC do arquivo-fonte:** 592


### `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:norm`

**Assinatura:**
```python
def norm(s: object) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_diagrama_fluxo.py`
**Linhas:** 94–100
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `norm` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `s` | positional-or-keyword | `object` | `obrigatório/ausente` | Parâmetro `s` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `txt`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `''.join`
- `re.sub`
- `re.sub('\\s+', ' ', txt).strip`
- `re.sub('\\s+', ' ', txt).strip().lower`
- `str`
- `str(s or '').strip`
- `unicodedata.combining`
- `unicodedata.normalize`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4eea6a684358943bc75f20322f813921f542f4d534bf3dc23316f01f90138a11`
**LOC do arquivo-fonte:** 592


### `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:to_int`

**Assinatura:**
```python
def to_int(value: Any) -> int | None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_diagrama_fluxo.py`
**Linhas:** 103–121
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `to_int` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `int | None`. Retornos observados estaticamente incluem: `None`; `int(value)`; `int(float(m.group(0).replace(',', '.')))`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `float`
- `int`
- `isinstance`
- `m.group`
- `m.group(0).replace`
- `re.search`
- `str`
- `str(value).strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:set_count`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `4eea6a684358943bc75f20322f813921f542f4d534bf3dc23316f01f90138a11`
**LOC do arquivo-fonte:** 592


### `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:set_count`

**Assinatura:**
```python
def set_count(counts: dict[str, int], key: str, value: Any, *, overwrite: bool = False) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_diagrama_fluxo.py`
**Linhas:** 124–129
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `set_count` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `counts` | positional-or-keyword | `dict[str, int]` | `obrigatório/ausente` | Parâmetro `counts` recebido pela função; não há descrição individual explícita no código. |
| `key` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `key` recebido pela função; não há descrição individual explícita no código. |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |
| `overwrite` | keyword-only | `bool` | `False` | Parâmetro `overwrite` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `to_int`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:derive_counts`
- `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:derive_from_triage_csv`
- `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:parse_org_flow_tables`
- `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:recursive_scan_json`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4eea6a684358943bc75f20322f813921f542f4d534bf3dc23316f01f90138a11`
**LOC do arquivo-fonte:** 592


### `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:canonical_key`

**Assinatura:**
```python
def canonical_key(label: str) -> str | None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_diagrama_fluxo.py`
**Linhas:** 132–159
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `canonical_key` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `label` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `label` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str | None`. Retornos observados estaticamente incluem: `key`; `'identificados'`; `'duplicatas_removidas'`; `'apos_deduplicacao'`; `'pre_triagem_ia'`; `'enviados_triagem'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `ALIASES.items`
- `norm`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:parse_org_flow_tables`
- `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:recursive_scan_json`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 11
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4eea6a684358943bc75f20322f813921f542f4d534bf3dc23316f01f90138a11`
**LOC do arquivo-fonte:** 592


### `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:recursive_scan_json`

**Assinatura:**
```python
def recursive_scan_json(obj: Any, counts: dict[str, int]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_diagrama_fluxo.py`
**Linhas:** 162–191
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `recursive_scan_json` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `obj` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `obj` recebido pela função; não há descrição individual explícita no código. |
| `counts` | positional-or-keyword | `dict[str, int]` | `obrigatório/ausente` | Parâmetro `counts` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `canonical_key`
- `isinstance`
- `obj.items`
- `obj.values`
- `recursive_scan_json`
- `set_count`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:derive_counts`
- `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:recursive_scan_json`
- `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:scan_json_files`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 7
loops: 5
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4eea6a684358943bc75f20322f813921f542f4d534bf3dc23316f01f90138a11`
**LOC do arquivo-fonte:** 592


### `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:scan_json_files`

**Assinatura:**
```python
def scan_json_files(out_dir: Path, prefix: str, counts: dict[str, int]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_diagrama_fluxo.py`
**Linhas:** 194–200
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `scan_json_files` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `out_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `out_dir` recebido pela função; não há descrição individual explícita no código. |
| `prefix` | positional-or-keyword | `str` | `obrigatório/ausente` | Prefixo canônico usado para compor nomes de artefatos. |
| `counts` | positional-or-keyword | `dict[str, int]` | `obrigatório/ausente` | Parâmetro `counts` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `json.loads`
- `out_dir.glob`
- `p.read_text`
- `recursive_scan_json`
- `sorted`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:derive_counts`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `*.json`

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `4eea6a684358943bc75f20322f813921f542f4d534bf3dc23316f01f90138a11`
**LOC do arquivo-fonte:** 592


### `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:parse_org_flow_tables`

**Assinatura:**
```python
def parse_org_flow_tables(text: str, counts: dict[str, int]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_diagrama_fluxo.py`
**Linhas:** 203–215
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Interpreta a responsabilidade representada por `org_flow_tables` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |
| `counts` | positional-or-keyword | `dict[str, int]` | `obrigatório/ausente` | Parâmetro `counts` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `c.strip`
- `canonical_key`
- `len`
- `line.startswith`
- `line.strip`
- `line.strip('|').split`
- `raw.strip`
- `set`
- `set_count`
- `text.splitlines`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:scan_org_files`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 1
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4eea6a684358943bc75f20322f813921f542f4d534bf3dc23316f01f90138a11`
**LOC do arquivo-fonte:** 592


### `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:scan_org_files`

**Assinatura:**
```python
def scan_org_files(out_dir: Path, prefix: str, counts: dict[str, int], org_path: Path | None = None) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_diagrama_fluxo.py`
**Linhas:** 218–231
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `scan_org_files` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `out_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `out_dir` recebido pela função; não há descrição individual explícita no código. |
| `prefix` | positional-or-keyword | `str` | `obrigatório/ausente` | Prefixo canônico usado para compor nomes de artefatos. |
| `counts` | positional-or-keyword | `dict[str, int]` | `obrigatório/ausente` | Parâmetro `counts` recebido pela função; não há descrição individual explícita no código. |
| `org_path` | positional-or-keyword | `Path \| None` | `None` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `candidates.append`
- `candidates.extend`
- `org_path.exists`
- `out_dir.glob`
- `p.read_text`
- `p.stat`
- `parse_org_flow_tables`
- `seen.add`
- `set`
- `sorted`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:derive_counts`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `*.org`

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `4eea6a684358943bc75f20322f813921f542f4d534bf3dc23316f01f90138a11`
**LOC do arquivo-fonte:** 592


### `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:read_csv_rows`

**Assinatura:**
```python
def read_csv_rows(path: Path) -> list[dict[str, str]]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_diagrama_fluxo.py`
**Linhas:** 234–242
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Lê a responsabilidade representada por `csv_rows` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `list[dict[str, str]]`. Retornos observados estaticamente incluem: `[dict(r) for r in reader]`; `[]`.

#### Efeitos colaterais e fronteiras externas

- Abre arquivo para leitura ou inspeção.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `csv.DictReader`
- `dict`
- `path.open`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:derive_from_triage_csv`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 1
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `4eea6a684358943bc75f20322f813921f542f4d534bf3dc23316f01f90138a11`
**LOC do arquivo-fonte:** 592


### `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:find_triage_csv`

**Assinatura:**
```python
def find_triage_csv(out_dir: Path, prefix: str) -> Path | None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_diagrama_fluxo.py`
**Linhas:** 245–255
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Localiza a responsabilidade representada por `triage_csv` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `out_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `out_dir` recebido pela função; não há descrição individual explícita no código. |
| `prefix` | positional-or-keyword | `str` | `obrigatório/ausente` | Prefixo canônico usado para compor nomes de artefatos. |

#### Retorno

Anotação declarada: `Path | None`. Retornos observados estaticamente incluem: `p`; `hits[0] if hits else None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `out_dir.glob`
- `p.exists`
- `p.stat`
- `sorted`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:derive_from_triage_csv`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.triagem_humana.csv`
- `.referencias_incluidas_seminario.csv`
- `.triagem_titulo_resumo.csv`
- `*triagem_humana*.csv`

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4eea6a684358943bc75f20322f813921f542f4d534bf3dc23316f01f90138a11`
**LOC do arquivo-fonte:** 592


### `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:upper`

**Assinatura:**
```python
def upper(row: dict[str, str], key: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_diagrama_fluxo.py`
**Linhas:** 258–259
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `upper` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `row` | positional-or-keyword | `dict[str, str]` | `obrigatório/ausente` | Parâmetro `row` recebido pela função; não há descrição individual explícita no código. |
| `key` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `key` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `str(row.get(key, '') or '').strip().upper()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `row.get`
- `str`
- `str(row.get(key, '') or '').strip`
- `str(row.get(key, '') or '').strip().upper`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.article_workflow.validators:ArticleWorkflow.format_status`
- `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:_author_surname`
- `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:_reference_line`
- `app_bundle.scripts.pipeline.institution_compliance:_check_org`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_decision`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_pretriage_sort_key`
- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:abnt_ref`
- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:main`
- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:manual_reexport`
- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:write_summary`
- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:write_xlsx`
- `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:derive_from_triage_csv`
- `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:upper`
- `app_bundle.scripts.pipeline.render_docx:render_docx`
- `app_bundle.scripts.pipeline.render_docx_canonico:add_cover`
- `app_bundle.scripts.pipeline.render_docx_canonico:author_abnt`
- `app_bundle.scripts.pipeline.render_docx_canonico:authors_abnt`
- `app_bundle.scripts.pipeline.render_org_latex:render_dissertation_front_matter`
- `gerar_artigo_final_unificado:_author_surname`
- `gerar_artigo_final_unificado:_reference_line`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4eea6a684358943bc75f20322f813921f542f4d534bf3dc23316f01f90138a11`
**LOC do arquivo-fonte:** 592


### `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:derive_from_triage_csv`

**Assinatura:**
```python
def derive_from_triage_csv(out_dir: Path, prefix: str, counts: dict[str, int]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_diagrama_fluxo.py`
**Linhas:** 262–309
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `derive_from_triage_csv` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `out_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `out_dir` recebido pela função; não há descrição individual explícita no código. |
| `prefix` | positional-or-keyword | `str` | `obrigatório/ausente` | Prefixo canônico usado para compor nomes de artefatos. |
| `counts` | positional-or-keyword | `dict[str, int]` | `obrigatório/ausente` | Parâmetro `counts` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `find_triage_csv`
- `len`
- `max`
- `read_csv_rows`
- `set_count`
- `upper`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:derive_counts`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 8
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4eea6a684358943bc75f20322f813921f542f4d534bf3dc23316f01f90138a11`
**LOC do arquivo-fonte:** 592


### `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:derive_counts`

**Assinatura:**
```python
def derive_counts(out_dir: Path, prefix: str = PREFIX_DEFAULT, org_path: Path | None = None, prisma_payload: dict[str, Any] | None = None) -> dict[str, int]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_diagrama_fluxo.py`
**Linhas:** 312–340
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `derive_counts` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `out_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `out_dir` recebido pela função; não há descrição individual explícita no código. |
| `prefix` | positional-or-keyword | `str` | `PREFIX_DEFAULT` | Prefixo canônico usado para compor nomes de artefatos. |
| `org_path` | positional-or-keyword | `Path \| None` | `None` | Caminho de arquivo ou diretório utilizado pela operação. |
| `prisma_payload` | positional-or-keyword | `dict[str, Any] \| None` | `None` | Payload estruturado recebido ou produzido pela operação. |

#### Retorno

Anotação declarada: `dict[str, int]`. Retornos observados estaticamente incluem: `counts`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `derive_from_triage_csv`
- `recursive_scan_json`
- `scan_json_files`
- `scan_org_files`
- `set_count`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:ensure_prisma_flow_diagram`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4eea6a684358943bc75f20322f813921f542f4d534bf3dc23316f01f90138a11`
**LOC do arquivo-fonte:** 592


### `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:fmt_n`

**Assinatura:**
```python
def fmt_n(counts: dict[str, int], key: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_diagrama_fluxo.py`
**Linhas:** 343–344
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `fmt_n` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `counts` | positional-or-keyword | `dict[str, int]` | `obrigatório/ausente` | Parâmetro `counts` recebido pela função; não há descrição individual explícita no código. |
| `key` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `key` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `str(counts[key]) if key in counts else '—'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:build_flow_table`
- `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:draw_diagram_png`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4eea6a684358943bc75f20322f813921f542f4d534bf3dc23316f01f90138a11`
**LOC do arquivo-fonte:** 592


### `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:draw_diagram_png`

**Assinatura:**
```python
def draw_diagram_png(counts: dict[str, int], png_path: Path) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_diagrama_fluxo.py`
**Linhas:** 347–453
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `draw_diagram_png` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `counts` | positional-or-keyword | `dict[str, int]` | `obrigatório/ausente` | Parâmetro `counts` recebido pela função; não há descrição individual explícita no código. |
| `png_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `RuntimeError`
- `arrow`
- `ax.axis`
- `ax.set_xlim`
- `ax.set_ylim`
- `ax.text`
- `box`
- `fig.savefig`
- `fig.tight_layout`
- `fmt_n`
- `max`
- `plt.close`
- `plt.subplots`
- `png_path.parent.mkdir`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:ensure_prisma_flow_diagram`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError(f'matplotlib não disponível para gerar PNG: {exc}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `4eea6a684358943bc75f20322f813921f542f4d534bf3dc23316f01f90138a11`
**LOC do arquivo-fonte:** 592


### `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:draw_diagram_png.box`

**Assinatura:**
```python
def box(x, y, w, h, title, body = '', fontsize = 8.1, face = '#F8FAFD', edge = '#3B5F8A', lw = 1.0)
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_diagrama_fluxo.py`
**Linhas:** 361–376
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `box` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `x` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `x` recebido pela função; não há descrição individual explícita no código. |
| `y` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `y` recebido pela função; não há descrição individual explícita no código. |
| `w` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `w` recebido pela função; não há descrição individual explícita no código. |
| `h` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `h` recebido pela função; não há descrição individual explícita no código. |
| `title` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `title` recebido pela função; não há descrição individual explícita no código. |
| `body` | positional-or-keyword | `não anotado` | `''` | Parâmetro `body` recebido pela função; não há descrição individual explícita no código. |
| `fontsize` | positional-or-keyword | `não anotado` | `8.1` | Parâmetro `fontsize` recebido pela função; não há descrição individual explícita no código. |
| `face` | positional-or-keyword | `não anotado` | `'#F8FAFD'` | Parâmetro `face` recebido pela função; não há descrição individual explícita no código. |
| `edge` | positional-or-keyword | `não anotado` | `'#3B5F8A'` | Parâmetro `edge` recebido pela função; não há descrição individual explícita no código. |
| `lw` | positional-or-keyword | `não anotado` | `1.0` | Parâmetro `lw` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `FancyBboxPatch`
- `ax.add_patch`
- `ax.text`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:draw_diagram_png`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4eea6a684358943bc75f20322f813921f542f4d534bf3dc23316f01f90138a11`
**LOC do arquivo-fonte:** 592


### `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:draw_diagram_png.arrow`

**Assinatura:**
```python
def arrow(x1, y1, x2, y2)
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_diagrama_fluxo.py`
**Linhas:** 378–385
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `arrow` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `x1` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `x1` recebido pela função; não há descrição individual explícita no código. |
| `y1` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `y1` recebido pela função; não há descrição individual explícita no código. |
| `x2` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `x2` recebido pela função; não há descrição individual explícita no código. |
| `y2` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `y2` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `FancyArrowPatch`
- `ax.add_patch`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:draw_diagram_png`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4eea6a684358943bc75f20322f813921f542f4d534bf3dc23316f01f90138a11`
**LOC do arquivo-fonte:** 592


### `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:build_flow_table`

**Assinatura:**
```python
def build_flow_table(counts: dict[str, int]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_diagrama_fluxo.py`
**Linhas:** 455–472
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Constrói a responsabilidade representada por `flow_table` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `counts` | positional-or-keyword | `dict[str, int]` | `obrigatório/ausente` | Parâmetro `counts` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'\n'.join(lines)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `fmt_n`
- `lines.append`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:org_prisma_section`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4eea6a684358943bc75f20322f813921f542f4d534bf3dc23316f01f90138a11`
**LOC do arquivo-fonte:** 592


### `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:org_prisma_section`

**Assinatura:**
```python
def org_prisma_section(counts: dict[str, int], image_filename: str, level: str = '*') -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_diagrama_fluxo.py`
**Linhas:** 475–485
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `org_prisma_section` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `counts` | positional-or-keyword | `dict[str, int]` | `obrigatório/ausente` | Parâmetro `counts` recebido pela função; não há descrição individual explícita no código. |
| `image_filename` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `image_filename` recebido pela função; não há descrição individual explícita no código. |
| `level` | positional-or-keyword | `str` | `'*'` | Parâmetro `level` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `f'{level} Fluxo de seleção\n\n#+CAPTION: Diagrama PRISMA adaptado ao fluxo de busca, pré-triagem por IA e triagem humana.\n#+ATTR_LATEX: :width 0.95\\textwidth\n[[file:{image_filename}]]\n\n{build_flow_table(counts)}\n\nA figura apresenta o funil de seleção da revisão. As contagens finais de triagem humana são derivadas do arquivo =triagem_humana.csv=; quando contagens intermediárias antigas entram em conflito com a planilha final, prevalece a reconciliação pela decisão humana final.\n'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `build_flow_table`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:inject_section`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4eea6a684358943bc75f20322f813921f542f4d534bf3dc23316f01f90138a11`
**LOC do arquivo-fonte:** 592


### `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:inject_section`

**Assinatura:**
```python
def inject_section(org_path: Path, counts: dict[str, int], image_path: Path) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_diagrama_fluxo.py`
**Linhas:** 488–526
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Injeta a responsabilidade representada por `section` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `org_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `counts` | positional-or-keyword | `dict[str, int]` | `obrigatório/ausente` | Parâmetro `counts` recebido pela função; não há descrição individual explícita no código. |
| `image_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `changed`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `enumerate`
- `len`
- `line.startswith`
- `lines.insert`
- `m.end`
- `m.group`
- `m.start`
- `m2.group`
- `m2.start`
- `next_heading.start`
- `org_path.read_text`
- `org_path.write_text`
- `org_prisma_section`
- `re.escape`
- `re.search`
- `text.endswith`
- `text.rstrip`
- `text.splitlines`
- `text[end:].lstrip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:ensure_prisma_flow_diagram`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 8
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4eea6a684358943bc75f20322f813921f542f4d534bf3dc23316f01f90138a11`
**LOC do arquivo-fonte:** 592


### `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:find_org`

**Assinatura:**
```python
def find_org(out_dir: Path, prefix: str, explicit: str | None = None) -> Path | None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_diagrama_fluxo.py`
**Linhas:** 529–542
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Localiza a responsabilidade representada por `org` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `out_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `out_dir` recebido pela função; não há descrição individual explícita no código. |
| `prefix` | positional-or-keyword | `str` | `obrigatório/ausente` | Prefixo canônico usado para compor nomes de artefatos. |
| `explicit` | positional-or-keyword | `str \| None` | `None` | Parâmetro `explicit` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path | None`. Retornos observados estaticamente incluem: `p`; `candidates[0] if candidates else None`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `out_dir.glob`
- `p.exists`
- `p.read_text`
- `p.stat`
- `sorted`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:ensure_prisma_flow_diagram`
- `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `*.org`

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 1
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `4eea6a684358943bc75f20322f813921f542f4d534bf3dc23316f01f90138a11`
**LOC do arquivo-fonte:** 592


### `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:ensure_prisma_flow_diagram`

**Assinatura:**
```python
def ensure_prisma_flow_diagram(cfg: dict[str, Any] | None = None, out_dir: str | Path | None = None, prefix: str = PREFIX_DEFAULT, org_path: str | Path | None = None, prisma_payload: dict[str, Any] | None = None, phase: str | None = None) -> Path | None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_diagrama_fluxo.py`
**Linhas:** 545–568
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Garante a responsabilidade representada por `prisma_flow_diagram` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any] \| None` | `None` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `out_dir` | positional-or-keyword | `str \| Path \| None` | `None` | Parâmetro `out_dir` recebido pela função; não há descrição individual explícita no código. |
| `prefix` | positional-or-keyword | `str` | `PREFIX_DEFAULT` | Prefixo canônico usado para compor nomes de artefatos. |
| `org_path` | positional-or-keyword | `str \| Path \| None` | `None` | Caminho de arquivo ou diretório utilizado pela operação. |
| `prisma_payload` | positional-or-keyword | `dict[str, Any] \| None` | `None` | Payload estruturado recebido ou produzido pela operação. |
| `phase` | positional-or-keyword | `str \| None` | `None` | Parâmetro `phase` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path | None`. Retornos observados estaticamente incluem: `None`; `image`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `counts_path.write_text`
- `derive_counts`
- `draw_diagram_png`
- `find_org`
- `inject_section`
- `json.dumps`
- `log`
- `org.exists`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.prisma_generic_orchestration:_ap003e_body_render_external_prisma_outputs_1`
- `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.diagrama_prisma.png`
- `.diagrama_prisma_contagens.json`

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4eea6a684358943bc75f20322f813921f542f4d534bf3dc23316f01f90138a11`
**LOC do arquivo-fonte:** 592


### `app_bundle.scripts.pipeline.prisma_diagrama_fluxo:main`

**Assinatura:**
```python
def main() -> int
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_diagrama_fluxo.py`
**Linhas:** 571–584
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa o ponto de entrada correspondente e coordena o fluxo associado.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `1`; `0`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `argparse.ArgumentParser`
- `ensure_prisma_flow_diagram`
- `find_org`
- `log`
- `parser.add_argument`
- `parser.parse_args`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `--org`
- `--out-dir`
- `--prefix`

#### Artefatos/caminhos literais reconhecidos no corpo

- `Nenhum arquivo .org encontrado em `

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `4eea6a684358943bc75f20322f813921f542f4d534bf3dc23316f01f90138a11`
**LOC do arquivo-fonte:** 592


### `app_bundle.scripts.pipeline.prisma_exportar_bib:norm`

**Assinatura:**
```python
def norm(v)
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_exportar_bib.py`
**Linhas:** 8–11
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `norm` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `v` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `v` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `re.sub('[^a-z0-9]+', '_', ''.join((ch for ch in s if not unicodedata.combining(ch)))).strip('_')`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `''.join`
- `re.sub`
- `re.sub('[^a-z0-9]+', '_', ''.join((ch for ch in s if not unicodedata.combining(ch)))).strip`
- `str`
- `str(v or '').strip`
- `str(v or '').strip().lower`
- `unicodedata.combining`
- `unicodedata.normalize`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `cb74100755f65effd874fc02e32eaf373f73dc8daa1ad103078594a56eeb7c81`
**LOC do arquivo-fonte:** 126


### `app_bundle.scripts.pipeline.prisma_exportar_bib:get`

**Assinatura:**
```python
def get(row, *names)
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_exportar_bib.py`
**Linhas:** 13–18
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `get` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `row` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `row` recebido pela função; não há descrição individual explícita no código. |
| `names` | var-positional | `não anotado` | `obrigatório/ausente` | Parâmetro `names` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `str(v).strip()`; `''`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `d.get`
- `norm`
- `row.items`
- `str`
- `str(v).strip`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.command_dispatch:dispatch_stage_006`
- `academic_pipeline.command_dispatch:dispatch_stage_011`
- `academic_pipeline.command_dispatch:dispatch_stage_015`
- `academic_pipeline.command_dispatch:dispatch_stage_016`
- `academic_pipeline.command_dispatch:dispatch_stage_017`
- `academic_pipeline.command_dispatch:dispatch_stage_019`
- `academic_pipeline.default_runtime:_ap003f_pipeline_core`
- `academic_pipeline.default_runtime:_openai_model_from_cfg`
- `academic_pipeline.default_runtime:work_cache_paths`
- `academic_pipeline.doctor_runtime:_section`
- `academic_pipeline.doctor_runtime:run_doctor_command`
- `academic_pipeline.document_orchestration:_refs_apply_runtime_policy_impl`
- `academic_pipeline.document_orchestration:_refs_disabled_impl`
- `academic_pipeline.document_orchestration:_resolve_latex_paths_for_recompile_impl`
- `academic_pipeline.document_orchestration:output_paths_impl`
- `academic_pipeline.document_orchestration:render_additional_language_versions_impl`
- `academic_pipeline.document_orchestration:run_document_stage_001`
- `academic_pipeline.document_orchestration:run_document_stage_002`
- `academic_pipeline.document_orchestration:run_document_stage_003`
- `academic_pipeline.document_orchestration:run_document_stage_006`
- `academic_pipeline.document_orchestration:run_document_stage_007`
- `academic_pipeline.document_orchestration:run_document_stage_012`
- `academic_pipeline.document_orchestration:run_recompile_impl`
- `academic_pipeline.prisma_generic_orchestration:_ap003e_body__section_1`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `cb74100755f65effd874fc02e32eaf373f73dc8daa1ad103078594a56eeb7c81`
**LOC do arquivo-fonte:** 126


### `app_bundle.scripts.pipeline.prisma_exportar_bib:dialect`

**Assinatura:**
```python
def dialect(path)
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_exportar_bib.py`
**Linhas:** 20–23
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `dialect` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `csv.Sniffer().sniff(sample, delimiters=',;\t')`; `csv.excel`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `csv.Sniffer`
- `csv.Sniffer().sniff`
- `path.read_text`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_exportar_bib:read_rows`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `cb74100755f65effd874fc02e32eaf373f73dc8daa1ad103078594a56eeb7c81`
**LOC do arquivo-fonte:** 126


### `app_bundle.scripts.pipeline.prisma_exportar_bib:read_rows`

**Assinatura:**
```python
def read_rows(path)
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_exportar_bib.py`
**Linhas:** 25–27
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Lê a responsabilidade representada por `rows` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `list(csv.DictReader(f, dialect=dialect(path)))`.

#### Efeitos colaterais e fronteiras externas

- Abre arquivo para leitura ou inspeção.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `csv.DictReader`
- `dialect`
- `list`
- `path.open`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_exportar_bib:export_bib`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `cb74100755f65effd874fc02e32eaf373f73dc8daa1ad103078594a56eeb7c81`
**LOC do arquivo-fonte:** 126


### `app_bundle.scripts.pipeline.prisma_exportar_bib:included`

**Assinatura:**
```python
def included(row)
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_exportar_bib.py`
**Linhas:** 29–32
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `included` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `row` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `row` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `True`; `norm(get(row, 'decisao_titulo_resumo', 'decisão_título_resumo')) == 'incluir' and norm(get(row, 'decisao_texto_completo', 'decisão_texto_completo')) in {'incluir', 'incluido', 'sim'}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `get`
- `norm`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_exportar_bib:export_bib`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `cb74100755f65effd874fc02e32eaf373f73dc8daa1ad103078594a56eeb7c81`
**LOC do arquivo-fonte:** 126


### `app_bundle.scripts.pipeline.prisma_exportar_bib:year_from`

**Assinatura:**
```python
def year_from(row)
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_exportar_bib.py`
**Linhas:** 34–37
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `year_from` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `row` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `row` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `m.group(0) if m else ''`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `get`
- `m.group`
- `map`
- `re.search`
- `row.values`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_exportar_bib:row_entry`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `cb74100755f65effd874fc02e32eaf373f73dc8daa1ad103078594a56eeb7c81`
**LOC do arquivo-fonte:** 126


### `app_bundle.scripts.pipeline.prisma_exportar_bib:bib_escape`

**Assinatura:**
```python
def bib_escape(s)
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_exportar_bib.py`
**Linhas:** 39–43
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `bib_escape` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `s` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `s` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `s`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `s.replace`
- `str`
- `str(s or '').strip`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `cb74100755f65effd874fc02e32eaf373f73dc8daa1ad103078594a56eeb7c81`
**LOC do arquivo-fonte:** 126


### `app_bundle.scripts.pipeline.prisma_exportar_bib:split_authors`

**Assinatura:**
```python
def split_authors(s)
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_exportar_bib.py`
**Linhas:** 45–50
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `split_authors` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `s` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `s` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `''`; `' and '.join(parts) if parts else s`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `' and '.join`
- `p.strip`
- `re.split`
- `str`
- `str(s or '').strip`
- `str(s or '').strip().strip`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `cb74100755f65effd874fc02e32eaf373f73dc8daa1ad103078594a56eeb7c81`
**LOC do arquivo-fonte:** 126


### `app_bundle.scripts.pipeline.prisma_exportar_bib:make_key`

**Assinatura:**
```python
def make_key(authors, year, title, used)
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_exportar_bib.py`
**Linhas:** 52–65
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Cria a responsabilidade representada por `key` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `authors` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `authors` recebido pela função; não há descrição individual explícita no código. |
| `year` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `year` recebido pela função; não há descrição individual explícita no código. |
| `title` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `title` recebido pela função; não há descrição individual explícita no código. |
| `used` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `used` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `key`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `len`
- `norm`
- `norm(f'{base}{year}{word}').replace`
- `re.findall`
- `re.split`
- `tok.lower`
- `used.add`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_exportar_bib:row_entry`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `cb74100755f65effd874fc02e32eaf373f73dc8daa1ad103078594a56eeb7c81`
**LOC do arquivo-fonte:** 126


### `app_bundle.scripts.pipeline.prisma_exportar_bib:choose_input`

**Assinatura:**
```python
def choose_input(out_dir, prefix)
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_exportar_bib.py`
**Linhas:** 67–72
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `choose_input` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `out_dir` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `out_dir` recebido pela função; não há descrição individual explícita no código. |
| `prefix` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Prefixo canônico usado para compor nomes de artefatos. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `(a, False)`; `(b, True)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `FileNotFoundError`
- `a.exists`
- `b.exists`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_exportar_bib:export_bib`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `FileNotFoundError(f'não encontrei {a} nem {b}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.referencias_incluidas_seminario.csv`
- `.triagem_humana.csv`

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `cb74100755f65effd874fc02e32eaf373f73dc8daa1ad103078594a56eeb7c81`
**LOC do arquivo-fonte:** 126


### `app_bundle.scripts.pipeline.prisma_exportar_bib:row_entry`

**Assinatura:**
```python
def row_entry(row, used)
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_exportar_bib.py`
**Linhas:** 74–92
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `row_entry` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `row` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `row` recebido pela função; não há descrição individual explícita no código. |
| `used` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `used` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `None`; `f'@{entry_type}{{{key},\n{body}\n}}'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `',\n'.join`
- `bib_escape`
- `fields.append`
- `get`
- `make_key`
- `split_authors`
- `year_from`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_exportar_bib:export_bib`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 8
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `cb74100755f65effd874fc02e32eaf373f73dc8daa1ad103078594a56eeb7c81`
**LOC do arquivo-fonte:** 126


### `app_bundle.scripts.pipeline.prisma_exportar_bib:export_bib`

**Assinatura:**
```python
def export_bib(out_dir, prefix, input_csv = None, output_bib = None, filter_included = None)
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_exportar_bib.py`
**Linhas:** 94–115
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Exporta a responsabilidade representada por `bib` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `out_dir` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `out_dir` recebido pela função; não há descrição individual explícita no código. |
| `prefix` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Prefixo canônico usado para compor nomes de artefatos. |
| `input_csv` | positional-or-keyword | `não anotado` | `None` | Parâmetro `input_csv` recebido pela função; não há descrição individual explícita no código. |
| `output_bib` | positional-or-keyword | `não anotado` | `None` | Parâmetro `output_bib` recebido pela função; não há descrição individual explícita no código. |
| `filter_included` | positional-or-keyword | `não anotado` | `None` | Parâmetro `filter_included` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `output_bib`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `'\n\n'.join`
- `Path`
- `Path(input_csv).resolve`
- `Path(out_dir).resolve`
- `Path(output_bib).resolve`
- `RuntimeError`
- `choose_input`
- `entries.append`
- `included`
- `len`
- `output_bib.parent.mkdir`
- `output_bib.write_text`
- `print`
- `read_rows`
- `row_entry`
- `set`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_exportar_bib:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError(f'nenhuma referência incluída encontrada em {input_csv}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.referencias_incluidas.bib`

#### Complexidade estrutural observada

```yaml
branches: 7
loops: 1
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `cb74100755f65effd874fc02e32eaf373f73dc8daa1ad103078594a56eeb7c81`
**LOC do arquivo-fonte:** 126


### `app_bundle.scripts.pipeline.prisma_exportar_bib:main`

**Assinatura:**
```python
def main(argv = None)
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_exportar_bib.py`
**Linhas:** 117–125
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa o ponto de entrada correspondente e coordena o fluxo associado.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `não anotado` | `None` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `0`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `argparse.ArgumentParser`
- `export_bib`
- `p.add_argument`
- `p.parse_args`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `--filtrar-incluidas`
- `--input`
- `--out-dir`
- `--output`
- `--prefix`

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `cb74100755f65effd874fc02e32eaf373f73dc8daa1ad103078594a56eeb7c81`
**LOC do arquivo-fonte:** 126


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:norm_ascii`

**Assinatura:**
```python
def norm_ascii(v: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 25–28
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `norm_ascii` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `v` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `v` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `''.join((ch for ch in s if not unicodedata.combining(ch)))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `''.join`
- `str`
- `unicodedata.combining`
- `unicodedata.normalize`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:norm_col`

**Assinatura:**
```python
def norm_col(v: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 31–32
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `norm_col` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `v` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `v` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `re.sub('[^a-z0-9]+', '_', norm_ascii(str(v or '').lower())).strip('_')`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `norm_ascii`
- `re.sub`
- `re.sub('[^a-z0-9]+', '_', norm_ascii(str(v or '').lower())).strip`
- `str`
- `str(v or '').lower`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:slug`

**Assinatura:**
```python
def slug(v: str, max_len: int = 120) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 35–37
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `slug` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `v` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `v` recebido pela função; não há descrição individual explícita no código. |
| `max_len` | positional-or-keyword | `int` | `120` | Parâmetro `max_len` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `re.sub('_+', '_', s)[:max_len].strip('_') or 'artigo'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `norm_ascii`
- `norm_ascii(v or '').lower`
- `re.sub`
- `re.sub('[^a-zA-Z0-9]+', '_', norm_ascii(v or '').lower()).strip`
- `re.sub('_+', '_', s)[:max_len].strip`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:sha256_file`

**Assinatura:**
```python
def sha256_file(path: Path) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 40–45
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `sha256_file` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `h.hexdigest()`.

#### Efeitos colaterais e fronteiras externas

- Abre arquivo para leitura ou inspeção.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `f.read`
- `h.hexdigest`
- `h.update`
- `hashlib.sha256`
- `iter`
- `path.open`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:sniff_csv`

**Assinatura:**
```python
def sniff_csv(path: Path)
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 48–53
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `sniff_csv` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `csv.Sniffer().sniff(sample, delimiters=',;\t')`; `csv.excel`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `csv.Sniffer`
- `csv.Sniffer().sniff`
- `path.read_text`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:read_csv`

**Assinatura:**
```python
def read_csv(path: Path) -> list[dict[str, str]]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 56–58
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Lê a responsabilidade representada por `csv` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `list[dict[str, str]]`. Retornos observados estaticamente incluem: `list(csv.DictReader(f, dialect=sniff_csv(path)))`.

#### Efeitos colaterais e fronteiras externas

- Abre arquivo para leitura ou inspeção.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `csv.DictReader`
- `list`
- `path.open`
- `sniff_csv`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:write_csv`

**Assinatura:**
```python
def write_csv(path: Path, rows: list[dict[str, Any]], fieldnames: list[str] | None = None) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 61–73
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Grava a responsabilidade representada por `csv` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `rows` | positional-or-keyword | `list[dict[str, Any]]` | `obrigatório/ausente` | Parâmetro `rows` recebido pela função; não há descrição individual explícita no código. |
| `fieldnames` | positional-or-keyword | `list[str] \| None` | `None` | Parâmetro `fieldnames` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Abre arquivo para leitura ou inspeção.
- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `csv.DictWriter`
- `keys.append`
- `path.open`
- `path.parent.mkdir`
- `w.writeheader`
- `w.writerows`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:pick`

**Assinatura:**
```python
def pick(row: dict[str, Any], *names: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 76–82
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `pick` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `row` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `row` recebido pela função; não há descrição individual explícita no código. |
| `names` | var-positional | `str` | `obrigatório/ausente` | Parâmetro `names` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `str(v).strip()`; `''`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `d.get`
- `norm_col`
- `row.items`
- `str`
- `str(v).strip`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:doi_from_text`

**Assinatura:**
```python
def doi_from_text(text: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 85–89
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `doi_from_text` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `''`; `m.group(0).strip().rstrip('.,;)').replace('\\_', '_').replace('%5C_', '_')`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `m.group`
- `m.group(0).strip`
- `m.group(0).strip().rstrip`
- `m.group(0).strip().rstrip('.,;)').replace`
- `m.group(0).strip().rstrip('.,;)').replace('\\_', '_').replace`
- `re.search`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:normalize_doi`

**Assinatura:**
```python
def normalize_doi(v: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 92–96
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Normaliza a responsabilidade representada por `doi` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `v` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `v` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `doi_from_text(v) or v`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `doi_from_text`
- `re.sub`
- `str`
- `str(v or '').strip`
- `v.replace`
- `v.replace('\\_', '_').replace`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `^https?://(dx\.)?doi\.org/`

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:read_env`

**Assinatura:**
```python
def read_env(root: Path) -> dict[str, str]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 99–110
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Lê a responsabilidade representada por `env` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `root` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `root` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, str]`. Retornos observados estaticamente incluem: `env`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `k.strip`
- `line.split`
- `line.startswith`
- `line.strip`
- `p.exists`
- `p.read_text`
- `p.read_text(encoding='utf-8', errors='ignore').splitlines`
- `v.strip`
- `v.strip().strip`
- `v.strip().strip('"').strip`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:read_toml`

**Assinatura:**
```python
def read_toml(path: Path | None) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 113–117
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Lê a responsabilidade representada por `toml` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path \| None` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `{}`; `tomllib.load(f)`.

#### Efeitos colaterais e fronteiras externas

- Abre arquivo para leitura ou inspeção.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `path.exists`
- `path.open`
- `tomllib.load`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:resolve_relative`

**Assinatura:**
```python
def resolve_relative(base_file: Path, maybe_path: str) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 120–124
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Resolve a responsabilidade representada por `relative` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `base_file` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `base_file` recebido pela função; não há descrição individual explícita no código. |
| `maybe_path` | positional-or-keyword | `str` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `p.resolve()`; `(base_file.parent / p).resolve()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `(base_file.parent / p).resolve`
- `Path`
- `Path(str(maybe_path)).expanduser`
- `p.is_absolute`
- `p.resolve`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:dados_pesquisa_path_from_config`

**Assinatura:**
```python
def dados_pesquisa_path_from_config(config: Path | None) -> Path | None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 127–138
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `dados_pesquisa_path_from_config` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `config` | positional-or-keyword | `Path \| None` | `obrigatório/ausente` | Configuração ou estrutura de configuração recebida pela rotina. |

#### Retorno

Anotação declarada: `Path | None`. Retornos observados estaticamente incluem: `None`; `resolve_relative(config, str(candidate))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `config.exists`
- `d.get`
- `d.get('busca_prisma', {}).get`
- `d.get('pesquisa', {}).get`
- `read_toml`
- `resolve_relative`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:flatten_text`

**Assinatura:**
```python
def flatten_text(obj: Any) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 141–148
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `flatten_text` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `obj` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `obj` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `''`; `' '.join((flatten_text(v) for v in obj.values()))`; `' '.join((flatten_text(x) for x in obj))`; `str(obj)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `flatten_text`
- `isinstance`
- `obj.values`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:research_context`

**Assinatura:**
```python
def research_context(prisma_config: Path | None, dados_pesquisa_path: Path | None) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 151–161
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `research_context` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `prisma_config` | positional-or-keyword | `Path \| None` | `obrigatório/ausente` | Configuração ou estrutura de configuração recebida pela rotina. |
| `dados_pesquisa_path` | positional-or-keyword | `Path \| None` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `' '.join(pieces)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `cfg.get`
- `dados_pesquisa_path_from_config`
- `flatten_text`
- `meta_path.exists`
- `pieces.append`
- `prisma_config.exists`
- `read_toml`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:find_pool_csvs`

**Assinatura:**
```python
def find_pool_csvs(prisma_out_dir: Path, extra_csvs: list[Path]) -> list[Path]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 164–182
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Localiza a responsabilidade representada por `pool_csvs` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `prisma_out_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `prisma_out_dir` recebido pela função; não há descrição individual explícita no código. |
| `extra_csvs` | positional-or-keyword | `list[Path]` | `obrigatório/ausente` | Parâmetro `extra_csvs` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[Path]`. Retornos observados estaticamente incluem: `found`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `found.append`
- `p.exists`
- `p.resolve`
- `prisma_out_dir.glob`
- `sorted`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:load_candidates`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `*.curadoria_ia_referencias.csv`
- `*curadoria*referencias*.csv`
- `*.triagem_humana.csv`
- `*.triagem_titulo_resumo.csv`
- `*triagem*.csv`
- `*referencias_incluidas_seminario.csv`
- `*referencias_incluidas*.csv`

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 3
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:text_tokens`

**Assinatura:**
```python
def text_tokens(text: str) -> set[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 185–194
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `text_tokens` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `set[str]`. Retornos observados estaticamente incluem: `{w for w in words if w not in stop}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `norm_ascii`
- `norm_ascii(text or '').lower`
- `re.findall`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:candidate_score`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:parse_numeric_score`

**Assinatura:**
```python
def parse_numeric_score(v: str) -> float | None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 197–207
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Interpreta a responsabilidade representada por `numeric_score` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `v` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `v` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `float | None`. Retornos observados estaticamente incluem: `None`; `max(0.0, min(1.0, val))`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `float`
- `m.group`
- `max`
- `min`
- `re.search`
- `str`
- `str(v).strip`
- `str(v).strip().replace`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:candidate_score`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:status_boost`

**Assinatura:**
```python
def status_boost(row: dict[str, Any]) -> float
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 210–218
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `status_boost` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `row` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `row` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `float`. Retornos observados estaticamente incluem: `0.18`; `0.08`; `-0.1`; `0.0`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `' '.join((str(v) for v in row.values())).lower`
- `any`
- `row.values`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:candidate_score`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 4
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:is_priority_title`

**Assinatura:**
```python
def is_priority_title(title: str, regexes: list[str]) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 221–232
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Verifica se `priority_title` conforme as regras codificadas na rotina.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `title` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `title` recebido pela função; não há descrição individual explícita no código. |
| `regexes` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `regexes` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `True`; `False`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `norm_ascii`
- `norm_ascii(title or '').lower`
- `pat.lower`
- `re.search`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:load_candidates`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:candidate_score`

**Assinatura:**
```python
def candidate_score(row: dict[str, Any], title: str, abstract: str, research_ctx: str, priority: bool) -> float
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 235–253
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `candidate_score` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `row` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `row` recebido pela função; não há descrição individual explícita no código. |
| `title` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `title` recebido pela função; não há descrição individual explícita no código. |
| `abstract` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `abstract` recebido pela função; não há descrição individual explícita no código. |
| `research_ctx` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `research_ctx` recebido pela função; não há descrição individual explícita no código. |
| `priority` | positional-or-keyword | `bool` | `obrigatório/ausente` | Parâmetro `priority` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `float`. Retornos observados estaticamente incluem: `round(max(0.0, min(1.0, final)), 4)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `flatten_text`
- `len`
- `max`
- `min`
- `parse_numeric_score`
- `pick`
- `round`
- `status_boost`
- `text_tokens`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:load_candidates`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:dedupe_key`

**Assinatura:**
```python
def dedupe_key(title: str, doi: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 256–260
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `dedupe_key` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `title` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `title` recebido pela função; não há descrição individual explícita no código. |
| `doi` | positional-or-keyword | `str` | `obrigatório/ausente` | Digital Object Identifier da referência bibliográfica. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'doi:' + normalize_doi(doi).lower()`; `'title:' + title_norm[:160]`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `norm_ascii`
- `norm_ascii(title).lower`
- `normalize_doi`
- `normalize_doi(doi).lower`
- `re.sub`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:load_candidates`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:load_candidates`

**Assinatura:**
```python
def load_candidates(prisma_out_dir: Path, extra_csvs: list[Path], research_ctx: str, priority_regexes: list[str]) -> list[Candidate]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 280–328
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Carrega a responsabilidade representada por `candidates` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `prisma_out_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `prisma_out_dir` recebido pela função; não há descrição individual explícita no código. |
| `extra_csvs` | positional-or-keyword | `list[Path]` | `obrigatório/ausente` | Parâmetro `extra_csvs` recebido pela função; não há descrição individual explícita no código. |
| `research_ctx` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `research_ctx` recebido pela função; não há descrição individual explícita no código. |
| `priority_regexes` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `priority_regexes` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[Candidate]`. Retornos observados estaticamente incluem: `rows_all`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `Candidate`
- `c.title.lower`
- `candidate_score`
- `dedupe_key`
- `doi_from_text`
- `enumerate`
- `find_pool_csvs`
- `is_priority_title`
- `json.dumps`
- `len`
- `normalize_doi`
- `pick`
- `print`
- `read_csv`
- `row.values`
- `rows_all.append`
- `rows_all.sort`
- `seen.add`
- `set`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 3
comprehensions: 1
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:fetch_bytes`

**Assinatura:**
```python
def fetch_bytes(url: str, headers: dict[str, str] | None = None, timeout: int = 45) -> tuple[bytes, str, str]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 331–340
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `fetch_bytes` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `url` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `url` recebido pela função; não há descrição individual explícita no código. |
| `headers` | positional-or-keyword | `dict[str, str] \| None` | `None` | Cabeçalhos usados em requisição/integração externa. |
| `timeout` | positional-or-keyword | `int` | `45` | Tempo máximo de espera para a operação, quando aplicável. |

#### Retorno

Anotação declarada: `tuple[bytes, str, str]`. Retornos observados estaticamente incluem: `(data, content_type, final_url)`.

#### Efeitos colaterais e fronteiras externas

- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `h.update`
- `resp.geturl`
- `resp.headers.get`
- `resp.read`
- `urllib.request.Request`
- `urllib.request.urlopen`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:download_pdf`
- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:fetch_text`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:fetch_text`

**Assinatura:**
```python
def fetch_text(url: str, headers: dict[str, str] | None = None, timeout: int = 45) -> tuple[str, str]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 343–345
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `fetch_text` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `url` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `url` recebido pela função; não há descrição individual explícita no código. |
| `headers` | positional-or-keyword | `dict[str, str] \| None` | `None` | Cabeçalhos usados em requisição/integração externa. |
| `timeout` | positional-or-keyword | `int` | `45` | Tempo máximo de espera para a operação, quando aplicável. |

#### Retorno

Anotação declarada: `tuple[str, str]`. Retornos observados estaticamente incluem: `(data.decode('utf-8', errors='replace'), final_url)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `data.decode`
- `fetch_bytes`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:fetch_json`
- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:html_pdf_links`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:fetch_json`

**Assinatura:**
```python
def fetch_json(url: str, headers: dict[str, str] | None = None, timeout: int = 45) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 348–350
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `fetch_json` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `url` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `url` recebido pela função; não há descrição individual explícita no código. |
| `headers` | positional-or-keyword | `dict[str, str] \| None` | `None` | Cabeçalhos usados em requisição/integração externa. |
| `timeout` | positional-or-keyword | `int` | `45` | Tempo máximo de espera para a operação, quando aplicável. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `json.loads(text)`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `fetch_text`
- `json.loads`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:save_pdf_data`

**Assinatura:**
```python
def save_pdf_data(data: bytes, dest: Path) -> tuple[bool, str]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 353–360
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Salva a responsabilidade representada por `pdf_data` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `data` | positional-or-keyword | `bytes` | `obrigatório/ausente` | Estrutura de dados de entrada. |
| `dest` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `dest` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `tuple[bool, str]`. Retornos observados estaticamente incluem: `(False, 'resposta não parece PDF')`; `(False, 'PDF pequeno demais; provável página de erro')`; `(True, 'ok')`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `data[:4096].lstrip`
- `data[:4096].lstrip().startswith`
- `dest.parent.mkdir`
- `dest.write_bytes`
- `len`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:download_pdf`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:download_pdf`

**Assinatura:**
```python
def download_pdf(url: str, dest: Path, headers: dict[str, str] | None = None, timeout: int = 80) -> tuple[bool, str]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 363–374
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Baixa a responsabilidade representada por `pdf` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `url` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `url` recebido pela função; não há descrição individual explícita no código. |
| `dest` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `dest` recebido pela função; não há descrição individual explícita no código. |
| `headers` | positional-or-keyword | `dict[str, str] \| None` | `None` | Cabeçalhos usados em requisição/integração externa. |
| `timeout` | positional-or-keyword | `int` | `80` | Tempo máximo de espera para a operação, quando aplicável. |

#### Retorno

Anotação declarada: `tuple[bool, str]`. Retornos observados estaticamente incluem: `save_pdf_data(data, dest)`; `(False, str(e))`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `fetch_bytes`
- `h.update`
- `save_pdf_data`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:html_pdf_links`

**Assinatura:**
```python
def html_pdf_links(url: str, max_links: int = 12) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 377–403
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `html_pdf_links` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `url` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `url` recebido pela função; não há descrição individual explícita no código. |
| `max_links` | positional-or-keyword | `int` | `12` | Parâmetro `max_links` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `[]`; `out`.

#### Efeitos colaterais e fronteiras externas

- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `fetch_text`
- `h.lower`
- `h.lower().startswith`
- `h.startswith`
- `href.strip`
- `len`
- `links.append`
- `out.append`
- `re.findall`
- `seen.add`
- `set`
- `u.lower`
- `u.rstrip`
- `urllib.parse.urljoin`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_crossref`
- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_direct_url_and_landing`
- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_elsevier_abstract_links`
- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_scopus_search`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.pdf`

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 3
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:title_similarity`

**Assinatura:**
```python
def title_similarity(a: str, b: str) -> float
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 406–407
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `title_similarity` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `a` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `a` recebido pela função; não há descrição individual explícita no código. |
| `b` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `b` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `float`. Retornos observados estaticamente incluem: `SequenceMatcher(None, norm_ascii(a).lower(), norm_ascii(b).lower()).ratio()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `SequenceMatcher`
- `SequenceMatcher(None, norm_ascii(a).lower(), norm_ascii(b).lower()).ratio`
- `norm_ascii`
- `norm_ascii(a).lower`
- `norm_ascii(b).lower`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_all_pdf_candidates`
- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_core`
- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_crossref`
- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_europepmc`
- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_openalex`
- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_scopus_search`
- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_semantic_scholar`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:openalex_abstract`

**Assinatura:**
```python
def openalex_abstract(w: dict[str, Any]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 410–418
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `openalex_abstract` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `w` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `w` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `''`; `' '.join((word for _, word in sorted(pos)))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `inv.items`
- `pos.append`
- `sorted`
- `w.get`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 2
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:candidate_item`

**Assinatura:**
```python
def candidate_item(pdf_url: str, source: str, **kwargs) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 421–424
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `candidate_item` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `pdf_url` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `pdf_url` recebido pela função; não há descrição individual explícita no código. |
| `source` | positional-or-keyword | `str` | `obrigatório/ausente` | Origem dos dados ou arquivo de entrada. |
| `kwargs` | var-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `kwargs` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `d`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `d.update`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:candidates_from_openalex_work`
- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_all_pdf_candidates`
- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_core`
- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_crossref`
- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_direct_url_and_landing`
- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_elsevier_abstract_links`
- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_elsevier_article_retrieval`
- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_europepmc`
- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_scopus_search`
- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_semantic_scholar`
- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_unpaywall`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_direct_url_and_landing`

**Assinatura:**
```python
def collect_direct_url_and_landing(c: Candidate) -> list[dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 427–439
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Coleta a responsabilidade representada por `direct_url_and_landing` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `c` | positional-or-keyword | `Candidate` | `obrigatório/ausente` | Parâmetro `c` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[dict[str, Any]]`. Retornos observados estaticamente incluem: `out`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `candidate_item`
- `html_pdf_links`
- `out.append`
- `u.lower`
- `urls.append`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_all_pdf_candidates`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `https://doi.org/`
- `.pdf`

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_unpaywall`

**Assinatura:**
```python
def collect_unpaywall(c: Candidate, email: str) -> list[dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 442–464
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Coleta a responsabilidade representada por `unpaywall` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `c` | positional-or-keyword | `Candidate` | `obrigatório/ausente` | Parâmetro `c` recebido pela função; não há descrição individual explícita no código. |
| `email` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `email` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[dict[str, Any]]`. Retornos observados estaticamente incluem: `[]`; `[candidate_item('', 'unpaywall', note=f'falha Unpaywall: {e}')]`; `out`.

#### Efeitos colaterais e fronteiras externas

- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `candidate_item`
- `data.get`
- `fetch_json`
- `loc.get`
- `locs.append`
- `locs.extend`
- `out.append`
- `u.lower`
- `urllib.parse.quote`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_all_pdf_candidates`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `https://api.unpaywall.org/v2/`
- `.pdf`

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 2
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:candidates_from_openalex_work`

**Assinatura:**
```python
def candidates_from_openalex_work(w: dict[str, Any], source: str) -> list[dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 467–484
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `candidates_from_openalex_work` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `w` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `w` recebido pela função; não há descrição individual explícita no código. |
| `source` | positional-or-keyword | `str` | `obrigatório/ausente` | Origem dos dados ou arquivo de entrada. |

#### Retorno

Anotação declarada: `list[dict[str, Any]]`. Retornos observados estaticamente incluem: `out`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `(w.get('doi') or '').replace`
- `candidate_item`
- `loc.get`
- `locs.append`
- `locs.extend`
- `openalex_abstract`
- `out.append`
- `w.get`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `https://doi.org/`

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_openalex`

**Assinatura:**
```python
def collect_openalex(c: Candidate, email: str, min_title_similarity: float, deep: bool) -> list[dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 487–509
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Coleta a responsabilidade representada por `openalex` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `c` | positional-or-keyword | `Candidate` | `obrigatório/ausente` | Parâmetro `c` recebido pela função; não há descrição individual explícita no código. |
| `email` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `email` recebido pela função; não há descrição individual explícita no código. |
| `min_title_similarity` | positional-or-keyword | `float` | `obrigatório/ausente` | Parâmetro `min_title_similarity` recebido pela função; não há descrição individual explícita no código. |
| `deep` | positional-or-keyword | `bool` | `obrigatório/ausente` | Parâmetro `deep` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[dict[str, Any]]`. Retornos observados estaticamente incluem: `out`.

#### Efeitos colaterais e fronteiras externas

- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `candidates_from_openalex_work`
- `data.get`
- `fetch_json`
- `out.append`
- `round`
- `title_similarity`
- `urllib.parse.quote`
- `w.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_all_pdf_candidates`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `https://api.openalex.org/works/doi:`
- `https://api.openalex.org/works?search=`

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 2
comprehensions: 0
exception_handlers: 2
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_crossref`

**Assinatura:**
```python
def collect_crossref(c: Candidate, min_title_similarity: float, deep: bool) -> list[dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 512–547
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Coleta a responsabilidade representada por `crossref` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `c` | positional-or-keyword | `Candidate` | `obrigatório/ausente` | Parâmetro `c` recebido pela função; não há descrição individual explícita no código. |
| `min_title_similarity` | positional-or-keyword | `float` | `obrigatório/ausente` | Parâmetro `min_title_similarity` recebido pela função; não há descrição individual explícita no código. |
| `deep` | positional-or-keyword | `bool` | `obrigatório/ausente` | Parâmetro `deep` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[dict[str, Any]]`. Retornos observados estaticamente incluem: `out`.

#### Efeitos colaterais e fronteiras externas

- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `candidate_item`
- `ct.lower`
- `data.get`
- `fetch_json`
- `html_pdf_links`
- `isinstance`
- `link.get`
- `m.get`
- `msg.get`
- `out.append`
- `title_similarity`
- `u.lower`
- `urllib.parse.quote`
- `urls.append`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_all_pdf_candidates`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `https://api.crossref.org/works/`
- `https://api.crossref.org/works?query.bibliographic=`
- `.pdf`

#### Complexidade estrutural observada

```yaml
branches: 8
loops: 4
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_semantic_scholar`

**Assinatura:**
```python
def collect_semantic_scholar(c: Candidate, api_key: str | None, min_title_similarity: float, deep: bool) -> list[dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 550–574
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Coleta a responsabilidade representada por `semantic_scholar` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `c` | positional-or-keyword | `Candidate` | `obrigatório/ausente` | Parâmetro `c` recebido pela função; não há descrição individual explícita no código. |
| `api_key` | positional-or-keyword | `str \| None` | `obrigatório/ausente` | Parâmetro `api_key` recebido pela função; não há descrição individual explícita no código. |
| `min_title_similarity` | positional-or-keyword | `float` | `obrigatório/ausente` | Parâmetro `min_title_similarity` recebido pela função; não há descrição individual explícita no código. |
| `deep` | positional-or-keyword | `bool` | `obrigatório/ausente` | Parâmetro `deep` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[dict[str, Any]]`. Retornos observados estaticamente incluem: `out`.

#### Efeitos colaterais e fronteiras externas

- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `(d.get('externalIds') or {}).get`
- `(d.get('openAccessPdf') or {}).get`
- `candidate_item`
- `d.get`
- `data.get`
- `fetch_json`
- `out.append`
- `round`
- `title_similarity`
- `urllib.parse.quote`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_all_pdf_candidates`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `https://api.semanticscholar.org/graph/v1/paper/`
- `https://api.semanticscholar.org/graph/v1/paper/search?query=`

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 1
comprehensions: 0
exception_handlers: 2
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_europepmc`

**Assinatura:**
```python
def collect_europepmc(c: Candidate, min_title_similarity: float) -> list[dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 577–600
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Coleta a responsabilidade representada por `europepmc` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `c` | positional-or-keyword | `Candidate` | `obrigatório/ausente` | Parâmetro `c` recebido pela função; não há descrição individual explícita no código. |
| `min_title_similarity` | positional-or-keyword | `float` | `obrigatório/ausente` | Parâmetro `min_title_similarity` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[dict[str, Any]]`. Retornos observados estaticamente incluem: `out`.

#### Efeitos colaterais e fronteiras externas

- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `(data.get('resultList') or {}).get`
- `(r.get('fullTextUrlList') or {}).get`
- `candidate_item`
- `data.get`
- `fetch_json`
- `out.append`
- `pdf.lower`
- `queries.append`
- `r.get`
- `round`
- `title_similarity`
- `u.get`
- `u.get('documentStyle', '').lower`
- `urllib.parse.quote`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_all_pdf_candidates`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.pdf`

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 3
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_core`

**Assinatura:**
```python
def collect_core(c: Candidate, core_key: str | None, min_title_similarity: float, deep: bool) -> list[dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 603–632
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Coleta a responsabilidade representada por `core` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `c` | positional-or-keyword | `Candidate` | `obrigatório/ausente` | Parâmetro `c` recebido pela função; não há descrição individual explícita no código. |
| `core_key` | positional-or-keyword | `str \| None` | `obrigatório/ausente` | Parâmetro `core_key` recebido pela função; não há descrição individual explícita no código. |
| `min_title_similarity` | positional-or-keyword | `float` | `obrigatório/ausente` | Parâmetro `min_title_similarity` recebido pela função; não há descrição individual explícita no código. |
| `deep` | positional-or-keyword | `bool` | `obrigatório/ausente` | Parâmetro `deep` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[dict[str, Any]]`. Retornos observados estaticamente incluem: `[]`; `out`.

#### Efeitos colaterais e fronteiras externas

- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `candidate_item`
- `data.get`
- `fetch_json`
- `isinstance`
- `item.get`
- `out.append`
- `pdfs.append`
- `r.get`
- `round`
- `str`
- `title_similarity`
- `urllib.parse.quote`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_all_pdf_candidates`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 8
loops: 4
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:elsevier_headers`

**Assinatura:**
```python
def elsevier_headers(api_key: str, insttoken: str | None = None, accept: str = 'application/json') -> dict[str, str]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 635–643
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `elsevier_headers` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `api_key` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `api_key` recebido pela função; não há descrição individual explícita no código. |
| `insttoken` | positional-or-keyword | `str \| None` | `None` | Parâmetro `insttoken` recebido pela função; não há descrição individual explícita no código. |
| `accept` | positional-or-keyword | `str` | `'application/json'` | Parâmetro `accept` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, str]`. Retornos observados estaticamente incluem: `h`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

Nenhum identificado pela análise estática.

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_elsevier_abstract_links`
- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_scopus_search`
- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:try_download_candidate`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_elsevier_article_retrieval`

**Assinatura:**
```python
def collect_elsevier_article_retrieval(c: Candidate, api_key: str | None, insttoken: str | None) -> list[dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 646–667
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Coleta a responsabilidade representada por `elsevier_article_retrieval` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `c` | positional-or-keyword | `Candidate` | `obrigatório/ausente` | Parâmetro `c` recebido pela função; não há descrição individual explícita no código. |
| `api_key` | positional-or-keyword | `str \| None` | `obrigatório/ausente` | Parâmetro `api_key` recebido pela função; não há descrição individual explícita no código. |
| `insttoken` | positional-or-keyword | `str \| None` | `obrigatório/ausente` | Parâmetro `insttoken` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[dict[str, Any]]`. Retornos observados estaticamente incluem: `[]`; `out`.

#### Efeitos colaterais e fronteiras externas

- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `candidate_item`
- `ids.append`
- `out.append`
- `re.findall`
- `seen.add`
- `set`
- `urllib.parse.quote`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_all_pdf_candidates`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 3
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_elsevier_abstract_links`

**Assinatura:**
```python
def collect_elsevier_abstract_links(c: Candidate, api_key: str | None, insttoken: str | None) -> list[dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 670–696
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Coleta a responsabilidade representada por `elsevier_abstract_links` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `c` | positional-or-keyword | `Candidate` | `obrigatório/ausente` | Parâmetro `c` recebido pela função; não há descrição individual explícita no código. |
| `api_key` | positional-or-keyword | `str \| None` | `obrigatório/ausente` | Parâmetro `api_key` recebido pela função; não há descrição individual explícita no código. |
| `insttoken` | positional-or-keyword | `str \| None` | `obrigatório/ausente` | Parâmetro `insttoken` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[dict[str, Any]]`. Retornos observados estaticamente incluem: `[]`; `out`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `candidate_item`
- `elsevier_headers`
- `fetch_json`
- `html_pdf_links`
- `json.dumps`
- `link.lower`
- `out.append`
- `pdf.rstrip`
- `re.findall`
- `set`
- `sorted`
- `urllib.parse.quote`
- `urls.append`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_all_pdf_candidates`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 6
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_scopus_search`

**Assinatura:**
```python
def collect_scopus_search(c: Candidate, api_key: str | None, insttoken: str | None, min_title_similarity: float, deep: bool) -> list[dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 699–738
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Coleta a responsabilidade representada por `scopus_search` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `c` | positional-or-keyword | `Candidate` | `obrigatório/ausente` | Parâmetro `c` recebido pela função; não há descrição individual explícita no código. |
| `api_key` | positional-or-keyword | `str \| None` | `obrigatório/ausente` | Parâmetro `api_key` recebido pela função; não há descrição individual explícita no código. |
| `insttoken` | positional-or-keyword | `str \| None` | `obrigatório/ausente` | Parâmetro `insttoken` recebido pela função; não há descrição individual explícita no código. |
| `min_title_similarity` | positional-or-keyword | `float` | `obrigatório/ausente` | Parâmetro `min_title_similarity` recebido pela função; não há descrição individual explícita no código. |
| `deep` | positional-or-keyword | `bool` | `obrigatório/ausente` | Parâmetro `deep` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[dict[str, Any]]`. Retornos observados estaticamente incluem: `[]`; `out`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `(data.get('search-results') or {}).get`
- `c.title.replace`
- `candidate_item`
- `data.get`
- `e.get`
- `elsevier_headers`
- `fetch_json`
- `html_pdf_links`
- `out.append`
- `queries.append`
- `round`
- `title_similarity`
- `urllib.parse.quote`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_all_pdf_candidates`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 10
loops: 3
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:collect_all_pdf_candidates`

**Assinatura:**
```python
def collect_all_pdf_candidates(c: Candidate, *, email: str, s2_key: str | None, core_key: str | None, elsevier_key: str | None, elsevier_insttoken: str | None, min_title_similarity: float, priority_min_title_similarity: float) -> list[dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 741–789
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Coleta a responsabilidade representada por `all_pdf_candidates` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `c` | positional-or-keyword | `Candidate` | `obrigatório/ausente` | Parâmetro `c` recebido pela função; não há descrição individual explícita no código. |
| `email` | keyword-only | `str` | `obrigatório/ausente` | Parâmetro `email` recebido pela função; não há descrição individual explícita no código. |
| `s2_key` | keyword-only | `str \| None` | `obrigatório/ausente` | Parâmetro `s2_key` recebido pela função; não há descrição individual explícita no código. |
| `core_key` | keyword-only | `str \| None` | `obrigatório/ausente` | Parâmetro `core_key` recebido pela função; não há descrição individual explícita no código. |
| `elsevier_key` | keyword-only | `str \| None` | `obrigatório/ausente` | Parâmetro `elsevier_key` recebido pela função; não há descrição individual explícita no código. |
| `elsevier_insttoken` | keyword-only | `str \| None` | `obrigatório/ausente` | Parâmetro `elsevier_insttoken` recebido pela função; não há descrição individual explícita no código. |
| `min_title_similarity` | keyword-only | `float` | `obrigatório/ausente` | Parâmetro `min_title_similarity` recebido pela função; não há descrição individual explícita no código. |
| `priority_min_title_similarity` | keyword-only | `float` | `obrigatório/ausente` | Parâmetro `priority_min_title_similarity` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[dict[str, Any]]`. Retornos observados estaticamente incluem: `uniq[:60] if deep else uniq[:25]`.

#### Efeitos colaterais e fronteiras externas

- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `candidate_item`
- `collect_core`
- `collect_crossref`
- `collect_direct_url_and_landing`
- `collect_elsevier_abstract_links`
- `collect_elsevier_article_retrieval`
- `collect_europepmc`
- `collect_openalex`
- `collect_scopus_search`
- `collect_semantic_scholar`
- `collect_unpaywall`
- `fn`
- `it.get`
- `item.get`
- `out.append`
- `round`
- `seen.add`
- `set`
- `title_similarity`
- `uniq.append`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:try_download_candidate`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 3
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:try_download_candidate`

**Assinatura:**
```python
def try_download_candidate(c: Candidate, fulltext_dir: Path, *, email: str, s2_key: str | None, core_key: str | None, elsevier_key: str | None, elsevier_insttoken: str | None, min_title_similarity: float, priority_min_title_similarity: float) -> tuple[bool, dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 792–840
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `try_download_candidate` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `c` | positional-or-keyword | `Candidate` | `obrigatório/ausente` | Parâmetro `c` recebido pela função; não há descrição individual explícita no código. |
| `fulltext_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `fulltext_dir` recebido pela função; não há descrição individual explícita no código. |
| `email` | keyword-only | `str` | `obrigatório/ausente` | Parâmetro `email` recebido pela função; não há descrição individual explícita no código. |
| `s2_key` | keyword-only | `str \| None` | `obrigatório/ausente` | Parâmetro `s2_key` recebido pela função; não há descrição individual explícita no código. |
| `core_key` | keyword-only | `str \| None` | `obrigatório/ausente` | Parâmetro `core_key` recebido pela função; não há descrição individual explícita no código. |
| `elsevier_key` | keyword-only | `str \| None` | `obrigatório/ausente` | Parâmetro `elsevier_key` recebido pela função; não há descrição individual explícita no código. |
| `elsevier_insttoken` | keyword-only | `str \| None` | `obrigatório/ausente` | Parâmetro `elsevier_insttoken` recebido pela função; não há descrição individual explícita no código. |
| `min_title_similarity` | keyword-only | `float` | `obrigatório/ausente` | Parâmetro `min_title_similarity` recebido pela função; não há descrição individual explícita no código. |
| `priority_min_title_similarity` | keyword-only | `float` | `obrigatório/ausente` | Parâmetro `priority_min_title_similarity` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `tuple[bool, dict[str, Any]]`. Retornos observados estaticamente incluem: `(True, cand)`; `(False, {'download_status': 'texto_completo_nao_recuperado', 'download_note': ' | '.join(errors[:12]) if errors else 'sem URL de PDF encontrada nas fontes consultadas', 'tentativas_pdf': len(candidates)})`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `' | '.join`
- `base.with_name`
- `cand.get`
- `cand.update`
- `collect_all_pdf_candidates`
- `dict`
- `download_pdf`
- `elsevier_headers`
- `enumerate`
- `errors.append`
- `len`
- `sha256_file`
- `slug`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.pdf`

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:bib_escape`

**Assinatura:**
```python
def bib_escape(v: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 843–844
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `bib_escape` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `v` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `v` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `str(v or '').replace('{', '\\{').replace('}', '\\}').replace('\n', ' ').strip()`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `str`
- `str(v or '').replace`
- `str(v or '').replace('{', '\\{').replace`
- `str(v or '').replace('{', '\\{').replace('}', '\\}').replace`
- `str(v or '').replace('{', '\\{').replace('}', '\\}').replace('\n', ' ').strip`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:bib_key`

**Assinatura:**
```python
def bib_key(row: dict[str, Any]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 847–852
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `bib_key` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `row` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `row` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `key or f"ref{row.get('ordem_inclusao_final', '')}"`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `row.get`
- `slug`
- `slug(f'{author}_{year}_{title}', 70).replace`
- `str`
- `str(row.get('authors') or 'ref').split`
- `str(row.get('authors') or 'ref').split(';')[0].split`
- `str(row.get('authors') or 'ref').split(';')[0].split(',')[0].strip`
- `str(row.get('authors') or 'ref').split(';')[0].split(',')[0].strip().split`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:write_bib`

**Assinatura:**
```python
def write_bib(path: Path, rows: list[dict[str, Any]]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 855–881
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Grava a responsabilidade representada por `bib` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `rows` | positional-or-keyword | `list[dict[str, Any]]` | `obrigatório/ausente` | Parâmetro `rows` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `',\n'.join`
- `'\n\n'.join`
- `bib_escape`
- `bib_key`
- `body.append`
- `entries.append`
- `fields.items`
- `path.parent.mkdir`
- `path.write_text`
- `r.get`
- `set`
- `str`
- `used.add`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 3
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:backup`

**Assinatura:**
```python
def backup(path: Path) -> Path | None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 884–889
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `backup` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `Path | None`. Retornos observados estaticamente incluem: `None`; `b`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `b.write_bytes`
- `path.exists`
- `path.read_bytes`
- `path.with_suffix`
- `time.strftime`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:activate_selection`

**Assinatura:**
```python
def activate_selection(dados_dir: Path, output_csv: Path, output_bib: Path, prefix: str | None) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 892–906
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `activate_selection` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `dados_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `dados_dir` recebido pela função; não há descrição individual explícita no código. |
| `output_csv` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `output_csv` recebido pela função; não há descrição individual explícita no código. |
| `output_bib` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `output_bib` recebido pela função; não há descrição individual explícita no código. |
| `prefix` | positional-or-keyword | `str \| None` | `obrigatório/ausente` | Prefixo canônico usado para compor nomes de artefatos. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `backup`
- `bib_target.write_bytes`
- `output_bib.read_bytes`
- `output_csv.read_bytes`
- `print`
- `t.write_bytes`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.referencias_incluidas_seminario.csv`
- `.referencias_incluidas.csv`
- `.referencias_incluidas.bib`

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:md_escape`

**Assinatura:**
```python
def md_escape(v: Any) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 909–910
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `md_escape` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `v` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `v` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `str(v or '').replace('|', '/').replace('\n', ' ').strip()`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `str`
- `str(v or '').replace`
- `str(v or '').replace('|', '/').replace`
- `str(v or '').replace('|', '/').replace('\n', ' ').strip`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:write_manual_pending`

**Assinatura:**
```python
def write_manual_pending(path: Path, priority_missing: list[dict[str, Any]], all_missing: list[dict[str, Any]]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 913–933
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Grava a responsabilidade representada por `manual_pending` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `priority_missing` | positional-or-keyword | `list[dict[str, Any]]` | `obrigatório/ausente` | Parâmetro `priority_missing` recebido pela função; não há descrição individual explícita no código. |
| `all_missing` | positional-or-keyword | `list[dict[str, Any]]` | `obrigatório/ausente` | Parâmetro `all_missing` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Abre arquivo para leitura ou inspeção.
- Lê arquivos ou artefatos do filesystem.
- Pode realizar chamada de rede ou consulta a serviço externo.

#### Chamadas realizadas pelo corpo direto

- `f.write`
- `md_escape`
- `path.open`
- `r.get`
- `str`
- `urllib.parse.quote`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `- Busca OpenAlex: https://openalex.org/search?filter=title.search:`
- `- Busca Semantic Scholar: https://www.semanticscholar.org/search?q=`
- `- Busca Crossref: https://search.crossref.org/?q=`

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_fulltext_garantido:main`

**Assinatura:**
```python
def main(argv = None) -> int
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_fulltext_garantido.py`
**Linhas:** 936–1177
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa o ponto de entrada correspondente e coordena o fluxo associado.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `não anotado` | `None` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `exit_code`.

#### Efeitos colaterais e fronteiras externas

- Abre arquivo para leitura ou inspeção.
- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.
- Lê arquivos ou artefatos do filesystem.
- Lê configuração/credenciais do ambiente do processo.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(args.art_dir).resolve`
- `Path(args.dados_pesquisa_path).resolve`
- `Path(args.prisma_config).resolve`
- `Path(args.prisma_out_dir).resolve`
- `Path(args.project_root).resolve`
- `Path(x).resolve`
- `SystemExit`
- `activate_selection`
- `argparse.ArgumentParser`
- `asdict`
- `bibs[0].name.replace`
- `bool`
- `counts.items`
- `counts_json.write_text`
- `dados_dir.glob`
- `dados_dir.mkdir`
- `dict`
- `dl.get`
- `enumerate`
- `env.get`
- `exclusions.append`
- `f.write`
- `float`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `SystemExit(f'ERRO: prisma-out-dir não existe: {prisma_out}')`
- `SystemExit('ERRO: nenhum candidato com título encontrado no universo PRISMA.')`

#### Opções CLI mencionadas diretamente no corpo

- `--art-dir`
- `--ativar-selecao-final`
- `--core-api-key`
- `--dados-pesquisa-path`
- `--elsevier-api-key`
- `--elsevier-insttoken`
- `--email`
- `--exigir-prioritarios`
- `--extra-csv`
- `--fail-if-insufficient`
- `--max-candidatos`
- `--min-title-similarity`
- `--prefix`
- `--priority-min-title-similarity`
- `--prisma-config`
- `--prisma-out-dir`
- `--project-root`
- `--semantic-scholar-api-key`
- `--sleep`
- `--target-n`
- `--titulo-prioritario-regex`

#### Artefatos/caminhos literais reconhecidos no corpo

- `*.referencias_incluidas.bib`
- `.referencias_incluidas.bib`
- `.referencias_incluidas_fulltext_garantido.csv`
- `.matriz_prisma_fulltext_garantido.csv`
- `.exclusoes_fulltext_nao_recuperado.csv`
- `.referencias_incluidas_fulltext_garantido.bib`
- `.contagens_prisma_fulltext_garantido.json`
- `.relatorio_fulltext_garantido.md`
- `.pendencias_download_manual.md`
- `orientacoes_fulltext_garantido_artigo.md`

#### Complexidade estrutural observada

```yaml
branches: 21
loops: 7
comprehensions: 5
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `7e237c15b4940622671c0e854c269e9b06ac5071e539e9aa8aad96dbb1ab1d95`
**LOC do arquivo-fonte:** 1181


### `app_bundle.scripts.pipeline.prisma_model:PrismaFlow.normalize_counts`

**Assinatura:**
```python
def normalize_counts(self) -> 'PrismaFlow'
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_model.py`
**Linhas:** 65–74
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `PrismaFlow`
**Decoradores:** `model_validator(mode='after')`
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Normaliza a responsabilidade representada por `counts` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `'PrismaFlow'`. Retornos observados estaticamente incluem: `self`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `max`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9261e27fcfbac2c3f4d3b383ae45f6ff1c0e6ed9483da5c6fc57124a335c8327`
**LOC do arquivo-fonte:** 137


### `app_bundle.scripts.pipeline.prisma_model:StudyRecord.normalize_key`

**Assinatura:**
```python
def normalize_key(cls, value: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_model.py`
**Linhas:** 96–97
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `StudyRecord`
**Decoradores:** `field_validator('bib_key')`, `classmethod`
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Normaliza a responsabilidade representada por `key` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cls` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `cls` recebido pela função; não há descrição individual explícita no código. |
| `value` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `str(value or '').strip().lstrip('@')`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `str`
- `str(value or '').strip`
- `str(value or '').strip().lstrip`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9261e27fcfbac2c3f4d3b383ae45f6ff1c0e6ed9483da5c6fc57124a335c8327`
**LOC do arquivo-fonte:** 137


### `app_bundle.scripts.pipeline.prisma_model:StudyRecord.normalize_doi`

**Assinatura:**
```python
def normalize_doi(cls, value: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_model.py`
**Linhas:** 101–105
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `StudyRecord`
**Decoradores:** `field_validator('doi')`, `classmethod`
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Normaliza a responsabilidade representada por `doi` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cls` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `cls` recebido pela função; não há descrição individual explícita no código. |
| `value` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `doi.strip().strip('.')`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `doi.strip`
- `doi.strip().strip`
- `re.sub`
- `str`
- `str(value or '').strip`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `^https?://(?:dx\.)?doi\.org/`

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9261e27fcfbac2c3f4d3b383ae45f6ff1c0e6ed9483da5c6fc57124a335c8327`
**LOC do arquivo-fonte:** 137


### `app_bundle.scripts.pipeline.prisma_model:PrismaReport.normalize_report`

**Assinatura:**
```python
def normalize_report(self) -> 'PrismaReport'
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_model.py`
**Linhas:** 127–137
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `PrismaReport`
**Decoradores:** `model_validator(mode='after')`
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Normaliza a responsabilidade representada por `report` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `'PrismaReport'`. Retornos observados estaticamente incluem: `self`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `PrismaFlow.model_validate`
- `len`
- `self.flow.model_dump`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `9261e27fcfbac2c3f4d3b383ae45f6ff1c0e6ed9483da5c6fc57124a335c8327`
**LOC do arquivo-fonte:** 137


### `app_bundle.scripts.pipeline.prisma_pipeline:prisma_enabled`

**Assinatura:**
```python
def prisma_enabled(cfg: dict[str, Any]) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_pipeline.py`
**Linhas:** 61–63
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `prisma_enabled` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `bool(rel.get('ativo', False))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `bool`
- `cfg.get`
- `isinstance`
- `rel.get`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.default_runtime:_ap003f_pipeline_core`
- `app_bundle.scripts.pipeline.prisma_pipeline:run_prisma_report_outputs`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `1da4b859211b698664d1471560886030cd3e601b0634f63f8f6391ed03925640`
**LOC do arquivo-fonte:** 148


### `app_bundle.scripts.pipeline.prisma_pipeline:prisma_output_paths`

**Assinatura:**
```python
def prisma_output_paths(cfg: dict[str, Any], document_out_dir: Path, document_prefix: str) -> tuple[Path, str]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_pipeline.py`
**Linhas:** 66–74
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `prisma_output_paths` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `document_out_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `document_out_dir` recebido pela função; não há descrição individual explícita no código. |
| `document_prefix` | positional-or-keyword | `str` | `obrigatório/ausente` | Prefixo canônico usado para compor nomes de artefatos. |

#### Retorno

Anotação declarada: `tuple[Path, str]`. Retornos observados estaticamente incluem: `(out_dir, prefix)`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(str(cfg.get('__config_dir__') or Path.cwd())).resolve`
- `Path.cwd`
- `bool`
- `cfg.get`
- `isinstance`
- `out_dir.mkdir`
- `paths.get`
- `rel.get`
- `resolve_path`
- `str`
- `str(paths.get('research_prefix') or rel.get('titulo_slug') or f'relatorio_prisma_{document_prefix}').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_pipeline:run_prisma_report_outputs`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `1da4b859211b698664d1471560886030cd3e601b0634f63f8f6391ed03925640`
**LOC do arquivo-fonte:** 148


### `app_bundle.scripts.pipeline.prisma_pipeline:_copy_bib_for_prisma`

**Assinatura:**
```python
def _copy_bib_for_prisma(bib_result: BibBuildResult, out_dir: Path) -> Path | None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_pipeline.py`
**Linhas:** 77–83
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Copia a responsabilidade representada por `bib_for_prisma` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `bib_result` | positional-or-keyword | `BibBuildResult` | `obrigatório/ausente` | Resultado intermediário ou final utilizado pela rotina. |
| `out_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `out_dir` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path | None`. Retornos observados estaticamente incluem: `None`; `dest`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `bib_result.bib_path.exists`
- `bib_result.bib_path.resolve`
- `dest.resolve`
- `shutil.copy2`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_pipeline:run_prisma_report_outputs`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `1da4b859211b698664d1471560886030cd3e601b0634f63f8f6391ed03925640`
**LOC do arquivo-fonte:** 148


### `app_bundle.scripts.pipeline.prisma_pipeline:run_prisma_report_outputs`

**Assinatura:**
```python
def run_prisma_report_outputs(cfg: dict[str, Any], docs: list[SourceDoc], orientations: list[SourceDoc], bib_result: BibBuildResult, document_out_dir: Path, document_prefix: str) -> dict[str, Any] | None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_pipeline.py`
**Linhas:** 86–148
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `prisma_report_outputs` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `docs` | positional-or-keyword | `list[SourceDoc]` | `obrigatório/ausente` | Coleção de documentos/entradas processados pela rotina. |
| `orientations` | positional-or-keyword | `list[SourceDoc]` | `obrigatório/ausente` | Parâmetro `orientations` recebido pela função; não há descrição individual explícita no código. |
| `bib_result` | positional-or-keyword | `BibBuildResult` | `obrigatório/ausente` | Resultado intermediário ou final utilizado pela rotina. |
| `document_out_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `document_out_dir` recebido pela função; não há descrição individual explícita no código. |
| `document_prefix` | positional-or-keyword | `str` | `obrigatório/ausente` | Prefixo canônico usado para compor nomes de artefatos. |

#### Retorno

Anotação declarada: `dict[str, Any] | None`. Retornos observados estaticamente incluem: `None`; `paths`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(str(cfg.get('__config_dir__') or Path.cwd())).resolve`
- `Path.cwd`
- `_copy_bib_for_prisma`
- `bool`
- `build_prisma_report`
- `cfg.get`
- `docx_cfg.get`
- `isinstance`
- `latex_cfg.get`
- `prisma_enabled`
- `prisma_output_paths`
- `raise_if_prisma_errors`
- `rel.get`
- `render_prisma_docx`
- `render_prisma_flow_svg`
- `render_prisma_org`
- `render_prisma_xlsx`
- `report.diagnostics.avisos.append`
- `report.model_dump`
- `resolve_path`
- `run_compile_sequence`
- `str`
- `validate_prisma_report`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.default_runtime:_ap003f_pipeline_core`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.prisma_report.json`
- `_fluxo_prisma.svg`
- `.org`
- `.docx`
- `.xlsx`
- `.prisma_outputs.json`

#### Complexidade estrutural observada

```yaml
branches: 13
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `1da4b859211b698664d1471560886030cd3e601b0634f63f8f6391ed03925640`
**LOC do arquivo-fonte:** 148


### `app_bundle.scripts.pipeline.prisma_validator:validate_prisma_report`

**Assinatura:**
```python
def validate_prisma_report(report: PrismaReport, *, strict: bool = True) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_validator.py`
**Linhas:** 13–43
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Valida a responsabilidade representada por `prisma_report` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `report` | positional-or-keyword | `PrismaReport` | `obrigatório/ausente` | Parâmetro `report` recebido pela função; não há descrição individual explícita no código. |
| `strict` | keyword-only | `bool` | `True` | Parâmetro `strict` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `errors + ['Aviso: ' + w for w in warnings]`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `enumerate`
- `errors.append`
- `len`
- `re.match`
- `report.metadata.titulo.strip`
- `study.justificativa.strip`
- `study.motivo.strip`
- `study.titulo.strip`
- `warnings.append`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_pipeline:run_prisma_report_outputs`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 11
loops: 2
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `ec79d03fb146270266b496a4d2a42212c48a0be11c51a10fcd38c54659b26800`
**LOC do arquivo-fonte:** 49


### `app_bundle.scripts.pipeline.prisma_validator:raise_if_prisma_errors`

**Assinatura:**
```python
def raise_if_prisma_errors(messages: list[str], title: str = 'Validação do relatório PRISMA falhou') -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/prisma_validator.py`
**Linhas:** 46–49
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `raise_if_prisma_errors` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `messages` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `messages` recebido pela função; não há descrição individual explícita no código. |
| `title` | positional-or-keyword | `str` | `'Validação do relatório PRISMA falhou'` | Parâmetro `title` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'\n- '.join`
- `RuntimeError`
- `m.startswith`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_pipeline:run_prisma_report_outputs`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError(title + ':\n- ' + '\n- '.join(messages))`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `ec79d03fb146270266b496a4d2a42212c48a0be11c51a10fcd38c54659b26800`
**LOC do arquivo-fonte:** 49


### `app_bundle.scripts.pipeline.project_tools:_packaged_app_bundle`

**Assinatura:**
```python
def _packaged_app_bundle() -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/project_tools.py`
**Linhas:** 59–64
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_packaged_app_bundle` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `candidate`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(__file__).resolve`
- `RuntimeError`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.project_tools:_find_app_bundle`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('Não consegui localizar o app_bundle instalado.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `0c990206d6325d952d49575210bc44f58f103760732eb5002ce62a58739151e2`
**LOC do arquivo-fonte:** 344


### `app_bundle.scripts.pipeline.project_tools:_find_app_bundle`

**Assinatura:**
```python
def _find_app_bundle(start: Path | None = None) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/project_tools.py`
**Linhas:** 67–75
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Localiza a responsabilidade representada por `app_bundle` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `start` | positional-or-keyword | `Path \| None` | `None` | Parâmetro `start` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `candidate`; `(candidate / 'app_bundle').resolve()`; `_packaged_app_bundle()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `(candidate / 'app_bundle').exists`
- `(candidate / 'app_bundle').resolve`
- `(start or Path.cwd()).resolve`
- `Path.cwd`
- `_packaged_app_bundle`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.project_tools:init_project`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `0c990206d6325d952d49575210bc44f58f103760732eb5002ce62a58739151e2`
**LOC do arquivo-fonte:** 344


### `app_bundle.scripts.pipeline.project_tools:_destination_app_bundle`

**Assinatura:**
```python
def _destination_app_bundle(base_dir: Path | None, resource_app_bundle: Path) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/project_tools.py`
**Linhas:** 78–86
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_destination_app_bundle` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `base_dir` | positional-or-keyword | `Path \| None` | `obrigatório/ausente` | Parâmetro `base_dir` recebido pela função; não há descrição individual explícita no código. |
| `resource_app_bundle` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `resource_app_bundle` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `resource_app_bundle`; `base`; `(base / 'app_bundle').resolve()`; `base / 'app_bundle'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `(base / 'app_bundle').is_dir`
- `(base / 'app_bundle').resolve`
- `Path`
- `Path(base_dir).expanduser`
- `Path(base_dir).expanduser().resolve`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.project_tools:init_project`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `0c990206d6325d952d49575210bc44f58f103760732eb5002ce62a58739151e2`
**LOC do arquivo-fonte:** 344


### `app_bundle.scripts.pipeline.project_tools:_write_placeholder_zip`

**Assinatura:**
```python
def _write_placeholder_zip(path: Path, readme_text: str) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/project_tools.py`
**Linhas:** 89–94
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Grava a responsabilidade representada por `placeholder_zip` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `readme_text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `path.exists`
- `path.parent.mkdir`
- `zf.writestr`
- `zipfile.ZipFile`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.project_tools:init_project`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `README.txt`

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `0c990206d6325d952d49575210bc44f58f103760732eb5002ce62a58739151e2`
**LOC do arquivo-fonte:** 344


### `app_bundle.scripts.pipeline.project_tools:_template_config`

**Assinatura:**
```python
def _template_config(app_bundle: Path, project_slug: str, project_type: str, institution: str = 'fgv') -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/project_tools.py`
**Linhas:** 97–134
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_template_config` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `app_bundle` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `app_bundle` recebido pela função; não há descrição individual explícita no código. |
| `project_slug` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `project_slug` recebido pela função; não há descrição individual explícita no código. |
| `project_type` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `project_type` recebido pela função; não há descrição individual explícita no código. |
| `institution` | positional-or-keyword | `str` | `'fgv'` | Parâmetro `institution` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `text`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `FileNotFoundError`
- `re.sub`
- `replacements.items`
- `source.exists`
- `source.read_text`
- `text.replace`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.project_tools:init_project`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `FileNotFoundError(f'Template TOML não encontrado: {source}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `atividade_exemplo.toml`
- `relatorio_prisma_exemplo.toml`
- `paper_exemplo.toml`
- `/doi_manifest.csv`
- `doi_manifest.csv`
- `../../projetos/paper_nome_do_tema/doi_manifest.csv`
- `../../projetos/atividade_aula_2/doi_manifest.csv`

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `0c990206d6325d952d49575210bc44f58f103760732eb5002ce62a58739151e2`
**LOC do arquivo-fonte:** 344


### `app_bundle.scripts.pipeline.project_tools:init_project`

**Assinatura:**
```python
def init_project(name: str, project_type: str = 'paper', base_dir: Path | None = None, overwrite: bool = False, institution: str = 'fgv') -> InitProjectResult
```

**Arquivo:** `app_bundle/scripts/pipeline/project_tools.py`
**Linhas:** 137–198
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `init_project` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `name` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `name` recebido pela função; não há descrição individual explícita no código. |
| `project_type` | positional-or-keyword | `str` | `'paper'` | Parâmetro `project_type` recebido pela função; não há descrição individual explícita no código. |
| `base_dir` | positional-or-keyword | `Path \| None` | `None` | Parâmetro `base_dir` recebido pela função; não há descrição individual explícita no código. |
| `overwrite` | positional-or-keyword | `bool` | `False` | Parâmetro `overwrite` recebido pela função; não há descrição individual explícita no código. |
| `institution` | positional-or-keyword | `str` | `'fgv'` | Parâmetro `institution` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `InitProjectResult`. Retornos observados estaticamente incluem: `InitProjectResult(project_dir, config_path, doi_manifest, documentos_zip, orientacoes_zip, readme_path)`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `FileExistsError`
- `InitProjectResult`
- `ValueError`
- `_destination_app_bundle`
- `_find_app_bundle`
- `_template_config`
- `_write_placeholder_zip`
- `any`
- `config_path.exists`
- `doi_manifest.exists`
- `name.strip`
- `project_dir.exists`
- `project_dir.iterdir`
- `project_dir.mkdir`
- `readme_path.exists`
- `slugify`
- `write_text`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `ValueError('Informe um nome de projeto.')`
- `FileExistsError(f'Projeto já existe: {project_dir}. Use --overwrite-project para sobrescrever arquivos seguros.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `doi_manifest.csv`
- `paper_config.toml`
- `README_PROJETO.md`

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `0c990206d6325d952d49575210bc44f58f103760732eb5002ce62a58739151e2`
**LOC do arquivo-fonte:** 344


### `app_bundle.scripts.pipeline.project_tools:_iter_zip_sources`

**Assinatura:**
```python
def _iter_zip_sources(input_zip: Path) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/project_tools.py`
**Linhas:** 201–212
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_iter_zip_sources` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `input_zip` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `input_zip` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `sorted(dict.fromkeys(names))`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `FileNotFoundError`
- `Path`
- `Path(name).suffix.lower`
- `dict.fromkeys`
- `input_zip.exists`
- `name.endswith`
- `names.append`
- `sorted`
- `zf.namelist`
- `zipfile.ZipFile`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `FileNotFoundError(f'ZIP não encontrado: {input_zip}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `0c990206d6325d952d49575210bc44f58f103760732eb5002ce62a58739151e2`
**LOC do arquivo-fonte:** 344


### `app_bundle.scripts.pipeline.project_tools:_iter_dir_sources`

**Assinatura:**
```python
def _iter_dir_sources(input_dir: Path, recursive: bool = True) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/project_tools.py`
**Linhas:** 215–226
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_iter_dir_sources` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `input_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `input_dir` recebido pela função; não há descrição individual explícita no código. |
| `recursive` | positional-or-keyword | `bool` | `True` | Parâmetro `recursive` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `sorted(dict.fromkeys(out))`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `FileNotFoundError`
- `dict.fromkeys`
- `input_dir.exists`
- `input_dir.glob`
- `input_dir.is_dir`
- `input_dir.rglob`
- `out.append`
- `path.is_file`
- `path.relative_to`
- `path.suffix.lower`
- `sorted`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `FileNotFoundError(f'Diretório não encontrado: {input_dir}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `0c990206d6325d952d49575210bc44f58f103760732eb5002ce62a58739151e2`
**LOC do arquivo-fonte:** 344


### `app_bundle.scripts.pipeline.project_tools:make_doi_manifest`

**Assinatura:**
```python
def make_doi_manifest(input_zip: Path | None, input_dir: Path | None, output: Path, overwrite: bool = True) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/project_tools.py`
**Linhas:** 229–246
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Cria a responsabilidade representada por `doi_manifest` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `input_zip` | positional-or-keyword | `Path \| None` | `obrigatório/ausente` | Parâmetro `input_zip` recebido pela função; não há descrição individual explícita no código. |
| `input_dir` | positional-or-keyword | `Path \| None` | `obrigatório/ausente` | Parâmetro `input_dir` recebido pela função; não há descrição individual explícita no código. |
| `output` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `output` recebido pela função; não há descrição individual explícita no código. |
| `overwrite` | positional-or-keyword | `bool` | `True` | Parâmetro `overwrite` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `{'source': source, 'output': str(output), 'total_files': len(files), 'files': files}`.

#### Efeitos colaterais e fronteiras externas

- Abre arquivo para leitura ou inspeção.
- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `FileExistsError`
- `ValueError`
- `_iter_dir_sources`
- `_iter_zip_sources`
- `csv.writer`
- `len`
- `output.exists`
- `output.open`
- `output.parent.mkdir`
- `str`
- `writer.writerow`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `ValueError('Informe --input-zip ou --input-dir.')`
- `FileExistsError(f'Arquivo já existe: {output}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `0c990206d6325d952d49575210bc44f58f103760732eb5002ce62a58739151e2`
**LOC do arquivo-fonte:** 344


### `app_bundle.scripts.pipeline.project_tools:inspect_bib`

**Assinatura:**
```python
def inspect_bib(path: Path, output_prefix: Path | None = None) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/project_tools.py`
**Linhas:** 253–315
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `inspect_bib` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `output_prefix` | positional-or-keyword | `Path \| None` | `None` | Prefixo canônico usado para compor nomes de artefatos. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `report`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `(note + ' ' + author).lower`
- `DOI_RE.match`
- `FileNotFoundError`
- `HTML_RE.search`
- `Path`
- `bib_entry_key`
- `by_identity.items`
- `by_identity.setdefault`
- `by_identity.setdefault(identity, []).append`
- `entry_head.group`
- `entry_head.group(1).lower`
- `entry_identity`
- `entry_issues.append`
- `extract_field`
- `issues.append`
- `keys.append`
- `len`
- `normalize_doi`
- `output_prefix.parent.mkdir`
- `path.exists`
- `path.read_text`
- `re.match`
- `render_bib_inspection_markdown`
- `split_bib_entries`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `FileNotFoundError(f'.bib não encontrado: {path}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.bib não encontrado: `
- `.json`
- `.md`

#### Complexidade estrutural observada

```yaml
branches: 15
loops: 1
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `0c990206d6325d952d49575210bc44f58f103760732eb5002ce62a58739151e2`
**LOC do arquivo-fonte:** 344


### `app_bundle.scripts.pipeline.project_tools:render_bib_inspection_markdown`

**Assinatura:**
```python
def render_bib_inspection_markdown(report: dict[str, Any]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/project_tools.py`
**Linhas:** 318–344
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Renderiza a responsabilidade representada por `bib_inspection_markdown` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `report` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `report` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'\n'.join(lines).rstrip() + '\n'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `', '.join`
- `'\n'.join`
- `'\n'.join(lines).rstrip`
- `duplicates.items`
- `item.get`
- `lines.append`
- `report.get`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.project_tools:inspect_bib`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 2
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `0c990206d6325d952d49575210bc44f58f103760732eb5002ce62a58739151e2`
**LOC do arquivo-fonte:** 344


### `app_bundle.scripts.pipeline.prompt_lock:build_prompt_lock`

**Assinatura:**
```python
def build_prompt_lock(cfg: dict[str, Any]) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/prompt_lock.py`
**Linhas:** 32–40
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Constrói a responsabilidade representada por `prompt_lock` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `{'version': PIPELINE_VERSION, 'generated_at': now_iso(), 'config_path': cfg.get('__config_path__'), 'institution_profile': cfg.get('__institution_profile_name__'), 'institution_profile_path': cfg.get('__institution_profile_path__'), 'prompts': prompt_report_for_cfg(cfg)}`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `cfg.get`
- `now_iso`
- `prompt_report_for_cfg`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prompt_lock:write_prompt_lock`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c2e0a21f4daeb7e880c43772b454131971ccac857501aa7f0f915f3cc00ba5e3`
**LOC do arquivo-fonte:** 80


### `app_bundle.scripts.pipeline.prompt_lock:write_prompt_lock`

**Assinatura:**
```python
def write_prompt_lock(cfg: dict[str, Any], output_path: Path) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/prompt_lock.py`
**Linhas:** 43–46
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Grava a responsabilidade representada por `prompt_lock` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `output_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `lock`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `build_prompt_lock`
- `write_json`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c2e0a21f4daeb7e880c43772b454131971ccac857501aa7f0f915f3cc00ba5e3`
**LOC do arquivo-fonte:** 80


### `app_bundle.scripts.pipeline.prompt_lock:render_prompt_lock_markdown`

**Assinatura:**
```python
def render_prompt_lock_markdown(lock: dict[str, Any]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prompt_lock.py`
**Linhas:** 49–76
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Renderiza a responsabilidade representada por `prompt_lock_markdown` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `lock` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `lock` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'\n'.join(lines).strip() + '\n'`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `'\n'.join(lines).strip`
- `lines.append`
- `lock.get`
- `prompts.items`
- `report.get`
- `src.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prompt_lock:write_prompt_lock_markdown`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c2e0a21f4daeb7e880c43772b454131971ccac857501aa7f0f915f3cc00ba5e3`
**LOC do arquivo-fonte:** 80


### `app_bundle.scripts.pipeline.prompt_lock:write_prompt_lock_markdown`

**Assinatura:**
```python
def write_prompt_lock_markdown(lock: dict[str, Any], output_path: Path) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/prompt_lock.py`
**Linhas:** 79–80
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Grava a responsabilidade representada por `prompt_lock_markdown` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `lock` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `lock` recebido pela função; não há descrição individual explícita no código. |
| `output_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `render_prompt_lock_markdown`
- `write_text`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.default_runtime:_ap003f_pipeline_core`
- `academic_pipeline.prisma_generic_orchestration:run_prisma_stage_004`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c2e0a21f4daeb7e880c43772b454131971ccac857501aa7f0f915f3cc00ba5e3`
**LOC do arquivo-fonte:** 80


### `app_bundle.scripts.pipeline.prompt_manager:PromptBundle.report`

**Assinatura:**
```python
def report(self) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/prompt_manager.py`
**Linhas:** 46–51
**Tipo:** method
**Visibilidade:** pública por nome
**Classe proprietária:** `PromptBundle`
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o método `report` da classe correspondente.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `self` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `self` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `{'task': self.task, 'total_chars': len(self.text), 'sources': [asdict(s) for s in self.sources]}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `asdict`
- `len`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_builder:_build_resumo_artigos_document_model_staged`
- `app_bundle.scripts.pipeline.document_builder:build_document_model`
- `app_bundle.scripts.pipeline.mindmap_manager:attach_existing_mindmap_if_available`
- `app_bundle.scripts.pipeline.mindmap_manager:generate_and_attach_mindmap`
- `app_bundle.scripts.pipeline.prisma_builder:build_prisma_report`
- `app_bundle.scripts.pipeline.prompt_manager:prompt_report_for_cfg`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c739b72f2fb83607c8e968d200cd4d3ee53618360f17c2296ce2c84c418163ad`
**LOC do arquivo-fonte:** 293


### `app_bundle.scripts.pipeline.prompt_manager:_cfg_section`

**Assinatura:**
```python
def _cfg_section(cfg: dict[str, Any], name: str) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/prompt_manager.py`
**Linhas:** 54–56
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_cfg_section` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `name` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `name` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `sec if isinstance(sec, dict) else {}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `cfg.get`
- `isinstance`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c739b72f2fb83607c8e968d200cd4d3ee53618360f17c2296ce2c84c418163ad`
**LOC do arquivo-fonte:** 293


### `app_bundle.scripts.pipeline.prompt_manager:_config_dir`

**Assinatura:**
```python
def _config_dir(cfg: dict[str, Any]) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/prompt_manager.py`
**Linhas:** 59–60
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_config_dir` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `Path(str(cfg.get('__config_dir__') or Path.cwd())).resolve()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(str(cfg.get('__config_dir__') or Path.cwd())).resolve`
- `Path.cwd`
- `cfg.get`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c739b72f2fb83607c8e968d200cd4d3ee53618360f17c2296ce2c84c418163ad`
**LOC do arquivo-fonte:** 293


### `app_bundle.scripts.pipeline.prompt_manager:_profile_dir`

**Assinatura:**
```python
def _profile_dir(cfg: dict[str, Any]) -> Path | None
```

**Arquivo:** `app_bundle/scripts/pipeline/prompt_manager.py`
**Linhas:** 63–68
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_profile_dir` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `Path | None`. Retornos observados estaticamente incluem: `None`; `p.parent if p.exists() else None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(str(raw)).expanduser`
- `Path(str(raw)).expanduser().resolve`
- `cfg.get`
- `p.exists`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c739b72f2fb83607c8e968d200cd4d3ee53618360f17c2296ce2c84c418163ad`
**LOC do arquivo-fonte:** 293


### `app_bundle.scripts.pipeline.prompt_manager:resolve_prompt_path`

**Assinatura:**
```python
def resolve_prompt_path(raw: Any, cfg: dict[str, Any]) -> Path | None
```

**Arquivo:** `app_bundle/scripts/pipeline/prompt_manager.py`
**Linhas:** 71–113
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Resolve paths de prompts.

**Docstring resumida:** Resolve paths de prompts.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `raw` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `raw` recebido pela função; não há descrição individual explícita no código. |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `Path | None`. Retornos observados estaticamente incluem: `None`; `(app_bundle / s.removeprefix('app://')).resolve()`; `(config_dir / s.removeprefix('config://')).resolve()`; `(profile_dir / s.removeprefix('profile://')).resolve()`; `p.resolve()`; `c.resolve()`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `(app_bundle / s.removeprefix('app://')).resolve`
- `(config_dir / s.removeprefix('config://')).resolve`
- `(profile_dir / s.removeprefix('profile://')).resolve`
- `Path`
- `Path(s).expanduser`
- `Path.cwd`
- `_config_dir`
- `_profile_dir`
- `c.exists`
- `c.resolve`
- `candidates[0].resolve`
- `find_app_bundle`
- `p.is_absolute`
- `p.resolve`
- `s.removeprefix`
- `s.startswith`
- `str`
- `str(raw).strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prompt_manager:_add_prompt`
- `app_bundle.scripts.pipeline.prompt_manager:validate_prompt_paths`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `Resolve paths de prompts.      Suporta:     - app://prompts/...     - config://arquivo.txt     - profile://prompts/...     - caminhos absolutos     - caminhos relativos ao diretório do TOML, app_bundle, raiz do projeto ou cwd.     `

#### Complexidade estrutural observada

```yaml
branches: 8
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c739b72f2fb83607c8e968d200cd4d3ee53618360f17c2296ce2c84c418163ad`
**LOC do arquivo-fonte:** 293


### `app_bundle.scripts.pipeline.prompt_manager:_iter_values`

**Assinatura:**
```python
def _iter_values(value: Any) -> Iterable[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/prompt_manager.py`
**Linhas:** 116–121
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_iter_values` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Iterable[str]`. Retornos observados estaticamente incluem: `[]`; `[str(v) for v in value if str(v).strip()]`; `[str(value)] if str(value).strip() else []`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `isinstance`
- `str`
- `str(v).strip`
- `str(value).strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prompt_manager:_add_prompt`
- `app_bundle.scripts.pipeline.prompt_manager:validate_prompt_paths`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c739b72f2fb83607c8e968d200cd4d3ee53618360f17c2296ce2c84c418163ad`
**LOC do arquivo-fonte:** 293


### `app_bundle.scripts.pipeline.prompt_manager:sanitize_general_execution_prompt`

**Assinatura:**
```python
def sanitize_general_execution_prompt(text: str) -> tuple[str, bool]
```

**Arquivo:** `app_bundle/scripts/pipeline/prompt_manager.py`
**Linhas:** 124–160
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Remove exigência inadequada de exposição de chain-of-thought.

**Docstring resumida:** Remove exigência inadequada de exposição de chain-of-thought.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `tuple[str, bool]`. Retornos observados estaticamente incluem: `(cleaned.strip() + '\n', cleaned != original)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `cleaned.strip`
- `re.sub`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prompt_manager:_read_prompt`
- `app_bundle.scripts.pipeline.prompt_manager:load_prompt_bundle`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c739b72f2fb83607c8e968d200cd4d3ee53618360f17c2296ce2c84c418163ad`
**LOC do arquivo-fonte:** 293


### `app_bundle.scripts.pipeline.prompt_manager:_read_prompt`

**Assinatura:**
```python
def _read_prompt(path: Path, *, category: str) -> tuple[str, bool]
```

**Arquivo:** `app_bundle/scripts/pipeline/prompt_manager.py`
**Linhas:** 163–168
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Lê a responsabilidade representada por `prompt` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `category` | keyword-only | `str` | `obrigatório/ausente` | Parâmetro `category` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `tuple[str, bool]`. Retornos observados estaticamente incluem: `(text.strip(), sanitized)`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `path.name.lower`
- `path.read_text`
- `sanitize_general_execution_prompt`
- `text.strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prompt_manager:load_prompt_bundle`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c739b72f2fb83607c8e968d200cd4d3ee53618360f17c2296ce2c84c418163ad`
**LOC do arquivo-fonte:** 293


### `app_bundle.scripts.pipeline.prompt_manager:_sha`

**Assinatura:**
```python
def _sha(text: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/prompt_manager.py`
**Linhas:** 171–172
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_sha` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `hashlib.sha256(text.encode('utf-8')).hexdigest()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `hashlib.sha256`
- `hashlib.sha256(text.encode('utf-8')).hexdigest`
- `text.encode`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prompt_manager:load_prompt_bundle`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c739b72f2fb83607c8e968d200cd4d3ee53618360f17c2296ce2c84c418163ad`
**LOC do arquivo-fonte:** 293


### `app_bundle.scripts.pipeline.prompt_manager:_add_prompt`

**Assinatura:**
```python
def _add_prompt(cfg: dict[str, Any], out: list[tuple[str, Path, str]], category: str, values: Any) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/prompt_manager.py`
**Linhas:** 175–184
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_add_prompt` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `out` | positional-or-keyword | `list[tuple[str, Path, str]]` | `obrigatório/ausente` | Parâmetro `out` recebido pela função; não há descrição individual explícita no código. |
| `category` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `category` recebido pela função; não há descrição individual explícita no código. |
| `values` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `values` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `_iter_values`
- `out.append`
- `resolve_prompt_path`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prompt_manager:load_prompt_bundle`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c739b72f2fb83607c8e968d200cd4d3ee53618360f17c2296ce2c84c418163ad`
**LOC do arquivo-fonte:** 293


### `app_bundle.scripts.pipeline.prompt_manager:_task_path_keys`

**Assinatura:**
```python
def _task_path_keys(task: str, document_type: str = '') -> list[tuple[str, str]]
```

**Arquivo:** `app_bundle/scripts/pipeline/prompt_manager.py`
**Linhas:** 187–203
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_task_path_keys` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `task` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `task` recebido pela função; não há descrição individual explícita no código. |
| `document_type` | positional-or-keyword | `str` | `''` | Parâmetro `document_type` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[tuple[str, str]]`. Retornos observados estaticamente incluem: `keys`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `(document_type or '').strip`
- `(document_type or '').strip().lower`
- `(task or 'document').strip`
- `(task or 'document').strip().lower`
- `keys.append`
- `keys.extend`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prompt_manager:load_prompt_bundle`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c739b72f2fb83607c8e968d200cd4d3ee53618360f17c2296ce2c84c418163ad`
**LOC do arquivo-fonte:** 293


### `app_bundle.scripts.pipeline.prompt_manager:load_prompt_bundle`

**Assinatura:**
```python
def load_prompt_bundle(cfg: dict[str, Any], task: str, *, document_type: str = '') -> PromptBundle
```

**Arquivo:** `app_bundle/scripts/pipeline/prompt_manager.py`
**Linhas:** 206–258
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Carrega a responsabilidade representada por `prompt_bundle` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `task` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `task` recebido pela função; não há descrição individual explícita no código. |
| `document_type` | keyword-only | `str` | `''` | Parâmetro `document_type` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `PromptBundle`. Retornos observados estaticamente incluem: `PromptBundle(task=task, text='', sources=[])`; `PromptBundle(task=task, text='\n\n'.join(chunks).strip(), sources=sources)`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `'\n\n'.join`
- `'\n\n'.join(chunks).strip`
- `PromptBundle`
- `PromptSource`
- `_add_prompt`
- `_cfg_section`
- `_read_prompt`
- `_sha`
- `_task_path_keys`
- `chunks.append`
- `cleaned.strip`
- `inline_entries.append`
- `key.replace`
- `label.startswith`
- `len`
- `path.exists`
- `prompts.get`
- `sanitize_general_execution_prompt`
- `seen.add`
- `set`
- `sources.append`
- `str`
- `str(prompts.get(inline_key) or '').strip`
- `str(prompts.get(key) or '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.bibliography_manager:infer_bib_metadata_ai`
- `app_bundle.scripts.pipeline.document_builder:_build_resumo_artigos_document_model_staged`
- `app_bundle.scripts.pipeline.document_builder:_common_generation_rules`
- `app_bundle.scripts.pipeline.document_builder:build_document_model`
- `app_bundle.scripts.pipeline.document_builder:build_prompt`
- `app_bundle.scripts.pipeline.mindmap_manager:attach_existing_mindmap_if_available`
- `app_bundle.scripts.pipeline.mindmap_manager:build_mindmap_prompt`
- `app_bundle.scripts.pipeline.mindmap_manager:generate_and_attach_mindmap`
- `app_bundle.scripts.pipeline.prisma_builder:build_prisma_report`
- `app_bundle.scripts.pipeline.prompt_manager:prompt_report_for_cfg`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 9
loops: 5
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c739b72f2fb83607c8e968d200cd4d3ee53618360f17c2296ce2c84c418163ad`
**LOC do arquivo-fonte:** 293


### `app_bundle.scripts.pipeline.prompt_manager:prompt_report_for_cfg`

**Assinatura:**
```python
def prompt_report_for_cfg(cfg: dict[str, Any]) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/prompt_manager.py`
**Linhas:** 261–265
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `prompt_report_for_cfg` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `{task: load_prompt_bundle(cfg, task, document_type=doc_type).report() for task in tasks}`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `_cfg_section`
- `doc.get`
- `load_prompt_bundle`
- `load_prompt_bundle(cfg, task, document_type=doc_type).report`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.diagnostics:make_run_report`
- `app_bundle.scripts.pipeline.prompt_lock:build_prompt_lock`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c739b72f2fb83607c8e968d200cd4d3ee53618360f17c2296ce2c84c418163ad`
**LOC do arquivo-fonte:** 293


### `app_bundle.scripts.pipeline.prompt_manager:validate_prompt_paths`

**Assinatura:**
```python
def validate_prompt_paths(cfg: dict[str, Any]) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/prompt_manager.py`
**Linhas:** 268–283
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Valida a responsabilidade representada por `prompt_paths` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `[]`; `errors`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `_cfg_section`
- `_iter_values`
- `errors.append`
- `p.exists`
- `prompts.get`
- `resolve_prompt_path`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.diagnostics:check_config`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c739b72f2fb83607c8e968d200cd4d3ee53618360f17c2296ce2c84c418163ad`
**LOC do arquivo-fonte:** 293


### `app_bundle.scripts.pipeline.prompt_manager:install_default_prompt_files`

**Assinatura:**
```python
def install_default_prompt_files(app_bundle: Path, source_dir: Path | None = None) -> dict[str, str]
```

**Arquivo:** `app_bundle/scripts/pipeline/prompt_manager.py`
**Linhas:** 286–293
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Cria a estrutura padrão de prompts. Útil para empacotamento/testes.

**Docstring resumida:** Cria a estrutura padrão de prompts. Útil para empacotamento/testes.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `app_bundle` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `app_bundle` recebido pela função; não há descrição individual explícita no código. |
| `source_dir` | positional-or-keyword | `Path \| None` | `None` | Parâmetro `source_dir` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, str]`. Retornos observados estaticamente incluem: `{'base': str(base)}`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `(base / 'document').mkdir`
- `(base / 'global').mkdir`
- `(base / 'prisma').mkdir`
- `(base / 'research').mkdir`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c739b72f2fb83607c8e968d200cd4d3ee53618360f17c2296ce2c84c418163ad`
**LOC do arquivo-fonte:** 293


### `app_bundle.scripts.pipeline.quality_report:_strip_non_visible_org_regions`

**Assinatura:**
```python
def _strip_non_visible_org_regions(text: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/quality_report.py`
**Linhas:** 34–39
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Remove regiões técnicas do Org antes da varredura de qualidade.

**Docstring resumida:** Remove regiões técnicas do Org antes da varredura de qualidade.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `text`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `re.sub`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.quality_report:_org_scan`
- `app_bundle.scripts.pipeline.quality_report:_technical_terms_found`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `ba57aa48ec5099d5c83c893354f4d2855fb3d153109d086f03b04841a2a705f6`
**LOC do arquivo-fonte:** 196


### `app_bundle.scripts.pipeline.quality_report:_technical_terms_found`

**Assinatura:**
```python
def _technical_terms_found(text: str) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/quality_report.py`
**Linhas:** 42–59
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Detecta termos técnicos proibidos evitando falso positivo por substring.

**Docstring resumida:** Detecta termos técnicos proibidos evitando falso positivo por substring.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `found`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_strip_non_visible_org_regions`
- `found.append`
- `normalize_title_loose`
- `re.escape`
- `re.search`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.quality_report:_org_scan`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `ba57aa48ec5099d5c83c893354f4d2855fb3d153109d086f03b04841a2a705f6`
**LOC do arquivo-fonte:** 196


### `app_bundle.scripts.pipeline.quality_report:_words`

**Assinatura:**
```python
def _words(text: str) -> int
```

**Arquivo:** `app_bundle/scripts/pipeline/quality_report.py`
**Linhas:** 62–63
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_words` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `len(re.findall('\\b\\w+\\b', text or '', flags=re.UNICODE))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `len`
- `re.findall`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.quality_report:build_quality_report`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `ba57aa48ec5099d5c83c893354f4d2855fb3d153109d086f03b04841a2a705f6`
**LOC do arquivo-fonte:** 196


### `app_bundle.scripts.pipeline.quality_report:_block_text`

**Assinatura:**
```python
def _block_text(block: Block) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/quality_report.py`
**Linhas:** 66–81
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_block_text` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `block` | positional-or-keyword | `Block` | `obrigatório/ausente` | Parâmetro `block` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `block.text`; `' '.join(parts)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `isinstance`
- `parts.append`
- `parts.extend`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.quality_report:_section_text`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `ba57aa48ec5099d5c83c893354f4d2855fb3d153109d086f03b04841a2a705f6`
**LOC do arquivo-fonte:** 196


### `app_bundle.scripts.pipeline.quality_report:_section_text`

**Assinatura:**
```python
def _section_text(section) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/quality_report.py`
**Linhas:** 84–85
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_section_text` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `section` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `section` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'\n'.join((_block_text(block) for block in section.blocks))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `_block_text`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.quality_report:build_quality_report`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `ba57aa48ec5099d5c83c893354f4d2855fb3d153109d086f03b04841a2a705f6`
**LOC do arquivo-fonte:** 196


### `app_bundle.scripts.pipeline.quality_report:_collect_citations`

**Assinatura:**
```python
def _collect_citations(document: AcademicDocument) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/quality_report.py`
**Linhas:** 88–97
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Coleta a responsabilidade representada por `citations` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `document` | positional-or-keyword | `AcademicDocument` | `obrigatório/ausente` | Parâmetro `document` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `keys`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `isinstance`
- `keys.append`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.quality_report:build_quality_report`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 4
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `ba57aa48ec5099d5c83c893354f4d2855fb3d153109d086f03b04841a2a705f6`
**LOC do arquivo-fonte:** 196


### `app_bundle.scripts.pipeline.quality_report:_org_scan`

**Assinatura:**
```python
def _org_scan(org_path: Path | None) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/quality_report.py`
**Linhas:** 100–111
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_org_scan` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `org_path` | positional-or-keyword | `Path \| None` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `{}`; `{'path': str(org_path), 'contains_empty_citation': '<empty citation>' in text, 'contains_org_cite': bool(re.search('\\[cite[:/]', text)), 'technical_terms_found': _technical_terms_found(text), 'raw_biblike_tokens': sorted(set(re.findall('\\b[a-z]+\\d{4}_[a-z0-9_]+\\b', visible)))[:50]}`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `_strip_non_visible_org_regions`
- `_technical_terms_found`
- `bool`
- `org_path.exists`
- `org_path.read_text`
- `re.findall`
- `re.search`
- `set`
- `sorted`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.quality_report:build_quality_report`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `ba57aa48ec5099d5c83c893354f4d2855fb3d153109d086f03b04841a2a705f6`
**LOC do arquivo-fonte:** 196


### `app_bundle.scripts.pipeline.quality_report:build_quality_report`

**Assinatura:**
```python
def build_quality_report(document: AcademicDocument, *, org_path: Path | None = None, bib_keys: list[str] | None = None) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/quality_report.py`
**Linhas:** 114–161
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Constrói a responsabilidade representada por `quality_report` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `document` | positional-or-keyword | `AcademicDocument` | `obrigatório/ausente` | Parâmetro `document` recebido pela função; não há descrição individual explícita no código. |
| `org_path` | keyword-only | `Path \| None` | `None` | Caminho de arquivo ou diretório utilizado pela operação. |
| `bib_keys` | keyword-only | `list[str] \| None` | `None` | Parâmetro `bib_keys` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `{'title': document.metadata.titulo, 'type': document.metadata.tipo_documento, 'total_words': total_words, 'sections': section_stats, 'citations_total': len(cited), 'cited_keys': cited, 'bibliography_entries_used_total': len(document.bibliography.entries_used or []), 'bibliography_entries_not_cited': not_cited, 'missing_in_bib': missing_in_bib, 'org_diagnostics': org_diagnostics, 'warnings': warnings, 'ok': not warnings}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `', '.join`
- `_collect_citations`
- `_org_scan`
- `_section_text`
- `_words`
- `any`
- `enumerate`
- `getattr`
- `len`
- `list`
- `normalize_title_loose`
- `org_diagnostics.get`
- `section_stats.append`
- `set`
- `sorted`
- `warnings.append`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `Há citações sem chave no .bib: `

#### Complexidade estrutural observada

```yaml
branches: 8
loops: 1
comprehensions: 4
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `ba57aa48ec5099d5c83c893354f4d2855fb3d153109d086f03b04841a2a705f6`
**LOC do arquivo-fonte:** 196


### `app_bundle.scripts.pipeline.quality_report:render_quality_markdown`

**Assinatura:**
```python
def render_quality_markdown(report: dict[str, Any]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/quality_report.py`
**Linhas:** 164–191
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Renderiza a responsabilidade representada por `quality_markdown` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `report` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `report` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'\n'.join(lines).rstrip() + '\n'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `'\n'.join(lines).rstrip`
- `lines.append`
- `report.get`
- `section.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.quality_report:write_quality_report`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `ba57aa48ec5099d5c83c893354f4d2855fb3d153109d086f03b04841a2a705f6`
**LOC do arquivo-fonte:** 196


### `app_bundle.scripts.pipeline.quality_report:write_quality_report`

**Assinatura:**
```python
def write_quality_report(report: dict[str, Any], output_md: Path) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/quality_report.py`
**Linhas:** 194–196
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Grava a responsabilidade representada por `quality_report` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `report` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `report` recebido pela função; não há descrição individual explícita no código. |
| `output_md` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `output_md` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `output_md.with_suffix`
- `render_quality_markdown`
- `write_json`
- `write_text`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.default_runtime:_ap003f_pipeline_core`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.json`

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `ba57aa48ec5099d5c83c893354f4d2855fb3d153109d086f03b04841a2a705f6`
**LOC do arquivo-fonte:** 196


### `app_bundle.scripts.pipeline.render_docx:parse_bib_entries`

**Assinatura:**
```python
def parse_bib_entries(text: str) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx.py`
**Linhas:** 36–58
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Interpreta a responsabilidade representada por `bib_entries` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `entries`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `entries.append`
- `len`
- `range`
- `text.find`
- `text[at:j + 1].strip`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `118720fdbedbad31e089de3018802c70ec7c4f0d608655a3abb80bd58fd207cb`
**LOC do arquivo-fonte:** 403


### `app_bundle.scripts.pipeline.render_docx:bib_key`

**Assinatura:**
```python
def bib_key(entry: str) -> str | None
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx.py`
**Linhas:** 61–63
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `bib_key` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `entry` | positional-or-keyword | `str` | `obrigatório/ausente` | Entrada bibliográfica ou registro individual. |

#### Retorno

Anotação declarada: `str | None`. Retornos observados estaticamente incluem: `m.group(1).strip() if m else None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `m.group`
- `m.group(1).strip`
- `re.match`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `118720fdbedbad31e089de3018802c70ec7c4f0d608655a3abb80bd58fd207cb`
**LOC do arquivo-fonte:** 403


### `app_bundle.scripts.pipeline.render_docx:field`

**Assinatura:**
```python
def field(entry: str, name: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx.py`
**Linhas:** 66–83
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `field` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `entry` | positional-or-keyword | `str` | `obrigatório/ausente` | Entrada bibliográfica ou registro individual. |
| `name` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `name` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `''`; `re.sub('\\s+', ' ', entry[start:j]).strip()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `entry[i].isspace`
- `len`
- `m.end`
- `range`
- `re.escape`
- `re.search`
- `re.sub`
- `re.sub('\\s+', ' ', entry[start:j]).strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.gerar_artigo_longo_fulltext_secional:parse_bib_entries`
- `app_bundle.scripts.pipeline.render_docx:bib_to_reference_map`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `118720fdbedbad31e089de3018802c70ec7c4f0d608655a3abb80bd58fd207cb`
**LOC do arquivo-fonte:** 403


### `app_bundle.scripts.pipeline.render_docx:bib_to_reference_map`

**Assinatura:**
```python
def bib_to_reference_map(bib_path: Path | None) -> dict[str, dict[str, str]]
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx.py`
**Linhas:** 86–95
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `bib_to_reference_map` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `bib_path` | positional-or-keyword | `Path \| None` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `dict[str, dict[str, str]]`. Retornos observados estaticamente incluem: `{}`; `out`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `bib_key`
- `bib_path.exists`
- `bib_path.read_text`
- `field`
- `parse_bib_entries`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx:render_docx`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `118720fdbedbad31e089de3018802c70ec7c4f0d608655a3abb80bd58fd207cb`
**LOC do arquivo-fonte:** 403


### `app_bundle.scripts.pipeline.render_docx:first_author_label`

**Assinatura:**
```python
def first_author_label(author: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx.py`
**Linhas:** 98–105
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `first_author_label` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `author` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `author` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'Autor'`; `first.split(',', 1)[0].strip()`; `parts[-1] if parts else first`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `first.split`
- `first.split(',', 1)[0].strip`
- `re.split`
- `re.split('\\s+and\\s+', author, flags=re.I)[0].strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx:citation_text`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `118720fdbedbad31e089de3018802c70ec7c4f0d608655a3abb80bd58fd207cb`
**LOC do arquivo-fonte:** 403


### `app_bundle.scripts.pipeline.render_docx:citation_text`

**Assinatura:**
```python
def citation_text(cit: Citation, refs: dict[str, dict[str, str]]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx.py`
**Linhas:** 108–121
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `citation_text` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cit` | positional-or-keyword | `Citation` | `obrigatório/ausente` | Parâmetro `cit` recebido pela função; não há descrição individual explícita no código. |
| `refs` | positional-or-keyword | `dict[str, dict[str, str]]` | `obrigatório/ausente` | Parâmetro `refs` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `''`; `labels[0]`; `', '.join(labels[:-1]) + ' e ' + labels[-1]`; `'(' + '; '.join(labels) + ')'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `', '.join`
- `'; '.join`
- `first_author_label`
- `labels.append`
- `len`
- `meta.get`
- `refs.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx:add_paragraph_from_content`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `118720fdbedbad31e089de3018802c70ec7c4f0d608655a3abb80bd58fd207cb`
**LOC do arquivo-fonte:** 403


### `app_bundle.scripts.pipeline.render_docx:reference_text`

**Assinatura:**
```python
def reference_text(key: str, meta: dict[str, str]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx.py`
**Linhas:** 124–142
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `reference_text` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `key` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `key` recebido pela função; não há descrição individual explícita no código. |
| `meta` | positional-or-keyword | `dict[str, str]` | `obrigatório/ausente` | Parâmetro `meta` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `' '.join(parts)`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `(meta.get('author') or meta.get('editor') or '').replace`
- `meta.get`
- `parts.append`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx:render_docx`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `https://doi.org/`

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `118720fdbedbad31e089de3018802c70ec7c4f0d608655a3abb80bd58fd207cb`
**LOC do arquivo-fonte:** 403


### `app_bundle.scripts.pipeline.render_docx:setup_styles`

**Assinatura:**
```python
def setup_styles(doc: Any) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx.py`
**Linhas:** 145–157
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `setup_styles` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doc` | positional-or-keyword | `Any` | `obrigatório/ausente` | Documento/registro individual processado pela rotina. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Pt`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx:render_docx`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `118720fdbedbad31e089de3018802c70ec7c4f0d608655a3abb80bd58fd207cb`
**LOC do arquivo-fonte:** 403


### `app_bundle.scripts.pipeline.render_docx:_paper_abstract_sidecar_exists`

**Assinatura:**
```python
def _paper_abstract_sidecar_exists(output_path: Path, cfg: dict[str, Any] | None) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx.py`
**Linhas:** 160–179
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Informa se o resumo auditável será inserido pelo pipeline após o DOCX.

**Docstring resumida:** Informa se o resumo auditável será inserido pelo pipeline após o DOCX.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `output_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `cfg` | positional-or-keyword | `dict[str, Any] \| None` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `False`; `True`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `(directory / f'{prefix}.resumos_paper.json').is_file`
- `bool`
- `cfg.get`
- `isinstance`
- `paths.get`
- `project.get`
- `section.get`
- `str`
- `str(paths.get('document_prefix') or '').strip`
- `str(project.get('preset') or '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx:render_docx`
- `app_bundle.scripts.pipeline.render_docx:render_markdown_for_pandoc`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.resumos_paper.json`

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `118720fdbedbad31e089de3018802c70ec7c4f0d608655a3abb80bd58fd207cb`
**LOC do arquivo-fonte:** 403


### `app_bundle.scripts.pipeline.render_docx:add_paragraph_from_content`

**Assinatura:**
```python
def add_paragraph_from_content(doc: Any, content: list[Any], refs: dict[str, dict[str, str]]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx.py`
**Linhas:** 182–193
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `add_paragraph_from_content` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doc` | positional-or-keyword | `Any` | `obrigatório/ausente` | Documento/registro individual processado pela rotina. |
| `content` | positional-or-keyword | `list[Any]` | `obrigatório/ausente` | Parâmetro `content` recebido pela função; não há descrição individual explícita no código. |
| `refs` | positional-or-keyword | `dict[str, dict[str, str]]` | `obrigatório/ausente` | Parâmetro `refs` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Cm`
- `bool`
- `citation_text`
- `doc.add_paragraph`
- `getattr`
- `isinstance`
- `p.add_run`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx:render_docx`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `118720fdbedbad31e089de3018802c70ec7c4f0d608655a3abb80bd58fd207cb`
**LOC do arquivo-fonte:** 403


### `app_bundle.scripts.pipeline.render_docx:_md_inlines`

**Assinatura:**
```python
def _md_inlines(content: list[Any]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx.py`
**Linhas:** 196–213
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_md_inlines` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `content` | positional-or-keyword | `list[Any]` | `obrigatório/ausente` | Parâmetro `content` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `''.join(parts)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `''.join`
- `'; '.join`
- `getattr`
- `isinstance`
- `parts.append`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx:render_markdown_for_pandoc`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 1
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `118720fdbedbad31e089de3018802c70ec7c4f0d608655a3abb80bd58fd207cb`
**LOC do arquivo-fonte:** 403


### `app_bundle.scripts.pipeline.render_docx:render_markdown_for_pandoc`

**Assinatura:**
```python
def render_markdown_for_pandoc(doc_model: AcademicDocument, out_dir: Path, cfg: dict[str, Any] | None = None) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx.py`
**Linhas:** 216–255
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Renderiza a responsabilidade representada por `markdown_for_pandoc` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doc_model` | positional-or-keyword | `AcademicDocument` | `obrigatório/ausente` | Identificador do modelo utilizado pela etapa de IA. |
| `out_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `out_dir` recebido pela função; não há descrição individual explícita no código. |
| `cfg` | positional-or-keyword | `dict[str, Any] \| None` | `None` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'\n'.join(lines)`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `'; '.join`
- `'\n'.join`
- `_md_inlines`
- `_paper_abstract_sidecar_exists`
- `block.text.replace`
- `enumerate`
- `is_ai_generated_reference_section_title`
- `lines.append`
- `max`
- `min`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx:_render_docx_with_pandoc`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `documento.docx`

#### Complexidade estrutural observada

```yaml
branches: 10
loops: 4
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `118720fdbedbad31e089de3018802c70ec7c4f0d608655a3abb80bd58fd207cb`
**LOC do arquivo-fonte:** 403


### `app_bundle.scripts.pipeline.render_docx:_render_docx_with_pandoc`

**Assinatura:**
```python
def _render_docx_with_pandoc(doc_model: AcademicDocument, output_path: Path, bib_path: Path | None, reference_docx: Path | None, cfg: dict[str, Any] | None) -> Path | None
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx.py`
**Linhas:** 258–282
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Renderiza a responsabilidade representada por `docx_with_pandoc` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doc_model` | positional-or-keyword | `AcademicDocument` | `obrigatório/ausente` | Identificador do modelo utilizado pela etapa de IA. |
| `output_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `bib_path` | positional-or-keyword | `Path \| None` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `reference_docx` | positional-or-keyword | `Path \| None` | `obrigatório/ausente` | Parâmetro `reference_docx` recebido pela função; não há descrição individual explícita no código. |
| `cfg` | positional-or-keyword | `dict[str, Any] \| None` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `Path | None`. Retornos observados estaticamente incluem: `None`; `output_path`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Executa processo ou ferramenta externa.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `(cfg.get('bibliografia', {}) if isinstance(cfg.get('bibliografia'), dict) else {}).get`
- `Path`
- `Path(str(cfg.get('__config_dir__') or output_path.parent)).resolve`
- `Path(str(raw_csl)).expanduser`
- `bib_path.exists`
- `cfg.get`
- `csl.exists`
- `csl.is_absolute`
- `diag.write_text`
- `docx_cfg.get`
- `isinstance`
- `md_path.write_text`
- `output_path.exists`
- `output_path.with_suffix`
- `reference_docx.exists`
- `render_markdown_for_pandoc`
- `shutil.which`
- `str`
- `subprocess.run`
- `tempfile.TemporaryDirectory`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx:render_docx`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `--bibliography`
- `--citeproc`
- `--csl`
- `--reference-doc`
- `--resource-path`

#### Artefatos/caminhos literais reconhecidos no corpo

- `documento.md`
- `.pandoc_docx_erro.txt`

#### Complexidade estrutural observada

```yaml
branches: 7
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `118720fdbedbad31e089de3018802c70ec7c4f0d608655a3abb80bd58fd207cb`
**LOC do arquivo-fonte:** 403


### `app_bundle.scripts.pipeline.render_docx:render_docx`

**Assinatura:**
```python
def render_docx(doc_model: AcademicDocument, output_path: Path, bib_path: Path | None = None, reference_docx: Path | None = None, cfg: dict[str, Any] | None = None) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx.py`
**Linhas:** 285–403
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Renderiza a responsabilidade representada por `docx` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doc_model` | positional-or-keyword | `AcademicDocument` | `obrigatório/ausente` | Identificador do modelo utilizado pela etapa de IA. |
| `output_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `bib_path` | positional-or-keyword | `Path \| None` | `None` | Caminho de arquivo ou diretório utilizado pela operação. |
| `reference_docx` | positional-or-keyword | `Path \| None` | `None` | Parâmetro `reference_docx` recebido pela função; não há descrição individual explícita no código. |
| `cfg` | positional-or-keyword | `dict[str, Any] \| None` | `None` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `rendered`; `output_path`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `'; '.join`
- `Cm`
- `Document`
- `Inches`
- `Path`
- `Pt`
- `RuntimeError`
- `_paper_abstract_sidecar_exists`
- `_render_docx_with_pandoc`
- `add_paragraph_from_content`
- `bib_to_reference_map`
- `bool`
- `cfg.get`
- `doc.add_heading`
- `doc.add_page_break`
- `doc.add_paragraph`
- `doc.add_picture`
- `doc.add_table`
- `doc.save`
- `doc_model.abstract.titulo.upper`
- `docx_cfg.get`
- `enumerate`
- `fig.title.upper`
- `img.exists`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError(f"Falha ao renderizar DOCX via Pandoc. Veja {output_path.with_suffix('.pandoc_docx_erro.txt')}")`
- `RuntimeError('python-docx não está instalado.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.pandoc_docx_erro.txt`

#### Complexidade estrutural observada

```yaml
branches: 34
loops: 10
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `118720fdbedbad31e089de3018802c70ec7c4f0d608655a3abb80bd58fd207cb`
**LOC do arquivo-fonte:** 403


### `app_bundle.scripts.pipeline.render_docx_canonico:die`

**Assinatura:**
```python
def die(msg: str) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 78–79
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `die` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `msg` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `msg` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `SystemExit`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:render_docx_for_article`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `SystemExit(msg)`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:now_stamp`

**Assinatura:**
```python
def now_stamp() -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 82–83
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `now_stamp` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `datetime.now().strftime('%Y%m%d_%H%M%S')`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `datetime.now`
- `datetime.now().strftime`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:backup`

**Assinatura:**
```python
def backup(path: Path, tag: str, quiet: bool = False) -> Path | None
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 86–93
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `backup` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `tag` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `tag` recebido pela função; não há descrição individual explícita no código. |
| `quiet` | positional-or-keyword | `bool` | `False` | Parâmetro `quiet` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path | None`. Retornos observados estaticamente incluem: `None`; `bak`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `now_stamp`
- `path.exists`
- `path.with_name`
- `print`
- `shutil.copy2`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:read_text`

**Assinatura:**
```python
def read_text(path: Path) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 96–99
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Lê a responsabilidade representada por `text` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `''`; `path.read_text(encoding='utf-8', errors='ignore')`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `path.exists`
- `path.read_text`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:read_json`

**Assinatura:**
```python
def read_json(path: Path) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 102–109
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Lê a responsabilidade representada por `json` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `{}`; `data if isinstance(data, dict) else {}`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `isinstance`
- `json.loads`
- `path.exists`
- `path.read_text`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:read_toml`

**Assinatura:**
```python
def read_toml(path: Path | None) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 112–119
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Lê a responsabilidade representada por `toml` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path \| None` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `{}`; `data if isinstance(data, dict) else {}`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `isinstance`
- `path.exists`
- `path.read_text`
- `tomllib.loads`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:flatten_dict`

**Assinatura:**
```python
def flatten_dict(obj: Any, prefix: str = '') -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 122–132
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `flatten_dict` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `obj` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `obj` recebido pela função; não há descrição individual explícita no código. |
| `prefix` | positional-or-keyword | `str` | `''` | Prefixo canônico usado para compor nomes de artefatos. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `out`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `enumerate`
- `flatten_dict`
- `isinstance`
- `obj.items`
- `out.update`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:first_by_keys`
- `app_bundle.scripts.pipeline.render_docx_canonico:flatten_dict`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:first_by_keys`

**Assinatura:**
```python
def first_by_keys(data: dict[str, Any], keys: set[str]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 135–144
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `first_by_keys` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `data` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Estrutura de dados de entrada. |
| `keys` | positional-or-keyword | `set[str]` | `obrigatório/ausente` | Parâmetro `keys` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'; '.join((str(x) for x in value if str(x).strip()))`; `str(value).strip()`; `''`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'; '.join`
- `flat.items`
- `flatten_dict`
- `isinstance`
- `path_key.rsplit`
- `path_key.rsplit('.', 1)[-1].lower`
- `str`
- `str(value).strip`
- `str(x).strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:build_metadata`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:strip_accents_for_sort`

**Assinatura:**
```python
def strip_accents_for_sort(s: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 147–152
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Remove a responsabilidade representada por `accents_for_sort` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `s` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `s` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `''.join((c for c in unicodedata.normalize('NFKD', s) if not unicodedata.combining(c))).lower()`; `s.lower()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `''.join`
- `''.join((c for c in unicodedata.normalize('NFKD', s) if not unicodedata.combining(c))).lower`
- `s.lower`
- `unicodedata.combining`
- `unicodedata.normalize`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:add_cover`
- `app_bundle.scripts.pipeline.render_docx_canonico:build_metadata`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:clean_spaces`

**Assinatura:**
```python
def clean_spaces(s: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 156–170
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Normaliza espaços e remove resíduos típicos de LaTeX/Org no texto visível.

**Docstring resumida:** Normaliza espaços e remove resíduos típicos de LaTeX/Org no texto visível.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `s` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `s` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `''`; `s.strip()`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `re.sub`
- `s.replace`
- `s.replace('\r\n', '\n').replace`
- `s.strip`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:add_cover`
- `app_bundle.scripts.pipeline.render_docx_canonico:bibtex_unescape`
- `app_bundle.scripts.pipeline.render_docx_canonico:clean_latex_inline`
- `app_bundle.scripts.pipeline.render_docx_canonico:invalid_cover_value`
- `app_bundle.scripts.pipeline.render_docx_canonico:parse_document_json_blocks`
- `app_bundle.scripts.pipeline.render_docx_canonico:parse_org_blocks.flush_paragraph`
- `app_bundle.scripts.pipeline.render_docx_canonico:reference_line`
- `app_bundle.scripts.pipeline.render_docx_canonico:render_docx_for_article`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:guess_prefix`

**Assinatura:**
```python
def guess_prefix(art_dir: Path, explicit: str | None = None) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 177–187
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `guess_prefix` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `art_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `art_dir` recebido pela função; não há descrição individual explícita no código. |
| `explicit` | positional-or-keyword | `str \| None` | `None` | Parâmetro `explicit` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `explicit`; `candidates[0].name.replace('.document.json', '')`; `tomls[0].stem`; `'artigo_final_atestmed_abnt'`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `art_dir.glob`
- `candidates[0].name.replace`
- `out.exists`
- `out.glob`
- `sorted`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:resolve_paths`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `*.document.json`
- `.document.json`
- `*.toml`

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:find_cfg`

**Assinatura:**
```python
def find_cfg(art_dir: Path, prefix: str, explicit: Path | None = None) -> Path | None
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 190–197
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Localiza a responsabilidade representada por `cfg` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `art_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `art_dir` recebido pela função; não há descrição individual explícita no código. |
| `prefix` | positional-or-keyword | `str` | `obrigatório/ausente` | Prefixo canônico usado para compor nomes de artefatos. |
| `explicit` | positional-or-keyword | `Path \| None` | `None` | Parâmetro `explicit` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path | None`. Retornos observados estaticamente incluem: `explicit`; `p`; `tomls[0] if tomls else explicit`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `art_dir.glob`
- `explicit.exists`
- `p.exists`
- `sorted`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:resolve_paths`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.toml`
- `*.toml`

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:resolve_paths`

**Assinatura:**
```python
def resolve_paths(art_dir: Path, cfg_art: Path | None, prefix: str | None, output: Path | None = None) -> ArticlePaths
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 200–219
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Resolve a responsabilidade representada por `paths` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `art_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `art_dir` recebido pela função; não há descrição individual explícita no código. |
| `cfg_art` | positional-or-keyword | `Path \| None` | `obrigatório/ausente` | Parâmetro `cfg_art` recebido pela função; não há descrição individual explícita no código. |
| `prefix` | positional-or-keyword | `str \| None` | `obrigatório/ausente` | Prefixo canônico usado para compor nomes de artefatos. |
| `output` | positional-or-keyword | `Path \| None` | `None` | Parâmetro `output` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `ArticlePaths`. Retornos observados estaticamente incluem: `ArticlePaths(art_dir=art_dir, output_dir=out, prefix=pfx, org=out / f'{pfx}.org', bib=out / f'{pfx}.bib', document_json=out / f'{pfx}.document.json', resumos_json=out / f'{pfx}.resumos_paper.json', compliance_json=out / f'{pfx}.compliance_report.json', run_report_json=out / f'{pfx}.run_report.json', cfg=cfg, docx=docx, canonical_json=out / f'{pfx}.canonical_docx.json')`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `(art_dir / 'output').resolve`
- `ArticlePaths`
- `art_dir.resolve`
- `cfg_art.resolve`
- `find_cfg`
- `guess_prefix`
- `output.parent.resolve`
- `output.resolve`
- `output.suffix.lower`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:render_docx_for_article`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.docx`
- `.org`
- `.bib`
- `.document.json`
- `.resumos_paper.json`
- `.compliance_report.json`
- `.run_report.json`
- `.canonical_docx.json`

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:parse_org_metadata`

**Assinatura:**
```python
def parse_org_metadata(org_text: str) -> dict[str, str]
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 228–238
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Interpreta a responsabilidade representada por `org_metadata` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `org_text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `dict[str, str]`. Retornos observados estaticamente incluem: `meta`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `line.strip`
- `m.group`
- `m.group(1).lower`
- `m.group(1).lower().replace`
- `m.group(2).strip`
- `org_text.splitlines`
- `re.match`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:build_metadata`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:parse_org_latex_header_macros`

**Assinatura:**
```python
def parse_org_latex_header_macros(org_text: str) -> dict[str, str]
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 241–260
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Extrai metadados de capa declarados em #+LATEX_HEADER.

**Docstring resumida:** Extrai metadados de capa declarados em #+LATEX_HEADER.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `org_text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `dict[str, str]`. Retornos observados estaticamente incluem: `macros`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `clean_latex_inline`
- `line.split`
- `line.strip`
- `line.strip().lower`
- `line.strip().lower().startswith`
- `org_text.splitlines`
- `re.findall`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:build_metadata`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:invalid_cover_value`

**Assinatura:**
```python
def invalid_cover_value(value: str) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 263–276
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `invalid_cover_value` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `True`; `False`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `clean_spaces`
- `clean_spaces(str(value)).strip`
- `re.fullmatch`
- `str`
- `v.lower`
- `v.startswith`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:add_cover`
- `app_bundle.scripts.pipeline.render_docx_canonico:first_valid`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:first_valid`

**Assinatura:**
```python
def first_valid(*values: str, default: str = '') -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 279–286
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `first_valid` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `values` | var-positional | `str` | `obrigatório/ausente` | Parâmetro `values` recebido pela função; não há descrição individual explícita no código. |
| `default` | keyword-only | `str` | `''` | Parâmetro `default` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `v`; `default`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `clean_latex_inline`
- `invalid_cover_value`
- `str`
- `str(value).strip`
- `str(value).strip().strip`
- `str(value).strip().strip('"').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:build_metadata`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:build_metadata`

**Assinatura:**
```python
def build_metadata(paths: ArticlePaths, org_text: str, document_json: dict[str, Any], toml_data: dict[str, Any]) -> dict[str, str]
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 289–342
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Constrói a responsabilidade representada por `metadata` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `paths` | positional-or-keyword | `ArticlePaths` | `obrigatório/ausente` | Parâmetro `paths` recebido pela função; não há descrição individual explícita no código. |
| `org_text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |
| `document_json` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `document_json` recebido pela função; não há descrição individual explícita no código. |
| `toml_data` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Estrutura de dados de entrada. |

#### Retorno

Anotação declarada: `dict[str, str]`. Retornos observados estaticamente incluem: `{'title': title, 'subtitle': subtitle, 'author': author, 'institution': institution, 'program': program, 'course': course, 'discipline': discipline, 'professor': professor, 'city': city, 'year': year, 'covernote': covernote, 'papertype': papertype}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `datetime.now`
- `document_json.get`
- `first_by_keys`
- `first_valid`
- `latex_meta.get`
- `merged.update`
- `org_meta.get`
- `parse_org_latex_header_macros`
- `parse_org_metadata`
- `str`
- `strip_accents_for_sort`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:render_docx_for_article`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:_strip_outer`

**Assinatura:**
```python
def _strip_outer(value: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 349–353
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Remove a responsabilidade representada por `outer` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `value.strip()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `value.endswith`
- `value.startswith`
- `value.strip`
- `value.strip().rstrip`
- `value.strip().rstrip(',').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:bibtex_unescape`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:bibtex_unescape`

**Assinatura:**
```python
def bibtex_unescape(value: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 356–377
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `bibtex_unescape` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `clean_spaces(value)`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `_strip_outer`
- `clean_spaces`
- `re.sub`
- `replacements.items`
- `value.replace`
- `value.replace('{', '').replace`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:author_abnt`
- `app_bundle.scripts.pipeline.render_docx_canonico:parse_bib_entries`
- `app_bundle.scripts.pipeline.render_docx_canonico:split_authors`
- `app_bundle.scripts.pipeline.render_docx_canonico:surname`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:parse_bib_entries`

**Assinatura:**
```python
def parse_bib_entries(bib: Path) -> dict[str, dict[str, str]]
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 380–447
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Interpreta a responsabilidade representada por `bib_entries` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `bib` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `bib` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, dict[str, str]]`. Retornos observados estaticamente incluem: `entries`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `bibtex_unescape`
- `fields.get`
- `fm.end`
- `fm.group`
- `fm.group(1).lower`
- `len`
- `m.end`
- `m.group`
- `m.group(1).lower`
- `m.group(2).strip`
- `m.start`
- `max`
- `re.search`
- `read_text`
- `ym.group`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 12
loops: 6
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:split_authors`

**Assinatura:**
```python
def split_authors(author_field: str) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 450–460
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `split_authors` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `author_field` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `author_field` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `[]`; `[p.strip() for p in parts if p.strip()]`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `author_field.split`
- `bibtex_unescape`
- `p.strip`
- `re.split`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:surname`

**Assinatura:**
```python
def surname(author: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 463–475
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `surname` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `author` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `author` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `''`; `author.split(',', 1)[0].strip()`; `parts[0]`; `' '.join(parts[-2:])`; `parts[-1]`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `author.split`
- `author.split(',', 1)[0].strip`
- `bibtex_unescape`
- `len`
- `parts[-2].lower`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:author_abnt`
- `app_bundle.scripts.pipeline.render_docx_canonico:citation_label`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:author_abnt`

**Assinatura:**
```python
def author_abnt(author: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 478–491
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `author_abnt` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `author` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `author` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `''`; `f'{last_up}, {first}'.strip().rstrip(',')`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `author.split`
- `author[:max(0, len(author) - len(last))].strip`
- `bibtex_unescape`
- `f'{last_up}, {first}'.strip`
- `f'{last_up}, {first}'.strip().rstrip`
- `last.upper`
- `len`
- `max`
- `p.strip`
- `surname`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:authors_abnt`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:authors_abnt`

**Assinatura:**
```python
def authors_abnt(fields: dict[str, str]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 494–502
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `authors_abnt` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `fields` | positional-or-keyword | `dict[str, str]` | `obrigatório/ausente` | Parâmetro `fields` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `(fields.get('organization') or fields.get('institution') or fields.get('id') or 'REFERÊNCIA').upper()`; `'; '.join([x for x in formatted if x])`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'; '.join`
- `(fields.get('organization') or fields.get('institution') or fields.get('id') or 'REFERÊNCIA').upper`
- `author_abnt`
- `fields.get`
- `split_authors`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:reference_line`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:citation_label`

**Assinatura:**
```python
def citation_label(fields: dict[str, str], textual: bool = False) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 505–520
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `citation_label` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `fields` | positional-or-keyword | `dict[str, str]` | `obrigatório/ausente` | Parâmetro `fields` recebido pela função; não há descrição individual explícita no código. |
| `textual` | positional-or-keyword | `bool` | `False` | Parâmetro `textual` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `f'{label} ({year})' if textual else f'{label}, {year}'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `fields.get`
- `len`
- `split_authors`
- `surname`
- `title.split`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:cite_keys`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 7
loops: 0
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:cite_keys`

**Assinatura:**
```python
def cite_keys(keys: str, entries: dict[str, dict[str, str]], textual: bool = False) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 523–532
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `cite_keys` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `keys` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `keys` recebido pela função; não há descrição individual explícita no código. |
| `entries` | positional-or-keyword | `dict[str, dict[str, str]]` | `obrigatório/ausente` | Coleção de entradas bibliográficas/registros. |
| `textual` | positional-or-keyword | `bool` | `False` | Parâmetro `textual` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'; '.join(parts)`; `'(' + '; '.join(parts) + ')' if parts else ''`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'; '.join`
- `citation_label`
- `entries.get`
- `key.strip`
- `keys.split`
- `parts.append`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:materialize_citations`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:materialize_citations`

**Assinatura:**
```python
def materialize_citations(text: str, entries: dict[str, dict[str, str]]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 535–540
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `materialize_citations` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |
| `entries` | positional-or-keyword | `dict[str, dict[str, str]]` | `obrigatório/ausente` | Coleção de entradas bibliográficas/registros. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `text`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `cite_keys`
- `m.group`
- `m.group(1).replace`
- `re.sub`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:parse_document_json_blocks`
- `app_bundle.scripts.pipeline.render_docx_canonico:parse_org_blocks`
- `app_bundle.scripts.pipeline.render_docx_canonico:parse_org_blocks.flush_paragraph`
- `app_bundle.scripts.pipeline.render_docx_canonico:parse_org_blocks.flush_table`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:reference_line`

**Assinatura:**
```python
def reference_line(fields: dict[str, str]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 543–579
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `reference_line` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `fields` | positional-or-keyword | `dict[str, str]` | `obrigatório/ausente` | Parâmetro `fields` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `clean_spaces(' '.join(chunks))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `', '.join`
- `authors_abnt`
- `chunks.append`
- `clean_spaces`
- `fields.get`
- `str`
- `vparts.append`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:add_references`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 9
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:clean_latex_inline`

**Assinatura:**
```python
def clean_latex_inline(s: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 588–619
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Limpa a responsabilidade representada por `latex_inline` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `s` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `s` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `''`; `clean_spaces(s)`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `clean_spaces`
- `range`
- `re.sub`
- `s.replace`
- `s.replace('\\%', '%').replace`
- `s.replace('\\%', '%').replace('\\&', '&').replace`
- `s.replace('\\LaTeX{}', 'LaTeX').replace`
- `s.replace('\ufffe', '-').replace`
- `s.replace('{', '').replace`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:_clean_abstract_piece`
- `app_bundle.scripts.pipeline.render_docx_canonico:_split_keywords`
- `app_bundle.scripts.pipeline.render_docx_canonico:extract_org_abstracts`
- `app_bundle.scripts.pipeline.render_docx_canonico:extract_resumos`
- `app_bundle.scripts.pipeline.render_docx_canonico:first_valid`
- `app_bundle.scripts.pipeline.render_docx_canonico:parse_document_json_blocks`
- `app_bundle.scripts.pipeline.render_docx_canonico:parse_org_blocks`
- `app_bundle.scripts.pipeline.render_docx_canonico:parse_org_blocks.flush_paragraph`
- `app_bundle.scripts.pipeline.render_docx_canonico:parse_org_blocks.flush_table`
- `app_bundle.scripts.pipeline.render_docx_canonico:parse_org_latex_header_macros`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:line_is_metadata_or_latex`

**Assinatura:**
```python
def line_is_metadata_or_latex(line: str) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 622–633
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `line_is_metadata_or_latex` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `line` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `line` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `False`; `True`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `line.strip`
- `low.startswith`
- `re.match`
- `stripped.lower`
- `stripped.startswith`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:parse_org_blocks`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:_extract_export_latex_blocks`

**Assinatura:**
```python
def _extract_export_latex_blocks(org_text: str) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 636–641
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Extrai a responsabilidade representada por `export_latex_blocks` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `org_text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `blocks`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `blocks.append`
- `m.group`
- `pat.finditer`
- `re.compile`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:extract_org_abstracts`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:_clean_abstract_piece`

**Assinatura:**
```python
def _clean_abstract_piece(s: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 644–647
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Limpa a responsabilidade representada por `abstract_piece` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `s` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `s` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `clean_latex_inline(s)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `clean_latex_inline`
- `re.sub`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:extract_org_abstracts`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:_split_keywords`

**Assinatura:**
```python
def _split_keywords(raw: str) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 650–653
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_split_keywords` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `raw` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `raw` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `[clean_latex_inline(x).rstrip('.') for x in re.split('[,;]', raw) if clean_latex_inline(x).rstrip('.')]`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `clean_latex_inline`
- `clean_latex_inline(x).rstrip`
- `re.split`
- `re.sub`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:extract_org_abstracts`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:extract_org_abstracts`

**Assinatura:**
```python
def extract_org_abstracts(org_text: str) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 656–702
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Extrai Resumo/Abstract do ORG final, inclusive de blocos LaTeX exportados.

**Docstring resumida:** Extrai Resumo/Abstract do ORG final, inclusive de blocos LaTeX exportados.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `org_text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `{k: v for k, v in result.items() if v}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'\n\n'.join`
- `_clean_abstract_piece`
- `_extract_export_latex_blocks`
- `_split_keywords`
- `clean_latex_inline`
- `m.group`
- `re.search`
- `result.items`
- `result.pop`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:extract_resumos`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 2
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:extract_resumos`

**Assinatura:**
```python
def extract_resumos(resumos_json: dict[str, Any], org_text: str) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 704–717
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Extrai a responsabilidade representada por `resumos` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `resumos_json` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `resumos_json` recebido pela função; não há descrição individual explícita no código. |
| `org_text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `out`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `clean_latex_inline`
- `en.get`
- `extract_org_abstracts`
- `isinstance`
- `items.get`
- `pt.get`
- `resumos_json.get`
- `str`
- `str(x).strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:render_docx_for_article`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 0
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:parse_org_blocks`

**Assinatura:**
```python
def parse_org_blocks(org_text: str, entries: dict[str, dict[str, str]]) -> list[dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 721–813
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Interpreta a responsabilidade representada por `org_blocks` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `org_text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |
| `entries` | positional-or-keyword | `dict[str, dict[str, str]]` | `obrigatório/ausente` | Coleção de entradas bibliográficas/registros. |

#### Retorno

Anotação declarada: `list[dict[str, Any]]`. Retornos observados estaticamente incluem: `blocks`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `blocks.append`
- `clean_latex_inline`
- `flush_paragraph`
- `flush_table`
- `len`
- `line.strip`
- `line_is_metadata_or_latex`
- `m.group`
- `materialize_citations`
- `min`
- `org_text.splitlines`
- `paragraph_lines.append`
- `raw.rstrip`
- `re.match`
- `stripped.endswith`
- `stripped.lower`
- `stripped.startswith`
- `table_lines.append`
- `title.lower`
- `{'section': 1, 'subsection': 2, 'subsubsection': 3}.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:build_body_blocks`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 15
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:parse_org_blocks.flush_paragraph`

**Assinatura:**
```python
def flush_paragraph() -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 729–749
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `flush_paragraph` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `blocks.append`
- `clean_latex_inline`
- `clean_spaces`
- `low.startswith`
- `materialize_citations`
- `text.lower`
- `text.lower().strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:parse_org_blocks`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:parse_org_blocks.flush_table`

**Assinatura:**
```python
def flush_table() -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 751–764
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `flush_table` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `any`
- `blocks.append`
- `c.strip`
- `clean_latex_inline`
- `ln.strip`
- `ln.strip().strip`
- `ln.strip().strip('|').split`
- `materialize_citations`
- `re.match`
- `rows.append`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:parse_org_blocks`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 1
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:parse_document_json_blocks`

**Assinatura:**
```python
def parse_document_json_blocks(document_json: dict[str, Any], entries: dict[str, dict[str, str]]) -> list[dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 815–836
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Interpreta a responsabilidade representada por `document_json_blocks` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `document_json` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `document_json` recebido pela função; não há descrição individual explícita no código. |
| `entries` | positional-or-keyword | `dict[str, dict[str, str]]` | `obrigatório/ausente` | Coleção de entradas bibliográficas/registros. |

#### Retorno

Anotação declarada: `list[dict[str, Any]]`. Retornos observados estaticamente incluem: `blocks`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `blocks.append`
- `clean_latex_inline`
- `clean_spaces`
- `document_json.get`
- `isinstance`
- `materialize_citations`
- `part.lower`
- `re.split`
- `re.sub`
- `sec.get`
- `str`
- `text.strip`
- `title.lower`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:build_body_blocks`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:build_body_blocks`

**Assinatura:**
```python
def build_body_blocks(org_text: str, document_json: dict[str, Any], entries: dict[str, dict[str, str]]) -> list[dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 839–844
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Constrói a responsabilidade representada por `body_blocks` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `org_text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |
| `document_json` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `document_json` recebido pela função; não há descrição individual explícita no código. |
| `entries` | positional-or-keyword | `dict[str, dict[str, str]]` | `obrigatório/ausente` | Coleção de entradas bibliográficas/registros. |

#### Retorno

Anotação declarada: `list[dict[str, Any]]`. Retornos observados estaticamente incluem: `org_blocks`; `parse_document_json_blocks(document_json, entries)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `b.get`
- `len`
- `parse_document_json_blocks`
- `parse_org_blocks`
- `sum`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:render_docx_for_article`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:set_run_font`

**Assinatura:**
```python
def set_run_font(run: Any, name: str = 'Times New Roman', size_pt: int = 12, bold: bool | None = None, italic: bool | None = None) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 852–872
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `set_run_font` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `run` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `run` recebido pela função; não há descrição individual explícita no código. |
| `name` | positional-or-keyword | `str` | `'Times New Roman'` | Parâmetro `name` recebido pela função; não há descrição individual explícita no código. |
| `size_pt` | positional-or-keyword | `int` | `12` | Parâmetro `size_pt` recebido pela função; não há descrição individual explícita no código. |
| `bold` | positional-or-keyword | `bool \| None` | `None` | Parâmetro `bold` recebido pela função; não há descrição individual explícita no código. |
| `italic` | positional-or-keyword | `bool \| None` | `None` | Parâmetro `italic` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `OxmlElement`
- `Pt`
- `RGBColor`
- `qn`
- `rfonts.set`
- `rpr.append`
- `run._element.get_or_add_rPr`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:add_abstracts`
- `app_bundle.scripts.pipeline.render_docx_canonico:add_body_paragraph`
- `app_bundle.scripts.pipeline.render_docx_canonico:add_centered_paragraph`
- `app_bundle.scripts.pipeline.render_docx_canonico:add_cover`
- `app_bundle.scripts.pipeline.render_docx_canonico:add_heading`
- `app_bundle.scripts.pipeline.render_docx_canonico:add_references`
- `app_bundle.scripts.pipeline.render_docx_canonico:add_table`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:set_style_font`

**Assinatura:**
```python
def set_style_font(style: Any, name: str = 'Times New Roman', size_pt: int = 12, bold: bool | None = None) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 875–891
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `set_style_font` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `style` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `style` recebido pela função; não há descrição individual explícita no código. |
| `name` | positional-or-keyword | `str` | `'Times New Roman'` | Parâmetro `name` recebido pela função; não há descrição individual explícita no código. |
| `size_pt` | positional-or-keyword | `int` | `12` | Parâmetro `size_pt` recebido pela função; não há descrição individual explícita no código. |
| `bold` | positional-or-keyword | `bool \| None` | `None` | Parâmetro `bold` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `OxmlElement`
- `Pt`
- `RGBColor`
- `qn`
- `rfonts.set`
- `rpr.append`
- `style._element.get_or_add_rPr`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:configure_doc_styles`

**Assinatura:**
```python
def configure_doc_styles(doc: Any, layout: dict[str, Any]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 894–934
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `configure_doc_styles` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doc` | positional-or-keyword | `Any` | `obrigatório/ausente` | Documento/registro individual processado pela rotina. |
| `layout` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Layout institucional/documental selecionado. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Cm`
- `Pt`
- `float`
- `int`
- `isinstance`
- `layout.get`
- `margins.get`
- `set_style_font`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:render_docx_for_article`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:add_centered_paragraph`

**Assinatura:**
```python
def add_centered_paragraph(doc: Any, text: str, *, bold: bool = False, size: int = 12, before: int = 0, after: int = 0) -> Any
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 937–946
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `add_centered_paragraph` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doc` | positional-or-keyword | `Any` | `obrigatório/ausente` | Documento/registro individual processado pela rotina. |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |
| `bold` | keyword-only | `bool` | `False` | Parâmetro `bold` recebido pela função; não há descrição individual explícita no código. |
| `size` | keyword-only | `int` | `12` | Parâmetro `size` recebido pela função; não há descrição individual explícita no código. |
| `before` | keyword-only | `int` | `0` | Parâmetro `before` recebido pela função; não há descrição individual explícita no código. |
| `after` | keyword-only | `int` | `0` | Parâmetro `after` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Any`. Retornos observados estaticamente incluem: `p`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Cm`
- `Pt`
- `doc.add_paragraph`
- `p.add_run`
- `set_run_font`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:add_cover`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:add_body_paragraph`

**Assinatura:**
```python
def add_body_paragraph(doc: Any, text: str, *, first_indent: bool = True, align: Any = WD_ALIGN_PARAGRAPH.JUSTIFY) -> Any
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 949–958
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `add_body_paragraph` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doc` | positional-or-keyword | `Any` | `obrigatório/ausente` | Documento/registro individual processado pela rotina. |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |
| `first_indent` | keyword-only | `bool` | `True` | Parâmetro `first_indent` recebido pela função; não há descrição individual explícita no código. |
| `align` | keyword-only | `Any` | `WD_ALIGN_PARAGRAPH.JUSTIFY` | Parâmetro `align` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Any`. Retornos observados estaticamente incluem: `p`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Cm`
- `Pt`
- `doc.add_paragraph`
- `p.add_run`
- `set_run_font`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:add_abstracts`
- `app_bundle.scripts.pipeline.render_docx_canonico:render_docx_for_article`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:add_heading`

**Assinatura:**
```python
def add_heading(doc: Any, text: str, level: int = 1) -> Any
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 961–968
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `add_heading` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doc` | positional-or-keyword | `Any` | `obrigatório/ausente` | Documento/registro individual processado pela rotina. |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |
| `level` | positional-or-keyword | `int` | `1` | Parâmetro `level` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Any`. Retornos observados estaticamente incluem: `p`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Cm`
- `doc.add_heading`
- `max`
- `min`
- `p.add_run`
- `set_run_font`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx:render_docx`
- `app_bundle.scripts.pipeline.render_docx_canonico:add_abstracts`
- `app_bundle.scripts.pipeline.render_docx_canonico:add_heading`
- `app_bundle.scripts.pipeline.render_docx_canonico:add_references`
- `app_bundle.scripts.pipeline.render_docx_canonico:render_docx_for_article`
- `app_bundle.scripts.pipeline.render_prisma_docx:render_prisma_docx`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:add_cover`

**Assinatura:**
```python
def add_cover(doc: Any, metadata: dict[str, str]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 972–1027
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `add_cover` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doc` | positional-or-keyword | `Any` | `obrigatório/ausente` | Documento/registro individual processado pela rotina. |
| `metadata` | positional-or-keyword | `dict[str, str]` | `obrigatório/ausente` | Parâmetro `metadata` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `' — '.join`
- `Cm`
- `Pt`
- `add_centered_paragraph`
- `clean_spaces`
- `datetime.now`
- `doc.add_page_break`
- `doc.add_paragraph`
- `enumerate`
- `inst_lines.append`
- `invalid_cover_value`
- `line.upper`
- `metadata.get`
- `metadata['professor'].strip`
- `note.split`
- `p.add_run`
- `p.add_run().add_break`
- `parts.append`
- `professor.lower`
- `professor.lower().startswith`
- `seen.add`
- `set`
- `set_run_font`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:render_docx_for_article`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 12
loops: 3
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:add_abstracts`

**Assinatura:**
```python
def add_abstracts(doc: Any, abstracts: dict[str, Any]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 1029–1057
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `add_abstracts` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doc` | positional-or-keyword | `Any` | `obrigatório/ausente` | Documento/registro individual processado pela rotina. |
| `abstracts` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `abstracts` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'; '.join`
- `Cm`
- `abstracts.get`
- `add_body_paragraph`
- `add_heading`
- `doc.add_paragraph`
- `p.add_run`
- `set_run_font`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:render_docx_for_article`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:add_table`

**Assinatura:**
```python
def add_table(doc: Any, rows: list[list[str]]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 1060–1078
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `add_table` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doc` | positional-or-keyword | `Any` | `obrigatório/ausente` | Documento/registro individual processado pela rotina. |
| `rows` | positional-or-keyword | `list[list[str]]` | `obrigatório/ausente` | Parâmetro `rows` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Cm`
- `doc.add_paragraph`
- `doc.add_table`
- `enumerate`
- `len`
- `max`
- `p.add_run`
- `range`
- `set_run_font`
- `table.add_row`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 2
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:add_references`

**Assinatura:**
```python
def add_references(doc: Any, entries: dict[str, dict[str, str]]) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 1081–1094
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `add_references` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doc` | positional-or-keyword | `Any` | `obrigatório/ausente` | Documento/registro individual processado pela rotina. |
| `entries` | positional-or-keyword | `dict[str, dict[str, str]]` | `obrigatório/ausente` | Coleção de entradas bibliográficas/registros. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `refs`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Cm`
- `Pt`
- `add_heading`
- `doc.add_paragraph`
- `entries.values`
- `p.add_run`
- `reference_line`
- `set_run_font`
- `sorted`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:render_docx_for_article`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:load_layout`

**Assinatura:**
```python
def load_layout(compliance_json: dict[str, Any]) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 1097–1116
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Carrega a responsabilidade representada por `layout` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `compliance_json` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `compliance_json` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `layout`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `compliance_json.get`
- `dict`
- `float`
- `int`
- `isinstance`
- `item.get`
- `m.group`
- `mm.group`
- `re.search`
- `str`
- `val.is_integer`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:render_docx_for_article`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 3
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:force_ooxml_black_docx`

**Assinatura:**
```python
def force_ooxml_black_docx(path: Path) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 1121–1145
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Força todas as definições OOXML de cor de fonte para preto.

**Docstring resumida:** Força todas as definições OOXML de cor de fonte para preto.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `data.decode`
- `item.filename.endswith`
- `item.filename.startswith`
- `path.exists`
- `path.with_suffix`
- `re.sub`
- `tmp.replace`
- `xml.encode`
- `zin.infolist`
- `zin.read`
- `zipfile.ZipFile`
- `zout.writestr`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx_canonico:render_docx_for_article`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:validate_docx`

**Assinatura:**
```python
def validate_docx(path: Path) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 1147–1195
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Valida a responsabilidade representada por `docx` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `report`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `len`
- `path.exists`
- `path.stat`
- `re.findall`
- `report['warnings'].append`
- `str`
- `xml.count`
- `zf.read`
- `zf.read('word/document.xml').decode`
- `zf.read('word/styles.xml').decode`
- `zipfile.ZipFile`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 2
comprehensions: 1
exception_handlers: 2
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:render_docx_for_article`

**Assinatura:**
```python
def render_docx_for_article(art: Path, prefix: str | None = None, cfg: Path | None = None, output: Path | None = None, quiet: bool = False) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 1197–1291
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Renderiza a responsabilidade representada por `docx_for_article` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `art` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `art` recebido pela função; não há descrição individual explícita no código. |
| `prefix` | positional-or-keyword | `str \| None` | `None` | Prefixo canônico usado para compor nomes de artefatos. |
| `cfg` | positional-or-keyword | `Path \| None` | `None` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `output` | positional-or-keyword | `Path \| None` | `None` | Parâmetro `output` recebido pela função; não há descrição individual explícita no código. |
| `quiet` | positional-or-keyword | `bool` | `False` | Parâmetro `quiet` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `paths.docx`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `Document`
- `add_abstracts`
- `add_body_paragraph`
- `add_cover`
- `add_heading`
- `add_references`
- `add_table`
- `asdict`
- `asdict(paths).items`
- `backup`
- `block.get`
- `build_body_blocks`
- `build_metadata`
- `clean_spaces`
- `configure_doc_styles`
- `datetime.now`
- `datetime.now().isoformat`
- `die`
- `doc.save`
- `extract_resumos`
- `force_ooxml_black_docx`
- `int`
- `isinstance`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.gerar_artigo_final_unificado:emit_docx_stage`
- `app_bundle.scripts.pipeline.render_docx_canonico:main`
- `gerar_artigo_final_unificado:emit_docx_stage`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `ERRO: não encontrei ORG nem document.json em `
- `ERRO: não consegui extrair corpo textual do ORG/document.json.`
- `ORG final; document.json como fallback; BIB final para referências`
- `.docx_canonico_report.json`

#### Complexidade estrutural observada

```yaml
branches: 16
loops: 1
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_docx_canonico:main`

**Assinatura:**
```python
def main(argv: list[str] | None = None) -> int
```

**Arquivo:** `app_bundle/scripts/pipeline/render_docx_canonico.py`
**Linhas:** 1294–1309
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa o ponto de entrada correspondente e coordena o fluxo associado.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `argv` | positional-or-keyword | `list[str] \| None` | `None` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `0`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `ap.add_argument`
- `ap.parse_args`
- `argparse.ArgumentParser`
- `render_docx_for_article`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `--art-dir`
- `--cfg-art`
- `--output`
- `--prefix`
- `--quiet`

#### Artefatos/caminhos literais reconhecidos no corpo

- `Gera DOCX ABNT/FGV canônico a partir do ORG/document.json/BIB finais. Versão v14 com disciplina no bloco institucional superior da capa.`
- `Caminho do DOCX de saída. Se omitido, usa output/<prefix>.docx.`

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `443bb2f31aab6a9cbe14be6852b12afe9cdb02e46e71a3d9f2928714c77c7ef3`
**LOC do arquivo-fonte:** 1313


### `app_bundle.scripts.pipeline.render_org_latex:clean_heading_title`

**Assinatura:**
```python
def clean_heading_title(title: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 42–51
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Remove numeração que a IA às vezes coloca dentro do título.

**Docstring resumida:** Remove numeração que a IA às vezes coloca dentro do título.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `title` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `title` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `text or str(title or '').strip()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `re.sub`
- `re.sub('^\\s*\\d+(?:\\.\\d+)*(?:[\\.)])?\\s+', '', text).strip`
- `str`
- `str(title or '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:org_heading`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:org_heading`

**Assinatura:**
```python
def org_heading(level: int, title: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 54–55
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `org_heading` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `level` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `level` recebido pela função; não há descrição individual explícita no código. |
| `title` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `title` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'*' * max(1, min(int(level or 1), 6)) + ' ' + clean_heading_title(title)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `clean_heading_title`
- `int`
- `max`
- `min`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:render_after_references_figures`
- `app_bundle.scripts.pipeline.render_org_latex:render_block_org`
- `app_bundle.scripts.pipeline.render_org_latex:render_document_body`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:_strip_numeric_citation_markers`

**Assinatura:**
```python
def _strip_numeric_citation_markers(text: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 58–62
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Remove a responsabilidade representada por `numeric_citation_markers` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `cleaned`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `re.sub`
- `re.sub('\\s{2,}', ' ', cleaned).strip`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:_is_citation_only_latex`

**Assinatura:**
```python
def _is_citation_only_latex(text: str) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 65–74
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Verifica se `citation_only_latex` conforme as regras codificadas na rotina.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `True`; `not masked`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `re.sub`
- `str`
- `str(text or '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:render_block_org`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:_plain_cell_text`

**Assinatura:**
```python
def _plain_cell_text(value: Any) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 77–81
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Normaliza texto de célula de tabela para saída LaTeX/Org.

**Docstring resumida:** Normaliza texto de célula de tabela para saída LaTeX/Org.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `text`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `re.sub`
- `str`
- `str(value if value is not None else '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:_latex_table_cell`
- `app_bundle.scripts.pipeline.render_org_latex:_render_table_as_latex`
- `app_bundle.scripts.pipeline.render_org_latex:_render_table_as_org_pipe`
- `app_bundle.scripts.pipeline.render_org_latex:_should_render_table_as_latex`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:_latex_table_cell`

**Assinatura:**
```python
def _latex_table_cell(value: Any) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 84–89
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Escapa conteúdo textual de célula, preservando quebras controladas.

**Docstring resumida:** Escapa conteúdo textual de célula, preservando quebras controladas.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `''`; `latex_escape(text)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_plain_cell_text`
- `latex_escape`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:_render_table_as_latex`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:_latex_table_colspec`

**Assinatura:**
```python
def _latex_table_colspec(ncols: int) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 92–109
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Define colspec responsivo para longtblr.

**Docstring resumida:** Define colspec responsivo para longtblr.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `ncols` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `ncols` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `' '.join((f'X[{w},l]' for w in weights[:n]))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `int`
- `max`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:_render_table_as_latex`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:_should_render_table_as_latex`

**Assinatura:**
```python
def _should_render_table_as_latex(block: Block) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 112–125
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Usa LaTeX responsivo quando a tabela tem muitas colunas/células longas.

**Docstring resumida:** Usa LaTeX responsivo quando a tabela tem muitas colunas/células longas.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `block` | positional-or-keyword | `Block` | `obrigatório/ausente` | Parâmetro `block` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `False`; `True`; `max_cell >= 90 or len(rows) >= 8`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_plain_cell_text`
- `len`
- `list`
- `max`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:render_table_block_org`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 2
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:_render_table_as_org_pipe`

**Assinatura:**
```python
def _render_table_as_org_pipe(block: Block) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 128–139
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Renderiza a responsabilidade representada por `table_as_org_pipe` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `block` | positional-or-keyword | `Block` | `obrigatório/ausente` | Parâmetro `block` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'\n'.join(lines)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `' | '.join`
- `'\n'.join`
- `_plain_cell_text`
- `len`
- `lines.append`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:render_table_block_org`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `---|`

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 1
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:_render_table_as_latex`

**Assinatura:**
```python
def _render_table_as_latex(block: Block) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 142–192
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Renderiza a responsabilidade representada por `table_as_latex` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `block` | positional-or-keyword | `Block` | `obrigatório/ausente` | Parâmetro `block` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'\n'.join(lines)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `' & '.join`
- `','.join`
- `'\n'.join`
- `_latex_table_cell`
- `_latex_table_colspec`
- `_plain_cell_text`
- `latex_escape`
- `len`
- `lines.append`
- `list`
- `max`
- `opts.append`
- `re.sub`
- `re.sub('[^A-Za-z0-9:_-]+', '_', str(block.id or 'tab:auto')).strip`
- `row_values`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:render_table_block_org`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 9
loops: 1
comprehensions: 3
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:_render_table_as_latex.row_values`

**Assinatura:**
```python
def row_values(row: list[Any]) -> list[Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 151–155
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `row_values` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `row` | positional-or-keyword | `list[Any]` | `obrigatório/ausente` | Parâmetro `row` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[Any]`. Retornos observados estaticamente incluem: `values`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `len`
- `list`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:_render_table_as_latex`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:render_table_block_org`

**Assinatura:**
```python
def render_table_block_org(block: Block) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 195–205
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Renderiza tabela com fallback responsivo para LaTeX.

**Docstring resumida:** Renderiza tabela com fallback responsivo para LaTeX.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `block` | positional-or-keyword | `Block` | `obrigatório/ausente` | Parâmetro `block` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `''`; `_render_table_as_latex(block)`; `_render_table_as_org_pipe(block)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_render_table_as_latex`
- `_render_table_as_org_pipe`
- `_should_render_table_as_latex`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:render_block_org`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:_cfg_section`

**Assinatura:**
```python
def _cfg_section(cfg: dict[str, Any] | None, name: str) -> dict[str, Any]
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 208–209
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_cfg_section` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any] \| None` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `name` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `name` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `cfg.get(name, {}) if cfg and isinstance(cfg.get(name), dict) else {}`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `cfg.get`
- `isinstance`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:_layout_spec`

**Assinatura:**
```python
def _layout_spec(cfg: dict[str, Any] | None, doc: AcademicDocument | None = None)
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 212–216
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_layout_spec` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any] \| None` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `doc` | positional-or-keyword | `AcademicDocument \| None` | `None` | Documento/registro individual processado pela rotina. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `resolve_layout_spec(cfg or {}, doc)`; `None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `resolve_layout_spec`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:_latex_class_from_layout`
- `app_bundle.scripts.pipeline.render_org_latex:render_front_matter`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:_latex_class_from_layout`

**Assinatura:**
```python
def _latex_class_from_layout(cfg: dict[str, Any] | None, doc: AcademicDocument | None, default: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 219–224
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_latex_class_from_layout` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any] \| None` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `doc` | positional-or-keyword | `AcademicDocument \| None` | `obrigatório/ausente` | Documento/registro individual processado pela rotina. |
| `default` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `default` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `spec.classe_latex`; `str(doc_cfg.get('classe_latex') or default)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_cfg_section`
- `_layout_spec`
- `doc_cfg.get`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:render_activity_front_matter`
- `app_bundle.scripts.pipeline.render_org_latex:render_dissertation_front_matter`
- `app_bundle.scripts.pipeline.render_org_latex:render_paper_front_matter`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:_first_nonempty`

**Assinatura:**
```python
def _first_nonempty(*values: Any, default: str = '') -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 227–232
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_first_nonempty` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `values` | var-positional | `Any` | `obrigatório/ausente` | Parâmetro `values` recebido pela função; não há descrição individual explícita no código. |
| `default` | keyword-only | `str` | `''` | Parâmetro `default` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `s`; `default`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `str`
- `str(v or '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:render_activity_front_matter`
- `app_bundle.scripts.pipeline.render_org_latex:render_dissertation_front_matter`
- `app_bundle.scripts.pipeline.render_org_latex:render_paper_front_matter`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:_as_latex_graphics_path`

**Assinatura:**
```python
def _as_latex_graphics_path(path_value: Any, fallback: str = 'fgv.png', cfg: dict[str, Any] | None = None) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 235–251
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Retorna caminho seguro para \includegraphics no LaTeX.

**Docstring resumida:** Retorna caminho seguro para \includegraphics no LaTeX.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path_value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `path_value` recebido pela função; não há descrição individual explícita no código. |
| `fallback` | positional-or-keyword | `str` | `'fgv.png'` | Parâmetro `fallback` recebido pela função; não há descrição individual explícita no código. |
| `cfg` | positional-or-keyword | `dict[str, Any] \| None` | `None` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `resolved.as_posix()`; `raw`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(str(cfg.get('__config_dir__'))).resolve`
- `cfg.get`
- `raw.replace`
- `resolve_path`
- `resolved.as_posix`
- `resolved.exists`
- `str`
- `str(path_value or '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:render_activity_front_matter`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:normalize_biblatex_style`

**Assinatura:**
```python
def normalize_biblatex_style(style: str | None) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 254–277
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Normaliza a responsabilidade representada por `biblatex_style` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `style` | positional-or-keyword | `str \| None` | `obrigatório/ausente` | Parâmetro `style` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `aliases.get(raw, raw or 'apa')`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `(style or '').strip`
- `(style or '').strip().lower`
- `aliases.get`
- `raw.replace`
- `raw.replace('_', '-').replace`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:biblatex_options_for_style`
- `app_bundle.scripts.pipeline.render_org_latex:bibliography_style_from_cfg`
- `app_bundle.scripts.pipeline.render_org_latex:enforce_abnt_biblatex_options`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:enforce_abnt_biblatex_options`

**Assinatura:**
```python
def enforce_abnt_biblatex_options(options: str | list[str] | tuple[str, ...], style: str | None) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 280–314
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Impõe a responsabilidade representada por `abnt_biblatex_options` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `options` | positional-or-keyword | `str \| list[str] \| tuple[str, ...]` | `obrigatório/ausente` | Parâmetro `options` recebido pela função; não há descrição individual explícita no código. |
| `style` | positional-or-keyword | `str \| None` | `obrigatório/ausente` | Parâmetro `style` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `','.join(out)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `','.join`
- `any`
- `isinstance`
- `normalize_biblatex_style`
- `out.append`
- `p.startswith`
- `p.strip`
- `parts.append`
- `parts.insert`
- `seen.add`
- `set`
- `str`
- `str(options or '').split`
- `str(p).strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:render_bibliography_preamble`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 10
loops: 1
comprehensions: 5
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:bibliography_style_from_cfg`

**Assinatura:**
```python
def bibliography_style_from_cfg(cfg: dict[str, Any] | None, doc: AcademicDocument | None = None) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 316–327
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `bibliography_style_from_cfg` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any] \| None` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `doc` | positional-or-keyword | `AcademicDocument \| None` | `None` | Documento/registro individual processado pela rotina. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `normalize_biblatex_style(str(raw))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_cfg_section`
- `bib_cfg.get`
- `doc_cfg.get`
- `getattr`
- `normalize_biblatex_style`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:_common_headers`
- `app_bundle.scripts.pipeline.render_prisma_org:render_prisma_org`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:biblatex_options_for_style`

**Assinatura:**
```python
def biblatex_options_for_style(style: str, cfg: dict[str, Any] | None = None) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 330–340
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `biblatex_options_for_style` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `style` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `style` recebido pela função; não há descrição individual explícita no código. |
| `cfg` | positional-or-keyword | `dict[str, Any] \| None` | `None` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `override`; `'backend=biber,style=abnt,sorting=nty,giveninits=true'`; `'backend=biber,style=apa,sorting=nyt'`; `f'backend=biber,style={style}'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_cfg_section`
- `bib_cfg.get`
- `normalize_biblatex_style`
- `str`
- `str(bib_cfg.get('latex_options') or '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:render_bibliography_preamble`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:strip_org_cite_export_lines`

**Assinatura:**
```python
def strip_org_cite_export_lines(org: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 345–352
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Remove diretivas #+CITE_EXPORT herdadas de templates antigos.

**Docstring resumida:** Remove diretivas #+CITE_EXPORT herdadas de templates antigos.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `org` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `org` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `re.sub('(?im)^\\s*#\\+cite_export:.*(?:\\n|$)', '', org)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `re.sub`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:render_org_latex`
- `app_bundle.scripts.pipeline.render_prisma_org:render_prisma_org`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:render_bibliography_preamble`

**Assinatura:**
```python
def render_bibliography_preamble(bib_filename: str, style: str, cfg: dict[str, Any] | None = None) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 354–363
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Renderiza a responsabilidade representada por `bibliography_preamble` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `bib_filename` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `bib_filename` recebido pela função; não há descrição individual explícita no código. |
| `style` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `style` recebido pela função; não há descrição individual explícita no código. |
| `cfg` | positional-or-keyword | `dict[str, Any] \| None` | `None` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `['#+LATEX_HEADER: % Bibliografia controlada pelo TOML; os estilos FGV não carregam biblatex.', f'#+LATEX_HEADER: \\usepackage[{enforce_abnt_biblatex_options(options, style)}]{{biblatex}}', f'#+LATEX_HEADER: \\addbibresource{{{bib_name}}}']`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `biblatex_options_for_style`
- `enforce_abnt_biblatex_options`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:_common_headers`
- `app_bundle.scripts.pipeline.render_prisma_org:render_prisma_org`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:render_block_org`

**Assinatura:**
```python
def render_block_org(block: Block, base_level: int = 1) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 366–392
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Renderiza a responsabilidade representada por `block_org` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `block` | positional-or-keyword | `Block` | `obrigatório/ausente` | Parâmetro `block` recebido pela função; não há descrição individual explícita no código. |
| `base_level` | positional-or-keyword | `int` | `1` | Parâmetro `base_level` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `(raw + (' ' + rendered if rendered else '')).strip()`; `rendered`; `org_heading(block.level, block.text or block.title)`; `'#+begin_quote\n' + (block.text or render_latex_inlines(block.content)).strip() + '\n#+end_quote'`; `'\n'.join((f'- {item}' for item in block.items))`; `'\n'.join((f'{i}. {item}' for i, item in enumerate(block.items, start=1)))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `(block.text or render_latex_inlines(block.content)).strip`
- `(raw + (' ' + rendered if rendered else '')).strip`
- `_is_citation_only_latex`
- `_strip_numeric_citation_markers`
- `block.text.strip`
- `enumerate`
- `len`
- `max`
- `org_heading`
- `render_latex_inlines`
- `render_latex_inlines(block.content).strip`
- `render_table_block_org`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:render_document_body`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 13
loops: 0
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:_normalise_language_code`

**Assinatura:**
```python
def _normalise_language_code(value: Any) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 398–405
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_normalise_language_code` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `aliases.get(raw, raw or 'pt-br')`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `aliases.get`
- `str`
- `str(value or '').strip`
- `str(value or '').strip().casefold`
- `str(value or '').strip().casefold().replace`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:_org_language_name`
- `app_bundle.scripts.pipeline.render_org_latex:_paper_abstract_rows`
- `app_bundle.scripts.pipeline.render_org_latex:_render_output_language`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:_org_language_name`

**Assinatura:**
```python
def _org_language_name(code: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 408–412
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_org_language_name` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `code` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `code` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'pt_BR'`; `normalized.split('-', 1)[0] or 'pt_BR'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_normalise_language_code`
- `normalized.split`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:_common_headers`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:_render_output_language`

**Assinatura:**
```python
def _render_output_language(cfg: dict[str, Any] | None, output_path: Path) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 415–421
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Renderiza a responsabilidade representada por `output_language` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any] \| None` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `output_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `_normalise_language_code(parts[index + 1])`; `_normalise_language_code(section.get('principal') or 'pt-BR')`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_cfg_section`
- `_normalise_language_code`
- `enumerate`
- `len`
- `list`
- `section.get`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:render_org_latex`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:_paper_abstract_sidecar_path`

**Assinatura:**
```python
def _paper_abstract_sidecar_path(output_path: Path, cfg: dict[str, Any] | None) -> Path | None
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 424–437
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Localiza o JSON de resumos no diretório principal, inclusive em cópias traduzidas.

**Docstring resumida:** Localiza o JSON de resumos no diretório principal, inclusive em cópias traduzidas.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `output_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `cfg` | positional-or-keyword | `dict[str, Any] \| None` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `Path | None`. Retornos observados estaticamente incluem: `candidate`; `None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_cfg_section`
- `candidate.exists`
- `candidate.is_file`
- `paths.get`
- `seen.add`
- `set`
- `str`
- `str(paths.get('document_prefix') or '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:_paper_abstract_rows`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.resumos_paper.json`

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:_paper_abstract_rows`

**Assinatura:**
```python
def _paper_abstract_rows(cfg: dict[str, Any] | None, output_path: Path, output_language: str) -> list[dict[str, Any]]
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 440–487
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_paper_abstract_rows` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `dict[str, Any] \| None` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `output_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `output_language` | positional-or-keyword | `str` | `obrigatório/ausente` | Idioma solicitado para geração, tradução ou renderização. |

#### Retorno

Anotação declarada: `list[dict[str, Any]]`. Retornos observados estaticamente incluem: `[]`; `rows`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `_cfg_section`
- `_cfg_section(cfg, 'idiomas_saida').get`
- `_normalise_language_code`
- `_paper_abstract_sidecar_path`
- `bool`
- `isinstance`
- `items.get`
- `json.loads`
- `payload.get`
- `requested.extend`
- `row.get`
- `rows.append`
- `section.get`
- `seen.add`
- `set`
- `sidecar.read_text`
- `str`
- `str(row.get('abstract') or '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:render_org_latex`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 10
loops: 1
comprehensions: 1
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:_render_paper_abstract_rows`

**Assinatura:**
```python
def _render_paper_abstract_rows(rows: list[dict[str, Any]]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 490–522
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Renderiza resumos no front matter sem criar headings numerados no corpo.

**Docstring resumida:** Renderiza resumos no front matter sem criar headings numerados no corpo.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `rows` | positional-or-keyword | `list[dict[str, Any]]` | `obrigatório/ausente` | Parâmetro `rows` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `''`; `'\n'.join(parts)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'; '.join`
- `'\n'.join`
- `bool`
- `enumerate`
- `isinstance`
- `latex_escape`
- `parts.append`
- `parts.extend`
- `re.sub`
- `row.get`
- `str`
- `str(item).strip`
- `str(row.get('abstract') or '').strip`
- `str(row.get('heading') or 'Resumo').strip`
- `str(row.get('keywords_heading') or 'Palavras-chave').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:render_paper_front_matter`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 1
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:_common_headers`

**Assinatura:**
```python
def _common_headers(meta: Any, bib_filename: str, cfg: dict[str, Any] | None) -> list[str]
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 525–534
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_common_headers` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `meta` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `meta` recebido pela função; não há descrição individual explícita no código. |
| `bib_filename` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `bib_filename` recebido pela função; não há descrição individual explícita no código. |
| `cfg` | positional-or-keyword | `dict[str, Any] \| None` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `[f'#+TITLE: {meta.titulo}', f'#+AUTHOR: {meta.autor}', f'#+LANGUAGE: {_org_language_name(output_language)}', '#+OPTIONS: toc:nil num:t title:nil html-postamble:nil ^:{}', *render_bibliography_preamble(bib_filename, style, cfg)]`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `(cfg or {}).get`
- `_org_language_name`
- `bibliography_style_from_cfg`
- `render_bibliography_preamble`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:render_activity_front_matter`
- `app_bundle.scripts.pipeline.render_org_latex:render_dissertation_front_matter`
- `app_bundle.scripts.pipeline.render_org_latex:render_paper_front_matter`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:render_paper_front_matter`

**Assinatura:**
```python
def render_paper_front_matter(doc: AcademicDocument, bib_filename: str, cfg: dict[str, Any] | None = None) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 537–568
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Renderiza a responsabilidade representada por `paper_front_matter` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doc` | positional-or-keyword | `AcademicDocument` | `obrigatório/ausente` | Documento/registro individual processado pela rotina. |
| `bib_filename` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `bib_filename` recebido pela função; não há descrição individual explícita no código. |
| `cfg` | positional-or-keyword | `dict[str, Any] \| None` | `None` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'\n'.join(lines).strip() + '\n\n'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'; '.join`
- `'\n'.join`
- `'\n'.join(lines).strip`
- `(cfg or {}).get`
- `_cfg_section`
- `_common_headers`
- `_first_nonempty`
- `_latex_class_from_layout`
- `_render_paper_abstract_rows`
- `doc.abstract.texto.strip`
- `doc_cfg.get`
- `isinstance`
- `latex_escape`
- `lines.append`
- `str`
- `str(doc_cfg.get('program_name') if 'program_name' in doc_cfg else meta.programa or '').strip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:render_front_matter`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:render_activity_front_matter`

**Assinatura:**
```python
def render_activity_front_matter(doc: AcademicDocument, bib_filename: str, cfg: dict[str, Any] | None = None) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 571–661
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Renderiza atividade FGV com cabeçalho institucional e Ficha Técnica visual.

**Docstring resumida:** Renderiza atividade FGV com cabeçalho institucional e Ficha Técnica visual.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doc` | positional-or-keyword | `AcademicDocument` | `obrigatório/ausente` | Documento/registro individual processado pela rotina. |
| `bib_filename` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `bib_filename` recebido pela função; não há descrição individual explícita no código. |
| `cfg` | positional-or-keyword | `dict[str, Any] \| None` | `None` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'\n'.join(lines).strip() + '\n\n'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `'\n'.join(lines).strip`
- `_as_latex_graphics_path`
- `_cfg_section`
- `_common_headers`
- `_first_nonempty`
- `_latex_class_from_layout`
- `atividade.get`
- `latex_cfg.get`
- `latex_escape`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:render_front_matter`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `fgv.png`

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:render_dissertation_front_matter`

**Assinatura:**
```python
def render_dissertation_front_matter(doc: AcademicDocument, bib_filename: str, cfg: dict[str, Any] | None = None) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 664–711
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Renderiza a responsabilidade representada por `dissertation_front_matter` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doc` | positional-or-keyword | `AcademicDocument` | `obrigatório/ausente` | Documento/registro individual processado pela rotina. |
| `bib_filename` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `bib_filename` recebido pela função; não há descrição individual explícita no código. |
| `cfg` | positional-or-keyword | `dict[str, Any] \| None` | `None` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'\n'.join(lines).strip() + '\n\n'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `'\n'.join(lines).strip`
- `_cfg_section`
- `_common_headers`
- `_first_nonempty`
- `_latex_class_from_layout`
- `atividade.get`
- `doc.abstract.texto.strip`
- `documento.get`
- `latex_escape`
- `meta.instituicao.upper`
- `p.strip`
- `str`
- `title_main.split`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:render_front_matter`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:render_front_matter`

**Assinatura:**
```python
def render_front_matter(doc: AcademicDocument, bib_filename: str, cfg: dict[str, Any] | None = None) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 714–731
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Renderiza a responsabilidade representada por `front_matter` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doc` | positional-or-keyword | `AcademicDocument` | `obrigatório/ausente` | Documento/registro individual processado pela rotina. |
| `bib_filename` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `bib_filename` recebido pela função; não há descrição individual explícita no código. |
| `cfg` | positional-or-keyword | `dict[str, Any] \| None` | `None` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `render_activity_front_matter(doc, bib_filename, cfg)`; `render_dissertation_front_matter(doc, bib_filename, cfg)`; `render_paper_front_matter(doc, bib_filename, cfg)`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `_layout_spec`
- `getattr`
- `render_activity_front_matter`
- `render_dissertation_front_matter`
- `render_paper_front_matter`
- `str`
- `str(getattr(doc.metadata, 'tipo_documento', 'paper') or 'paper').lower`
- `str(spec.front_matter if spec else '').strip`
- `str(spec.front_matter if spec else '').strip().lower`
- `str(spec.front_matter if spec else '').strip().lower().replace`
- `str(spec.genero_academico if spec else getattr(doc.metadata, 'tipo_documento', 'paper')).strip`
- `str(spec.genero_academico if spec else getattr(doc.metadata, 'tipo_documento', 'paper')).strip().lower`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:render_org_latex`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 7
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:_normalize_heading_for_skip`

**Assinatura:**
```python
def _normalize_heading_for_skip(title: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 745–757
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Normaliza a responsabilidade representada por `heading_for_skip` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `title` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `title` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `text`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `re.sub`
- `re.sub('\\s+', ' ', text).strip`
- `re.sub('^\\d+(?:\\.\\d+)*\\s+', '', text).strip`
- `replacements.items`
- `str`
- `str(title or '').strip`
- `str(title or '').strip().lower`
- `text.replace`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:is_ai_generated_reference_section_title`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:is_ai_generated_reference_section_title`

**Assinatura:**
```python
def is_ai_generated_reference_section_title(title: str) -> bool
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 760–769
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Detecta seção de referências criada pela IA.

**Docstring resumida:** Detecta seção de referências criada pela IA.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `title` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `title` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `_normalize_heading_for_skip(title) in REFERENCE_SECTION_TITLES`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_normalize_heading_for_skip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_docx:render_docx`
- `app_bundle.scripts.pipeline.render_docx:render_markdown_for_pandoc`
- `app_bundle.scripts.pipeline.render_org_latex:render_document_body`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:render_document_body`

**Assinatura:**
```python
def render_document_body(doc: AcademicDocument) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 772–783
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Renderiza a responsabilidade representada por `document_body` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doc` | positional-or-keyword | `AcademicDocument` | `obrigatório/ausente` | Documento/registro individual processado pela rotina. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'\n\n'.join((part for part in out if part is not None)).rstrip() + '\n'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'\n\n'.join`
- `'\n\n'.join((part for part in out if part is not None)).rstrip`
- `is_ai_generated_reference_section_title`
- `org_heading`
- `out.append`
- `render_block_org`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:render_org_latex`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 2
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:_mask_existing_citation_commands`

**Assinatura:**
```python
def _mask_existing_citation_commands(text: str) -> tuple[str, list[str]]
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 787–800
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Mascara comandos LaTeX de citação já válidos antes de saneamentos.

**Docstring resumida:** Mascara comandos LaTeX de citação já válidos antes de saneamentos.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `tuple[str, list[str]]`. Retornos observados estaticamente incluem: `(masked, placeholders)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `re.sub`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:_sanitize_numeric_citation_markers_in_line`
- `app_bundle.scripts.pipeline.render_org_latex:_sanitize_raw_bibkeys_in_visible_line`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:_mask_existing_citation_commands.repl`

**Assinatura:**
```python
def repl(match: re.Match[str]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 791–793
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `repl` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `match` | positional-or-keyword | `re.Match[str]` | `obrigatório/ausente` | Parâmetro `match` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `f'@@CITE_PLACEHOLDER_{len(placeholders) - 1}@@'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `len`
- `match.group`
- `placeholders.append`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:_restore_existing_citation_commands`

**Assinatura:**
```python
def _restore_existing_citation_commands(text: str, placeholders: list[str]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 803–806
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_restore_existing_citation_commands` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |
| `placeholders` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `placeholders` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `text`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `enumerate`
- `text.replace`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:_sanitize_numeric_citation_markers_in_line`
- `app_bundle.scripts.pipeline.render_org_latex:_sanitize_raw_bibkeys_in_visible_line`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:_sanitize_raw_bibkeys_in_visible_line`

**Assinatura:**
```python
def _sanitize_raw_bibkeys_in_visible_line(line: str, keys: list[str]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 809–829
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Converte chaves BibTeX cruas em uma linha visível de ORG.

**Docstring resumida:** Converte chaves BibTeX cruas em uma linha visível de ORG.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `line` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `line` recebido pela função; não há descrição individual explícita no código. |
| `keys` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `keys` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `line`; `_restore_existing_citation_commands(text, placeholders)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_mask_existing_citation_commands`
- `_restore_existing_citation_commands`
- `line.strip`
- `re.escape`
- `re.sub`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:sanitize_raw_bibkeys_in_org`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:sanitize_raw_bibkeys_in_org`

**Assinatura:**
```python
def sanitize_raw_bibkeys_in_org(org: str, doc: AcademicDocument, bib_keys: list[str] | None = None) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 832–888
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Converte chaves BibTeX cruas residuais em \parencite no ORG final.

**Docstring resumida:** Converte chaves BibTeX cruas residuais em \parencite no ORG final.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `org` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `org` recebido pela função; não há descrição individual explícita no código. |
| `doc` | positional-or-keyword | `AcademicDocument` | `obrigatório/ausente` | Documento/registro individual processado pela rotina. |
| `bib_keys` | positional-or-keyword | `list[str] \| None` | `None` | Parâmetro `bib_keys` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `org or ''`; `'\n'.join(out) + ('\n' if org.endswith('\n') else '')`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `(org or '').splitlines`
- `_sanitize_raw_bibkeys_in_visible_line`
- `line.lstrip`
- `list`
- `low.startswith`
- `org.endswith`
- `out.append`
- `sorted`
- `str`
- `str(k).strip`
- `str(k).strip().lstrip`
- `stripped.lower`
- `stripped.startswith`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:render_org_latex`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 9
loops: 1
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:_visible_org_lines_transform`

**Assinatura:**
```python
def _visible_org_lines_transform(org: str, transform) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 891–934
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Aplica transformação apenas em linhas visíveis do ORG.

**Docstring resumida:** Aplica transformação apenas em linhas visíveis do ORG.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `org` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `org` recebido pela função; não há descrição individual explícita no código. |
| `transform` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `transform` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `org or ''`; `'\n'.join(out) + ('\n' if org.endswith('\n') else '')`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `(org or '').splitlines`
- `line.lstrip`
- `low.startswith`
- `org.endswith`
- `out.append`
- `stripped.lower`
- `stripped.startswith`
- `transform`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:sanitize_numeric_citation_markers_in_org`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 9
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:_sanitize_numeric_citation_markers_in_line`

**Assinatura:**
```python
def _sanitize_numeric_citation_markers_in_line(line: str, bib_keys: list[str]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 937–970
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Converte citações numéricas cruas do tipo [1] ou [1, 2] em \parencite.

**Docstring resumida:** Converte citações numéricas cruas do tipo [1] ou [1, 2] em \parencite.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `line` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `line` recebido pela função; não há descrição individual explícita no código. |
| `bib_keys` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `bib_keys` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `line`; `_restore_existing_citation_commands(text, placeholders)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_mask_existing_citation_commands`
- `_restore_existing_citation_commands`
- `re.sub`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:sanitize_numeric_citation_markers_in_org`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:_sanitize_numeric_citation_markers_in_line.repl`

**Assinatura:**
```python
def repl(match: re.Match[str]) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 949–966
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `repl` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `match` | positional-or-keyword | `re.Match[str]` | `obrigatório/ausente` | Parâmetro `match` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `match.group(0)`; `'\\parencite{' + ','.join(keys) + '}'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `','.join`
- `int`
- `keys.append`
- `len`
- `match.group`
- `n.strip`
- `re.fullmatch`
- `re.split`
- `str`
- `str(bib_keys[idx]).strip`
- `str(bib_keys[idx]).strip().lstrip`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 1
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:sanitize_numeric_citation_markers_in_org`

**Assinatura:**
```python
def sanitize_numeric_citation_markers_in_org(org: str, bib_keys: list[str] | None = None) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 973–978
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Saneia citações numéricas visíveis geradas indevidamente pela IA.

**Docstring resumida:** Saneia citações numéricas visíveis geradas indevidamente pela IA.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `org` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `org` recebido pela função; não há descrição individual explícita no código. |
| `bib_keys` | positional-or-keyword | `list[str] \| None` | `None` | Parâmetro `bib_keys` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `org or ''`; `_visible_org_lines_transform(org, lambda line: _sanitize_numeric_citation_markers_in_line(line, keys))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_sanitize_numeric_citation_markers_in_line`
- `_visible_org_lines_transform`
- `str`
- `str(k).strip`
- `str(k).strip().lstrip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:render_org_latex`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:render_after_references_figures`

**Assinatura:**
```python
def render_after_references_figures(doc: AcademicDocument) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 981–1000
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Renderiza a responsabilidade representada por `after_references_figures` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doc` | positional-or-keyword | `AcademicDocument` | `obrigatório/ausente` | Documento/registro individual processado pela rotina. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'\n'.join(blocks).rstrip() + ('\n' if blocks else '')`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `'\n'.join(blocks).rstrip`
- `blocks.append`
- `fig.path.replace`
- `org_heading`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:render_org_latex`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_org_latex:render_org_latex`

**Assinatura:**
```python
def render_org_latex(doc: AcademicDocument, output_path: Path, bib_filename: str, cfg: dict[str, Any] | None = None, bib_keys: list[str] | None = None) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_org_latex.py`
**Linhas:** 1003–1018
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Renderiza a responsabilidade representada por `org_latex` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doc` | positional-or-keyword | `AcademicDocument` | `obrigatório/ausente` | Documento/registro individual processado pela rotina. |
| `output_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `bib_filename` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `bib_filename` recebido pela função; não há descrição individual explícita no código. |
| `cfg` | positional-or-keyword | `dict[str, Any] \| None` | `None` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `bib_keys` | positional-or-keyword | `list[str] \| None` | `None` | Parâmetro `bib_keys` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `org`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `_paper_abstract_rows`
- `_render_output_language`
- `dict`
- `list`
- `normalize_snippet_placeholders`
- `org.rstrip`
- `render_after_references_figures`
- `render_document_body`
- `render_front_matter`
- `sanitize_numeric_citation_markers_in_org`
- `sanitize_raw_bibkeys_in_org`
- `strip_org_cite_export_lines`
- `write_text`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `14b7ccf8f7ffd14c54538298df2770d650fd6b6ceb5f97b1014c2b78460f6a47`
**LOC do arquivo-fonte:** 1018


### `app_bundle.scripts.pipeline.render_prisma_docx:setup_docx_styles`

**Assinatura:**
```python
def setup_docx_styles(doc: Any) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/render_prisma_docx.py`
**Linhas:** 22–33
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `setup_docx_styles` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doc` | positional-or-keyword | `Any` | `obrigatório/ausente` | Documento/registro individual processado pela rotina. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Pt`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_prisma_docx:render_prisma_docx`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `f691deb04e730266ba7a6d3e6a964ab2b267aec6705b3a4bf0edc73b16b4fdbb`
**LOC do arquivo-fonte:** 157


### `app_bundle.scripts.pipeline.render_prisma_docx:add_justified_paragraph`

**Assinatura:**
```python
def add_justified_paragraph(doc: Any, text: str) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/render_prisma_docx.py`
**Linhas:** 36–40
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `add_justified_paragraph` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doc` | positional-or-keyword | `Any` | `obrigatório/ausente` | Documento/registro individual processado pela rotina. |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Cm`
- `doc.add_paragraph`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_prisma_docx:render_prisma_docx`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f691deb04e730266ba7a6d3e6a964ab2b267aec6705b3a4bf0edc73b16b4fdbb`
**LOC do arquivo-fonte:** 157


### `app_bundle.scripts.pipeline.render_prisma_docx:add_table`

**Assinatura:**
```python
def add_table(doc: Any, headers: list[str], rows: list[list[str]]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/render_prisma_docx.py`
**Linhas:** 43–55
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `add_table` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `doc` | positional-or-keyword | `Any` | `obrigatório/ausente` | Documento/registro individual processado pela rotina. |
| `headers` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Cabeçalhos usados em requisição/integração externa. |
| `rows` | positional-or-keyword | `list[list[str]]` | `obrigatório/ausente` | Parâmetro `rows` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `doc.add_paragraph`
- `doc.add_table`
- `enumerate`
- `len`
- `str`
- `table.add_row`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 4
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f691deb04e730266ba7a6d3e6a964ab2b267aec6705b3a4bf0edc73b16b4fdbb`
**LOC do arquivo-fonte:** 157


### `app_bundle.scripts.pipeline.render_prisma_docx:study_rows`

**Assinatura:**
```python
def study_rows(studies: list[StudyRecord]) -> list[list[str]]
```

**Arquivo:** `app_bundle/scripts/pipeline/render_prisma_docx.py`
**Linhas:** 58–65
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `study_rows` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `studies` | positional-or-keyword | `list[StudyRecord]` | `obrigatório/ausente` | Parâmetro `studies` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[list[str]]`. Retornos observados estaticamente incluem: `rows`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'; '.join`
- `enumerate`
- `rows.append`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_prisma_docx:render_prisma_docx`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f691deb04e730266ba7a6d3e6a964ab2b267aec6705b3a4bf0edc73b16b4fdbb`
**LOC do arquivo-fonte:** 157


### `app_bundle.scripts.pipeline.render_prisma_docx:render_prisma_docx`

**Assinatura:**
```python
def render_prisma_docx(report: PrismaReport, output_path: Path, reference_docx: Path | None = None, flow_svg_path: Path | None = None) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/render_prisma_docx.py`
**Linhas:** 68–157
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Renderiza a responsabilidade representada por `prisma_docx` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `report` | positional-or-keyword | `PrismaReport` | `obrigatório/ausente` | Parâmetro `report` recebido pela função; não há descrição individual explícita no código. |
| `output_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `reference_docx` | positional-or-keyword | `Path \| None` | `None` | Parâmetro `reference_docx` recebido pela função; não há descrição individual explícita no código. |
| `flow_svg_path` | positional-or-keyword | `Path \| None` | `None` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `output_path`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `'; '.join`
- `Cm`
- `Document`
- `Inches`
- `Pt`
- `RuntimeError`
- `add_justified_paragraph`
- `add_table`
- `doc.add_heading`
- `doc.add_page_break`
- `doc.add_paragraph`
- `doc.add_picture`
- `doc.save`
- `flow_svg_path.exists`
- `output_path.parent.mkdir`
- `reference_docx.exists`
- `setup_docx_styles`
- `str`
- `study_rows`
- `title.add_run`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_pipeline:run_prisma_report_outputs`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('python-docx não está instalado.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 13
loops: 4
comprehensions: 1
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `f691deb04e730266ba7a6d3e6a964ab2b267aec6705b3a4bf0edc73b16b4fdbb`
**LOC do arquivo-fonte:** 157


### `app_bundle.scripts.pipeline.render_prisma_flow:flow_items`

**Assinatura:**
```python
def flow_items(flow: PrismaFlow) -> list[tuple[str, int]]
```

**Arquivo:** `app_bundle/scripts/pipeline/render_prisma_flow.py`
**Linhas:** 14–24
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `flow_items` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `flow` | positional-or-keyword | `PrismaFlow` | `obrigatório/ausente` | Parâmetro `flow` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[tuple[str, int]]`. Retornos observados estaticamente incluem: `[('Registros identificados', flow.identificados), ('Duplicados removidos', flow.duplicados_removidos), ('Registros após deduplicação', flow.apos_deduplicacao), ('Registros triados por título/resumo', flow.triados_titulo_resumo), ('Excluídos na triagem', flow.excluidos_titulo_resumo), ('Textos completos avaliados', flow.avaliados_texto_completo), ('Excluídos após texto completo', flow.excluidos_texto_completo), ('Estudos incluídos', flow.incluidos)]`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

Nenhum identificado pela análise estática.

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_prisma_flow:render_prisma_flow_latex`
- `app_bundle.scripts.pipeline.render_prisma_flow:render_prisma_flow_svg`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c3795f7ef2098c15098e05853fa541f56ada20cc1d74302e57c598115dbb4963`
**LOC do arquivo-fonte:** 66


### `app_bundle.scripts.pipeline.render_prisma_flow:render_prisma_flow_latex`

**Assinatura:**
```python
def render_prisma_flow_latex(report: PrismaReport) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_prisma_flow.py`
**Linhas:** 27–38
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Fluxograma em LaTeX simples, sem depender de TikZ/Graphviz.

**Docstring resumida:** Fluxograma em LaTeX simples, sem depender de TikZ/Graphviz.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `report` | positional-or-keyword | `PrismaReport` | `obrigatório/ausente` | Parâmetro `report` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'\n'.join(blocks)`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `blocks.append`
- `enumerate`
- `flow_items`
- `label.replace`
- `len`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_prisma_org:render_prisma_org`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c3795f7ef2098c15098e05853fa541f56ada20cc1d74302e57c598115dbb4963`
**LOC do arquivo-fonte:** 66


### `app_bundle.scripts.pipeline.render_prisma_flow:render_prisma_flow_svg`

**Assinatura:**
```python
def render_prisma_flow_svg(report: PrismaReport, output_path: Path) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/render_prisma_flow.py`
**Linhas:** 41–66
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Renderiza a responsabilidade representada por `prisma_flow_svg` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `report` | positional-or-keyword | `PrismaReport` | `obrigatório/ausente` | Parâmetro `report` recebido pela função; não há descrição individual explícita no código. |
| `output_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `output_path`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `enumerate`
- `flow_items`
- `html.escape`
- `len`
- `output_path.parent.mkdir`
- `output_path.write_text`
- `parts.append`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_pipeline:run_prisma_report_outputs`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `<svg xmlns="http://www.w3.org/2000/svg" width="`

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `c3795f7ef2098c15098e05853fa541f56ada20cc1d74302e57c598115dbb4963`
**LOC do arquivo-fonte:** 66


### `app_bundle.scripts.pipeline.render_prisma_org:_table`

**Assinatura:**
```python
def _table(headers: list[str], rows: list[list[str]], name: str = '', caption: str = '') -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_prisma_org.py`
**Linhas:** 36–47
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_table` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `headers` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Cabeçalhos usados em requisição/integração externa. |
| `rows` | positional-or-keyword | `list[list[str]]` | `obrigatório/ausente` | Parâmetro `rows` recebido pela função; não há descrição individual explícita no código. |
| `name` | positional-or-keyword | `str` | `''` | Parâmetro `name` recebido pela função; não há descrição individual explícita no código. |
| `caption` | positional-or-keyword | `str` | `''` | Parâmetro `caption` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'\n'.join(lines)`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `' | '.join`
- `'\n'.join`
- `len`
- `lines.append`
- `str`
- `str(cell or '').replace`
- `str(cell or '').replace('\n', ' ').replace`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_prisma_org:render_prisma_org`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `---|`

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `91c756bfc15dab47df8e070320f170d6748b031f7e5b99f4f13ba99254e3992e`
**LOC do arquivo-fonte:** 171


### `app_bundle.scripts.pipeline.render_prisma_org:_study_rows`

**Assinatura:**
```python
def _study_rows(studies: list[StudyRecord]) -> list[list[str]]
```

**Arquivo:** `app_bundle/scripts/pipeline/render_prisma_org.py`
**Linhas:** 50–62
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_study_rows` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `studies` | positional-or-keyword | `list[StudyRecord]` | `obrigatório/ausente` | Parâmetro `studies` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[list[str]]`. Retornos observados estaticamente incluem: `rows`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'; '.join`
- `enumerate`
- `rows.append`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_prisma_org:render_prisma_org`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `91c756bfc15dab47df8e070320f170d6748b031f7e5b99f4f13ba99254e3992e`
**LOC do arquivo-fonte:** 171


### `app_bundle.scripts.pipeline.render_prisma_org:render_prisma_org`

**Assinatura:**
```python
def render_prisma_org(report: PrismaReport, output_path: Path, bib_filename: str | None = None, cfg: dict | None = None) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/render_prisma_org.py`
**Linhas:** 65–171
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Renderiza a responsabilidade representada por `prisma_org` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `report` | positional-or-keyword | `PrismaReport` | `obrigatório/ausente` | Parâmetro `report` recebido pela função; não há descrição individual explícita no código. |
| `output_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `bib_filename` | positional-or-keyword | `str \| None` | `None` | Parâmetro `bib_filename` recebido pela função; não há descrição individual explícita no código. |
| `cfg` | positional-or-keyword | `dict \| None` | `None` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `org`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `'; '.join`
- `'\n'.join`
- `Path`
- `_study_rows`
- `_table`
- `bibliography_style_from_cfg`
- `latex_escape`
- `lines.append`
- `org.replace`
- `org.replace('\n\n\n', '\n\n').strip`
- `render_bibliography_preamble`
- `render_prisma_flow_latex`
- `str`
- `strip_org_cite_export_lines`
- `write_text`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_pipeline:run_prisma_report_outputs`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 11
loops: 0
comprehensions: 4
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `91c756bfc15dab47df8e070320f170d6748b031f7e5b99f4f13ba99254e3992e`
**LOC do arquivo-fonte:** 171


### `app_bundle.scripts.pipeline.render_prisma_xlsx:_autosize`

**Assinatura:**
```python
def _autosize(ws) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/render_prisma_xlsx.py`
**Linhas:** 20–28
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_autosize` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `ws` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `ws` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Alignment`
- `get_column_letter`
- `len`
- `max`
- `min`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_prisma_xlsx:_append_studies`
- `app_bundle.scripts.pipeline.render_prisma_xlsx:render_prisma_xlsx`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `73fcefcc0a1b32e6f8834dbd56e396b64ea91fb64e1a26411b6425ee30771805`
**LOC do arquivo-fonte:** 99


### `app_bundle.scripts.pipeline.render_prisma_xlsx:_style_header`

**Assinatura:**
```python
def _style_header(ws) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/render_prisma_xlsx.py`
**Linhas:** 31–39
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_style_header` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `ws` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `ws` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Alignment`
- `Border`
- `Font`
- `PatternFill`
- `Side`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_prisma_xlsx:_append_studies`
- `app_bundle.scripts.pipeline.render_prisma_xlsx:render_prisma_xlsx`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `73fcefcc0a1b32e6f8834dbd56e396b64ea91fb64e1a26411b6425ee30771805`
**LOC do arquivo-fonte:** 99


### `app_bundle.scripts.pipeline.render_prisma_xlsx:_append_studies`

**Assinatura:**
```python
def _append_studies(ws, studies: list[StudyRecord]) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/render_prisma_xlsx.py`
**Linhas:** 42–50
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_append_studies` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `ws` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `ws` recebido pela função; não há descrição individual explícita no código. |
| `studies` | positional-or-keyword | `list[StudyRecord]` | `obrigatório/ausente` | Parâmetro `studies` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'; '.join`
- `_autosize`
- `_style_header`
- `ws.append`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_prisma_xlsx:render_prisma_xlsx`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `73fcefcc0a1b32e6f8834dbd56e396b64ea91fb64e1a26411b6425ee30771805`
**LOC do arquivo-fonte:** 99


### `app_bundle.scripts.pipeline.render_prisma_xlsx:render_prisma_xlsx`

**Assinatura:**
```python
def render_prisma_xlsx(report: PrismaReport, output_path: Path) -> Path
```

**Arquivo:** `app_bundle/scripts/pipeline/render_prisma_xlsx.py`
**Linhas:** 53–99
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Renderiza a responsabilidade representada por `prisma_xlsx` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `report` | positional-or-keyword | `PrismaReport` | `obrigatório/ausente` | Parâmetro `report` recebido pela função; não há descrição individual explícita no código. |
| `output_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `output_path`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `RuntimeError`
- `Workbook`
- `_append_studies`
- `_autosize`
- `_style_header`
- `list`
- `output_path.parent.mkdir`
- `str`
- `wb.create_sheet`
- `wb.save`
- `ws.append`
- `ws_q.append`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.prisma_pipeline:run_prisma_report_outputs`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('openpyxl não está instalado para exportar XLSX.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `73fcefcc0a1b32e6f8834dbd56e396b64ea91fb64e1a26411b6425ee30771805`
**LOC do arquivo-fonte:** 99


### `app_bundle.scripts.pipeline.utils:slugify`

**Assinatura:**
```python
def slugify(text: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/utils.py`
**Linhas:** 12–17
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `slugify` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `re.sub('_+', '_', text).strip('_') or 'item'`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `re.sub`
- `re.sub('_+', '_', text).strip`
- `repl.items`
- `str`
- `str(text or '').strip`
- `str(text or '').strip().lower`
- `text.replace`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `2acb618894448925c6b54b4bc038cc183a56142a9ca5dadb3fe4fcc167554727`
**LOC do arquivo-fonte:** 73


### `app_bundle.scripts.pipeline.utils:normalize_title_loose`

**Assinatura:**
```python
def normalize_title_loose(text: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/utils.py`
**Linhas:** 20–25
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Normaliza a responsabilidade representada por `title_loose` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `re.sub('\\s+', ' ', text).strip()`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `re.sub`
- `re.sub('\\s+', ' ', text).strip`
- `repl.items`
- `str`
- `str(text or '').strip`
- `str(text or '').strip().lower`
- `text.replace`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.bibliography_manager:_match_doc_to_bib_key`
- `app_bundle.scripts.pipeline.bibliography_manager:doi_for_doc`
- `app_bundle.scripts.pipeline.bibliography_manager:entry_identity`
- `app_bundle.scripts.pipeline.bibliography_manager:load_doi_manifest`
- `app_bundle.scripts.pipeline.bibliography_manager:make_bib_key`
- `app_bundle.scripts.pipeline.diagnostics:check_config`
- `app_bundle.scripts.pipeline.diagnostics:validate_docx_file`
- `app_bundle.scripts.pipeline.document_validator:_find_technical_leak_locations_in_model.check`
- `app_bundle.scripts.pipeline.document_validator:_technical_term_pattern`
- `app_bundle.scripts.pipeline.document_validator:find_technical_leaks_in_text`
- `app_bundle.scripts.pipeline.document_validator:validate_document_model`
- `app_bundle.scripts.pipeline.document_validator:validate_org_text`
- `app_bundle.scripts.pipeline.institution_compliance:_check_config`
- `app_bundle.scripts.pipeline.institution_compliance:_check_org`
- `app_bundle.scripts.pipeline.prisma_builder:_collect_records_from_json`
- `app_bundle.scripts.pipeline.prisma_builder:load_records_from_research_dir`
- `app_bundle.scripts.pipeline.quality_report:_technical_terms_found`
- `app_bundle.scripts.pipeline.quality_report:build_quality_report`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `2acb618894448925c6b54b4bc038cc183a56142a9ca5dadb3fe4fcc167554727`
**LOC do arquivo-fonte:** 73


### `app_bundle.scripts.pipeline.utils:shorten_text`

**Assinatura:**
```python
def shorten_text(text: str, limit: int = 12000) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/utils.py`
**Linhas:** 28–30
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `shorten_text` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |
| `limit` | positional-or-keyword | `int` | `12000` | Parâmetro `limit` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `text if len(text) <= limit else text[:limit].rstrip() + '…'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `len`
- `re.sub`
- `re.sub('\\s+', ' ', str(text or '')).strip`
- `str`
- `text[:limit].rstrip`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.bibliography_manager:infer_bib_metadata_ai`
- `app_bundle.scripts.pipeline.corpus_manager:read_text_file_with_diagnostics`
- `app_bundle.scripts.pipeline.document_builder:_docs_index_context`
- `app_bundle.scripts.pipeline.document_builder:_generate_article_section`
- `app_bundle.scripts.pipeline.document_builder:_generate_comparison_section`
- `app_bundle.scripts.pipeline.document_builder:_generate_intro_synthesis_bundle`
- `app_bundle.scripts.pipeline.document_builder:_orientation_context`
- `app_bundle.scripts.pipeline.document_builder:_section_plain_text`
- `app_bundle.scripts.pipeline.document_builder:build_prompt`
- `app_bundle.scripts.pipeline.mindmap_manager:build_mindmap_prompt`
- `app_bundle.scripts.pipeline.prisma_builder:_record_from_any`
- `app_bundle.scripts.pipeline.prisma_builder:study_from_doc`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `2acb618894448925c6b54b4bc038cc183a56142a9ca5dadb3fe4fcc167554727`
**LOC do arquivo-fonte:** 73


### `app_bundle.scripts.pipeline.utils:write_text`

**Assinatura:**
```python
def write_text(path: Path, content: str) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/utils.py`
**Linhas:** 33–35
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Grava a responsabilidade representada por `text` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `content` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `content` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `path.parent.mkdir`
- `path.write_text`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `2acb618894448925c6b54b4bc038cc183a56142a9ca5dadb3fe4fcc167554727`
**LOC do arquivo-fonte:** 73


### `app_bundle.scripts.pipeline.utils:read_json`

**Assinatura:**
```python
def read_json(path: Path) -> Any
```

**Arquivo:** `app_bundle/scripts/pipeline/utils.py`
**Linhas:** 38–39
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Lê a responsabilidade representada por `json` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `Any`. Retornos observados estaticamente incluem: `json.loads(path.read_text(encoding='utf-8'))`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `json.loads`
- `path.read_text`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `2acb618894448925c6b54b4bc038cc183a56142a9ca5dadb3fe4fcc167554727`
**LOC do arquivo-fonte:** 73


### `app_bundle.scripts.pipeline.utils:write_json`

**Assinatura:**
```python
def write_json(path: Path, payload: Any) -> None
```

**Arquivo:** `app_bundle/scripts/pipeline/utils.py`
**Linhas:** 42–43
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Grava a responsabilidade representada por `json` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `payload` | positional-or-keyword | `Any` | `obrigatório/ausente` | Payload estruturado recebido ou produzido pela operação. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `json.dumps`
- `write_text`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.default_runtime:_ap003f_pipeline_core`
- `academic_pipeline.doctor_runtime:run_doctor_command`
- `academic_pipeline.prisma_generic_orchestration:run_prisma_stage_004`
- `app_bundle.scripts.pipeline.bibliography_manager:build_bibliography`
- `app_bundle.scripts.pipeline.document_builder:_write_checkpoint`
- `app_bundle.scripts.pipeline.institution_compliance:write_compliance_reports`
- `app_bundle.scripts.pipeline.prisma_pipeline:run_prisma_report_outputs`
- `app_bundle.scripts.pipeline.project_tools:inspect_bib`
- `app_bundle.scripts.pipeline.prompt_lock:write_prompt_lock`
- `app_bundle.scripts.pipeline.quality_report:write_quality_report`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `2acb618894448925c6b54b4bc038cc183a56142a9ca5dadb3fe4fcc167554727`
**LOC do arquivo-fonte:** 73


### `app_bundle.scripts.pipeline.utils:resolve_path`

**Assinatura:**
```python
def resolve_path(raw: Any, base_dir: Path | None = None) -> Path | None
```

**Arquivo:** `app_bundle/scripts/pipeline/utils.py`
**Linhas:** 46–55
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Resolve a responsabilidade representada por `path` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `raw` | positional-or-keyword | `Any` | `obrigatório/ausente` | Parâmetro `raw` recebido pela função; não há descrição individual explícita no código. |
| `base_dir` | positional-or-keyword | `Path \| None` | `None` | Parâmetro `base_dir` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path | None`. Retornos observados estaticamente incluem: `None`; `p.resolve()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path.cwd`
- `os.path.expanduser`
- `p.is_absolute`
- `p.resolve`
- `str`
- `str(raw).strip`

#### Chamadores inferidos no escopo auditado

- `academic_pipeline.default_runtime:work_cache_paths`
- `academic_pipeline.prisma_generic_orchestration:_ap003e_body_render_external_prisma_outputs_1`
- `academic_pipeline.prisma_generic_orchestration:_ap003e_body_research_output_paths_1`
- `app_bundle.scripts.pipeline.bibliography_manager:build_bibliography`
- `app_bundle.scripts.pipeline.corpus_manager:collect_orientation_docs`
- `app_bundle.scripts.pipeline.corpus_manager:discover_local_documents`
- `app_bundle.scripts.pipeline.diagnostics:_out_dir_and_prefix_from_cfg`
- `app_bundle.scripts.pipeline.diagnostics:_work_cache_dirs_from_cfg`
- `app_bundle.scripts.pipeline.diagnostics:check_config`
- `app_bundle.scripts.pipeline.diagnostics:make_run_report`
- `app_bundle.scripts.pipeline.diagnostics:run_doctor`
- `app_bundle.scripts.pipeline.institution_compliance:_check_config`
- `app_bundle.scripts.pipeline.prisma_builder:build_prisma_report`
- `app_bundle.scripts.pipeline.prisma_pipeline:prisma_output_paths`
- `app_bundle.scripts.pipeline.prisma_pipeline:run_prisma_report_outputs`
- `app_bundle.scripts.pipeline.render_org_latex:_as_latex_graphics_path`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `2acb618894448925c6b54b4bc038cc183a56142a9ca5dadb3fe4fcc167554727`
**LOC do arquivo-fonte:** 73


### `app_bundle.scripts.pipeline.utils:latex_escape`

**Assinatura:**
```python
def latex_escape(value: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/utils.py`
**Linhas:** 58–63
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `latex_escape` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `''.join((replacements.get(ch, ch) for ch in str(value or '')))`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `''.join`
- `replacements.get`
- `str`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:_latex_table_cell`
- `app_bundle.scripts.pipeline.render_org_latex:_render_paper_abstract_rows`
- `app_bundle.scripts.pipeline.render_org_latex:_render_table_as_latex`
- `app_bundle.scripts.pipeline.render_org_latex:render_activity_front_matter`
- `app_bundle.scripts.pipeline.render_org_latex:render_dissertation_front_matter`
- `app_bundle.scripts.pipeline.render_org_latex:render_paper_front_matter`
- `app_bundle.scripts.pipeline.render_prisma_org:render_prisma_org`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `2acb618894448925c6b54b4bc038cc183a56142a9ca5dadb3fe4fcc167554727`
**LOC do arquivo-fonte:** 73


### `app_bundle.scripts.pipeline.utils:normalize_snippet_placeholders`

**Assinatura:**
```python
def normalize_snippet_placeholders(text: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/utils.py`
**Linhas:** 66–69
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Normaliza a responsabilidade representada por `snippet_placeholders` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `text`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `m.group`
- `m.group(1).strip`
- `re.sub`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.render_org_latex:render_org_latex`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `2acb618894448925c6b54b4bc038cc183a56142a9ca5dadb3fe4fcc167554727`
**LOC do arquivo-fonte:** 73


### `app_bundle.scripts.pipeline.utils:safe_filename`

**Assinatura:**
```python
def safe_filename(name: str) -> str
```

**Arquivo:** `app_bundle/scripts/pipeline/utils.py`
**Linhas:** 72–73
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `safe_filename` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `name` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `name` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `slugify(Path(name).stem) + Path(name).suffix.lower()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(name).suffix.lower`
- `slugify`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `2acb618894448925c6b54b4bc038cc183a56142a9ca5dadb3fe4fcc167554727`
**LOC do arquivo-fonte:** 73


### `app_bundle.scripts.pipeline.validar_artigo_longo_fulltext:count_words`

**Assinatura:**
```python
def count_words(text: str) -> int
```

**Arquivo:** `app_bundle/scripts/pipeline/validar_artigo_longo_fulltext.py`
**Linhas:** 9–13
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `count_words` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `len(re.findall('\\b[\\wÀ-ÿ-]+\\b', text))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `len`
- `re.findall`
- `re.sub`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.validar_artigo_longo_fulltext:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f1f2e8b94a8826e1251bd169f8946b611a367fc36fbe998ad73d565f2a7d86c1`
**LOC do arquivo-fonte:** 99


### `app_bundle.scripts.pipeline.validar_artigo_longo_fulltext:count_bib_entries`

**Assinatura:**
```python
def count_bib_entries(text: str) -> int
```

**Arquivo:** `app_bundle/scripts/pipeline/validar_artigo_longo_fulltext.py`
**Linhas:** 15–16
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `count_bib_entries` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `len(re.findall('(?m)^@\\w+\\s*\\{', text))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `len`
- `re.findall`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.validar_artigo_longo_fulltext:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f1f2e8b94a8826e1251bd169f8946b611a367fc36fbe998ad73d565f2a7d86c1`
**LOC do arquivo-fonte:** 99


### `app_bundle.scripts.pipeline.validar_artigo_longo_fulltext:count_pdf_pages`

**Assinatura:**
```python
def count_pdf_pages(pdf: Path) -> int
```

**Arquivo:** `app_bundle/scripts/pipeline/validar_artigo_longo_fulltext.py`
**Linhas:** 18–28
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `count_pdf_pages` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `pdf` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `pdf` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `0`; `int(m.group(1))`.

#### Efeitos colaterais e fronteiras externas

- Executa processo ou ferramenta externa.

#### Chamadas realizadas pelo corpo direto

- `int`
- `m.group`
- `pdf.exists`
- `re.search`
- `str`
- `subprocess.check_output`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.validar_artigo_longo_fulltext:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `f1f2e8b94a8826e1251bd169f8946b611a367fc36fbe998ad73d565f2a7d86c1`
**LOC do arquivo-fonte:** 99


### `app_bundle.scripts.pipeline.validar_artigo_longo_fulltext:main`

**Assinatura:**
```python
def main()
```

**Arquivo:** `app_bundle/scripts/pipeline/validar_artigo_longo_fulltext.py`
**Linhas:** 30–96
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa o ponto de entrada correspondente e coordena o fluxo associado.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Sem anotação de retorno explícita. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `SystemExit`
- `ap.add_argument`
- `ap.parse_args`
- `argparse.ArgumentParser`
- `bib.exists`
- `bib.read_text`
- `count_bib_entries`
- `count_pdf_pages`
- `count_words`
- `doc_json.exists`
- `doc_json.read_text`
- `errors.append`
- `org.exists`
- `org.read_text`
- `org_txt.lower`
- `print`
- `re.search`
- `term.lower`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `SystemExit(1)`

#### Opções CLI mencionadas diretamente no corpo

- `--art-dir`
- `--min-paginas`
- `--min-palavras`
- `--min-referencias`

#### Artefatos/caminhos literais reconhecidos no corpo

- `artigo_final_atestmed_abnt.org`
- `artigo_final_atestmed_abnt.bib`
- `artigo_final_atestmed_abnt.pdf`
- `artigo_final_atestmed_abnt.document.json`
- `document.json palavras aproximadas: `

#### Complexidade estrutural observada

```yaml
branches: 9
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `f1f2e8b94a8826e1251bd169f8946b611a367fc36fbe998ad73d565f2a7d86c1`
**LOC do arquivo-fonte:** 99


### `atualizar_academic_pipeline_bundle:norm_rel`

**Assinatura:**
```python
def norm_rel(path: Path) -> str
```

**Arquivo:** `atualizar_academic_pipeline_bundle.py`
**Linhas:** 80–81
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `norm_rel` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `path.as_posix().lstrip('./')`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `path.as_posix`
- `path.as_posix().lstrip`

#### Chamadores inferidos no escopo auditado

- `atualizar_academic_pipeline_bundle:collect_files_to_copy`
- `atualizar_academic_pipeline_bundle:is_protected`
- `atualizar_academic_pipeline_bundle:main`
- `atualizar_academic_pipeline_bundle:make_backup_and_copy`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `a0c0e4ccb2217552514faf5a5b612bf3c3a5c5daafc5d60c61c7313852d76cfb`
**LOC do arquivo-fonte:** 474


### `atualizar_academic_pipeline_bundle:is_protected`

**Assinatura:**
```python
def is_protected(rel: str) -> bool
```

**Arquivo:** `atualizar_academic_pipeline_bundle.py`
**Linhas:** 84–86
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Verifica se `protected` conforme as regras codificadas na rotina.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `rel` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `rel` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `bool`. Retornos observados estaticamente incluem: `any((rel == p.rstrip('/') or rel.startswith(p) for p in PROTECTED_PREFIXES))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `any`
- `norm_rel`
- `p.rstrip`
- `rel.startswith`

#### Chamadores inferidos no escopo auditado

- `atualizar_academic_pipeline_bundle:collect_files_to_copy`
- `atualizar_academic_pipeline_bundle:make_backup_and_copy`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `a0c0e4ccb2217552514faf5a5b612bf3c3a5c5daafc5d60c61c7313852d76cfb`
**LOC do arquivo-fonte:** 474


### `atualizar_academic_pipeline_bundle:log`

**Assinatura:**
```python
def log(msg: str) -> None
```

**Arquivo:** `atualizar_academic_pipeline_bundle.py`
**Linhas:** 89–90
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `log` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `msg` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `msg` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `print`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `a0c0e4ccb2217552514faf5a5b612bf3c3a5c5daafc5d60c61c7313852d76cfb`
**LOC do arquivo-fonte:** 474


### `atualizar_academic_pipeline_bundle:fail`

**Assinatura:**
```python
def fail(msg: str, code: int = 1) -> None
```

**Arquivo:** `atualizar_academic_pipeline_bundle.py`
**Linhas:** 93–95
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `fail` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `msg` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `msg` recebido pela função; não há descrição individual explícita no código. |
| `code` | positional-or-keyword | `int` | `1` | Parâmetro `code` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `SystemExit`
- `print`

#### Chamadores inferidos no escopo auditado

- `atualizar_academic_pipeline_bundle:find_src_root`
- `atualizar_academic_pipeline_bundle:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `SystemExit(code)`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `a0c0e4ccb2217552514faf5a5b612bf3c3a5c5daafc5d60c61c7313852d76cfb`
**LOC do arquivo-fonte:** 474


### `atualizar_academic_pipeline_bundle:unzip_to_temp`

**Assinatura:**
```python
def unzip_to_temp(zip_path: Path, tmp_root: Path) -> Path
```

**Arquivo:** `atualizar_academic_pipeline_bundle.py`
**Linhas:** 98–102
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `unzip_to_temp` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `zip_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `tmp_root` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `tmp_root` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `tmp_root`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `log`
- `zf.extractall`
- `zipfile.ZipFile`

#### Chamadores inferidos no escopo auditado

- `atualizar_academic_pipeline_bundle:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `a0c0e4ccb2217552514faf5a5b612bf3c3a5c5daafc5d60c61c7313852d76cfb`
**LOC do arquivo-fonte:** 474


### `atualizar_academic_pipeline_bundle:find_src_root`

**Assinatura:**
```python
def find_src_root(extract_dir: Path) -> Path
```

**Arquivo:** `atualizar_academic_pipeline_bundle.py`
**Linhas:** 105–123
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Localiza a responsabilidade representada por `src_root` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `extract_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `extract_dir` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `src_root`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `(p.parent / 'academic_pipeline' / '__main__.py').is_file`
- `(p.parent / 'pyproject.toml').is_file`
- `candidates.append`
- `candidates.sort`
- `extract_dir.rglob`
- `fail`
- `len`
- `log`
- `p.is_dir`

#### Chamadores inferidos no escopo auditado

- `atualizar_academic_pipeline_bundle:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `pyproject.toml`
- `Não encontrei academic_pipeline/__main__.py e pyproject.toml junto de app_bundle no ZIP.`

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `a0c0e4ccb2217552514faf5a5b612bf3c3a5c5daafc5d60c61c7313852d76cfb`
**LOC do arquivo-fonte:** 474


### `atualizar_academic_pipeline_bundle:iter_files_under`

**Assinatura:**
```python
def iter_files_under(base: Path) -> Iterable[Path]
```

**Arquivo:** `atualizar_academic_pipeline_bundle.py`
**Linhas:** 126–137
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `iter_files_under` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `base` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `base` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Iterable[Path]`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `base.exists`
- `base.is_file`
- `os.walk`

#### Chamadores inferidos no escopo auditado

- `atualizar_academic_pipeline_bundle:collect_files_to_copy`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 2
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `a0c0e4ccb2217552514faf5a5b612bf3c3a5c5daafc5d60c61c7313852d76cfb`
**LOC do arquivo-fonte:** 474


### `atualizar_academic_pipeline_bundle:collect_files_to_copy`

**Assinatura:**
```python
def collect_files_to_copy(src_root: Path, copy_prefixes: list[str], include_root_files: bool) -> list[Path]
```

**Arquivo:** `atualizar_academic_pipeline_bundle.py`
**Linhas:** 140–170
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Coleta a responsabilidade representada por `files_to_copy` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `src_root` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `src_root` recebido pela função; não há descrição individual explícita no código. |
| `copy_prefixes` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `copy_prefixes` recebido pela função; não há descrição individual explícita no código. |
| `include_root_files` | positional-or-keyword | `bool` | `obrigatório/ausente` | Parâmetro `include_root_files` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[Path]`. Retornos observados estaticamente incluem: `unique`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `f.exists`
- `f.is_file`
- `f.relative_to`
- `files.append`
- `is_protected`
- `iter_files_under`
- `norm_rel`
- `prefix.strip`
- `seen.add`
- `set`
- `unique.append`

#### Chamadores inferidos no escopo auditado

- `atualizar_academic_pipeline_bundle:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 4
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `a0c0e4ccb2217552514faf5a5b612bf3c3a5c5daafc5d60c61c7313852d76cfb`
**LOC do arquivo-fonte:** 474


### `atualizar_academic_pipeline_bundle:make_backup_and_copy`

**Assinatura:**
```python
def make_backup_and_copy(src_root: Path, dst_root: Path, files: list[Path], backup_root: Path, dry_run: bool = False) -> dict
```

**Arquivo:** `atualizar_academic_pipeline_bundle.py`
**Linhas:** 173–225
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Cria a responsabilidade representada por `backup_and_copy` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `src_root` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `src_root` recebido pela função; não há descrição individual explícita no código. |
| `dst_root` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `dst_root` recebido pela função; não há descrição individual explícita no código. |
| `files` | positional-or-keyword | `list[Path]` | `obrigatório/ausente` | Parâmetro `files` recebido pela função; não há descrição individual explícita no código. |
| `backup_root` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `backup_root` recebido pela função; não há descrição individual explícita no código. |
| `dry_run` | positional-or-keyword | `bool` | `False` | Parâmetro `dry_run` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict`. Retornos observados estaticamente incluem: `manifest`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `(backup_root / 'manifest_update.json').write_text`
- `_dt.datetime.now`
- `_dt.datetime.now().isoformat`
- `backup_file.parent.mkdir`
- `backup_root.mkdir`
- `dst_file.exists`
- `dst_file.parent.mkdir`
- `is_protected`
- `json.dumps`
- `len`
- `log`
- `manifest['backed_up'].append`
- `manifest['copied'].append`
- `manifest['new_files'].append`
- `norm_rel`
- `shutil.copy2`
- `src_file.relative_to`
- `str`

#### Chamadores inferidos no escopo auditado

- `atualizar_academic_pipeline_bundle:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `manifest_update.json`

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `a0c0e4ccb2217552514faf5a5b612bf3c3a5c5daafc5d60c61c7313852d76cfb`
**LOC do arquivo-fonte:** 474


### `atualizar_academic_pipeline_bundle:rollback`

**Assinatura:**
```python
def rollback(manifest: dict) -> None
```

**Arquivo:** `atualizar_academic_pipeline_bundle.py`
**Linhas:** 228–249
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `rollback` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `manifest` | positional-or-keyword | `dict` | `obrigatório/ausente` | Parâmetro `manifest` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `backup_file.exists`
- `dst_file.exists`
- `dst_file.parent.mkdir`
- `dst_file.unlink`
- `list`
- `log`
- `manifest.get`
- `reversed`
- `set`
- `shutil.copy2`

#### Chamadores inferidos no escopo auditado

- `atualizar_academic_pipeline_bundle:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `a0c0e4ccb2217552514faf5a5b612bf3c3a5c5daafc5d60c61c7313852d76cfb`
**LOC do arquivo-fonte:** 474


### `atualizar_academic_pipeline_bundle:run_cmd`

**Assinatura:**
```python
def run_cmd(cmd: list[str], cwd: Path, log_file: Path, check: bool = True) -> int
```

**Arquivo:** `atualizar_academic_pipeline_bundle.py`
**Linhas:** 252–281
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `cmd` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cmd` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `cmd` recebido pela função; não há descrição individual explícita no código. |
| `cwd` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `cwd` recebido pela função; não há descrição individual explícita no código. |
| `log_file` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `log_file` recebido pela função; não há descrição individual explícita no código. |
| `check` | positional-or-keyword | `bool` | `True` | Parâmetro `check` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `rc`.

#### Efeitos colaterais e fronteiras externas

- Abre arquivo para leitura ou inspeção.
- Executa processo ou ferramenta externa.
- Interage com o usuário ou escreve saída em terminal/interface.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `RuntimeError`
- `lf.write`
- `log`
- `log_file.open`
- `print`
- `proc.wait`
- `str`
- `subprocess.Popen`

#### Chamadores inferidos no escopo auditado

- `atualizar_academic_pipeline_bundle:run_tests`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError(f'Comando falhou com código {rc}: {rendered}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `a0c0e4ccb2217552514faf5a5b612bf3c3a5c5daafc5d60c61c7313852d76cfb`
**LOC do arquivo-fonte:** 474


### `atualizar_academic_pipeline_bundle:run_tests`

**Assinatura:**
```python
def run_tests(dst_root: Path, config: str | None, document_json: str | None, do_render: bool, do_show_prompts: bool, do_pipenv_sync: bool, log_file: Path) -> None
```

**Arquivo:** `atualizar_academic_pipeline_bundle.py`
**Linhas:** 284–346
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `tests` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `dst_root` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `dst_root` recebido pela função; não há descrição individual explícita no código. |
| `config` | positional-or-keyword | `str \| None` | `obrigatório/ausente` | Configuração ou estrutura de configuração recebida pela rotina. |
| `document_json` | positional-or-keyword | `str \| None` | `obrigatório/ausente` | Parâmetro `document_json` recebido pela função; não há descrição individual explícita no código. |
| `do_render` | positional-or-keyword | `bool` | `obrigatório/ausente` | Parâmetro `do_render` recebido pela função; não há descrição individual explícita no código. |
| `do_show_prompts` | positional-or-keyword | `bool` | `obrigatório/ausente` | Parâmetro `do_show_prompts` recebido pela função; não há descrição individual explícita no código. |
| `do_pipenv_sync` | positional-or-keyword | `bool` | `obrigatório/ausente` | Parâmetro `do_pipenv_sync` recebido pela função; não há descrição individual explícita no código. |
| `log_file` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `log_file` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `RuntimeError`
- `log`
- `p.relative_to`
- `pipeline_dir.glob`
- `run_cmd`
- `sorted`
- `str`

#### Chamadores inferidos no escopo auditado

- `atualizar_academic_pipeline_bundle:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('--render exige --config.')`
- `RuntimeError('--render exige --document-json.')`

#### Opções CLI mencionadas diretamente no corpo

- `--check-config`
- `--config`
- `--doctor`
- `--document-json`
- `--list-toml-profiles`
- `--render exige --config.`
- `--render exige --document-json.`
- `--show-prompts`
- `--somente-renderizar`

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 7
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `a0c0e4ccb2217552514faf5a5b612bf3c3a5c5daafc5d60c61c7313852d76cfb`
**LOC do arquivo-fonte:** 474


### `atualizar_academic_pipeline_bundle:parse_args`

**Assinatura:**
```python
def parse_args() -> argparse.Namespace
```

**Arquivo:** `atualizar_academic_pipeline_bundle.py`
**Linhas:** 349–388
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Interpreta a responsabilidade representada por `args` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `argparse.Namespace`. Retornos observados estaticamente incluem: `parser.parse_args()`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `argparse.ArgumentParser`
- `parser.add_argument`
- `parser.parse_args`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `--config`
- `--copy-prefix`
- `--copy-root-files`
- `--document-json`
- `--dry-run`
- `--dst`
- `--pipenv-sync`
- `--render`
- `--rollback-on-fail`
- `--show-prompts`
- `--zip`

#### Artefatos/caminhos literais reconhecidos no corpo

- `Caminho do TOML relativo ao destino, ex.: app_bundle/projetos/.../config.toml`
- `Caminho do document.json relativo ao destino, usado com --render.`
- `Copia também arquivos de raiz como README, Pipfile e requirements.txt.`

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `a0c0e4ccb2217552514faf5a5b612bf3c3a5c5daafc5d60c61c7313852d76cfb`
**LOC do arquivo-fonte:** 474


### `atualizar_academic_pipeline_bundle:main`

**Assinatura:**
```python
def main() -> int
```

**Arquivo:** `atualizar_academic_pipeline_bundle.py`
**Linhas:** 391–470
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa o ponto de entrada correspondente e coordena o fluxo associado.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `0`; `1`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(args.dst).expanduser`
- `Path(args.dst).expanduser().resolve`
- `Path(args.zip).expanduser`
- `Path(args.zip).expanduser().resolve`
- `_dt.datetime.now`
- `_dt.datetime.now().strftime`
- `backup_root.mkdir`
- `canonical_metadata.is_file`
- `canonical_package.is_file`
- `collect_files_to_copy`
- `dst_root.exists`
- `f.relative_to`
- `fail`
- `find_src_root`
- `log`
- `make_backup_and_copy`
- `norm_rel`
- `parse_args`
- `rollback`
- `run_tests`
- `tempfile.TemporaryDirectory`
- `unzip_to_temp`
- `zip_path.exists`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `pyproject.toml`
- `update_test_log.txt`

#### Complexidade estrutural observada

```yaml
branches: 7
loops: 1
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `a0c0e4ccb2217552514faf5a5b612bf3c3a5c5daafc5d60c61c7313852d76cfb`
**LOC do arquivo-fonte:** 474


### `configurar_pretriagem_ia_prisma:_section_bounds`

**Assinatura:**
```python
def _section_bounds(lines: list[str]) -> tuple[int, int]
```

**Arquivo:** `configurar_pretriagem_ia_prisma.py`
**Linhas:** 31–45
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_section_bounds` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `lines` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `lines` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `tuple[int, int]`. Retornos observados estaticamente incluem: `(start, end)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `RuntimeError`
- `enumerate`
- `len`
- `line.strip`
- `lines[index].lstrip`
- `range`
- `stripped.startswith`

#### Chamadores inferidos no escopo auditado

- `configurar_pretriagem_ia_prisma:_render_updated`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('A seção [busca_prisma] não foi encontrada no TOML.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `82d629145538c05120256e07f7567c3de0bb0244822ab5433d1c41d565c9bdc9`
**LOC do arquivo-fonte:** 124


### `configurar_pretriagem_ia_prisma:_render_updated`

**Assinatura:**
```python
def _render_updated(text: str) -> str
```

**Arquivo:** `configurar_pretriagem_ia_prisma.py`
**Linhas:** 48–71
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Renderiza a responsabilidade representada por `updated` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `rendered + ('\n' if had_newline or rendered else '')`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `_section_bounds`
- `dict`
- `insertion.append`
- `insertion.extend`
- `match.group`
- `pattern.match`
- `range`
- `re.compile`
- `seen.add`
- `set`
- `text.endswith`
- `text.splitlines`

#### Chamadores inferidos no escopo auditado

- `configurar_pretriagem_ia_prisma:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 1
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `82d629145538c05120256e07f7567c3de0bb0244822ab5433d1c41d565c9bdc9`
**LOC do arquivo-fonte:** 124


### `configurar_pretriagem_ia_prisma:_validate_toml`

**Assinatura:**
```python
def _validate_toml(text: str) -> None
```

**Arquivo:** `configurar_pretriagem_ia_prisma.py`
**Linhas:** 74–78
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Valida a responsabilidade representada por `toml` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `text` | positional-or-keyword | `str` | `obrigatório/ausente` | Conteúdo textual de entrada. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `RuntimeError`
- `tomllib.loads`

#### Chamadores inferidos no escopo auditado

- `configurar_pretriagem_ia_prisma:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError(f'A atualização proposta gerou TOML inválido: {exc}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `82d629145538c05120256e07f7567c3de0bb0244822ab5433d1c41d565c9bdc9`
**LOC do arquivo-fonte:** 124


### `configurar_pretriagem_ia_prisma:main`

**Assinatura:**
```python
def main() -> int
```

**Arquivo:** `configurar_pretriagem_ia_prisma.py`
**Linhas:** 81–116
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa o ponto de entrada correspondente e coordena o fluxo associado.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `0`.

#### Efeitos colaterais e fronteiras externas

- Abre arquivo para leitura ou inspeção.
- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `FileNotFoundError`
- `Path`
- `Path(args.config).expanduser`
- `Path(args.config).expanduser().resolve`
- `_render_updated`
- `_validate_toml`
- `argparse.ArgumentParser`
- `datetime.now`
- `datetime.now().strftime`
- `parser.add_argument`
- `parser.parse_args`
- `path.is_file`
- `path.open`
- `path.read_text`
- `path.with_name`
- `path.write_text`
- `print`
- `shutil.copy2`
- `tomllib.load`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `FileNotFoundError(f'TOML não encontrado: {path}')`

#### Opções CLI mencionadas diretamente no corpo

- `--dry-run`

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `82d629145538c05120256e07f7567c3de0bb0244822ab5433d1c41d565c9bdc9`
**LOC do arquivo-fonte:** 124


### `diagnosticar_fontes_prisma:_project_root`

**Assinatura:**
```python
def _project_root() -> Path
```

**Arquivo:** `diagnosticar_fontes_prisma.py`
**Linhas:** 26–35
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Localiza a raiz quando o script foi copiado para ela.

**Docstring resumida:** Localiza a raiz quando o script foi copiado para ela.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `candidate`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `(candidate / 'app_bundle' / 'scripts' / 'pipeline' / 'prisma_busca_externa.py').is_file`
- `Path`
- `Path(__file__).resolve`
- `Path.cwd`
- `Path.cwd().resolve`
- `RuntimeError`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError('Não encontrei app_bundle/scripts/pipeline/prisma_busca_externa.py. Copie este script para a raiz do projeto e execute-o a partir dela.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `fcad537963f75f26341502c0b42f5c65cac9b58c3dd73e61b6e31b5bf44f0910`
**LOC do arquivo-fonte:** 245


### `diagnosticar_fontes_prisma:_load_config`

**Assinatura:**
```python
def _load_config(path: Path) -> dict[str, Any]
```

**Arquivo:** `diagnosticar_fontes_prisma.py`
**Linhas:** 38–45
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Carrega a responsabilidade representada por `config` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `dict[str, Any]`. Retornos observados estaticamente incluem: `cfg`.

#### Efeitos colaterais e fronteiras externas

- Abre arquivo para leitura ou inspeção.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `FileNotFoundError`
- `path.expanduser`
- `path.expanduser().resolve`
- `path.is_file`
- `path.open`
- `str`
- `tomllib.load`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `FileNotFoundError(f'Configuração não encontrada: {path}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `fcad537963f75f26341502c0b42f5c65cac9b58c3dd73e61b6e31b5bf44f0910`
**LOC do arquivo-fonte:** 245


### `diagnosticar_fontes_prisma:_format_status`

**Assinatura:**
```python
def _format_status(status: dict[str, Any] | None) -> str
```

**Arquivo:** `diagnosticar_fontes_prisma.py`
**Linhas:** 48–59
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Formata a responsabilidade representada por `status` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `status` | positional-or-keyword | `dict[str, Any] \| None` | `obrigatório/ausente` | Parâmetro `status` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'status indisponível'`; `str(status.get('status') or 'status não informado') + suffix`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `', '.join`
- `'; '.join`
- `status.get`
- `str`
- `suffixes.append`

#### Chamadores inferidos no escopo auditado

- `diagnosticar_fontes_prisma:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `fcad537963f75f26341502c0b42f5c65cac9b58c3dd73e61b6e31b5bf44f0910`
**LOC do arquivo-fonte:** 245


### `diagnosticar_fontes_prisma:_query_label`

**Assinatura:**
```python
def _query_label(item: dict[str, Any]) -> str
```

**Arquivo:** `diagnosticar_fontes_prisma.py`
**Linhas:** 62–67
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_query_label` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `item` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `item` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `f'{block}: {query}'`; `query or block or 'consulta não identificada'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `item.get`
- `str`
- `str(item.get('bloco_rotulo') or item.get('rotulo') or '').strip`
- `str(item.get('consulta') or item.get('query') or '').strip`

#### Chamadores inferidos no escopo auditado

- `diagnosticar_fontes_prisma:_print_provider_result`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `fcad537963f75f26341502c0b42f5c65cac9b58c3dd73e61b6e31b5bf44f0910`
**LOC do arquivo-fonte:** 245


### `diagnosticar_fontes_prisma:_print_provider_result`

**Assinatura:**
```python
def _print_provider_result(item: dict[str, Any], *, labels: dict[str, str]) -> int
```

**Arquivo:** `diagnosticar_fontes_prisma.py`
**Linhas:** 70–100
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Exibe a responsabilidade representada por `provider_result` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `item` | positional-or-keyword | `dict[str, Any]` | `obrigatório/ausente` | Parâmetro `item` recebido pela função; não há descrição individual explícita no código. |
| `labels` | keyword-only | `dict[str, str]` | `obrigatório/ausente` | Parâmetro `labels` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `failures`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `_query_label`
- `int`
- `isinstance`
- `item.get`
- `labels.get`
- `print`
- `row.get`
- `str`
- `str(item.get('error') or '').strip`
- `str(item.get('url') or '').strip`
- `str(row.get('error') or '').strip`

#### Chamadores inferidos no escopo auditado

- `diagnosticar_fontes_prisma:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `fcad537963f75f26341502c0b42f5c65cac9b58c3dd73e61b6e31b5bf44f0910`
**LOC do arquivo-fonte:** 245


### `diagnosticar_fontes_prisma:main`

**Assinatura:**
```python
def main() -> int
```

**Arquivo:** `diagnosticar_fontes_prisma.py`
**Linhas:** 103–241
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa o ponto de entrada correspondente e coordena o fluxo associado.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `2`; `0`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `' '.join((str(item) for item in research.get('palavras_chave', []) if str(item))).strip`
- `', '.join`
- `'_'.join`
- `PROVIDER_LABELS.get`
- `_format_status`
- `_load_config`
- `_print_provider_result`
- `_project_root`
- `argparse.ArgumentParser`
- `args.config.expanduser`
- `args.config.expanduser().resolve`
- `args.saida.expanduser`
- `args.saida.expanduser().resolve`
- `artifacts.get`
- `cfg.get`
- `copy.deepcopy`
- `datetime.now`
- `datetime.now(timezone.utc).strftime`
- `int`
- `isinstance`
- `list`
- `max`
- `min`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `--limite`
- `--query`
- `--query não pode ser vazia.`
- `--saida`

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 15
loops: 3
comprehensions: 2
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `fcad537963f75f26341502c0b42f5c65cac9b58c3dd73e61b6e31b5bf44f0910`
**LOC do arquivo-fonte:** 245


### `diagnosticar_fontes_prisma:main.progress`

**Assinatura:**
```python
def progress(message: str) -> None
```

**Arquivo:** `diagnosticar_fontes_prisma.py`
**Linhas:** 210–211
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `progress` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `message` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `message` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `print`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.document_builder:_progress`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_enrich_with_unpaywall`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_pretriage_with_ai`
- `app_bundle.scripts.pipeline.prisma_busca_externa:run_external_prisma_search`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `fcad537963f75f26341502c0b42f5c65cac9b58c3dd73e61b6e31b5bf44f0910`
**LOC do arquivo-fonte:** 245


### `gerar_artigo_final_unificado:now_stamp`

**Assinatura:**
```python
def now_stamp() -> str
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 30–31
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `now_stamp` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `datetime.now().strftime('%Y%m%d_%H%M%S')`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `datetime.now`
- `datetime.now().strftime`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:backup`

**Assinatura:**
```python
def backup(path: Path, tag: str, quiet: bool = False) -> Path | None
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 34–41
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `backup` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `tag` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `tag` recebido pela função; não há descrição individual explícita no código. |
| `quiet` | positional-or-keyword | `bool` | `False` | Parâmetro `quiet` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path | None`. Retornos observados estaticamente incluem: `None`; `bak`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `now_stamp`
- `path.exists`
- `path.with_name`
- `print`
- `shutil.copy2`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:run`

**Assinatura:**
```python
def run(cmd: list[str], cwd: Path | None = None, env: dict[str, str] | None = None, quiet: bool = False) -> tuple[int, str]
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 44–57
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `run` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cmd` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `cmd` recebido pela função; não há descrição individual explícita no código. |
| `cwd` | positional-or-keyword | `Path \| None` | `None` | Parâmetro `cwd` recebido pela função; não há descrição individual explícita no código. |
| `env` | positional-or-keyword | `dict[str, str] \| None` | `None` | Parâmetro `env` recebido pela função; não há descrição individual explícita no código. |
| `quiet` | positional-or-keyword | `bool` | `False` | Parâmetro `quiet` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `tuple[int, str]`. Retornos observados estaticamente incluem: `(p.returncode, out)`.

#### Efeitos colaterais e fronteiras externas

- Executa processo ou ferramenta externa.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `(p.stdout or b'').decode`
- `print`
- `str`
- `subprocess.run`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:call_module_main`

**Assinatura:**
```python
def call_module_main(module_name: str, argv: list[str], quiet: bool = False) -> str
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 60–81
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `call_module_main` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `module_name` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `module_name` recebido pela função; não há descrição individual explícita no código. |
| `argv` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Sequência de argumentos de linha de comando; `None` usa os argumentos do processo atual. |
| `quiet` | positional-or-keyword | `bool` | `False` | Parâmetro `quiet` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `captured`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `RuntimeError`
- `buf.getvalue`
- `contextlib.nullcontext`
- `contextlib.redirect_stderr`
- `contextlib.redirect_stdout`
- `hasattr`
- `importlib.import_module`
- `int`
- `io.StringIO`
- `mod.main`
- `module_name.rsplit`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `RuntimeError(f'O módulo {module_name} não possui função main().')`
- `RuntimeError(f'Falha ao executar {module_name} com código {code}.\n{captured[-4000:]}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:ensure_cfg`

**Assinatura:**
```python
def ensure_cfg(cfg: Path, template_cfg: Path | None, quiet: bool = False) -> None
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 84–94
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Garante a responsabilidade representada por `cfg` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `Path` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `template_cfg` | positional-or-keyword | `Path \| None` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `quiet` | positional-or-keyword | `bool` | `False` | Parâmetro `quiet` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `SystemExit`
- `cfg.exists`
- `cfg.parent.mkdir`
- `print`
- `shutil.copy2`
- `template_cfg.exists`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `SystemExit(f'ERRO: TOML não encontrado: {cfg}\nInforme --template-cfg para criar o TOML a partir de um modelo.')`
- `SystemExit(f'ERRO: template TOML não encontrado: {template_cfg}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:set_toml_value_if_present`

**Assinatura:**
```python
def set_toml_value_if_present(txt: str, key: str, value: str) -> str
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 97–101
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `set_toml_value_if_present` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `txt` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `txt` recebido pela função; não há descrição individual explícita no código. |
| `key` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `key` recebido pela função; não há descrição individual explícita no código. |
| `value` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `txt`; `pat.sub(lambda m: m.group(1) + value, txt)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `m.group`
- `pat.search`
- `pat.sub`
- `re.compile`
- `re.escape`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:prepare_cfg`

**Assinatura:**
```python
def prepare_cfg(cfg: Path, target_n: int, min_palavras: int, quiet: bool = False) -> None
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 104–119
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `prepare_cfg` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cfg` | positional-or-keyword | `Path` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `target_n` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `target_n` recebido pela função; não há descrição individual explícita no código. |
| `min_palavras` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `min_palavras` recebido pela função; não há descrição individual explícita no código. |
| `quiet` | positional-or-keyword | `bool` | `False` | Parâmetro `quiet` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `backup`
- `cfg.read_text`
- `cfg.write_text`
- `print`
- `set_toml_value_if_present`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:parse_bib_keys`

**Assinatura:**
```python
def parse_bib_keys(bib: Path) -> list[str]
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 122–126
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Interpreta a responsabilidade representada por `bib_keys` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `bib` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `bib` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `[]`; `re.findall('@\\w+\\s*\\{\\s*([^,\\s]+)', txt)`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `bib.exists`
- `bib.read_text`
- `re.findall`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:sanitize_bib`

**Assinatura:**
```python
def sanitize_bib(bib: Path, quiet: bool = False) -> None
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 129–146
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Saneia a responsabilidade representada por `bib` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `bib` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `bib` recebido pela função; não há descrição individual explícita no código. |
| `quiet` | positional-or-keyword | `bool` | `False` | Parâmetro `quiet` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `SystemExit`
- `backup`
- `bib.exists`
- `bib.read_text`
- `bib.write_text`
- `pat.sub`
- `print`
- `re.compile`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `SystemExit(f'ERRO: BIB não encontrado: {bib}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:convert_literal_citations`

**Assinatura:**
```python
def convert_literal_citations(txt: str, keys: list[str]) -> str
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 149–168
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Converte a responsabilidade representada por `literal_citations` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `txt` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `txt` recebido pela função; não há descrição individual explícita no código. |
| `keys` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `keys` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `txt`; `re.sub('(?<![A-Za-z0-9])\\[([0-9]+(?:\\s*,\\s*[0-9]+)*)\\](?![A-Za-z0-9])', repl_num, txt)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `re.sub`
- `set`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:convert_literal_citations.repl_key`

**Assinatura:**
```python
def repl_key(m: re.Match[str]) -> str
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 153–155
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `repl_key` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `m` | positional-or-keyword | `re.Match[str]` | `obrigatório/ausente` | Parâmetro `m` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `f'\\parencite{{{key}}}' if key in keyset else m.group(0)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `m.group`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:convert_literal_citations.repl_num`

**Assinatura:**
```python
def repl_num(m: re.Match[str]) -> str
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 157–167
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `repl_num` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `m` | positional-or-keyword | `re.Match[str]` | `obrigatório/ausente` | Parâmetro `m` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `m.group(0)`; `f"\\parencite{{{','.join((keys[n - 1] for n in nums))}}}"`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `','.join`
- `all`
- `int`
- `len`
- `m.group`
- `nums.append`
- `piece.isdigit`
- `piece.strip`
- `raw.split`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:generic_text_fixes`

**Assinatura:**
```python
def generic_text_fixes(txt: str) -> str
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 171–188
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `generic_text_fixes` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `txt` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `txt` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `txt`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `re.sub`
- `txt.replace`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:remove_org_bib_headers`

**Assinatura:**
```python
def remove_org_bib_headers(txt: str) -> str
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 191–197
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Remove a responsabilidade representada por `org_bib_headers` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `txt` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `txt` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `txt`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `re.sub`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:insert_org_bib_headers`

**Assinatura:**
```python
def insert_org_bib_headers(txt: str, bib_name: str) -> str
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 200–212
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `insert_org_bib_headers` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `txt` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `txt` recebido pela função; não há descrição individual explícita no código. |
| `bib_name` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `bib_name` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'\n'.join(lines).rstrip() + '\n'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `'\n'.join(lines).rstrip`
- `enumerate`
- `line.startswith`
- `lines.insert`
- `reversed`
- `txt.splitlines`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:ensure_org_references`

**Assinatura:**
```python
def ensure_org_references(txt: str) -> str
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 215–220
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Garante a responsabilidade representada por `org_references` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `txt` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `txt` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `txt`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `re.sub`
- `txt.rstrip`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:patch_org`

**Assinatura:**
```python
def patch_org(org: Path, bib_name: str, keys: list[str], quiet: bool = False) -> None
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 223–236
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `patch_org` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `org` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `org` recebido pela função; não há descrição individual explícita no código. |
| `bib_name` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `bib_name` recebido pela função; não há descrição individual explícita no código. |
| `keys` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `keys` recebido pela função; não há descrição individual explícita no código. |
| `quiet` | positional-or-keyword | `bool` | `False` | Parâmetro `quiet` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `SystemExit`
- `backup`
- `convert_literal_citations`
- `ensure_org_references`
- `generic_text_fixes`
- `insert_org_bib_headers`
- `org.exists`
- `org.read_text`
- `org.write_text`
- `print`
- `remove_org_bib_headers`
- `txt.replace`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `SystemExit(f'ERRO: ORG não encontrado: {org}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:_lisp_string`

**Assinatura:**
```python
def _lisp_string(value: str) -> str
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 239–240
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_lisp_string` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `json.dumps(value, ensure_ascii=False)`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `json.dumps`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:export_org_to_tex`

**Assinatura:**
```python
def export_org_to_tex(org: Path, out: Path, project_root: Path, quiet: bool = False) -> None
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 243–289
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Exporta a responsabilidade representada por `org_to_tex` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `org` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `org` recebido pela função; não há descrição individual explícita no código. |
| `out` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `out` recebido pela função; não há descrição individual explícita no código. |
| `project_root` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `project_root` recebido pela função; não há descrição individual explícita no código. |
| `quiet` | positional-or-keyword | `bool` | `False` | Parâmetro `quiet` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `Path`
- `SystemExit`
- `_lisp_string`
- `fh.write`
- `org.exists`
- `run`
- `script_path.unlink`
- `shutil.which`
- `str`
- `tempfile.NamedTemporaryFile`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `SystemExit(f'ERRO: ORG não encontrado: {org}')`
- `SystemExit('ERRO: emacs não encontrado no PATH.')`
- `SystemExit(f'ERRO: falha no export Org -> TeX.\n{output[-4000:]}')`

#### Opções CLI mencionadas diretamente no corpo

- `--batch`

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:remove_tex_bib_config`

**Assinatura:**
```python
def remove_tex_bib_config(tex: str) -> str
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 292–299
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Remove a responsabilidade representada por `tex_bib_config` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `tex` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `tex` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `tex`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `re.sub`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:insert_tex_bib_config`

**Assinatura:**
```python
def insert_tex_bib_config(tex: str, bib_name: str) -> str
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 302–306
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `insert_tex_bib_config` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `tex` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `tex` recebido pela função; não há descrição individual explícita no código. |
| `bib_name` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `bib_name` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `tex.replace('\\begin{document}', insert + '\\begin{document}', 1)`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `SystemExit`
- `tex.replace`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `SystemExit('ERRO: não encontrei \\begin{document} no TEX.')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:ensure_tex_references`

**Assinatura:**
```python
def ensure_tex_references(tex: str) -> str
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 309–314
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Garante a responsabilidade representada por `tex_references` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `tex` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `tex` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `tex`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `re.sub`
- `tex.replace`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:patch_tex`

**Assinatura:**
```python
def patch_tex(tex_path: Path, bib_name: str, keys: list[str], quiet: bool = False) -> None
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 317–330
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `patch_tex` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `tex_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `bib_name` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `bib_name` recebido pela função; não há descrição individual explícita no código. |
| `keys` | positional-or-keyword | `list[str]` | `obrigatório/ausente` | Parâmetro `keys` recebido pela função; não há descrição individual explícita no código. |
| `quiet` | positional-or-keyword | `bool` | `False` | Parâmetro `quiet` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `SystemExit`
- `backup`
- `convert_literal_citations`
- `ensure_tex_references`
- `generic_text_fixes`
- `insert_tex_bib_config`
- `print`
- `remove_tex_bib_config`
- `tex.replace`
- `tex_path.exists`
- `tex_path.read_text`
- `tex_path.write_text`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `SystemExit(f'ERRO: TEX não encontrado: {tex_path}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:compile_pdf`

**Assinatura:**
```python
def compile_pdf(out: Path, prefix: str, project_root: Path, quiet: bool = False) -> Path
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 333–357
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Compila a responsabilidade representada por `pdf` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `out` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `out` recebido pela função; não há descrição individual explícita no código. |
| `prefix` | positional-or-keyword | `str` | `obrigatório/ausente` | Prefixo canônico usado para compor nomes de artefatos. |
| `project_root` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `project_root` recebido pela função; não há descrição individual explícita no código. |
| `quiet` | positional-or-keyword | `bool` | `False` | Parâmetro `quiet` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `pdf`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Lê configuração/credenciais do ambiente do processo.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `':'.join`
- `SystemExit`
- `env.get`
- `os.environ.copy`
- `p.exists`
- `p.unlink`
- `pdf.exists`
- `run`
- `str`
- `tex.exists`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `SystemExit(f'ERRO: TEX não encontrado: {tex}')`
- `SystemExit(f"ERRO: falha em {' '.join(cmd)}\n{output[-4000:]}")`
- `SystemExit(f'ERRO: PDF não foi gerado: {pdf}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.tex`
- `.pdf`

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:validate_pdf`

**Assinatura:**
```python
def validate_pdf(pdf: Path, quiet: bool = False) -> None
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 360–385
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Valida a responsabilidade representada por `pdf` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `pdf` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `pdf` recebido pela função; não há descrição individual explícita no código. |
| `quiet` | positional-or-keyword | `bool` | `False` | Parâmetro `quiet` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Retornos observados estaticamente incluem: `None`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `SystemExit`
- `any`
- `enumerate`
- `hits.append`
- `pdf.exists`
- `print`
- `re.search`
- `run`
- `shutil.which`
- `str`
- `txt.splitlines`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `SystemExit(f'ERRO: PDF não encontrado para validação: {pdf}')`
- `SystemExit('ERRO: pdftotext falhou na validação.')`
- `SystemExit('ERRO: validação encontrou problemas no PDF final:\n' + '\n'.join(hits[:40]))`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 7
loops: 1
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:_strip_braces_quotes`

**Assinatura:**
```python
def _strip_braces_quotes(value: str) -> str
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 389–400
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Remove a responsabilidade representada por `braces_quotes` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `value` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `value` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `value`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `len`
- `re.sub`
- `re.sub('\\s+', ' ', value).strip`
- `value.replace`
- `value.replace('{', '').replace`
- `value.strip`
- `value.strip().rstrip`
- `value.strip().rstrip(',').strip`
- `value[1:-1].strip`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:parse_bib_entries_for_docx`

**Assinatura:**
```python
def parse_bib_entries_for_docx(bib: Path) -> dict[str, dict[str, str]]
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 403–433
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Extrai metadados suficientes para materializar citações e referências no DOCX.

**Docstring resumida:** Extrai metadados suficientes para materializar citações e referências no DOCX.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `bib` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `bib` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `dict[str, dict[str, str]]`. Retornos observados estaticamente incluem: `{}`; `entries`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `_strip_braces_quotes`
- `bib.exists`
- `bib.read_text`
- `field_pat.finditer`
- `fields.get`
- `fm.group`
- `fm.group(1).lower`
- `len`
- `m.end`
- `m.group`
- `m.group(2).strip`
- `max`
- `re.compile`
- `re.finditer`
- `re.search`
- `ym.group`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 3
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:_split_authors`

**Assinatura:**
```python
def _split_authors(author_field: str) -> list[str]
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 436–446
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_split_authors` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `author_field` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `author_field` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `list[str]`. Retornos observados estaticamente incluem: `[]`; `[p.strip() for p in parts if p.strip()]`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_strip_braces_quotes`
- `author_field.split`
- `p.strip`
- `re.split`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 3
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:_author_surname`

**Assinatura:**
```python
def _author_surname(author: str) -> str
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 449–461
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_author_surname` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `author` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `author` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `''`; `author.split(',', 1)[0].strip()`; `tokens[0]`; `author.title()`; `tokens[-1]`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_strip_braces_quotes`
- `author.split`
- `author.split(',', 1)[0].strip`
- `author.title`
- `author.upper`
- `len`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:_citation_authors`

**Assinatura:**
```python
def _citation_authors(fields: dict[str, str], textual: bool = False) -> str
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 464–476
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_citation_authors` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `fields` | positional-or-keyword | `dict[str, str]` | `obrigatório/ausente` | Parâmetro `fields` recebido pela função; não há descrição individual explícita no código. |
| `textual` | positional-or-keyword | `bool` | `False` | Parâmetro `textual` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `title.split(':', 1)[0][:60]`; `authors[0]`; `f'{authors[0]} e {authors[1]}' if textual else f'{authors[0]}; {authors[1]}'`; `f'{authors[0]}, {authors[1]} e {authors[2]}' if textual else f'{authors[0]}; {authors[1]}; {authors[2]}'`; `f'{authors[0]} et al.'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_author_surname`
- `_split_authors`
- `fields.get`
- `len`
- `title.split`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 0
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:_cite_text_for_key`

**Assinatura:**
```python
def _cite_text_for_key(key: str, entries: dict[str, dict[str, str]], textual: bool = False) -> str
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 479–483
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_cite_text_for_key` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `key` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `key` recebido pela função; não há descrição individual explícita no código. |
| `entries` | positional-or-keyword | `dict[str, dict[str, str]]` | `obrigatório/ausente` | Coleção de entradas bibliográficas/registros. |
| `textual` | positional-or-keyword | `bool` | `False` | Parâmetro `textual` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `f'{authors} ({year})' if textual else f'{authors}, {year}'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `_citation_authors`
- `entries.get`
- `fields.get`
- `key.strip`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:materialize_citations_for_docx`

**Assinatura:**
```python
def materialize_citations_for_docx(txt: str, entries: dict[str, dict[str, str]]) -> str
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 486–498
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `materialize_citations_for_docx` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `txt` | positional-or-keyword | `str` | `obrigatório/ausente` | Parâmetro `txt` recebido pela função; não há descrição individual explícita no código. |
| `entries` | positional-or-keyword | `dict[str, dict[str, str]]` | `obrigatório/ausente` | Coleção de entradas bibliográficas/registros. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `txt`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `re.sub`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:materialize_citations_for_docx.repl_textcite`

**Assinatura:**
```python
def repl_textcite(m: re.Match[str]) -> str
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 487–489
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `repl_textcite` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `m` | positional-or-keyword | `re.Match[str]` | `obrigatório/ausente` | Parâmetro `m` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'; '.join((_cite_text_for_key(k, entries, textual=True) for k in keys))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'; '.join`
- `_cite_text_for_key`
- `k.strip`
- `m.group`
- `m.group(1).split`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:materialize_citations_for_docx.repl_parencite`

**Assinatura:**
```python
def repl_parencite(m: re.Match[str]) -> str
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 491–493
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `repl_parencite` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `m` | positional-or-keyword | `re.Match[str]` | `obrigatório/ausente` | Parâmetro `m` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `'(' + '; '.join((_cite_text_for_key(k, entries, textual=False) for k in keys)) + ')'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'; '.join`
- `_cite_text_for_key`
- `k.strip`
- `m.group`
- `m.group(1).split`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 2
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:_reference_line`

**Assinatura:**
```python
def _reference_line(fields: dict[str, str]) -> str
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 501–520
**Tipo:** function
**Visibilidade:** interna/privada por convenção
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `_reference_line` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `fields` | positional-or-keyword | `dict[str, str]` | `obrigatório/ausente` | Parâmetro `fields` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `' '.join(parts)`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `_strip_braces_quotes`
- `authors.upper`
- `fields.get`
- `fields.get('ID', 'REFERÊNCIA').upper`
- `fields.get('author', '').replace`
- `parts.append`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:render_references_for_docx`

**Assinatura:**
```python
def render_references_for_docx(entries: dict[str, dict[str, str]]) -> str
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 523–527
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Renderiza a responsabilidade representada por `references_for_docx` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `entries` | positional-or-keyword | `dict[str, dict[str, str]]` | `obrigatório/ausente` | Coleção de entradas bibliográficas/registros. |

#### Retorno

Anotação declarada: `str`. Retornos observados estaticamente incluem: `''`; `'\n\n'.join(lines)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'\n\n'.join`
- `_reference_line`
- `entries.items`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:make_docx_source_org`

**Assinatura:**
```python
def make_docx_source_org(org: Path, bib: Path, out: Path, prefix: str) -> Path
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 530–559
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Cria a responsabilidade representada por `docx_source_org` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `org` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `org` recebido pela função; não há descrição individual explícita no código. |
| `bib` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `bib` recebido pela função; não há descrição individual explícita no código. |
| `out` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `out` recebido pela função; não há descrição individual explícita no código. |
| `prefix` | positional-or-keyword | `str` | `obrigatório/ausente` | Prefixo canônico usado para compor nomes de artefatos. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `tmp`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `cleaned_lines.append`
- `generic_text_fixes`
- `line.strip`
- `materialize_citations_for_docx`
- `org.read_text`
- `parse_bib_entries_for_docx`
- `re.match`
- `re.sub`
- `re.subn`
- `render_references_for_docx`
- `tmp.write_text`
- `txt.replace`
- `txt.replace('\\%', '%').replace`
- `txt.rstrip`
- `txt.splitlines`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.docx_source.org`

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:ensure_reference_docx`

**Assinatura:**
```python
def ensure_reference_docx(reference_docx: Path, quiet: bool = False) -> Path
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 562–648
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Garante a responsabilidade representada por `reference_docx` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `reference_docx` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `reference_docx` recebido pela função; não há descrição individual explícita no código. |
| `quiet` | positional-or-keyword | `bool` | `False` | Parâmetro `quiet` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `reference_docx`.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `Cm`
- `Document`
- `Pt`
- `SystemExit`
- `doc.add_paragraph`
- `doc.save`
- `name.startswith`
- `print`
- `reference_docx.exists`
- `reference_docx.parent.mkdir`
- `set_style_font`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `SystemExit(f'ERRO: python-docx não está disponível para criar o reference DOCX.\nInstale no ambiente do projeto com: pipenv install python-docx\nDetalhe: {exc}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 6
loops: 2
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:ensure_reference_docx.set_style_font`

**Assinatura:**
```python
def set_style_font(style, name: str = 'Times New Roman', size_pt: int = 12, bold: bool | None = None) -> None
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 578–589
**Tipo:** nested-function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa o helper/callback local `set_style_font` no escopo da função externa.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `style` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `style` recebido pela função; não há descrição individual explícita no código. |
| `name` | positional-or-keyword | `str` | `'Times New Roman'` | Parâmetro `name` recebido pela função; não há descrição individual explícita no código. |
| `size_pt` | positional-or-keyword | `int` | `12` | Parâmetro `size_pt` recebido pela função; não há descrição individual explícita no código. |
| `bold` | positional-or-keyword | `bool \| None` | `None` | Parâmetro `bold` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `OxmlElement`
- `Pt`
- `qn`
- `rfonts.set`
- `rpr.append`
- `style._element.get_or_add_rPr`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 1
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:validate_docx`

**Assinatura:**
```python
def validate_docx(docx_path: Path) -> None
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 651–660
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Valida a responsabilidade representada por `docx` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `docx_path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `SystemExit`
- `docx_path.exists`
- `docx_path.stat`
- `set`
- `zf.namelist`
- `zipfile.ZipFile`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `SystemExit(f'ERRO: DOCX não foi gerado corretamente: {docx_path}')`
- `SystemExit(f'ERRO: DOCX inválido, word/document.xml ausente: {docx_path}')`
- `SystemExit(f'ERRO: arquivo DOCX inválido: {docx_path}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:emit_docx_stage`

**Assinatura:**
```python
def emit_docx_stage(art: Path, prefix: str, project_root: Path, reference_docx: Path | None = None, quiet: bool = False) -> Path
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 663–672
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** docstring + AST

**Finalidade:** Gera DOCX canônico por python-docx, usando ORG/document.json/BIB finais.

**Docstring resumida:** Gera DOCX canônico por python-docx, usando ORG/document.json/BIB finais.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `art` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `art` recebido pela função; não há descrição individual explícita no código. |
| `prefix` | positional-or-keyword | `str` | `obrigatório/ausente` | Prefixo canônico usado para compor nomes de artefatos. |
| `project_root` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `project_root` recebido pela função; não há descrição individual explícita no código. |
| `reference_docx` | positional-or-keyword | `Path \| None` | `None` | Parâmetro `reference_docx` recebido pela função; não há descrição individual explícita no código. |
| `quiet` | positional-or-keyword | `bool` | `False` | Parâmetro `quiet` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `render_docx_for_article(art=art, prefix=prefix, cfg=None, output=None, quiet=quiet)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `render_docx_for_article`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:prepare_fulltext_stage`

**Assinatura:**
```python
def prepare_fulltext_stage(art: Path, cfg: Path, target_n: int, min_palavras: int, chars_por_pdf: int, quiet: bool = False) -> None
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 674–681
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `prepare_fulltext_stage` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `art` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `art` recebido pela função; não há descrição individual explícita no código. |
| `cfg` | positional-or-keyword | `Path` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `target_n` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `target_n` recebido pela função; não há descrição individual explícita no código. |
| `min_palavras` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `min_palavras` recebido pela função; não há descrição individual explícita no código. |
| `chars_por_pdf` | positional-or-keyword | `int` | `obrigatório/ausente` | Parâmetro `chars_por_pdf` recebido pela função; não há descrição individual explícita no código. |
| `quiet` | positional-or-keyword | `bool` | `False` | Parâmetro `quiet` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `call_module_main`
- `print`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `--art-dir`
- `--cfg-art`
- `--chars-por-pdf`
- `--min-palavras`
- `--target-n`

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:generate_article_stage`

**Assinatura:**
```python
def generate_article_stage(art: Path, cfg: Path, quiet: bool = False) -> None
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 684–691
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Gera a responsabilidade representada por `article_stage` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `art` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `art` recebido pela função; não há descrição individual explícita no código. |
| `cfg` | positional-or-keyword | `Path` | `obrigatório/ausente` | Configuração efetiva do projeto, normalmente carregada do TOML e já enriquecida por defaults/perfil. |
| `quiet` | positional-or-keyword | `bool` | `False` | Parâmetro `quiet` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `None`. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `call_module_main`
- `print`
- `str`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `--art-dir`
- `--cfg-art`

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:final_fix_stage`

**Assinatura:**
```python
def final_fix_stage(art: Path, prefix: str, project_root: Path, quiet: bool = False) -> Path
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 694–714
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `final_fix_stage` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `art` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `art` recebido pela função; não há descrição individual explícita no código. |
| `prefix` | positional-or-keyword | `str` | `obrigatório/ausente` | Prefixo canônico usado para compor nomes de artefatos. |
| `project_root` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `project_root` recebido pela função; não há descrição individual explícita no código. |
| `quiet` | positional-or-keyword | `bool` | `False` | Parâmetro `quiet` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Anotação declarada: `Path`. Retornos observados estaticamente incluem: `pdf`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `SystemExit`
- `bib.exists`
- `compile_pdf`
- `export_org_to_tex`
- `org.exists`
- `out.exists`
- `parse_bib_keys`
- `patch_org`
- `patch_tex`
- `sanitize_bib`
- `tex.exists`
- `validate_pdf`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `SystemExit(f'ERRO: pasta output não encontrada: {out}')`
- `SystemExit(f'ERRO: ORG não encontrado: {org}')`
- `SystemExit(f'ERRO: BIB não encontrado: {bib}')`
- `SystemExit(f'ERRO: TEX não foi gerado: {tex}')`

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

- `.org`
- `.tex`
- `.bib`

#### Complexidade estrutural observada

```yaml
branches: 4
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_artigo_final_unificado:main`

**Assinatura:**
```python
def main() -> int
```

**Arquivo:** `gerar_artigo_final_unificado.py`
**Linhas:** 717–771
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa o ponto de entrada correspondente e coordena o fluxo associado.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Anotação declarada: `int`. Retornos observados estaticamente incluem: `0`.

#### Efeitos colaterais e fronteiras externas

- Interage com o usuário ou escreve saída em terminal/interface.

#### Chamadas realizadas pelo corpo direto

- `Path`
- `Path(__file__).resolve`
- `Path(args.art_dir).resolve`
- `Path(args.cfg_art).resolve`
- `Path(args.reference_docx).resolve`
- `Path(args.template_cfg).resolve`
- `SystemExit`
- `ap.add_argument`
- `ap.parse_args`
- `argparse.ArgumentParser`
- `art.exists`
- `emit_docx_stage`
- `ensure_cfg`
- `final_fix_stage`
- `generate_article_stage`
- `prepare_cfg`
- `prepare_fulltext_stage`
- `print`
- `str`
- `sys.path.insert`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

- `SystemExit(f'ERRO: diretório do artigo não encontrado: {art}')`

#### Opções CLI mencionadas diretamente no corpo

- `--art-dir`
- `--cfg-art`
- `--chars-por-pdf`
- `--docx-only`
- `--emit-docx`
- `--min-palavras`
- `--prefix`
- `--quiet`
- `--reference-docx`
- `--skip-generate`
- `--skip-prepare`
- `--target-n`
- `--template-cfg`

#### Artefatos/caminhos literais reconhecidos no corpo

- `Modelo DOCX de referência. Se omitido, cria/usa app_bundle/templates/docx/reference_abnt_fgv.docx.`

#### Complexidade estrutural observada

```yaml
branches: 10
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `3998f07129f915c72331f845c3ffe743661404ffbf6b5216a9fa9ff5d10150a9`
**LOC do arquivo-fonte:** 775


### `gerar_log_diagnostico_artigo:run`

**Assinatura:**
```python
def run(cmd, cwd = None, env = None, timeout = 300)
```

**Arquivo:** `gerar_log_diagnostico_artigo.py`
**Linhas:** 13–27
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa a responsabilidade representada por `run` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `cmd` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `cmd` recebido pela função; não há descrição individual explícita no código. |
| `cwd` | positional-or-keyword | `não anotado` | `None` | Parâmetro `cwd` recebido pela função; não há descrição individual explícita no código. |
| `env` | positional-or-keyword | `não anotado` | `None` | Parâmetro `env` recebido pela função; não há descrição individual explícita no código. |
| `timeout` | positional-or-keyword | `não anotado` | `300` | Tempo máximo de espera para a operação, quando aplicável. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `(p.returncode, p.stdout)`; `(999, f'[ERRO AO EXECUTAR] {cmd}\n{type(e).__name__}: {e}\n')`.

#### Efeitos colaterais e fronteiras externas

- Executa processo ou ferramenta externa.

#### Chamadas realizadas pelo corpo direto

- `subprocess.run`
- `type`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 1
```

**SHA-256 do arquivo-fonte:** `17a83fda712aa3ab63957358282f70aa425d53b587dca8bae4defd36c5f4d757`
**LOC do arquivo-fonte:** 271


### `gerar_log_diagnostico_artigo:read`

**Assinatura:**
```python
def read(path: Path, max_chars = None)
```

**Arquivo:** `gerar_log_diagnostico_artigo.py`
**Linhas:** 30–36
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Lê a responsabilidade representada por `read` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `path` | positional-or-keyword | `Path` | `obrigatório/ausente` | Caminho de arquivo ou diretório utilizado pela operação. |
| `max_chars` | positional-or-keyword | `não anotado` | `None` | Parâmetro `max_chars` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `f'[AUSENTE] {path}\n'`; `txt[:max_chars] + f'\n\n[TRUNCADO em {max_chars} caracteres]\n'`; `txt`.

#### Efeitos colaterais e fronteiras externas

- Lê arquivos ou artefatos do filesystem.

#### Chamadas realizadas pelo corpo direto

- `len`
- `path.exists`
- `path.read_text`

#### Chamadores inferidos no escopo auditado

- `app_bundle.scripts.pipeline.bibliography_manager:http_get_json`
- `app_bundle.scripts.pipeline.diagnostics:file_sha256`
- `app_bundle.scripts.pipeline.gerar_artigo_longo_fulltext_secional:openai_chat`
- `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:download_pdf`
- `app_bundle.scripts.pipeline.prisma_baixar_fulltext_artigos:fetch_json`
- `app_bundle.scripts.pipeline.prisma_busca_externa:_request_text`
- `app_bundle.scripts.pipeline.prisma_congelar_artigo:sha256_file`
- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:call_ai_batch`
- `app_bundle.scripts.pipeline.prisma_curadoria_ia_referencias:read_xlsx`
- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:fetch_bytes`
- `app_bundle.scripts.pipeline.prisma_fulltext_garantido:sha256_file`
- `app_bundle.scripts.pipeline.render_docx_canonico:force_ooxml_black_docx`
- `app_bundle.scripts.pipeline.render_docx_canonico:validate_docx`
- `gerar_log_diagnostico_artigo:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `17a83fda712aa3ab63957358282f70aa425d53b587dca8bae4defd36c5f4d757`
**LOC do arquivo-fonte:** 271


### `gerar_log_diagnostico_artigo:section`

**Assinatura:**
```python
def section(log, title)
```

**Arquivo:** `gerar_log_diagnostico_artigo.py`
**Linhas:** 39–42
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `section` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `log` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `log` recebido pela função; não há descrição individual explícita no código. |
| `title` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `title` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `log.append`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `17a83fda712aa3ab63957358282f70aa425d53b587dca8bae4defd36c5f4d757`
**LOC do arquivo-fonte:** 271


### `gerar_log_diagnostico_artigo:grep_lines`

**Assinatura:**
```python
def grep_lines(txt, pattern, flags = 0, context = 0)
```

**Arquivo:** `gerar_log_diagnostico_artigo.py`
**Linhas:** 45–56
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `grep_lines` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `txt` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `txt` recebido pela função; não há descrição individual explícita no código. |
| `pattern` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `pattern` recebido pela função; não há descrição individual explícita no código. |
| `flags` | positional-or-keyword | `não anotado` | `0` | Parâmetro `flags` recebido pela função; não há descrição individual explícita no código. |
| `context` | positional-or-keyword | `não anotado` | `0` | Contexto explícito de dependências utilizado para desacoplar a rotina de imports/globais. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `'\n'.join(out) if out else '[sem ocorrências]\n'`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `enumerate`
- `len`
- `max`
- `min`
- `out.append`
- `range`
- `re.compile`
- `rx.search`
- `txt.splitlines`

#### Chamadores inferidos no escopo auditado

- `gerar_log_diagnostico_artigo:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 2
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `17a83fda712aa3ab63957358282f70aa425d53b587dca8bae4defd36c5f4d757`
**LOC do arquivo-fonte:** 271


### `gerar_log_diagnostico_artigo:tex_window`

**Assinatura:**
```python
def tex_window(tex_txt, center, radius = 20)
```

**Arquivo:** `gerar_log_diagnostico_artigo.py`
**Linhas:** 59–63
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Implementa a rotina `tex_window` conforme as regras e chamadas presentes no corpo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `tex_txt` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `tex_txt` recebido pela função; não há descrição individual explícita no código. |
| `center` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `center` recebido pela função; não há descrição individual explícita no código. |
| `radius` | positional-or-keyword | `não anotado` | `20` | Parâmetro `radius` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `'\n'.join((f'{i:05d}: {lines[i - 1]}' for i in range(start, end + 1)))`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `'\n'.join`
- `len`
- `max`
- `min`
- `range`
- `tex_txt.splitlines`

#### Chamadores inferidos no escopo auditado

- `gerar_log_diagnostico_artigo:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 1
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `17a83fda712aa3ab63957358282f70aa425d53b587dca8bae4defd36c5f4d757`
**LOC do arquivo-fonte:** 271


### `gerar_log_diagnostico_artigo:parse_bib_keys`

**Assinatura:**
```python
def parse_bib_keys(bib_txt)
```

**Arquivo:** `gerar_log_diagnostico_artigo.py`
**Linhas:** 66–67
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Interpreta a responsabilidade representada por `bib_keys` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `bib_txt` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `bib_txt` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `re.findall('(?m)^@\\w+\\s*\\{\\s*([^,\\s]+)', bib_txt)`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `re.findall`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 0
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `17a83fda712aa3ab63957358282f70aa425d53b587dca8bae4defd36c5f4d757`
**LOC do arquivo-fonte:** 271


### `gerar_log_diagnostico_artigo:parse_tex_cite_keys`

**Assinatura:**
```python
def parse_tex_cite_keys(tex_txt)
```

**Arquivo:** `gerar_log_diagnostico_artigo.py`
**Linhas:** 70–77
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Interpreta a responsabilidade representada por `tex_cite_keys` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `tex_txt` | positional-or-keyword | `não anotado` | `obrigatório/ausente` | Parâmetro `tex_txt` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Retornos observados estaticamente incluem: `keys`.

#### Efeitos colaterais e fronteiras externas

- Nenhum efeito externo evidente foi identificado pela análise estática do corpo direto.

#### Chamadas realizadas pelo corpo direto

- `k.strip`
- `keys.append`
- `m.group`
- `m.group(1).split`
- `re.finditer`

#### Chamadores inferidos no escopo auditado

- `gerar_log_diagnostico_artigo:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 2
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `17a83fda712aa3ab63957358282f70aa425d53b587dca8bae4defd36c5f4d757`
**LOC do arquivo-fonte:** 271


### `gerar_log_diagnostico_artigo:copy_if_exists`

**Assinatura:**
```python
def copy_if_exists(src: Path, dst_dir: Path)
```

**Arquivo:** `gerar_log_diagnostico_artigo.py`
**Linhas:** 80–82
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Copia a responsabilidade representada por `if_exists` conforme a implementação deste módulo.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

| Parâmetro | Tipo/kind | Anotação | Default | Explicação |
| --- | --- | --- | --- | --- |
| `src` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `src` recebido pela função; não há descrição individual explícita no código. |
| `dst_dir` | positional-or-keyword | `Path` | `obrigatório/ausente` | Parâmetro `dst_dir` recebido pela função; não há descrição individual explícita no código. |

#### Retorno

Sem anotação de retorno explícita. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Escreve, cria, remove, move ou substitui artefatos no filesystem.

#### Chamadas realizadas pelo corpo direto

- `shutil.copy2`
- `src.exists`

#### Chamadores inferidos no escopo auditado

- `gerar_log_diagnostico_artigo:main`

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

Nenhum identificado pela análise estática.

#### Artefatos/caminhos literais reconhecidos no corpo

Nenhum identificado pela análise estática.

#### Complexidade estrutural observada

```yaml
branches: 1
loops: 0
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `17a83fda712aa3ab63957358282f70aa425d53b587dca8bae4defd36c5f4d757`
**LOC do arquivo-fonte:** 271


### `gerar_log_diagnostico_artigo:main`

**Assinatura:**
```python
def main()
```

**Arquivo:** `gerar_log_diagnostico_artigo.py`
**Linhas:** 85–267
**Tipo:** function
**Visibilidade:** pública por nome
**Classe proprietária:** `None`não se aplica
**Decoradores:** nenhum
**Base da explicação:** inferência conservadora por nome + AST

**Finalidade:** Executa o ponto de entrada correspondente e coordena o fluxo associado.

**Docstring:** ausente; a descrição acima é conservadora e deriva do nome, assinatura e corpo AST.

#### Parâmetros

A função não declara parâmetros.

#### Retorno

Sem anotação de retorno explícita. Não há `return` explícito identificado no corpo direto.

#### Efeitos colaterais e fronteiras externas

- Abre arquivo em modo de escrita/alteração.
- Escreve, cria, remove, move ou substitui artefatos no filesystem.
- Interage com o usuário ou escreve saída em terminal/interface.
- Lê arquivos ou artefatos do filesystem.
- Lê configuração/credenciais do ambiente do processo.

#### Chamadas realizadas pelo corpo direto

- `' '.join`
- `'\n'.join`
- `Path`
- `Path(args.art_dir).resolve`
- `ap.add_argument`
- `ap.parse_args`
- `argparse.ArgumentParser`
- `bbl.exists`
- `build.glob`
- `build.mkdir`
- `copy_if_exists`
- `datetime.now`
- `datetime.now().isoformat`
- `datetime.now().strftime`
- `diag.mkdir`
- `enumerate`
- `env.get`
- `final_log.write_text`
- `grep_lines`
- `isolated_log.exists`
- `latex_log.exists`
- `len`
- `log.append`
- `os.environ.copy`

#### Chamadores inferidos no escopo auditado

Nenhum identificado pela análise estática.

> Relações de chamada são inferidas estaticamente pelo nome do callee e somente são atribuídas quando o nome da função-alvo é único no inventário. Chamadas dinâmicas, `getattr`, registries e dispatch indireto podem não aparecer aqui.

#### Exceções/erros levantados explicitamente

Nenhum identificado pela análise estática.

#### Opções CLI mencionadas diretamente no corpo

- `--art-dir`
- `--prefix`
- `--version`

#### Artefatos/caminhos literais reconhecidos no corpo

- `.org`
- `.tex`
- `.bib`
- `.pdf`
- `diagnostico_artigo_latex_biber.txt`

#### Complexidade estrutural observada

```yaml
branches: 5
loops: 9
comprehensions: 0
exception_handlers: 0
```

**SHA-256 do arquivo-fonte:** `17a83fda712aa3ab63957358282f70aa425d53b587dca8bae4defd36c5f4d757`
**LOC do arquivo-fonte:** 271


---

## 45. Índice de usabilidade por função pública

As funções marcadas como **pública por nome** não são automaticamente APIs públicas suportadas. A API pública formal continua sendo definida pelo pacote, `__all__`, entrypoints e CLI. Esta seção ajuda o mantenedor a distinguir:

- funções públicas de entrypoint;
- funções de domínio reutilizáveis;
- helpers internos sem `_` que ainda não constituem contrato público;
- métodos/callbacks de interface.

Para uso normal do programa, prefira:

```text
academic-pipeline
python -m academic_pipeline
```

e as opções descritas nas partes de usabilidade deste manual. O dicionário técnico serve principalmente à manutenção, revisão, diagnóstico e evolução do código.

---

## 46. Gate documental integral

```yaml
mppg_manual_source_definition_reference_gate:
  source_scanned_by_ast: true
  parse_errors: 0
  operational_source_modules_documented: 75
  classes_documented: 66
  source_definitions_found: 1506
  individual_definition_cards: 1506
  source_definition_coverage_percent: 100
  undocumented_source_definitions: 0
  unique_qualified_names: 1502
  duplicate_qualified_names: 4
  definitions_in_duplicate_groups: 8
  shadowed_definition_candidates: 4
  static_final_binding_candidates: 1502
  duplicate_cards_disambiguated_by_source_line: true
  historical_files_separated_from_source_scope: true
  function_signatures_recorded: true
  parameters_recorded: true
  return_contract_recorded: true
  explicit_raises_recorded: true
  calls_recorded: true
  caller_inference_labeled_as_static: true
  side_effects_recorded: true
  source_sha256_recorded: true
  no_manual_function_allowlist_used: true

mppg_manual_runtime_reachability_gate:
  claimed: false
  reason: "AST inventory documents source definitions but does not by itself prove runtime reachability"
```

Decisões:

```text
MPPG_MANUAL_SOURCE_DEFINITION_REFERENCE_GATE_APPROVED
MPPG_MANUAL_RUNTIME_REACHABILITY_GATE_NOT_CLAIMED
```

---

## 47. Política de atualização automática do manual

Toda alteração funcional futura que acrescente, remova ou modifique símbolos operacionais deve exigir regeneração deste apêndice AST antes do staging documental.

O reconstrutor deve ser novamente executado no HEAD homologado. A publicação deve ser bloqueada se:

```text
definições-fonte encontradas != fichas geradas
AST inválida
arquivo de fonte operacional novo fora do inventário
definição removida ainda documentada como vigente
função nova ausente do dicionário
redefinição no mesmo namespace sem adjudicação
```

---

## 48. Estado documental desta edição

```yaml
manual:
  canonical_filename: MPPG_MANUAL_TECNICO_ACADEMIC_PIPELINE.md
  reconstructed_from_source: true
  per_function_explanations: true
  source_definition_coverage_percent: 100
  source_definitions_documented: 1506
  unique_qualified_names: 1502
  duplicate_qualified_names: 4
  duplicate_definitions_adjudicated: true
  runtime_reachability_coverage: not_assessed
  undocumented_source_definitions: 0
  source_head: 019464a6aefd38acbe346fa1a0d286f68e9e34df
```


---

## 49. Nota de auditoria da reconstrução integral

A primeira execução do reconstrutor identificou 1506 definições AST e produziu
1506 fichas, mas quatro nomes qualificados apareciam duas vezes e geravam
apenas 1502 identificadores únicos. A auditoria pós-geração corrigiu a
semântica documental:

- cobertura de **definições-fonte** permanece `1506/1506 = 100%`;
- os quatro grupos redefinidos foram explicitamente identificados;
- todas as oito fichas participantes desses grupos ganharam identificação inequívoca por linha;
- quatro definições anteriores foram classificadas como candidatas a sombreamento;
- quatro definições posteriores foram classificadas como candidatas ao binding textual final;
- o manual deixou de chamar o inventário AST de prova de alcance em runtime.

Essa correção não altera o código do Academic Pipeline. Ela torna a documentação tecnicamente
mais precisa e auditável.

## Fichamento acadêmico

### Finalidade e roteamento

O Academic Pipeline oferece um fluxo especializado para fichamentos acadêmicos, com integração ao perfil institucional da FGV. A especialização é selecionada pelo preset `fichamento_fgv`, que materializa `document_type = "atividade"` e `content_type = "fichamento"` e utiliza o prompt canônico `app://prompts/document/fichamento.txt`.

O fichamento é uma especialização de conteúdo da atividade acadêmica; não constitui um novo tipo estrutural independente de documento. O `document.json` permanece como autoridade semântica canônica entre a geração de conteúdo e as etapas de renderização para DOCX/PDF.

### Modelo de atividade e contrato de conteúdo

O modelo/orientação da atividade é tratado como autoridade de instrução para a geração do conteúdo, e não como `reference_docx` de renderização. O fluxo de fichamento exige a seguinte ordem semântica final:

1. `REFERÊNCIAS BIBLIOGRÁFICAS`;
2. `SÍNTESE DOS TEXTOS`;
3. `PRINCIPAIS CONCEITOS E ARGUMENTOS`;
4. `ANÁLISE CRÍTICA E REFLEXÕES PESSOAIS`;
5. `CONEXÕES E DIÁLOGOS ENTRE OS TEXTOS`;
6. `APLICAÇÕES EM POLÍTICAS PÚBLICAS E GOVERNO`;
7. `QUESTÕES PARA APROFUNDAMENTO`.

A etapa de construção do documento valida o perfil de fichamento, a presença mínima do corpus e a ordem das sete seções antes de considerar o conteúdo apto à renderização.

### Seleção e aquisição do corpus

O configurador suporta três modos canônicos de entrada para fichamento:

- `documentos_locais`: utiliza documentos locais já fornecidos;
- `corpus_externo`: pesquisa, seleciona e baixa corpus externo;
- `corpus_hibrido`: combina documentos locais com corpus externo.

O valor `busca_externa` permanece reservado à semântica terminal de busca/relatório PRISMA e não deve ser reutilizado como modo de aquisição do corpus do fichamento.

A configuração de fichamento utiliza as seções `[selecao_corpus]` e `[fichamento]`. Os limites mínimo, alvo e máximo do corpus devem ser inteiros positivos e coerentes entre si. O mínimo operacional do fichamento é de três textos válidos.

Somente documentos locais de texto integral efetivamente disponíveis e validados contam para o mínimo do corpus. Registros apenas descobertos, DOI/URL, metadados, resumos sem texto integral, downloads falhos ou documentos inválidos não satisfazem esse gate. A deduplicação permanece baseada no conteúdo e deve ser preservada.

### Orquestração de corpus externo

A aquisição de corpus externo é centralizada em `academic_pipeline/external_corpus_orchestration.py`, separada do gerenciador de corpus e do construtor de documentos. A orquestração reutiliza os componentes de busca, curadoria e aquisição existentes, incluindo as primitivas:

- `run_external_prisma_search()`;
- `load_candidates()`;
- `try_download_candidate()`;
- `discover_local_documents()`.

O runtime padrão resolve o corpus por essa camada antes da construção do documento. A lógica de rede e de download não deve ser deslocada para `corpus_manager` nem para `document_builder`.

Quando houver seleção/curadoria externa sujeita a revisão humana, a geração permanece bloqueada enquanto o gate de revisão não estiver satisfeito.

### Configuração e execução

O preset `fichamento_fgv` gera por padrão `fichamento_config.toml`. Para os modos `corpus_externo` e `corpus_hibrido`, o configurador exige tema e recorte de busca e registra os parâmetros de seleção do corpus. O prompt especializado é resolvido pelo perfil institucional FGV.

A geração de conteúdo deve produzir redação acadêmica coerente, fiel às fontes e compatível com as orientações da atividade. Controles de voz, criticidade e reflexão pessoal pertencem à etapa de geração do conteúdo canônico; não devem ser introduzidos posteriormente por manipulação do DOCX/PDF.

### Preservação de contratos

A funcionalidade de fichamento não restaura entrypoints, módulos ou árvores predecessoras aposentadas. Contratos históricos permanecem como proveniência e não devem ser recriados para compatibilidade. O fluxo atual deve continuar usando nomes canônicos e as autoridades operacionais vigentes.
