# Permanent MPPG Orchestrator

Routine use after this front closes:

```bash
cd /home/gustavodetarso/Documentos/mppg
mppg-orchestrator run
```

The deterministic controller owns repository state and authorization gates.
The OpenAI semantic agent auto-adjudicates blockers only through strict,
allowlisted read-only tools and read-only/ephemeral automatic actions.
